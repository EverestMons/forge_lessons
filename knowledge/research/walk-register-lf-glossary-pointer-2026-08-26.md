# Walk register — `lf-glossary-pointer-2026-08-26` (lessons-forge)

**schema_version:** `0.3`

**Plan:** `lessons-forge/knowledge/decisions/drafts/executable-lf-glossary-pointer.md`
**Tier:** T1 (Small — one doc retirement in lessons-forge; class shop-infra by repo). **Panel: none** (T1 two-walk form; escalate on direction-class).
**Opened:** 2026-08-26

---

## Walk 0 — context pin (REAL, measured 2026-08-26)

1. **Clone origin:** plan 543 (`bellows/knowledge/decisions/Done/executable-543.md`, Done TODAY) — its POST-FOLD form; every 543 walk fold carried from birth as clone artifacts: the toplevel-relative worktree law (543 W1-F2 — and 543's QA measured the dispatch really running in `.bellows-worktrees/543`), the regex `^## ` count (543 W1-F1, the -F-caret trap), the torn-pointer re-entry predicate (543 W1-F3), the completeness guard IN the control flow, the recorded-not-predicted post-counts.
2. **Structural difference from 543, stated:** NO wrap.md edit — 543 already re-pointed 3d shop-wide, so the SC-5 ordering precondition is ALREADY satisfied globally; this plan is the W2 half only (completeness guard + pointer-ize one file). One task pair, not two edits.
3. **Target pins:** `knowledge/glossary.md` **10 lines, exactly 2 `## ` entries** (Gate 1 (routing); DISPOSITION line); root `GLOSSARY.md` `[project: lessons-forge]` census **2**; consumer sweep: `grep -rinF "glossary" --include="*.py"` over the lessons-forge repo → **0 hits** (no code parses the file; measured this authoring).
4. **Design notes (v0 facts — clone-carried + adapted):**
   - **(a)** completeness guard: both `## <term>` bodies must match the central `## <term> [project: lessons-forge]` bodies (trailing-whitespace-normalized) BEFORE the overwrite; mismatch/missing → SystemExit naming terms, NO write. Term extraction strips the tag suffix; `Gate 1 (routing)` contains parentheses — matched as a literal string, never a pattern.
   - **(b)** post-counts recorded by Step 1 in the dev note; QA compares recorded values.
   - **(c)** re-entry branch: entries=2 ∧ RETIRED=0 → run; RETIRED=1 ∧ entries=0 ∧ "Do not add entries here"=1 → commit-check only; anything else (torn pointer) → HALT with all values.
   - **(d)** pre-write assert: exactly 2 entries at write time.
   - **(e)** worktree law: `cd "$(git rev-parse --show-toplevel)"`, all in-repo paths relative; sole absolute path is the read-only root `GLOSSARY.md`.
5. **id prediction:** id_sequence read 544.

⚠️ Walk 0 carries no fold rows. Walks 1–2 appended AFTER the draft exists, from real passes.
