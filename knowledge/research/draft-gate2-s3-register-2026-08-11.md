# Executable: Gate 2 codification — proposal 312 into DRAFTING_CYCLE.md §3 (the walk register is an output, not a scratch buffer)

**Type:** Executable
**Project:** lessons-forge
**Depends on:** **executable-338** (bellows, Done 2026-08-10 — shipped the walk-register schema doc and its standalone validator under `bellows/`, **deliberately NOT wired to a gate**; this amendment must not wire it either), the **v1.8 `gate2-s5-conformance-2026-08-09`** codification = `executable-330` (**the CLONE ORIGIN**: a single-section §-amendment from a Gate-1 `codify` routing, corpus path, no §6 deviation), and ⚠️⚠️ **`halted-executable-334` / `shape-amendment-2026-08-09` — the TRUE NEWEST same-class**, which took the doctrine v1.8 → v2.0. **Measured, not recalled: `git log --oneline -- DRAFTING_CYCLE.md` → `759b171 [334]` newest, `0fb567a [330]` next.** §2.6 mandates diffing against the newest, and until the re-base this plan named 330 as "newest same-class" and never opened 334. ⚠️ **334 is `halted` with its verdict OPEN**, **executable-342** (lessons-forge, Done 2026-08-11 — routed proposal 312 to `accepted|codify`, which is this plan's licence). DRAFTING_CYCLE at **v2.0**.
**Created:** 2026-08-11
**Author:** Planner
**Slug:** `gate2-s3-register-2026-08-11` (stable across any crash-redo re-deposit — the Rule 20 `plan_slug`)
**dispatch_mode:** bellows
**pause_for_verdict:** always
**Priority:** 10
**cycle_tier:** T2
**qa_steps:** [2]
**Test Scope:** targeted — this plan changes **no code**. ⚠️ **Measured at authoring 2026-08-11, with a same-instrument positive control:** `grep -F` over `bellows/scripts/plan_lint.py` and `bellows/gates.py` for `scratchpad` / `walk register` / `walk_register` returns **zero hits**, while the identical invocation over the identical files finds `Drafting Cycle` **11 times**. **No `plan_lint` behaviour reads what this amendment changes, so §6's coordinate-doctrine-and-gate requirement is satisfied by stating it — no gate edit, no test change.** Step 2 runs the bellows suite whole as a regression floor only.

⚠️⚠️ **PLANNER, AT DEPOSIT — re-run `shasum -a 256 DRAFTING_CYCLE.md` and update Task A item 2's literal if the doctrine has moved, then re-verify every fragment pin, the site enumeration and `E1_TAIL`'s offset against the new bytes.** *(This instruction lived inside Task A item 2 until re-based walk 3, where the Destruction lens caught it: an agent reading "re-verify and update it if the doctrine has moved" one sentence after "mismatch → HALT" has been handed permission to defeat the pin. **Two audiences, two places — the agent halts, the Planner re-measures.**)*

⚠️ **ID NOTE — the deposit filename is decided AT DEPOSIT.** `id_sequence` read **343** at authoring (2026-08-11). **Re-read at deposit.** No deposit filename carries the plan id — all are date-slugged.

---

## Why this exists

**§3 says one thing and the shop does another, and the divergence is load-bearing in the direction §3 does not expect.**

§3's live text (read at source, `DRAFTING_CYCLE.md`): *"Full walk-by-walk analysis lives in a scratchpad file (`scratchpad/`, session-local and ephemeral); only the per-lens summary lines appear in the plan's `## Drafting Cycle` block."*

**Practice commits the register.** FORWARD 51 records specimen 1's register committed at `a7077ca` and read as the primary source by diagnostic 336's successor. Three registers were committed on a single day. **Proposal 312 (entry 304, routed `accepted|codify` by plan 342) is the corpus form of the same finding.**

⚠️⚠️ **The sharp half is FORWARD 51's, and it is why this is not cosmetic:** §3's own compact-form rule tells an author to *"move the reasoning to the register."* **If the register is ephemeral, that instruction is a DELETION** — it directs detail to a location that does not outlive the reader the rule anticipates. **The doctrine currently mandates a move into a buffer it also declares disposable.**

**What this plan codifies — proposal 312's own words:** *treat the walk register as an output of the cycle, not a scratch buffer — commit it per phase alongside the draft. When a rule directs detail to another location, verify that location outlives the reader the rule anticipates.*

**What this plan does NOT do:**
- ⚠️⚠️ **It does NOT wire `walk_register_lint.py` to any gate.** Plan 338 shipped that validator and **deliberately left it unwired** — "a schema proven on two files has not earned a gate" — and FORWARD 55 item 2 makes a **measured false-positive rate** the precondition. **An agent that adds a gate call, a `plan_lint` check, or a test asserting register conformance has left this plan → HALT.** *(The validator is `walk_register_lint.py` under bellows' scripts directory — named without a path so the lint's path-existence check does not read a prose citation as a declared deposit.)*
- **It does NOT mandate the schema.** The walk-register schema doc exists under `bellows/` and is bellows-owned; this amendment makes the register an **output**, and says nothing about its columns.
- **No `plan_lint` edit, no test change** — see Test Scope's measurement.
- **No LESSONS.md touch. No corpus write.** ⚠️⚠️ **Proposal 312 does NOT flip to `implemented` — this is a PARTIAL codification (four of six sites) and flipping it would record work as done that is two edits short.** It stays `accepted|codify` until the residue lands. *(An earlier draft said the flip "rides Step 2's ledger" — wrong twice: a ledger cannot write a DB row, and on a partial there is nothing to flip.)* **The residue rides a FORWARD row emitted by a plan whose project owns the register.**

## Scope

- **Edits exactly ONE existing file:** `/Users/marklehn/Developer/GitHub/DRAFTING_CYCLE.md` **(root repo)** — the clone origin's form, verbatim in force.
- ⚠️⚠️ **THE DOCTRINE FILE IS NOT A PROJECT-PREFIXED DEPOSIT AND IS NOT LISTED IN `**Deposits:**`.** It lives at the shop root, outside every project tree, and is declared here in Scope prose as an absolute path. *(This is what the `(o2)` lint WARN on the walk-0 draft was telling me: a bare `DRAFTING_CYCLE.md` in a Deposits block is neither project-prefixed nor absolute. The origin does not list it at all.)*
- ⚠️ **The doctrine commit is made at the ROOT repo**, path-scoped to exactly `DRAFTING_CYCLE.md`, **never `git add -A`**; `git -C` absolute; post-commit name-only assertion. The project-prefixed deposits (dev log, QA, evidence) commit in the `lessons-forge` tree as usual.
- ⚠️⚠️ **RE-BASED 2026-08-11 (CEO decision): FOUR edits ship, TWO are HELD.** The context pin showed lines **38 (E2)** and **125 (E5)** were last written by **plan 334, which is `halted` with an open verdict** — editing them would build on work the CEO may revert. **E2 and E5 are deferred, not dropped**; E1, E3, E4 and E6 all sit on text from CLOSED plans (287, 328, 298, 307 respectively).
- ⚠️⚠️ **THIS SHIPS A KNOWINGLY PARTIAL CODIFICATION, and the re-based walk MEASURED the cost more precisely than the decision that authorised it.** The split was taken on the framing *"§2:38 and §3:125 still say scratchpad"* — which reads as a distant, cross-section staleness. **It is not.** Measured: **E1 is line 123 and the held E5 is line 125, two lines apart, separated by a single blank line, BOTH INSIDE §3.** After this plan a reader of §3 sees, in adjacent paragraphs:
  - **123:** *"the walk register — an **output** of the cycle, **not a scratch buffer** … committed per phase"*
  - **125:** *"The per-finding detail stays in the **scratchpad** register"*
  **That is a direct self-contradiction inside one section, not an inconsistency across sections.** ⚠️ **Stated because the CEO's decision was taken on the weaker framing.** The alternative remains worse — holding E1 too would leave the plan codifying none of proposal 312's primary content, and editing line 125 builds on halted plan 334's open verdict. **The residue is therefore not merely owed; it is owed URGENTLY, and the History row says so.** **The History row records this as PARTIAL and names the residue; proposal 312 does NOT flip to `implemented`.**
- ⚠️⚠️ **ONE MECHANISM ONLY: ALL FOUR EDITS REPLACE A SPAN INSIDE AN EXISTING LINE. NONE INSERTS A LINE, NONE REPLACES A WHOLE LINE, NONE DELETES ONE.** *(The newest same-class plan declares its two mechanisms explicitly — "E1, E5 and E7 REPLACE one physical line with one physical line; E2, E3, E4 and E8 INSERT a new line after a located line" — and warns that confusing them corrupts the file. This plan has one mechanism and says so.)* **EVERY EDIT IS A MID-LINE SUBSTRING SUBSTITUTION. NOT ONE IS A WHOLE LINE.** Measured by the context pin: E1 line 123 (704 chars, col 88), E3 line 86 (295, col 149), E4 line 112 (1298, col 1115), E6 line 102 (506, col 26). ⚠️ **An earlier draft called these "one physical line" edits and said "copy it byte for byte" — a whole-line read of E1 alone destroys 438 characters of §3 carrying two other shipped rules.**
- ⚠️⚠️ **SUPERSEDED, five operative sites across four sections — measured 2026-08-11, and the walk-0 draft scoped only ONE of them.** Every occurrence of `scratchpad`/`ephemeral` in the file was enumerated by section: **§2:38, §2.6:86, §2.8:112, §3:123, §3:125 — all five OPERATIVE, ZERO in History rows.** ⚠️ **A SIXTH edit (E6, §2.7) was added at walk 2** — not a `scratchpad` site, but the rule the after-text cites; see the amendment section. Amending §3:123 alone would leave four clauses still calling the register a scratchpad: **a half-applied doctrine change, which is the fold-sweep class landing on the governing document itself.** Plus the **§6 History block** (version bump + dated row). ⚠️ **The `**Version:**` line at the top of the file is a THIRD site and it must move with the History row** — a changelog row claiming v2.1 above a header still reading v2.0 is the record-decay class this shop measures.
- ⚠️ **Numbering is stable for life (§6).** This amendment **rewrites an existing clause**; it adds no sub-question and retires none. **Do not renumber anything.**
- **No push** — commit only.
- ⚠️ **DEPOSIT BASENAMES ARE DECLARED — do not re-date any at run time** (the origin's 320/329 clause, dropped by this clone and restored at walk 3). **Every deposit basename carries `2026-08-11` and it stays**, even if the run date differs. The slug date is an identifier, not a timestamp; re-dating breaks the Scope/Deposits match and every cross-reference in this plan.
- ⚠️⚠️ **VERDICT-WINDOW POSTURE — this ships GOVERNING TEXT, live, before any verdict.** The edit lands in the **main checkout's** `DRAFTING_CYCLE.md`, not a worktree copy, so **from Task B until this plan closes, v2.1 governs every drafting cycle in the shop** — including this plan's own remaining walks and its cold panel. **A Step-2 HALT leaves v2.1 live by design; that is NOT a rollback instruction.** Never `git restore` or `revert` the doctrine on a halt — rollback is a CEO decision. *(The `executable-332` posture clause, which this clone had dropped.)*

### ⚠️ HALT ROUTING — the inputs each step reads

**If any input below is missing or unreadable, HALT the step that needs it and NAME it. Never improvise a substitute.** *(Carried from the clone origin, whose own note records this list form being dropped by three prior plans.)*

| step | input | if missing |
|---|---|---|
| 1 | `/Users/marklehn/Developer/GitHub/DRAFTING_CYCLE.md` at v2.0 with the pinned BEFORE line present exactly once | HALT — the clause moved or another amendment landed; this plan must re-base |
| 1 | this plan's `## The amendment` AFTER text | HALT — **never compose a replacement** |
| 2 | the Step-1 dev log, with both blob hashes and the root sha | HALT — the doctrine edit is unverifiable without them |
| 2 | `/Users/marklehn/Developer/GitHub/RULE_20_SELF_CHECK_BLOCK.md` | HALT — the self-check is mandatory and may not be paraphrased |

### ⚠️ Environment facts — observed, not predicted
1. `grep` here is a **ugrep shim**: `-F` mandatory for every literal; a non-`-F` search **exits 1 silently on a present line**, so exit 1 is never a safe "absent".
2. Shell state does NOT persist between commands — **`cd /abs/path` is the FIRST token of every compound**, and every commit compound asserts `git rev-parse --show-toplevel` before committing.
3. `DRAFTING_CYCLE.md` carries em-dashes, `§` and backticks — **`encoding="utf-8"` explicitly on any read or write**.
4. ⚠️⚠️ **SHELL-HOSTILE PROBES — measured, and E1 is the one that bites.** The **E1 BEFORE fragment contains BOTH an apostrophe** (`the plan's`) **and backticks** (`` `## Drafting Cycle` ``); E3, E4, E6 and `E1_TAIL` contain neither. **Quote every probe with DOUBLE quotes — single-quote concatenation silently drops the apostrophe** and the probe then searches for something that is not there, returning a confident zero. **Backticks inside double quotes are command substitution: pass E1's fragment via a file or a Python literal, never inline in a shell string.** ⚠️ **Prefer matching E1 in Python rather than by shell `grep` at all.** *(The origin records this as "a measured authoring near-miss" and this clone had dropped it — recovered at re-based walk 3.)*

---

## The amendment

**Replace** §3's clause — currently *"Full walk-by-walk analysis lives in a scratchpad file (`scratchpad/`, session-local and ephemeral)"* — with text carrying **all three** of proposal 312's elements:

1. **The register is an OUTPUT of the cycle, not a scratch buffer.**
2. **It is committed per phase, alongside the draft** (which aligns it with §2.7's per-phase commit rule, already doctrine since v1.5).
3. **The general rule 312 states:** *when a rule directs detail to another location, verify that location outlives the reader the rule anticipates.*

⚠️ **The compact-form rule itself does not change.** The plan body still carries structure, not narrative; only the per-lens summary lines still appear in the `## Drafting Cycle` block. **What changes is the destination's durability, not the split.**

⚠️ **Reconcile FORWARD 51 explicitly, per 312's own `suggested_action`.** The amendment text names the divergence it closes so a later reader does not re-open it.

⚠️⚠️ **THE AFTER TEXT IS GIVEN BELOW. THE AGENT PLACES IT AND NEVER COMPOSES IT.** This is the clone origin's single most load-bearing convention and the walk-0 draft had inverted it — it said *"wording is the DEV's to compose."* **For a doctrine file that would land unreviewed prose in the shop's governing document**, authored by an agent, outside the drafting cycle that exists to review exactly this. **The wording below was written by the Planner and is what the walks and the cold panel review.**

⚠️⚠️ **E1 IS A BOUNDED MID-LINE SPAN — 179 characters starting at column 88 of line 123, in a 704-character line. Replace ONLY that span.**
- **KEEP the head**, which ends: `…the plan body carries structure, not narrative. `
- **REPLACE the 179 chars below** — from `Full walk-by-walk` through `\`## Drafting Cycle\` block.`
- ⚠️ **KEEP the 438-character tail**, which begins ` Do not keep a running fold-count in the Cycle Log —` and carries **two other shipped §3 rules** (the no-running-fold-count rule, and proposal 206's gate-matching-string prohibition from v1.3). **Destroying it is the single worst outcome available to this plan.**
- ⚠️⚠️ **CAPTURE THE TAIL BEFORE EDITING — the assertion below has no input otherwise.** In Task A, read line 123 and store the substring from column 267 to end-of-line (**438 chars at authoring**) as `E1_TAIL`, and record it verbatim in the dev log. ⚠️⚠️ **After the edit, LOCATE THE TAIL BY CONTENT, NEVER BY COLUMN — the edit moves it.** Measured: the BEFORE span is **179 chars**, the AFTER span is **573**, so the tail shifts from column 267 to column **661**. **Find `E1_TAIL` by its opening literal (` Do not keep a running fold-count in the Cycle Log —`) and assert the amended line ENDS WITH `E1_TAIL`, byte-identical.** A column-indexed comparison reads 394 characters of the new text and reports a mismatch on a perfectly correct edit. ⚠️ *(The plan mandated "assert the tail is byte-identical to its pre-edit bytes" without ever producing those bytes — a post-condition with no baseline, the same class as a dev log told to record a commit hash that does not yet exist.)*

**BEFORE (the 179-char span, pinned — matches exactly once):**
> Full walk-by-walk analysis lives in a scratchpad file (`scratchpad/`, session-local and ephemeral); only the per-lens summary lines appear in the plan's `## Drafting Cycle` block.

**AFTER (place verbatim):**
> Full walk-by-walk analysis lives in the **walk register — an output of the cycle, not a scratch buffer.** The register is **committed per phase alongside the draft** (§2.7's per-phase commit rule) and outlives the session; only the per-lens summary lines appear in the plan's `## Drafting Cycle` block. ⚠️ **When a rule directs detail to another location, verify that location outlives the reader the rule anticipates** — the compact form above tells an author to move reasoning *to* the register, and under the former ephemeral default that instruction was a **deletion**.

⚠️⚠️ **THE LEADING `> ` IS MARKDOWN QUOTING IN THIS PLAN AND IS NOT PART OF THE TEXT.** Both the BEFORE and AFTER blocks above are rendered as blockquotes so they read as quoted material; **the target line in `DRAFTING_CYCLE.md` is an ordinary paragraph line and must stay one.** Strip the `> ` from the first column before matching or placing. **An instruction to "copy it byte for byte" is exactly what makes this dangerous** — the bytes as presented include a marker that must not travel.
⚠️ **Nothing else in this plan can catch it:** a `> `-prefixed line still *contains* every AFTER fragment, so QA row 1b passes and the sweep passes. *(This also said "Task C's region count passes" — that gate was removed at cold seat 1, which measured the correct run at seven hunks, not eight.)* **QA row 1c below is the only observer.**
⚠️ **The blockquote guard specifically IS an addition beyond the clone origin** — measured: `executable-330` presents its own AFTER text in blockquotes, mandates "the AFTER text is GIVEN", and **states no stripping convention and carries no equivalent check.** It shipped and closed anyway, so **the origin carries that hazard latent and it is worth a FORWARD row.**
⚠️⚠️ **But do NOT generalise from it — walk 4 checked and the origin is RICHER than this clone on the neighbouring guards, not poorer.** `executable-330` carries **C11** (*"every verification asserts the POST-condition per edit kind: replacement → old-absent AND new-present"*) and the **one-physical-line mandate**, both of which this clone had dropped and both of which walk 4 recovered. **Walk 3 was one grep away from recording a recovery as an invention.**

⚠️⚠️ **ONE-PHYSICAL-LINE MANDATE (stated at the producing site, recovered from the clone origin at walk 4).** **E1's replacement paragraph and the §6 History row each land as exactly ONE physical line — no hard wraps, no reflow.** The doctrine's §3 clause is one physical line today, and every line-based instrument in this plan assumes it: the sweep reports hits *with their section*, Task C counts changed *regions*, and QA row 1c reads *each amended line's first character*. **A wrapped-but-otherwise-correct edit breaks all three and false-HALTs a correct run.** Verify each landed as one line before declaring done.

⚠️ **Element map, so a partial placement is visible:** *output not scratch buffer* → sentence 1; *committed per phase alongside the draft* → sentence 2; *the outlives-the-reader rule* → sentence 3. **The compact-form split survives intact in sentence 2's second clause.**

⚠️ **"Alongside the draft" is doing LOCATION work and must not be left implicit** — the amendment removes `scratchpad/` and has to say where the register goes instead. It goes where the draft goes: the project's `knowledge/research/`. That is already practice (specimen 1's register is committed there).

### The SECONDARY edits — THREE live (E3, E4, E6), TWO held (E2, E5)

⚠️ **These are minimal, mechanical substitutions. PLACE them; do not reword the surrounding sentences.** **Each BEFORE fragment must match exactly once in the whole file — assert that before editing; zero or two → HALT.** ⚠️ *(This said "at the named line" while the table carries no line numbers and the note below forbids locating by line at all.)*

| # | section | BEFORE fragment | AFTER fragment |
|---|---|---|---|
| ~~E2~~ | ~~§2:38~~ | — | ⛔ **HELD — line 38 was written by halted plan 334 (open verdict)** |
| E3 | §2.6:86 | `Fold notes live in the scratchpad walk register` | `Fold notes live in the walk register` |
| E4 | §2.8:112 | `kept in the scratchpad walk register` | `kept in the walk register` |
| ~~E5~~ | ~~§3:125~~ | — | ⛔ **HELD — line 125 was INSERTED by halted plan 334 (open verdict)** |
| **E6** | **§2.7:102** | `Commit the draft after every phase` | `Commit the draft and its walk register after every phase` |

⚠️ **E3, E4 and E6 are short unique phrases and are safe as direct substitutions.** Only E1 needs the bounded-span treatment above, because only E1 replaces a long run of text. ⚠️ *(The re-base fold dropped this paragraph BETWEEN E5's row and E6's row, splitting the markdown table and orphaning E6 outside it — an agent reading the table would have seen three rows and no E6. Caught by the re-based walk's lens 1.)*

⚠️⚠️ **E6 EXISTS BECAUSE THE AFTER-TEXT CITES §2.7 AND §2.7 DOES NOT SAY IT.** Read at source 2026-08-11: §2.7's per-phase-commit rule says *"Commit the draft after every phase"* and mentions the register **zero times**. Without E6, §3 would tell a reader the register is committed per phase *per §2.7's rule*, and §2.7 would not carry it — **reproducing, one level down, exactly the doctrine/practice divergence this amendment exists to close.** ⚠️ **A citation that claims more than the cited text says is the class this shop has measured repeatedly; here the fix is to make the citation true.** *(BEFORE fragment verified unique; AFTER fragment has zero pre-existing occurrences.)*

⚠️ **Line numbers are given for orientation only — locate each by its BEFORE fragment, never by line number**, because E1 changes the file's length before these run.

⚠️⚠️ **DO NOT USE A GLOBAL FIND-REPLACE. Measured at authoring 2026-08-11: `scratchpad walk register` occurs 3 times, but `scratchpad` occurs 5 — so the obvious global substitution lands THREE of the five sites and silently leaves E1's `a scratchpad file (\`scratchpad/\`, session-local and ephemeral)` and E5's `scratchpad register` (no "walk") untouched.** The sweep would catch it, but only if the sweep runs. **Apply E1–E5 one at a time, each against its own asserted-unique fragment.**

⚠️ **Verified at authoring: all FIVE AFTER fragments (E2–E6) have ZERO pre-existing occurrences**, so each edit lands somewhere new and a later reader pinning any of them finds exactly one.

---

## STEP 1 — DEV

> **FIRST — post a short visible chat message (1–2 sentences).** Do NOT rename this plan file. Read your specialist file.
>
> **Task A — ESTABLISH STATE (five items: toplevel, file sha, blob+porcelain, the fresh-run gate, earnability).**
> ⚠️⚠️ **EVERY root-repo operation uses `git -C /Users/marklehn/Developer/GitHub` and an ABSOLUTE file path. Shell state does not persist between commands** (environment fact 2), so a `cd` in one invocation does not hold for the next — **a bare `git hash-object DRAFTING_CYCLE.md` run from your lessons-forge worktree hashes the wrong thing or fails, silently either way.**
> 1. `git -C /Users/marklehn/Developer/GitHub rev-parse --show-toplevel` — must print `/Users/marklehn/Developer/GitHub`. Anything else → **HALT.**
> 2. ⚠️⚠️ **FILE-LEVEL PIN — the literal is HERE, not deferred.** `shasum -a 256 /Users/marklehn/Developer/GitHub/DRAFTING_CYCLE.md` — ⚠️ **compare the HASH FIELD ONLY; the command prints `<hash>  <path>` and the path differs by tree** — **must equal `0964e1a70d6752f8656051b865ebcbdf76cb3e35e1b12415e547b54c45e4a7c7`** (measured 2026-08-11). **Mismatch → HALT. That is the whole rule for the executing agent — do not adjust the literal, do not re-derive it, do not proceed.** ⚠️ **Every anchor in this plan — the fragment pins, the site enumeration, the sweep's permitted set, `E1_TAIL`'s offset — was measured against exactly these bytes**, so a different file makes all of them claims about something else, and **updating the pin to match a moved file would silently re-point every one of them at text nobody verified.**
>    *(Until re-based walk 3 this item said "mismatch against the value the Planner declares at deposit" and **the plan declared none** — an unexecutable gate, which cold seat 2 reported and an earlier fold left unfixed. Its rationale also carried three stale counts, all superseded by the re-base. **The sha is what makes the BEFORE line genuinely pinned; the clause probe alone is a 27-character substring, and drift elsewhere on that 704-char line would pass it unseen.**)*
> 3. `git -C /Users/marklehn/Developer/GitHub hash-object DRAFTING_CYCLE.md` — record as `PRE_EDIT_BLOB`. `git -C /Users/marklehn/Developer/GitHub status --porcelain -- DRAFTING_CYCLE.md` must be **EMPTY**.
> 4. ⚠️⚠️ **THE FRESH-RUN GATE — ONE RULE, NO BRANCHES. Every failing input reaches the same place: STOP and report.**
>    Run all three probes and print all three results:
>    - `grep -c -F "gate2-s3-register-2026-08-11" /Users/marklehn/Developer/GitHub/DRAFTING_CYCLE.md` — **must be 0.**
>    - the `**Version:**` line — **must read `2.0`.**
>    - `grep -c -F "session-local and ephemeral" /Users/marklehn/Developer/GitHub/DRAFTING_CYCLE.md` — **must be exactly 1.**
>
>    **All three hold → FRESH RUN → proceed to item 5 (earnability).**
>    **ANY of them otherwise → STOP THE STEP. Do not run Task B. Report all three values verbatim and hand the state to the CEO.**
>
>    ⚠️ **WHAT THIS GATE CANNOT SEE, stated so nobody mistakes it for complete:** a dispatch that died **part-way through Task B** — say E3 placed, E4 and E6 not — leaves the slug at 0, the version at 2.0 and the clause pin at 1, so **all three probes pass and the gate reads FRESH RUN.** ⚠️ **What catches it is Task B item 2's per-fragment uniqueness assertion**, which finds E3's BEFORE fragment already gone and **HALTs on zero.** The gate bounds the *file-level* state; the per-edit assertions bound the *edit-level* state. **Neither is the whole guard, and the partial-Task-B case is the one only the second can see.**
>
>    ⚠️⚠️ **THE BRANCH TREE THIS REPLACES WAS CUT AT RE-BASED WALK 3 (CEO decision), and the evidence is that it produced FIVE separate HIGH findings across four walks** — items 3/4 disagreeing, item 3's own sub-branches disagreeing, item 4 twice, and the slug probe "signalling" a stop to a branch that read *proceed*. **Every one was a branch mis-routing, and every patch was individually correct.** The tree computed a LABEL — *already-landed re-entry* / *partial application* / *foreign amendment* / *moved clause* — **and the plan hands every one of those states to the CEO without acting on it. It was computing a classification nobody consumes.**
>    ⚠️ **What the cut gives up, stated:** the automatic label. **What it buys:** a gate with **no branches, so it cannot mis-route, and nothing to soften, so it cannot relax** — the two failure modes that produced all five findings. **The three raw values still let the CEO diagnose in one read, which is the only place the diagnosis was ever used.**
>
> 5. ⚠️⚠️ **POST-CONDITION EARNABILITY — PROVE EACH ASSERT CAN FAIL, BEFORE EDITING.** Recovered from the true newest same-class (`halted-executable-334` A0(5), which credits the class to `executable-330`). **Run every NEW post-condition's literal against the PRE-EDIT doctrine and confirm it returns the failing value** — **every AFTER fragment on a LIVE row of the edit table must return 0** now (the held rows carry `—`, not a fragment), and `E1_TAIL` must be capturable. *(Measured at authoring: all four live AFTER fragments return 0.)* **Record the zeros in the dev log.** ⚠️⚠️ **This plan needed it more than any other check it carries: it shipped THREE successive sweep post-conditions, each measured on the pre-edit file and each UNEARNABLE — "exactly one hit in the History row", then "exactly two hits", then the four-hit set. An earnability run at Task A would have caught all three before a single lens looked at them.** **Any post-condition that already passes before the edit is not a post-condition → fix it or drop it.**
>
> **Task B — AMEND EXACTLY THE *LIVE* ROWS OF THE EDIT TABLE, plus the `**Version:**` line and the `## History` block. Nothing else.**
> ⚠️⚠️ **THE STRUCK-THROUGH ROWS (E2, E5) ARE NOT SITES — they are a record of what is deliberately NOT being edited.** Applying either one violates the CEO's hold and breaks the sweep's permitted set. ⚠️ *(Walk 2's lens 1 replaced a stale count with a pointer to the table — correctly — and the table contains the held rows, so "amend exactly the sites in the edit table" silently re-admitted the two edits the re-base removed. **Fourth consecutive walk where the Destruction lens caught a relaxation introduced by that walk's own lens 1.**)*
> ⚠️⚠️ **NO COUNT IS STATED HERE, DELIBERATELY, AND THAT IS A FIX.** This heading has carried a wrong number **seven times** across this cycle — "three", then "seven", then "eight", each surviving a fold that updated its siblings, each a number no assertion reads. **The edit table in `## The amendment` is the single source of the live edit set; every count in prose is a copy that goes stale the next time the set moves.** *(§2.8's third resolution: when every patch is individually correct and the class keeps recurring, delete the thing being patched.)*
> 1. **§3's clause — PLACE the GIVEN after-text verbatim. Do NOT compose it.** ⚠️⚠️ **A prior fold fixed this in the amendment section above and left THIS line — the only place an agent acts on — still saying "compose the wording."** The BEFORE line is pinned and the AFTER text is written out in `## The amendment`; **copy it byte for byte.** Any wording difference from the given text, however small, is a **HALT** — this is the shop's governing document, and its prose is what the walks and the cold panel reviewed, not what an agent writes at execution time.
> 2. **Place E3, E4 and E6** — the THREE live secondary edits (**E2 and E5 are HELD**) — each located by its BEFORE fragment, each asserted to match exactly once before editing. ⚠️ **Per the origin's C1: `grep -c -F "<fragment>"` — DOUBLE quotes, per environment fact 4 — must return exactly 1 — if it returns MORE, LENGTHEN the fragment until it is unique rather than picking an occurrence; if it returns ZERO, HALT (the file moved).** Never fall back to a line number.
> 3. **Version line — LENGTHENED ANCHOR, DECLARED DATE, NO REPLACE-ALL.**
>    - ⚠️⚠️ **`2.0 (2026-08-09)` occurs TWICE in the file** (measured: the version line at `:5` and the v2.0 History row at `:190`). **A token-level or global substitution destroys the v2.0 changelog row and mints two "2.1" rows.** Anchor on the **whole line** — `**Version:** 2.0 (2026-08-09). Amended only through the Iteration Protocol (§6).` — asserted to match exactly once, and replace that line alone.
>    - ⚠️ **THE NEW DATE IS DECLARED, NOT COMPUTED: `**Version:** 2.1 (2026-08-11). Amended only through the Iteration Protocol (§6).`** Do not recompute it at run time (the declared-basename rule applies to the version date too). **A stale `(2026-08-09)` on a 2.1 line passes every other check in this plan.**
>    *(All three guards are the clone origin's E2, recovered by cold seat 1.)*
> 4. **The `## History` block** — ⚠️ **not inside §6: it is its own `##` heading at `:189`, after `## When this file changes` at `:185`. PREPEND as the FIRST bullet under `## History`** (ordering confirmed newest-first at authoring: `:190` 2.0, `:191` 1.8, `:192` 1.7). — ⚠️⚠️ **THIS ROW IS COMPOSED, AND IT IS THE ONE EXCEPTION TO "PLACE, NEVER COMPOSE" — the boundary is stated because the plan otherwise says both things.** E1–E6 are **operative rules**: their wording binds future readers and is what the walks and the cold panel reviewed, so it is GIVEN. **A History row is a RECORD of what changed**, it binds nobody, and it must name run-time facts the Planner cannot know at authoring (the actual commit, what the sweep returned). **Compose it — and only it.** *(The clone origin composes its History row the same way.)*
> Prepend a dated `2.1` row naming: the slug `gate2-s3-register-2026-08-11`; **Gate-2 codification of proposal 312** (Gate 1 2026-08-10, routed `codify` by plan 342 — **corpus path, no §6 deviation**); the units amended, **read off the edit table** (§2.6, §2.7 and §3 for the live edits); ⚠️⚠️ **PARTIAL, stated in the row itself: four of six sites codified (§2.6, §2.7, §3:123 clause, plus the §2.7 cadence); §2:38 and §3:125 HELD because halted plan 334 wrote them and its verdict is open. The doctrine is knowingly inconsistent on those two references until the residue lands.** **FORWARD 51 therefore PARTIALLY reconciled, not closed** — ⚠️ **and the row states that §3 is left SELF-CONTRADICTORY at lines 123 vs 125 until the residue lands, so a later reader encountering both does not read it as doctrine drift of unknown origin**; and ⚠️ **§6's coordinate-doctrine-and-gate clause discharged by SAYING: no gate edit — measured, `plan_lint.py` and `gates.py` contain zero `scratchpad`/`walk register` references against a same-instrument positive control of 11 `Drafting Cycle` hits; §4 unchanged and in lockstep.** Name the inheritors: the two remaining Gate-2 batches (`PLANNER_TEMPLATE.md` 10 items, `DRAFTING_CYCLE.md` 12 items) and the §2 rewrite carrying cluster (A).
>
> ⚠️⚠️ **SWEEP — and the post-condition is ZERO, not a classification.** `grep -n -F -e "ephemeral" -e "scratchpad" /Users/marklehn/Developer/GitHub/DRAFTING_CYCLE.md`. ⚠️⚠️ **RE-BASED PERMITTED SET — FOUR hits, each named and located. Anything else is a missed site.**
> | # | where | why it survives |
> |---|---|---|
> | 1 | **§2 line 38** | E2 is **HELD** (halted plan 334's text) |
> | 2 | **§3 line 125** | E5 is **HELD** (halted plan 334's text) |
> | 3 | **§3 line 123**, inside E1's own replacement | its rationale reads *"under the former **ephemeral** default…"* |
> | 4 | **the v2.1 History row** | only if the composed row mentions the old behaviour — **conditional, so do NOT hard-gate on it** |
> **PASS = hits 1, 2 and 3 present and located; hit 4 permitted; ZERO hits anywhere else.**
> ⚠️ **This is ALSO the observer for the HELD edits, and it is the only one.** If an agent "helpfully" applies E2 or E5, hit 1 or hit 2 **disappears** and the sweep fails — so a missing permitted hit is as much a defect as an extra one. **Say which hits were found; do not report only a total.**
> ⚠️ **Line numbers are safe to name here because every edit in this plan is a within-line substitution — E1 replaces a 179-char span inside line 123 without changing the line count, and E3/E4/E6 are phrase swaps. No line moves.** *(Were any edit to insert or delete a line, this permitted set would have to be re-keyed to fragments.)* Report every hit with its line and section.
> ⚠️ *(SUPERSEDED reasoning, kept because it is the measurement that got here: the post-condition was "exactly one hit, inside the History row" — unearnable, since the five-site count was taken before E1's after-text existed. It then became "exactly two", which hard-gated on the conditional History hit. **Three successive post-conditions, none earnable. A post-condition measured on the pre-state is a post-condition about the wrong file.**)*

⚠️⚠️ **SUPERSEDED — the two-hit set: Cold seat 1 measured this by applying all eight edits to a scratch copy and re-running the sweep:** **(i) E1's OWN replacement clause in §3**, whose closing rationale reads *"under the former **ephemeral** default that instruction was a deletion"*, and **(ii) the v2.1 History row**, if the composed row mentions the old behaviour. **Pre-edit measurement: five hits, all operative, zero in History rows.**
> ⚠️ **The earlier post-condition — "exactly one surviving hit, inside the History row" — was UNEARNABLE and would have HALTed a perfectly executed run**, because the five-site measurement was taken against the file *before* E1's after-text existed and was never re-run against the plan's own replacement text. **A post-condition measured on the pre-state is a post-condition about the wrong file.**
> **PASS = exactly the two named hits, each located: one in §3 inside E1's clause, one in the §6 History block. Any hit elsewhere is a missed site → fix before declaring done.** Report every hit with its section and line.
> *(The walk-0 draft asked for an operative-vs-historical classification. That was built on a false premise — there are no historical hits to filter. The measurement replaced the guess.)*
>
> **Task C — PROVE THE POST-CONDITION, both directions.**
> - ⚠️⚠️ **DO NOT GATE ON A REGION OR HUNK COUNT — cold seat 1 measured the correct run at SEVEN hunks, not eight.** E1 (§3:123) and E5 (§3:125) are two lines apart, so at git's default `-U3` they **coalesce into one hunk**. This assertion read "three" until walk 1, "seven" until walk 2, "eight" until the panel — **three successive values, each of which would have failed a correct run, and the plan defines no unit for "region" anyway.**
> - **`git diff` the file and REPORT the hunk list verbatim as evidence, gating on nothing.** ⚠️⚠️ **The completeness gate is the per-edit fragment set, read off the LIVE rows of the edit table** — each live BEFORE absent, each live AFTER present, plus the version line and the History row — **which is per-edit and cannot coalesce.**
> - ⚠️⚠️ **THE HELD ROWS INVERT: E2's and E5's BEFORE fragments MUST STILL BE PRESENT.** *(This gate read "six BEFOREs absent" until walk 2 — a count carried across the re-base that would have FAILED a correct run and, worse, pushed an agent to apply the held edits to make it pass. **Eighth instance of the count class in this cycle and the only one with teeth: it did not merely misstate the set, it contradicted the CEO's hold.**)* *(The origin pinned a `--numstat` line-count instead; this plan has no such pin, so the fragment set is it.)*
> - ⚠️⚠️ **THE COMPLETENESS INVARIANT IS THE SWEEP, not the region count.** A region count proves *how many* places changed, never *which*. **Zero surviving `scratchpad`/`ephemeral` hits outside the new History row is what proves E1–E5 landed — ⚠️ **but NOT E6, which carries no `scratchpad` token; QA row 1b's fragment assertion is the only check that sees it**** — and it is the only check that catches a missed site.
> - ⚠️ **Assert `after != before` on the §3 clause AND `PRE_EDIT_BLOB != POST_EDIT_BLOB`** — a check that only greps for the new wording can certify an edit that never landed.
> - ⚠️⚠️ **And assert every BEFORE fragment returns ZERO.** Presence-of-AFTER proves the new text arrived; **absence-of-BEFORE proves the old text LEFT** — and for E6 nothing else can tell a replace from an insert.
> - ⚠️ **Assert the Version line and the History row agree** — both say 2.1, or **HALT**.
>
> ⚠️⚠️ **ORDER: COMMIT THE DOCTRINE BEFORE WRITING THE DEV LOG.** The dev log must record the root-repo sha, and **that sha does not exist until the doctrine commit is made** — a file cannot carry the hash of a commit that has not happened. **So: Task C proves the edit → FINAL ACTION step 1 commits the doctrine at the root → THEN Task D writes the dev log carrying that sha → FINAL ACTION step 2 commits the dev log in the worktree.** *(Plan 342 hit this exact chicken-and-egg at walk 6 with its own two commits; it is a property of any step that commits twice and logs both.)*
>
> **Task D — DEV LOG (written AFTER the doctrine commit).** Deposit `lessons-forge/knowledge/development/gate2-s3-register-dev-log-2026-08-11.md` with the **root-repo doctrine sha**, ⚠️ **a per-edit BEFORE/AFTER pair for ALL FOUR live edits (E1, E3, E4, E6)**, **`E1_TAIL` verbatim**, **the Task-A(6) earnability zeros**,, ⚠️ **the §6 History row's full text verbatim** (it is the one COMPOSED region, so it is the one a verdict reader most needs in front of them rather than having to re-open the doctrine), (so a reader verifies each landed without re-opening the doctrine), and: both blob hashes, the pinned pre-edit clause verbatim, the post-edit clause verbatim, the RAW `git diff`, and the sweep classification. **RAW output, never a summary.**
> ⚠️ **END THE DEV LOG WITH `### Ledger Updates` and `#### Prompt Feedback`** (the origin's both-steps form, dropped by this clone). **The daemon parses these PER STEP** (`bellows.py:1282-1287`) — a Step-1 prompt-feedback or ledger observation with no section never reaches the ledger. **An Output Receipt is required**; `gates.py:381` reads deposit paths from it. ⚠️ **No `#### Forward Register` in either step** — see Scope.
>
> **FINAL ACTION — TWO COMMITS IN TWO REPOSITORIES. ⚠️⚠️ The Scope files are NOT in the same repo and one commit cannot hold both.** `DRAFTING_CYCLE.md` is in the **root repo**; the dev log is inside the **`lessons-forge` submodule**, a separate git repository the root sees only as a gitlink. **You run in a `lessons-forge` worktree, so the root repo is not your cwd.**
> 1. **Doctrine commit, ROOT repo:** `git -C /Users/marklehn/Developer/GitHub add DRAFTING_CYCLE.md`, then commit with the pathspec naming exactly it — **never `git add -A`** — then assert `git -C /Users/marklehn/Developer/GitHub show --name-only --format= HEAD` prints exactly `DRAFTING_CYCLE.md`. **Record this sha in the dev log.**
> 2. **Dev-log commit, WORKTREE:** `ROOT=$(git rev-parse --show-toplevel)`; assert `pwd -P` equals `$ROOT` and that `$ROOT` is a `lessons-forge` checkout; commit the dev log with a pathspec naming exactly it; assert `git show --name-only --format= HEAD` prints exactly it.
> **Commit only — NO push, in either repo.**
> ⚠️ **The root-repo commit is INVISIBLE to the daemon's `files_changed`**, computed from the worktree diff (`bellows.py:990 _parse_diff_stat`). ⚠️ *Verified at walk 3 because the origin describes this differently, calling `_gate_scope_check` "cwd-scoped": read at source, `_gate_scope_check(plan_text, step_number, files_changed, failures)` takes `files_changed` as an argument and can only see what `_parse_diff_stat` put there. Both point at the same blindness; the `_parse_diff_stat` citation names where it originates.* **That is expected, and is why `DRAFTING_CYCLE.md` is absent from Deposits** — the doctrine edit's evidence is the dev log's RAW diff plus the recorded root sha, not a gate check. **Do NOT "fix" the gate's blindness by copying the doctrine file into the worktree.**
>
> **Scope (worktree — what the daemon sees):**
> - `lessons-forge/knowledge/development/gate2-s3-register-dev-log-2026-08-11.md`
>
> **Scope (root repo — outside the daemon's view, asserted by the step itself):**
> - `/Users/marklehn/Developer/GitHub/DRAFTING_CYCLE.md`
>
> **STOP. Do NOT proceed to Step 2. Wait for CEO verdict.**

**Deposits:**
- `lessons-forge/knowledge/development/gate2-s3-register-dev-log-2026-08-11.md`

⚠️ **`DRAFTING_CYCLE.md` is deliberately ABSENT from this Deposits block** — it lives at the shop root, outside every project tree, so a bare entry is neither project-prefixed nor absolute and the origin does not list it either. **The doctrine edit's evidence is the dev log's RAW diff plus the root-repo commit assertion**, not a Deposits line.

---

## STEP 2 — QA

> **Task Q0 — RE-PIN.** `cd /Users/marklehn/Developer/GitHub`; assert `rev-parse --show-toplevel`; `git log -1 --oneline -- DRAFTING_CYCLE.md` is Step 1's commit; `git status --porcelain --` the two Scope paths is EMPTY; `git hash-object DRAFTING_CYCLE.md` equals Step 1's `POST_EDIT_BLOB`.
>
> **MANDATORY — Rule 20 self-check (canonical block, the exact template, NOT a paraphrase)** from `/Users/marklehn/Developer/GitHub/RULE_20_SELF_CHECK_BLOCK.md`. **All FOUR placeholders:** `plan_slug`: `gate2-s3-register-2026-08-11`; `qa_report_path`: `<abs>/lessons-forge/knowledge/qa/gate2-s3-register-qa-2026-08-11.md`; `evidence_dir` derived from `pwd`; `required_evidence_files`: `[suite.txt, amendment-audit.txt, gate-lockstep.txt]`. **Deposit all three BEFORE running the block — it `sys.exit(1)`s on any missing OR ZERO-BYTE file.** Include the literal stdout: banner `Rule 20 — QA Self-Check Results` and `PASSED — SELF-CHECK PASSED`, byte-exact (em-dash U+2014).
>
> ⚠️ **REPORT STRUCTURE — immediately after the verification table write exactly `## Evidence and Narrative`**; the gate scopes its search to a heading containing "verification".
>
> **Evidence rule:** RAW command output, never a summary.
>
> **Verification table (HALT on any FAIL):**
>
> **1. THE THREE ELEMENTS LANDED.** The §3 clause carries all three of proposal 312's elements — register-as-output, per-phase commit alongside the draft, and the outlives-the-reader rule. **Quote the clause verbatim and map each element to its text.** ⚠️ **A clause carrying two of three is a partial codification and is a FAIL**, not a pass with a note. → `amendment-audit.txt`
> **1b. ⚠️⚠️ E2–E5 LANDED WITH THE RIGHT TEXT — the sweep cannot tell you this.** Row 4 proves `scratchpad` is GONE; it does **not** prove the phrase was replaced correctly. **An agent that deleted the words outright, or garbled the sentence, passes row 4.** Assert each AFTER fragment is present **exactly once**: `lives in the walk register`, `Fold notes live in the walk register`, `kept in the walk register`, `The per-finding detail stays in the walk register`, `Commit the draft and its walk register after every phase`. **Five assertions, five fragments, verbatim.** ⚠️ **E6 is NOT covered by the sweep** — it contains no `scratchpad` token, so this row is the ONLY thing that can see it. *(All four were measured to have ZERO pre-existing occurrences at authoring, so a hit here is this plan's edit and nothing else.)* → `amendment-audit.txt`
>
> **1d. ⚠️⚠️ EACH AMENDED REGION IS ONE PHYSICAL LINE — the observer for lens 4's mandate, which arrived without one.** Assert that E1's replacement clause and the §6 History row are each **exactly one line** in the live file (locate by AFTER fragment, confirm the fragment's line carries the whole clause and the next line begins a different construct). ⚠️ **Nothing else sees a wrap:** row 1b greps a fragment that survives being split across lines, row 1c reads only a first character, and Task C counts regions — a wrapped edit is correct to all three while breaking every line-based instrument in this plan. *(The origin pinned this with a `git diff --numstat` count and an awk bullet count; this plan carries neither, so this row is it.)* → `amendment-audit.txt`
>
> **1e. ⚠️⚠️ THE §3 TAIL SURVIVED — 438 bytes carrying TWO OTHER SHIPPED RULES, and until re-based walk 3 nothing in QA looked at it.** Materialize the pre-edit line 123 from `PRE_EDIT_BLOB` (`git cat-file -p <blob>`), extract the substring from column 267 to end-of-line, and assert **the live line 123 ENDS WITH those exact bytes**. ⚠️ **Locate by content, never by column — the edit moves the tail from column 267 to 661** (span 179 → 573 chars). ⚠️⚠️ **This is the plan's highest-value guard and it was Step-1-only: the step that MAKES the edit was the only party checking it survived.** ⚠️ **And that is a HALF-CARRIED origin guard, not an omission of my own invention:** `executable-330` puts its E1 post-condition (C11) in **Step 2**, at its QA row 2 — **this clone carried C11's Task-C half and dropped its QA half.** The bytes carry §3's no-running-fold-count rule and proposal 206's gate-matching-string prohibition (v1.3) — **destroying them is the worst outcome available to this plan, and it would pass every other QA row**, since the fragment assertions, the sweep and the blockquote check all read only what the edit added. → `amendment-audit.txt`
>
> **1c. ⚠️ NO STRAY BLOCKQUOTE MARKER.** The plan renders the BEFORE/AFTER text as markdown blockquotes; the doctrine's line is a plain paragraph. **Assert that none of the EIGHT amended lines begins with `>` or `> `** — E1–E6, the version line, and the History bullet. ⚠️ *(This said "six" until the panel, leaving the version line and the History row outside the check.)* — read each amended line's first character from the live file. ⚠️ **This is the ONLY check that sees it:** a `> `-prefixed line still contains every AFTER fragment, so row 1b, the sweep and the region count all pass while the doctrine renders as a quote. → `amendment-audit.txt`
>
> **2. THE COMPACT-FORM SPLIT SURVIVED.** §3 still says the plan body carries structure not narrative, and still directs only per-lens summary lines to the `## Drafting Cycle` block. ⚠️ **This amendment changes the destination's durability, not the split** — a rewrite that collapsed them is a scope breach. → `amendment-audit.txt`
> **3. VERSION AND CHANGELOG AGREE.** `**Version:**` reads 2.1 **and** the newest History row is a 2.1 row naming **§2, §2.6, §2.7, §2.8 and §3** (all five amended sections), proposal 312, the slug, and the no-gate-edit statement. **Both, compared to each other.** → `amendment-audit.txt`
> **4. NO `SCRATCHPAD`/`EPHEMERAL` SURVIVES OUTSIDE THE NEW HISTORY ROW.** Re-run the sweep independently of the dev log. **Measured at authoring: five hits, ALL operative, ZERO in History rows** — so the pass condition is **exactly one surviving hit, inside the v2.1 History row this plan adds**, and every other hit is a missed site. ⚠️ *(This row asked for an operative-vs-historical classification until walk 2 — the premise walk 1 measured away and updated in Task B while leaving it here.)* → `amendment-audit.txt`
> **5. ⚠️⚠️ NO GATE WAS WIRED — the deletion-shaped check.** `grep -F` over `bellows/scripts/plan_lint.py`, `bellows/gates.py` and `bellows/tests/test_plan_lint.py` for `scratchpad` / `walk register` / `walk_register` / `walk_register_lint` returns **zero**, **paired with a same-instrument positive control** (`Drafting Cycle` in `plan_lint.py`, expected 11 at authoring — report what it returns). ⚠️ **A bare zero is not evidence; the control is what makes it one.** And `git log --name-only` over this plan's commits shows **no bellows file touched at all.** → `gate-lockstep.txt`
> **6. REGRESSION FLOOR — THIS PROJECT'S SUITE, WITH ITS PATH.** `python3 -m pytest src/test_lessons_forge.py -q` from `$ROOT`; record the raw summary line VERBATIM. **Measured at authoring 2026-08-11: `55 passed`.** ⚠️ **This said "the bellows suite" until the panel — a different repository, which this plan touches zero files in, so it could not detect anything this plan can break, and no path or command was given.** This plan changes no code, so the expected result is **no failures**; a count change is reported and named, not asserted against a literal. → `suite.txt`
> **7. NOTHING ELSE MOVED — named paths, not a clean-tree assertion.** ⚠️⚠️ **Do NOT assert `git status --porcelain` at the shop root is EMPTY: it is not, and cannot be. Measured 2026-08-11 — the root carries a modified `lessons-forge` submodule pointer as a matter of course, and THIS PLAN's own worktree commit is one of the things that moves it.** A clean-tree assertion here fails a correct run. **Instead: assert the root's `DRAFTING_CYCLE.md` is CLEAN (`git -C <root> status --porcelain -- DRAFTING_CYCLE.md` empty, i.e. the edit is committed), and assert by name that `PLANNER_TEMPLATE.md`, `RULE_20_SELF_CHECK_BLOCK.md`, `LESSONS.md` and `lessons-forge.db` are absent from this plan's commits.
>
> **Then `## Evidence and Narrative`, then the Output Receipt.**
>
> ⚠️ **`### Ledger Updates`** — author via `Write`/`Edit` (the daemon parses assistant text and Write/Edit content, NOT Bash), EXACTLY ONCE, complete, never re-edited; `##`-level after `## Evidence and Narrative`; blank line after the last subsection.
>
> ⚠️⚠️ **`#### Forward Register`: ONE row, and only this one.** *FORWARD 51 is RECONCILED by DRAFTING_CYCLE v2.1 (§3 now treats the walk register as a committed per-phase output). Row 49 (drafts carry no fold-granular history) is ADVANCED but NOT closed — a committed per-phase register supplies the pre-fold states row 49 asks for, and whether that is sufficient is row 49's own measurement, not this plan's.*
> ⚠️ **This is a LESSONS-FORGE plan, so this row lands in `lessons-forge/knowledge/FORWARD.md`** — `bellows.py:_append_forward_row` resolves to the plan's OWN project. **Rows 51 and 49 live in the BELLOWS register and are closed there by Rule 42 wrap reconciliation, not by this Receipt.** Say so in the narrative.
>
> **FINAL ACTION — COMMIT YOUR DEPOSITS**, `git add` first, pathspec on the COMMIT naming exactly the Scope files, then assert `git show --name-only --format= HEAD`. **Commit only — NO push.**
>
> **Scope:**
> - `lessons-forge/knowledge/qa/gate2-s3-register-qa-2026-08-11.md`
> - `lessons-forge/knowledge/qa/evidence/gate2-s3-register-2026-08-11/suite.txt`
> - `lessons-forge/knowledge/qa/evidence/gate2-s3-register-2026-08-11/amendment-audit.txt`
> - `lessons-forge/knowledge/qa/evidence/gate2-s3-register-2026-08-11/gate-lockstep.txt`

**Deposits:**
- `lessons-forge/knowledge/qa/gate2-s3-register-qa-2026-08-11.md`
- `lessons-forge/knowledge/qa/evidence/gate2-s3-register-2026-08-11/suite.txt`
- `lessons-forge/knowledge/qa/evidence/gate2-s3-register-2026-08-11/amendment-audit.txt`
- `lessons-forge/knowledge/qa/evidence/gate2-s3-register-2026-08-11/gate-lockstep.txt`

---

## Drafting Cycle

**Tier:** T2 — computed, not judged. **T-6 fires** (governance surface: this edits doctrine). **T-1 does not fire** (one file, two units). **T-2 does not fire** (no data write; proposal 312's status flip rides the ledger). **T-5 does not fire** (a doctrine edit is cleanly revertible by `git`, and the file is TRACKED — unlike the corpus DB). ⚠️⚠️ **T-8 FIRES FOR E1, and the walk-0 "proven clone" framing was wrong about it.** Measured at re-based walk 2: **neither predecessor has ever replaced a SPAN INSIDE a line.** `executable-330` E1 replaces *"that ENTIRE single-line paragraph (the only paragraph of §5)"* — a whole line, bounded by its ending only to identify which paragraph. `halted-executable-334` states its mechanisms outright: *"E1, E5 and E7 **REPLACE one physical line with one physical line**; E2, E3, E4 and E8 INSERT a new line."* **E1 here leaves an 87-char head and a 438-char tail in place. That mechanism has NO precedent in either plan.**
⚠️ **The tier is unchanged — T-6 already sets T2 — but the framing is what licenses inheriting machinery, and it licensed inheriting the wrong machinery.** The "one physical line" language this plan carried, and the whole-line reading that would have destroyed two shipped §3 rules, **came from predecessors whose edits genuinely were whole lines.** Every guard E1 needs — the bounded span, head/tail preservation, the tail-located-by-content assertion — **had to be invented, which is exactly why walk 1's and walk 2's Vulnerabilities passes each found a defect in it.**
**T-8 does NOT fire for E3, E4 and E6** — those are phrase substitutions with direct precedent in both plans.

⚠️ **T2 means the cold-reader panel (§2.6) is owed on top of the five-lens walk, and a proven-clone framing does not waive it** — §2.6 says so outright, and this session has already measured why: plan 342's clone-diff found three defects its origin carried.

**Walks:** ⚠️ **RE-BASED 2026-08-11 — the walk counter RESTARTED.** Walks 1–4 and cold seats 1–2 read a **superseded six-edit shape** whose clone-diff was aimed at the wrong sibling; per entry 294 a restructuring resets the convergence curve, and this was a shape change, not a fold. **Re-based walks: 2 — five lenses each**, strictly sequential. The superseded curve is preserved below as evidence, not as progress toward this artifact's bar.

**RE-BASED WALK 1** — 12 findings. Weak spots 3 (the re-base itself split the edit table, orphaning E6 outside it); Destruction 1 (⚠️ **E1 and the HELD E5 are two lines apart in §3 — a direct self-contradiction inside one section, not the cross-section staleness the split decision was taken on**); Vulnerabilities 2 (the tail assertion had no captured baseline); Integration 3 (⚠️⚠️ **the §2.6-mandated diff against 334 — never run until now — recovered POST-CONDITION EARNABILITY, the slug re-entry key, and the explicit mechanism declaration**); ACID 3 (two items numbered 3, routing four instructions).

**RE-BASED WALK 2** — 10 findings, **10/10 fold-introduced.**
- Weak spots:          4 folded. ⚠️ **The count class reached SEVEN instances, so the fold DELETED the counts rather than correcting them an eighth time** — Task B now points at the edit table as the single source (§2.8's third resolution).
- Destruction:         2 folded, **both HIGH**. ⚠️⚠️ **Pointing at the table re-admitted the HELD rows** — "amend exactly the sites in the edit table" silently restored E2/E5. **FOURTH consecutive walk where Destruction caught a relaxation introduced by that walk's own lens 1.** And Task C's completeness gate still read "six BEFOREs absent" — ⚠️ **which after the re-base would FAIL a correct run and push an agent to apply the held edits to make it pass. Eighth count-class instance and the only one with teeth: it contradicted the CEO's hold.**
- Vulnerabilities:     2 folded, 1 HIGH. ⚠️ **`E1_TAIL` is captured at column 267 but the edit moves the tail to column 661** (span 179 → 573 chars). A column-indexed comparison reads 394 chars of new text and reports a mismatch **on a correct edit**, while the 438 bytes it protects sit past where it stopped looking. Re-anchored to locate by content.
- Integration-record:  1 folded, 1 HIGH. ⚠️⚠️ **T-8 FIRES FOR E1 and the "proven clone" framing was wrong.** Neither predecessor has EVER replaced a span inside a line — 330's E1 replaces *"that ENTIRE single-line paragraph"*, 334 declares *"REPLACE one physical line with one physical line."* **The "one physical line" language and the whole-line reading that would have destroyed two shipped §3 rules were inherited from plans whose edits genuinely were whole lines.** Every guard E1 needs had to be invented — which is why Vulnerabilities found a defect in it on both re-based walks.
- ACID:                1 folded, 1 HIGH. ⚠️ **The slug probe "signalled" a stop-state to a later branch that read `2.0 → proceed`.** **Fifth instance of two classifiers giving one state two answers. The rule being re-learned: a probe that detects a stop-state must ITSELF stop.**

**RE-BASED WALK 3** — opened with a CUT (CEO decision), then five lenses over the result.
- ⛔ **THE CUT:** the re-entry branch tree → a **three-probe fresh-run gate with no branches**. **23 lines → 12.** Evidence: the tree produced **FIVE separate HIGH findings across four walks**, every one a branch mis-routing, every patch individually correct — and it computed a LABEL the plan hands to the CEO without acting on. **Diff-reviewed: 22 lines removed, all tree; every guard it carried verified still present by literal.**
- Weak spots:          3 folded, 1 HIGH. ⚠️ **The file-sha gate was UNEXECUTABLE — "mismatch against the value the Planner declares at deposit", and the plan declared none.** Cold seat 2 reported it and an earlier fold added the instruction without the literal. **Second half-folded seat-2 finding.** Literal now in the plan and verified live. Its rationale also carried three stale counts in one sentence.
- Destruction:         1 folded, 1 HIGH. ⚠️⚠️ **FIFTH consecutive walk where lens 2 caught a relaxation from that walk's own lens 1** — and a NEW variant: **two audiences sharing one paragraph.** The sha item read *"mismatch → HALT"* then *"re-verify and update it if the doctrine has moved"* — the second sentence addressed to the **Planner**, inside a step the **agent** executes, handing it permission to defeat the pin. **Split by audience. LESSONS 2026-08-11.**
- Vulnerabilities:     2 folded, 1 HIGH. ⚠️⚠️ **The 438-byte §3 tail — carrying two other shipped rules — had NO QA observer.** Task C asserted it, and Task C is in the step that makes the edit. **Every other QA row reads only what the edit ADDED; nothing read what it might have removed.** QA row 1e added.
- Integration-record:  2 folded, **2/2 pre-existing.** ⚠️ **Row 1e is a HALF-CARRIED origin guard, not an invention:** 330 puts its E1 post-condition (C11) in **Step 2** — this clone carried C11's Task-C half and dropped its QA half. **Third half-carried guard this cycle.** ⚠️⚠️ **And the origin's shell-hostile-probe rule was dropped: E1's BEFORE fragment carries BOTH an apostrophe and backticks** (E3/E4/E6 and `E1_TAIL` carry neither) — single-quote concatenation silently drops the apostrophe and returns a confident zero. **Match E1 in Python, never by shell `grep`.**
- ACID:                2 folded. The C1 example modelled single quotes — the exact pattern environment fact 4 forbids. And **the fresh-run gate's bound is now stated: a part-way-through-Task-B death passes all three probes**; only the per-fragment uniqueness assertion sees it.

**Re-based walk 3 total: 10 findings, 10 folded — 2 pre-existing, 8 fold-introduced, 4 HIGH.**

⚠️ **RE-BASED CURVE: 12 → 10 → 10 findings; HIGH 5 → 5 → 4.** Flat, not falling. ⚠️⚠️ **And the cut did not stop the lens-1→lens-2 pattern: it has now run FIVE consecutive walks.** The region changed; the pattern did not, which says it is a property of how folds are made rather than of the region they land in.

⚠️⚠️ **THE BAR IS NOT MET.** Four instruction-changing HIGH findings. **Re-based walk 4 is owed.** ⚠️ **But the shape of this walk differs from every previous one: 10 of 10 findings were fold-introduced and NONE came from outside the artifact** — the first walk in this cycle where lens 4 read the context and returned a finding about the plan's own framing rather than a defect it had missed. **That is the convergence signature the superseded curve never reached.**

**SUPERSEDED CURVE (six-edit shape, kept as evidence):** walks 1–4 = 14/14/13/9 findings at 7%/100%/77%/67% fold-introduced, 18 HIGH; cold seats 1–2 = 34 findings, 12 HIGH. **Warm: 62% of findings were the walks' own damage. Cold: 7× the findings and 7× the HIGH per pass, every one pre-existing.**

**SUPERSEDED — five lenses each, strictly sequential, each lens acting on the draft as folded by the previous.
- Weak spots:          w1 3 folded — 3/3 pre-existing, 2 HIGH. ⚠️⚠️ **A walk-0 fold had replaced "the DEV composes the wording" with "the agent PLACES it" — in the amendment section only, leaving Task B, THE ONLY PLACE AN AGENT ACTS, still saying "compose the wording."** Agent prose would have gone into the shop's governing document. Also: **the two Scope files are in DIFFERENT REPOSITORIES** (root repo vs the `lessons-forge` submodule) and one commit cannot hold both; and every root operation needed `git -C <abs>`, since shell state does not persist and a bare `git hash-object` from the worktree hashes the wrong thing silently.
- Destruction:         w1 2 folded — 2/2 pre-existing, 1 HIGH. The dev log had to record a root-repo sha **that does not exist when Task D writes it** — reordered so the doctrine commits first, then the log is written, then the log commits. *(Plan 342 hit this identical chicken-and-egg at walk 6; it is a property of any step that commits twice and logs both.)* Also restored the origin's **HALT-routing input table**, which this clone had dropped.
- Vulnerabilities:     w1 **dry** — the BEFORE pin returns exactly one line and no History row carries it; the two-repo commit adds no degenerate case (a pathspec commit ignores the dirty submodule pointer the worktree commit creates). ⚠️ **The after-text's `§2.7` cross-reference was VERIFIED at source** — a wrong section reference shipped into doctrine is not fixable by a later fold, it needs another Gate-2 cycle.
- Integration-record:  w1 6 folded — 6/6 pre-existing, **the walk's biggest finding.** ⚠️⚠️ **The doctrine calls the register a scratchpad in FIVE OPERATIVE SITES across FOUR SECTIONS — §2:38, §2.6:86, §2.8:112, §3:123, §3:125 — and this plan was scoped to ONE.** Shipping it would have left four clauses contradicting the amendment: **the fold-sweep class landing on the governing document.** Added E2–E5 as pinned mechanical substitutions. ⚠️ **The sweep's premise was also wrong** — it asked for an operative-vs-historical classification, and the measurement shows **ZERO History hits**; the post-condition is now simply zero surviving hits outside the new History row.
- ACID:                w1 3 folded — 2 pre-existing, **1 fold-introduced**, 1 HIGH. ⚠️ **Lens 4's five-site fold updated Scope and Task B and left Task C asserting "exactly three regions changed"** — it is now seven, and the stale assertion would have failed a CORRECT run. **The completeness invariant is the SWEEP, not a region count:** a count proves how many places changed, never which. Also: the zero-hit re-entry state was being reported as a moved clause.

**Walk 1 total: 14 findings, 14 folded — 13 pre-existing (93%), 1 fold-introduced (7%), 5 HIGH.**

⚠️ **Nine of the fourteen came from reading OUTSIDE the draft** — the clone origin (4 at walk 0), and the live doctrine file (5 sites, lens 4). **The draft read on its own terms was largely fine; what it got wrong was everything about its own context.**

**Walk 2** — the new surface was walk 1's own folds: the E2–E5 table, the two-repo FINAL ACTION, the reordered Task D, the HALT-routing table and Task C's region count.
- Weak spots:          w2 4 folded — **4/4 fold-introduced.** ⚠️ **Task B's heading still read "in three coordinated sites" after the five-site fold reached Scope and Task C — the third instance of that sweep class here.** And Task A item 4 **halted on the state item 3 declares legitimate**, describing it as the thing it is not.
- Destruction:         w2 2 folded — **2/2 fold-introduced**, 1 HIGH. ⚠️⚠️ **Lens 1's reclassification had quietly become a RELAXATION:** walk 1 made item 4 a hard `HALT`; lens 1 softened it to "report it as such" while fixing a real misattribution, **and the stop went with it.** Also measured: **a global `scratchpad walk register` replace lands 3 of 5 sites** (3 hits vs 5), silently missing E1 and E5 — global replace now forbidden with the measurement attached.
- Vulnerabilities:     w2 5 folded — **5/5 fold-introduced**, 2 HIGH. ⚠️⚠️ **QA row 7 asserted the shop root's `git status --porcelain` is EMPTY. Measured: 3 entries, and THIS PLAN's own worktree commit moves the submodule pointer that is one of them — a check that fails a correct run.** ⚠️ **And the sweep proves ABSENCE, not correct replacement:** an agent that deleted the phrases outright passes row 4. Added row 1b's per-fragment assertion. Plus the `executable-332` **verdict-window posture** clause this clone had dropped — the edit is live from Task B and governs this plan's own remaining walks.
- Integration-record:  w2 1 folded, **swept across nine sites**, 1 HIGH. ⚠️⚠️ **The after-text cited §2.7 for the register's commit cadence, and §2.7 — read at source — says "Commit the draft" and mentions the register ZERO times.** Shipping it would reproduce, one level down, the exact divergence this amendment closes. **E6 added to make the citation true.**
- ACID:                w2 2 folded — 2/2 fold-introduced, 1 HIGH. ⚠️⚠️ **The re-entry classifier keyed on E1's absence alone and would have blessed a PARTIAL amendment as a completed re-entry** — the state E6 makes most likely, since E6 carries no `scratchpad` token and no sweep can see it. Now: check all six AFTER fragments; any absent → partial-application HALT, naming which.

**Walk 2 total: 14 findings, 14 folded — 0 pre-existing, 14 fold-introduced (100%), 5 HIGH.**

⚠️ **THE ORIGIN SPLIT TURNED: 93% pre-existing → 100% fold-introduced**, with the count flat at 14. §2 predicts the flat count: every fold is an unreviewed edit, so folding N creates a fresh surface of N.

⚠️⚠️ **THE BAR IS NOT MET.** Five instruction-changing HIGH findings — three of which would have failed or falsely passed a correct run (the root-clean assertion, the softened stop, the partial-amendment blessing). **Walk 3 is owed, and the T2 cold panel after the sequential walk meets the bar.**

**Walk 3** — the new surface was walk 2's folds: the re-entry classifier, E6 and its nine count-references, the verdict-window posture and QA rows 1b/7.
- Weak spots:          w3 4 folded — **4/4 fold-introduced.** ⚠️ **Task A item 4 declared `2.1 + slug` a completed re-entry on its own — reopening, ONE ITEM LATER, the partial-amendment hole walk 2's ACID pass had just closed in item 3.** Plus three more E6 count-references the lens-4 fold had missed.
- Destruction:         w3 1 folded — **1/1 fold-introduced**, 1 HIGH. ⚠️⚠️ **Lens 1's own fix removed the STOP along with the duplicate classifier.** Item 3 only branches on ZERO E1-pin hits, so a file at `2.1` with E1 **not applied** returns ONE hit, takes item 3's ordinary *proceed* path, and this item would have followed it into re-applying on top of a partial amendment. **Second time in this plan a reclassification became a relaxation, both on this item, both caught by Destruction one lens later.**
- Vulnerabilities:     w3 3 folded — 3/3 fold-introduced, 1 HIGH. ⚠️⚠️ **The BEFORE/AFTER text is rendered as markdown BLOCKQUOTES and the instruction says "copy it byte for byte" — the bytes include a `> ` marker that must not travel.** And **nothing in the plan could catch it**: a `> `-prefixed line still contains every AFTER fragment, so row 1b, the sweep and the region count all pass while the doctrine renders as a quote. **QA row 1c added as the sole observer.**
- Integration-record:  w3 3 folded — 3/3 pre-existing, 1 HIGH. ⚠️ **The origin's "deposit basenames are DECLARED — do not re-date at run time" clause had been dropped**, and this plan's basenames are all date-slugged. ⚠️ **The blockquote guard is an ADDITION, not a recovery: `executable-330` states no stripping convention and carries no equivalent check — it ships the hazard latent, which is worth a FORWARD row.** The `_parse_diff_stat` citation was verified at source because the origin describes the same blindness as `_gate_scope_check` being "cwd-scoped".
- ACID:                w3 2 folded — 2/2 fold-introduced. ⚠️ **The plan forbids composition (E1–E6 are GIVEN) and then requires it (the History row), with no stated boundary.** Stated: operative rules are placed because their wording binds future readers; a History row is a record that binds nobody and must carry run-time facts. The composed row now goes into the dev log verbatim, because it is the one region a verdict reader cannot check from the plan alone.

**Walk 3 total: 13 findings, 13 folded — 3 pre-existing (23%), 10 fold-introduced (77%), 3 HIGH.**

⚠️ **THE CURVE: 14 (93% pre-existing) → 14 (100% fold-introduced) → 13 (77% fold-introduced), HIGH 5 → 5 → 3.** The three pre-existing findings all came from lens 4 — **the only lens that reads outside the artifact, and the same pattern this session measured on plans 341 and 342.**

⚠️⚠️ **THE BAR IS NOT MET.** Three instruction-changing HIGH findings, including one that would have re-applied an amendment on top of a partial one. **Walk 4 is owed; the T2 cold panel follows the sequential walk meeting the bar.**

**Walk 4** — the new surface was walk 3's folds: the reordered re-entry branches, QA row 1c, the basename clause and the composition boundary.
- Weak spots:          w4 2 folded — **2/2 fold-introduced**, 1 HIGH. ⚠️ **Item 3's version check sat LAST, under the fragment check**, so a genuinely-moved clause was diagnosed as a partial application. Reordered, first-match-wins. Also: the branch said "CHECK ALL SIX" and listed five.
- Destruction:         w4 2 folded — **2/2 fold-introduced**, 1 HIGH. ⚠️⚠️ **THIRD CONSECUTIVE WALK where Destruction caught a relaxation introduced by that walk's own lens 1.** The reorder sent a **partial application of THIS plan** (E1–E4 placed, crash before the version bump) to the re-base branch. **Both orderings are wrong alone: the version says WHETHER something amended the file; the fragments say WHO.**
- Vulnerabilities:     w4 1 folded — 1/1 fold-introduced, 1 HIGH. ⚠️ **E6's BEFORE contains no `scratchpad`, so an INSERT (adding the new sentence without removing the old) passes row 1b, the sweep and the region count alike.** Verified mechanically that E6's BEFORE is not a substring of its AFTER, so both genuinely coexist. **Presence-of-AFTER proves the new text arrived; absence-of-BEFORE proves the old text left.**
- Integration-record:  w4 3 folded — **3/3 pre-existing**, 1 HIGH. ⚠️⚠️ **The origin carries C11 — "replacement → old-absent AND new-present" — and the ONE-PHYSICAL-LINE mandate, both dropped by this clone.** ⚠️ **Walk 3 recorded the blockquote guard as "an addition beyond the origin" and was one grep from generalising that: the origin is RICHER than this clone on the neighbouring guards, not poorer.** Record corrected. Also recovered C1's remedy: a non-unique anchor is LENGTHENED, never resolved by picking an occurrence.
- ACID:                w4 1 folded — 1/1 fold-introduced. ⚠️ **Lens 4's one-physical-line mandate arrived WITHOUT AN OBSERVER** — row 1b's fragment survives a wrap, row 1c reads one character, Task C counts regions. The origin pinned it with `numstat` and an awk count; this plan carried neither. **QA row 1d added.**

**Walk 4 total: 9 findings, 9 folded — 3 pre-existing (33%), 6 fold-introduced (67%), 5 HIGH.**

⚠️ **THE CURVE: 14 (7% fold-introduced) → 14 (100%) → 13 (77%) → 9 (67%), HIGH 5 → 5 → 3 → 5.** The count is falling; the fold-introduced share is NOT climbing, and **every walk's pre-existing findings come from lens 4 alone** — the only lens that reads outside the artifact. **Three walks running, that lens has produced 3, 3 and 3.**

⚠️⚠️ **THE BAR IS NOT MET.** Five instruction-changing HIGH findings. **Walk 5 is owed; the T2 cold panel follows the sequential walk meeting the bar.**

⚠️ **Walk 1's numbers, for the curve: 14 findings, 13 pre-existing (93%), 1 fold-introduced, 5 HIGH.**

**Clone-diff against the newest same-class (v1.8 `gate2-s5-conformance-2026-08-09`), recorded at walk 0:**
- **CARRIED:** the corpus-path framing (Gate 1 routed → Gate 2 codifies, no §6 deviation); the in-section rationale; the §6 coordinate-doctrine-and-gate discharge by explicit statement; the version + dated History row; naming the inheritors.
- **ADAPTED:** §5 → §3; two proposals → one; "rescheduled" → "re-scoped the destination's durability".
- **ADDED (no precedent in v1.8):** the three-site coordination (clause + Version line + History row) with a cross-assertion that the Version line and the changelog agree. ⚠️⚠️ **RETRACTED by cold seat 1: this previously read "v1.8's amendment did not touch the Version line as a separate risk" — FALSE.** The origin's **E2 IS the version line**, named as its own edit, with a lengthened anchor, a measured twice-occurring-token hazard, a declared date and four QA probes. **That false premise is what licensed dropping all three guards.** Walk 4 caught this class on the neighbouring guards and swept only C11 and the one-physical-line mandate; **the version-line half was never re-checked, and the false claim survived to license a live defect.**; and the operative-vs-historical sweep classification, because "ephemeral" appears in History rows that must NOT be edited.
- **DELIBERATELY NOT ADDED:** any wiring of `walk_register_lint.py`. **Plan 338 shipped it unwired on purpose and FORWARD 55 item 2 makes a measured FP rate the precondition** — re-adding it here would be the "clone re-adds machinery a sibling deliberately deleted" class §2.6 names.

**Cold panel (§2.6) — CONVENED BEFORE THE BAR, on CEO direction 2026-08-11. ⚠️ This is a DECLARED §2.6 DEVIATION and it is recorded as one.** §2.6 convenes the panel once the sequential walk meets §2's bar; walk 4 did not meet it (five instruction-changing HIGH). **The reasoning:** walk 4's own close measured *why* the sequential walk was not going to exhaust this plan — the count fell 14/14/13/9 while the fold-introduced share stalled at 67%, because **lens 4 returned three PRE-EXISTING findings on each of the last three walks, all from outside the artifact** (the clone origin, the live doctrine, the daemon source). **That is an under-read context, not a converging artifact, and the panel's evidence-attack and clone-diff briefs aim at exactly that surface.** Precedent: the session-29/320 CEO-authorized panel and the standing autonomous-panel grant; specimen 1's F4′ records that a non-converging artifact cannot otherwise reach the shop's highest-yield instrument. **Seats run SEQUENTIALLY, each reading the draft as folded by the previous — a concurrent run would be a panel pass, not a walk.**

**Conformance (§5):** ⚠️⚠️ **RECORD CORRECTED AT THE PANEL — this read "not yet run", which was false and had been false for four walks.** `plan_lint` has run at the **deposit path resolution** (via a non-claimable preflight name) after **every fold of every lens** — roughly fifteen runs. ⚠️ **Latest, taken during the panel fold: exit 0, ONE warning** — `Drafting Cycle closing indicates fold as last event, not a dry lens pass`, which is **TRUE and EARNED**. The two structural WARNs this plan carried for four walks (`missing lens(es)`, `T2 missing cold-panel line`) **cleared when the walk records and the cold-panel line were written** — they were the gate correctly reporting an unearned state, and they cleared by being earned. **§5 requires the pass before the adversarial passes close — specifically before the cold panel on T2 — and it was run; the RECORD said otherwise.** Cold seat 1 flagged the record and was right to.

**Closing:** ⛔ **NOT REACHED.** Walk 4 closed on a fold with five instruction-changing HIGH findings; the record-class condition fails. ⚠️ **The count is falling (14/14/13/9) but the fold-introduced share is not climbing (100%/77%/67%), because lens 4 keeps returning 3 pre-existing findings per walk from OUTSIDE the artifact.** That is not the noise floor — it is an under-read context. **Walk 5 is owed; the T2 cold panel follows. This artifact is NOT depositable.**
