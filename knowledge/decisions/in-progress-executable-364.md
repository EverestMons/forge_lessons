# Executable: Gate 2 — the cold-panel bundle (327/328/329/332): DRAFTING_CYCLE v2.5 → v2.6 + PANEL_SEAT_TEMPLATE.md v1.0, then flip the four to `implemented` (330 RE-SCOPED at panel seat 1)

**Type:** Executable
**Project:** lessons-forge
**Depends on:** executable-360 + executable-362 (the Gate-1 routing, written and QA-completed), executable-356 (Done — the builder-mechanism lineage; its panel-hardened form is this clone's origin and newest same-class), executable-359/357 (Done — the batch lineage)
**Created:** 2026-08-12
**Author:** Planner
**Slug:** `gate2-coldpanel-2026-08-12`
**dispatch_mode:** bellows
**pause_for_verdict:** always
**Priority:** 5
**cycle_tier:** T2
**qa_steps:** 2
**Test Scope:** targeted (Rule 21 — single test module; QA row 5 re-derives; baseline 55 passed / 0 skipped)

⚠️ **ID NOTE:** id read at deposit (`next_id` **363** at authoring — a PREDICTION; four foreign consumptions this session prove the window; the freeze reads fresh).

## Why
The Gate-2 queue holds the cold-panel five (routed by plan 360, QA-completed by 362); THIS PLAN CODIFIES FOUR: **328** meter-at-convene, **329** the Execution brief (the 324 mapping's missing registry row), **332** the mechanical/judgment split's four panel structures, **327** the seat-prompt template mandate + the NEW ARTIFACT `PANEL_SEAT_TEMPLATE.md` v1.0. **330 (register schema 0.2) is RE-SCOPED, not codified — this plan's own panel seat 1 (evidence-attack) found its premise FALSE: a committed v0.1 schema doc (`bellows/knowledge/architecture/walk-register-schema.md`, plan 338) and a LIVE validator (`bellows/scripts/walk_register_lint.py`) exist, and DC §3's own pointer names the schema doc — shipping 0.2 as DC prose would FORK the schema. 330 stays `accepted|codify`; the queue row is its schedule; its plan targets the schema-owner surfaces.** The sibling **331** (cold-front timing) stands `reference|backlog` with its decision-packet FORWARD row pinned in plan 360 — deliberately NOT codified here. Four doctrine edits via the committed builder, one verbatim template copy with sha assert, one scoped 4-row flip. Corpus path proper, no §6 deviation; real walks + the T2 cold panel — **run under the very discipline this plan codifies: the meter opened at convene, seats built from the template source, pin tables handed as claims-to-attack.**

⚠️ **Sentinel exposure: the four's prior stamp `2026-08-12T17:12:07Z` is Z-form and GLOB-MATCHING** (360's write) — the one-value `NOT IN` exclusion carries the guard; re-measured, not inherited. **Categories MIXED by measurement: 327/332 `instrumentation`, 328/329 `governance_rule`** (a 2/2 split per-id at authoring — never assert uniformity; 330, also instrumentation, is NOT in the flip set).

## Scope
- **One doctrine file** via the COMMITTED builder `governance/knowledge/research/builder-gate2-coldpanel-2026-08-12.py` (all-or-nothing; anchors count-asserted pre-edit, probes count-asserted post-edit — the builder's own assert caught a wrong predicted count at its first dry-run and left the file untouched, the mechanism working; dry-run numstat **`7 1`** (re-measured after the seat-1 re-scope), H2/H3 censuses **9/11 UNCHANGED** (no new headings), 280 → **286** lines).
- **One NEW file by verbatim copy:** `cp governance/knowledge/research/panel-seat-template-v1.md /Users/marklehn/Developer/GitHub/PANEL_SEAT_TEMPLATE.md` — sha asserted BOTH sides == `f8d2626abe6eb0d0a3f8a4a38eb9ed4513f27ef041b97d96c288953ff280ffb4` (39 lines; the committed source is the review surface, the root copy is the artifact).
- **One DB write:** the FOUR `accepted|codify → implemented`, `status_updated_by='ceo'`, in-statement Z-stamp; **330 and 331 are READ, never written.**
- **No code edit; no LESSONS/FORWARD touch by any step.** Gate coupling: none — the three distinctive tokens (`panel meter opens`, `Execution brief`, `PANEL_SEAT_TEMPLATE`) measure 0 in both `/Users/marklehn/Developer/GitHub/bellows/scripts/plan_lint.py` and `/Users/marklehn/Developer/GitHub/bellows/gates.py`; rule-number couplings exactly `Rule 20/22/26`; zero `DRAFTING_CYCLE[^ ]*:[0-9]+` line-cites in bellows `*.py` (all measured; QA row 7 re-sweeps with positive control).
- Environment facts, VERBATIM: (1) `grep` is a ugrep shim — `-F` every literal; a zero-match `grep -c` prints `0` and EXITS 1; the printed count is the assertion; never `&&`-chain zero-count probes. (2) Shell state does NOT persist — same-invocation scratch. (3) `find`, never a glob; `--` before dash-leading literals. (4) The DB is gitignored and worktree-absent — canonical absolute path only.
- Verdict-window posture: v2.6 governs from the Step-1 commit; HALT holds; rollback is CEO's.

**Rule 93 — declared blanket form:** Step-1 mandates are observed collectively — the builder by QA row 2's probe re-run, the template copy by row 3's dual-sha, F/F2 by rows 1/4(a), CHANGES/GLOBOK by row 4(a), **BK and MAXID by the Receipt's named-sentinel mandate** (the receipt is their observer). This line is the declaration.

## Freeze checklist (deposit path — items 1–5 BEFORE the copy, item 6 immediately AFTER it)
1. Substitute the read id at the bootstrap `<id>` site AND in TASK F's `-m` string; probe, OCCURRENCE form: `grep -oF -- '<id' <deposit-path> | wc -l` → **2** (both residual tokens on this checklist line).
2. **Diff the draft against the mirror immediately before the copy** — empty-except-substitutions is the deposit precondition (the 359 stale-mirror guard).
3. Final `plan_lint` at the FAITHFUL scratchpad mirror (NEVER the real `decisions/` — same-second claim-and-DISPATCH, proven live) — WARN set matches the Conformance paragraph.
4. A0-fresh re-check: the FOUR still `accepted|codify` @ `2026-08-12T17:12:07Z` with the 2/2 category mix (and 330 still `accepted|codify` — the queue's fifth, unflipped by design); DC sha still the A1 pin; template source sha still `f8d2626a…`; builder re-run numstat still `7 1`.
5. The Closing line contains `instruction` and `/ record` with no italic-parenthetical placeholder (earnable — fails the placeholder, passes only the real Closing; 356's item, restored by seat 2 after the clone dropped it).
6. Post-copy, same minute: `ls` the real `decisions/` — the claim reads `in-progress-executable-<READ-ID>.md` with the item-1 id (mismatch = foreign consumption — report, never re-copy).

## Conflict Ledger
**C1** the four-id set immutable (330's EXCLUSION equally immutable — a seat-1 decision, never silently reversed). **C2** builder all-or-nothing; anchors count-asserted; the template ships by COPY with dual-sha, never composed. **C3** doctrine before DB; A0 completes the half-state. **C4** backup adjacent, `BK=4` by value via `?immutable=1` against THE FOUND BACKUP's absolute path. **C5** flip scoped `id IN (327,328,329,332) AND status='accepted'`. **C6** sentinels BY NAME — PRE, ACC, MAXID, BK, CHANGES, GLOBOK — SIX, counted; `CHANGES=4`, `GLOBOK=4` with the one-value exclusion. **C7** capture inside the flip txn: `id <= 332 AND id NOT IN (327,328,329,332)` → **328 rows** (332−4; contiguity measured — note 330 AND 331 ARE in the capture, correctly: neither is flipped). **C8** commits cd-first + pathspec + name-only + toplevel; every post-commit assert `-C`-pinned to CAPTURE_COMMIT, never `HEAD` (foreign in-window commits proven twice this session). **C9** serialized dispatch stated; non-dependent guards named at A0/QA.

## How to Run This Plan
**Bootstrap:**
```
Read the plan at knowledge/decisions/in-progress-executable-364.md (the daemon renames on claim). Execute Step 1 ONLY. After completing Step 1, STOP and wait for my confirmation.
```
⚠️ HALT ROUTING: Step 1 reads this plan, the live `/Users/marklehn/Developer/GitHub/DRAFTING_CYCLE.md`, the COMMITTED builder + template source, the canonical DB. Step 2 reads this plan, the dev-log, both live doctrine files, the DB (read-only), the merged captures, `/Users/marklehn/Developer/GitHub/RULE_20_SELF_CHECK_BLOCK.md`.

---

## Drafting Cycle

**Tier:** T2 — governance surface (DRAFTING_CYCLE + a new root artifact + corpus). Clone lineage: 356's FINAL panel-hardened form cloned FULL; the template-copy task is the one NEW machinery class (verbatim-copy-with-dual-sha, the simplest in the lineage).

**Walk 0 (context pin, §2.0):** DC sha `817677db…` (live v2.5, porcelain clean); all five builder anchors count-1 measured; four probe tokens 0-pre *(superseded seat 1: FOUR anchors — the §4 head dropped with E5 — and THREE tokens; seat 5 closed the pointer this pin was owed)*; dry-run executed `OK — 5 edits`, numstat `9 1`, H2/H3 9/11 unchanged, 288 lines *(all superseded seat 1: `OK — 4 edits`, `7 1`, 286 — the standing pins)*; template source committed, sha `f8d2626a…`, 39 lines, root target ABSENT; the five's stamp `2026-08-12T17:12:07Z` Z-GLOB-MATCHING → one-value exclusion load-bearing; categories MIXED per id (3 instrumentation / 2 governance_rule — measured, never uniform-asserted); capture bound 332−5=**327** (331 correctly inside) *(superseded seat 1: 332−4=**328**, 330 AND 331 inside)*; gate-coupling swept 0×4 + controls *(seat 1: three tokens post-re-scope)*. Clone-diff vs 356: the template-copy task added (new class, dual-sha both sides); the census machinery retargeted (rules-census → H2/H3 + line count — DC has no numbered-rules section; the spelled python census DROPPED with its premise absent, stated); E0 denylist rotated (PLANNER_TEMPLATE now protected, DC now the target); all Rule-85/CAPTURE_COMMIT/backup/flip machinery carried re-tokened. Register: `governance/knowledge/research/walk-register-gate2-coldpanel-2026-08-12.md`.

**Direction verdict (after walk 1): PROCEED** — origin sound (356's final form), mechanism sound (builder dry-run stable `9 1`; the builder's own assert demonstrated fail-closed at its first run), licence sound (360's routing + 362's QA re-verified; categories per id at the pin).

**Walks:** per-lens commits govern folds (v2.5 — soon v2.6 — discipline).
- Weak spots:          w1 1 folded — instruction, clone-adaptation (QA row 2's census expectations initially carried 356's rules-census shape; retargeted to DC's real invariants — H2 9 / H3 11 / line count / the probe tokens at their per-token expectations (1/1/2, seat 4 corrected this line's own 'four at 1' claim — the template token expects 2) — at both steps' sites). ⚠️ Seat-4 note: this fold carries NO per-lens commit — it landed inside the v0 commit during authoring; declared in the register's Deviations, the discipline held from the panel on.
- Destruction:         w1 dry (all five edits are inserts + one line-replace; every anchor asserts count-1 pre-edit; neighbour text preserved by construction — the registry's Attribution bullet and §2.7/§4 heads verified 1→1 both sides on the dry-run).
- Vulnerabilities:     w1 executed — builder dry-run + the full G1/G2/G3 flip rehearsal against a scratch DB copy: PRE=5/ACC=5/MAXID=332, CHANGES=5/GLOBOK=5, capture 327 lines, re-run CHANGES=0; the template dual-sha rehearsed (copy + both shasums match); lint EXIT 0 at the faithful mirror.
- Integration-record:  w1 dry (the doctrine content cross-read against the five entries' own text — each bullet carries its proposal/entry attribution; the History row names the 331 split; the v2.5 History row untouched).
- ACID:                w1 dry (doctrine-before-DB carried; the template copy is idempotent; A0's branches own every half-state incl. template-copied-but-DC-uncommitted; the backup is the durability floor).

**Walk-1 split: instruction 1 / record 0.** Re-opens; walk 2 owed.

**Walk 2** (whole artifact; new surface = the walk-1 retarget):
- Weak spots:          w2 dry (the census retarget verified at both sites from the artifact).
- Destruction:         w2 dry.
- Vulnerabilities:     w2 executed — battery re-run stable (dry-run `9 1`, DB rehearsal exact, dual-sha, lint).
- Integration-record:  w2 dry.
- ACID:                w2 dry.

**Walk-2 split: instruction 0 / record 0 — LITERAL DRY. The walk phase meets the §2 bar; T2: the cold panel convenes on this artifact before the Closing.**

**Conformance (§5):** run at shape-stability and after every culmination at the FAITHFUL mirror (fidelity files: `src/test_lessons_forge.py`, `src/db.py`, `agents/FORGE_LESSONS_AGENT.md`, `knowledge/FORWARD.md`). **Measured through walk 2: EXIT 0, TWO WARNs — the T2 cold-panel line (EARNED-absent: the placeholder is phrased so the bold-keyword check cannot match until the real seat record lands — walk 2 caught the v0 placeholder SATISFYING the check pre-panel, the authored-clean-gate trap) and the benign (o1) conditional (`resume-sweep.txt`, a HALT-branch evidence path). The panel-line WARN clears at the FIRST committed seat record, not at panel close (seat 3 measured the clear mid-panel — the earned-clear point relabelled). Final expected set: ONE — the (o1) conditional.** Last run: post-seat-4 fold — EXIT 0, ONE WARN (seat 4: a future phase is not a run; the deposit-time re-run is OWED by freeze item 3, not pre-recorded).

**Panel meter (opened at CONVENE per the discipline this plan ships):** baseline 563k/45. Seat 1 ≈143k (recorded at THIS fold after seat 2 caught the omission — the plan was re-running its own E2 decay); seat 2 ≈180k; seat 3 ≈162k; seat 4 ≈196k; seat 5 ≈163k. Panel total ≈844k; findings 8+13+3+8+2 = 34 (2 HIGH); baseline 563k/45.

**Cold panel (T2)** — sequential, seats built from the committed template source (its first exercise), pin tables handed as claims-to-attack, author-verified and folded between seats; per-seat commits.
- **Seat 1 (weak spots + evidence-attack): 8 findings — 1 HIGH / 3 MED / 4 LOW; instruction 6 / record 2; all pre-existing-v0. THE HIGH RE-SCOPED THE PLAN: E5's premise ("schema 0.1 existed only as a register-file convention") was FALSE — a committed v0.1 schema doc (plan 338) and a LIVE validator exist, and DC §3's own pointer names them; shipping 0.2 as DC prose would have FORKED the schema. The Gate-1 dedup probe had grepped `schema_version` in the WRONG PLACES — the literal appears 4× in the schema doc and 1× in the validator (seat 4 corrected seat 1's post-mortem: the failure class was probe SCOPE, not representation — the probe was right, its search population was register files only).** Resolution: E5 dropped; 330 stays `accepted|codify` (the queue row is its schedule; its plan targets the schema-owner surfaces); flip set 5→4; every count re-measured and re-rehearsed (numstat `7 1`, 286 lines, PRE=4/ACC=5/CAP=328/CHANGES=4/GLOBOK=4, post-flip queue=1 exactly 330). MEDs mooted by the drop (E5 placement; E5's invented fields) except S1-4: E2's line-to-hold had DROPPED "the aimed-brief judgment and author-verify stay fully cold" — the evidence held them cold; restored. LOWs: the mirror rule now explicit in E1's guardrails (S1-5); QA row 3's order-only probe strengthened to adjacency (S1-7). Author-verified-not-folded, stated: the template's "seat 1, id 354" claim is TRUE per the gate2-pt3 session record (the cold reader lacked it — a correct challenge, a verified answer); the registry intro's three-brief parenthetical is a historical description of the 324 mapping (the new brief self-describes its layering). Evidence-attack tally: faithful 3 / compressed 3 (all three now repaired or dropped) / silenced 0 / false-premise 1 (killed). Pin table: every pin the seat leaned on re-ran MATCH.
- **Seat 2 (destruction + clone-diff): 13 findings — 1 HIGH / 6 MED / 6 LOW; instruction 8 / record 5; origins 5 fold-introduced (seat 1's re-scope sweep misses — the yield-does-not-decay claim performing LIVE inside the plan that codifies it), 1 walk-2, 7 pre-existing clone construction. THE HIGH: TASK F's spelled commit message still shipped `register schema 0.2` in its subject — the permanent git record would have asserted the exact thing the re-scope un-shipped, and no probe read it; message corrected.** Re-scope sweep completed: the Step-1 heading, the builder docstring, the register header's SHIPS-0.2 claim, the register row-2 WARN under-count — all struck or corrected in place. The meter's own decay caught: seat 1's cost was recorded NOWHERE at its fold — the plan re-running its own E2 bullet; both seats now recorded. Clone drops restored from 356: the Closing-earnable freeze item; A0 branch 4's defining predicate; the report-LOUDLY dirt clause; the observed-triple definition; the resume-sweep joins-Scope clause; QA row 4's DECLARED FALLBACK + branch-1 reference; QA row 6 rebuilt to the concurrent-safe specific-entries form (319–324 absent, foreign ids reconciled); C9's non-dependent-guards tail. Hunk table: ~54 hunks — 3 dropped-unjustified (all restored), 3 dropped-justified (premises verified TRUE against live data), 6 mis-adapted (corrected), ~39 carried, 0 re-added-after-deletion. Attacked-and-held: template-copy overwrite bounded; E0 rotation symmetric; 330/331 preservation in-query; no guard weakened in B/G1/G2/G3/F/F2.
- **Seat 3 (vulnerabilities + the Execution brief — the brief's FIRST exercise, shipped by this very plan): 3 findings — 0 HIGH / 0 MED / 3 LOW; instruction 1 / record 2. Execution log: 52 expected-vs-measured comparisons, 52 MATCH / 0 MISMATCH** — builder both directions (the re-run AssertionError leaves the file byte-untouched, proven twice), the full flip sequence on a DB copy with all adversarial variants fail-safe (G2 re-run CHANGES=0; foreign half-flip caught by PRE and CHANGES; `.output`-to-missing-dir fails closed pre-flip; BK non-vacuous — 0 on a wrong-state backup; GLOBOK's exclusion proven load-bearing 4-vs-0), dual-sha + overwrite variant, lint at the faithful mirror, every gate-neutrality sweep with controls, and every seat-1/2 claimed fold verified LANDED. The LOWs, folded: the commit-discovery's `-n 1` capped its own output by construction (can never fail high — replaced with the counted form, ≥2 = ambiguous-history HALT); the Conformance's earned-clear point relabelled (the panel-line WARN clears at the FIRST committed seat record — measured mid-panel); the H2 census annotated (9 includes §3's fenced decoy, invariant both sides). Not executed, stated per contract: TASK F/F2 real commits, QA row 5's pytest, freeze item 6's daemon watch.
- **Seat 4 (integration-record + clone-diff-record): 8 findings — 0 HIGH / 5 MED / 3 LOW; instruction 0 / record 8 — the instruction surface held completely; every finding is in the cycle's own account. Zero phantom folds (22 claimed folds all LANDED per the per-seat diffs); numbers ≈38 MATCH / 5 MISMATCH / 3 STALE / 5 UNVERIFIABLE.** The record repairs: seat 1's probe post-mortem corrected (SCOPE, not representation — `schema_version` lives 4×/1× in the surfaces the probe never searched); seat 2's restoration count reconciled at EIGHT against the diff (the commit subject's 'six' is immutable history, corrected in the register); walk-1's fold declared commit-less (it landed inside v0 — the per-lens discipline held from the panel on, the gap declared not laundered); walk-2b's earn-form fold given its missing register row (2b, retroactive-marked); **the register's schema_version line repaired to PARSE — seat 2's annotated form had broken the validator (PRE-SCHEMA), the prose-only-pin defect this plan codifies against, now CONFORMANT by the validator's own run**; supersession pointers added to every superseded walk-0 pin; the walk-1 line's own 'four tokens at 1' claim corrected; the Conformance last-run cell made honest (a future phase is not a run). Cross-record coherence of the re-scope verified: the Why, E7, register, seat lines, TASK F message, and the DB tell ONE story.
- **Seat 5 (ACID + system capstone, ran LAST on the artifact as folded by seats 1–4): 2 findings — 0 HIGH / 0 MED / 2 LOW; instruction 1 / record 1; both seat-1 re-scope residue that survived three subsequent seats. VERDICT: SAFE-AS-WRITTEN — zero blocking findings.** Isolation: 10 windows, 10 STATED / 0 LUCKY / 0 MISSING (one stated-deferred, named). Execution: 48/48 MATCH — builder both directions, the full flip rehearsal with queue-post exactly 330, both adversarial fail-safes re-proven, the freeze probes incl. the Closing assert FAILING the placeholder (earnable), the faithful-mirror lint at the declared ONE WARN, the register validator CONFORMANT. Fold-interaction matrix: 10 seat-pairs, 8 CLEAN, 2 residual conflicts = the two findings (folded: QA row 7's 'four'→THREE — the sweep-built-from-own-wording class; the walk-0 anchor pins' missing supersession markers closed at both sites). ACID: all EIGHT half-states — the five originals plus the three 330-flipped worlds the re-scope created — terminate in a stated branch; the 330-preservation claim survives a resume because it is re-measured in-query, never remembered. The meter's seat-5 slot recorded at this fold (the S5-3 obligation discharged in the same motion it was filed).

**Walk 3 — the post-panel bar-meeting close** (whole artifact; new surface = the panel's ~26 folds):
- Weak spots:          w3 dry (old five-set literal 0; no un-struck schema-0.2 or stale-count site; the two remaining `9 1` mentions are dated event records, historical by the standing convention).
- Destruction:         w3 dry (no guard relaxed by any panel fold; the re-scope's subtractions all carry measured-false premises inline).
- Vulnerabilities:     w3 executed — builder re-run `OK — 4 edits` numstat `7 1` stable; lint at the faithful mirror EXIT 0, ONE (o1) WARN (the declared set); freeze token geometry 4→2 earnable; register validator CONFORMANT.
- Integration-record:  w3 dry (meter complete 5/5 seats ≈844k; seat lines ↔ register rows 3–7 consistent; the closing-record re-read done on this block and the register's final state).
- ACID:                w3 dry (seat 5's eight half-states stand; the freeze schedule 1–5-pre/6-post coherent; nothing moved since the capstone).

**Walk-3 split: instruction 0 / record 0 — DRY. The §2 bar met on the dry branch after the panel.**

**Closing:** walk 3 dry — instruction 0 / record 0; closed on the dry branch after 3 walks + a 5-seat cold panel (34 findings, 2 HIGH, ≈844k fully metered — the first panel run under the discipline it ships: meter from convene, template-built seats, claims-to-attack pin tables, scripted inter-seat battery); the mid-cycle RE-SCOPE (330 out, premise measured false) is the panel's headline yield; the §2.7 closing-record re-read done; residue: none.

---

## STEP 1 — DEV (builder + template copy + commit + flip the four)

> **FIRST — visible chat message; do NOT rename this plan file.** Doctrine edits land at ABSOLUTE root paths; a HALT after edits leaves the tree as-is, reported loudly.
> **A0 (first match wins):** (1) the four `implemented` → verify commit-by-slug via the QA row-1 SPELLED discovery (NOT FOUND → HALT), deposits survived, RECOVERY receipt if the capture is lost + fresh G3; report complete. (2) docs committed (the discovery returns the commit, BOTH files in name-only) + the four still `accepted` → **assert porcelain clean ON TOP for both paths (post-commit dirt → report LOUDLY, never sweep)**, DOC_SHA/TPL_SHA FROM THE COMMIT, reuse any `pre-g2cp-*` backup via prefix-only `find` (BK assert mandatory; >1 match → HALT), re-run the probes against committed content (`git -C /Users/marklehn/Developer/GitHub show <commit>:DRAFTING_CYCLE.md | grep -cF …`) — then B → G. (3) **docs modified-UNCOMMITTED** (either path dirty) → **HALT**; sweep the probes against the live files, DEPOSIT the landed/not-landed table at `knowledge/qa/evidence/gate2-coldpanel-2026-08-12/resume-sweep.txt` (joins Scope on this path only), report — CEO directs. (4) **fresh-with-unexplained-backup** (a `pre-g2cp-*` backup exists, no doctrine commit naming the slug, the four still `accepted`) → **HALT**. (5) fresh — porcelain clean for BOTH target paths AND the E0 denylist, DC version reads `2.5`, root `PANEL_SEAT_TEMPLATE.md` ABSENT, the four `accepted|codify` @ `2026-08-12T17:12:07Z` with the 2/2 category mix (327/332 instrumentation, 328/329 governance_rule; 330 also present-accepted, unflipped by design), no `pre-g2cp-` backup → A1. ⚠️ **A state matching NO branch → HALT with the observed triple (porcelain for both target paths + version/History-head + the per-id read-back) — STATE-level, distinct from the version check.**
> **A1 — pins:** `shasum -a 256 /Users/marklehn/Developer/GitHub/DRAFTING_CYCLE.md` == `817677db4a3df2a50bdaf345138e441533dc30690e6ced378482573fb79b79b6` AND `shasum -a 256 /Users/marklehn/Developer/GitHub/governance/knowledge/research/panel-seat-template-v1.md` == `f8d2626abe6eb0d0a3f8a4a38eb9ed4513f27ef041b97d96c288953ff280ffb4`. Either mismatch → HALT.
> **BUILDER:** run exactly `python3 /Users/marklehn/Developer/GitHub/governance/knowledge/research/builder-gate2-coldpanel-2026-08-12.py /Users/marklehn/Developer/GitHub/DRAFTING_CYCLE.md /Users/marklehn/Developer/GitHub/DRAFTING_CYCLE.md` → exit 0 + `OK — 4 edits applied` naming all four. **AssertionError → HALT, quote verbatim — the file is UNTOUCHED by construction; never patch, never retry modified.** Post-conditions, `grep -cF` (`--` where dash-leading) on the live file:
> 1. `panel meter opens when the panel CONVENES` → 1
> 2. `**Execution brief.**` → 1
> 3. `PANEL_SEAT_TEMPLATE.md` → 2
> 4. `**Version:** 2.6` → 1; `**Version:** 2.5` → 0; `slug gate2-coldpanel-2026-08-12` → 1 (the History row); `Walk-register schema 0.2` → **0** (the re-scope's own probe — a 1 here means E5 leaked back in)
> 5. Censuses: `grep -cE '^## '` → **9 unchanged** (⚠️ seat 3: the 9 includes ONE fenced decoy — §3's example block carries a `## Drafting Cycle` line inside a code fence; the raw count is invariant both sides for THIS plan, so the bare census is the correct probe here, annotated per §2.7's strip-fences rule); `grep -cE '^### '` → **11 unchanged**; `wc -l` → **286**.
> **TASK T — the template:** `cp /Users/marklehn/Developer/GitHub/governance/knowledge/research/panel-seat-template-v1.md /Users/marklehn/Developer/GitHub/PANEL_SEAT_TEMPLATE.md` then `shasum -a 256` BOTH paths → both == `f8d2626a…` (full hash above). Mismatch → HALT (the copy is the only composition permitted).
> **E0 denylist:** porcelain — `DRAFTING_CYCLE.md` modified + `PANEL_SEAT_TEMPLATE.md` untracked are THE expected states; HALT iff any of `PLANNER_TEMPLATE.md`, `RULE_20_SELF_CHECK_BLOCK.md`, `READONLY_AUDIT_CONTRACT.md`, `SPECIALIST_TEMPLATE.md`, `INTERMEDIATE_DECISION_PHRASES.md` dirty; other root dirt reported, never a HALT.
> **DOC_SHA** (DC) and **TPL_SHA** pinned before commit. **TASK F:** `cd /Users/marklehn/Developer/GitHub && git add DRAFTING_CYCLE.md PANEL_SEAT_TEMPLATE.md && git commit -m "[364] gate2(gate2-coldpanel-2026-08-12): the cold-panel four — meter-at-convene, Execution brief, the four panel structures, seat-template mandate — DC 2.5 -> 2.6 + PANEL_SEAT_TEMPLATE v1.0 (330 schema re-scoped)" -- DRAFTING_CYCLE.md PANEL_SEAT_TEMPLATE.md && git rev-parse HEAD && git rev-parse --show-toplevel` (expect the new hash then the governance root; wrong/missing → NOT COMMITTED). **The printed hash is CAPTURE_COMMIT — every post-commit assert names it, NEVER `HEAD`.** Numstat, spelled: `git -C /Users/marklehn/Developer/GitHub show <CAPTURE_COMMIT> --numstat --format=` → exactly TWO lines: `7	1	DRAFTING_CYCLE.md` and `39	0	PANEL_SEAT_TEMPLATE.md`; different → HALT. **F2:** `git -C /Users/marklehn/Developer/GitHub show <CAPTURE_COMMIT>:DRAFTING_CYCLE.md | shasum -a 256` == DOC_SHA; `git -C /Users/marklehn/Developer/GitHub show <CAPTURE_COMMIT>:PANEL_SEAT_TEMPLATE.md | shasum -a 256` == TPL_SHA == `f8d2626a…`; name-only spelled: `git -C /Users/marklehn/Developer/GitHub show <CAPTURE_COMMIT> --name-only --format=` → exactly the two paths.
> **B — backup** (skip creation iff A0 branch 2 reused a found `pre-g2cp-*` backup — ONE ever; `find` >1 match → HALT), exactly: `sqlite3 -bail /Users/marklehn/Developer/GitHub/lessons-forge/lessons-forge.db ".timeout 5000" ".backup /Users/marklehn/Developer/GitHub/lessons-forge/pre-g2cp-$(date -u +%Y%m%d_%H%M%S).db"`; locate via prefix-only `find`; assert against THE FOUND BACKUP: `sqlite3 -bail "file:<found-abs>?immutable=1" ".timeout 5000" "SELECT 'BK='||COUNT(*) FROM lesson_proposals WHERE id IN (327,328,329,332) AND status='accepted';"` → **BK=4**; else HALT.
> **G1 — rehearsal** (runner: `sqlite3 -bail /Users/marklehn/Developer/GitHub/lessons-forge/lessons-forge.db ".timeout 5000" ".read <abs>"`, exit-0 + empty-stderr per invocation; file `knowledge/development/g2cp-rehearsal.sql`, exactly):
> ```
> BEGIN IMMEDIATE;
> SELECT 'PRE='||COUNT(*) FROM lesson_proposals WHERE id IN (327,328,329,332) AND status='accepted' AND route='codify';
> SELECT 'ACC='||COUNT(*) FROM lesson_proposals WHERE status='accepted' AND route='codify';
> SELECT 'MAXID='||MAX(id) FROM lesson_proposals;
> ROLLBACK;
> ```
> Assert **PRE=4, ACC=5, MAXID=332** (⚠️ ACC is FIVE, not four — 330 is the accepted fifth, unflipped by design; ACC>5 → in-window routing → HALT; fewer → HALT naming ids; MAXID>332 reported-benign, the capture bound stays).
> **G2 — the flip** (`mkdir -p` the evidence dir FIRST; file `knowledge/development/g2cp-flip.sql`, exactly; `.output` absolute from `pwd`):
> ```
> BEGIN IMMEDIATE;
> .output <tree-abs>/knowledge/qa/evidence/gate2-coldpanel-2026-08-12/outside-range-ids.txt
> SELECT id||'|'||category||'|'||status||'|'||COALESCE(route,'')||'|'||COALESCE(status_updated_at,'')||'|'||COALESCE(status_updated_by,'') FROM lesson_proposals WHERE id <= 332 AND id NOT IN (327,328,329,332) ORDER BY id;
> .output stdout
> UPDATE lesson_proposals SET status='implemented', status_updated_at=strftime('%Y-%m-%dT%H:%M:%SZ','now'), status_updated_by='ceo' WHERE id IN (327,328,329,332) AND status='accepted';
> SELECT 'CHANGES='||changes();
> SELECT 'GLOBOK='||COUNT(*) FROM lesson_proposals WHERE id IN (327,328,329,332) AND status_updated_at GLOB '20[0-9][0-9]-[0-9][0-9]-[0-9][0-9]T[0-9][0-9]:[0-9][0-9]:[0-9][0-9]Z' AND status_updated_at NOT IN ('2026-08-12T17:12:07Z');
> COMMIT;
> ```
> **Sentinels CHANGES=4, GLOBOK=4** (the prior stamp is Z-form GLOB-MATCHING — the `NOT IN` carries the guard); either off → HALT with numbers. Capture **328 lines** (330 and 331 correctly among them), read post-commit — mismatch = record + HALT with the flip landed, never re-run. The four-id set is IMMUTABLE.
> **G3 — read-back**, exactly: `sqlite3 -bail -readonly /Users/marklehn/Developer/GitHub/lessons-forge/lessons-forge.db ".timeout 5000" "SELECT id||'|'||category||'|'||status||'|'||COALESCE(route,'')||'|'||status_updated_by||'|'||status_updated_at FROM lesson_proposals WHERE id IN (327,328,329,330,331,332) ORDER BY id;"` → RAW to `flip-readback.txt`: the four `implemented|codify|ceo|<Z ≠ 2026-08-12T17:12:07Z>` with their measured categories; **330 UNCHANGED at `accepted|codify|ceo|2026-08-12T17:12:07Z` AND 331 UNCHANGED at `reference|backlog|ceo|2026-08-12T17:12:07Z` — the preservation reads, in-query by design.**
> **Receipt** with SIX named sentinels — PRE, ACC, MAXID, BK, CHANGES, GLOBOK — + DOC_SHA + TPL_SHA + CAPTURE_COMMIT + the two numstat lines · `### Ledger Updates` · `#### Prompt Feedback` · `#### Forward Register`: `NONE`. **FINAL ACTION** — commit deposits: `cd <your-tree-abs>` first token, pathspec on the COMMIT, name-only assert, bare `git rev-parse --show-toplevel` last.
>
> **Scope:**
> - `knowledge/development/dev-log-gate2-coldpanel-step-1-2026-08-12.md`
> - `knowledge/development/g2cp-rehearsal.sql`
> - `knowledge/development/g2cp-flip.sql`
> - `knowledge/qa/evidence/gate2-coldpanel-2026-08-12/outside-range-ids.txt`
> - `knowledge/qa/evidence/gate2-coldpanel-2026-08-12/flip-readback.txt`
>
> **Deposits:**
> - `lessons-forge/knowledge/development/dev-log-gate2-coldpanel-step-1-2026-08-12.md`
> - `lessons-forge/knowledge/development/g2cp-rehearsal.sql`
> - `lessons-forge/knowledge/development/g2cp-flip.sql`
> - `lessons-forge/knowledge/qa/evidence/gate2-coldpanel-2026-08-12/outside-range-ids.txt`
> - `lessons-forge/knowledge/qa/evidence/gate2-coldpanel-2026-08-12/flip-readback.txt`

## STEP 2 — QA

> **FIRST — do NOT rename this plan file. Deliverable Verification (Rule 8/17)**, ✅/❌ table, any ❌ → HALT. **Rule 20 self-check** (canonical block from `/Users/marklehn/Developer/GitHub/RULE_20_SELF_CHECK_BLOCK.md`; `plan_slug`: `gate2-coldpanel-2026-08-12`; `qa_report_path`: `<tree>/knowledge/qa/gate2-coldpanel-qa-2026-08-12.md`; `evidence_dir`: `<tree>/knowledge/qa/evidence/gate2-coldpanel-2026-08-12/`; `required_evidence_files`: `[doc-integrity.txt, db-invariants.txt, gate-neutrality.txt, pytest_targeted.txt]`, all four BEFORE the block; literal stdout: the banner `Rule 20 — QA Self-Check Results` and the `PASSED — SELF-CHECK PASSED` line, byte-exact). `## Evidence and Narrative` after the table. ONE read-only DB form; RAW evidence.
> **1. DOC INTEGRITY** — commit discovered by slug, spelled: `git -C /Users/marklehn/Developer/GitHub log --format='%H %s' --grep='gate2-coldpanel-2026-08-12' -- DRAFTING_CYCLE.md` → exactly ONE line by count (`| wc -l` → 1; seat 3: an `-n 1` form caps output BY CONSTRUCTION and can never fail high — a slug-quoting revert would be the silently-truncated second match; empty = NOT FOUND → HALT; ≥2 = ambiguous history → HALT); committed CONTENT shas (`git -C … show <commit>:<path> | shasum -a 256`; `e3b0c442…` = the show FAILED) == live == DOC_SHA/TPL_SHA; TPL_SHA == the source pin `f8d2626a…` == live root copy == committed source file (FOUR-way); porcelain empty for both paths; name-only exactly the two files (F2's spelled form with `<commit>`). → `doc-integrity.txt`
> **2. THE CLAUSES LANDED** — the five Step-1 post-condition probe sets re-run independently on the live file: the three tokens → 1/1/2; `Walk-register schema 0.2` → **0** (the re-scope probe); version 2.6 → 1, 2.5 → 0, the History row → 1; H2/H3 → 9/11 unchanged; `wc -l` → 286. → `doc-integrity.txt`
> **3. COHERENCE** — the v2.5 History row intact (`grep -cF 'slug gate2-dc5-2026-08-11'` → 1); the registry's Attribution bullet intact (`grep -cF -- '- **Attribution.** Every panel finding names'` → 1) with the Execution brief directly ABOVE it (adjacency probe, seat 1: `grep -n` both heads — Attribution's line number MINUS the brief's == **1**, order alone is not adjacency); `### 2.7`/`## 4.` heads intact → 1 each; the root template's own probes: `grep -cF 'THE INCIDENT MANDATE' /Users/marklehn/Developer/GitHub/PANEL_SEAT_TEMPLATE.md` → 1, `grep -cF 'CLAIMS TO ATTACK' …` → 1. → `doc-integrity.txt`
> **4. FLIP + BLAST RADIUS** — (a) per id: the FOUR `implemented|codify|ceo`, Z-stamped ≠ the pinned prior, categories per the authoring measurement (327/332 instrumentation, 328/329 governance_rule); **330 UNCHANGED at `accepted|codify`** (the re-scoped fifth — its presence IS the pass condition; a flipped 330 = scope violation → HALT) **and 331 UNCHANGED at `reference|backlog`**; (b) **`accepted|codify` → 1 — exactly 330, the re-scoped schema row awaiting its own plan** (0 or ≥2 both FAIL); (c) re-run the EXACT capture SELECT (COPY from G2), diff vs the deposited 328-line file; partition (impossible-id → HALT; deleted-row → HALT; else concurrent, named, no halt). DECLARED FALLBACK (capture lost to a crashed worktree): degraded co-stamp sweep, labelled — A0 branch 1's RECOVERY receipt names it. → `db-invariants.txt`
> **5. TESTS** — single-module premise re-derived (`find <tree-abs>/src -name 'test_*.py'` → exactly one; a second = report + run whole `src/`, never HALT); pytest FOREGROUND vs 55/0, delta reported never asserted. → `pytest_targeted.txt`
> **6. CONSUMER SEMANTICS** — quote `/Users/marklehn/Developer/GitHub/lessons-forge/src/lessons_forge.py:31` verbatim (`implemented` IS terminal); `get_unclassified_entries` (read-only) → the batch entries **319–324 ABSENT from the work list before and after** (the concurrent-safe form, seat 2 — a foreign in-window ingest legitimately makes the list non-empty: reconcile-note naming ids, never a fail; ANY of 319–324 present → ❌). → `db-invariants.txt`
> **7. GATE-NEUTRALITY** — (a) `panel meter opens`, `Execution brief`, `PANEL_SEAT_TEMPLATE` → 0 in both `/Users/marklehn/Developer/GitHub/bellows/scripts/plan_lint.py` AND `/Users/marklehn/Developer/GitHub/bellows/gates.py`; positive control `Drafting Cycle` in plan_lint.py → ≥1 (print the count). ⚠️ All THREE expected values are 0 (seat 5: 'four' survived the re-scope — the sweep-built-from-own-wording class performing live) — env fact 1: printed counts, never `&&`-chained. (b) Rule-number coupling: `grep -rohE 'Rule [0-9]+' <both files> | sort -u` → exactly `Rule 20`, `Rule 22`, `Rule 26`. (c) Line-citation sweep: `grep -rn -E 'DRAFTING_CYCLE[^ ]*:[0-9]+' /Users/marklehn/Developer/GitHub/bellows --include='*.py'` → zero hits (prints nothing, exits 1 — env fact 1; (b) is the positive-output control). → `gate-neutrality.txt`
> `## Evidence and Narrative` · Receipt · `### Ledger Updates` · `#### Forward Register`: `NONE`. **FINAL ACTION** — commit deposits, cd-first + pathspec + name-only + bare toplevel.
>
> **Scope:**
> - `knowledge/qa/gate2-coldpanel-qa-2026-08-12.md`
> - `knowledge/qa/evidence/gate2-coldpanel-2026-08-12/doc-integrity.txt`
> - `knowledge/qa/evidence/gate2-coldpanel-2026-08-12/db-invariants.txt`
> - `knowledge/qa/evidence/gate2-coldpanel-2026-08-12/gate-neutrality.txt`
> - `knowledge/qa/evidence/gate2-coldpanel-2026-08-12/pytest_targeted.txt`
>
> **Deposits:**
> - `lessons-forge/knowledge/qa/gate2-coldpanel-qa-2026-08-12.md`
> - `lessons-forge/knowledge/qa/evidence/gate2-coldpanel-2026-08-12/doc-integrity.txt`
> - `lessons-forge/knowledge/qa/evidence/gate2-coldpanel-2026-08-12/db-invariants.txt`
> - `lessons-forge/knowledge/qa/evidence/gate2-coldpanel-2026-08-12/gate-neutrality.txt`
> - `lessons-forge/knowledge/qa/evidence/gate2-coldpanel-2026-08-12/pytest_targeted.txt`
