# Executable: the shape amendment — DRAFTING_CYCLE v1.8 → v2.0 (termination criterion, record blind spot, coverage-not-aim), then flip cluster A to `implemented`

**Type:** Executable
**Project:** lessons-forge
**Depends on:** the shape-decision record (`/Users/marklehn/Developer/GitHub/shape-decision-packet-2026-08-09.md` — D1/D2/D3 are the CEO decisions this plan lands; provenance, no step reads it), executable-326 (lessons-forge, Done — the Gate-1 routing write that stamped all seven rows `status_updated_at='2026-08-09T01:20:01Z'`, the exact prior value G2's sentinel excludes), executable-311 (lessons-forge, Done — the cycle that created proposals 233/238/246/247/258/259/271), executable-330 (lessons-forge, Done — the clone origin: doctrine edit + scoped proposal flip), DRAFTING_CYCLE.md at v1.8 (precondition, checked at A0)
**Created:** 2026-08-09
**Author:** Planner
**Slug:** `shape-amendment-2026-08-09` (authoring-time; stable across any crash-redo re-deposit — the A0 re-entry key and the Rule 20 `plan_slug`)
**dispatch_mode:** bellows
**pause_for_verdict:** always
**Priority:** 5
**cycle_tier:** T2
**qa_steps:** 3
**Test Scope:** targeted (Rule 21) — ⚠️ **and the deviation is stated rather than silent (panel seat 4).** Rule 21's `targeted` form expects a `pytest_targeted.txt` evidence file; **this plan touches no code**, so there is no changed module to target. What replaces the suite is named and executed: the G1 rehearsal, the two in-transaction sentinels, the value-guarded backup, and — restored at panel seat 4 — an **executed** consumer check (Item 3b), not merely a source quote.

⚠️ **ID NOTE — the deposit filename is decided AT DEPOSIT.** Slug+date name form; id read from `id_sequence` at deposit, never at authoring (the thrice-proven consumed-in-window class).

---

## Why this exists — the cycle terminates on its own noise, and the criterion cannot say so

The shape decision, reserved since session 24, was held 2026-08-09 and settled **five** questions. This plan lands **D1, D2 and D3** as doctrine. The other two have stated landings elsewhere and are named here so all five are accounted for (panel seat 1): **D4** (five seats, at least one register-plain; seat count and model tiering RESERVED pending a clean A/B) changes no doctrine — §2.6 already reserves composition to the CEO — and is applied by this plan's own panel; **D5** (measure convergence across plans) is instrumentation routed to the funnel, and is the reason 259 flips PARTIAL rather than whole.

**The measurement the decision rests on.** §2's current doneness criterion is *rotation to an unexamined region coming back dry*. Two things are now known about it:

1. **The "unexamined region" half is incoherent** (LESSONS 227). A walk is every lens over the whole artifact, so after walk 1 no unexamined region exists — a pass can never again be justified by naming one.
2. **The cycle reaches dry, but it reaches it on its own noise.** Five independent measurements of the late-pass self-inflicted share: 6-of-9 → 8-of-8 → 2-of-2 (proposal 238); ~40% fold-residue at exec-324; **14 of 19** at exec-330 with walk 4 at 0-for-3 pre-existing (LESSONS 227); **3 of 4** at exec-332; 10-of-10 ACID passes catching a culmination-introduced defect (proposal 247).

⚠️ **A premise correction carried deliberately into the record:** proposal 259 — the cluster's centerpiece — argues the dry condition is *unreachable by construction*, citing four consecutive plans that missed it. **That premise has since been falsified:** 322, 324, 329 and 330 all closed literal-dry, 330's "earned rather than authored." This plan therefore does NOT adopt 259's stated rationale. It adopts 259's *remedy* (close on composition; judged stops normal) on the corrected grounds above — the criterion's defect is cost and coherence, not unreachability. Recording which half of a cited proposal survived is the point; a codification that quietly inherits a falsified premise carves it into doctrine.

**The cost argument, and why it is superlinear.** Every fold is an edit no lens has read — §2.6 already states this — so each pass manufactures the workload of the next. Cutting a pass removes both its own cost and the damage it would have created downstream.

**Why D2 is inseparable from D1.** A composition bar stops when only record-class findings remain. That is precisely the class the shop's terminal blind spot conceals: a walk certifies everything except the paragraph recording it (proposal 271), and the Cycle Log is the region rewritten most and read least (proposal 258). **Adopted alone, D1 would make the known blind spot the stopping condition.** The CEO ruled the pair mandatory in the same amendment; this plan honours that — E3 and E4 are not optional riders.

**Routing:** the corpus path proper (§6) — LESSONS entries → forge ingest → cycle 311 classification → Gate 1 routed the seven `reference|backlog` INTO this reserved decision → the decision was held → this plan codifies. No §6 deviation to declare. ⚠️ Gate 1 routed them `backlog` meaning *parked pending the decision*; the decision has now consumed them, so **route flips to `codify` alongside status** (see the G-tasks) — leaving `backlog` would state they are still parked.

## Scope — one doctrine file, seven edits plus version/History; one scoped 7-row flip; nothing else

- **Edits exactly ONE existing file:** `/Users/marklehn/Developer/GitHub/DRAFTING_CYCLE.md` (root repo), via E1–E5 and E7–E8, plus E6's version/History row. **The AFTER text is GIVEN — the agent PLACES it, never composes it.**
- **One DB write:** a scoped `UPDATE` over exactly seven ids at `/Users/marklehn/Developer/GitHub/lessons-forge/lessons-forge.db`. ⚠️ **DOWNSTREAM EFFECT NAMED — it is not status-cosmetic, and this plan changes TWO consumer-visible fields where clone origin 330 changed one** (330 carried this statement, twice; this clone dropped it and panel seat 2 restored it having MEASURED the answer). `_TERMINAL_STATUSES = frozenset(('implemented','rejected','superseded','reference'))` at `lessons-forge/src/lessons_forge.py:31`, so `reference → implemented` is **terminal → terminal** and the ingest update path's stale-vs-flag behaviour is unchanged; the only `route` READER is a report query filtered `WHERE p.status IN ('proposed','ambiguous')` (`:538`), so `backlog → codify` is invisible to it. **The write is behaviour-neutral — verified, not assumed, and QA re-asserts it from source.**
- **No code edit.** No `.py`, no gate, no template, no other doc. Needing one means the premise failed → HALT. (Declared `.sql` files are DATA for sqlite `.read`, not code.)
- **§6's coordinate-doctrine-and-gate:** ⚠️ **a gate question IS owed here and is answered explicitly, not waived.** §4's `plan_lint` checks read the header tier line and the `## Drafting Cycle` block; the closing check WARNs when the last lens line carries a fold-token and no `dry` token. **Under E1's bar a correct plan may legitimately close on a judged stop rather than a dry line — and the lint's closing check then WARNs on a CORRECT plan.** ⚠️ **Measured by construction at lens 4, not reasoned** (LESSONS, the claim-survived-every-pass entry — ⚠️ cited by LINE number, unlike the ordinal `LESSONS 227` above; two different units written identically, flagged at panel seat 4: a claim survived five warm walks, five ACID passes and a lint run because every pass READ it instead of opening the gate). The precise mechanism, **corrected at panel seat 1 after re-reading `plan_lint.py`** — the check does NOT read the Closing line: it finds the **last LENS RESULT line** before `**Closing:**` and WARNs if that line carries a fold-token and no `dry` token. The mandate that puts the token there is **A8′** (each per-lens line carries its origin split), not A1′'s Cycle-Log sentence. **So the amendment mandates the exact token that triggers the WARN** — systematic, not incidental. Boundary results: a per-lens line omitting the split does NOT warn (but is non-compliant); and the multi-lens `- All lenses, wN:` form is unparseable by the lens regex, which does **not** mean silence — it falls through to the legacy closing-prose branch, which fires unless the Closing line carries a `dry` token, and A8′ does not require one. So a compliant multi-lens judged-stop close CAN warn, by a different code path. This is open FORWARD row 39, which the amendment now gives a second reason to close. This plan does NOT change the gate (warn-first is §4's declared posture and a WARN blocks nothing). It **declares the divergence in the History row and records the owed register row in Step 1's dev log** so the gate half is scheduled, not forgotten — ⚠️ **not via the ledger channel**, which writes to this project's register rather than the bellows one where the linter's queue lives (ACID a2); it lands by Rule 42 direct edit at wrap. ⚠️ **Which clause licenses the deferral — corrected at panel seat 2.** §6's deferral licence is scoped to *a codification that touches §1 or §4*, and this plan touches **neither**. The clause its edits DO fire is `## When this file changes`: *keep the `plan_lint` self-check (§4) in lockstep with §1/§3* — E4 and E8 edit **§3**, and that clause carries **no** deferral licence. The disposition, stated rather than assumed: §3's edits add a Cycle-Log paragraph and a worked closing form; **neither changes any structure `plan_lint` parses** (§4 reads the header tier line, the lens-name set, the panel line and the Closing line — none of which E4/E8 alter), so the lockstep is satisfied by a no-gate-change statement, not by a deferral. The separate §4 divergence above is a consequence of §2's new bar, is warn-first, and is carried by the register row. Silently shipping a doctrine that makes the linter lie is what §6 forbids; saying which clause applies and why is the point.

---

## Method + boundaries

- **HALT ROUTING — the inputs each step reads; if any is missing or unreadable, HALT the step that needs it and name it, never improvise** (the 306/309/329/330 list form): Step 1 reads `/Users/marklehn/Developer/GitHub/DRAFTING_CYCLE.md` and this plan file. Step 2 reads this plan file, the Step-1 dev log, **the LIVE `/Users/marklehn/Developer/GitHub/DRAFTING_CYCLE.md`** (P0.4 shasums it and Task M's entire mapping greps it), and the DB at its canonical absolute path. Step 3 reads all of the above plus `/Users/marklehn/Developer/GitHub/RULE_20_SELF_CHECK_BLOCK.md`, `/Users/marklehn/Developer/GitHub/bellows/scripts/plan_lint.py` (item 5), `lessons-forge/src/lessons_forge.py` (item 3b), and **Step 2's committed evidence deposits** — ⚠️ an unreadable deposited capture is a HALT, never licence to re-derive it — **EXCEPT where Step 2's dev log names the DEGRADED path, in which case the per-path deposit set governs and the absent captures are expected** (330 carries this carve-out; this clone kept the rule and dropped the exception while ADDING the path that needs it — panel seat 4; without it the false HALT lands on the crash-recovery branch, where the flip has already committed). ⚠️ **This list was stale at authoring — panel seat 2 found the doctrine file missing from Step 2's row, on the plan whose own rule is to re-derive it.** **Re-derive this list from the steps as written before running** (the stale-routing class — the 309 rule).
- ⚠️ **Three steps, deliberately — a stated deviation from clone origin 330's two-step shape.** 330 placed its doctrine edits and its flip in one step. Here the flip asserts that the doctrine *implements* all seven proposals, and that claim is only true of the wording that actually lands. Splitting puts a verdict gate between them so the Planner reads the landed doctrine before authorizing the flip. **The coupling is the reason; do not "simplify" it back to two steps.**
- ⚠️ **Every `**Deposits:**` filename is the DECLARED deposit, matched by basename. Do NOT re-date any at run time** — the slug date stays even if the run date differs; the ONLY place the live date is used is E6's version/History text (the 320 no-re-date clause).
- ⚠️ **ONE register row is owed at wrap, by Rule 42 direct edit, and it does NOT travel the ledger channel** (ACID a3 collected this; panel seat 4 reduced it from two; panel seat 5 caught the bolded lead-in still reading TWO/NEITHER after the strike below — **wrap is a skim-the-bold operation**, and a wrap agent reading only the header would file the false row-41 entry this fold exists to prevent): (i) the **new deferred-gate row** for `plan_lint`'s closing check, whose text Step 1 records verbatim in its dev log, owed to **`bellows/knowledge/FORWARD.md`** — the channel would file it in this project's register instead; ⚠️ **ONE row, not two — corrected at panel seat 4.** ~~(ii) row 41's closure~~ is **NOT this plan's to close**: row 41's owed work is an edit to **§4**, and this plan touches neither §1 nor §4. It belongs to the held `dc-s4-prose` plan, which names it as the deferral it discharges. Marking it `closed-by-plan-<this id>` would be a **false entry in the register the shop uses to find undone work**, retiring the tracking for the one §4 defect this plan's own Scope says makes the linter lie (Rule 42 closes a row only on a closing plan at `lifecycle_state='closed'`). ⚠️ If the plan stops after Step 1, (i) is still owed and only Step 1's dev log carries it.
- ⚠️ **A stop between Step 1 and Step 2 is a CONSISTENT state, not a broken one (stated at ACID):** the doctrine is live at v2.0 while the seven proposals still read `reference|backlog`. The corpus lagging the doctrine is recoverable by re-dispatching the flip; the reverse — proposals marked `implemented` against doctrine that never landed — is the state the three-step split exists to prevent. Do not "fix" the lag by hand-editing the DB.
- ⚠️ **Verdict-window posture:** from the Step-1 doctrine commit until close, **v2.0 GOVERNS the shop**. A later HALT holds v2.0 live for the CEO — it is NOT a rollback instruction. Never `git restore`/`revert` the doctrine or anything else on a HALT; rollback is a CEO decision, not an agent action (the 320/329/330 posture).
- ⚠️ **The held §4-prose plan re-bases.** `draft-executable-dc-s4-prose-2026-08-09.md` is written against `1.8 → 1.9`. This plan takes **2.0**, so that draft's A0 version precondition and its History row become `2.0 → 2.1`. Named here so the re-base is scheduled, not discovered. ⚠️ Its History row must also drop the "1.9" it currently predicts — a re-base that fixes the precondition and leaves the row is the half-applied-fold class.

---

## How to Run This Plan

**Bootstrap prompt:**
```
Read the plan at knowledge/decisions/in-progress-executable-<id>.md (the daemon renames the deposited placeholder on claim). Execute Step 1 ONLY. After completing Step 1, STOP and wait for my confirmation.
```

---
---

## STEP 1 — DEV (place the seven doctrine edits + the version/History row, commit)

> **FIRST — post a short visible chat message (1–2 sentences) confirming you are starting this plan.** Do NOT rename this plan file. ⚠️ **Every command touching the doctrine file uses the ABSOLUTE operand `/Users/marklehn/Developer/GitHub/DRAFTING_CYCLE.md` — never a relative filename** (the 309/320 discipline). Read the live file at each anchor BEFORE editing; quote the exact before/after for each edit in the dev log.
>
> **Task A0 — pre-edit preconditions (runs FIRST, unconditionally, before the cleanliness check — the state it exists for is a crashed run).**
> (1) **Already-present probe / re-entry key:** `grep -c -F "shape-amendment-2026-08-09"` against the absolute operand. **≥1** → a prior run committed the History row: this is a **re-entry** — verify each edit below is already present, skip any that are, proceed to the dev-log duties with pins re-derived from the live file; before-texts come from `git -C /Users/marklehn/Developer/GitHub show <recorded-hash>^:DRAFTING_CYCLE.md`, where `<recorded-hash>` is `git -C /Users/marklehn/Developer/GitHub log -1 --format=%H -- DRAFTING_CYCLE.md` and that commit's message MUST name the slug. ⚠️ **Before declaring a non-naming commit FOREIGN, distinguish it from your own uncommitted work** (panel seat 3): if the slug appears in the working tree but NOT in `git -C /Users/marklehn/Developer/GitHub show HEAD:DRAFTING_CYCLE.md`, the true state is *a prior run wrote the row and died before committing* — a different diagnosis from a third-party edit. ⚠️ **Recoverable, but NOT BY YOU (panel seat 5, which found this state named as recoverable with every branch leading to a HALT and no route — the gap where an agent improvises):** HALT, deposit the resume-sweep table, name the state as *edits present, uncommitted, no slug commit*, and **do not commit, restore or revert.** Committing that working tree would carry doctrine edits no pin, no earnability probe and no anchor-uniqueness check has validated, under a message naming the slug — indistinguishable from a clean Step 1 to every downstream check. Re-dispatch is the CEO's call. Only a committed newest doctrine commit that does not name the slug is foreign → HALT. ⚠️ On this shim a zero-match `grep -c` prints `0` and EXITS 1 — **the printed count is the assertion**; never `&&`-chain it or read its exit status as failure.
> (2) **Cleanliness:** `git -C /Users/marklehn/Developer/GitHub status --porcelain -- DRAFTING_CYCLE.md` must be empty; dirty on the FRESH path → HALT.
> (3) **Version:** the live `**Version:**` line must read `1.8` on the fresh path; anything else with slug-count 0 means an in-window bump by another actor → HALT (the edits and History row need re-basing).
> (3b) ⚠️ **BOTH-PATH checks and a NONE-MATCH catch-all** (330/329 form, dropped by this clone and restored at panel seat 2). Cleanliness (2) and version (3) run on **both** paths with per-path expectations — `1.8` fresh, `2.0` re-entry — because a doctrine file dirty ON TOP of the slug-named commit must be reported LOUDLY and never swept into any commit, not silently accepted. ⚠️ **If the observed state matches NEITHER the fresh nor the re-entry branch, HALT and report the full observed triple (slug count, version, porcelain) — never improvise a route.** On any Step-1 HALT where the doctrine is DIRTY or any A*n*′ text is already partially present — ⚠️ **gated on the OBSERVED state, not on edits attempted in this run** (panel seat 3: a crash between the edits and the E6 commit resumes into the fresh branch having attempted nothing, so a this-run gate produces no artifact in exactly the crash it was written for) — run a per-edit `grep -F` sweep of the A*n*′ texts against the live file and **DEPOSIT the landed/not-landed table** as `knowledge/qa/evidence/shape-amendment-2026-08-09/resume-sweep.txt` — chat output is not durable, and a HALT that leaves nothing on disk leaves the CEO nothing to read (this file joins Scope on that path only).  ⚠️ **Moved here at the confirming walk:** it was laid out AFTER (4) while retroactively amending (2) and (3), which an agent had already executed two items earlier. Label kept stable — fifteen sites reference these numbers, and §6's numbering-is-stable-for-life spirit applies to a plan's own anchors too.
> (4) **Anchor uniqueness (FRESH path only)** — `grep -c -F` against the absolute operand, each count exactly 1, and note the two kinds are NOT interchangeable: **whole-line pins** — the full pre-edit texts **A1**, **A5** and **A6**, which E1/E5/E7 replace; **placement anchors** — the literals `Execute against real data.` (E2's insertion point), `The compact form is` (E4's) and `**Closing:** full walk 3 dry` (E8's, which locates the first fence line strictly after it); plus the line `## History`. ⚠️ A1, A5 and A6 are the only pre-edit texts retyped in this plan; A2, A4 and E8's fence are located by anchor and never retyped, so do not look for them as full-line pins.
> (4b) **Clause-key integrity — added at ACID a2, and it protects a coupling walk 2 created.** Step 2's seven pinned clause keys are literal substrings drawn from A1′/A2′/A3′/A4′. Nothing otherwise binds them: a later fold to any A-prime — a cold-panel wording change, say — would silently strip a key, and Step 2 would then HALT on a *correct* doctrine with a confusing message. **Before any edit, assert each of the seven keys listed in Step 2 is a substring of this plan's own pinned A-prime block, and deposit the seven results.** A key that no longer matches its source means the plan drifted after authoring → HALT before writing anything, not after the doctrine has landed.
> (5) **Post-condition earnability — prove each assert CAN fail before the edit (FRESH path only; see the re-entry clause at the end)** (the probe-earnability class, exec-330): the slug count at (1) is 0, and each of these must print 0 against the pre-edit doctrine — `fold-introduced`, `covers, it does not target`, `Re-read the closing record`, `part of the artifact, and every walk covers it`, `RE-OPENS THE WALK`, `Once the sequential walk meets`, `judged stop`. Any probe already satisfied pre-edit is blind on arrival → HALT. **Deposit all seven counts;** they are the proof that each post-condition below can fail. ⚠️ **ON THE RE-ENTRY PATH this check is run against `git -C /Users/marklehn/Developer/GitHub show <recorded-hash>^:DRAFTING_CYCLE.md`, expecting 0 THERE and ≥1 live** — panel seat 5 found this clause unscoped while (4) beside it is fresh-only, so on re-entry the slug count is ≥1 and all seven literals return ≥1 by definition: **the recovery branch A0(1) exists to enable was unreachable through its own preconditions.**
>
> ⚠️⚠️ **STRIP THE BLOCKQUOTE PREFIX — the pinned texts below are stored as plan-side blockquote lines and MUST NOT enter the doctrine that way** (329's mandate, dropped by this clone and restored at panel seat 2). For every A*n*′ you place: **strip one leading `> ` (greater-than, space); a bare `>` becomes a blank line.** ⚠️ **The two `---BEGIN/END PINNED TEXT---` sentinel lines are plan-side layout and are NEVER inserted.** This is not cosmetic: an unstripped insert corrupts seven doctrine lines into blockquotes, and every QA presence grep would still pass, because QA reads the same prefixed literal out of this plan. The QA step asserts the doctrine-side sentinel count is 0 with the plan-side count of 1 as its positive control.
>
> **Edits.** ⚠️ **Two mechanisms, and confusing them corrupts the file:** **E1, E5 and E7 REPLACE one physical line with one physical line**; **E2, E3, E4 and E8 INSERT a new line after a located anchor and modify nothing.** A*n* is the pre-edit text, A*n*′ the post-edit text; both are pinned verbatim between the sentinels below and the agent composes nothing. (An earlier draft said every edit was a replacement — corrected at walk 2 lens 1; an agent acting on that would have overwritten the anchors E2/E3/E4/E8 are supposed to leave intact.)
> - **E1 — §2 doneness criterion:** replace A1 with A1′ (the composition + fold-origin bar; retires rotation-to-an-unexamined-region; judged stops become a normal declared outcome).
> - **E2 — §2.7 coverage-not-aim (D3, proposals 233/246):** INSERT A2′ as a new bullet immediately AFTER the verbatim line A2 (the `**Execute against real data.**` bullet), preserving surrounding blank-line structure. *(E2/E3/E4/E8 are INSERTs; E1/E5/E7 are whole-line REPLACEMENTS. Place E2 first, then E3 immediately after it.)* ⚠️ **Blank-line structure is MANDATED at every insert site, not left to placement** (panel seat 2): E2 and E3 are consecutive `- ` bullets in a bullet run — **no** blank line before, between, or after; E4 and E8 are standalone paragraphs — **exactly one** blank line above and below each. This makes the addition count exactly derivable rather than computed by the agent from what it just did.
> - **E3 — §2.7 closing-record re-read (D2, proposal 271):** INSERT A3′ as a new bullet immediately AFTER the bullet placed by E2.
> - **E4 — §3 Cycle Log as covered region (D2, proposal 258):** INSERT A4′ as a new standalone paragraph immediately AFTER the verbatim line A4 (the `The compact form is **load-bearing**` paragraph), separated by one blank line. ⚠️ **Changed from a replacement at lens 1:** appending the new sentences onto A4 would have required the agent to COMPOSE the merged line — which this plan's own no-composition rule forbids, and which no whole-line byte-compare could verify. As an insert, A4′ is a complete pinned line and A4 is never touched.
> - **E5 — §2.6 aim/coverage reconciliation:** replace A5 with A5′. ⚠️ **Load-bearing, not cosmetic:** §2.6 currently instructs aiming the next walk at the quiet step, which E2's covers-not-targets rule contradicts outright. Shipping E2 without E5 lands a doctrine that argues with itself.
> - **E7 — §2.6 panel precondition:** replace A6 with A6′. *(There is deliberately no A7 — anchor ids and edit ids are offset from E7 onward; do not go looking for one on the re-entry path.)* ⚠️ **Found at lens 2 and load-bearing:** §2.6 currently convenes the cold panel *after the sequential walk goes dry*. Under E1's bar a walk may legitimately never go literally dry, so as written the panel's precondition becomes unreachable and a T2 author has no stated trigger. Shipping E1 without E7 strands the panel.
> - **E8 — §3 worked form for the new close:** INSERT A8′ as a new paragraph immediately AFTER the verbatim line `` ``` `` that CLOSES the example block in §3 (located as the first ``` line strictly after the `**Closing:** full walk 3 dry` line — assert exactly one such line at that position) and BEFORE the `For **T0** the block collapses…` line. ⚠️ **The example is NOT mutated:** the dry close it shows stays valid and stays the default. A8′ adds the judged-stop form beside it, because authors copy the example and a bar with no worked instance does not get followed.
> - **E6 — version + History:** bump `1.8 (2026-08-09)` → `2.0 (<date>)` from `date +%F` (never the slug's date, never the long `date` default form); the trailing "Amended only through the Iteration Protocol (§6)." sentence survives verbatim. INSERT a new FIRST bullet in `## History` (above the 1.8 bullet), ONE physical line, no wrapping. ⚠️⚠️ **THIS IS A TWELVE-ELEMENT COMPOUND MANDATE — the exact shape that lost exactly one element at 328 while every daemon gate passed, and the census class of FORWARD row 40.** It is therefore specified as an ENUMERATED LIST with one mechanical assert per element at QA 1(e); write it element by element and tick each off, never as one flowing sentence checked by eye:
> **(1)** opens `**2.0 (<date>):**` · **(2)** names the slug `shape-amendment-2026-08-09` · **(3)** names all seven proposal ids `233, 238, 246, 247, 258, 259, 271` · **(4)** states the new bar — record-class-only AND predominantly fold-introduced, origin split stated as a number — and that judged stops are a normal outcome ⚠️ **without containing the string `a normal outcome, not a deviation`, which is 259's pinned clause key: a second occurrence breaks QA item 4's exactly-1 assert, which runs AFTER the flip** (panel seat 5) · **(5)** states the D2 pair (the closing-record re-read and the Cycle Log as a covered region) as **mandatory with D1, not optional riders** · **(6)** states D3's codification (a walk covers, it does not target) · **(7)** records the routing — corpus path proper, **no §6 deviation** · **(8)** ⚠️ records the **259 premise correction**: remedy adopted, stated rationale NOT adopted, naming that four plans closed literal-dry after the proposal was written · **(9)** ⚠️ states the **§4/`plan_lint` divergence** and that a register row carries the deferred gate half · **(10)** ⚠️ **NAMES THE AMENDED UNITS — `§2`, `§2.6`, `§2.7`, `§3`.** §6 requires each row to name the units it changed, and **every existing History row does** (panel seat 2) · **(11)** ⚠️ **NAMES THE INHERITORS** — `## When this file changes` requires *note which in-flight plans inherit it*, and the 1.7 and 1.8 rows both discharge it; 329's own panel closed this exact gap one version earlier. Name the held `dc-s4-prose` plan (re-basing 2.0 → 2.1) and the DC-surgical batch (carrying §2.8's oscillation rule) · **(12)** ⚠️ **RECORDS THE DB DISPOSITION** in the 1.8 row's form — the seven proposals flipped to `implemented|codify` — so the changelog can answer from itself whether the corpus flip happened for this version (panel seat 4).
> ⚠️ Element (8) is the one most likely to be dropped — it is the only element that corrects rather than asserts, and this plan's own Why section exists to stop a falsified premise being carved into doctrine. A row missing (8) defeats the plan's stated purpose while passing every other check. The row **DESCRIBES the new text and never quotes it** — QA's presence asserts key on exactly-once counts and a quoting row would double one and HALT a correct run (the 329 seat-3/seat-5 class).
>
> ---BEGIN PINNED TEXT---
>
> **A1 (pre-edit, §2):**
>
> Walk the lenses **in order, one pass per lens per walk.** Fold all accepted findings after each pass. Re-run a lens only on a **subsequent** walk — a fold's defect is usually caught by a *different* lens on different evidence. The cycle is **done** when a full walk returns zero or only-minor findings **over a region the previous walk did not touch**. ⚠️ **A falling finding-count is NOT the convergence signal** — severity falls because the same regions are being re-read, not because the artifact is sound. Budget for a cold panel's yield staying **flat**: a five-lens sequential panel returned 11 / 12 / 12 / 12 / 12 with no decay, roughly a third of each round being defects the immediately preceding round's folds introduced. The signal is **rotation**: a walk aimed at a previously unexamined region coming back dry. The **last event before deposit must be a lens pass, not a fold**; if the final pass folded anything, a brief confirming pass supplies the closing lens pass (expected dry; a new material finding re-opens the walk). Fold-and-deposit **exactly once**.
>
> **A1′ (post-edit, §2):**
>
> Walk the lenses **in order, one pass per lens per walk.** Fold all accepted findings after each pass. Re-run a lens only on a **subsequent** walk — a fold's defect is usually caught by a *different* lens on different evidence. The cycle is **done** when a full walk returns findings that are **record-class only** — nothing that would change what an executing agent DOES — **and predominantly fold-introduced**, meaning defects this cycle's own folds created rather than defects that pre-existed it. **Both conditions are required, and the origin split is stated as a number in the Cycle Log** (`N of M fold-introduced`). ⚠️ **A falling finding-count is NOT the convergence signal** — severity falls because the same regions are being re-read, not because the artifact is sound. Budget for a cold panel's yield staying **flat**: a five-lens sequential panel returned 11 / 12 / 12 / 12 / 12 with no decay, roughly a third of each round being defects the immediately preceding round's folds introduced. **The signal is the noise floor, not an unexamined region:** after walk 1 there is none — a walk is every lens over the whole artifact — so no pass may be justified by naming one. A pass instead names the **new surface the last culmination created**, and reports the origin split of what it found; a pass whose findings are mostly its predecessor's fold damage is the noise floor, not progress (measured: 14 of 19 at exec-330, walk 4 at 0-for-3 pre-existing; 3 of 4 at exec-332; ten of ten ACID passes catching a culmination-introduced defect). The **last event before deposit is either a dry lens pass or a declared judged stop meeting the bar above** — **a judged stop is a normal outcome, not a deviation**, recorded with its reasoning. ⚠️ **A finding that is not record-class RE-OPENS THE WALK:** the bar is unmet and the cycle continues. Folds made on a closing walk that DOES meet the bar are record-class by the bar's own condition; those landing in the closing record are read by the closing-record re-read (§2.7), and **any that land elsewhere are enumerated individually in the residue list** — the re-read covers the record, not the whole artifact, and must not be cited as though it did. ⚠️ **This is a stated relaxation, not an oversight:** the prior criterion required a further confirming pass whenever the final pass folded anything, so a qualifying close may now deposit with record-class edits no lens has read. On T2 the cold panel supplies that reader (§2.6 — the panel is not waived by a judged stop). **On T1 there is no such reader, so a T1 judged stop rests on the residue enumeration and the closing-record re-read alone.** ⚠️ **A judged stop is auditable or it is not a stop.** Both of the bar's conditions are the author's own judgement, and the author is the party who wants to finish, so a bare assertion of record-class-ness is not a close — the older criterion was checkable by anyone (zero findings is observable) and its replacement stays checkable only by showing the work. **The Closing line therefore carries the origin split as a number and NAMES each residue finding's class in a clause apiece** (`3 record-class: two count-word lags, one stale label`); the per-finding detail — what each was, where, and which fold produced it — lives in the scratchpad walk register, which the closing-record re-read reads. ⚠️ **This is the ONE bounded exception to §3's compact-form rule, and §3 states it** — the bar cannot be audited from a log that may not name what it stopped on. Fold-and-deposit **exactly once**.
>
> **A2 (pre-edit anchor, §2.7 — INSERT AFTER this line; reproduced here only as the placement anchor, NOT modified):**
>
> The full `- **Execute against real data.** …` bullet, ending with the parenthetical that cites diag-308 and the (D) standard adopted 2026-08-06. ⚠️ The agent locates this line by `grep -n -F "Execute against real data."` (exactly one hit asserted at A0(4)) and inserts after that line number — the bullet is long and is NOT retyped here, so no accidental near-duplicate can be introduced.
>
> **A2′ (new §2.7 bullet):**
>
> - **A walk covers, it does not target.** A walk is every lens over the WHOLE artifact; a pass aimed at selected regions is not a walk and may not be recorded as one. Aim manufactures a falling count that reads as convergence and is not: same artifact, same reader, same day, the only variable being aim — the aimed pass returned 3 findings leaving twelve regions unreached, and the untargeted pass that followed returned 8 with zero unreached. The cost argument for aiming does not survive either — a complete five-lens walk covered twenty-seven sections for roughly the token budget of one targeted round, and a CRITICAL defect had by then survived nine aimed rounds in the one region no reader was ever pointed at. **Coverage is the unit of a walk; a count is not.** Directing extra attention within full coverage is legitimate; narrowing the pass is not.
>
> **A3′ (new §2.7 bullet, placed immediately after A2′):**
>
> - **Re-read the closing record after the close.** A walk certifies everything except the paragraph that records it — the closing prose is written after the last pass has run, so it is pass-unexamined **by construction**, a structural blind spot rather than an oversight. After the final pass and its folds, re-read the closing record alone — the Closing line, the per-lens summary lines, the status header — adversarially and against the artifact. This is a short read of a paragraph, never a walk, and it is **mandatory at EVERY close — dry or judged stop alike.** It is most load-bearing on a judged stop, because the bar's own condition guarantees the residue is record-class; but the blind spot is structural, so a dry close does not escape it. (Measured: the shop's first post-**dry**-close re-read raised 2, both in closing prose, one claiming what its cited precedent declines to claim — the measurement comes from the DRY branch, which is why the rule may not be scoped to the other one.)
>
> **A4 (pre-edit anchor, §3 — INSERT AFTER this line; NOT modified):**
>
> The full paragraph beginning `The compact form is **load-bearing**` and ending `…evaluated as if the QA step had said it.` ⚠️ The agent locates it by `grep -n -F "The compact form is"` (exactly one hit asserted at A0(4)) and inserts a blank line plus A4′ after that line number. The paragraph is long and is deliberately NOT retyped here, so no accidental near-duplicate can be introduced and nothing in it can be lost.
>
> **A4′ (new §3 paragraph, complete and standalone):**
>
> **The Cycle Log is part of the artifact, and every walk covers it — name it explicitly in the walk's coverage rather than assuming it was swept.** The record is rewritten more often than any other region and read less often: attention follows what each phase changed, never what the changes accumulated into. (Measured: a walk that finally read a Cycle Log no lens had covered returned six of its eight findings there, every one the record decaying while the artifact converged.) **Count record-decay findings separately from artifact findings** in the per-lens lines — they are the class §2's bar reads, and merging them into one total hides exactly the signal it needs. ⚠️ **One bounded exception to the compact form above:** a cycle closing on a judged stop names each residue finding's CLASS in a clause apiece on the Closing line, alongside the origin split. The per-finding detail stays in the scratchpad register; what enters the block is a class list, not narrative.
>
> **A5 (pre-edit, §2.6):**
>
> Before each walk, identify which step mutates and when it was last examined. If a walk's folds all land in one step, aim the next walk at the other step deliberately — review-target rotation prevents the quiet step from accumulating unexamined risk while the noisy one absorbs all attention. **The panel's yield does not decay, and that is structural.** Every fold is an unreviewed edit, so folding N findings creates a fresh unreviewed surface of N edits which the next reader is the first to see. Do not read a flat or rising round as panel failure, and do not read a falling one as convergence.
>
> **A5′ (post-edit, §2.6):**
>
> Before each walk, identify which step mutates and when it was last examined. If a walk's folds all land in one step, give the other step deliberate attention on the next walk (**review-target rotation**) — **within full coverage, never instead of it** (§2.7's covers-not-targets rule): directing attention is legitimate, narrowing the pass is not, and the quiet step must not accumulate unexamined risk while the noisy one absorbs all attention. **The panel's yield does not decay, and that is structural.** Every fold is an unreviewed edit, so folding N findings creates a fresh unreviewed surface of N edits which the next reader is the first to see. Do not read a flat or rising round as panel failure, and do not read a falling one as convergence.
>
> **A6 (pre-edit, §2.6):**
>
> After the sequential walk goes dry, rotate the **reviewer**, not the lens: run the five lenses **cold** — fresh-context readers given only the artifact plus repo read access, **sequentially** (a concurrent cold run is a panel pass, not a walk — cumulation lives in the draft, so sequential preserves it). Author-verify cold findings; a cold reader can misread deliberate design as a defect.
>
> **A6′ (post-edit, §2.6):**
>
> Once the sequential walk meets §2's bar — dry, or a judged stop with its residue enumerated — rotate the **reviewer**, not the lens: run the five lenses **cold** — fresh-context readers given only the artifact plus repo read access, **sequentially** (a concurrent cold run is a panel pass, not a walk — cumulation lives in the draft, so sequential preserves it). Author-verify cold findings; a cold reader can misread deliberate design as a defect. ⚠️ **The panel is not waived by a judged stop** — a stop reached on record-class residue is exactly the state a cold reader is best placed to examine, and the panel's own findings re-open the walk on the same terms as any other lens pass.
>
> **A8′ (new §3 paragraph, placed after the example block):**
>
> A cycle closing on a **judged stop** rather than a dry pass records the same block with two additions: each per-lens line carries its origin split (`w4 3 folded — 1 pre-existing, 2 fold-introduced`), and the Closing line states the bar it met, enumerates the residue by class, and gives the reasoning — for example, `**Closing:** w4 met the bar — 3 findings, all record-class (two count-word lags, one stale label), 2 of 3 fold-introduced; closing-record re-read run, dry; judged stop, deposited once.` The dry form above remains valid and remains the default; this is the form for the other legitimate outcome.
>
> ---END PINNED TEXT---
>
> **Task E0 — PRE-COMMIT DENYLIST, scoped to the governance doctrine set** (330's, dropped by this clone and restored at panel seat 2; 330 deliberately NARROWED it from an all-root-`*.md` form that false-HALTs under normal parallel-terminal topology — inherit the narrowed result). Immediately before the commit, HALT iff any of these is dirty at the root: `PLANNER_TEMPLATE.md`, `RULE_20_SELF_CHECK_BLOCK.md`, `READONLY_AUDIT_CONTRACT.md`, `SPECIALIST_TEMPLATE.md`, `INTERMEDIATE_DECISION_PHRASES.md`. Foreign governance activity in-window is a re-base risk, and this plan's blast radius is wider than 330's.
>
> **Commit at the governance root, pathspec-limited** (the root repo carries unrelated working state): `git -C /Users/marklehn/Developer/GitHub add DRAFTING_CYCLE.md && git -C /Users/marklehn/Developer/GitHub commit -m "<message naming the slug>" -- DRAFTING_CYCLE.md`. **Output Receipt required** (Rule 15's producer — the next step reads its status; without it Step 2 has nothing to check). Record in the dev log: an explicit `Output Receipt: Complete` line naming every deliverable produced, the path-scoped commit hash, the post-commit `shasum -a 256` DOC_SHA, the PLAN_SHA (`shasum -a 256` of this plan file as read this step — sole carrier of the pinned text), all A0 outputs including the (5) earnability zeros, exact before/after for **each of E1–E8**, the `--numstat` pair, and **three pre-existing sentences quoted verbatim from regions no edit touches** (the retained-material keys).
>
> ⚠️ **TWO REPOS, TWO COMMITS — state this before writing anything** (the 330 topology; the `phase-commits-assert-the-repo` class). The doctrine file lives in the ROOT repo and is committed with an explicit `git -C /Users/marklehn/Developer/GitHub` operand. The deposits live in the **dispatch tree**, which under bellows is a run-specific worktree whose absolute path CANNOT be hardcoded. Derive it at the top of the step and **RECORD ITS LITERAL VALUE in this step's deposit**: `TREE="$(git rev-parse --show-toplevel)"; echo "$TREE"`. ⚠️⚠️ **Shell state does NOT persist between invocations** (330's environment fact, dropped by this clone and restored at panel seat 2) — a bare `$TREE` in a LATER command expands EMPTY, turning `git -C "$TREE" add` into `git -C ""` and a deposit write into a write at the filesystem root. **Every later use either re-derives it in the SAME invocation or uses the recorded absolute literal.** The same rule already applies to the `.sql` files, which are not shell at all. ⚠️ **Do NOT assert that `TREE` ends in `lessons-forge`** — bellows worktrees live under `bellows/.bellows-worktrees/`, so a legitimate dispatch root does not carry the project name in its last component (checked live at lens 3). Assert instead that `TREE` is a git toplevel containing `knowledge/decisions`, and **print `TREE` and `git -C "$TREE" remote -v` into the dev log** so the repo identity is recorded rather than assumed. A deposit written from an underived relative path is a guaranteed wrong-repo write (320's w1 lesson, and cwd does not persist between shell calls).
>
> The dev log is WRITTEN at `"$TREE"/knowledge/development/shape-amendment-dev-log-step-1-2026-08-09.md`, then staged and committed **in that repo** path-scoped as the step's final action: `git -C "$TREE" add -- <path> && git -C "$TREE" commit -m "<message>" -- <path>` (a bare pathspec commit ERRORS on an untracked file — `git add` first). This commit is SEPARATE from the doctrine commit above and lands in a different repository; do not combine them.
>
> End the step's chat output with `### Ledger Updates` carrying `#### Prompt Feedback`. ⚠️⚠️ **Emit `#### Forward Register` carrying exactly the word `NONE` — never a real row.** (ACID a2 established the mechanism; panel seat 5 reconciled the form after finding Step 1 forbidding the block while Step 3 asserted every step emitted it — two seats' folds pointing opposite ways, with nothing sweeping between them.) The reason is mechanical, not stylistic: `_apply_ledger_updates` calls `_append_forward_row(project_path, …)`, so the daemon writes the row into **this plan's project register — `lessons-forge/knowledge/FORWARD.md`** — while the deferred work is on `bellows/scripts/plan_lint.py`, whose queue is **`bellows/knowledge/FORWARD.md`** (six existing `plan_lint` rows live there). Emitting the block would file the §6 obligation in a register nobody working on the linter reads. ⚠️ A prior draft also told this step to VERIFY the row afterwards — **unsatisfiable in principle**: the daemon parses the transcript and applies ledger updates *after* worktree teardown, so nothing the step can read yet contains its own row.
>
> **Instead: record the row text verbatim in the dev log**, under a heading naming it as owed to the BELLOWS register, for a **Rule 42 direct edit at wrap** — the channel cannot reach the bellows register from this project. (Row 41 is a *different* row, owed by the held §4-prose plan, not by this one — panel seat 4.) The text to record:
>
> `- plan_lint §4 closing check vs the v2.0 judged-stop close: v2.0 requires each per-lens line to state its origin split, which contains the substring fold, while a judged stop carries no dry token. The check evaluates the LAST LENS RESULT LINE before the Closing line (lens_line_re over the Drafting Cycle block), not the Closing line itself, so its fold-without-dry condition fires on a CORRECT plan — systematically, because the doctrine mandates the trigger token. Measured by construction 2026-08-09: a canonical single-lens line stating the split WARNs; the same line without the split does not (but is non-compliant); a multi-lens All-lenses line is unparseable by the lens regex and falls through to the legacy closing-prose branch, which fires unless the Closing line carries a dry token that v2.0 does not require. Row 39 is the parse half and now has a second reason to close. Warn-first blocks nothing, so this is deferred rather than urgent, but the gate half of the §6 doctrine-and-gate pair is owed: teach the check to recognise a declared judged stop, or key it on the origin-split line instead. Deferred per §6, slug shape-amendment-2026-08-09.`
>
> **Scope:**
> - `knowledge/development/shape-amendment-dev-log-step-1-2026-08-09.md`
>
> ⚠️ **On the A0(3b) HALT path ONLY, `knowledge/qa/evidence/shape-amendment-2026-08-09/resume-sweep.txt` joins this Scope list** (the 329 conditional-scope form). ACID a4 found A0(3b) promising this file *joins Scope on that path* while the Scope block never named it — so the durable HALT artifact seat 2 added could not be deposited without a `scope_check` failure, on the one path where the CEO has nothing else to read.
>
> **STOP. Wait for CEO verdict.**

**Deposits:**
- `lessons-forge/knowledge/development/shape-amendment-dev-log-step-1-2026-08-09.md` — A0 outputs incl. earnability zeros, before/after for each of E1–E8, commit hash, DOC_SHA, PLAN_SHA, numstat pair, the three retained-material sentences.

---

## STEP 2 — DEV (the seven-row flip)

> ⚠️ **This step runs ONLY after the Planner has read the landed doctrine at the Step-1 gate.** The flip asserts the doctrine implements all seven proposals; that claim is about the wording that actually landed, which is why the gate sits here.
>
> **Task P0 — PREFLIGHT.** ⚠️ **One contract: prove this step is entering a KNOWN corpus state, from a VERIFIED predecessor, into a world that still matches what that predecessor recorded.** Nothing below is optional and the order is the order.
>
> ⚠️ **Collapsed from six separately-bolted-on tasks at the CEO's direction (§2.8's third resolution).** Task P0 had been re-folded in four consecutive passes — an ordering fix, a layout fix, a carrier pin, a referent fix — every patch correct and every one creating the next. The patches were not the problem; the region was. **Do not re-expand this into separate tasks; add to a clause.**
>
> **P0.1 — LOCATE.** `TREE="$(git rev-parse --show-toplevel)"`; assert it is a git toplevel containing `knowledge/decisions`; print `TREE` and `git -C "$TREE" remote -v` into this step's dev log. ⚠️ Each step runs in a FRESH agent context and a fresh worktree, so Step 1's derivation does not carry, and **shell state does not persist between invocations** — every later use re-derives in the same command or uses the recorded literal. Three paths are fixed and are used in the forms given, never re-derived and never abbreviated:
> · **DB** — `/Users/marklehn/Developer/GitHub/lessons-forge/lessons-forge.db` (gitignored and absent from every worktree; a bare relative name does not fail, it **CREATES an empty file** and every query then answers from nothing). Assert it exists and is non-empty **before the first query below**. Every invocation: `-bail`, `.timeout 5000`, `-readonly` on reads, exit 0 and empty stderr asserted. **No heredocs** — author each `.sql` with your file-WRITE tool and run it via `.read`, with the fully expanded absolute path written INTO the file (`sqlite3` does not expand shell variables).
> · **DOCTRINE** — `/Users/marklehn/Developer/GitHub/DRAFTING_CYCLE.md`, always absolute, always with `git -C /Users/marklehn/Developer/GitHub`.
> · **STEP-1 DEV LOG** — `"$TREE"/knowledge/development/shape-amendment-dev-log-step-1-2026-08-09.md`, carried into this worktree by the teardown merge of Step 1's commit. It is the source of every recorded value P0.4 compares against.
>
> **P0.2 — CORPUS STATE, before any dev-log read.** Read the seven rows `-readonly` from the DB. **FRESH** = all seven uniform `reference|backlog` with `status_updated_at='2026-08-09T01:20:01Z'` — proceed. **ALREADY FLIPPED** = all seven `implemented|codify` with a timestamp ≠ that value — a prior run committed the transaction and died before depositing: take the DEGRADED path (P0.5). **Anything else** → HALT naming each id's status. ⚠️ **This precedes the receipt read deliberately:** were the receipt checked first, a partial Step-1 dev log would STOP the step before this probe ran, and a crash where the flip HAS landed and the log has not would never reach the degraded path.
>
> **P0.3 — PREDECESSOR.** The dev log is clean (`git -C "$TREE" status --porcelain -- knowledge/development/shape-amendment-dev-log-step-1-2026-08-09.md` empty); its newest path-scoped commit is Step 1's, message naming the slug; and its **Output Receipt reads Complete** (Rule 15). Not Complete → STOP and report; do not proceed to P0.4 and never to the flip. ⚠️ **The carrier is pinned before its contents are trusted** — every value P0.4 uses is read out of this file, so an unpinned carrier means the pins below check the doctrine against a number an editor chose.
>
> **P0.4 — WORLD MATCHES RECORD.** `shasum -a 256` of the DOCTRINE equals the dev log's DOC_SHA, and its newest path-scoped commit equals the recorded hash; `shasum -a 256` of the claimed plan file equals the recorded PLAN_SHA. Any mismatch → HALT. ⚠️ Task M maps proposal ids onto sentences in the LIVE doctrine and the flip that follows is irreversible: without these, Task M would map against text Step 1 never wrote and the DB would assert `implemented` about a doctrine nobody approved.
>
> **P0.5 — DEGRADED PATH (entered only from P0.2's already-flipped state).** Run P0.3 and P0.4 unchanged — **the pins are never skipped; with the flip already landed they are more load-bearing, not less.** Then skip Task B, G1 and G2 entirely and go to Task M and G3. Reconstruct the outside-range capture as a post-hoc read (`id <= 273 AND id NOT IN (233,238,246,247,258,259,271)` — the literal list) and label it **post-flip, reconstructed — NOT the in-transaction capture**; the in-transaction guarantee cannot be recovered after the fact. Recover the pre-flip state from the crashed run's backup located with `find /Users/marklehn/Developer/GitHub/lessons-forge -maxdepth 1 -name 'pre-shape-amendment-*.db'` — ⚠️ **`find`, never a bare glob: zsh ABORTS on an unmatched glob**, which is this branch's own no-match case — read through `file:<path>?immutable=1` (never `-readonly`: a WAL `.backup` has no `-shm` sidecar and fails with error 14), and label it *recovered from the prior run's backup*. No backup → the pre-flip state is **LOST**, recorded plainly, never substituted with post-flip values. ⚠️ **Task B is correctly skipped here and no file named `pre-*` may be created after the flip** — a post-flip file under a pre-flip name is an artifact a later restore would trust.
>
> **Task M — the mapping, written BEFORE any write.** For each of the seven ids, locate in the LIVE post-Step-1 doctrine the **clause** that implements it and quote that clause, one row per id — **the key is a distinct sub-string, never the whole line.** ⚠️ **Clause-level, not line-level, and the distinction is load-bearing:** four of the seven map into two lines (233 and 246 both into A2′; 238, 247 and 259 all into A1′), so a line-level key would let one line's presence vouch for three ids at once and the mapping could not detect a dropped clause. Each id's key must be independently falsifiable:
> ⚠️ **The seven keys are PINNED LITERALS, not descriptions — fixed at walk 2 lens 1.** An earlier draft described each clause ("the noise-floor sentence"), which left the agent to choose its own substring; two agents would then pick different keys and QA item 4 could not re-check the same thing it asserted. Each key below was verified at authoring to occur **exactly once** across the post-edit texts and **zero times** in the pre-edit doctrine, so each is both unique and earnable:
> · **233** → `twenty-seven sections for roughly the token budget of one targeted round`
> · **246** → `3 findings leaving twelve regions unreached`
> · **238** → `is the noise floor, not progress`
> · **247** → `ten of ten ACID passes catching a culmination-introduced defect` ⚠️ **(re-keyed at panel seat 1: the earlier key was 259's composition clause, so 247's key would still have matched if A1′'s ACID/class-drift material were dropped — reproducing at clause level the very failure clause-keys exist to prevent)**
> · **259** → `a normal outcome, not a deviation` ⚠️ **PARTIAL — record it as such:** 259 has THREE asks (close on composition; **measure convergence across plans**; judged stops normal). This plan implements the first and third. The middle one is the packet's **D5**, adopted as instrumentation routed to the funnel — **not doctrine, and not shipped here.** ⚠️ **Raise this at the Step-2 verdict gate:** `implemented` is terminal, and the packet warns that if D5 is never built, D1's own three-cycle re-measurement has nothing to read. Do not flip 259 silently as though it were whole.
> · **258** → `The record is rewritten more often than any other region` ⚠️ **PARTIAL — record it as such:** 258 asks four things; A4′ implements the scheduled-coverage half and the count-record-decay-separately half, but **not** its phase-labelling of narrative blocks. Route that quarter to the DC-surgical batch alongside §2.8's oscillation rule.
> · **271** → `the closing prose is written after the last pass has run`
> Each key is located by `grep -c -F` returning exactly 1 against the live doctrine. ⚠️ **If any id's clause is absent, HALT — do not flip that id and do not flip the rest.** A partial flip is a false record in both directions: flipping an unimplemented id lies about doctrine, and flipping six of seven silently redefines the batch. (This is the coupling the three-step split exists for.)
>
> **Task B — backup, taken ADJACENT to the flip (immediately before G1, not at step start — a backup separated from the write by a long gap does not contain what the write is about to overwrite), via SQLite's own mechanism, not the filesystem:** `sqlite3 -bail /Users/marklehn/Developer/GitHub/lessons-forge/lessons-forge.db ".timeout 5000" ".backup /Users/marklehn/Developer/GitHub/lessons-forge/pre-shape-amendment-<UTC timestamp>.db"` — ⚠️ **the destination is pinned BESIDE the canonical DB, not in the dispatch tree** (ACID a3: it was an undefined placeholder, on the plan's only restore instrument). The tree is a worktree that is torn down; a backup written there is destroyed by the teardown that follows, and the DB directory is where the untracked-DB policy already keeps such files. ⚠️ **A `cp` of a live SQLite file can capture a torn state** if any writer is mid-transaction; `.backup` is transaction-consistent and is what the clone origin used. ⚠️ **Assert the backup is RESTORABLE, not merely present, and assert it by VALUE** (330's rule; restored at lens 3, then corrected at panel seat 2 which caught the restored form asserting a bare count): read it through an **immutable URI** — `sqlite3 -bail "file:<backup-abs-path>?immutable=1" "SELECT 'BK='||COUNT(*) FROM lesson_proposals WHERE id IN (233,238,246,247,258,259,271) AND status='reference' AND route='backlog';"` → must print **`BK=7`**. ⚠️⚠️ **NOT `-readonly` — panel seat 3 ran it and it CANNOT work.** `lessons-forge.db` is in **WAL** mode; `.backup` writes a WAL-mode copy with no `-shm`/`-wal` sidecars, and a read-only connection cannot create the shared-memory file, so sqlite returns `unable to open database file (14)` with no `BK=` line at all. The failure reads as *the backup is corrupt* — the exact opposite of the truth — and both obvious improvisations are harmful: dropping `-readonly` WRITES sidecars into the CEO's restore instrument, and re-taking the backup reproduces it. `?immutable=1` needs no shm and writes nothing (verified: returns `BK=7`, exit 0, no sidecars). ⚠️ **The status/route predicate is the whole mechanism** — it is what proves the backup is an INVERSE of the pending write. A bare row count passes on a backup taken AFTER the flip, which is the shop's own count-is-not-a-value-guard lesson re-committed inside the guard restored to close it, and it matters most on the degraded path, where P0.2 reads the pre-flip state out of this same file and labels it authoritative. ⚠️ Do NOT assert whole-corpus row-count equality against the live DB: a concurrent forge ingest between `.backup` and the check would false-HALT correct work. Record the path, the `shasum -a 256`, and both assertion outputs. This is the CEO's restore instrument, not the agent's.
>
> **G1 — REHEARSAL** (file `knowledge/development/shape-amendment-flip-rehearsal.sql`), run against the canonical absolute DB under the invocation discipline above — read-only, so `-bail -readonly`, `.timeout 5000`, exit 0 and empty stderr asserted:
> `SELECT 'PRE='||COUNT(*) FROM lesson_proposals WHERE id IN (233,238,246,247,258,259,271) AND status='reference' AND route='backlog';`
> `SELECT id||'|'||status||'|'||COALESCE(route,'')||'|'||COALESCE(status_updated_at,'')||'|'||COALESCE(status_updated_by,'') FROM lesson_proposals WHERE id IN (233,238,246,247,258,259,271) ORDER BY id;`
> Assert stdout shows **`PRE=7`** followed by exactly seven state rows, and **deposit those rows as `pre-flip-state.txt`** (declared deposit). ⚠️ **Found at lens 2 — this capture is the only readable record of what the flip destroys.** G2's outside-range capture deliberately EXCLUDES these seven ids, and the UPDATE overwrites their `status`, `route` and their Gate-1 `status_updated_at`, so without this the pre-state exists nowhere but a whole-DB backup. Restoring seven rows from a backup to read them is not a comparable instrument. ⚠️ **Load-bearing and never skippable as redundant: it HALTS BEFORE any write.** The `CHANGES` sentinel can only REPORT a bad predicate after the UPDATE has committed — proven by construction at exec-330 (one row pre-flipped → the flip committed the rest and printed a passing count at exit 0). **Prevention and reporting are not the same guard.** `PRE` ≠ 7 → HALT naming WHICH id is off and its current status (the 298 forensic-signature discipline — never just "the flip failed").
>
> **G2 — THE FLIP, capture INSIDE the same transaction** (file `knowledge/development/shape-amendment-flip.sql`, run against the canonical absolute DB per the invocation discipline above). ⚠️⚠️ **`sqlite3` DOES NOT EXPAND SHELL VARIABLES — panel seat 1 ran it: `.output "$TREE"/...` is parsed as a quoted filename plus a surplus argument, giving `extra parameter` and exit 1 under `-bail`.** When you AUTHOR the `.sql` file, substitute the derived value and write the **fully expanded absolute path**; the `/ABSOLUTE/PATH/TO/TREE/` shown below is a stand-in you replace. **Assert before running: the authored `.sql` contains no `$` character.** ⚠️ Note this failure is NOT the missing-directory case below and is not cured by `mkdir` — if G2 exits 1, read the actual error before choosing a response.
> ⚠️ **`mkdir -p` the evidence directory BEFORE running G2.** `.output` to a path whose directory does not exist fails, and **verified live at lens 3:** under `-bail` sqlite aborts on that error and exits 1 **before reaching the UPDATE**, so nothing commits. ⚠️ **This is the ONE pre-commit failure for which re-running G2 is safe**, and it is called out because the never-re-run rule below governs POST-commit states: if G2 exits non-zero AND the read-back shows all seven rows still `reference|backlog`, the transaction never landed — create the directory and re-run. **Never respond by deleting or redirecting the `.output` line**; the capture is a declared deposit, not scaffolding.
> ```
> BEGIN IMMEDIATE;
> .output /ABSOLUTE/PATH/TO/TREE/knowledge/qa/evidence/shape-amendment-2026-08-09/outside-range-ids.txt
> SELECT id||'|'||category||'|'||status||'|'||COALESCE(route,'')||'|'||COALESCE(status_updated_at,'')||'|'||COALESCE(status_updated_by,'') FROM lesson_proposals WHERE id <= 273 AND id NOT IN (233,238,246,247,258,259,271) ORDER BY id;
> .output stdout
> UPDATE lesson_proposals SET status='implemented', route='codify', status_updated_at=strftime('%Y-%m-%dT%H:%M:%SZ','now'), status_updated_by='ceo' WHERE id IN (233,238,246,247,258,259,271) AND status='reference' AND route='backlog';
> SELECT 'CHANGES='||changes();
> SELECT 'GLOBOK='||COUNT(*) FROM lesson_proposals WHERE id IN (233,238,246,247,258,259,271) AND status_updated_at GLOB '20[0-9][0-9]-[0-9][0-9]-[0-9][0-9]T[0-9][0-9]:[0-9][0-9]:[0-9][0-9]Z' AND status_updated_at <> '2026-08-09T01:20:01Z';
> COMMIT;
> ```
> ⚠️ **The `<> '2026-08-09T01:20:01Z'` exclusion is load-bearing, not decoration:** all seven rows ALREADY carry a GLOB-matching timestamp from the Gate-1 routing write (measured 2026-08-09 — a bare GLOB count returns 7 BEFORE this flip runs, making the sentinel vacuous). Excluding the pinned prior value turns a shape check into a value guard only this plan's own write can satisfy.
> Assert the capture file exists with **266 lines** (corpus measured 273 rows, MAX(id)=273, 2026-08-09; 273 − 7). ⚠️ **Read AFTER the transaction commits, so a mismatch does NOT mean "abort" — the flip has already landed.** Below 266 → a corpus row was deleted since authoring; above → the `id <= 273` bound's premise no longer holds. Either way: **do not re-run G2, do not edit the capture** — record the observed count, name which ids differ, HALT for a CEO read with the flip reported as landed. ⚠️ **Verify this line count against the live corpus at run time before treating 266 as authoritative** — it is a prediction made at authoring, and an in-window forge ingest changes it.
> ⚠️ **Known mechanism limit, priced:** sqlite scripts cannot conditionally ROLLBACK, so sentinels print before a COMMIT that happens regardless. Protection is STRUCTURAL: G1 proved exactly 7 rows match, and the `status='reference' AND route='backlog'` predicate makes the UPDATE unable to touch anything else. **Read both sentinels: `CHANGES=7` and `GLOBOK=7`. Either off → HALT with both values and the per-id current status.** ⚠️⚠️ **THE ID LIST IS AN IMMUTABLE INPUT: a sentinel mismatch is NEVER resolved by editing it** — not by dropping an id to make a count match, not by adding one, not by widening the predicate. The only response is HALT with the numbers (the 326 immutable-lists rule).
>
> **G3 — READ-BACK,** canonical absolute DB, `-bail -readonly`, `.timeout 5000`, exit 0 / empty stderr asserted: all seven rows in the SAME column order G1 emitted — `id|status|route|status_updated_at|status_updated_by` (panel seat 1: the two files are read side by side and had their last two columns swapped) → deposit RAW as `flip-readback.txt`; each must read `implemented|codify|<timestamp>|ceo` with the timestamp ≠ `2026-08-09T01:20:01Z` — ⚠️ **column order corrected at panel seat 3, which ran G2+G3 and produced the real rows; seat 1's column fold had landed on the QUERY and not on the EXPECTATION**, so the literal would have matched nothing and HALTed exactly the runs where everything went right.
>
> ⚠️ **`lessons-forge.db` and the backup are deliberately ABSENT from Scope and from the commit — the DB is UNTRACKED by shop policy (plan 30); `git add`ing either would re-track it AGAINST that policy.** The mutation's evidence is the mapping + capture + read-back, never the DB itself.
>
> **FINAL ACTION — commit the deposits** with the pathspec on the COMMIT naming exactly the Scope paths **for the path actually taken** (see PER-PATH DEPOSITS below — the degraded set is a subset, and committing a fresh-path pathspec on a degraded run errors on files that legitimately do not exist), then assert `git show --name-only --format= HEAD` prints exactly them (the 326/265 form). ⚠️ **Committing is what carries these files through the worktree teardown merge into Step 3's tree** — an uncommitted deposit fails the deposit gate AND leaves Step 3 unable to read the evidence it must verify, which is a HALT with nothing to inspect. (An earlier draft said it "strands QA on its fallback"; there is no fallback — the referent was dangling, found at ACID a3.)
>
> End with `### Ledger Updates` carrying `#### Prompt Feedback` **and `#### Forward Register` with exactly the word `NONE`** — ⚠️ **added at the confirming walk: Step 3's item 7 asserts one `NONE` subsection PER STEP, and the panel-seat-5 reconciliation reached Steps 1 and 3 but not this one. That is the seam class seat 5 named, reproduced inside the fold that closed seat 5's seam finding.**
>
> ⚠️⚠️ **PER-PATH DEPOSITS.** On the **FRESH** path all six deposits below are produced. On the **DEGRADED** path (P0.5) G1/G2/Task B never run, so `shape-amendment-flip-rehearsal.sql`, `shape-amendment-flip.sql` and the in-transaction `outside-range-ids.txt` **do not exist and MUST NOT be written after the fact** — a rehearsal file for a rehearsal that never ran is a fabricated record, and the deposit gate is not a reason to manufacture one. That path deposits the dev log, `flip-readback.txt`, the reconstructed `outside-range-ids.txt` and `pre-flip-state.txt` (or records the pre-state as LOST) — **the recovery mechanics are stated once, in P0.5, and are not repeated here.** **Name the path taken at the top of the dev log**; the verdict reader needs it to know which deposit set to expect. ⚠️ **DECLARED BENIGN-BY-DESIGN (panel seat 5):** the `**Deposits:**` block below is read mechanically by the deposit gate, so a DEGRADED run takes three to four `deposit_exists` gate failures **by construction**. That is expected, not a broken step — the Planner issues continue-with-reasoning on it; fabricating the missing files to satisfy the gate is forbidden and is the failure this declaration exists to prevent.
>
> **Scope:**
> - `knowledge/development/shape-amendment-dev-log-step-2-2026-08-09.md`
> - `knowledge/development/shape-amendment-flip-rehearsal.sql`
> - `knowledge/qa/evidence/shape-amendment-2026-08-09/pre-flip-state.txt`
> - `knowledge/development/shape-amendment-flip.sql`
> - `knowledge/qa/evidence/shape-amendment-2026-08-09/outside-range-ids.txt`
> - `knowledge/qa/evidence/shape-amendment-2026-08-09/flip-readback.txt`
>
> **STOP. Wait for CEO verdict.**

**Deposits:**
- `lessons-forge/knowledge/development/shape-amendment-dev-log-step-2-2026-08-09.md` — an explicit `Output Receipt: Complete` line naming every deliverable produced on the path taken (Rule 15), the path taken, TREE and its remote, Task M's seven-row mapping keyed on the plan's pinned clause literals, backup path + sha, PRE/CHANGES/GLOBOK values, the capture line count with its live re-verification.
- `lessons-forge/knowledge/development/shape-amendment-flip-rehearsal.sql`
- `lessons-forge/knowledge/qa/evidence/shape-amendment-2026-08-09/pre-flip-state.txt` — the seven rows exactly as they stood before the flip; the only readable record of what it overwrote.
- `lessons-forge/knowledge/development/shape-amendment-flip.sql`
- `lessons-forge/knowledge/qa/evidence/shape-amendment-2026-08-09/outside-range-ids.txt`
- `lessons-forge/knowledge/qa/evidence/shape-amendment-2026-08-09/flip-readback.txt`

---

## STEP 3 — QA

> **(A) DERIVE `TREE`** — each step runs in a FRESH agent context and a fresh worktree, so Step 1's derivation does not carry** (walk 3 lens 1: only Step 1 had it, while this step uses tree-relative paths). `TREE="$(git rev-parse --show-toplevel)"`; assert it is a git toplevel containing `knowledge/decisions`; print `TREE` and `git -C "$TREE" remote -v` into this step's deposit. Every tree-relative path below resolves against it.
>
> ⚠️ **EXECUTION ORDER FOR THIS STEP — lettered, so it cannot be confused with this step's own Item numbers** (the confirming walk found `(2)` meaning *Item 0* and `(3)` meaning *Items 1–5*, the same collision it fixed in Step 2): **(A)** derive `TREE` · **(B)** Deliverable Verification · **(C)** Item 0 pins · **(D)** Items 1–5 · **(E)** Item 6 receipt · **(F)** Item 7. *(Panel seat 5: "before anything else" was literally unsatisfiable, since Deliverable Verification reads dev logs at tree-relative paths and therefore requires the derivation.)*
>
> **(B) — Deliverable Verification (Rule 8 / Rule 17)** (330's form; dropped by this clone and restored at panel seat 4). Read Step 1's and Step 2's dev logs, confirm each **Output Receipt reads Complete**, then verify every file they claim exists and carries the described change. Table: `| Deliverable | Expected | Status (✅/❌) | Evidence |`. **This is not sampling — every declared deposit for the path taken is checked.** ⚠️ **Why it precedes the pins:** item 0's shas are IDENTITY checks — they prove the bytes are the ones Step 1 left, and prove nothing about whether Step 1 FINISHED. A Step-1 that HALTed at A0(3b), deposited a partial dev log, and drew a `continue` by mistake passes every pin, and Step 2's irreversible flip would already have run before item 1's presence greps noticed (panel seat 4).
>
> **Item 0 — pin chain.** ⚠️⚠️ **EVERY git command in this item and item 1 carries `git -C /Users/marklehn/Developer/GitHub` and the absolute file operand** — panel seat 3 found them bare, and the preamble's `TREE` derivation actively steers them into the dispatch worktree, **where `git status --porcelain -- DRAFTING_CYCLE.md` prints nothing and exits 0**: the in-window-edit guard passes VACUOUSLY on a file that repo has never seen, and `git show <hash>^:…` / `git diff --numstat` die on `bad object`. (a) the doctrine's newest path-scoped commit equals Step 1's recorded hash; (b) `shasum -a 256` on the ABSOLUTE doctrine path equals the recorded DOC_SHA; (c) `git -C /Users/marklehn/Developer/GitHub status --porcelain -- DRAFTING_CYCLE.md` is empty, **paired with a liveness control** — the same probe against a path known tracked in the root repo must speak, so an empty result cannot be read as clean; (d) both dev logs are clean and their newest path-scoped commits are their own steps' (messages name the slug); (e) `shasum -a 256` of the claimed plan file equals the recorded PLAN_SHA. Any mismatch → HALT (in-window edit).
>
> **Item 1 — the edits landed exactly.** All `grep -F`, raw output deposited. (a) **A1, A5 and A6 are GONE** — and only those three, since E2/E3/E4/E8 are inserts that replace nothing: `grep -c -F` each prints 0, PAIRED with the same probe against `git show <recorded-hash>^:DRAFTING_CYCLE.md` printing 1 for each (the live positive control; the (D) standard — a negative never stands alone). ⚠️ **A4 must still be PRESENT and unmodified** — assert it, do not assert its absence: `grep -c -F "The compact form is"` against the live doctrine prints 1. Asserting A4 gone would fail a correct run. (b) **A1′, A2′, A3′, A4′, A5′, A6′, A8′ present exactly once each, WHOLE-LINE — `grep -c -x -F -f <patternfile>`.** ⚠️⚠️ **Two mechanical requirements, both proven necessary by panel seat 3 running them.** (i) **Write each literal to a pattern file with your file-WRITE tool and pass it with `-f`.** ⚠️⚠️ **STRIP THE LEADING `> ` FROM EACH PLAN-SIDE LINE BEFORE WRITING IT** — the primes are stored blockquoted in this plan, and with `-x` mandatory a retained prefix makes every one of the seven a total mismatch: `grep -c -x -F -f` returns **0** for all seven against a PERFECTLY CORRECT doctrine, and this plan has trained you that the printed count is the assertion. Positive control: the pattern file's first line begins `Walk the lenses`, not `> `. (Panel seat 5: the strip mandate was scoped to PLACEMENT in Step 1 and never reached verification in Step 3, which runs in a fresh context — the mirror image of the corruption seat 3 closed.) **NEITHER shell quoting form is safe either:** A1′ carries 4 backticks (command substitution inside `"…"`) and 10 apostrophes (which break `'…'`); A8′ carries 4 backticks. Seat 3 demonstrated a correct file reporting `0` — and this plan has trained the agent that the printed count IS the assertion, so a mangled pattern reads as ABSENT and HALTs after the irreversible flip. (ii) **`-x` is mandatory:** without it a substring match passes on a `> `-prefixed line, and seat 3 built the fully blockquote-corrupted doctrine and showed **all seven primes, all seven clause keys and the line counts pass every check as written** — the `-x` form returns 0 for all seven and catches it.
> (b2) **Corruption asserts — these did NOT exist and Step 1 claimed they did** (panel seat 3): `grep -c -E '^> ' /Users/marklehn/Developer/GitHub/DRAFTING_CYCLE.md` must print **0** (verified earnable: the live v1.8 doctrine has zero such lines), positive control = the same probe against this plan file, which returns hundreds; and the two `---BEGIN/END PINNED TEXT---` sentinel literals must count **0** in the doctrine, with their count of 1 in this plan as the live positive control. (c) **Placement:** A2′ and A3′ line numbers fall inside §2.7 (between its heading and the §2.8 heading) and A3′ = A2′ + 1; A1′ inside §2; A4′ inside §3 and strictly AFTER the A4 anchor line; A5′ and A6′ inside §2.6; A8′ strictly AFTER the example block's closing fence and strictly BEFORE the `For **T0** the block collapses` line — ⚠️ **and NOT bounded by "inside §3"** (panel seat 3): §3's worked example is a fenced block whose first line is literally a top-level `## Drafting Cycle` heading, so any `^## ` heading scan closes §3 there and a correctly placed A8′ computes as OUTSIDE it — a false HALT. The two fence-relative bounds are unambiguous on their own; the same probe-boundary class this cycle already hit twice at walk 3. (d) **Version:** `grep -c -F "**Version:** 2.0 (202"` exactly 1, Iteration-Protocol sentence intact. (e) **History — TWELVE elements, TWELVE asserts, one per element** (the 328 compound-drop counter-measure; a single eyeball read of the row is exactly what failed there). Against the row's own line, `grep -F` each and record pass/fail per element: **(1)** `**2.0 (` · **(2)** `shape-amendment-2026-08-09` · **(3)** each of `233`, `238`, `246`, `247`, `258`, `259`, `271` present · **(4)** a fold-origin term AND a judged-stop term · **(5)** a term naming the D2 pair as mandatory · **(6)** a covers-not-targets term · **(7)** a routing term plus the no-deviation statement · **(8)** a 259-premise-correction term — ⚠️ **this element has no other producer anywhere in the plan; if it is absent the row silently carries the falsified premise this plan exists to correct** · **(9)** `plan_lint` plus a deferred/register term · **(10)** each of `§2`, `§2.6`, `§2.7`, `§3` present · **(11)** an inheritor term naming `dc-s4-prose` and the surgical batch · **(12)** a DB-disposition term naming the flip. **Any element missing → HALT naming which number.** Report all twelve outcomes even when all pass — a bare "row OK" is the read that failed at 328. (f) **numstat:** `git diff --numstat <hash>^ <hash> -- DRAFTING_CYCLE.md`. ⚠️ **The expected pair is a PREDICTION the plan does not assert is correct** — deletions **4** (A1, A5, A6, the version line — A4 is not replaced) and additions **9** (A1′, A2′, A3′, A4′, A5′, A6′, A8′, the version line, the History row), **plus exactly 2 blank-line additions** — E4 and E8 each add one blank separator above and reuse the existing one below; E2/E3 add none (the mandated structure above). Derivation shown so the arithmetic is checkable on the line: **9 content + 2 blank = 11 additions / 4 deletions.** ⚠️ **Measured, not computed: panel seat 3 applied all eight edits mechanically to a copy of the live doctrine and `git diff --numstat` returned exactly `11 4`.** The earlier figure of 13 was a bad sum introduced at panel seat 2 and would have HALTed a perfectly correct run after the irreversible flip. ⚠️ Derived from the plan's OWN mandated structure, not from what the agent did — a circular check cannot catch a stray addition, which is the whole point of calling this the whole-file deletion guard. If observed ≠ derived, do NOT force it: deposit the full `git diff` and HALT for Planner adjudication. This pair is the whole-file deletion guard — a deletion elsewhere is invisible to every presence grep. (g) **retained material:** the three quoted sentences from untouched regions still present verbatim.
>
> **Item 2 — the doctrine does not argue with itself (the E5 check).** `grep -n -F "aim the next walk"` must print 0 against the live doctrine, PAIRED with a positive control proving the probe speaks (the same literal against the pre-edit blob prints 1). ⚠️ This is the one assert that would catch E2 shipping without E5 — a contradiction no presence grep can see, because both texts would be present and each would look correct alone.
>
> **Item 3 — the flip.** (a) read back all seven and assert POSITIONALLY rather than by a concatenated literal (panel seat 3): `status=implemented`, `route=codify`, `status_updated_by=ceo`, `status_updated_at` ≠ `2026-08-09T01:20:01Z`; (b) **outside-range integrity:** re-run the capture query live — ⚠️ **use the EXACT `SELECT` from Step 2's G2 block, the one carrying `category`** (panel seat 5: G2's capture emits six fields `id|category|status|route|status_updated_at|status_updated_by` while G1/G3 emit five without `category`; re-typing the adjacent query makes **all 266 lines** differ and the item then HALTs offering *concurrent forge activity* as a plausible and entirely wrong diagnosis, after the flip). Shape control: the first line must carry six `|`-separated fields. Then `diff` it against the committed `outside-range-ids.txt` — an EMPTY diff is the pass, and its liveness is anchored by `wc -l` on both being positive and equal (a symmetric empty extraction would vacuously pass). ⚠️ **The expected diff is empty, so a zero-byte or missing file must NOT be read as success** — assert both files non-empty first, and record blob hashes and the compared line counts beside the result (the 332 seat-1 inverted-gate finding: Rule 20 flags a zero-byte evidence file CRITICAL, so an expected-empty comparison needs a receipt, not a silence). ⚠️ **A non-empty diff here is not automatically a defect.** An arbitrary verdict-window separates Step 2 from this step, and a parallel terminal running a forge cycle can legitimately touch out-of-range proposal rows. **Still HALT — but NAME the differing ids and state that concurrent forge activity is a candidate explanation; never reconcile by re-running the capture or editing the committed file** (329's ACID w1, finding 5.3 — the concurrency-naming form; **not** its seat 5, corrected at panel seat 4: a wrong attribution poisons exactly the brief-yield query §2.6's Attribution convention exists to keep cheap). (c) **path-aware (ACID a2):** on the FRESH path — the backup exists at the recorded path with the recorded sha and the recorded restorability outputs, **AND `pre-flip-state.txt` is non-empty and carries exactly seven `id|status|route|…` lines, each reading `reference|backlog` with `status_updated_at=2026-08-09T01:20:01Z`** (added at panel seat 1, when nothing on the normal path asserted this file at all; ⚠️ since panel seat 2 repointed `evidence_dir` at the evidence directory, Rule 20 now covers its **zero-byte** case via `required_evidence_files` — what this assert adds is the **content** check Rule 20 cannot make); on the DEGRADED path Task B did not run, so assert instead that the dev log names the degraded path, that no file named `pre-*` was created after the flip, and that `pre-flip-state.txt` is either labelled as recovered from a prior backup or the pre-state is recorded as LOST. Reading a fresh-path assert against a degraded run is a false HALT; reading a degraded-path assert against a fresh run hides a missing backup.
>
> **Item 3b — consumer semantics, BOTH halves of the 330 row-9 form** (this clone had kept only the source-quote half; the executable half restored at panel seat 4, which noted 330 itself restored it after ITS parent 326 had it and 330 dropped it — a third drop of the same guard). **(a) Source:** quote `_TERMINAL_STATUSES` from `lessons-forge/src/lessons_forge.py` and confirm it contains BOTH `reference` and `implemented`; quote the `route`-reading report query and confirm its status filter excludes both. **(b) EXECUTED, post-flip:** run one status-reading consumer and one route-reading consumer against the LIVE corpus and deposit the raw output — confirm the seven ids do not reappear as unclassified or re-queued for re-proposal. ⚠️ **A flip that silently re-queued its own entries is a regression no row-value check can see** (330's wording), and this plan writes TWO consumer-visible fields where 330 wrote one. Either half failing → HALT: the Scope's behaviour-neutral claim would be false.
>
> **Item 4 — Task M's mapping is true, re-checked independently.** For each of the seven ids, re-run `grep -c -F` on the **pinned clause key from the plan** (not on whatever substring Step 2's dev log happened to quote — the point of pinning them is that this step asserts the same literal Task M did) and confirm each returns exactly 1 against the LIVE doctrine, then read the surrounding sentence and confirm it supports the id's proposal. ⚠️ **This is the row-vs-evidence cross-check (the 329 item-2(d) class): the flip has already carved `implemented` into the corpus, and if the mapping is wrong the DB now lies about doctrine.** Any id whose sentence is absent or does not support the claim → HALT and report which.
>
> **Item 5 — self-application, and it is this plan's most interesting evidence.** Run `python3 /Users/marklehn/Developer/GitHub/bellows/scripts/plan_lint.py <this plan file>`; deposit literal stdout + exit code. The Planner's declared expected WARN set and exit code are carried in the Cycle Log; QA records the ACTUAL and flags any difference — silence is not the requirement. ⚠️ **If this plan closed on a judged stop, the closing check is EXPECTED to WARN** — that is the §4 divergence the History row declares and the FORWARD row schedules, not a defect. Record it as the first live specimen.
>
> **Item 6 — emit the QA Receipt with the canonical Rule 20 self-check block** from `/Users/marklehn/Developer/GitHub/RULE_20_SELF_CHECK_BLOCK.md` (absolute), values (exactly four, one per placeholder in the canonical block): `plan_slug`: `shape-amendment-2026-08-09`; `qa_report_path`: the **expanded absolute** path to `<TREE>/knowledge/qa/shape-amendment-qa-2026-08-09.md`; `evidence_dir`: the **expanded absolute** path to `<TREE>/knowledge/qa/evidence/shape-amendment-2026-08-09`; `required_evidence_files`: `[pre-flip-state.txt, outside-range-ids.txt, flip-readback.txt]`. ⚠️ **Values are literal expanded paths — never an unexpanded `$TREE`, which the block would take verbatim** (panel seat 2). ⚠️ **`evidence_dir` points at the EVIDENCE, corrected at panel seat 2:** it previously pointed at `knowledge/development`, so the four artifacts this plan's irreversible write actually rests on got no zero-byte CRITICAL coverage at all — `flip-readback.txt` was a declared deposit that no QA item asserted even existed. ⚠️ **On the DEGRADED path the carve-out is `pre-flip-state.txt`, NOT `outside-range-ids.txt` — inverted at panel seat 5, which found it naming the one file that path DOES produce and omitting the one that can legitimately be absent.** Drop `pre-flip-state.txt` from `required_evidence_files` **iff** the dev log records the pre-state as LOST (no recoverable prior backup), and say so in the report. Left as written, the exact state P0.2 exists for — crash after the flip with no backup — produced a correct Step 2 and then an unresolvable Rule 20 CRITICAL, with fabrication forbidden and no stated remedy: **a dead end on the plan's own recovery branch, after the irreversible write.** ⚠️ ORDERING: write the QA report FIRST with its verification table complete (one row per numbered item, under a `##` heading containing `verification`), THEN run the block, THEN append its literal stdout — the block reads `qa_report_path` from disk and CRITICAL-fails if missing; ⚠️ **its hedging scan is scoped to markdown table rows whose status cell is exactly a positive token — NOT whole-file** (verified against the block's `is_positive_row` at panel seat 1; the whole-file claim was imported from `plan_lint`'s banner check and is false). Raw evidence quoted outside a table row is never scanned, so **quote raw command output freely and never redact it**; keep hedging words out of positive-status rows, and the status cell holds exactly one token and nothing else. ⚠️⚠️ **The hedging list includes `not run` and `skipped`, matched as a SUBSTRING over the whole row — and those are the degraded path's own vocabulary** (panel seat 3 ran the block's scanner: a truthful row reading *"Task B did not run on this path, which is correct"* CRITICAL-fails on `not run`). Do NOT reword the truth out of the table; use phrasings that state the same fact without the keywords — *"Task B correctly bypassed on this path"*, *"pre-state recorded as LOST; no `pre-*` file created"*. ⚠️ **Every evidence file must be non-empty** (zero-byte → CRITICAL). The banner `Rule 20 — QA Self-Check Results` and the `PASSED — SELF-CHECK PASSED` line must both appear BYTE-EXACT (em-dash U+2014) — the whole-plan check requires the LITERALS and a paraphrase FAILs it. If FAILED, HALT.
>
> **Item 7 —** `### Ledger Updates` carries `#### Prompt Feedback`. ⚠️ **Each step emits `#### Forward Register` carrying exactly the word `NONE`** — corrected at panel seat 4. An ABSENT block is unassertable and every sibling in this lineage (326/329/330) emits one, so habitual agent behaviour is to emit a real row, which `_append_forward_row(project_path, …)` would file into **lessons-forge's** register — the precise outcome ACID a2 folded to prevent, defended until now only by prose. `NONE` is verified inert (`parser.py` drops a body whose text is `none`/`n/a`) **and** is a positive, greppable token. **Assert it: exactly one `NONE` Forward subsection per step, and zero appended rows in either register.** The real deferred-gate row is recorded in Step 1's dev log for a Rule 42 direct edit at wrap. ⚠️ **Assert it is carried:** confirm Step 1's dev log contains the row text under its owed-to-bellows heading — it is the only surviving carrier of the §6 obligation, and a dev log missing it means the deferral was declared and then dropped.
>
> QA report WRITTEN at its absolute path, then staged and committed path-scoped AS THE FINAL ACTION, after item 6's block-append.
>
> **Scope:**
> - `knowledge/qa/shape-amendment-qa-2026-08-09.md`
>
> **STOP. Wait for CEO verdict.**

**Deposits:**
- `lessons-forge/knowledge/qa/shape-amendment-qa-2026-08-09.md` — RAW outputs for items 0–5 beside their producing commands (never summaries), the verification table, the Rule 20 stdout.

---

## Drafting Cycle

⚠️ **THIS SECTION IS A RECORD, NOT INSTRUCTIONS.** Gate-matching strings are described, never quoted; nothing here is addressed to any executing agent (step spans run to end-of-file and absorb this block).

**Tier:** T2 — trigger fired: T-6 (governance surface: the doctrine file plus the proposal corpus). Clone origin: **executable-330** (doctrine edit + scoped proposal flip); secondary same-class sibling for the panel's clone-diff seat: **executable-329**. ⚠️ Deliberate divergence from the origin: three steps, not two — the flip's truth depends on the landed wording (Method note).
**Conformance (§5):** first run at lens 4 of walk 1 (shape-stability, before the adversarial passes close — the v1.8 mandate). **Exit 1**, one FAIL (QA banner paraphrased — folded) and one suite-scope WARN (bare token in pinned doctrine prose — folded). Re-run after those folds: exit 0. **Last run: panel seat 4 → exit 0, three WARNs** (§5 requires the LAST run's exit code with the phase it ran at; this line previously recorded only walk 1's, across nine intervening culminations — an unrecorded pass being indistinguishable from one that never ran is §5's own sentence, and panel seat 4 caught it on the plan amending the section §5 lives in). **Re-run and rewrite this phase token at every culmination and at deposit. ⚠️⚠️ **MEASURED AT THE DEPOSIT PATH — corrected at deposit.** The `(o1)` check derives `project_root` as the path before `/knowledge/`, so its answers are LOCATION-DEPENDENT: from `scratchpad/` it reported three WARNs, from a bare temp dir one, and **from the real deposit location TWO** — `knowledge/decisions` resolves and EXISTS under `lessons-forge`, so it does not warn there. A state measured anywhere but the deposit path is a value QA compares against and finds wrong. **DECLARED EXPECTED STATE AT THE DEPOSIT PATH — exit 0, exactly TWO WARNs:** the §4 closing WARN (systematic under v2.0 for a judged-stop close, per the Scope bullet — the first live specimen of the declared divergence), and `(o1) missing path …/resume-sweep.txt` (output-path class, a conditional HALT-path deposit that by definition does not exist on a clean run). ⚠️ Superseded scratchpad-measured classification, kept for the audit trail: `(o1) missing path knowledge/decisions` (output-path class, the bootstrap prompt's runtime plan path); `(o1) missing path …/resume-sweep.txt` (same class — a conditional HALT-path deposit added at panel seat 2, which by definition does not exist on a clean run); and the §4 closing WARN (systematic under v2.0, per the Scope bullet).** ⚠️ **Refreshed at panel seat 1, which caught this paragraph asserting a residue of one WARN and claiming ACID had not run when a1/a2/a3 were all recorded above — the record decaying while the artifact converged, inside the plan that codifies 258.** ⚠️ **An exit 0 here is still evidence about FORM only, never that the cycle finished** — the honest state is the Walks line. **The expected state at deposit is re-declared at close, and the lint's verdict is evidence about form only — never about whether the cycle finished.** (The `earn-the-clean-gate-do-not-author-it` class, caught at lens 4 on this plan's own record.)
**Walks:** 1 COMPLETE (27 pre-existing / 4 fold-introduced, 31 folds). **Walk 2 COMPLETE: 13 findings, 2 pre-existing / 11 fold-introduced (85%). Walk 3 COMPLETE — lenses 1–4 plus ACID a3: 12 findings, 4 pre-existing / 8 fold-introduced (67%).** ⚠️ **The per-walk splits above were RECOMPUTED at panel seat 1** — the block had carried a cycle-to-date headline of 63 against per-lens lines summing to 56, three walk headers disagreeing with their own splits, **on the artifact whose whole subject is making that number auditable.** ⚠️ **The cycle-to-date running total has since been REMOVED rather than corrected (panel seat 5):** §3 says *do not keep a running fold-count in the Cycle Log — fold counts belong in the compact per-lens lines, not as a separate running tally*, and a cycle-to-date figure is exactly that. Per-walk and per-seat counts stay (they are per-pass, which is what §3 mandates and what v2.0's bar reads); the cumulative figure lives in the scratchpad register, where D5's cross-plan instrument will read it. **Invariant held explicitly: on every line `folded = pre-existing + fold-introduced`, and each walk total is the sum of its lens lines.** ⚠️ **Still not at the bar:** a3 produced a HIGH that would have destroyed the restore instrument for an irreversible write, so the residue is not record-class only — though the composition is thinning (one dry lens at w3; a3's remainder is notation and referents). ⚠️ **The origin ratio has inverted between walks — 13% at w1, 85% at w2 — the noise-floor signal this amendment codifies, appearing in the cycle that codifies it.** ⚠️ **The bar is NOT met, and the reason is exactly why the bar has two conditions:** the ratio alone would license a stop, but walk 2 produced four HIGHs that were logic-class, not record-class — one would have corrupted the doctrine file, three would have corrupted or fabricated the record of an irreversible write. **A ratio-only bar would have stopped this cycle one walk too early.**
- Weak spots: w1 **7 folded (6 pre-existing / 1 fold-introduced), 2 HIGH** — all pre-existing (draft-origin), 0 fold-introduced, as expected on a first pass over a fresh draft. **HIGH:** A4′ was written as a compose-this instruction — an angle-bracketed placeholder standing in for the live paragraph, followed by the new sentences — which contradicted the plan's own no-composition rule and made the whole-line byte-compare unsatisfiable — E4 restructured from a replacement into an INSERT of a complete standalone paragraph, so A4 is now never touched. **HIGH:** the Step-1 deposit named no repository and no path derivation while the plan spans TWO repos (doctrine in the root, deposits in a run-specific bellows worktree) — the topology is now stated, the tree root derived once via `rev-parse --show-toplevel` and printed into the dev log, and the two commits declared separate. **MED:** A0(4) instructed grepping "the five full pre-edit texts A1–A5" when only A1/A5 are retyped and A2/A4 are placement anchors — the two kinds are now enumerated separately with a note that they are not interchangeable. **MED:** Task M's keys were line-level while its claim is clause-level — four of seven ids map into two lines, so one line's presence would have vouched for three ids; keys are now distinct sub-strings, each independently falsifiable. **MED:** the Forward row text was left for the agent to compose despite the plan's own no-composition rule and the channel's six known failure modes — now pinned verbatim with a single-emit instruction. **MED:** QA 3(b)'s capture diff would have HALTed as a defect on legitimate concurrent forge activity across the verdict window — still HALTs, but now names the ids and states concurrency as a candidate explanation (the 329 seat-5 form). Dependent corrections swept from the E4 fix: QA 1(a) now asserts A4 PRESENT rather than gone (asserting its absence would have failed a correct run) and 1(f)'s deletion prediction moved 4 → 3 with three inserts, not two.
- **Premises executed live this pass** (§2.7 execute-against-real-data, not argued): all four A0(5) earnability literals return 0 against the pre-edit doctrine, so every new post-condition is earnable rather than blind on arrival; `implemented|codify` confirmed as the live corpus shape from 330's flip of 232/245, so the route flip produces a consistent record; A1 and A5 whole-line pins and all three placement anchors verify at exactly 1.
- **One fold-introduced finding, caught within the pass (7th, LOW):** the record line written for the A4′ fold QUOTED the placeholder string it had just removed, re-seeding the artifact with the text the fold deleted — §3's reflexive class, and the first fold-introduced finding of this cycle. Re-worded to describe. Recorded rather than quietly corrected (§2.7 attestation integrity): the origin split for this pass is therefore **6 pre-existing / 1 fold-introduced**.
- Destruction: w1 **7 folded, 3 HIGH** — **6 pre-existing / 1 fold-introduced** (the record lag). Run as a DIFF REVIEW of every replacement, not a checklist (the 332 lesson: a retained-material checklist cannot see what you did not think to list). **HIGH (2.2):** the mechanical clause-diff of A1→A1′ found a GUARD silently deleted — the old criterion's *a new material finding re-opens the walk* had no counterpart in the replacement, so a material finding on a closing walk had nothing telling the cycle to continue; restored explicitly and marked as not relaxed. **HIGH (2.1):** §2.6 convenes the cold panel *after the sequential walk goes dry* — a precondition the new bar can make unreachable, stranding the panel on any judged-stop close; E7 added, and it also states the panel is NOT waived by a judged stop. **HIGH (2.2):** the new bar is structurally more gameable than the one it replaces — literal dry is observable by anyone, whereas both new conditions are judgements made by the party who wants to finish; mitigated by requiring the closing walk's residue to be ENUMERATED (each finding, class, origin), so a stop shows its work rather than asserting it. **MED (2.1):** §3's canonical example block shows only dry closes and no origin split — authors copy the example, so the new form had no worked instance; E8 adds one BESIDE the example without mutating it, the dry close staying the default. **MED (2.3):** the flip erases the seven rows' `status`/`route`/Gate-1 timestamp while G2's capture deliberately excludes those very ids — the pre-state existed nowhere but a whole-DB backup; G1 now emits and deposits `pre-flip-state.txt`. Dependent sweep from E7/E8: A0(4) pins, A0(5) earnability (now seven literals, all verified 0 pre-edit), QA 1(a)/(b)/(c), and the numstat prediction (4 deletions / 9 additions, four inserts). **Fold-introduced (LOW):** the Scope heading and its edit-range still named the pre-E7/E8 count and range after those edits landed — the record-decay class, swept at the same pass rather than left for a later one.
- Vulnerabilities: w1 **5 folded, 2 HIGH** — **5 pre-existing / 0 fold-introduced**. Both HIGHs are CLONE-DROPS: guards clone origin 330 states explicitly that this clone silently failed to inherit. **HIGH (3.1):** the plan contained **zero `sqlite3` invocations** — G1/G2/Task B gave SQL *content* but never named the database. 330 carries the reason verbatim: the DB is gitignored and **absent from every worktree**, so a bare relative name does not error, it **CREATES an empty file** and every query answers from nothing. A full invocation discipline is restored (canonical absolute path only, exists-and-non-empty pre-assert, `-bail`, `.timeout 5000`, `-readonly` on reads, exit-0 + empty-stderr per invocation, no heredocs). **HIGH (3.1):** Task B took a filesystem copy of a live SQLite file — which can capture a torn state mid-transaction — and asserted only that the backup EXISTS; 330 used `.backup` and asserted RESTORABILITY. Both restored. **MED (3.3):** G2's `.output` fails if the evidence directory is absent; **verified live** that `-bail` aborts before the UPDATE so nothing commits — but the plan's never-re-run-G2 rule governs post-commit states and would have sent an agent into an unnecessary HALT, or tempted it to delete the `.output` line. `mkdir -p` mandated, and the one safe pre-commit re-run distinguished from the forbidden post-commit one. **MED (3.1):** the `TREE` assertion required the path to end in `lessons-forge`, which a legitimate bellows worktree does not — checked live: worktrees live under `bellows/.bellows-worktrees/`. Re-based onto a structural assert plus a recorded `remote -v`. **LOW (3.1):** the no-heredoc rule (330's #1 forbidden op) was missing.
- **Premises executed live this pass:** the `.output`/`-bail` abort semantics were tested on a scratch DB rather than assumed (transaction rolled back, exit 1, source row unchanged); E8's anchor was proven unique — the doctrine holds exactly two fence lines (127 open, 139 close) and the first strictly after the example's Closing line is unambiguously 139; every mandated literal search in the plan carries `-F` (the bare `grep` hits are prose — the count has since grown with the record and is re-measured per pass, not carried); the seven A0(5) probes all return 0 pre-edit.
- Integration-record: w1 **6 folded (4 pre-existing / 2 fold-introduced), 1 HIGH + 1 FAIL** — **4 pre-existing / 2 fold-introduced** (the two below). Run together with the §5 conformance pass at shape-stability (the v1.8 practice this cycle inherits). **HIGH (4.1) — the §6 divergence claim was UNVERIFIED, and LESSONS 2643 names exactly that failure** ("the claim survived five warm walks, five ACID passes and a `plan_lint` run, because every one of those passes READ the assertion rather than opening the gate"). Constructed the case instead: a judged-stop close **does** WARN — and the mechanism is sharper than the draft claimed, because A1′ *mandates* the origin split, the split contains the `fold` substring, and a judged stop carries no `dry` token, so **the doctrine mandates the exact token that trips the check**. Two boundary results from the same probe: without the split it does not warn (non-compliant), and in the multi-lens form it does not warn because the check cannot parse that form at all — FORWARD row 39, which this amendment now gives a second reason to close. Scope bullet, pinned FORWARD row and History instruction all re-based onto the measurement. **FAIL (4.1):** the lint returned a hard FAIL on the QA banner pair — Step 3 paraphrased the banner where the whole-plan check requires the LITERAL; **the identical finding appears in 330's own walk-1 record**, so this is a repeat of a class the record already paid for, not a new one. Both literals now carried. **MED (4.1):** the lint raised a suite-scope WARN on step 1 — the bare token sat inside **pinned doctrine prose** absorbed by the step span (§3's reflexive class, the mirror of 330's re-idiomed shell liveness finding); re-worded, and the doctrine reads the same. **Fold-introduced this pass, both from the record of the folds themselves (the §3 reflexive class, third instance this cycle):** (i) the record's description of the suite-scope fold RE-CREATED the condition in a different step span — spans run to end-of-file, so the Cycle Log carried it into step 3; re-idiomed. (ii) writing "ACID not yet run" into the record SATISFIED the missing-lens check for ACID, turning exit 1 into exit 0 while ACID genuinely has not run — the clean gate authored rather than earned; declared explicitly in the conformance line above rather than chased, since a record must be able to name a lens it has not run. **Residue classified, not silenced:** the two block-shape WARNs (missing Integration/ACID lines; closing not-yet-dry) are the honest in-progress state and clear as the cycle completes; the `(o1)` missing-path WARN is the known output-path class, firing on the bootstrap prompt's runtime plan path.
- ACID (a1, run apart as its own phase): **6 folded, 3 HIGH** — **6 pre-existing / 0 fold-introduced**. Read as a three-step schedule with two verdict gates of arbitrary wall-clock over four shared stores (root doctrine, the DB, the dispatch worktree, the bellows register). **HIGH (5.4) — an UNRECOVERABLE state existed:** Step 1 carried a re-entry branch and Step 2 carried none (probed: zero mentions of re-entry, DOC_SHA or PLAN_SHA in the whole step). A crash after the flip COMMITS but before the dev log deposits left the DB irreversibly flipped, no evidence on disk, and a re-run whose rehearsal would HALT with no path forward. Task P0.2 adds the re-entry probe and a DEGRADED path that reconstructs the capture post-hoc and **labels it as reconstructed rather than passing it off as the in-transaction original**. **HIGH (5.3):** Step 2's Task M maps proposal ids onto sentences in the LIVE doctrine and then flips irreversibly — with no pin to what Step 1 landed. An in-window edit by another actor would have the DB asserting `implemented` against text nobody approved; P0.4/(3) pin the DOC_SHA, the commit hash and the PLAN_SHA before Task M reads anything. **HIGH (5.1):** E6's History row is a **nine-element compound mandate** — counted mechanically, and the exact shape that dropped exactly one element at 328 while every daemon gate passed (FORWARD row 40's census class). QA asserted four of the nine, and **the 259 premise correction — the element with no other producer anywhere in the plan — had no assert at all**, so the row could have carried the falsified premise this plan exists to correct and passed every check. Now an enumerated nine-element list with nine asserts and a HALT naming the missing number. **MED (5.2):** a stop between Steps 1 and 2 leaves doctrine live and the corpus lagging — acceptable and recoverable, but unstated; now stated, with the inverse (proposals `implemented` against doctrine that never landed) named as the state the three-step split prevents. **MED (5.2):** nothing verified the FORWARD row actually landed, though null-parse is a known channel failure mode and that row is the only thing scheduling the deferred gate half; post-condition added. **LOW (5.4):** backup adjacency to the flip was unstated.
- **Verified sound this pass (not relaxations):** A5′ subordinates review-target rotation to full coverage but does not weaken it — under mandatory coverage the quiet step is covered by definition, so the guard is strictly stronger, not relaxed; A1′ retains the falling-count and flat-panel-yield warnings verbatim; the flip's blast radius is bounded and proven ours by the id list, the `reference`+`backlog` predicate, the rehearsal that HALTs before any write, and the adjacent backup.
**Walk 2 — lenses 1–4:** **9 folded, 1 HIGH** — **2 pre-existing / 7 fold-introduced (78%)**. New surface named per §2's own bar: walk 1's 31 folds rewrote Task P0 (entirely new), E6's nine-element enumeration, QA 1(e), the DB invocation discipline, Task B, G1/G2, A6′/A8′ (new pinned texts), A1′ (heavily), A4′ (restructured), the TREE derivation and the FORWARD block.
- Weak spots: w2 **5 folded, 1 HIGH** — **1 pre-existing / 4 fold-introduced**. **HIGH (1.1):** the Edits preamble asserted *every* edit replaces one physical line with one physical line — false since walk 1 turned E4 into an insert and added E8; an agent acting on it would have OVERWRITTEN the very anchors E2/E3/E4/E8 exist to leave intact. Both mechanisms now stated explicitly with the edit ids split between them. **MED (1.2):** Task M's seven clause keys were DESCRIPTIONS, not literals, so the agent chose its own substrings and QA item 4 could not re-assert the same thing Task M asserted; all seven now pinned as literals, each verified at authoring to occur exactly once across the post-edit texts and zero times pre-edit. Item 4 re-based onto the pinned keys. **Fold-introduced record decay (3):** the step-1 header still said "five edits", and the dev-log mandate and Deposits line still named the range E1–E6, after E7/E8 landed at walk 1.
- Destruction: w2 **1 folded** — **0 pre-existing / 1 fold-introduced**. The ACID-added degraded path skipped Task B without saying so, leaving an agent free to take a post-flip backup and file it under the `pre-*` name — a misleading artifact a later restore would trust. Skip is now stated as correct, with post-flip `pre-*` naming forbidden. Verified sound: A6′ lets the panel convene on a judged stop, which is not a relaxation — the panel is explicitly not waived and its findings re-open the walk on the same terms as any lens.
- Vulnerabilities: w2 **1 folded** — **0 pre-existing / 1 fold-introduced**. P0.2's reconstruction query carried an ellipsis where the id list belongs, leaving the agent to fill in an immutable input; the literal seven-id list is now given.
- Integration-record: w2 **2 folded (1 pre-existing / 1 fold-introduced), plus one premise verified** — **1 pre-existing / 1 fold-introduced**. ⚠️ **Verified rather than assumed:** A1′ mandates a fold-origin token in the Cycle Log while §3 forbids the log carrying strings a gate matches — a real-looking contradiction. `gates.py` has **zero** fold-token matches, so the §4 lint WARN is a linter signal and not a daemon gate, and the two rules do not collide. Folded: the record of walk 1's Scope sweep QUOTED the stale strings it had removed — the §3 reflexive class for the **fourth** time this cycle; de-quoted to describe.
- ACID (a2, run apart over walk 2's folds): **4 folded, 3 HIGH** — **0 pre-existing / 4 fold-introduced (100%)**. Every finding was created by the culmination immediately before it — proposal 247's law at n=11, observed on the plan that codifies it. **HIGH (5.2) — the FORWARD row was aimed at the wrong repository AND its guard was unsatisfiable.** Read the daemon: `_apply_ledger_updates` calls `_append_forward_row(project_path, …)`, so the row lands in **lessons-forge**'s register while the deferred `plan_lint` work queues in **bellows**' (six existing `plan_lint` rows live there) — the §6 obligation would have been filed where nobody working on the linter looks. And the a1-added post-condition told the step to verify its own row afterwards, which cannot work: ledger updates apply *after* transcript parse and worktree teardown, so nothing the step can read yet contains it. Both replaced by recording the text in the dev log for a Rule 42 direct edit at wrap — the mechanism this plan already uses for row 41, for the same reason. **HIGH (5.2):** walk 2 pinned seven clause keys as literals drawn from A1′–A4′ and bound them to nothing; a later fold to any A-prime would silently strip a key and HALT Step 2 on a *correct* doctrine. A0(4b) now asserts each key is a substring of its source before any edit lands — failing early, in the plan, rather than late, after the doctrine is written. **Fold-introduced, caught within the pass (4th finding, LOW):** correcting the Forward channel left QA item 7 still asserting that Step 1 opens a row — the record decaying inside the same phase that changed the instruction; re-based, and extended into a positive assert that the dev log actually carries the owed text. **HIGH (5.1/5.4):** the degraded path added at a1 declared the same five deposits as the fresh path, three of which **cannot exist** on it — the deposit gate would have pressed the agent to fabricate a rehearsal file for a rehearsal that never ran. Per-path deposits now stated, fabrication forbidden, pre-flip state recovered from the crashed run's own backup or recorded as LOST, and QA 3(c) made path-aware in both directions.
**Walk 3 — lenses 1–4:** **8 folded, 1 HIGH** — **2 pre-existing / 6 fold-introduced (75%)**. New surface from walk 2's culmination: A0(4b), the per-path deposits block, the rewritten FORWARD paragraph, QA 3(c) and item 7, Task M's pinned keys, the two-mechanism Edits preamble.
- Weak spots: w3 **5 folded, 1 HIGH** — **1 pre-existing / 4 fold-introduced**. **HIGH (1.3):** the `TREE` derivation existed **only in Step 1**, while Steps 2 and 3 both use tree-relative absolute paths — and each step runs in a fresh agent context and a fresh worktree, so nothing carries. Step 2's `.output` capture path and Step 3's `qa_report_path`/`evidence_dir` would each have been resolved by guesswork against a path that cannot be hardcoded. Derivation block added to both. **MED:** the per-path block said "all five deposits" against a Scope listing **six** — the count went stale when lens 2 added `pre-flip-state.txt` and ACID a2 wrote the prose without recounting. **MED:** G1 carried its invocation instruction twice, and the two copies disagreed on `-readonly`; collapsed to one. **LOW ×2:** the FINAL ACTION commit named "the Scope paths" with no path-awareness, so a degraded run would error on files that legitimately do not exist; and the Step-2 Deposits description still advertised "quoted doctrine sentences" after walk 2 replaced them with pinned literals.
- Destruction: w3 **1 folded, 0 pre-existing / 1 fold-introduced.** ACID a2's degraded branch read as "skip to Task M and G3", which could be taken to skip **P0.4/(3) — the doctrine and plan pins**. Those are what make Task M's mapping trustworthy, and on a path where the flip has ALREADY landed they are more load-bearing, not less. Both now stated as running on both paths; only G1/G2/Task B are skipped.
- Vulnerabilities: w3 **dry.** Re-ran the degenerate cases against the walk-2 surface: the corpus is contiguous 1..273 (COUNT=MAX=273), so the 266-line capture prediction holds and is hedged for run-time re-verification; sqlite `'now'` is UTC and satisfies the GLOB; a partial prior flip is caught by `PRE≠7` naming the offending id; the `id <= 273` bound still excludes an in-window ingest.
- Integration-record: w3 **2 folded, 1 pre-existing / 1 fold-introduced.** The Scope section still said the plan "opens a FORWARD row" — stale since ACID a2 moved it out of the ledger channel; re-based to name the dev-log carrier and the Rule 42 landing. ⚠️ **Verified rather than assumed:** §2.7's bullets are physically consecutive (the `Execute against real data.` bullet at doctrine line 91 is immediately followed by the next bullet), so QA 1(c)'s `A3′ = A2′ + 1` adjacency assert is satisfiable rather than a guess.
- **Probe note, recorded because it changed a conclusion twice:** the verification probe for the `TREE` finding first reported step 3 as SAFE — the step span runs to end-of-file and absorbed the Cycle Log's own mention of the derivation command — and a second probe was then fooled by a section boundary matching an inline mention of the block name at the Scope section. Only a line-addressed search gave the true answer. The `probe-must-match-representation` class, hit twice inside one lens on this plan's own tooling.
- ACID (a3, run apart over walk 3's folds): **4 folded, 1 HIGH** — **2 pre-existing / 2 fold-introduced**. **HIGH (5.4) — the backup had no destination, and the destination it would have been given DESTROYS it.** Task B's `.backup` target was an undefined placeholder; the obvious reading resolves it inside the dispatch tree, which is a **worktree torn down at step end** — so the only restore instrument for this plan's one irreversible write would have been deleted by the teardown that follows it, and nothing downstream would have noticed until a restore was actually needed. Pinned beside the canonical DB, where the untracked-DB policy already keeps such files. **MED (5.2):** the dispatch-tree root was written **four different ways** — three of them undefined placeholders — in a plan whose own walk-2 fold established that placeholders left for the agent to fill are a defect class; unified and swept to every site. **MED (5.2):** a dangling referent — the Step-2 commit warned that an uncommitted deposit strands QA on a fallback that is defined nowhere; replaced with the real mechanism (committed deposits carry through the teardown merge into Step 3's tree; an uncommitted one leaves QA with nothing to read). **MED (5.4):** ACID a2 created a SECOND wrap-owed register row, leaving that work named in three separate places and collected in none; one Method line now names both rows, their target register, and the fact that neither travels the ledger channel.
- **Out of scope, recorded not folded:** §2.8's oscillation rule reads *"the per-lens finding count stops trending toward dry"* — already in tension with §2's own falling-count warning at v1.8, and the new bar makes it staler. It is not part of D1/D2/D3, so widening this plan to reach it would repeat the scope-creep 330's cut pass paid for. **Route: the DC-surgical batch.**
- **CEO directive folded mid-pass:** version target 1.9 → **2.0**. Swept to all seven sites in this plan. ~~Plus the six in the held §4-prose draft … caught by sweeping rather than patching where noticed.~~ ⚠️ **RETRACTED at panel seat 4 (§2.7 attestation integrity — struck, not quietly corrected): that sweep was HALF-APPLIED and the claim was false.** Two sites in the held draft still carried the old version at seat 4 — E3's History-row instruction (`opens **1.9 …`) and QA (d)'s anchor parenthetical — so that draft would have bumped its version line to 2.1 while writing a row opening 1.9 above a bullet this plan displaces. **The parenthetical claiming the half-applied class had been avoided was itself a specimen of it**, and the Method note two hundred lines away stated the same obligation correctly in the future tense while this line stated it as done. Both sites landed at seat 4; the claimed-fold-never-landed class, caught only because a cold reader opened the named artifact.
**Cold panel (T2), run under a one-directive autonomous grant (waiver bounded to this panel); five seats with at least one register-plain, per D4. Sequential, author-verified and folded between seats.** **COMPLETE — five seats, 13·14·10·11·12 = 60 findings, 20 HIGH, every one author-verified real and folded, per-seat committed.** ⚠️ Yield did not decay across seats (13/14/10/11/12), reproducing §2.6's flat-yield prediction; the register-plain seat 3 produced 5 HIGH, the FIFTH consecutive adds-not-monopolizes datum; and the system seat found its entire HIGH set at seat-to-seat seams — a class no earlier seat could see.
- **Seat 1** (weak-spots-cold + evidence-attack brief): **13 findings, 3 HIGH**, all author-verified real, all folded. HIGHs: `$TREE` inside the `.sql` files — sqlite does not expand shell variables, so G2 died at `extra parameter` with the plan's own diagnostic branch naming the wrong cause and its only cure forbidden two lines above (a defect ACID a3's notation sweep introduced); **A3′ had narrowed 271's re-read to judged-stop closes when 271's only measurement comes from a post-DRY close** — the evidence-attack brief catching an exclusion that silenced the class its cited evidence measured; and **259/258 are PARTIAL, not implemented** (259's cross-plan-measurement ask is the packet's D5, routed to the funnel; 258's phase-labelling quarter is unimplemented), so a terminal `implemented` flip would have over-claimed both. Also: the Cycle Log's own arithmetic did not reconcile — headline 63 against per-lens lines summing to 56, three walk headers disagreeing with their own splits — **on the artifact whose subject is making that number auditable**; recomputed and now invariant-held. Plus 247's clause key was 259's clause; Rule 20's hedging scan is row-scoped not whole-file; the §4 mechanism was mis-described in both carriers; no fresh-path assert on `pre-flip-state.txt`; G1/G3 column order swapped; D4/D5 had no stated landing.
- **Seat 5** (ACID-cold + SYSTEM brief, last on the fully-folded artifact): **12 findings, 5 HIGH**, all author-verified real, all folded. ⚠️⚠️ **ITS CENTRAL RESULT, and the most valuable thing the panel produced: EVERY ONE of its five HIGHs lives at a SEAM BETWEEN TWO SEATS' FOLDS, not inside either fold.** Seat 2 scoped the blockquote-strip mandate to PLACEMENT in Step 1; seat 3 made `-x` mandatory in Step 3 — neither wrong, and together they return **0 for all seven primes against a perfectly correct doctrine**, HALTing after the flip. ACID a2 removed the Forward block; seat 4 asserted every step emits one — a direct cross-seat contradiction with nothing sweeping between them. ACID a1 added the re-entry branch; nobody path-scoped A0(5) when seat 2 path-scoped A0(4), leaving **the recovery branch unreachable through its own preconditions**. ACID a2 built the per-path deposits; seat 2 repointed `evidence_dir`; the Rule 20 carve-out ended up naming the one file the degraded path DOES produce and omitting the one that can be LOST — **a dead end on the plan's own recovery branch, after the irreversible write.** ⚠️ **And the declared conformance state was FALSE:** four WARNs, not three, the extra one caused by seat 4's own Cycle-Log bullet quoting a gate-matching token — §3's reflexive class for the FIFTH time, on the plan that edits §3. Fixed by de-quoting and re-earning the set, per §3's own re-run rule, not by editing the count. ⚠️ **Then a SIXTH instance: the fold that closed the fifth re-tripped it** — the replacement Closing line contained a bare gate token, caught only because the set was re-run after writing it. MEDs: three mandates each claimed to be "first" in Step 2 and two in Step 3, one of them literally unsatisfiable — now one numbered order per step, with the re-entry probe deliberately ahead of the Rule 15 receipt read (otherwise a partial dev log STOPs the step before the DEGRADED path can ever be entered); the step's first DB query sat OUTSIDE the invocation discipline's own stated scope; item 3(b) left the capture SELECT unpinned though G2's carries `category` and G1/G3's does not, so a re-typed adjacent query makes all 266 lines differ and the item HALTs on a plausible, wrong diagnosis; the Method lead-in still read TWO/NEITHER after seat 4 struck row 41; and **the cycle-to-date running tally was itself the construct §3 forbids** — removed rather than corrected, per-pass counts retained. LOWs: a named-recoverable Step-1 crash state with a diagnosis and no route (the gap where an agent improvises by committing unvalidated doctrine edits under a slug-named message), plus four stale record statements.
- **Seat 4** (integration-cold + clone-diff RECORD half): **11 findings, 3 HIGH**, all author-verified real, all folded. HIGHs: **the entire Rule 8 / 15 / 17 chain was absent** — no Deliverable Verification, no Output Receipts, no prior-step status read — where clone origin 330 carries all three; the plan's pins are IDENTITY checks that prove the bytes are Step 1's and prove nothing about whether Step 1 FINISHED, so a partial Step 1 drawing a mistaken continue would reach the irreversible flip and only be caught after it. **A fold this Cycle Log recorded as landed had not landed** — the version sweep into the held §4-prose draft was half-applied, leaving it set to bump its version line to 2.1 while writing a History row opening 1.9; the parenthetical claiming the half-applied class had been avoided was itself a specimen of it. Landed at this seat and the false attestation **struck in place** per §2.7 rather than quietly corrected. And **E6's element list omitted the inheritor clause** `## When this file changes` requires and both recent History rows carry — the gap 329's own panel closed one version earlier — plus the DB-disposition clause, on the plan whose second step IS the flip; now twelve elements with twelve asserts. MEDs: the Closing line was stale by two seats and 24 findings while the panel line beside it was current (now bound by a ledger constraint); **row 41 was mis-owned** — its work is a §4 edit this plan does not touch, so closing it here would be a false entry in the register the shop uses to find undone work; **no Conflict Ledger existed** despite a real cross-lens conflict joint-resolved at seat 2, and §2.8 mandates one per fold; §5's recording clause was unmet (last recorded phase was walk 1, nine culminations back); the header's declared scope carried no supporting evidence, and 330's row-9 **executable** consumer half had been reduced to a source quote — a third drop of a guard 330 itself restored from 326. LOWs: the HALT-routing rule kept 330's no-re-derive clause but dropped its degraded carve-out while ADDING the path that needs it; three citations failed a read at source, including a wrong seat attribution that poisons the very brief-yield query §2.6's Attribution convention exists to keep cheap.
- **Seat 3** (vulnerabilities-cold, **REGISTER-PLAIN** — no aimed brief, per D4): **10 findings, 5 HIGH**, all author-verified real, all folded. ⚠️ **FIFTH consecutive adds-not-monopolizes datum, and the strongest yet: the plain seat produced five HIGHs that neither aimed brief found, and MEASURED almost all of them by execution rather than reading.** HIGHs: it applied all eight edits mechanically and got `11 4` from `git diff --numstat` against the plan's pinned **13/4** — a bad sum introduced at panel seat 2 that would have HALTed a perfect run after the irreversible flip; **`.backup` of a WAL database cannot be read with `-readonly`** (error 14, no `BK=` line), so the restorability assert could never pass and its two obvious improvisations either write sidecars into the CEO's restore instrument or reproduce the error — `?immutable=1` proven working; **A1′ carries 4 backticks and 10 apostrophes and A8′ 4 backticks, so NEITHER shell quoting form is safe** and a mangled pattern prints `0` into a plan that has trained the agent to read the count as the assertion; **the blockquote-corruption asserts panel seat 2's fix CLAIMED to exist did not exist** — seat 3 built the corrupted doctrine and showed all seven primes, all seven clause keys and the line counts passing every check as written, then supplied the `-x` and `^> ` forms that catch it; and **G3's expected row literal had its last two columns swapped**, seat 1's column fold having landed on the query and not the expectation. MEDs: Step 3's git commands carried no root `-C` operand while the preamble steers them into the dispatch worktree, where the in-window-edit guard **passes vacuously** on a file that repo has never seen; the `inside §3` bound for A8′ is unsatisfiable because §3's fenced example opens with a literal top-level heading; and Rule 20's hedging list contains `not run` and `skipped` — the degraded path's own vocabulary — so a truthful row CRITICAL-fails. LOWs: a duplicated `plan_slug` value and a parenthetical made stale by seat 2's own fold; two degenerate crash points where the resume-sweep gate produces nothing in the crash it exists for and a resuming agent mislabels its own uncommitted work as a foreign edit.
- **Seat 2** (destruction-cold + clone-diff vs origin 330 AND newest same-class 329): **14 findings, 4 HIGH**, all author-verified real, all folded — and the clone-diff earned the seat outright, since **nine of the fourteen are guards a parent states and this clone silently failed to inherit.** HIGHs: **329's blockquote-prefix conversion was missing entirely**, so seven doctrine lines would have been inserted as blockquotes while every QA presence grep passed — the verification chain is prefix-blind in both directions; **A1′'s residue enumeration contradicted §3's own compact-form rule** (*only the per-lens summary lines appear in the block*), the exact defect class E5 exists for, in a section this plan is editing — reconciled by bounding the log to a class list with detail in the scratchpad, and stating the exception in §3 itself; **the restored backup assert had lost its value predicate**, so a backup taken AFTER the flip would pass it — count-is-not-a-value-guard re-committed inside the guard restored to close it; and **shell state does not persist between invocations**, so a later bare `$TREE` expands EMPTY into `git -C ""` and a write at the filesystem root. MEDs: E6 named no amended units though §6 requires it and every prior row does; **the deferral licence cited was the wrong clause** — §6's is scoped to §1/§4, which this plan does not touch, while the `§1/§3` lockstep it DOES fire carries no licence (now discharged by a no-gate-change statement); 330's pre-commit governance denylist dropped; the flip's consumer semantics never stated (seat 2 measured them benign and the plan now asserts it from source); A5′ had deleted the unit name *review-target rotation* and its rationale, rotting a v1.2 History citation; the HALT-routing list omitted the doctrine file Step 2 both shasums and greps; A0 had no none-match catch-all and skipped cleanliness/version on re-entry; `evidence_dir` pointed away from the evidence, leaving `flip-readback.txt` a declared deposit no item asserted. LOWs: a zsh glob that aborts on the very no-match case its branch exists for; unstated blank-line structure making the numstat additions half circular.
**Confirming walk (cw, all four lenses over the panel-folded artifact; ACID a4 runs apart and has NOT run): 4 folded, 1 HIGH — 0 pre-existing / 4 fold-introduced (100%).** New surface named: the panel's 60 folds rewrote the execution orders, the Forward reconciliation, the pattern-file mandate, A0(5)'s scoping, the degraded Rule 20 carve-out, the DB-discipline placement, item 3(b)'s pinned query, the Conflict Ledger, the Rule 8/15/17 chain and E6's twelve elements. **HIGH (1.1/ordering):** Step 2's step-level order collided with Task P0's own internal numbering — **two different `(2)`s in one step** — and the Rule 15 block sat physically ABOVE the task holding step-order item (1), so an agent reading top-to-bottom would run the receipt check before the re-entry probe: **the exact failure seat 5's ordering fold was written to prevent, re-created by the fix for it.** Both step orders are now lettered and the text laid out to match. **MED (4.1):** Step 3's order statement carried the same collision — `(2)` meaning *Item 0*, `(3)` meaning *Items 1–5*. **MED (2.2):** Step 2 carried **no** Forward Register mandate while Step 3 asserts one `NONE` subsection PER STEP — panel seat 5's reconciliation reached Steps 1 and 3 and not this one. ⚠️ **That is the seam class seat 5 named, reproduced inside the fold that closed seat 5's seam finding.** **LOW (1.2):** A0's `(3b)` was laid out after `(4)` while retroactively amending `(2)` and `(3)`; moved into execution order with its label kept stable, because fifteen sites reference these numbers and renumbering would rewrite record lines. **Verified sound this pass:** E6's twelve elements and QA 1(e)'s twelve asserts are in exact parity; Step 2's Scope and Deposits both carry six entries; every remaining angle-bracket form is a legitimate runtime value, not an unfilled placeholder; pins 1/1 and all seven clause keys unique-and-earnable after 64 folds; lint holds at the three declared WARNs, earned. ⚠️ **Probe note:** the deposits-parity probe first returned a nonsense 91 from an over-matching regex — corrected by a line-addressed read, the same probe-must-match-representation class this cycle has now hit three times.
- **Untargeted walk (uw, all four lenses over the WHOLE artifact — not aimed at the collapse; §2.7's covers-not-targets rule applied to this cycle's own last pass): 1 folded — 0 pre-existing / 1 fold-introduced (100%), and it is RECORD-CLASS.** The finding: a Cycle-Log line asserting *the four bare `grep` hits are prose* — true when written at walk 3, stale at eleven after later folds added prose mentions. It changes nothing any executing agent does. Re-based to a re-measured-per-pass form rather than a carried number. **Executed this pass rather than read:** E6/QA element parity 12/12; `#### Forward Register NONE` present in all three steps; no unfilled placeholders (the four angle-bracket forms remaining are legitimate runtime values, each confirmed at its site); all five lens names present in the log; all four ledger constraints carry an executable check; the seven clause keys unique-and-earnable; **all seven A0(5) probes verified both EARNABLE (0 pre-edit) and EARNED (≥1 in the post-edit texts)** — the check that heavy folding could have silently broken by editing a probe's string out of its A-prime; every mandated literal search carries `-F` (the bare hits are prose); Step 3's only reference to the collapsed preflight resolves; and every numeric claim re-verified against reality — seven edits, 11/4 numstat, 266-line capture, PRE/CHANGES/GLOBOK/BK all 7, the seven-id list at 7 sites, and zero residual references to the pre-twelve-element History form.
- **COLLAPSE of Task P0 (CEO direction, §2.8's third resolution) + confirming read over the collapsed region: 0 findings.** P0 had been re-folded in FOUR consecutive passes — seat 5's ordering, cw's layout, a4's carrier pin, cw2's referent — every patch correct and every one creating the next. §2.8 names three-or-more re-folds on one region as the point to stop patching and ask whether the REGION is the problem. It was. **Six separately-bolted-on tasks — a lettered execution order, a TREE derivation, a DB-invocation-discipline block, a re-entry probe, a Rule 15 receipt read, and three pins — collapsed into ONE preflight with one contract:** *prove this step is entering a known corpus state, from a verified predecessor, into a world that still matches what that predecessor recorded.* Five clauses, P0.1 locate → P0.2 corpus state → P0.3 predecessor → P0.4 world-matches-record → P0.5 degraded. **The collapse also closed the ordering class outright:** there is no longer an order statement that can drift from a layout, because the block IS the order. ~1,700 characters of duplication removed (the standalone discipline block, and the degraded mechanics that had been stated twice). ⚠️ **Verified as a DIFF REVIEW, not a checklist** (the 332 lesson): all **26** guards the six tasks carried were enumerated against the collapsed block — TREE derivation and remote, shell-state warning, the empty-DB trap, exists-and-non-empty, invocation flags, no-heredocs, sqlite's no-variable-expansion, both absolute paths, the dev-log location, all three corpus states plus the none-match HALT, the probe-before-receipt rationale, the receipt read, the carrier pin, both value pins, and every degraded-path mechanic — **26/26 carried, none dropped.** All 14 `P0(n)` references swept to the clause names; 0 residual. Pins 1/1, seven clause keys unique-and-earnable, lint at the three declared WARNs.
- **Confirming pass (cw2, all four lenses over the a4-folded artifact): 1 folded, 0 pre-existing / 1 fold-introduced (100%).** **Weak spots — 1 folded, and it was ACID a4's own repair:** P0.3, the pin added one pass earlier to close an isolation window, carried `<step-1 dev log>` as an **unfilled placeholder**, and no part of Step 2 named that file's path — although (2) and (3) also read it for their comparison values. **The pin written to close a window was itself unexecutable.** Path now named at the task head and in the pin. **Destruction — dry** (the added path statement is additive; nothing relaxed). **Vulnerabilities — dry**, with two premises executed rather than read: `git -C <root> -- relative/path` resolves the pathspec against the `-C` directory even from a foreign cwd, so the new pin runs as written; and Scope/Deposits parity holds at 1/1, 6/6, 1/1 across the three steps. **Integration-record — dry** (C4 satisfied: the panel line and Closing line agree; the Walks line matches the passes actually run). ⚠️ **THE BAR IS NOT MET, and the reason is worth stating precisely:** the single finding is fold-introduced (satisfying the origin condition) but **logic-class, not record-class** — an unexecutable pin changes what an agent does. One condition of two. ⚠️⚠️ **§2.8 SIGNAL — Task P0 has now been re-folded in FOUR consecutive passes** (seat 5's ordering, cw's layout, a4's carrier pin, cw2's referent), every patch correct and every one creating the next. §2.8 names three-or-more re-folds on one region as the point at which to stop patching and ask whether the region should be **collapsed** rather than repaired. **That is a design question for the CEO, not a fold.**
- ACID (a4, run apart over the confirming walk's folds): **3 folded, 1 HIGH** — **0 pre-existing / 3 fold-introduced (100%)**. **HIGH (5.3) — the comparison CARRIER was unpinned in the step that acts on it.** P0.4 and P0.4 compare the doctrine and the plan against a DOC_SHA and PLAN_SHA read out of Step 1's dev log, and nothing in Step 2 pinned that log; Step 3 closes the same window at item 0(d) and Step 2 was left open. A verdict-window edit to the dev log feeds Step 2 wrong comparison values, **both pins pass against them, and the irreversible flip proceeds against a doctrine nobody verified** — the pins would be checking the doctrine against a number an editor chose. Added as P0.3; the 329 seat-5 class. **MED (5.2) — the order/layout mismatch recurred for a THIRD consecutive pass.** Seat 5 fixed the header and left the layout; the confirming walk fixed the layout and left the Rule 15 block BELOW both pins, contradicting the order it had just declared. It now sits between P0.2 and P0.4, which is also substantively correct: a non-Complete receipt must stop the step **before** the pins try to read values the dev log may not carry. **MED (5.4):** A0(3b) promised that `resume-sweep.txt` *joins Scope on that path* and the Scope block never named it — so the durable HALT artifact seat 2 added could not be deposited without a `scope_check` failure, on the one path where the CEO has nothing else to read. Conditional Scope entry added in the 329 form. **Verified sound:** the confirming walk's two unasserted string replacements both landed (checked rather than assumed — the assert-the-post-condition class); Step 3's lettered order matches its layout exactly; A0's moved `(3b)` reads correctly in its new position with every one of its fifteen reference sites intact.
**Conflicts:** one cross-lens conflict arose and was joint-resolved in one move at panel seat 2 — A1′'s residue enumeration vs §3's compact-form rule — settled by bounding the log to a class list and stating the exception in §3 itself. Ledger added at panel seat 4, which found the plan had none despite §2.8 mandating one per fold (and noted that A0(4b) is a run-time check compensating for a drafting-time constraint that was never recorded):
- **C1** — A1′'s residue enumeration and §3's compact form stay reconciled (check: A4′'s stated bounded exception).
- **C2** — each of Task M's seven clause keys stays a substring of its source A-prime (check: A0(4b)).
- **C3** — the doctrine commit precedes and pins the flip (check: P0.4).
- **C4** — the panel line and the Closing line are rewritten by the SAME edit that appends a seat, never one alone (check: both carry the seat count and the per-seat yields — **not** a running cycle total, which §3 forbids; panel seat 5 found the two lines comparing nothing).
**Closing:** the untargeted walk (uw) MET THE BAR — 1 finding, record-class only (one stale count claim in this log), 1 of 1 fold-introduced; closing-record re-read run per §2.7 — **it found 1 record-class error in this line and it is corrected below**; **judged stop, deposited once.** ⚠️ **Reasoning, recorded so a later reader can judge it:** this is a judged stop, not a dry close, and it is the NORMAL outcome v2.0 defines rather than a deviation. The cycle ran 3 walks + 4 ACID passes + a five-seat cold panel + a §2.8 collapse + 2 confirming passes + this untargeted walk — **125 findings.** Post-panel yields were 4 → 3 → 1 → 0 → 1, every one 100% fold-introduced: the cycle had been converging on its own repairs, which is precisely the noise floor this amendment codifies. The §2.8 collapse of Task P0 closed the recurring class structurally rather than patching it a fifth time. ⚠️ **Expected lint state at deposit: exit 0 with THREE WARNs**, exactly as declared in the Conformance line — the two `(o1)` output-path classes plus the §4 closing WARN. ⚠️ **CORRECTED BY THE CLOSING-RECORD RE-READ, and this is the specimen 271 predicted:** this line first claimed FOUR, reasoning that the judged stop would ADD the §4 WARN. It does not — that WARN had been firing throughout the cycle and was already one of the declared three. The §4 divergence is real and this close is its first live instance, **but the arithmetic asserted about it was wrong, and it was wrong in prose written after the last pass had run** — the region no walk can reach by construction. **The re-read was mandatory under this very amendment (A3′), it ran, and it caught a false claim on its first use.** That is the pairing argument for D2 made by the artifact rather than argued in the packet.
