# Executable: Gate-1 routing write for proposals 327–332 — 5 → `accepted|codify`, 1 → `reference|backlog`, per the cold-panel cluster synthesis

**Type:** Executable
**Project:** lessons-forge
**Depends on:** executable-359 (Done — classified the six, stamps NULL), executable-350 (Done — the routing-write lineage, direct clone origin and newest same-class), executable-357 (Done — ingested the batch)
**Created:** 2026-08-12
**Author:** Planner
**Slug:** `gate1-write-327-332-2026-08-12`
**dispatch_mode:** bellows
**pause_for_verdict:** always
**Priority:** 5
**cycle_tier:** T1
**qa_steps:** 2
**Test Scope:** targeted (Rule 21 — single module, session-verified; QA row 4 re-derives; baseline 55/0)

⚠️ **ID NOTE:** id read at deposit (`next_id` **360** at authoring — a PREDICTION; 358 went to the parallel terminal, the freeze reads fresh).

## Why
Gate 1 for the six is DECIDED (CEO-directed 2026-08-12, taken on plan 359's cluster synthesis and flag-(G) dispositions, reversible): **A-set** `327,328,329,330,332` → `accepted|codify` (the §2.6 cold-panel bundle: the seat-prompt contract + `PANEL_SEAT_TEMPLATE.md` build (327 — the template ships as a governance-root artifact, RULE_20_SELF_CHECK_BLOCK precedent), meter-at-convene (328), the execution brief (329), register schema 0.2 (330 — ⚠️ the schema spec is currently defined NOWHERE in doctrine, only as a register-file convention; the Gate-2 plan locates or creates its home), the four panel structures (332 — the HOW behind 328/330, bundle-mate)); **P-set** `331` → `reference|backlog` (the cold-front-timing SHAPE DECISION — its own entry text forbids codifying it as a bullet; parked AND SCHEDULED, not just parked: **the row text, pinned verbatim for the wrap's Rule-44 owner-mismatch channel into the GOVERNANCE register:** *Cold-front timing decision packet (proposal 331, entry 323): assemble the shape-session packet — the 2026-08-11 origin table (7× cold pre-existing yield), the gate2-pt3 walk-vs-panel split (2 vs ~22), the fresh-readers register — and decide the warm/cold allocation and seat composition. Owner: governance/DRAFTING_CYCLE. | deferred-work | 360 | open*).

**Dedup against live v2.5 (Rule 58 — anchors named, licence to disagree granted: A0's live read overrides this table, HALT + CEO re-decides on any mismatch):** 328 EXTENDS §2.6's existing meter mandate (the 286 bullet fixes cadence; the entry adds CREATION TIMING — partial, not duplicate); 329 ADDS a brief to the existing seat-brief registry (extension); 332's scripted battery EXTENDS 286's residue-battery cadence (mechanization, not restatement); 327 has NO existing artifact (`PANEL_SEAT_TEMPLATE.md` measured absent at authoring); 330's schema versioning has no doctrine home (measured: `schema_version` appears in register files only). No full duplicates; all five codify dispositions are extends-or-new.

⚠️ **Sentinel posture — the 350 cleanest case, RE-MEASURED not inherited: all six prior stamps are NULL** (359's classifier set `proposed_at` only; measured per id 2026-08-12), so `status_updated_at IS NOT NULL AND` Z-GLOB is a complete value guard with **no prior-value exclusion needed** — stated so nobody clones one in.

## Scope
- **One DB write session, two scoped UPDATEs in ONE transaction** at the canonical absolute path; `status_updated_by='ceo'` (the Gate-1 authority, lineage convention). No doctrine touch, no LESSONS touch, no FORWARD touch by any step (the pinned row rides the WRAP channel, Planner-appended — receipts say `NONE`).
- Env facts: the standing four (ugrep `-F` + zero-count exit-1, printed count is the assertion; same-invocation state; `find` never glob; canonical absolute DB path — a bare relative name CREATES an empty file).
- **Post-write expected corpus:** `accepted|codify` = **5**, `proposed` = **0**, `reference` STATUS = **16** with route split **9 `reference` + 7 `backlog`** (six pre-existing backlog-route rows measured — ids 161/169/291/294/299/301 — plus 331; ⚠️ walk 2: the drafted `15+1` split was ASSUMED from the status count and measured WRONG), `stale` = 3, total 332.

## Freeze checklist (deposit path — items 1–4 BEFORE the copy, item 5 immediately AFTER it)
1. Substitute the read id at the bootstrap `<id>` site AND the pinned FORWARD row's `<plan-id>` token; probes, OCCURRENCE form: `grep -oF -- '<id' <deposit-path> | wc -l` → **2** and `grep -oF -- '<plan-id' <deposit-path> | wc -l` → **2** (each: both residual tokens on this checklist line).
2. **Diff the draft against the mirror immediately before the copy** — an empty diff is the deposit precondition (the plan-359 stale-mirror variant of the least-reviewed-last-edit class).
3. Final `plan_lint` at the FAITHFUL scratchpad mirror (NEVER the real `decisions/` — the daemon claims AND DISPATCHES same-second, proven live) — WARN set matches the Conformance paragraph.
4. A0-fresh re-check: all six still `proposed` with NULL stamps; `accepted|codify` still 0.
5. Post-copy, same minute: `ls` the real `decisions/` — the claim reads `in-progress-executable-<READ-ID>.md` with the item-1 id (mismatch = foreign in-window consumption — report, never re-copy).

## Conflict Ledger
**C1** both id-sets immutable — a sentinel mismatch is never resolved by editing them. **C2** backup adjacent, `BK=6` by value via `?immutable=1` against THE FOUND BACKUP's absolute path. **C3** capture inside the transaction, before the UPDATEs: `id <= 332 AND id NOT IN (327,328,329,330,331,332)` → **326 rows** (contiguity measured). **C4** sentinels BY NAME: PRE, PRE_A, PRE_P, BK, CHANGES_A, GLOBOK_A, CHANGES_P, GLOBOK_P — EIGHT, counted. **C5** commits cd-first + pathspec + name-only + toplevel, post-commit asserts `-C`-pinned to the printed hash, never `HEAD` (Rule 85 as amended; a parallel terminal commits in-window, proven twice this session). **C6** serialized dispatch stated; non-dependent guards at A0/QA.

## How to Run This Plan
**Bootstrap:**
```
Read the plan at knowledge/decisions/in-progress-executable-360.md (the daemon renames on claim). Execute Step 1 ONLY. After completing Step 1, STOP and wait for my confirmation.
```
⚠️ HALT ROUTING: Step 1 reads this plan + the canonical DB. Step 2 reads this plan, the dev-log, the DB (read-only), the merged captures, `/Users/marklehn/Developer/GitHub/RULE_20_SELF_CHECK_BLOCK.md`.

---

## Drafting Cycle

**Tier:** T1 — scoped 6-row routing write, the 350 lineage's smallest instance; clone of 350's final form with the sets, sentinels and capture re-derived from live measurement (the cleanest-sentinel premise RE-VERIFIED, not inherited).

**Walk 0 (context pin):** six rows `proposed|NULL-stamp` measured per id; capture bound 332−6=**326** (contiguity measured); `accepted|codify`=0, `reference`=15, `stale`=3 at authoring; route CHECK constraint admits `codify/backlog/reference` (source-read `src/db.py:44`); `PANEL_SEAT_TEMPLATE.md` absent; `schema_version` doctrine-homeless (both dedup anchors). Clone-diff vs 350: sets 12→6 and two-way→two-way (A/P for A/R), the 325-target-reversal machinery DROPPED with its premise absent (no target corrections here — stated), sentinel count 9→8, the NULL-stamp premise re-measured, freeze gains items 2 (the 359 stale-mirror guard) and 5 (the id-claim verify); CAPTURE_COMMIT asserts carried from the 356 lineage.

**Walk 1** (whole artifact, five lenses, sequential):
- Weak spots:          w1 1 folded — instruction, clone-adaptation (the post-write expected-corpus line initially wrote `reference|reference`=16 from 350's recall; 331 routes `reference|backlog`, so the VALUE guard is the STATUS count 16 with route split 15 `reference`-route + 1 `backlog`-route — respelled at both sites, QA row 3 asserts the split explicitly).
- Destruction:         w1 dry (one transaction, all-or-nothing; the three-set reversal branch dropped WITH its stated premise; no guard relaxed).
- Vulnerabilities:     w1 executed — the G1/G2 SQL rehearsed against a scratch DB copy: PRE=6/PRE_A=5/PRE_P=1, CHANGES_A=5/GLOBOK_A=5/CHANGES_P=1/GLOBOK_P=1, capture 326 lines, re-run CHANGES_A=0 (idempotence fail-safe); zero-count probes never `&&`-chained.
- Integration-record:  w1 dry (the Rule-44 pin follows the 356 precedent verbatim including the substitution-site freeze probe; the dedup table cites live v2.5 reads).
- ACID:                w1 dry (single write step + QA behind one gate; the NULL-stamp value guard is complete without exclusions; half-states owned by A0's branches; backup is the durability floor).

**Walk-1 split: instruction 1 / record 0.** Re-opens; walk 2 owed.

**Walk 2** (whole artifact; new surface = the walk-1 respell):
- Vulnerabilities:     w2 executed, **1 folded — instruction: the FULL G1/G2 rehearsal on a scratch DB copy returned every sentinel exact (PRE 6/5/1, CAP 326, CHANGES/GLOBOK 5/5/1/1, re-run 0 fail-safe) AND `POST_REF_BL=7` — the walk-1 respell's `15+1` route split was itself a RECALLED number; measured: SIX pre-existing backlog-route rows (161/169/291/294/299/301). The predicted-number class, caught by the rehearsal the pin exists to gate.** Both sites corrected to the measured 9+7.
- The other four:      w2 dry.

**Walk-2 split: instruction 1 / record 0.** Re-opens; walk 3 owed — one measured-constant firming, the convergence shape.

**Walk 3** (whole artifact; new surface = the corrected split):
- Weak spots:          w3 dry (the 9+7 pin re-verified FROM THE ARTIFACT against a fresh live measurement at both sites).
- Destruction:         w3 dry (nothing relaxed by the split correction).
- Vulnerabilities:     w3 dry (rehearsal outputs stable on the scratch copy; the corrected pin matches POST_REF_BL=7).
- Integration-record:  w3 dry (the walk-2 catch recorded at both sites and here; pins consistent).
- ACID:                w3 dry (one transaction unchanged; the value guard now measures the route split it claims); lint at the FAITHFUL mirror on the deposit-shaped bytes — the Conformance paragraph carries the measured set.

**Walk-3 split: instruction 0 / record 0 — DRY. The §2 bar met on the dry branch; T1, no panel owed.**

**Conformance (§5):** shape-stability run post-walk-1, re-runs after every culmination and at deposit, at the faithful mirror. **History: post-walk-2 → EXIT 0, ONE WARN (fold-as-last-event — the walk-2/3 record had collapsed the named lens lines, hiding the dry close from the §4 last-lens-line check); cleared EARNED by expanding walk 3 to the five named lens lines. Final: EXIT 0, ZERO WARNs.** Last run: at deposit.

**Closing:** walk 3 dry — instruction 0 / record 0; closed on the dry branch after 3 walks; clone-diff at walk 0; residue: none.

---

## STEP 1 — DEV (the two routing writes, one transaction)

> **FIRST — visible chat message; do NOT rename this plan file.**
> **A0 (first match wins):** (1) both sets landed (A-set `accepted|codify`, P-set 331 `reference|backlog`) → verify per id, RECOVERY receipt if captures lost, report complete. ⚠️ **A partial set-state is UNREACHABLE — G2 is ONE transaction; both UPDATEs commit atomically.** (2) fresh — all six `proposed`, stamps NULL, `accepted|codify`=0 → proceed; ⚠️ **a `pre-g1cp-*.db` backup already on this path** (a crash before G2's COMMIT leaves one with nothing written) → REUSE via prefix-only `find` (never a second; >1 match → HALT) and run the `BK=6` assert against it. (3) Other observed state → HALT with the per-id read-back.
> **B — backup**, exactly: `sqlite3 -bail /Users/marklehn/Developer/GitHub/lessons-forge/lessons-forge.db ".timeout 5000" ".backup /Users/marklehn/Developer/GitHub/lessons-forge/pre-g1cp-$(date -u +%Y%m%d_%H%M%S).db"` (exit 0, empty stderr); locate via prefix-only `find`; `?immutable=1` assert: `SELECT 'BK='||COUNT(*) FROM lesson_proposals WHERE id BETWEEN 327 AND 332 AND status='proposed';` → **BK=6**; else HALT.
> **G1 — rehearsal** (runner for G1/G2 alike: `sqlite3 -bail /Users/marklehn/Developer/GitHub/lessons-forge/lessons-forge.db ".timeout 5000" ".read <abs>"`, exit 0 + empty stderr per invocation, same-invocation scratch outside git trees; file `knowledge/development/g1cp-rehearsal.sql`, exactly):
> ```
> BEGIN IMMEDIATE;
> SELECT 'PRE='||COUNT(*) FROM lesson_proposals WHERE id BETWEEN 327 AND 332 AND status='proposed' AND status_updated_at IS NULL;
> SELECT 'PRE_A='||COUNT(*) FROM lesson_proposals WHERE id IN (327,328,329,330,332) AND status='proposed';
> SELECT 'PRE_P='||COUNT(*) FROM lesson_proposals WHERE id = 331 AND status='proposed';
> ROLLBACK;
> ```
> Assert **PRE=6, PRE_A=5, PRE_P=1**; any off → HALT naming the set.
> **G2 — the writes** (`mkdir -p` the evidence dir FIRST; file `knowledge/development/g1cp-flip.sql`, exactly; `.output` absolute from `pwd`):
> ```
> BEGIN IMMEDIATE;
> .output <tree-abs>/knowledge/qa/evidence/gate1-write-327-332-2026-08-12/outside-range-ids.txt
> SELECT id||'|'||status||'|'||COALESCE(route,'')||'|'||COALESCE(status_updated_at,'') FROM lesson_proposals WHERE id <= 332 AND id NOT IN (327,328,329,330,331,332) ORDER BY id;
> .output stdout
> UPDATE lesson_proposals SET status='accepted', route='codify', status_updated_at=strftime('%Y-%m-%dT%H:%M:%SZ','now'), status_updated_by='ceo' WHERE id IN (327,328,329,330,332) AND status='proposed';
> SELECT 'CHANGES_A='||changes();
> SELECT 'GLOBOK_A='||COUNT(*) FROM lesson_proposals WHERE id IN (327,328,329,330,332) AND status='accepted' AND route='codify' AND status_updated_at GLOB '20[0-9][0-9]-[0-9][0-9]-[0-9][0-9]T[0-9][0-9]:[0-9][0-9]:[0-9][0-9]Z';
> UPDATE lesson_proposals SET status='reference', route='backlog', status_updated_at=strftime('%Y-%m-%dT%H:%M:%SZ','now'), status_updated_by='ceo' WHERE id = 331 AND status='proposed';
> SELECT 'CHANGES_P='||changes();
> SELECT 'GLOBOK_P='||COUNT(*) FROM lesson_proposals WHERE id = 331 AND status='reference' AND route='backlog' AND status_updated_at GLOB '20[0-9][0-9]-[0-9][0-9]-[0-9][0-9]T[0-9][0-9]:[0-9][0-9]:[0-9][0-9]Z';
> COMMIT;
> ```
> **Sentinels CHANGES_A=5, GLOBOK_A=5, CHANGES_P=1, GLOBOK_P=1** (the NULL prior stamps make the bare Z-GLOB a complete value guard — no exclusion, per the Why); any off → HALT with numbers. Capture **326 lines**, read post-commit — mismatch = record + HALT with the writes landed, never re-run. Both id-sets IMMUTABLE.
> **G3 — read-back**, exactly: `sqlite3 -bail -readonly /Users/marklehn/Developer/GitHub/lessons-forge/lessons-forge.db ".timeout 5000" "SELECT id||'|'||category||'|'||status||'|'||COALESCE(route,'')||'|'||status_updated_by||'|'||status_updated_at FROM lesson_proposals WHERE id BETWEEN 327 AND 332 ORDER BY id;"` → RAW to `routing-readback.txt`: 327/328/329/330/332 all `accepted|codify|ceo|<Z>`, 331 `reference|backlog|ceo|<Z>`.
> **Receipt** with EIGHT named sentinels — PRE, PRE_A, PRE_P, BK, CHANGES_A, GLOBOK_A, CHANGES_P, GLOBOK_P (the receipt IS BK's observer) · `### Ledger Updates` · `#### Prompt Feedback` · `#### Forward Register`: `NONE` (the pinned governance row rides the WRAP channel — Planner-appended, per Rule 44). **FINAL ACTION** — commit deposits: `cd <your-tree-abs>` first token, pathspec on the COMMIT, name-only assert, bare `git rev-parse --show-toplevel` last.
>
> **Scope:**
> - `knowledge/development/dev-log-gate1-write-327-332-2026-08-12.md`
> - `knowledge/development/g1cp-rehearsal.sql`
> - `knowledge/development/g1cp-flip.sql`
> - `knowledge/qa/evidence/gate1-write-327-332-2026-08-12/outside-range-ids.txt`
> - `knowledge/qa/evidence/gate1-write-327-332-2026-08-12/routing-readback.txt`
>
> **Deposits:**
> - `lessons-forge/knowledge/development/dev-log-gate1-write-327-332-2026-08-12.md`
> - `lessons-forge/knowledge/development/g1cp-rehearsal.sql`
> - `lessons-forge/knowledge/development/g1cp-flip.sql`
> - `lessons-forge/knowledge/qa/evidence/gate1-write-327-332-2026-08-12/outside-range-ids.txt`
> - `lessons-forge/knowledge/qa/evidence/gate1-write-327-332-2026-08-12/routing-readback.txt`

## STEP 2 — QA

> **FIRST — do NOT rename this plan file. Deliverable Verification (Rule 8/17)**, ✅/❌ table, any ❌ → HALT. **Rule 20 self-check** (canonical block from `/Users/marklehn/Developer/GitHub/RULE_20_SELF_CHECK_BLOCK.md`; `plan_slug`: `gate1-write-327-332-2026-08-12`; `qa_report_path`: `<tree>/knowledge/qa/gate1-write-qa-2026-08-12.md`; `evidence_dir`: `<tree>/knowledge/qa/evidence/gate1-write-327-332-2026-08-12/`; `required_evidence_files`: `[db-invariants.txt, outside-range-ids.txt, routing-readback.txt, pytest_targeted.txt]`, all four BEFORE the block; literal stdout: the banner `Rule 20 — QA Self-Check Results` and the `PASSED — SELF-CHECK PASSED` line, byte-exact). `## Evidence and Narrative` after the table. ONE read-only DB form; RAW evidence.
> **1. THE ROUTES LANDED** — per id: 327/328/329/330/332 `accepted|codify|ceo` Z-stamped; 331 `reference|backlog|ceo` Z-stamped. → `routing-readback.txt` (re-run fresh, diff vs the deposited)
> **2. BLAST RADIUS** — re-run the EXACT capture SELECT (COPY from G2, never re-type), diff vs the deposited 326-line file; partition (impossible-id → HALT; deleted-row → HALT; else concurrent, named, no halt). → `db-invariants.txt`
> **3. CORPUS SHAPE** — `accepted|codify` = **5** (the new Gate-2 queue); `proposed` = **0**; `reference` STATUS = **16** with route split **9 `reference` + 7 `backlog`** (six pre-existing backlog rows — 161/169/291/294/299/301 — plus 331; ⚠️ the value guard is the SPLIT, not the status count — and walk 2 measured the drafted 15+1 split WRONG, the predicted-number class caught by the scratch rehearsal's POST_REF_BL=7); `stale` = 3 (98/121/130); total = 332. Zero-emitting forms; printed counts are the assertions. → `db-invariants.txt`
> **4. TESTS** — single-module premise re-derived (`find <tree-abs>/src -name 'test_*.py'` → exactly one; a second = report + run whole `src/`, never HALT); pytest vs 55/0, delta reported never asserted. → `pytest_targeted.txt`
> **5. CONSUMER SEMANTICS** — `get_unclassified_entries` (read-only) still `[]` (routing does not un-classify); the six source entries 319–324 present and unchanged (sentinel: entry-318 hash `260857bbc71e818b74f503f2984f2b6e5c2854e84e97e4522f9e74b2ccdd0cb8` intact). → `db-invariants.txt`
> `## Evidence and Narrative` · Receipt · `### Ledger Updates` · `#### Forward Register`: `NONE`. **FINAL ACTION** — commit deposits, cd-first + pathspec + name-only + bare toplevel.
>
> **Scope:**
> - `knowledge/qa/gate1-write-qa-2026-08-12.md`
> - `knowledge/qa/evidence/gate1-write-327-332-2026-08-12/db-invariants.txt`
> - `knowledge/qa/evidence/gate1-write-327-332-2026-08-12/pytest_targeted.txt`
>
> **Deposits:**
> - `lessons-forge/knowledge/qa/gate1-write-qa-2026-08-12.md`
> - `lessons-forge/knowledge/qa/evidence/gate1-write-327-332-2026-08-12/db-invariants.txt`
> - `lessons-forge/knowledge/qa/evidence/gate1-write-327-332-2026-08-12/pytest_targeted.txt`
