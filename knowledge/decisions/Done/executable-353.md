# Executable: Gate 2 — the DRAFTING_CYCLE five (315/317/318/319/325): v2.4 → v2.5, then flip the five to `implemented`

**Type:** Executable
**Project:** lessons-forge
**Depends on:** executable-350 (Done — wrote the routing + 325's target reversal; stamped all five `2026-08-12T00:29:31Z`), executable-348 + executable-346 (Done — the builder-mechanism lineage), the 2026-08-11 packet (`d08110b`; provenance), DRAFTING_CYCLE.md at v2.4 (precondition, A0)
**Created:** 2026-08-11
**Author:** Planner
**Slug:** `gate2-dc5-2026-08-11`
**dispatch_mode:** bellows
**pause_for_verdict:** always
**Priority:** 5
**cycle_tier:** T2
**qa_steps:** 2
**Test Scope:** targeted (Rule 21 — single module, session-verified ×7; QA row 5 re-derives; baseline 55/0)

⚠️ **ID NOTE:** id read at deposit (`next_id` **351** at authoring — a PREDICTION).

## Why
The five DRAFTING_CYCLE rows from Gate 1 (packet `d08110b`): **315** per-LENS commits (the sequential-fold observer), **317** the diff-review instrument, **318** clone-diff before walk 1 + carried-guards-vs-live-data, **319** the guard pricing gate, **325** the edit-anchor rule — the recovered declared addition, homed by 350's target reversal. Seven builder edits (five clauses + version 2.4→2.5 + History), one scoped 5-row flip. **Corpus path proper, no §6 deviation; real walks under the v2.4 bar (T2 — T-6 fires: doctrine + corpus).**

⚠️ **Sentinel exposure: the five's prior stamp `2026-08-12T00:29:31Z` is Z-form and GLOB-MATCHING** (350's write) — the one-value `NOT IN` exclusion carries the guard; re-measured, not inherited.

## Scope
- **One doctrine file** via the COMMITTED builder `governance/knowledge/research/builder-gate2-dc5-2026-08-11.py` (all-or-nothing; anchors count-asserted incl. the `--` guard for dash-leading literals; dry-run numstat **`7 4`**, all five clause probes earnable 0→1, `2.4 (2026-08-11)` 2→1, History 14→**15**).
- **One DB write:** the five `accepted|codify → implemented`, `status_updated_by='ceo'`, in-statement Z-stamp.
- **No code edit; no LESSONS/FORWARD touch.** §6 gate coupling: none — §2.6/§2.7 prose only; QA row 7 sweeps tokens with positive control (pointer swept, seat 4).
- Environment facts, VERBATIM (seat 2 — 348's panel restored these once already; the label form re-compressed them, the recurrence class):
  1. `grep` is a ugrep shim: **`-F` every literal**; a zero-match `grep -c` prints `0` and EXITS 1 — the printed count is the assertion; never `&&`-chain zero-count probes (QA row 7's four expected values are all 0 — this fact is load-bearing there; pointer swept at seat 4 after the row-6 insertion renumbered).
  2. Shell state does NOT persist — same-invocation scratch dirs.
  3. `find`, never a glob; `--` before dash-leading grep literals.
  4. The DB is gitignored and worktree-absent: a bare relative name CREATES an empty file — canonical absolute path only.
- Verdict-window posture: v2.5 governs from the Step-1 commit; HALT holds; rollback is CEO's.

**Rule 93 — declared blanket form (seat 2):** Step-1 mandates are observed collectively — the builder asserts by QA row 2's probe re-run, F/F2 by rows 1/3, CHANGES/GLOBOK by row 4(a), **BK and MAXID by the Receipt's named-sentinel mandate itself** (no QA row reads the backup — the receipt is their observer, seat 4); the category-baseline violation is constructed by row 4(a)'s pin, the commit-drift violation by the Rule-85 asserts. This line is the declaration.

## Freeze checklist (deposit path, before the copy)
1. Substitute the read id at the bootstrap `<id>` site AND in TASK F's `-m` string (both sites — the parent substituted both; seat 4); probe exactly, OCCURRENCE form: `grep -oF -- '<id' <deposit-path> | wc -l` → **2** post-substitution (both residual tokens live on this checklist line; seat 5 rehearsed the seat-4 `-cF` form and it false-fails at 1 — the 270 line-vs-occurrence trap, inside the freeze itself).
2. Final `plan_lint` — WARN set matches the Conformance paragraph.
3. A0-fresh re-check: the five still `accepted|codify` stamped `2026-08-12T00:29:31Z` **with categories matching the seat-3 per-id table**; doctrine sha still the A1 pin; builder re-run numstat still `7 4`.
4. The Closing line contains `instruction` and `/ record` with no italic-parenthetical placeholder (the 348 seat-5 assert, dropped by this clone — seat 4; earnable: fails today's placeholder, passes only once the real Closing lands).

## Conflict Ledger
**C1** the five-id set immutable. **C2** builder all-or-nothing; anchors asserted with `--` where dash-leading. **C3** doctrine before DB; A0 completes the half-state. **C4** backup adjacent, `BK=5` by value via `?immutable=1`. **C5** flip scoped `id IN (315,317,318,319,325) AND status='accepted'`. **C6** sentinels BY NAME — PRE, ACC, MAXID, BK, CHANGES, GLOBOK — SIX, counted (seat 5: two seats' folds each updated one site and neither swept this row — the count-in-prose class); `CHANGES=5`, `GLOBOK=5` with the one-value exclusion. **C7** capture inside the flip txn: `id <= 326 AND id NOT IN (<the five>)` → **321 rows** (contiguity measured). **C8** commits cd-first + pathspec + name-only + toplevel (Rule 85 both halves). **C9** serialized dispatch stated; non-dependent guards named at A0/QA.

## How to Run This Plan
**Bootstrap:**
```
Read the plan at knowledge/decisions/in-progress-executable-353.md (the daemon renames on claim). Execute Step 1 ONLY. After completing Step 1, STOP and wait for my confirmation.
```
⚠️ HALT ROUTING: Step 1 reads this plan, the live `/Users/marklehn/Developer/GitHub/DRAFTING_CYCLE.md`, the COMMITTED builder, the canonical DB. Step 2 reads this plan, the dev-log, the live doctrine, the DB (read-only), the merged captures, `/Users/marklehn/Developer/GitHub/RULE_20_SELF_CHECK_BLOCK.md`.

---

## Drafting Cycle

**Tier:** T2 — T-6 fires (doctrine + corpus). Clone lineage: the 346/348 committed-builder mechanism, all session-36 hardenings applied at authoring (from-birth lint, `--` dash guards, named sentinels, spelled invocations, freeze checklist, Rule-85 both halves, cd-first worktree finals).

**Walk 0 (context pin, §2.0):** sha `c678f288…` (live v2.4, porcelain clean); anchors count-1 with ONE sequential nuance stated (seat 4): E4's anchor exists only AFTER E1 runs — it measures 0 on the live file and is asserted inside the builder's sequential state, not against the pre-edit file; five's prior stamp `2026-08-12T00:29:31Z` measured Z-GLOB-MATCHING → the one-value exclusion is load-bearing; capture bound 326−5=321 (contiguity measured); dry-run executed: numstat `7 4`, probes earnable, History 14→15. Register: `governance/knowledge/research/walk-register-gate2-dc5-2026-08-11.md`.

**Direction verdict (after walk 1): PROCEED** — origin sound (348 lineage), mechanism sound (committed builder, dry-run stable), licence sound (packet + 350's routing, re-verified).

**Walks:** 5 (swept per culmination).
- Weak spots:          w1 1 folded — instruction, pre-existing ~~(the `per-LENS` probe predicted 2 with an INVENTED cross-reference; measured 1 — both probe sites corrected)~~ **struck (seats 1+4): the correction's explanation was false and the corrected probe still verified nothing — the true fix is the seat-1 operative-clause swap; struck here rather than quietly rewritten, per the lens-attestation rule.**
- Destruction:         w1 dry (all five clauses additive; neighbour bullets intact on the dry-run).
- Vulnerabilities:     w1 executed — the probe count run (1); battery: five still `accepted`; lint EXIT 0.
- Integration-record:  w1 dry (register conformant from birth, synced this culmination).
- ACID:                w1 dry (E1/E4 same-bullet interaction proven safe by the all-or-nothing dry-run + the corrected probe; flip machinery unchanged from the verified lineage).

**Walk-1 split: instruction 1 / record 0; origin 1 of 1 pre-existing.** Re-opens; walk 2 owed.

**Walk 2** (whole artifact; new surface = walk 1's fold):
- Weak spots:          w2 1 folded — instruction, pre-existing (A0 state 2 cited "the C-probe set", a name defined nowhere in this plan's ledger — renamed to the seven numbered probes).
- Destruction:         w2 dry (neighbour probes executed on the dry-run: all four heads intact at 1).
- Vulnerabilities:     w2 executed — battery: five `accepted`, ACC 8, sha unchanged; lint ONE WARN (the cold-panel line, earned — the missing-lens WARN cleared as walk 1's lines landed).
- Integration-record:  w2 1 folded — record, pre-existing (Conformance filled warm with the two-state lint history).
- ACID:                w2 dry.

**Walk-2 split: instruction 1 / record 1; origin 2 of 2 pre-existing.** Re-opens; walk 3 owed.

**Walk 3** (whole artifact; new surface = walk 2's folds):
- Weak spots:          w3 1 folded — instruction, pre-existing (⚠️ the QA tail probe was PRE-SATISFIED: `and every future cycle` measured 1 pre-edit — the v2.3 row ends with it — while the plan claimed 0-earnable; the 346-documented class, caught by execution; swapped to `FORWARD 57's gate half`, verified 0→1).
- Destruction:         w3 dry.
- Vulnerabilities:     w3 executed — the swapped probe verified both sides; battery stable (five accepted, sha unchanged); lint ONE earned WARN.
- Integration-record:  w3 dry (records synced same-culmination).
- ACID:                w3 dry.

**Walk-3 split: instruction 1 / record 0; origin 1 of 1 pre-existing.** Re-opens; walk 4 owed.

**Walk 4** (whole artifact; the SYSTEMATIC pin sweep — three walks caught probe-class defects piecemeal, so walk 4 ran every pin both-sides in one pass):
- Weak spots + Vulnerabilities: w4 executed — twelve pins + the History count, ALL CLEAN both sides; battery stable (five `accepted`, sha unchanged); lint EXIT 0 at the one earned WARN.
- Destruction:         w4 dry.
- Integration-record:  w4 dry (records synced).
- ACID:                w4 dry.

**Walk-4 split: instruction 0 / record 0 — LITERAL DRY. The walk phase meets the v2.4 bar on the dry branch.** ⚠️ T2: the cold panel is NOT waived — it convenes on this artifact before the Closing is written; the panel's findings re-open the walk on the same terms as any lens pass.

**Conformance (§5) — from birth:** v0 → EXIT 0 with the two earned WARNs (missing-lens, cold-panel); post-walk-1 → EXIT 0, ONE WARN — the missing-lens cleared EARNED as the per-lens lines landed; the cold-panel WARN stood honestly until the panel convened — ⚠️ **then at panel seat 2, measured by execution: EXIT 0, ZERO WARNs — the cold-panel WARN cleared EARNED when seat 1's panel line landed** (the §3/309 disappearance rule's explanation, recorded in the 348 form) — and the seat-2 A0 restoration then re-introduced exactly ONE warning: `(o1)` on the conditional `resume-sweep.txt`, the lineage's standing benign class, present BECAUSE the recovery path now exists. **Freeze step 2 matches this ONE-warning set.**

**Cold panel (T2):** sequential, author-verified and folded between seats.
- **Seat 1 (weak spots + evidence-attack): 4 findings — 1 MED / 3 LOW; instruction 1 / record 3; all verified and folded.** The MED survived FOUR walks including walk 1's own correction: **probe 1 never verified E1 at all** — its sole post-edit hit is the History row, E1's bullet reading the words in reverse order; the walk-1 narrative explaining the count was itself false. Swapped to E1's operative clause (`after each lens's fold inside a walk`, seat-verified earnable 0→1); the walk-1 register row corrected in place. LOWs: 317's per-item clause recorded as pre-covered in the History row; 315's one-way homing declared there too (both same-line builder edits, numstat invariant `7 4`); the false walk-1 mechanism narrative struck. Everything else verified clean at ~17 executed checks: all anchors, all pins both-sides, DB invariants, gate-neutrality with positive control, source-claim traces for all five proposals.
- **Seat 2 (destruction + clone-diff): 11 findings — 2 HIGH / 3 MED / 6 LOW; instruction 8 / record 3; clone-diff across 19 regions: 4 DROPPED, 2 MIS-ADAPTED/STALE, 0 re-added-after-deletion; all verified and folded.** The HIGHs: **A0 had collapsed 5→3 states** — the modified-uncommitted and unexplained-backup HALTs fell through to a version check the dangerous state PASSES (states restored, state-level catch-all, conditional resume-sweep deposit back in Scope); and **the environment facts were re-compressed to the very label 348's panel paid to restore** — the recurrence class live, with fact 1 load-bearing for this plan's own all-zeros QA row (restored verbatim). MEDs: the consumer-semantics row restored (`:31` quote + the five entries); the Conformance record's stale WARN claim corrected by execution TWICE (the cold-panel WARN cleared earned at seat 1; the A0 restoration then re-introduced the one benign conditional-path WARN); the Rule-93 blanket declaration added. LOWs: 6-field projections + category pin restored; MAXID=326; the fourth neighbour probe spelled; the 9/11 heading census rows; E4 gains its §2.8 deferral (builder, numstat stable `7 4`); the register's append-order convention declared.
- **Seat 5 (ACID + system capstone): 7 findings — 1 HIGH / 2 MED / 4 LOW; instruction 4 / record 3; 3 of 7 FOLD-INTRODUCED by seats 2–4 — the capstone catching the panel's own half-states.** The HIGH: seat 4's freeze-item-1 hardening mis-pinned the count in the 270 `-c`-vs-occurrence trap — REHEARSED and proven to false-fail a correct substitution (the same item's THIRD generation of fixes; the last-edit-least-reviewed class measured across generations). MEDs: C6's five-vs-six sentinel half-state (two seats each updated one site, neither swept the ledger); branch-5's unscoped porcelain false-HALTs every fresh dispatch against the live root's near-permanent benign dirt. LOWs: branch-2's porcelain + spelled git-show probes; F2's literal ambiguity respelled; two register gaps noted honestly. Category (b): NO PHANTOM FOLDS among ~45 — every claimed fold grep-verified in the artifact. Category (f): the shipped v2.5 carries NO panel-introduced contradictions. VERDICT: safe after the fixes (all applied) + this close.
- *(Panel meter, per §2.6's 286 bullet: seats 1–4 ≈ 107k + 184k + 87k + 160k = 538k subagent tokens, 30 findings pre-seat-5 — vs the 563k/45 baseline; seat 4.)*
- **Seat 3 (vulnerabilities, register-plain): 4 findings — 1 HIGH / 3 LOW; instruction 3 / record 1 (corrected at seat 4 — the freeze-phrasing fix is instruction; the register tags were right and the log was wrong); ~30 commands executed incl. the FULL flip sequence on scratch snapshots with two constructed failure states — all fired correctly (pre-flipped row caught at G1 AND G2's CHANGES; missing evidence dir aborted pre-UPDATE with the DB untouched).** The HIGH, execution-proven: **315's category is `instrumentation`, not the pinned `governance_rule`** — an INHERITED claim ("all five measured") that the seat's scratch flip proved false-HALTs post-commit on a correct run; per-id category table now at G3, QA 4(a), and freeze step 3. The sixth consecutive register-plain HIGH. LOWs: B's assert gains its spelled found-backup target (a canonical-target run passes vacuously); freeze item 1's self-colliding zero-expectation rephrased; the seat-1 register row's touch tags corrected. Also verified clean by execution: all fourteen pins both-sides, quoting/escaping including the apostrophe probes, consumer semantics pre/post, gate-neutrality with control, pytest 55/0, lint at the one-WARN pin byte-for-byte. Data point: `id_sequence` reads 352 — the 351 prediction consumed in-window AGAIN (the freeze re-token covers it).
- **Seat 4 (integration-record + clone-diff-record): 11 findings — 1 HIGH / 6 MED / 4 LOW; instruction 4 / record 7; ~40 commands, every plan-pinned number reproduced except those it filed; all folded.** The HIGH: **the record sections TRAILED after STEP 2 — the exact 313-placement violation 348's panel paid a HIGH to fix**, re-committed by this clone's compressed v0 (the block now sits above STEP 1; third instance of the compression-is-where-clones-bleed pattern this cycle). MEDs: the dropped Closing-form freeze assert restored (item 4); the seat-2 MAXID fold integrated INTO the G1 fence (the appended-sentence form contradicted "exactly"); two stale row-6 pointers swept post-renumbering; the seat-3 split corrected (3/1 — the register was right, the log wrong); the false "struck" claim made TRUE by actually striking at both sites; the register's schema deviations declared honestly. LOWs: E4's sequential-anchor nuance stated; the receipt named as BK/MAXID's observer (six sentinels); TASK F's id joins the freeze substitution with a spelled probe; the §2.6 panel meter added (seats 1–4 ≈ 538k / 30 findings vs the 563k/45 baseline).

**Walk 5 (the post-panel bar-meeting close — the panel's ~37 findings re-opened the walk per §2.6):** the systematic both-sides sweep re-run on the FINAL builder/draft state: nine pins + History 14→15 ALL CLEAN, numstat `7 4` stable, battery stable (five `accepted`), lint EXIT 0 at the one-WARN pin. **Instruction 0 / record 0 — LITERAL DRY.**

**Closing:** walk 5 dry — instruction 0 / record 0; last event = lens pass, run after the full five-seat panel (37 findings, 4 HIGH, ~684k tokens vs the 563k/45 baseline; every finding author-verified, folded, and re-verified by the capstone's no-phantom sweep). The compression lesson is the cycle's headline: every HIGH traced to v0 economy re-dropping a parent's paid-for hardening — the recurrence class measured four ways. Closing-record re-read run: seat splits and register rows reconcile (with the two declared gaps), the meter arithmetic checks, the walk tracker reads 5, Conformance matches the measured one-WARN state. Fold-and-deposit exactly once.

## STEP 1 — DEV (run the builder, commit, flip the five)

> **FIRST — visible chat message; do NOT rename this plan file.** The doctrine edit lands at the ABSOLUTE root path; a HALT after edits leaves the tree as-is, reported loudly.
> **A0 (first match wins):** (1) five `implemented` → verify commit-by-slug `gate2-dc5-2026-08-11` (NOT FOUND → HALT), deposits survived, RECOVERY receipt if the capture is lost + fresh G3 + row-4 DECLARED FALLBACK named; report complete. (2) docs committed (newest doctrine commit names the slug) + five still `accepted` → **assert the doctrine path's porcelain clean ON TOP of the commit (post-commit dirt → report LOUDLY, never sweep)**, DOC_SHA FROM THE COMMIT, reuse any `pre-dc5-*` backup via prefix-only `find` (BK assert mandatory either way), re-run the SEVEN probes on the committed content **via `git -C /Users/marklehn/Developer/GitHub show <commit>:DRAFTING_CYCLE.md | grep -cF …` (the spelled retrieval; seat 5)** (the numbered list below — an earlier revision said "the C-probe set", a name defined nowhere; walk 2), then B → G. (3) **docs modified-UNCOMMITTED** (porcelain dirty for the doctrine path) → **HALT**; sweep the seven probes against the live file, DEPOSIT the landed/not-landed table at `knowledge/qa/evidence/gate2-dc5-2026-08-11/resume-sweep.txt` (joins Scope on this path only), report — CEO directs. (4) **fresh-with-unexplained-backup** (a `pre-dc5-*` backup exists, no doctrine commit, five `accepted`) → **HALT**. (5) fresh — porcelain clean FOR THE DOCTRINE PATH AND THE E0 DENYLIST FILES ONLY (seat 5: the root carries near-permanent benign dirt — the daemon's submodule pointer, `scratchpad/` — an unscoped clean false-HALTs every fresh dispatch; E0's tolerance governs), version reads `2.4`, five `accepted|codify` stamped `2026-08-12T00:29:31Z`, no `pre-dc5-` backup → A1. Version cross-check every path (2.4, or 2.5-first-bullet-names-THIS-slug). ⚠️ **A state matching NO branch → HALT with the observed triple (porcelain + version/first-bullet + per-id read-back) — this catch-all is STATE-level, distinct from the version check** (seat 2: the collapsed A0 let an uncommitted-edit state fall through to improvisation ahead of the flip).
> **A1 — pin:** `shasum -a 256 /Users/marklehn/Developer/GitHub/DRAFTING_CYCLE.md` == `c678f288deba2725fa878969e10362f618de532786a2f75f1a1c4c391d52e54e`. Mismatch → HALT. Anchor re-proof is the builder's asserts.
> **BUILDER:** run exactly `python3 /Users/marklehn/Developer/GitHub/governance/knowledge/research/builder-gate2-dc5-2026-08-11.py /Users/marklehn/Developer/GitHub/DRAFTING_CYCLE.md /Users/marklehn/Developer/GitHub/DRAFTING_CYCLE.md` → exit 0 + `OK — 7 edits applied` naming all seven. **AssertionError → HALT, quote verbatim — the file is UNTOUCHED by construction; never patch, never retry modified.** Post-conditions, `grep -cF` (`--` before dash-leading literals) on the live file:
> 1. `after each lens's fold inside a walk` → **1** (E1's OPERATIVE clause, earnable 0→1 — ⚠️ two earlier probe forms both failed to verify E1 at all: walk 1's corrected `per-LENS within a walk` matches ONLY the History row, the bullet reading the words in reverse order; panel seat 1 caught what four walks certified)
> 2. `The instrument for a subtractive cut is a DIFF REVIEW` → 1
> 3. `run that diff BEFORE walk 1` → 1
> 4. `Price what a guard is FOR before folding it in` → 1
> 5. `An EDIT ANCHOR is not a probe` → 1
> 6. `**Version:** 2.5 (2026-08-11). Amended only through the Iteration Protocol` → 1; `2.4 (2026-08-11)` → 1
> 7. History bullets via `awk '/^## History/{f=1;next} f&&/^- \*\*/{n++} END{print n}'` → **15**
> **E0 denylist:** porcelain — `DRAFTING_CYCLE.md` modified; HALT iff any of `PLANNER_TEMPLATE.md`, `RULE_20_SELF_CHECK_BLOCK.md`, `READONLY_AUDIT_CONTRACT.md`, `SPECIALIST_TEMPLATE.md`, `INTERMEDIATE_DECISION_PHRASES.md` dirty; other root dirt reported.
> **DOC_SHA** pinned before commit. **TASK F:** `cd /Users/marklehn/Developer/GitHub && git add DRAFTING_CYCLE.md && git commit -m "[353] gate2(gate2-dc5-2026-08-11): the DC five — per-lens commits, diff-review, early clone-diff, pricing gate, edit-anchor — doctrine 2.4 -> 2.5" -- DRAFTING_CYCLE.md && git rev-parse --show-toplevel` (expect the governance root; wrong/missing → NOT COMMITTED). Numstat expect **`7	4`**; different → HALT. **F2:** the committed CONTENT's sha — `git -C /Users/marklehn/Developer/GitHub show HEAD:DRAFTING_CYCLE.md | shasum -a 256` — == DOC_SHA (seat 5: "commit sha" read literally compares a commit id to a file hash); name-only exactly the one file.
> **B — backup**, exactly: `sqlite3 -bail /Users/marklehn/Developer/GitHub/lessons-forge/lessons-forge.db ".timeout 5000" ".backup /Users/marklehn/Developer/GitHub/lessons-forge/pre-dc5-$(date -u +%Y%m%d_%H%M%S).db"`; locate via prefix-only `find`; assert against THE FOUND BACKUP's absolute path (never the canonical DB — a canonical-target run passes vacuously; seat 3): `sqlite3 -bail "file:<found-backup-abs>?immutable=1" ".timeout 5000" ...` with `SELECT 'BK='||COUNT(*) FROM lesson_proposals WHERE id IN (315,317,318,319,325) AND status='accepted'` → **BK=5**; else HALT.
> **G1 — rehearsal** (runner: `sqlite3 -bail <canonical-db> ".timeout 5000" ".read <abs>"`, exit-0 + empty-stderr per invocation, same-invocation scratch; file `knowledge/development/dc5-rehearsal.sql`, exactly):
> ```
> BEGIN IMMEDIATE;
> SELECT 'PRE='||COUNT(*) FROM lesson_proposals WHERE id IN (315,317,318,319,325) AND status='accepted' AND route='codify';
> SELECT 'ACC='||COUNT(*) FROM lesson_proposals WHERE status='accepted' AND route='codify';
> SELECT 'MAXID='||MAX(id) FROM lesson_proposals;
> ROLLBACK;
> ```
> Assert **PRE=5, ACC=8, MAXID=326** (fewer → HALT naming ids; ACC>8 → in-window routing → HALT; MAXID>326 reported-benign, the capture bound stays).
> **G2 — the flip** (`mkdir -p` the evidence dir FIRST; file `knowledge/development/dc5-flip.sql`, exactly; `.output` absolute from `pwd`):
> ```
> BEGIN IMMEDIATE;
> .output <tree-abs>/knowledge/qa/evidence/gate2-dc5-2026-08-11/outside-range-ids.txt
> SELECT id||'|'||category||'|'||status||'|'||COALESCE(route,'')||'|'||COALESCE(status_updated_at,'')||'|'||COALESCE(status_updated_by,'') FROM lesson_proposals WHERE id <= 326 AND id NOT IN (315,317,318,319,325) ORDER BY id;
> .output stdout
> UPDATE lesson_proposals SET status='implemented', status_updated_at=strftime('%Y-%m-%dT%H:%M:%SZ','now'), status_updated_by='ceo' WHERE id IN (315,317,318,319,325) AND status='accepted';
> SELECT 'CHANGES='||changes();
> SELECT 'GLOBOK='||COUNT(*) FROM lesson_proposals WHERE id IN (315,317,318,319,325) AND status_updated_at GLOB '20[0-9][0-9]-[0-9][0-9]-[0-9][0-9]T[0-9][0-9]:[0-9][0-9]:[0-9][0-9]Z' AND status_updated_at NOT IN ('2026-08-12T00:29:31Z');
> COMMIT;
> ```
> **Sentinels CHANGES=5, GLOBOK=5** (⚠️ the prior stamp is Z-form GLOB-MATCHING — the `NOT IN` carries the guard); either off → HALT with numbers. Capture **321 lines**, read post-commit — mismatch = record + HALT with the flip landed. The five-id set is IMMUTABLE.
> **G3 — read-back**, exactly: `sqlite3 -bail -readonly /Users/marklehn/Developer/GitHub/lessons-forge/lessons-forge.db ".timeout 5000" "SELECT id||'|'||category||'|'||status||'|'||COALESCE(route,'')||'|'||status_updated_by||'|'||status_updated_at FROM lesson_proposals WHERE id IN (315,317,318,319,325) ORDER BY id;"` → RAW to `flip-readback.txt`: per-id: **315=`instrumentation`**, 317/318/319/325=`governance_rule`, each `|implemented|codify|ceo|<Z ≠ 2026-08-12T00:29:31Z>` — ⚠️ an earlier revision pinned ALL FIVE `governance_rule`, an INHERITED claim; seat 3 queried the truth and ran the full flip on scratch proving the false pin HALTs post-commit on a correct run (the sixth consecutive register-plain HIGH). `category` rides both projections so QA row 4(a) asserts preservation per id against THIS table.
> **Receipt** with SIX named sentinels — PRE, ACC, MAXID, BK, CHANGES, GLOBOK (the receipt IS BK's observer: no QA row reads the backup; seat 4) — + DOC_SHA + commit + numstat · `### Ledger Updates` · `#### Prompt Feedback` · `#### Forward Register`: `NONE`. **FINAL ACTION** — commit deposits: `cd <your-tree-abs>` first token, pathspec on the COMMIT, name-only assert, bare `git rev-parse --show-toplevel` last.
>
> **Scope:**
> - `knowledge/development/dev-log-gate2-dc5-step-1-2026-08-11.md`
> - `knowledge/development/dc5-rehearsal.sql`
> - `knowledge/development/dc5-flip.sql`
> - `knowledge/qa/evidence/gate2-dc5-2026-08-11/outside-range-ids.txt`
> - `knowledge/qa/evidence/gate2-dc5-2026-08-11/flip-readback.txt`
>
> **Deposits:**
> - `lessons-forge/knowledge/development/dev-log-gate2-dc5-step-1-2026-08-11.md`
> - `lessons-forge/knowledge/development/dc5-rehearsal.sql`
> - `lessons-forge/knowledge/development/dc5-flip.sql`
> - `lessons-forge/knowledge/qa/evidence/gate2-dc5-2026-08-11/outside-range-ids.txt`
> - `lessons-forge/knowledge/qa/evidence/gate2-dc5-2026-08-11/flip-readback.txt`

## STEP 2 — QA

> **FIRST — do NOT rename this plan file. Deliverable Verification (Rule 8/17)**, ✅/❌, any ❌ → HALT. **Rule 20 self-check** (canonical block from `/Users/marklehn/Developer/GitHub/RULE_20_SELF_CHECK_BLOCK.md`; `plan_slug`: `gate2-dc5-2026-08-11`; `qa_report_path`: `<tree>/knowledge/qa/gate2-dc5-qa-2026-08-11.md`; `evidence_dir`: `<tree>/knowledge/qa/evidence/gate2-dc5-2026-08-11/`; `required_evidence_files`: `[doc-integrity.txt, db-invariants.txt, gate-neutrality.txt, pytest_targeted.txt]`, all four BEFORE the block; literal stdout: the banner `Rule 20 — QA Self-Check Results` and the `PASSED — SELF-CHECK PASSED` line, byte-exact). `## Evidence and Narrative` after the table. ONE read-only DB form; RAW evidence.
> **1. DOC INTEGRITY** — commit discovered by slug; three-way sha (commit == live == DOC_SHA; `e3b0c442…` = show-failed); porcelain empty; name-only exact. → `doc-integrity.txt`
> **2. THE CLAUSES LANDED** — the seven Step-1 post-condition probes re-run independently (same `--` discipline), incl. `after each lens's fold inside a walk` → 1 (E1's operative clause — the seat-1 probe swap) and the History awk → 15; **tail probe** `grep -cF "FORWARD 57's gate half"` on the new row's tail → 1 (earnable — measured 0 pre-edit / 1 post; ⚠️ an earlier revision pinned `and every future cycle` claiming 0 pre-edit — MEASURED 1: the v2.3 row already ends with it, the pre-satisfied-tail class 346's own QA row documented; caught by walk 3 running the grep). → `doc-integrity.txt`
> **3. COHERENCE** — `2.4 (2026-08-11)` → 1 (the prior row, intact); the four neighbour-bullet heads intact (`- **Per-phase commits`, `- **Sequential-fold rule`, `**A count is not a value guard.**`, `grep -cF 'When cloning a plan, diff the machinery against the'` → 1) each with `--` guards where dash-leading. → `doc-integrity.txt`
> **4. FLIP + BLAST RADIUS** — (a) five `implemented|codify|ceo`, Z-stamped ≠ the pinned prior, category preserved PER ID against the seat-3 table (315=`instrumentation`, the rest `governance_rule`); (b) `accepted|codify` → **3** (the PT three, ids 316/324/326 named); (c) re-run the EXACT capture SELECT (COPY from G2), diff vs the deposited 321-line file; partition (impossible-id → HALT; deleted → HALT; else concurrent, named). DECLARED FALLBACK (capture lost): degraded co-stamp sweep, labelled. → `db-invariants.txt`
> **5. DOCTRINE PARSES (seat 2):** `grep -cE '^## '` → **9 unchanged** and `grep -cE '^### '` → **11 unchanged** (both sides measured on the dry-run — the edits add bullets, zero headings). → `doc-integrity.txt`
> **5b. TESTS** — single-module premise re-derived; pytest vs 55/0, delta reported. → `pytest_targeted.txt`
> **6. CONSUMER SEMANTICS (seat 2 — both parents carry it):** quote `/Users/marklehn/Developer/GitHub/lessons-forge/src/lessons_forge.py:31` verbatim (`implemented` IS terminal, `accepted` is NOT); run `get_unclassified_entries` (read-only) → the five source entries (307, 309, 310, 311, 317) ABSENT — dispositioned before, dispositioned after (a preservation check; the flip's terminal-boundary crossing is the intended meaning of codified). → `db-invariants.txt`
> **7. GATE-NEUTRALITY** — `per-LENS`, `DIFF REVIEW`, `pricing gate`, `EDIT ANCHOR` → 0 in both `/Users/marklehn/Developer/GitHub/bellows/scripts/plan_lint.py` AND `/Users/marklehn/Developer/GitHub/bellows/gates.py` (gates.py at the bellows ROOT); positive control `Drafting Cycle` in plan_lint.py → 11. → `gate-neutrality.txt`
> `## Evidence and Narrative` · Receipt · `### Ledger Updates` · `#### Forward Register`: `NONE`. **FINAL ACTION** — commit deposits, cd-first + pathspec + name-only + bare toplevel.
>
> **Scope:**
> - `knowledge/qa/gate2-dc5-qa-2026-08-11.md`
> - `knowledge/qa/evidence/gate2-dc5-2026-08-11/doc-integrity.txt`
> - `knowledge/qa/evidence/gate2-dc5-2026-08-11/db-invariants.txt`
> - `knowledge/qa/evidence/gate2-dc5-2026-08-11/gate-neutrality.txt`
> - `knowledge/qa/evidence/gate2-dc5-2026-08-11/pytest_targeted.txt`
>
> **Deposits:**
> - `lessons-forge/knowledge/qa/gate2-dc5-qa-2026-08-11.md`
> - `lessons-forge/knowledge/qa/evidence/gate2-dc5-2026-08-11/doc-integrity.txt`
> - `lessons-forge/knowledge/qa/evidence/gate2-dc5-2026-08-11/db-invariants.txt`
> - `lessons-forge/knowledge/qa/evidence/gate2-dc5-2026-08-11/gate-neutrality.txt`
> - `lessons-forge/knowledge/qa/evidence/gate2-dc5-2026-08-11/pytest_targeted.txt`

