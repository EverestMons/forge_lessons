# Lessons Forge — Gate 1 Route Disposition 2026-07-29 (route the 8 session-12 proposals)
**Date:** 2026-07-29 | **Tier:** Small | **Dispatch Mode:** bellows | **Test Scope:** targeted | **Execution:** Step 1 (DEV — route + verify) → Step 2 (QA) | **qa_steps:** 2 | **pause_for_verdict:** always
**cycle_tier:** T2

## CEO Context

Gate 1 for the 8 proposals cycle 283 produced. **CEO disposition, taken 2026-07-29: all 8 → `codify`.** Gate 2 (codification into doctrine) is a separate plan with a CEO decision between.

**⚠️ THIS GATE WRITES `route` ONLY — NO status change.** All 8 are `codify` → Gate-2-bound → all KEEP `status='proposed'`. **There is no hand-written SQL in this plan:** every write is `set_proposal_route(conn, id, 'codify')` (`src/lessons_forge.py:256` — parameterised `UPDATE lesson_proposals SET route = ? WHERE id = ?`, validates against `_VALID_ROUTES = frozenset(('codify','backlog','reference'))`, **no internal commit**). Because no status moves, the **status distribution is byte-identical before/after** — an invariant to CHECK. The only delta is `route` NULL→`codify` on exactly eight rows.

**Clone lineage.** Direct clone of **282** (the prior Gate 1, which routed 2). Per entry 183 — clone against the NEWEST same-class plan — the machinery is additionally diffed forward against **283** (the immediately preceding plan, whose cold walk found and fixed defects 282 still carries). Hardenings inherited from 283 that 282 lacks are marked **[from 283]** at their site.

### The disposition table

| proposal | entry | substance | `target_artifact` | category | route |
|---|---|---|---|---|---|
| 193 | 185 | sweep every site stating the same rule/number/path; weight toward the mutating step | `PLANNER_TEMPLATE.md` | `governance_rule` | **codify** |
| 194 | 186 | rotate the TARGET, not just the reviewer | `DRAFTING_CYCLE.md` | `governance_rule` | **codify** |
| 195 | 187 | verification granularity must match change granularity; assert what was KEPT | `DRAFTING_CYCLE.md` | `governance_rule` | **codify** |
| 196 | 188 | read the cited rule; clones reproduce ABSENCES | `PLANNER_TEMPLATE.md` | `governance_rule` | **codify** |
| 197 | 189 | §3's "compact" is load-bearing | `DRAFTING_CYCLE.md` | `governance_rule` | **codify** |
| 198 | 190 | four `plan_lint` §4 defects | `DRAFTING_CYCLE.md` | `instrumentation` | **codify** |
| 199 | 191 | what the Rule 20 block does and does not verify | `RULE_20_SELF_CHECK_BLOCK.md` | `instrumentation` | **codify** |
| 200 | 192 | write a lens result only after the lens ran | `DRAFTING_CYCLE.md` | `governance_rule` | **codify** |

**Dedup performed at Gate 1, as cycle 283 deferred it.** None of the 8 is already codified in its target artifact. Three are **partial** — an existing rule stops short and the proposal amends it: **193** (Checklist #26 names the sweep but not the close-the-fold condition or the mutating-step weighting), **197** (§3 says "one line per lens is the floor" but not the mechanical consequences), **200** (§4 *names* the residual-trust risk but supplies no rule against it). Five are wholly new. ⚠️ **Recorded for Gate 2, not acted on here: 195's parent lesson — the 2026-07-25 "verify the subsumption against live data" entry — appears never to have been codified into `DRAFTING_CYCLE.md`** (zero hits for `subsum`/`another check covers`/`enumerated`). Gate 2 can land the parent principle and this refinement together.

**⚠️ Rule 46 does NOT fire on 198/199.** A Gate-1 reader seeing "hard code couplings" reaches for Rule 46 (`PLANNER_TEMPLATE.md:1012`), which directs *Planner-side workarounds for a daemon bug* to `FORWARD.md` rather than codification. ⚠️ **Rule 46 does not reach them ON ITS OWN STATED TERMS, and that is the instrument to use — not a judgment about substance.** The rule is scoped to *medium-confidence* proposals flagged as a workaround, and carves out explicitly that *"proposals with high confidence… may be accepted on merit."* **Measured live: `confidence='high'` on all eight**, so the carve-out applies mechanically. (The substantive reading agrees — entry 190 prescribes concrete `plan_lint.py` edits and entry 191 documents `gates.py` behaviour, so these are gate FIXES not workarounds — but the confidence field settles it without an argument, and proposal 196, which this plan routes, is about reading the cited rule rather than reasoning past it.) **`confidence` is therefore added to Task A0-pre's and QA row 1's SELECT so the premise is re-verified at run time, not just at authoring.** ⚠️ **Scope the §6 citation precisely: it applies to 198 ONLY.** `DRAFTING_CYCLE.md` §6's "Coordinate doctrine and gate" bullet is scoped to a codification *touching §1 or §4 of that file* — 198 is a §4 defect, squarely in scope. **199 targets `RULE_20_SELF_CHECK_BLOCK.md` and `gates.py`, neither of which §6 governs**, so for 199 the doc+gate pairing is an ANALOGY with no codified rule behind it yet. Both are Gate 2's problem, not this plan's — but Gate 2 should inherit the distinction rather than a settled-looking citation.

**⚠️⚠️ FOUR CONDITIONS DIFFER FROM 282 — cloned unchanged, each would false-HALT or false-PASS.**

1. **Eight targets, not two** — ids **193–200**, entry_ids **185–192**, a contiguous 1:1 mapping (`193→185 … 200→192`).
2. **Categories are MIXED.** 282 asserted `category='governance_rule'` on every target. **Here 198 and 199 are `instrumentation`** (the two tooling-defect entries). A uniform assertion false-HALTs a correct corpus.
3. **THREE target artifacts, not two** — `PLANNER_TEMPLATE.md` (193, 196), `DRAFTING_CYCLE.md` (194, 195, 197, 198, 200), `RULE_20_SELF_CHECK_BLOCK.md` (199).
4. **`proposed` is 10 at baseline, not 2** — this cycle's 8 plus the **still-parked pair 191/192**, which carry `route='codify'` from 282 and remain Gate-2-bound. **This plan must leave 191/192 untouched**, exactly as 283 did.

**⚠️⚠️ A0 items (2) and (4) ARE BOTH 62 — THE 282 COLLISION, LIVE AGAIN.** They are DIFFERENT measurements that coincide before the write: (2) is the **total** route-NOT-NULL count; (4) is the **outside-range** count (`id NOT BETWEEN 193 AND 200`). They are equal now only because the 8 targets are route-NULL and therefore excluded from both. **After the write they diverge: (2) becomes 70, (4) stays 62.** Capture and report them **separately**; comparing an after-value to the wrong before-item passes for the wrong reason on a clean run. 282 found this at 60/60 and it recurs unchanged at 62/62.

**⚠️ Authoring baseline — measured 2026-07-29 against the live DB, to be CONFIRMED not assumed** (Checklist #29 — report actuals; a mismatch is a finding, not a number to adjust to): status distribution `implemented 137, superseded 28, rejected 15, reference 7, proposed 10, stale 3` (total **200**); total route-NOT-NULL **62**; outside-range route-NOT-NULL **62**; `get_unclassified_entries(conn)` `[]`; all 8 targets `route IS NULL`.

**⚠️ Test Scope: `targeted` — justified here because Rule 21 (`PLANNER_TEMPLATE.md:604`+) REQUIRES a justification for this scope.** This plan writes **no code**: it sets `route` on eight rows through an existing helper and deposits markdown, modifying zero source files, so the blast radius is bounded and there is no cross-bucket regression surface. ⚠️ **And for this repo targeted ≡ full — `lessons-forge` has exactly ONE test module (`src/test_lessons_forge.py`, 55 tests), so `pytest src/` IS both sets.** Declaring `full-suite` would promise what Rule 21 defines as *"the targeted tests AND the full suite… Evidence files: `pytest_targeted.txt` AND `pytest_full.txt`… the self-check requires both to be present"* — two files that would be byte-identical here. ⚠️ **Recorded because the lineage drifted on exactly this field: 281 declared `both` and 283 declared `full-suite`, neither implementing the extra run either mandate requires; 282 declared `targeted` and was right.** This plan follows 282.
>
⚠️⚠️ **ONE CONSEQUENCE OF THAT CHOICE, DECLARED so it is not read as a violation.** Rule 21's `targeted` clause also says the QA step *"must NOT run the full suite unless the targeted run reveals unexpected failures."* **Row 6 runs `pytest src/ -v`, which for this repo IS the entire module — so the targeted set and the full suite are the SAME command and the SAME 55 tests, and the prohibition is not satisfiable as a distinction here.** Rule 21's taxonomy presumes the two are separable; with one test module they are not. Running the module is therefore both "the targeted tests" (Rule 21's mandate) and, unavoidably, the full suite (its prohibition). **Declared rather than left implicit, because the alternative readings are worse: skipping the run would leave a DB-mutating plan with no regression evidence, and declaring `full-suite` would promise two byte-identical evidence files.**

**Scope discipline:** routing only. **Do NOT edit `DRAFTING_CYCLE.md`, `PLANNER_TEMPLATE.md`, `RULE_20_SELF_CHECK_BLOCK.md`, or `bellows/scripts/plan_lint.py`** — codification is Gate 2. **Do NOT touch proposals 191 or 192.** **Do NOT change any `status`.** **Do NOT touch `src/`** — this plan writes no code. Do NOT append to `LESSONS.md` while this plan is deposited-but-un-run.

**⚠️ Concurrency — dispatch with no other lessons-forge plan in flight.** This is an `R(before) → W → R(after)` schedule over a shared store; the Step-1 isolation pre-flight is the guard and Task C is the detection.

**No diagnostic precedes this plan, deliberately** (recurring-gate practice): every unknown was measured inline against live data at authoring.

**Deposit-once discipline:** deposited exactly once. **Authoring self-check:** `plan_lint.py` run at authoring — expect exit 0. ⚠️ **Per proposal 198, a clean `plan_lint` exit is NOT evidence the §4 block ran** (three of its sub-checks cannot fail; the block is warn-only, and on plan 283 its closing WARN fired, cleared, and fired again on prose wording alone while the disposition never changed). **Read the Cycle Log's Closing line, never the WARN.**

## How to Run This Plan

Bellows dispatches this plan automatically when deposited; no manual bootstrap required. **[from 283 — Rule 35 (`PLANNER_TEMPLATE.md:892`) requires this section to be omitted or replaced with exactly this note for `dispatch_mode: bellows`; 282 and its parents all carried a full manual bootstrap block instead, which is inert prose the daemon ignores.]**

---
---

## STEP 1 — DEV (route the 8, then verify)

---

> **FIRST — post a short visible chat message (1-2 sentences) confirming you are starting this plan and your immediate next action.** Do NOT rename the plan file.
>
> You are the Forge Developer (`/Users/marklehn/Developer/GitHub/forge/agents/FORGE_DEVELOPER.md` — skip with a note if absent). **Every canonical-DB access uses the ABSOLUTE path** `/Users/marklehn/Developer/GitHub/lessons-forge/lessons-forge.db`. **`forge/forge.db` is a REAL but DIFFERENT database — never open it.** Run commands from **your own working tree**; the ONLY exception is canonical-DB access by the absolute path.
>
> ⚠️ **`?mode=ro` NON-HALT FALLBACK — stated HERE because task one uses it.** A00's `.backup` reads through a `?mode=ro` URI, and the DB is WAL-mode: if the `-shm`/`-wal` sidecars are absent, `sqlite3` errors `Error: unable to open database file` on a perfectly healthy DB. **On that specific error: `echo "$BK"` FIRST, then re-run with that SAME LITERAL path, without the `?mode=ro` URI — do NOT recompute the timestamp.** ⚠️⚠️ **Verified by execution: the failing `.backup` leaves a 0-BYTE FILE at `$BK` on disk.** Shell state does not persist between your commands, so a re-run that recomputes `$(date -u …)` produces a SECOND filename — leaving an empty corpse and a real backup both matching the glob, with the empty one earlier. **Then `rm` the 0-byte file and say you did.** Note the fallback in the Receipt, and continue — only a failure of the fallback is a real failure. ⚠️ Without this at A00, the error false-HALTs the plan at its FIRST command, before any write, with the remedy described in a later step that has not run.
>
> **Open canonical read-WRITE** for the writes (`sqlite3.connect(<abs path>)`; do NOT reuse a `?mode=ro` handle). The eight `set_proposal_route` calls are ONE transaction: a **SINGLE `conn.commit()` after all eight** (the helper does not commit internally).
>
> **⚠️ EXECUTION ORDER — run in exactly this sequence.**
> 1. **A00** — restore point, then verify it is real.
> 2. **A0-iso** — isolation pre-flight.
> 3. **A0-pre** — precondition: targets match the disposition table.
> 4. **A0-snap** — capture the four before-snapshots.
> 5. **A** — the eight writes, then one commit.
> 6. **B**, **C** — read-only post-verification. Run ALL of them before halting on any one.
> 7. Deposit and commit.
>
> **Scope:**
> - `knowledge/development/gate-1-route-session-12-captures-2026-07-29.md`
>
> **Task A00 — TAKE A RESTORE POINT BEFORE ANY WRITE, THEN VERIFY IT IS REAL.** Use `.backup`, NOT `cp` (live WAL). Write to the MAIN tree by ABSOLUTE path (a worktree-local backup dies at teardown). Build the path in a shell variable FIRST — this exact form is verified-working:
> ```
> D=/Users/marklehn/Developer/GitHub/lessons-forge/knowledge/decisions
> F=$(ls "$D" 2>/dev/null | grep -E '^(in-progress|verdict-pending|halted|parked)-executable-[0-9]+\.md$')
> N=$(printf '%s' "$F" | grep -c .)
> [ "$N" = "1" ] || { echo "HALT: expected exactly 1 own plan file (in-progress/verdict-pending/halted/parked), found $N"; exit 1; }
> PLAN_ID=$(printf '%s' "$F" | sed -E 's/^(in-progress|verdict-pending|halted|parked)-executable-([0-9]+)\.md$/\2/')
> case "$PLAN_ID" in ''|*[!0-9]*) echo "HALT: derived plan id '$PLAN_ID' is not numeric"; exit 1;; esac
> mkdir -p /Users/marklehn/Developer/GitHub/lessons-forge/data/backups
> BK="/Users/marklehn/Developer/GitHub/lessons-forge/data/backups/lessons-forge-pre-gate1-${PLAN_ID}-$(date -u +%Y%m%dT%H%M%SZ).db"
> echo "PLAN_ID=$PLAN_ID"; echo "BK=$BK"
> sqlite3 "file:/Users/marklehn/Developer/GitHub/lessons-forge/lessons-forge.db?mode=ro" ".backup '$BK'"
> ```
> ⚠️ Do NOT inline `$(date …)` between single-quoted parts of the `.backup` argument — sqlite3 MISPARSES that and writes NO backup. ⚠️ **[from 283] The filename must carry THIS plan's id, because `data/backups/` already holds six-plus files matching the generic `pre-*` prefixes and a non-unique glob resolves to a stale snapshot from another cycle.** ⚠️ **The id is DERIVED from your own in-progress filename, not hardcoded** — the Planner cannot know it at authoring time (the daemon mints it on claim), and a hardcoded guess that misses would put the restore point where no later reader globs for it. ⚠️⚠️ **The derivation ASSERTS EXACTLY ONE MATCH and HALTs otherwise — verified necessary by execution at authoring: with two matching files present, a bare `basename $(ls …)` yields the multi-line garbage `284.md\n285.md\n.md` and the backup path is nonsense.** ⚠️ **It also matches `verdict-pending-` as well as `in-progress-`, and it uses `grep -E` rather than a shell glob — BOTH verified necessary by execution.** A glob-only form accepting just `in-progress-` false-HALTs a re-dispatch that finds the plan in `verdict-pending-` state; and **the runtime shell is zsh, where an unmatched glob in a multi-glob `ls` errors the WHOLE command** (`no matches found`), so the two-glob form returns empty and false-HALTs the ordinary single-file case. All four states were executed at authoring: in-progress → id derived; verdict-pending → id derived; two files → HALT; none → HALT. This assertion cannot be delegated to the isolation pre-flight (Task A0-iso) because **A00 runs FIRST** — by design, since the restore point must precede everything. So A00 carries its own single-match guard. **`PLAN_ID` and the resolved `$BK` are both printed before the backup runs.** ⚠️ **Why the id-in-filename is worth these five lines, stated so a future clone inherits the reason and not just the machinery.** **Nothing in this plan ever reads the backup back** — there is no resume ladder that globs for it, and the **Output Receipt (item 6) records its absolute path**, which is the primary and normal retrieval route. The filename matters in exactly one narrow case: **the step dies after the backup and before the deposit, so the receipt does not exist**, and a human then has to pick the right file out of the six-plus already in `data/backups/`. That case is real (it is what plan 283 hit) but it is the *only* one — so this is a cheap hedge on a narrow window, not a load-bearing mechanism. **Do not grow it further.** **Then VERIFY:** `PRAGMA integrity_check` must return `ok`, AND the backup's `COUNT(*)` for `lesson_entries` / `lesson_proposals` must equal the LIVE DB's counts at backup time (expected 192 / 200 — confirm against live, not against these literals). Any failure → HALT before any write. State the absolute path in the deposit.
>
> **Task A0-iso — ISOLATION pre-flight, from the tree where the state lives.** Assert no other writer. **Read by ABSOLUTE MAIN-TREE path — a worktree-relative `ls` passes VACUOUSLY.** Run `ls /Users/marklehn/Developer/GitHub/lessons-forge/knowledge/decisions/` and assert the POSITIVE signal: **your own plan file MUST appear** (as `in-progress-*`, `verdict-pending-*`, `halted-*` **or** `parked-*` — ⚠️ **all FOUR, verified as bellows' live prefix tuple `("in-progress-", "verdict-pending-", "halted-", "parked-")`; a two-prefix check false-HALTs every resume of a halted or rate-limit-parked step, and reports the flatly wrong cause "you are reading the wrong tree"**) AND no OTHER `in-progress-*`/`verdict-pending-*` lessons plan. If your file is absent you are reading the wrong tree → HALT (fail-closed: write nothing). Then read the **route-NOT-NULL count** and the **status distribution** twice a moment apart; confirm both unchanged. ⚠️ **Do NOT use `get_unclassified_entries()` as the quiescence signal — it filters on `status`, never `route`, so a route conflict is invisible to it.** Any concurrent writer possible → HALT.
>
> **Task A0-pre — PRECONDITION: verify the targets match the disposition table BEFORE writing.** `SELECT id, entry_id, status, category, confidence, route, target_layer, target_artifact FROM lesson_proposals WHERE id BETWEEN 193 AND 200 ORDER BY id` and assert, **per row**:
> - exactly **8 rows**;
> - the id→entry_id mapping is exactly `193→185, 194→186, 195→187, 196→188, 197→189, 198→190, 199→191, 200→192` — ⚠️ **a contiguous mapping is easy to eyeball as correct; check each pair, because an off-by-one shifts every disposition by one row**;
> - each `status='proposed'`;
> - each `route` is **NULL or already `codify`** (idempotent re-run tolerated);
> - `target_layer='governance'` on all 8;
> - **`category` and `target_artifact` match the disposition table PER ROW** — ⚠️ **do NOT assert a uniform category:** 198 and 199 are `instrumentation`, the other six `governance_rule`. A uniform assertion cloned from 282 false-HALTs this correct corpus.
>
> **HALT on genuine drift** (missing id, wrong entry_id, a status other than `proposed`, a route other than NULL/`codify`, or a category/target_artifact that disagrees with the table). ⚠️ **A disagreement here means the disposition table is describing a corpus that no longer exists — the CEO's routing decision was made against the table, so writing anyway would apply a decision to rows it was not made about.**
>
> **Task A0-snap — CAPTURE THE FOUR BEFORE-SNAPSHOTS, pre-write.** Record and KEEP, each on its OWN labelled line:
> 1. the full status distribution
> 2. the **TOTAL** route-NOT-NULL count
> 3. `get_unclassified_entries(conn)`
> 4. the **OUTSIDE-RANGE** route-NOT-NULL count — `SELECT COUNT(*) FROM lesson_proposals WHERE route IS NOT NULL AND id NOT BETWEEN 193 AND 200`
> 4b. **THE PARKED PAIR'S ROW** — `SELECT id, status, route, target_artifact FROM lesson_proposals WHERE id IN (191,192)`, as RAW output. ⚠️⚠️ **This is the ONE invariant in the plan that had no captured before-value, and it protects the pair the plan exists not to harm (ledger CL1).** Without it, Task C4's "unchanged" and QA row 5 compare against this plan's *authoring-time literal* rather than a measured state — so **a change occurring DURING Step 1 is indistinguishable from one that predated dispatch**, and CL1 records a survival constraint the plan cannot actually measure. Numbered `4b` deliberately, mirroring the existing `4b`/`5b` convention, so adding it does not force a renumber sweep of the whole Receipt.
>
> ⚠️⚠️ **(2) AND (4) ARE DIFFERENT MEASUREMENTS THAT ARE BOTH 62 BEFORE THE WRITE — capture and report them SEPARATELY.** They coincide only because the 8 targets are route-NULL and excluded from both. **After the write they diverge: (2) → 70, (4) stays 62.** Crossing them passes for the wrong reason on a clean run. **Authoring-measured expectations, to be confirmed not assumed:** status distribution `implemented 137, superseded 28, rejected 15, reference 7, proposed 10, stale 3` (total 200); total route-NOT-NULL **62**; `get_unclassified` `[]`; outside-range **62**. Report ACTUALS. **Do NOT re-read "before" after Task A's write.** ⚠️⚠️ **AND IF THIS IS A RESUME, SAY SO — these values are then post-partial-write and the "(pre-write)" label would be false.** A0-pre explicitly tolerates *k* targets already `codify`; in that case item (2) reads **62+k** and item (4) still 62. **Label the block `resume — snapshot taken post-partial-write`, state k, and do NOT treat the 62→62+k delta as premise drift** — Checklist #29's "a mismatch is a finding, not a number to adjust to" applies to an UNEXPLAINED mismatch, and this one is explained by A0-pre's own observation. ⚠️ No downstream check breaks (B3/row 3 are same-instant; row 4 anchors to the resume-invariant item (4); the rise over item (2) is 0 ≤ 8) — the risk is a false halt on a correct corpus, or a value adjusted to match a literal.
>
> **Task A — record the eight routes** with `set_proposal_route(conn, proposal_id, 'codify')` from `src.lessons_forge` (NOT hand-written SQL), for **193, 194, 195, 196, 197, 198, 199, 200**. **No status is written — leave all eight `status='proposed'`.** **Then a SINGLE `conn.commit()`.**
>
> **⚠️ TASKS B AND C ARE READ-ONLY AND INDEPENDENT — RUN ALL OF THEM BEFORE HALTING ON ANY ONE.** No check depends on another having passed, and none can alter the DB further. **The write has already happened**, so an early halt does not protect the corpus — it only decides how much the CEO knows about a corpus that is already changed. Halting at B1 hands over "a route looks wrong" while leaving unanswered whether a status moved (B2), whether an out-of-range route changed (C2), and whether classification staled (C3) — the three questions that decide whether this is a one-row anomaly or a corrupted corpus, and therefore what the recovery is. Run every check, record every measured value, THEN halt and report all findings together.
>
> ⚠️ **AND DO NOT ATTEMPT TO REPAIR ANYTHING YOU FIND — report it, do not fix it.** This is the one step holding an open read-WRITE handle, so unlike QA you *can* act on what you discover, and continuing past a detection is exactly when that becomes tempting. Do not re-run `set_proposal_route`, do not hand-write corrective SQL, do not restore from the backup, do not "tidy" a value so the next check passes. A discrepancy at B or C means a PREMISE of this plan is wrong, and a plan operating on a wrong premise must stop and be adjudicated, not self-correct. **The restore point exists for the CEO's decision, not for yours.**
>
> ⚠️⚠️ **IF YOU HALT AT ANY POINT AFTER TASK A's `conn.commit()`, YOU MUST STILL DEPOSIT AND COMMIT BEFORE STOPPING.** This clause exists in the parent plan and was dropped in cloning; it is restored here because **this is the mutating step** and "report and halt" does not otherwise say *write a file*. Once the commit has landed the corpus is changed, and **the four before-snapshots exist only in your context** — the pre-write state cannot be re-measured, and QA rows 2(b) and 4 mark themselves `❌ (unverifiable)` without them. So before you stop: write the dev-log with before-items (1)–(4b), the backup path and its verification result, whatever read-backs completed, and an explicit statement of which task halted and why; then **`git commit` by explicit pathspec**. ⚠️ **Mark any item you could not reach as `NOT CAPTURED — halted at <task>`** rather than omitting it — ⚠️ **and item (5) is the EXCEPTION: mark it PER SUB-CHECK, not wholesale.** Item (5) spans SIX sub-checks here (B1/B2/B3 + C2/C3/C4), so one blanket marker would either discard read-backs you actually produced or claim ones you did not. Write a line per sub-check, each either its raw output or its own `NOT CAPTURED — halted at <task>` — an omitted item is indistinguishable from a forgotten one, and `Status: Partial — HALTED at <task>` is a legitimate receipt value that must actually be reachable. **A halt with a committed record is a clean outcome; a halt with nothing on disk leaves a mutated production corpus with no audit trail.**
>
> **Task B — verify the ABSOLUTE post-state.**
> ⚠️ **B1 IS THE ONLY THING THAT OBSERVES THE WRITE'S EFFECT — it is not redundant with A0-pre.** Verified in code: `set_proposal_route` executes a bare `UPDATE lesson_proposals SET route = ? WHERE id = ?` and **never checks `rowcount`**, so a call naming an id that does not exist is a **SILENT NO-OP** — no exception, no signal, and the helper returns normally. (It validates the *route value* and raises on a bad one, but nothing validates the *id*.) A0-pre proves the 8 rows existed **before** the write; only B1's read-back proves the write **landed**. Neither substitutes for the other.
>
> - **B1** — read back all 8 rows: `SELECT id, entry_id, status, route, category, target_artifact FROM lesson_proposals WHERE id BETWEEN 193 AND 200 ORDER BY id`. Each `route='codify'`, each `status='proposed'`, category and target_artifact unchanged from A0-pre. **Absolute assertion — no before-anchor.**
> - **B2** — the full status distribution is **byte-identical to A0-snap item (1)**. No status moved; `proposed` is still 10.
> - **B3** — the TOTAL route-NOT-NULL count. ⚠️ **Assert a SAME-INSTANT IDENTITY, not a delta and not a cross-time comparison: measure both counts NOW and assert total == outside-range(now) + 8 (== 70 on this corpus).** Anchoring to before-item (4) mixes a now-value with a then-value, so an externally-added out-of-range route would fail this check for a reason that belongs to Task C2. Same-instant keeps the fault localized: B3 answers "did exactly my 8 land?", C2 answers "did anything outside move?" This is resume-invariant, because after this write all 8 targets are non-NULL regardless of how many already were. **A delta form — "before-item (2) + 8" — is WRONG on the exact resume A0-pre explicitly tolerates:** if 3 targets were already `codify`, item (2) already counts them and the true rise is 5, so a `+8` assertion false-FAILs a correct corpus. (282 avoided this with "rose by ≤N"; the identity is stronger and simpler.) **Also report the rise over item (2) as a measured value — it must be ≤ 8** — but adjudicate on the identity.
>
> **Task C — confirm the blast radius.** Measure and report actuals (do NOT hardcode):
> - **C2** — `SELECT COUNT(*) FROM lesson_proposals WHERE route IS NOT NULL AND id NOT BETWEEN 193 AND 200` equals **A0-snap before-item (4)** — the outside-range count, **NOT item (2), the total** (equal before the write, divergent after) — and is **UNCHANGED**, measured **62**. The 8 targets were route-NULL and already excluded from item (4); this count does **NOT** rise by 8. Any change means a route outside 193–200 moved → HALT.
> - **C3** — `get_unclassified_entries(conn)` UNCHANGED from A0-snap item (3) (expected `[]`; a delta signals an unrelated staling → HALT).
> - **C4 — ⚠️ THE PARKED PAIR IS UNTOUCHED.** `SELECT id, status, route, target_artifact FROM lesson_proposals WHERE id IN (191,192)` — ⚠️ **Compare against A0-snap before-item (4b), not against this plan's stated literal** — that is what makes "unchanged" a measurement rather than an assumption, and it is the only way to tell a change made DURING this step from one that predated dispatch. ⚠️ **Apply row 5's DIRECTIONAL rule here too — a flat `proposed`-only assertion violates ledger CL1 and false-HALTs a correct run.** Step 1 also sits on the far side of an arbitrary window in two reachable sequences: the Gate-2 plan for 191/192 ships between this plan's DEPOSIT and its dispatch, or Step 1 halts, waits hours/days for adjudication, and is re-dispatched. **And C4 runs AFTER `conn.commit()` while A0-pre never inspects 191/192 — so there is no pre-write catch: the plan would write correctly, then false-HALT, emitting `Status: Partial` and marking all nine QA rows `❌`.** So: `status` unchanged **or** `proposed → terminal` (`implemented`/`rejected`/`superseded`/`reference`) → **PASS**, recording `Gate-2 shipped in-window`. `status → stale`, or any change to `route`/`target_artifact` while `status='proposed'` → **HALT**. Targets expected `DRAFTING_CYCLE.md` / `PLANNER_TEMPLATE.md`. ⚠️ **This has no analogue in 282, where 191/192 WERE the targets. Here they are bystanders mid-arc**, and C2 counts them but cannot tell you *which* outside-range row moved. Any change → HALT.
>
> **Deposit:** `knowledge/development/gate-1-route-session-12-captures-2026-07-29.md` — the eight `set_proposal_route` calls made, and the read-back of all 8 rows as **RAW command output, not a summary**, plus an Output Receipt.
>
> **The Output Receipt MUST OPEN with an explicit status line from this CLOSED SET [from 283]:** `Status: Complete` (every task ran) · `Status: Partial — HALTED at <task>, <reason>` (you stopped). **The first is the only proceed-value.** (The `Complete`/`Partial` vocabulary is the template's, `PLANNER_TEMPLATE.md:433`.) Step 2's first action reads this line. A halted status is a legitimate value — do NOT write `Complete` to make the step look tidy. It MUST then carry each of the following on its OWN labelled line — **QA rows 4 and 5 mark themselves `❌ (unverifiable)` if before-items (4) and (4b) are not reported separately, so a summarized "snapshots captured" is a plan-halting deposit:**
> 1. before-item **(1)** — the full status distribution (pre-write)
> 2. before-item **(2)** — the **TOTAL** route-NOT-NULL count (pre-write)
> 3. before-item **(3)** — `get_unclassified_entries(conn)` (pre-write)
> 4. before-item **(4)** — the **OUTSIDE-RANGE** route-NOT-NULL count (pre-write) ⚠️ **distinct from (2) even though both read 62**
> 4b. before-item **(4b)** — **the parked pair's row** (`id, status, route, target_artifact` for 191/192), pre-write, as RAW output. ⚠️⚠️ **QA row 5 marks itself `❌ (unverifiable)` if this item is absent, so omitting it false-fails the guard that protects the pair on every otherwise-clean run.** It is the anchor for Task C4, QA row 5, and QA row 7's reconciliation condition (a). ⚠️ **On a RESUME, prefer the PRIOR dispatch's recorded item (4b) if a committed Step-1 deposit exists; only re-measure if none does, and say which you used.** A resume re-reads this live, so if a concurrent actor had already moved 191/192, the resume would record the damaged state as "before" and Task C4 / row 5 would compare it to itself and PASS — the corruption invisible exactly when a resume is happening. **Severity is bounded here** (this plan cannot itself stale a proposal — `set_proposal_route` writes only `route` — and a concurrent lessons cycle is forbidden by the dispatch precondition), which is why this is a preference rather than the committed-stub machinery a corpus-mutating plan would need.
> 5. the after-values for B1 / B2 / B3 and C2 / C3 / C4, **each labelled with its anchor**: B2 → before-item (1); B3 → before-item (2); C2 → before-item **(4)**; C3 → before-item (3). **B1 and C4 are ABSOLUTE assertions of final state — label them "absolute (no before-anchor)".** Do not invent a before-item for them.
> 6. the absolute backup path **plus its verification result** — the `PRAGMA integrity_check` output and the backup-vs-live count match, as RAW output. ⚠️ **The restore point is a belt-and-braces artefact, NOT this plan's recovery instrument.**
>
>    ⚠️⚠️ **RECOVERY FOR THIS PLAN IS SURGICAL, and that makes whole-DB-restore machinery unearned (Rule 56, `PLANNER_TEMPLATE.md:1081`: resume machinery is justified only when the interrupted work is NOT reproducible).** The entire mutation is `route='codify'` on eight known ids, so its inverse is one statement: **`UPDATE lesson_proposals SET route = NULL WHERE id BETWEEN 193 AND 200`.** A prior draft grew ~10 lines here — a cross-plan-id backup glob, per-candidate content probes, and PRISTINE/THIS-RUN labelling rules — to choose between whole-DB restore candidates that the very next clause declares unusable. **Trimmed on four enumerated premises, each verified rather than assumed** (entry 187, which this plan routes): (i) the mutation is reversible by one UPDATE; (ii) the plan already forbids a whole-DB restore, because one taken after an in-window CEO Gate-2 disposition of 191/192 would revert it; (iii) the 0-byte-backup hazard is handled independently at A00, which `rm`s the corpse and says so; (iv) **no QA row consumes the PRISTINE label** — grep-confirmed, so nothing downstream loses an anchor.
>
>    ⚠️ **On a resume, record BOTH backup paths** — this run's and any earlier one's. **The one ranking still needed: under this plan id, the EARLIER UTC stamp is the pre-mutation snapshot** (the 0-byte corpse having been `rm`ed at A00). That is the only discriminator the trim removed and it costs one line; beyond it, do not rebuild selection machinery — the recovery is the surgical UPDATE above, not a file.
> 7. **`#### Files Created or Modified`** — every **git-tracked** file this step wrote, by path (for this step: the single deposit). ⚠️⚠️ **Do NOT list the backup `.db` or the canonical DB here.** `.gitignore` matches `*.db`, so for those `git status --porcelain` is empty (passes) while `git log -1` is empty (never committed → fails row 0's "exists on disk AND is committed"), and row 0's repair narrowing cannot reach a path that is not a declared deposit — a literal reading would manufacture a Critical `❌` on a clean run. **The backup is recorded under item 6, and row 0's clause (iii) is its reader.** **[from 283 — QA row 0 reads this section and asserts it checked EVERY entry.]** ⚠️ Rule 17's literal string is **`Files Created or Modified (Code)`**, and the identical string at `:515` belongs to **Rule 8**, not Rule 17. The `(Code)` suffix is deliberately dropped here because this plan's deliverables are a markdown deposit and a database mutation, not code, and row 0 greps the un-suffixed heading. **Declared so the divergence from the template's literal string is not read as an error** — 283 carried this declaration and the clone dropped it.
> 8. any flags **or HALT conditions encountered** — including a condition you hit and cleared, which the `Status:` line alone cannot record
>
> ⚠️ **Every measured value is deposited as RAW COMMAND OUTPUT, not a summary or a re-typed figure.** "Raw" means the output is PRESENT, not that it is unlabelled — annotate freely, but the annotation accompanies the raw output, it never replaces it. **QA rows 3, 4 and 5 each anchor to these values, and the pre-write state no longer exists to re-measure.**
>
> Canonical Python file-write — no heredoc. Commit by explicit pathspec. `#### Prompt Feedback` in `### Ledger Updates`.
>
> **Deposits:**
> - `lessons-forge/knowledge/development/gate-1-route-session-12-captures-2026-07-29.md`

---
---

## STEP 2 — QA

---

> **Before starting, read the Step 1 deposit — `knowledge/development/gate-1-route-session-12-captures-2026-07-29.md`, project-relative to YOUR OWN working tree (the `lessons-forge/…` form in the Deposits block is root-joined; do not prepend it here — the plan-225 trap) — and confirm its Output Receipt opening status line reads `Status: Complete`. Anything else — including `Status: Partial — HALTED at …` — stops this step.** ⚠️⚠️ **"Stops" does NOT mean "writes nothing" — it is governed by the HALT-MUST-LEAVE-A-COMMITTED-RECORD rule below, and the two must not be read as contradicting each other.** Produce and commit the QA report anyway, stating the halted Step-1 status and marking every verification row `❌ (Step 1 halted — not verifiable)`. ⚠️ **Note the sequencing that makes this coherent: the header declares `pause_for_verdict: always`, so a halted Step 1 reached the CEO BEFORE you were dispatched — if you are running at all, the CEO has seen the halt and authorised a continue.** They authorised it to obtain a record; stopping with zero deposits hands them a bare `deposit_exists` gate failure instead, which is strictly less than they asked for. ⚠️ **An ALLOWLIST, not a prefix match.** ⚠️ **This is a DELIBERATE NARROWING of `PLANNER_TEMPLATE.md:433`, declared so you do not "correct" it back:** the template permits proceeding on *"Complete or Partial with known acceptable scope"*, but every halt condition in Step 1 concerns whether the write landed correctly, and this step's whole job is certifying that write.
>
> Post a short visible chat message confirming you are starting Step 2 (QA). You are Lessons Forge QA (`agents/FORGE_LESSONS_AGENT.md`). Same absolute-DB rule. **Verification and reporting only — no product-code changes, no DB writes; if a check fails, report it, do NOT fix it.** Do NOT use Monitor. Do NOT edit PROJECT_STATUS directly.
>
> **MANDATORY — Rule 20 self-check (canonical block, Checklist #4 — the exact template with all four placeholders filled, NOT a paraphrase).** ⚠️ **Form selection:** a **DB-mutating plan with a real pytest suite** → the **full canonical block with real evidence files**, not a simple banner. Run the block from `/Users/marklehn/Developer/GitHub/RULE_20_SELF_CHECK_BLOCK.md` (ABSOLUTE path — it lives at the governance root, not in the lessons-forge worktree). Fill ONLY the four placeholders:
> - `plan_slug`: `gate-1-route-session-12-captures-2026-07-29`
> - `qa_report_path`: `<your-own-tree-abs>/knowledge/qa/gate-1-route-qa-2026-07-29.md`
> - `evidence_dir`: `<your-own-tree-abs>/knowledge/qa/evidence/gate-1-route-session-12-captures-2026-07-29/` — **derive from `pwd` (your OWN working tree), NOT a hardcoded main-tree path** (the plan-225 trap).
> - `required_evidence_files`: `["db-invariants.txt", "pytest_targeted.txt"]` — ⚠️ **Python string literals, QUOTED [from 283].** Unquoted they raise `NameError` before any check runs, and the gate then reports "no Rule 20 banner" — a misleading cause for a transcription slip.
>
> Deposit both evidence files BEFORE running the block — it `sys.exit(1)`s if either is missing or empty. **Include the block's literal stdout in the QA report; the banner `## Rule 20 — QA Self-Check Results` and the line `PASSED — SELF-CHECK PASSED` must appear VERBATIM** (byte-enforced by `bellows/gates.py`). **If the block prints `FAILED`, do not proceed with closure — halt and report to CEO** (`RULE_20_SELF_CHECK_BLOCK.md:22`, the canonical template's own closing sentence). ⚠️ **Writing the verification table does NOT satisfy this gate [from 283 / Rule 48(c)]** — `_gate_rule_20_self_check` reads the deposited report for the banner and PASSED line only. End with a self-grep confirming the banner.
>
> **⚠️ WHAT THE BLOCK DOES AND DOES NOT VERIFY — proposal 199, which this plan routes, documents it.** It reads **evidence-file presence and hedging keywords only. It cannot see your verification verdicts.** A table with a genuine `❌` and both evidence files present prints **`PASSED — SELF-CHECK PASSED`, exit 0**. So **expect `PASSED` even on an honest halt** — the gate that catches a failure is `gates.py::_gate_rule_22_verification`. **You must NOT flip a row to ✅, soften its wording, drop it, or re-scope a check to make the block print `PASSED`.** If any row is `❌`, add one line under the stdout naming EVERY `❌` row and stating the step is expected to fail at the rule_22 gate.
>
> **⚠️ Rule 19 — REQUIRED VERBATIM IN THIS PROMPT** (`PLANNER_TEMPLATE.md:561`; absent from 281, present in 282, and it must be read BEFORE the avoidance guidance below): *"If you cannot complete a check, mark it ❌ with a reason. Do NOT mark it ✅ and explain why you couldn't verify. Any ✅ row containing hedging keywords will auto-fail during the self-check in Rule 20."*
>
> ⚠️ **Read that mandate BEFORE what follows.** Hedging language in a ✅ row signals a check that was not performed. The guidance below teaches you to avoid tripping the scanner **with honest measurements** — it does NOT license rewording a check you did not run. **A check you did not execute is `❌`, whatever words you use.**
>
> **⚠️⚠️ DO NOT QUOTE THE HEDGING-KEYWORD LIST — OR THIS PLAN'S PROSE — INTO ANY LINE CONTAINING A `|`.** The block detects a positive-status row by cell equality but scans it for hedging by **whole-line substring**, so any `|`-bearing line pairing a positive token with "pending", "inferred", "assumed", "not run" — even as a quotation — emits CRITICAL and `sys.exit(1)`. Keep table cells SHORT; put narrative below the table.
>
> ⚠️⚠️ **A HEDGING KEYWORD IS FATAL EVEN AS A MEASURED VALUE, including a ZERO count [from 283].** `skipped` is on the list at `RULE_20_SELF_CHECK_BLOCK.md:41`, so the natural cell `55 passed, 0 skipped` CRITICALs a clean run. **Write row 6's value as `<N> passed` and nothing else** — measure `<N>`; Planner's measured collection at authoring: **55**; report the actual.
>
> **⚠️ On a FAILED block run, route its raw stdout to `db-invariants.txt` and describe the failure in the report WITHOUT reproducing the offending row.** The block echoes the offending line verbatim; pasting that into the report makes the echo re-trip the scan permanently, foreclosing Rule 20's own recovery option (a). **This is a declared, narrow deviation on the FAILING path only** — on a PASSING run paste the stdout as Rule 20 mandates. **This step takes EXACTLY THREE declared deviations, enumerated here so the list is CLOSED and CHECKABLE:** (i) the `Status: Complete`-only allowlist, a declared narrowing of `PLANNER_TEMPLATE.md:433`; (ii) **this one** — Rule 20's unconditional "include the literal stdout in the QA report", narrowed on the FAILING path only; and (iii) row 0's narrowing of Rule 17's repair permission to RE-COMMITTING a declared deposit path. **No other rule may be narrowed on any reasoning.** ⚠️ An earlier draft said "this ONE deviation and no others" while the step in fact took three — an agent reaching (i) or (iii) would find a narrowing the closure denied, and the correct inference is that the closure is unreliable, which discards the guard entirely.
>
> **Evidence-source rule.** Every SQL row states which DB it ran against (canonical = `sqlite3 "file:/Users/marklehn/Developer/GitHub/lessons-forge/lessons-forge.db?mode=ro" "<query>"`). ⚠️ **`?mode=ro` NON-HALT FALLBACK:** the DB is WAL-mode and a read-only handle needs its `-shm`/`-wal` sidecars; their absence yields `Error: unable to open database file` on a perfectly healthy DB. **On that specific error, re-run the same command against the same absolute path WITHOUT the `?mode=ro` URI, note that you did, and continue** — only a failure of the fallback is a real failure. Deposit **RAW command output, never a summary of it**.
>
> **Scope:**
> - `knowledge/qa/gate-1-route-qa-2026-07-29.md`
> - `knowledge/qa/evidence/gate-1-route-session-12-captures-2026-07-29/db-invariants.txt`
> - `knowledge/qa/evidence/gate-1-route-session-12-captures-2026-07-29/pytest_targeted.txt`
>
> ⚠️ **The verification table MUST sit under a top-level `## ` heading containing "Verification"** — write exactly `## Verification Table`, with columns `| # | Claim | Status (✅/❌) | Measured value | DB source | Evidence |` (**the Evidence column is mandated by Rule 18 `:558` — cite the evidence file BY PATH**; Rule 17 `:555` fixes the `Status (✅/❌)` spelling). `gates.py:657` tracks sections via `startswith("## ")` only, so a `###` heading puts the table OUTSIDE the scanned region and an `❌` would go undetected.
>
> ⚠️⚠️ **AND THE SECTION NEVER CLOSES — close it yourself [from 283].** `in_verification_section` is never cleared by `###`/`####`, so it runs to end-of-file and every later `|`-bearing block is graded as if it were the verification table. **Immediately after the table, write exactly `## Evidence and Narrative`**, and keep the Rule 20 stdout, the Output Receipt and `### Ledger Updates` under `## `-level headings.
>
> ⚠️ **Use ONLY `✅` or `❌` — no third value.** A table where no row carries a positive token and none carries `❌` produces **zero** failures at BOTH gates; a table of hedged non-verdicts passes silently. If an outcome feels like neither, it IS an `❌`.
>
> **⚠️ A FAILING ROW DOES NOT LICENSE SKIPPING THE REST.** This step has **nine checks — rows 0, 1, 2, 3, 4, 5, 5b, 6, 7** — mutually independent. **Run all nine, record every measured value, and halt only after the table is complete.** Inside a row, "→ halt" means *mark it `❌`, record why, and keep going*. **A HALT MUST STILL LEAVE A COMMITTED RECORD:** finish the report, write the evidence files, and commit whatever deposits exist by explicit pathspec.
>
> ## Verification Table
>
> 0. **Deliverable verification (Rule 17 — MANDATED in the final step, BEFORE the regression check) [from 283].** Read Step 1's Receipt `#### Files Created or Modified` section and verify **EVERY** listed deliverable — **not sampling.** For each: confirm it exists on disk and is committed **in its CURRENT state — two commands, both required**: `git log --oneline -1 -- <path>` **AND** `git status --porcelain -- <path>` which must be EMPTY. ⚠️ **`git log` alone is insufficient** — a file committed then modified returns its old commit, so the check passes while the content is uncommitted. **Any ❌ here is a Critical finding that blocks Done.** ⚠️ Rule 17 permits "re-create the file"; **this plan narrows that to RE-COMMITTING a deposit path it already declares.**
>
>    ⚠️⚠️ **AND CHECK TWO THINGS NO OTHER ROW COVERS — the parent had these and the clone dropped them, which left Receipt items 5 and 6 with NO downstream reader at all.** Restoring a mandated deposit without restoring its reader reinstates the language, not the mechanism (the defect shape row 5b names):
>    - **(iii)** the Receipt records the **absolute path of the restore point AND its verification result** (`PRAGMA integrity_check` + the backup-vs-live count match).
>    - **(iv)** the Receipt contains **Step 1's own B1/B2/B3 and C2/C3/C4 read-backs**. ⚠️ **Rows 1–5b re-measure CURRENT DB state, so they prove the OUTCOME is right but cannot show Step 1 verified its own write.** Without (iv), a Step 1 that skipped Tasks B and C entirely and happened to write correctly closes clean, and Step 1's "run all of B and C before halting" bound becomes unauditable. **You are checking PRESENCE, not values — the values are rows 1–5b's job.** ⚠️⚠️ **AND RECORD THIS ROW IN RULE 17's MANDATED FORM, which this plan's parent 282 and its predecessor 283 both omitted — a mandated element absent from parent AND clone is invisible to a clone-diff (proposal 196, which this plan routes).** Rule 17 (`:555`) does not merely require the checks; it requires *"Output as a verification table: `| Deliverable | Expected | Status (✅/❌) | Evidence |`. This is not sampling — every listed deliverable is checked."* One row of the main table cannot give "every deliverable" a structural home, and a requirement living only in prose does not survive execution (Rule 54, `:1069`). **So: keep this as row 0 of the main table, and put its evidence under `## Evidence and Narrative` as a `| Deliverable | Expected | Status (✅/❌) | Evidence |` sub-table with ONE ROW PER FILE listed in Step 1's Receipt item 7.** ⚠️ That sub-table is `|`-bearing prose outside the verification section — keep its cells short and free of hedging keywords. ⚠️ **A `NOT CAPTURED — halted at <task>` marker is recorded verbatim as this row's evidence but does NOT satisfy the clause** — the record IS incomplete, and an `❌` the CEO expects is an accurate report, not a false failure.
> 1. **All 8 routed.** `SELECT id, entry_id, status, route, category, confidence, target_artifact FROM lesson_proposals WHERE id BETWEEN 193 AND 200 ORDER BY id` — 8 rows, **each `confidence='high'`** (the Rule 46 carve-out premise, re-verified at run time), each `route='codify'`, each `status='proposed'`, and each `category`/`target_artifact` matching the plan's disposition table **per row** (⚠️ **NOT a uniform category** — 198/199 are `instrumentation`). Absolute assertion.
> 2. **No status moved — adjudicated in two parts, because a flat "byte-identical" CONTRADICTS row 5.** ⚠️⚠️ **Row 5 explicitly blesses a CEO Gate-2 ship for 191/192 inside the Step-1→Step-2 window as `✅`. That event moves two rows `proposed → implemented`, so the distribution is NOT byte-identical — and a flat assertion here marks `❌` on the exact event row 5 marks `✅`.** The plan would fail itself on a legitimate CEO action. So:
>    - **(a) THIS PLAN'S BLAST RADIUS — a hard assertion.** All 8 targets (193–200) are still `status='proposed'`. This is what this plan is responsible for, and `set_proposal_route` cannot move a status, so any deviation is a genuine `❌`.
>    - **(b) THE REST OF THE CORPUS — reconciled, not asserted flat.** Compare the full distribution to before-item (1). **Byte-identical → `✅`.** Any delta → identify WHICH proposals moved and reconcile against row 5: a `proposed → terminal` move confined to **191/192** is the blessed in-window Gate-2 ship — record it as `✅` with the same `Gate-2 shipped in-window` note row 5 uses, quoting both rows so the two agree in the report. **Any other movement — a different id, or any move to `stale` — is `❌`.** ⚠️ **If Step 1's Receipt does not report item (1) separately, mark this row `❌` (unverifiable).**
> 3. **Blast radius — total.** ⚠️ **Adjudicate on a SAME-INSTANT identity, not a cross-time one:** measure both counts now and assert **total route-NOT-NULL == outside-range route-NOT-NULL (measured NOW) + 8**. ⚠️ **Do NOT anchor this to before-item (4)** — that mixes a now-value with a then-value, so an externally-added route outside the range fails BOTH this row and row 4, and this row fails for a reason that is not its own. **Keeping it same-instant means row 3 answers "did exactly my 8 land?" and row 4 answers "did anything outside move?" — one fault, one failing row, correct localization.** On this corpus both resolve to **70 == 62 + 8**. Resume-invariant, because after the write all 8 targets are non-NULL however many already were. **A `before-item (2) + 8` form false-FAILs any partial resume** — Step 1's precondition explicitly tolerates a target already routed `codify`, and item (2) would already count it. Report the rise over item (2) as a measured value (**must be ≤ 8**), but do not adjudicate on it.
> 4. **Blast radius — outside range.** `SELECT COUNT(*) FROM lesson_proposals WHERE route IS NOT NULL AND id NOT BETWEEN 193 AND 200` equals Step 1's before-item **(4)**, **UNCHANGED**, measured **62**. ⚠️⚠️ **Anchor each after-value to its OWN before-item — (2)↔total, (4)↔outside-range. Both before-items read 62, so crossing them silently passes while checking nothing.** **If Step 1's deposit does not report item (4) separately, this row is `❌ (unverifiable)`** — and append the verbatim receipt lines you searched, plus your grep and its empty result, to `db-invariants.txt` BEFORE marking it.
> 5. **The parked pair is untouched — anchored to Step 1's before-item (4b), not to this plan's literal.** ⚠️ **If Step 1's Receipt does not report item (4b), mark this row `❌` (unverifiable)** — the same fail-closed rule rows 2(b) and 4 apply to their anchors. `SELECT id, status, route, target_artifact FROM lesson_proposals WHERE id IN (191,192)`. ⚠️ **No analogue in 282. Row 4 counts these two but cannot say WHICH outside-range row moved**, so this row names them directly.
>
>    ⚠️⚠️ **ADJUDICATE `status` DIRECTIONALLY — a flat "both still `proposed`" false-`❌`s the CEO's own workflow.** The approval gate between Step 1 and this step is hours-to-days, and **191/192 are Gate-2-bound**: the CEO may legitimately ship their Gate 2 inside that window, flipping them `proposed → implemented`. The discriminator this corpus already uses: **a move to `stale` is corruption; a move to a `_TERMINAL_STATUSES` member (`implemented`/`rejected`/`superseded`/`reference`) is a CEO disposition.** So — `status` unchanged **or** moved from `proposed` to a terminal status → **`✅`** with a `Gate-2 shipped in-window` note recorded. `status` → `stale` → **`❌`**. Any change to `route` or `target_artifact` while `status` is still `proposed` → **`❌`** (nothing legitimate moves those mid-flight); if `status` also moved terminal, treat a re-placement as part of the same Gate-2 disposition and record it rather than failing. ⚠️ **Row 4 is unaffected either way** — a Gate-2 ship changes `status`, not `route`, so the outside-range count stays 62.
> 5b. **Classification unchanged across the approval window.** `get_unclassified_entries(conn)` **UNCHANGED from Step 1's before-item (3)**. ⚠️ **This helper needs a live `sqlite3.Connection`, and this step declares NO DB writes — so open it READ-ONLY: `sqlite3.connect("file:/Users/marklehn/Developer/GitHub/lessons-forge/lessons-forge.db?mode=ro", uri=True)`.** The natural `sqlite3.connect(<path>)` is read-WRITE on the live production DB and touches its WAL sidecars, contradicting this step's own no-writes declaration. Same `?mode=ro` fallback applies — anchor to the recorded before-value, NOT to a hardcoded `[]`. ⚠️ **Without this row, Step-1 Receipt item (3) is a mandated deposit with NO downstream reader** — the shape this plan flags as a defect elsewhere. It matters because the Step-1→Step-2 window is hours-to-days and Step 1's own C3 closes only *within* Step 1: row 2(b) catches proposal-level staling, but an entry-level classification delta is otherwise unchecked. Planner's measured value at authoring: `[]`.
> 6. **Full suite** — `python3 -m pytest src/ -v` to an explicit pass/fail, raw tail shown. Compute the baseline from `--collect-only` and reconcile against the most recent prior QA in `knowledge/qa/`. Confirm 0 regressions. ⚠️ **Also assert `src/` is untouched** — `git status --porcelain -- src/` run from YOUR OWN working tree (not `-C <root>`; `src/` lives in this submodule) must be EMPTY. This plan writes no code, so any `src/` delta is out of scope. It is a secondary guard — `scope_check` IS cwd-visible for `lessons-forge/src/`, unlike the root doctrine files in row 7 — but the parent carried it and it costs one command.
> 7. **The Gate-2 target artifacts are UNCHANGED by this plan.** ⚠️ `lessons-forge` is a submodule; these files are tracked by the ROOT repo and do NOT exist in your worktree — a plain `git diff` passes VACUOUSLY. One invocation, capturing the exit code:
>    `git -C /Users/marklehn/Developer/GitHub status --porcelain -- DRAFTING_CYCLE.md PLANNER_TEMPLATE.md RULE_20_SELF_CHECK_BLOCK.md; echo "PORCELAIN-EXIT=$?"`
>
>    ⚠️⚠️ **AND RE-MEASURE THE CONTENT PINS — porcelain ALONE CANNOT SEE A COMMITTED EDIT, so without these the "this row IS the guard" claim below is FALSE.** The parent plan carried `shasum` pins for exactly this reason and they were dropped in cloning; restored here. Run `shasum -a 256 /Users/marklehn/Developer/GitHub/{DRAFTING_CYCLE.md,PLANNER_TEMPLATE.md,RULE_20_SELF_CHECK_BLOCK.md}` and compare against the Planner's authoring pins:
>    - `DRAFTING_CYCLE.md` → `d8f17394c08d7dc7…`
>    - `PLANNER_TEMPLATE.md` → `49b726447498d0c5…`
>    - `RULE_20_SELF_CHECK_BLOCK.md` → `c90ffb4bea0063e9…`
>
>    ⚠️⚠️ **ONE NARROW RECONCILIATION, because otherwise this row CONTRADICTS row 5 on the event row 5 BLESSES.** Row 5 blesses an in-window CEO Gate-2 ship of 191/192 — and **Gate 2 IS codification into the target artifact**: 191 targets `DRAFTING_CYCLE.md`, 192 targets `PLANNER_TEMPLATE.md` (measured live). **So the blessed event necessarily edits TWO of the three files this row pins**, leaving porcelain empty (it is committed) and both pins mismatched. Without a bless-path this row marks `❌` — the one `❌` declared unadjudicatable and Critical — on a correct run plus a legitimate CEO action. That is the same defect the walk-1 ACID fold fixed at row 2 and did not sweep here, and a direct violation of ledger constraint CL3.
>
>    **So: a pin mismatch on `DRAFTING_CYCLE.md` or `PLANNER_TEMPLATE.md` ONLY is `✅ (reconciled)` IFF BOTH hold** — (a) row 5 recorded a `proposed → terminal` transition for 191/192 **measured against before-item (4b)** — a DB-backed transition, not an in-report assertion, and (b) `git -C /Users/marklehn/Developer/GitHub log --oneline -- <path>` shows the change is that codification commit. Record `Gate-2 shipped in-window`, quote row 5 so the two rows agree in the report, and capture the log + diff into `db-invariants.txt` either way. ⚠️ **Everything else stays absolutist: a non-empty porcelain is `❌` regardless; a mismatch on `RULE_20_SELF_CHECK_BLOCK.md` is `❌`** (its only proposal, 199, is Gate-2-bound *after* this gate, so nothing legitimate touches it now); **and any mismatch you cannot tie to (a)+(b) is `❌`.** ⚠️ **This is the ONLY adjudication this row permits — you may not extend it by analogy.** ⚠️ **The pins above are TRUNCATED to their leading 16 hex characters — compare only that prefix of your measured digest.** A full 64-char digest will never equal a 16-char-plus-ellipsis string, and this row forbids adjudicating a failure away, so an unstated comparison rule manufactures a `❌` on a clean tree. ⚠️ These are the *authoring-time* values — a mismatch means the file changed between authoring and now, which is exactly what the attribution capture below is for; report the measured value, do not adjust to the literal (Checklist #29). ⚠️ **Why `bellows/scripts/plan_lint.py` is PROHIBITED (CEO Context) but deliberately NOT pinned here:** a prohibition is inert and costs nothing, but a content pin on an actively-churning bellows file could only ever false-HALT this plan on an unrelated change. The asymmetry is intentional — do NOT "complete" the pin set. **Also record root HEAD** (`git -C <root> rev-parse --short HEAD`; authoring value `881ec60`) — a HEAD delta alone is a reconcile-note, but paired with a pin mismatch it identifies the commit.
>    ⚠️⚠️ **NON-EMPTY PORCELAIN IS `❌`. FULL STOP. YOU DO NOT ADJUDICATE IT [from 283, which had to restore this after an earlier draft softened it].** Plan 282: ***"There is no other guard; this row IS the guard"*** — `scope_check` is cwd-scoped and cross-repo-blind, so nothing else can see an edit to a root doctrine file. **You may not mark this ✅ on the reasoning that the change looks unrelated, pre-existing, or someone else's — that judgment is not yours to make.** On any non-empty result: capture `git -C <root> log --oneline` and `git -C <root> diff` for those paths into `db-invariants.txt` **before** halting, mark `❌`, and let the CEO adjudicate at the verdict gate. **A non-zero exit code is likewise `❌`.**
>
> **Write rows 0–5, 5b and 7 raw output to `db-invariants.txt`; row 6's pytest tail to `pytest_targeted.txt`.** ⚠️ **Non-empty is ALL the Rule 20 block checks — a one-byte file passes it.** Before running the block, **self-grep each file for markers proving the content landed.** ⚠️⚠️ **A single `PORCELAIN-EXIT=` marker is VACUOUS for 8 of the 9 rows routed to `db-invariants.txt`:** that string comes from row 7's `echo`, which prints unconditionally, so an agent that writes only row 7's block and drops or truncates rows 0–5b satisfies both the Rule 20 non-empty check AND the grep — including row 4's specifically mandated "append the verbatim receipt lines you searched" evidence. **That is exactly the shape proposal 199 describes, and this plan routes it.** So: **prefix each row's block in `db-invariants.txt` with a literal `ROW-<n>:` line** (`ROW-0:` … `ROW-5b:`, `ROW-7:`), then assert **`grep -c '^ROW-' db-invariants.txt` == 8** and record the count. For `pytest_targeted.txt`, grep the pytest summary line. Record every grep's result.
>
> **Deposit:** the QA report — table under `## Verification Table` with the mandated columns, raw output, the Rule 20 banner + PASSED line + literal stdout (passing runs only), Output Receipt — plus the two evidence files. Canonical Python file-write — no heredoc. **Commit by EXPLICIT PATHSPEC** — `git add <exact paths>` then `git commit -m "…" -- <exact paths>`. **NEVER `git commit -a`, never an unscoped `git add -A`.** ⚠️ **`git add` FIRST — load-bearing for NEW files:** `git commit -- <untracked path>` fails with `pathspec … did not match`. **The fix is to `git add` and retry — NEVER to fall back to `git commit -a`.**
>
> In `### Ledger Updates` include `#### Project Status` (one milestone: Gate 1 complete for the session-12 batch — 8 codify / 0 backlog / 0 reference; all 8 remain `proposed` and Gate-2-bound; the parked pair 191/192 unharmed), `#### Forward Register` (Gate 2 owes: the doc+gate pairing for proposals 198/199 per §6, and the un-codified 2026-07-25 subtractive-trim parent lesson noted in the CEO Context), and `#### Prompt Feedback`.
>
> **Deposits:**
> - `lessons-forge/knowledge/qa/gate-1-route-qa-2026-07-29.md`
> - `lessons-forge/knowledge/qa/evidence/gate-1-route-session-12-captures-2026-07-29/` (multiple files per Rule 20 self-check)
>
> ⚠️ **The evidence DIRECTORY is one bullet; the filenames are deliberately NOT listed [from 283].** `PLANNER_TEMPLATE.md:808` (Rule 26): *"Evidence files MUST be represented by the evidence directory as a single bullet… Do NOT list individual evidence files… duplicating them in `**Deposits:**` is forbidden."* 282 and its parents violate this. It matters because this step may halt mid-table and commit only the deposits that exist — a declared-but-unwritten file would produce a false gate failure on an honest halt.
>
> **Do NOT move this plan to `Done/`.** The close path is owned by Bellows on continue-verdict consumption — **never by the agent** (`PLANNER_TEMPLATE.md:513`, Rule 8: *"Either way, the agent is not the actor"*). This is unconditional; an `unauthorized_done_move` force-recovers the file and FAILs this step's gates.

---

## Drafting Cycle

**This section is a RECORD of the drafting cycle, NOT instructions for any step.** Nothing here modifies the steps above. ⚠️ It sits inside the final step's extracted span (`gates.py:449` runs the final step's match to end-of-file with no later step to stop at) — a parsing artifact, not scope. Per §3 "compact" and proposal 197 (which this plan routes), the per-fold narrative is NOT here.

**Tier:** T2 — trigger fired: **T-2** (production-data mutation — writes `route` on 8 canonical proposals). T-6 does NOT fire (routing metadata, no doctrine edit — that is Gate 2). Proven clone of 282, so T-8 does not fire; T-2 sets the floor. The corpus holds a parked non-terminal pair (191/192) this plan must not touch, so the cold panel is not skippable.
**Walks:** v0 → v15. **Walk 1 COMPLETE** (all five lenses folded, 11); cold panel in progress. Destruction run first (entry 186 — aim the walk at the step that MUTATES).
- Weak spots:          w1 3 folded (the derived-id command from Destruction's own fold produces MULTI-LINE GARBAGE on two in-progress matches — found by EXECUTING it, not reading it; and A00 runs BEFORE the isolation check that would catch that, so A00 carries its own single-match HALT. Row 5 asserted a flat "both still proposed", which false-`❌`s a legitimate Gate-2 ship for 191/192 inside the Step-1→Step-2 approval window → directional rule. `set_proposal_route:256` re-verified live, citation accurate).
- Destruction:         w1 3 folded (B3/row-3 asserted a `+8` DELTA that false-FAILs the partial resume A0-pre explicitly tolerates — 282 used "rose by ≤N"; replaced with the resume-invariant IDENTITY total == outside-range + 8; swept BOTH sites. Backup filename hardcoded a guessed plan id the Planner cannot know — now derived from the in-progress filename with a HALT on an empty derivation).
- Vulnerabilities:     w1 2 folded. cold 6 (1 rejected). **confirming 2 folded — NOT dry, both from the PREVIOUS pass's own fold.** (a) Destruction added before-item **(4b)** and anchored three consumers to it, but **never added a Receipt deposit slot for it** — so QA row 5's fail-closed clause ("if the Receipt does not report item (4b), mark `❌`") would have fired on EVERY clean run, false-failing the guard the fold existed to install. Receipt item 4b added; sweep re-verified across all consumers (C4, row 5, row 7 condition (a), the halt-durability before-item list). (b) The 4b block was inserted BEFORE its anchor, so A0-snap enumerated `1, 2, 3, 4b, 4` — out of order, with 4b forward-referencing numbering that came after it. Reordered to `1, 2, 3, 4, 4b`. **Both are the add-the-capture-but-not-the-deposit shape; the guard is only as real as its slot.** **cold 6 folded, 1 REJECTED.** ⚠️ **The worst was found by EXECUTION, not reading:** the `?mode=ro` fallback path leaves a **0-BYTE file** at `$BK`, and because shell state does not persist between agent commands the re-run recomputes `$(date)` into a NEW filename — so an empty corpse and a real backup both match the glob, the corpse has the earlier stamp, and Receipt item 6's "lexicographically-first = PRISTINE, roll back to THIS" rule hands the CEO **an empty file as the sole recovery artefact for a production mutation**. Compounded by a second tension: if recovery is a fresh deposit rather than a re-dispatch, bellows mints a NEW id (`bellows.py:378`), so the `<PLAN_ID>`-scoped glob cannot see the first run's pristine backup at all — the id-scoping hardening and the earliest-stamp rule are each right alone and wrong together. **Both were resolved by selecting PRISTINE on CONTENT** (pre-mutation iff `route IS NULL` on 193-200 reads 8 inside the candidate) — ⚠️ **and that machinery was later TRIMMED by the cold integration lens** on four enumerated premises, because it selected between whole-DB restore candidates the plan forbids using. **The surviving hazard controls are A00's `rm` of the 0-byte corpse and the one-line earlier-stamp rule in item 6.** Noted here so a clone does not hunt for content-probe machinery and re-add it (entry 189: sweep the body for citations the log no longer explains). Also: the plan-file regex accepted only 2 of bellows' FOUR live lifecycle prefixes, so every resume of a **halted** or rate-limit-**parked** step would HALT at the first command reporting the wrong cause; `db-invariants.txt`'s single `PORCELAIN-EXIT=` marker was vacuous for 8 of 9 rows (it comes from an unconditional `echo`) → per-row `ROW-<n>:` markers with a counted assertion; row 5b's helper needed an explicit read-only handle in a declared no-writes step; the shasum pins were truncated with no comparison rule, which would manufacture a `❌` on a clean tree. **REJECTED with evidence:** the reader placed `set_proposal_route` at `:257`; it is at **`:256`** as cited. **The weak-spots fold broke on its own edge, twice, both found by EXECUTION:** it accepted only `in-progress-` (false-HALTs a re-dispatch sitting at `verdict-pending-`), and the two-glob `ls` form fails under zsh, where an unmatched glob errors the WHOLE command — so the ORDINARY single-file case returned empty and HALTed. Replaced with a glob-free `grep -E` match; all four states (in-progress / verdict-pending / two / none) executed at authoring. Also: `set_proposal_route` never checks `rowcount`, so a bad id is a SILENT no-op — B1's read-back is the only effect-observation and is now stated as non-redundant with A0-pre.
- Integration-record:  w1 1 folded (the PLAN_ID/backup-filename apparatus had no stated WHY — nothing in this plan reads the backup back and the Receipt already records its absolute path, so it is a cheap hedge on ONE narrow window [death after backup, before deposit], not a mechanism; bounded and marked do-not-grow, so a clone inherits the reason rather than the machinery — the Rule 56 pattern from 283). Citations re-opened and confirmed: `set_proposal_route:256`, Rule 46 `:1012` ("reject daemon-bug workaround proposals"), Rule 35 `:892`, Checklist #29, and the claim that 282 carries the manual bootstrap block Rule 35 says to omit. Premise re-measured at this pass: 62 / 62 / proposed 10 — unchanged.
- ACID:                w1 2 folded. cold 6. **confirming 2 folded — NOT dry.** (a) IC1's scope fix created an UNDECLARED tension with the rest of Rule 21: its `targeted` clause also says the QA step *"must NOT run the full suite"*, and row 6's `pytest src/` IS the whole module — so targeted and full are the same command and the same 55 tests, and the prohibition is not satisfiable as a distinction here. Declared explicitly, since the alternatives are worse (no regression evidence, or two byte-identical files). **A fix in one clause of a rule left the plan crosswise with another clause of the SAME rule.** (b) Before-item (4b) had no resume anchor — a resume re-reads it live, so a pair already moved by a concurrent actor would be recorded as "before" and C4/row 5 would compare it to itself and PASS, invisible exactly when a resume is happening. Now prefers the prior dispatch's recorded value; severity bounded because this plan cannot itself stale a proposal (`set_proposal_route` writes only `route`) and a concurrent cycle is forbidden. **cold 6 folded, and the HIGH is the one that matters: row 5 BLESSES an event row 7 then FAILS.** Gate 2 *is* codification into the target artifact, and 191 targets `DRAFTING_CYCLE.md` while 192 targets `PLANNER_TEMPLATE.md` — **both pinned by row 7** — so the blessed in-window Gate-2 ship necessarily mismatches two pins while leaving porcelain empty, and row 7 declared that `❌` unadjudicatable and Critical. Identical to the walk-1 row-2 defect, unswept to row 7; a direct CL3 violation. Fixed with one narrow, closed reconciliation (row-5 transition recorded AND the commit identified by `git log`), everything else absolutist. Also: Task C4 kept the flat `proposed`-only assertion the ledger's CL1 amendment retired — and it runs AFTER the commit with no pre-write catch, so a correct run would write, then false-HALT, then fail all nine QA rows; the gitignored backup `.db` was in scope for row 0's "is committed" check, manufacturing a Critical `❌` on a clean run; a resume's A0-snap labelled post-write values "(pre-write)" and invited a drift-halt; the ledger's C1/C2/C3 names collided with Task C's post-trim C2/C3/C4, so a clone applying a ledger line to the like-named check would "fix" a sound guard; and the Cycle Log still advertised the trimmed PRISTINE machinery as live. **Original w1 finding retained:** two fold-interactions no single lens could see. **(1) Row 2 CONTRADICTED row 5:** the weak-spots fold blessed a CEO Gate-2 ship for 191/192 in the Step-1→Step-2 window as `✅`, but that moves two rows `proposed → implemented` and row 2 still asserted the distribution byte-identical — so the plan marked `❌` and `✅` on the same event. Split into (a) a hard assertion over THIS plan's 8 targets and (b) a reconciliation for the rest, quoting row 5 so the two agree. **(2)** B3 and row 3 anchored the total-count identity to a BEFORE value, mixing now with then; made same-instant so one fault produces one failing row and the blast-radius question stays localized — swept BOTH sites.
**Cold panel (T2):** COMPLETE — all 5 lenses run as sequential fresh-context readers, each on the prior's folded draft; every finding author-verified before folding., sequential fresh-context readers; every finding author-verified before folding.
- Cold weak-spots: → v6: 9 folded, **all clone-drift — guards the parent carried that this clone silently dropped.** ⚠️ **The two worst:** (1) Step 1, the MUTATING step, had NO halt-durability clause — 282's "IF YOU HALT AFTER `conn.commit()`, YOU MUST STILL DEPOSIT AND COMMIT" was gone, so a halt at B or C left a mutated corpus with the four before-snapshots existing only in the agent's context and the `Partial — HALTED` receipt value unreachable. (2) Row 7 kept 282's absolutist "this row IS the guard" language while **dropping the `shasum` content pins that make it true for a COMMITTED edit** — the same guard, third consecutive plan, third distinct failure mode (282 had it; 283 weakened it and the cold walk restored it; here the language survived and the mechanism did not). Also: the Step-2 opener contradicted its own halt rule and ignored that `pause_for_verdict: always` means the CEO already authorised the continue; Receipt item (3) had no downstream consumer (new row 5b); "this ONE declared deviation" was false, there were three (now enumerated); no `?mode=ro` fallback in either step (latent false-HALT at task one); the `src/` untouched check and its scope prohibition both dropped; the Step-1 deposit unnamed by path; the restore-point VERIFICATION result never deposited. ⭐ Reader independently re-verified the full backbone — all 8 disposition rows, 62/62, 200 proposals, `set_proposal_route:256` with no rowcount check, 55 tests, and every doctrine citation — plus the three dedup claims and the un-codified-parent flag.
**Conflicts (CL-prefixed throughout, to avoid colliding with Task C's C2/C3/C4 sub-checks — a collision the C1 trim created):**
CL1 — the parked pair 191/192 must survive unchanged on `route` and `target_artifact`, and on `status` EXCEPT a CEO-driven terminal transition, which must be recorded (destruction w1; amended by weak spots w1 for the approval-window case; enforced at BOTH Task C4 and QA row 5 after cold ACID found only row 5 carried it).
CL2 — no assertion may be a delta where a resume can legitimately move the starting point; prefer resume-invariant identities (destruction w1).
CL3 — every cross-row assertion must agree with every other on the SAME event: a check that blesses an outcome obliges its siblings to bless it too (ACID w1; violated twice — row 2 vs row 5, then row 7 vs row 5 — and repaired both times).
**Ledger status at confirming-walk close — re-checked against the instructions as they now stand:** **CL1 SATISFIED** — enforced at BOTH Task C4 and QA row 5, each anchored to before-item (4b), which now has a Receipt slot and a resume preference. **CL2 SATISFIED** — B3 and row 3 are same-instant identities, row 4 anchors to the resume-invariant item (4), and (4b)'s live-re-read residue is closed. **CL3 SATISFIED** — rows 2(b), 4, 5, 5b and 7 now all agree on the blessed in-window Gate-2 ship. ⚠️ **CL1 and CL3 were each found VIOLATED by the plan's own instructions during the cold panel and repaired** — the third consecutive plan in this lineage where the ledger recorded a constraint the instructions did not honour, which is itself the argument for keeping the ledger.
**Closing:** NOT REACHED — walk 1 complete (11 folds); **cold panel COMPLETE, all 5 lenses, +36 folds incl. 2 subtractive trims, with 2 cold findings rejected on evidence.** **Confirming walk COMPLETE — all five lenses run, all five folded (1/1/2/1/2 = 7).** Per §3's Cycle-Log template the last event before deposit must be a lens pass, and the last event is a fold, so §2's closing condition is NOT met. **Cycle totals: 54 folds** — walk 1: 11 · cold panel: 36 (incl. 2 subtractive trims, 2 findings rejected on evidence) · confirming walk: 7. **Not deposit-ready on §2's test; the disposition is the CEO's, on the same fail-closed standard 283 shipped under.**  Per §3's Cycle-Log template (and §2.7's sequential-fold rule) the last event must be a lens pass; the last event is a fold. Not deposit-ready.
