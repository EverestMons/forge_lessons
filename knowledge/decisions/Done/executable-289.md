# Lessons Forge — Gate 1 Route Disposition 2026-07-31 (route the 6 session-13/14 proposals)
**Date:** 2026-07-31 | **Tier:** Small | **Dispatch Mode:** bellows | **Test Scope:** targeted | **Execution:** Step 1 (DEV — route + verify) → Step 2 (QA) | **qa_steps:** 2 | **pause_for_verdict:** always
**cycle_tier:** T2

## CEO Context

Gate 1 for the 6 proposals cycle 288 produced. **CEO disposition, taken 2026-07-31: all 6 → `codify`.** Gate 2 (codification into doctrine) is a separate plan with a CEO decision between.

**⚠️ THIS GATE WRITES `route` ONLY — NO status change.** All 6 are `codify` → Gate-2-bound → all KEEP `status='proposed'`. **There is no hand-written SQL in this plan:** every write is `set_proposal_route(conn, id, 'codify')` (`src/lessons_forge.py:256` — re-verified live at authoring: parameterised `UPDATE lesson_proposals SET route = ? WHERE id = ?`, validates against `_VALID_ROUTES`, **no internal commit, and no `rowcount` check — a bad id is a SILENT no-op**). Because no status moves, the **status distribution is byte-identical before/after** — an invariant to CHECK. The only delta is `route` NULL→`codify` on exactly six rows.

**Clone lineage.** Direct clone of **284** (the prior Gate 1, which routed 8). Per entry 183 — clone against the NEWEST same-class plan — **284 is both the clone origin AND the newest same-class plan**, so §2.6's diff-against-the-newest requirement is satisfied by the origin diff itself; there is no second same-class plan to diff against. ⚠️ **Plan 288 was consulted for SPECIFIC FACTS ONLY** (the Forward Register evidence, the ledger-constraint account, the cycle's measured figures) — **it is a cycle plan, a different class, and its machinery has NOT been diffed against this plan.** ⚠️ **An earlier draft claimed such a forward-diff had been performed; it had not, and the claim is struck rather than quietly softened.** ⚠️ **A "proven clone" is where the cold panel's value is highest — this plan does NOT down-tier on clone provenance**, and the panel is handed 284 as the comparison artifact.

**⚠️⚠️ PROVENANCE CONVENTION — every execution claim in this plan is marked, because three of them were inherited and worn as this plan's own before being caught.** A cloned plan inherits its parent's *evidence sentences* along with its machinery, and a sentence like *"verified by execution"* reads identically whether the author ran it or copied it. **Three separate claims in this plan were the latter** — the authoring self-check recorded as run before it had been, a forward-diff against the previous plan that was never performed, and A00's derivation described as verified-working on 284's authority. **All three are now either discharged by execution or struck.** From here:
- **`[EXECUTED HERE — <date>]`** — run at authoring in this session, with the observed result stated.
- **`[INHERITED FROM <plan> — NOT RE-EXECUTED]`** — believed on a parent's evidence, with the reason it was not re-run.

⚠️⚠️ **SCOPE — the convention governs EXECUTION claims about commands and procedures, NOT measured values.** A claim of the form *"this form is verified-working"*, *"run at authoring"*, *"executed across all states"* carries a marker or is read as unverified. **Measured values — the authoring baseline, the dedup counts, cited line numbers, corpus figures — are governed by the separate and already-stated discipline that every one is CONFIRMED at run time, not assumed** (Checklist #29: report actuals; a mismatch is a finding, not a number to adjust to). ⚠️ **The scope is stated because an unqualified "unmarked means unverified" would sweep a dozen correctly-measured claims in this plan into looking doubtful — a rule introduced without sweeping the sites it governs, which is the ledger-decay shape this plan documents three times over.**

**Stating the convention rather than patching the three instances is proposal 205's discipline — write the general form, so later sites are checked against it — applied to the plan that routes proposal 205.**

### The disposition table

| proposal | entry | substance | `target_artifact` | category | route |
|---|---|---|---|---|---|
| 201 | 193 | pin the content hash of an artifact a step COPIES at run time; a version number is inert | `PLANNER_TEMPLATE.md` | `governance_rule` | **codify** |
| 202 | 194 | deletion is the third resolution to oscillation, alongside joint-resolve and escalate | `DRAFTING_CYCLE.md` | `governance_rule` | **codify** |
| 203 | 195 | verify a guard's NECESSITY against the runtime, not only its correctness | `PLANNER_TEMPLATE.md` | `governance_rule` | **codify** |
| 204 | 196 | state what a mandated command PRINTS on success vs failure, and confirm they differ | `DRAFTING_CYCLE.md` | `governance_rule` | **codify** |
| 205 | 197 | sweep a fold FORWARD as a rule, not just sideways as an edit | `PLANNER_TEMPLATE.md` | `governance_rule` | **codify** |
| 206 | 198 | the Cycle Log must contain no string a gate matches | `DRAFTING_CYCLE.md` | `governance_rule` | **codify** |

**⚠️ TWO PLACEMENT QUESTIONS THE BATON RESERVED FOR GATE 1 — BOTH PUT TO THE CEO, BOTH ADJUDICATED, RECORDED AS DECISIONS RATHER THAN AS PREMISES.** The session baton named these by name *"so Gate 1 decides it deliberately"*, and `target_artifact` is the field Gate 2 acts on, so a settled-looking assertion would quietly remove a decision the record reserved.
- **Entry 193 → proposal 201: `PLANNER_TEMPLATE.md` CONFIRMED over the defensible second placement `RULE_20_SELF_CHECK_BLOCK.md`.** The proposal is a *planning* rule — pin the hash of any artifact a step copies at run time, and distinguish unchanged-by-this-plan from unchanged-since-authoring — so it belongs with the rules that govern plan authorship, not inside the copied artifact itself. ⚠️ **Noted for Gate 2: the block IS the artifact the rule is about, so a cross-reference from `RULE_20_SELF_CHECK_BLOCK.md` may still be wanted; that is a Gate-2 shaping question, not a re-opening of placement.**
- **Entry 197 → proposal 205: `PLANNER_TEMPLATE.md` CONFIRMED**, as a Checklist #26 amendment — see the PARTIAL analysis below.

**Dedup performed at Gate 1 (cycle 288 deferred it, and its Step-1 prompt explicitly forbade it).** Measured against live doctrine at authoring — none of the six is already codified in its target artifact. **Four are wholly new:**
- **201** — zero hits for `content hash` / `shasum` anywhere in `PLANNER_TEMPLATE.md`. Rule 39 (`PLANNER_TEMPLATE.md:918` — pre-edit verification of SA-derived claims) is adjacent but distinct: it re-verifies a *claim* at edit time; 201 pins an *artifact's bytes* across the authoring→runtime window.
- **202** — `DRAFTING_CYCLE.md` §2.8's oscillation bullet offers **only** joint-resolve or escalate. §2.7's subtractive-trim clause governs *how* to trim, never *when to choose* trimming.
- **203** — literal (`-F`) counts in `PLANNER_TEMPLATE.md`: `necessity` **0**, `unreachable` **0**, `reachable` **1** — and the single `reachable` hit is at `:388`, in an unrelated clause about whether an MCP tool is available to an agent. ⚠️ **An earlier draft stated all three as zero; the count is corrected here, and the conclusion is unchanged.** Rule 56 asks *is the interrupted work reproducible*; 203 asks *is the recovered-from state reachable at all*. Complement, not duplicate.
- **204** — §2.7's execute-against-real-data bullet says **run** the check; nothing in the file says state what success and failure each PRINT.

**Two are PARTIAL — an existing clause stops short and the proposal amends it:**
- **205** — Checklist #26 (`PLANNER_TEMPLATE.md:1287`) is entirely present-tense: sweep the artifact for **existing** siblings, weight toward the mutating step. Its `Source:` line reads `proposals 136 + 162 + 193` — plan 287 strengthened it **yesterday** (`3c327e3`, 2026-07-30). **205's forward-sweep (write the general form as a ledger constraint so LATER folds are checked against it) has no analogue there.**
- **206** — ⚠️ **`DRAFTING_CYCLE.md:107` ALREADY STATES THE MECHANISM**, added by plan 287 (`3c327e3`, 2026-07-30) codifying the previous batch's proposal 197: *the final QA step's gate span absorbs the block, so a gate-matching string quoted in the log is evaluated as if the QA step had said it.* **What 206 adds is the prohibition, the re-run-the-gate check after editing the log, and the diagnostic signature (a WARN that disappears when you edit the LOG).** Gate 2 amends that clause; it does not write a new one. ⚠️ **Recorded here because a Gate-2 author reading only the proposal text would land a duplicate paragraph beside an existing sentence.**

**⚠️⚠️ RULE 46 GENUINELY ENGAGES ON 206 — and is cleared on its own stated terms, not waved past.** Rule 46 (`PLANNER_TEMPLATE.md:1012`) directs *Planner-side workarounds for a daemon bug* to `FORWARD.md` rather than codification. 206 names a live `gates.py:449` mechanism, so the rule is correctly reached for. **It does not fire, on two independent measured grounds:** (a) the rule is scoped to **medium-confidence** proposals and carves out that *"proposals with high confidence… may be accepted on merit"* — **measured live: `confidence='high'` on all six**, so the carve-out applies mechanically; (b) the rule's own second carve-out covers *"workaround value independent of the bug fix"* — 206's discipline (do not quote gate-matching strings in a record) holds whether or not the span regex is ever changed. **`confidence` is therefore in Task A0-pre's and QA row 1's SELECT so the premise is re-verified at run time, not only at authoring.**

**⚠️⚠️ AND RULE 46's OTHER HALF IS OWED — BUT THE CHANNEL ITSELF IS BROKEN, AND THAT IS MEASURED, NOT SUSPECTED.** Rule 46 does not merely permit codification — it directs the underlying bug to the project's `FORWARD.md` via the Output Receipt's `#### Forward Register`, which **the daemon appends post-merge; agents never write `FORWARD.md` directly.** Three facts, each verified at authoring with a literal (`-F`) search:

1. **Plan 288 DID mandate the block.** `executable-288.md:540` instructs a `#### Forward Register` carrying **three** enumerated items, citing Rule 46 and naming `bellows/knowledge/FORWARD.md` explicitly.
2. **288's QA agent DID emit it.** `lessons-forge/knowledge/qa/cycle-qa-2026-07-30.md:152` carries the heading followed by three correctly formatted numbered items — the `encoding=` portability gap and the two measured `plan_lint` §4 defects.
3. **And `bellows/knowledge/FORWARD.md`'s newest row is still #24, dated 2026-06-14.**

⚠️⚠️ **AND THE CAUSE IS NOT A DEFECT AT ALL — IT IS AN UNCONFIGURED DESTINATION. THIS IS THE THIRD READING OF THIS EVIDENCE AND THE FIRST ONE TAKEN FROM THE CODE PATH.** **[EXECUTED HERE — 2026-07-31]** `bellows.py:1417` computes the destination as **`<project_path>/knowledge/FORWARD.md`** — the *plan's own project*. For a lessons-forge plan that is **`lessons-forge/knowledge/FORWARD.md`, which DOES NOT EXIST**, so the daemon logs `ledger: no FORWARD.md in project, skipping forward append` and returns. **Verified in plan 288's own run log.** ⚠️⚠️ **`bellows/knowledge/FORWARD.md` was NEVER the destination for a lessons-forge plan; its newest row #24 is expected, not evidence.**

⚠️ **Two earlier readings in this plan's own drafting were wrong and are retracted:** (1) *"288 never wrote the block"* — false, it did; (2) *"the failure is the daemon append"* — **also false, the daemon behaved exactly as written.** **The obligation reached the artifact, reached the deposit, and then hit a project with no register file.**

⚠️⚠️ **A SECOND MECHANISM FACT, measured, that makes the earlier remediation unsatisfiable even if the file existed:** `bellows.py:1430` reduces the captured item to **`lines[0]`** — one row, first line only — and `parser.py:75` terminates the capture at the first blank line. **Four bullets would yield ONE row carrying item (a); (b), (c) and (d) would be silently discarded.** An earlier draft promised *"the four rows confirmed present"*; **that was never achievable.**

**Three consequences this plan must honour rather than paper over:**
- **The Forward Register block is still emitted, because Rule 46 prescribes that channel and the block is a real part of this plan's record** — but **this plan makes NO claim that any row reaches any `FORWARD.md`.** Row 0(v) verifies the block is present in the deposit and nothing more; that is now its complete and accurate job.
- **The durable destination is the session baton (`shop_next_session.md`), not a register file.** ⚠️ **An earlier draft designated a post-close read of `bellows/knowledge/FORWARD.md` as "the PRIMARY remediation"; that check would have inspected a file this project never writes to, found nothing, and reported a defect that does not exist.** **Struck.** All **six** items are carried at wrap into the baton, which is read at the start of every session.
- ⚠️ **The real, actionable finding for the CEO is a GAP, not a bug: `lessons-forge/knowledge/FORWARD.md` does not exist, so every Rule 46 routing from a lessons-forge plan is silently discarded by design.** The fix is a CEO decision — create the project's register file, or route bellows-side tooling items through a bellows-project plan. **Recorded in the Forward Register block itself and in the baton; not acted on by this plan.**

**⚠️ 205's Gate 2 obligation, recorded so Gate 2 inherits it as a requirement and not as a note.** The CEO's disposition on 205 is `codify` **with the remedy mechanized** — a `plan_lint` check, not a prose instruction to remember. The reason is measured, in the same session that produced 205: **three Conflict Ledger constraints were violated by their own authors, two inside the edit that created them** (C6 opened for the sweep problem and never populated; C7/C8 carried rosters that decayed; C9 was violated at two sites from where it was written and proved over-broad). 205's proposed remedy leans on exactly that mechanism. **Codifying "write it into the ledger" without a mechanical check codifies the thing that just failed 3/3.** Session 15's codification-effect measurement is the corroborating evidence: 4 of 5 predictions failed, and **the only one that passed was the prediction about a mechanical habit rather than a judgement one.** ⚠️ **This is Gate 2's scope, NOT this plan's — recorded in the Receipt's Forward Register and in the disposition report, acted on nowhere in this plan.**

**⚠️⚠️ FOUR CONDITIONS DIFFER FROM 284 — cloned unchanged, each would false-HALT or false-PASS.**

1. **Six targets, not eight** — ids **201–206**, entry_ids **193–198**, a contiguous 1:1 mapping (`201→193 … 206→198`).
2. **⚠️ CATEGORIES ARE UNIFORM HERE — THE INVERSE OF 284's WARNING, AND THE MOST LIKELY CLONE-DRIFT DEFECT IN THIS PLAN.** 284 carried a prominent `do NOT assert a uniform category` warning because its 198/199 were `instrumentation`. **Here all six are `governance_rule`, all `high`, all `target_layer='governance'` — measured live (`SELECT DISTINCT` returns exactly one row).** Carrying 284's warning forward verbatim would tell the agent to expect a mix that does not exist; inverting it into a *uniform assertion* is correct **but the check must still be performed PER ROW** — the expectation is uniform, the verification is not. A `SELECT DISTINCT`-style aggregate would pass while an individual row carried the wrong `target_artifact`.
3. **TWO target artifacts, not three** — `PLANNER_TEMPLATE.md` (201, 203, 205) and `DRAFTING_CYCLE.md` (202, 204, 206). **`RULE_20_SELF_CHECK_BLOCK.md` is NOT a target of this batch** — but it stays pinned in QA row 7, and ⚠️ **the reason is proposal 201, which this plan routes, not merely "nothing should move it": Step 2 COPIES the canonical Python block out of that file at run time.** That is precisely 201's authoring-vs-runtime drift window — a plan authored against the block as it stands, deposited, then executed after the block changes. **The row-7 pin is the content hash 201 prescribes**, and stating the real rationale keeps a later editor from removing a pin whose only stated justification was a generality.
4. **⚠️⚠️ THERE IS NO PARKED PAIR — `proposed` IS EXACTLY THIS PLAN'S SIX.** 284 ran with `proposed = 10`: its 8 targets plus the Gate-2-bound pair 191/192, bystanders it had to protect. **Session 14 drove `proposed` to 0 and session 15's cycle created exactly these six** — measured at authoring: `SELECT id FROM lesson_proposals WHERE status='proposed'` returns `201, 202, 203, 204, 205, 206` and nothing else.

**⚠️⚠️ CONSEQUENCE OF (4): 284's ENTIRE BYSTANDER SUBSYSTEM IS DELETED, NOT ADAPTED — and the premises are enumerated because §2.7 requires it.** Removed: before-item **(4b)** (the parked pair's row), **Task C4**, **QA row 5**, **QA row 7's `Gate-2 shipped in-window` reconciliation**, and **ledger constraint CL1**. Per §2.7's subtractive-trim clause — *verify the subsumption against live data, per item, not in aggregate* — each premise was measured, not reasoned:
- **(i)** The `proposed` set is exactly {201…206}. **Measured.** There is no bystander row for (4b)/C4/row 5 to name.
- **(ii)** Gate 2 acts on rows that are `proposed` **and** `route='codify'`. **No FOREIGN proposal is in that state** — measured — so no *other* batch's Gate 2 can ship in-window, and row 7's bless-path has no foreign trigger. ⚠️⚠️ **THE PREMISE IS NARROWED TO "FOREIGN" DELIBERATELY, AND AN EARLIER DRAFT OVERSTATED IT.** It claimed *no* in-window Gate-2 ship was reachable — **but this plan's own Task A is exactly what makes 201–206 Gate-2-eligible.** Once Step 1 commits, a Gate-2 plan for these six could in principle be dispatched inside the hours-to-days approval window, flipping them toward `implemented` and editing two of the three files row 7 pins — **breaking QA row 2's flat assertion and two of row 7's three pins at once.** ⚠️⚠️ **That residual is NOT "closed" — it is SHIFTED ONTO THE CEO, and the honest word matters.** A dispatch precondition lives in this section as prose: **no step reads it, no gate enforces it, and bellows dispatches on deposit.** 284 could adjudicate the equivalent event mechanically, anchored to a measured DB transition plus a `git log` identification; **this plan has no such mechanism and cannot adjudicate it at all** — a violation surfaces as a Critical `❌` on rows 1, 2 and 7 *after* the corpus is mutated, blocking Done on a state the CEO deliberately created. **The trade is accepted because the alternative re-admits an adjudication path that can excuse a real failure — but it is a convention, not a guard, and an earlier draft called it "closed".** The precondition: **no Gate-2 plan for 201–206 may be dispatched until this plan closes.** Recorded alongside the existing "no other lessons-forge plan in flight" precondition. **A bless-path is the weaker fix** — it would re-open an adjudication route inside rows that are strictly stronger absolutist, to cover an event the CEO fully controls the timing of.
- **(iii)** Therefore **QA row 7 returns to fully absolutist** (plan 282's *"there is no other guard; this row IS the guard"*), which is both simpler and **stronger** than 284's reconciled form. ⚠️ This is a *hardening* by deletion, and it must not be read as a relaxation.

⚠️⚠️ **THE TRADE THIS BUYS, STATED — AN EARLIER DRAFT RECORDED ONLY THE UPSIDE.** The deletion does not merely harden; it also **narrows what this plan can tolerate**. 284 ran correctly with `proposed = 10`; **this plan HALTS at A0-pre on a seventh `proposed` row and fails QA row 2 flat if one appears in the approval window.** A foreign proposal created by a concurrent lessons cycle — a legitimate action, not corruption — therefore produces a Critical `❌` on an otherwise perfect run. ⚠️ **That is the same false-fail class 284's split row 2 existed to prevent, re-created in a different form.** **The trade is accepted deliberately** — an absolutist row that occasionally false-fails is safer here than an adjudication path that can excuse a real failure, and the exposure is closed by concurrency precondition (1) being scoped to the whole plan rather than to dispatch. **Recorded so the next clone inherits the cost as well as the benefit.**
- **(iv)** ⚠️⚠️ **CORRECTED — AN EARLIER DRAFT CLAIMED THE BLAST-RADIUS QUESTION WAS "CARRIED, IN FULL, BY Task C1 / QA row 4 (the outside-range count)". MEASURED AGAINST LIVE DATA, THAT WAS FALSE.** A count of `route IS NOT NULL` rows **cannot see a foreign row moving between two non-NULL routes** — seven such rows exist, and flipping any one leaves every count-based guard passing. **284's deleted row asserted route VALUES; the count asserts only membership.** ⚠️ **This was the one deletion premise never verified against live data, in a plan that quotes §2.7's per-item subtractive-trim mandate and routes proposal 195.** **The capability is now genuinely restored** — by before-item **(4b)**, a value-level row image of everything outside the range, asserted at **C1(b)** and **QA row 4(b)**. **That is a generalisation of what 284 carried, not a replacement for it, and it is stronger than the parent's version because it watches every foreign row rather than two named ones.**

⚠️ **This deletion is proposal 202's own discipline — *when a region keeps getting re-folded, the answer is usually DELETION, not another patch* — applied to the plan that routes proposal 202.** Recorded deliberately: an author adapting 284's four-part bystander apparatus to a corpus with no bystander would produce correct machinery for an unreachable state, which is **exactly the defect proposal 203 describes**. Two of this batch's own proposals bear directly on how this plan was drafted.

**⚠️⚠️ A0 items (2) AND (4) ARE BOTH 70 — THE COLLISION, LIVE FOR THE THIRD CONSECUTIVE GATE 1.** They are DIFFERENT measurements that coincide before the write: (2) is the **total** route-NOT-NULL count; (4) is the **outside-range** count (`id NOT BETWEEN 201 AND 206`). They are equal now only because the 6 targets are route-NULL and therefore excluded from both. **After the write they diverge: (2) becomes 76, (4) stays 70.** Capture and report them **separately**; comparing an after-value to the wrong before-item passes for the wrong reason on a clean run. 282 found this at 60/60, 284 at 62/62, and it recurs here at **70/70**.

**⚠️ Authoring baseline — measured 2026-07-31 against the live canonical DB, to be CONFIRMED not assumed** (Checklist #29 — report actuals; a mismatch is a finding, not a number to adjust to): status distribution `implemented 147, superseded 28, rejected 15, reference 7, proposed 6, stale 3` (total **206**); `lesson_entries` **198**; total route-NOT-NULL **70**; outside-range route-NOT-NULL **70**; `get_unclassified_entries(conn)` `[]`; all 6 targets `route IS NULL`; test collection **55**.

**⚠️ Test Scope: `targeted` — Rule 21 (`PLANNER_TEMPLATE.md:604`) REQUIRES a justification for this scope.** This plan writes **no code**: it sets `route` on six rows through an existing helper and deposits markdown, modifying zero source files, so the blast radius is bounded and there is no cross-bucket regression surface. ⚠️ **And for this repo targeted ≡ full — `lessons-forge` has exactly ONE test module (`src/test_lessons_forge.py`, 55 tests measured at authoring), so `pytest src/` IS both sets.** Declaring `full-suite` would promise two byte-identical evidence files. **Lineage note: 281 declared `both` and 283 declared `full-suite`, neither implementing the extra run either mandate requires; 282 and 284 declared `targeted` and were right.** This plan follows 284.

⚠️⚠️ **ONE CONSEQUENCE OF THAT CHOICE, DECLARED so it is not read as a violation.** Rule 21's `targeted` clause also says the QA step *"must NOT run the full suite unless the targeted run reveals unexpected failures."* **Row 6 runs `pytest src/`, which for this repo IS the entire module — so the targeted set and the full suite are the SAME command and the SAME 55 tests, and the prohibition is not satisfiable as a distinction here.** Rule 21's taxonomy presumes the two are separable; with one test module they are not. Declared rather than left implicit, because the alternatives are worse: skipping the run would leave a DB-mutating plan with no regression evidence.

**Scope discipline:** routing only. **Do NOT edit `DRAFTING_CYCLE.md`, `PLANNER_TEMPLATE.md`, `RULE_20_SELF_CHECK_BLOCK.md`, `bellows/scripts/plan_lint.py`, or `bellows/gates.py`** — codification is Gate 2. **Do NOT change any `status`.** **Do NOT touch `src/`** — this plan writes no code. **Do NOT touch any proposal outside 201–206.** **Do NOT append to `LESSONS.md` while this plan is deposited-but-un-run** — the nine held candidates stay in scratchpad. ⚠️ **The reason is NOT the batch-pin trap, and citing it here would be wrong:** that trap fires on a lessons-forge *cycle* plan, which asserts a `would_insert == N` batch delta and halts when the delta moves. **This plan makes no such assertion and would not halt on an append.** The real reason is narrower — the held candidates belong to a batch whose boundary Gate 2 has not yet set, so appending now commits them to a cycle that has not been scoped. ⚠️ **Recorded precisely because a guard justified by an inapplicable premise is the defect proposal 203 describes, and this plan routes proposal 203.**

**⚠️ Concurrency — TWO preconditions, BOTH scoped to the whole plan, not to dispatch.** (1) **No other lessons-forge plan in flight — from dispatch until this plan CLOSES**, ⚠️ **not merely at dispatch: the Step-1→Step-2 approval gate is hours to days, and a lessons cycle starting inside it can create a seventh `proposed` row that this plan CANNOT tolerate (see the trade below).** (2) ⚠️ **No Gate-2 plan for 201–206 dispatched until this plan CLOSES** — Task A is what makes these six Gate-2-eligible, so unlike a foreign batch this one becomes reachable *because of* this plan; see premise (ii). This is an `R(before) → W → R(after)` schedule over a shared store.

⚠️ **What actually guards what, stated accurately rather than flatteringly.** Task **C1** (outside-range count) and **QA row 2** (flat distribution) are the real detection. **The Step-1 isolation pre-flight is a cheap sanity read, NOT a concurrency guard** — its two-samples-apart check samples only the interval the agent actually waits — which is why Step 1 now mandates a stated minimum of 5 seconds rather than leaving it to "a moment." ⚠️ **An earlier draft called that pre-flight "the guard"; it cannot bear that weight, and advertising it as load-bearing is worse than the weak check itself.** A0-iso's real value is the POSITIVE signal (your own plan file is present, so you are reading the right tree) and the absence of a second in-progress plan — both of which it does reliably.

**No diagnostic precedes this plan, deliberately** (recurring-gate practice): every unknown was measured inline against live data at authoring.

**Deposit-once discipline:** to be deposited exactly once; `knowledge/decisions/` verified to contain only `Done/` at authoring.

**⚠️ Authoring self-check — `plan_lint.py` WAS RUN, twice, and its ACTUAL output is recorded rather than assumed.** ⚠️ **First run: exit 1** — two WARNs and one FAIL. ⚠️ **After the fixes below, subsequent runs: exit 0.** ⚠️⚠️ **FINAL PRE-DEPOSIT RUN (CL7), against the artifact as deposited: `PLAN_LINT-EXIT=0`, with exactly ONE WARN — the §2 closing-condition warning, which is CORRECT, expected, and deliberately left firing** (see the Cycle Log's Closing line). **All eight structural checks PASS: header, dispatch_mode, pause_for_verdict, both steps' Deposits blocks, the QA banner pair, and both steps' Scope blocks.** ⚠️ **The only edit after that run is this sentence recording it; it changes prose, not structure, and a re-run confirmed the same verdict.** **Both runs are recorded because the first one's findings are the reason the second is clean; a record showing only the clean run would hide the two real defects it found.**

⚠️⚠️ **A RECORDED LINTER RESULT DECAYS WITH EVERY SUBSEQUENT EDIT — so the ORDER is part of the obligation.** Any edit made after a run invalidates the recorded exit code for the artifact that actually ships; a result written mid-cycle describes a file that no longer exists. **`plan_lint.py` MUST therefore be re-run as the LAST action before deposit, and the figure recorded here replaced with that final run's output.** ⚠️ **Until that final run, treat the exit code above as describing an earlier draft, not this one.** The three original findings and their dispositions:
- **WARN — Drafting Cycle closing indicates a fold as the last event, not a dry lens pass.** ⚠️ **CORRECT, and deliberately so at the time of the run:** the Cycle Log's Closing line is phrased so this check *cannot* clear until a walk actually closes dry. **This WARN is the check working, not a defect to word around.**
- **WARN — step 2 mentions tests but declares no test scope.** Real: the check looks for a `test_*.py` filename or a `tests/` prefix inside the step text, and the step said only `pytest src/`. **Fixed by naming `src/test_lessons_forge.py` in row 6**, which is accurate — it is the repo's only test module.
- **FAIL — (c) QA banner pair: missing banner and PASSED line.** ⚠️⚠️ **This one is important, and it is a genuine tension with proposal 206 that only RUNNING the linter exposed.** Check (c) does `banner in plan_text` over the **whole plan**, so it *requires* a QA plan to contain two literal gate-matching strings. An earlier draft removed them — applying 206's discipline — and thereby broke a check its parent passed. **Resolved by quoting both strings in the QA step's INSTRUCTIONS while keeping them out of the Cycle Log**, which is what 206 actually governs: the Log is a *record*, the step prompt is an *instruction*, and only the former is absorbed into a gate span as if the step had said it.

⚠️⚠️ **CARRY THIS TO GATE 2 — it is a live codification hazard for proposal 206.** If 206 is codified as *"the PLAN must contain no gate-matching string"* rather than *"the CYCLE LOG must contain none"*, **every QA plan in the shop immediately fails `plan_lint` check (c)**, which mandates the opposite. **The scope word is load-bearing.** Discovered by execution, not by reading — which is proposal 204's discipline applied to this plan's own authoring.

⚠️ **Per the previous batch's proposal 198, a clean `plan_lint` exit is NOT evidence the §4 block ran** — the block is warn-only and several sub-checks cannot fail. **Read the Cycle Log's Closing line, never the WARN.**

## How to Run This Plan

Bellows dispatches this plan automatically when deposited; no manual bootstrap required. (Rule 35 requires this section to be omitted or replaced with exactly this note for `dispatch_mode: bellows`.)

---
---

## STEP 1 — DEV (route the 6, then verify)

---

> **FIRST — post a short visible chat message (1-2 sentences) confirming you are starting this plan and your immediate next action.** Do NOT rename the plan file.
>
> You are the Forge Developer (`/Users/marklehn/Developer/GitHub/forge/agents/FORGE_DEVELOPER.md` — skip with a note if absent). **Every canonical-DB access uses the ABSOLUTE path** `/Users/marklehn/Developer/GitHub/lessons-forge/lessons-forge.db`. **`forge/forge.db` is a REAL but DIFFERENT database — never open it.** Run commands from **your own working tree**; the ONLY exception is canonical-DB access by the absolute path.
>
> ⚠️ **`?mode=ro` — THE PRIMARY PATH IS EXECUTED-HERE; THE FALLBACK IS INHERITED. The two are marked distinctly because half this block is measured and half is not.**
>
> **[EXECUTED HERE — 2026-07-31]** The `.backup` through the `?mode=ro` URI was run at authoring against the live canonical DB: **exit 0, 937,984 bytes written, `PRAGMA integrity_check` → `ok`, and the backup's counts `198|206` equal to live.** The primary path works.
>
> ⚠️ **[EXECUTED HERE — 2026-07-31]** the fallback below, **on a sidecar-less COPY of the canonical DB — production never touched.** A `cp` does not copy the `-shm`/`-wal` sidecars, which reproduces the condition exactly. Measured: **read through `?mode=ro` → `Error: in prepare, unable to open database file (14)`, exit 14; `.backup` through `?mode=ro` → `Error: unable to open database file`, exit 1, leaving a 0-BYTE file at the target path.** **All three inherited claims are TRUE.**

⚠️⚠️ **RETRACTION, recorded rather than quietly corrected.** An earlier draft marked this branch `[INHERITED FROM 284 — NOT RE-EXECUTED]` on the stated reason that *"reproducing that condition would require removing the live production DB's WAL sidecars, which is not an acceptable thing to do."* **That reason was false** — a one-command copy reproduces it with zero risk, as above. ⚠️ **CL8 admits an inherited claim only "with the reason it was not re-run"; this plan's newest convention was therefore satisfied in FORM and not in SUBSTANCE on its first application** — reinstating the language without the mechanism, which is the exact defect shape this plan flags elsewhere. **Found by a cold reader who tried the thing I had asserted was impractical.**

**The condition itself:** A00's `.backup` reads through a `?mode=ro` URI, and the DB is WAL-mode — **if the `-shm`/`-wal` sidecars are absent, `sqlite3` errors on a perfectly healthy DB.**

⚠️ **WHEN THAT STATE IS ACTUALLY REACHABLE, measured — because a guard whose precondition never holds is proposal 203's defect, and this plan routes 203.** **[EXECUTED HERE — 2026-07-31]** On the canonical DB the sidecars are PRESENT and `?mode=ro` returns exit 0; and after a clean `sqlite3` close they **persist** (both sidecars still on disk, read still exit 0). **So this fallback does NOT fire on the ordinary canonical path.** It becomes reachable when the DB arrives without its sidecars — a **copy or restore**, which is not hypothetical: `lessons-forge/CLAUDE.md` documents exactly that as the recovery route (*"On a fresh clone, recover by copying from an existing machine"*). ⚠️ **The guard is therefore KEPT and its precondition is now stated rather than assumed** — the failure of an earlier draft was never that the branch was unreachable, but that nobody had asked. **On that specific error: `echo "$BK"` FIRST, then re-run with that SAME LITERAL path, without the `?mode=ro` URI — do NOT recompute the timestamp.** ⚠️⚠️ **The failing `.backup` leaves a 0-BYTE FILE at `$BK` on disk.** ⚠️⚠️ **DO NOT `rm` ANYTHING ON THE MANDATED PATH — AN EARLIER DRAFT TOLD YOU TO, AND IT WOULD HAVE DELETED THE BACKUP.** **[EXECUTED HERE — 2026-07-31]:** re-running to the SAME literal path **overwrites the 0-byte corpse with the good backup** (verified: 0 bytes → 937,984 bytes, `ok`, `198|206`). **There is then exactly ONE file at `$BK` and it is the good one**; an `rm "$BK"` at that point destroys the restore point you just took, on the failure path, before any write. ⚠️ **The two-file "empty corpse plus real backup" state only arises on the branch this instruction FORBIDS — recomputing `$(date -u …)` into a second filename.** So: **do not recompute the timestamp, do not `rm`.** If you ever do end up with two files under this plan id, say so and leave both. Note the fallback in the Receipt, and continue — only a failure of the fallback is a real failure.
>
> **Open canonical read-WRITE** for the writes (`sqlite3.connect(<abs path>)`; do NOT reuse a `?mode=ro` handle). The six `set_proposal_route` calls are ONE transaction: a **SINGLE `conn.commit()` after all six** (the helper does not commit internally).
>
> **⚠️ EXECUTION ORDER — run in exactly this sequence.**
> 1. **A00** — restore point, then verify it is real.
> 2. **A0-iso** — isolation pre-flight.
> 3. **A0-pre** — precondition: targets match the disposition table.
> 4. **A0-snap** — capture the four before-snapshots.
> 5. **A** — the six writes, then one commit.
> 6. **B**, **C** — read-only post-verification. Run ALL of them before halting on any one.
> 7. Deposit and commit. ⚠️ **This is the CLEAN-RUN order. A halt at 1–4 also deposits and commits — but only when the tree is CONFIRMED; see the two-halves halt-durability rule below, which is the authority on every halt path.**
>
> **Scope:**
> - `knowledge/development/gate-1-route-session-13-14-captures-2026-07-31.md`
>
> **Task A00 — TAKE A RESTORE POINT BEFORE ANY WRITE, THEN VERIFY IT IS REAL.** Use `.backup`, NOT `cp` (live WAL). Write to the MAIN tree by ABSOLUTE path (a worktree-local backup dies at teardown). Build the path in a shell variable FIRST. ⚠️ **[EXECUTED HERE — 2026-07-31]** This exact form was **run at authoring against the real directory, under the real runtime shell (`zsh`) and the real `ugrep` shim**, across all six reachable states — **all FOUR lifecycle prefixes plus both failure modes**: `in-progress-` → id derived; `verdict-pending-` → id derived; `halted-` → id derived; `parked-` → id derived; zero matches → `HALT: … found 0`; two matches → `HALT: … found 2`. **All six behaved as documented.** ⚠️ **An earlier draft attested "all five reachable states" and silently omitted `halted-` — 3 of the 4 prefixes it calls load-bearing.** The missing state was then executed rather than the wording softened. ⚠️ **An earlier draft carried the bare phrase "this exact form is verified-working" — inherited from plan 284 and restated in this plan's voice without having been run here. It has now been run; the claim below is this session's evidence, not a parent's.**
> ```
> D=/Users/marklehn/Developer/GitHub/lessons-forge/knowledge/decisions
> F=$(ls "$D" 2>/dev/null | grep -E '^(in-progress|verdict-pending|halted|parked)-executable-[0-9]+\.md$')
> N=$(printf '%s' "$F" | grep -c .)
> [ "$N" = "1" ] || { echo "HALT: expected exactly 1 own plan file, found $N"; exit 1; }
> PLAN_ID=$(printf '%s' "$F" | sed -E 's/^(in-progress|verdict-pending|halted|parked)-executable-([0-9]+)\.md$/\2/')
> case "$PLAN_ID" in ''|*[!0-9]*) echo "HALT: derived plan id '$PLAN_ID' is not numeric"; exit 1;; esac
> mkdir -p /Users/marklehn/Developer/GitHub/lessons-forge/data/backups
> BK="/Users/marklehn/Developer/GitHub/lessons-forge/data/backups/lessons-forge-pre-gate1-${PLAN_ID}-$(date -u +%Y%m%dT%H%M%SZ).db"
> echo "PLAN_ID=$PLAN_ID"; echo "BK=$BK"
> sqlite3 "file:/Users/marklehn/Developer/GitHub/lessons-forge/lessons-forge.db?mode=ro" ".backup '$BK'"
> ```
> ⚠️ **The single-match guard asserts that exactly ONE lifecycle-prefixed file exists — it does NOT assert the file is YOURS.** On the wrong-tree branch (the one A0-iso exists to catch, and A0-iso runs *after* this task) a single foreign plan file yields a derived id that is not yours, and A00 then writes a real backup stamped with another plan's id — on a branch the halt rule says writes nothing. **It is gitignored, additive, mutates no data, and no gate sees it**, so this is RECORDED rather than guarded: the cost is one stray file in `data/backups/` misattributed in exactly the narrow human-recovery case the id-stamping exists to serve. **Inherited from 284 unchanged. Do NOT "fix" it by moving A00 after A0-iso — the restore point must precede everything.**
>
> ⚠️ **[INHERITED FROM 284 — NOT RE-EXECUTED]** Do NOT inline `$(date …)` between single-quoted parts of the `.backup` argument — sqlite3 MISPARSES that and writes NO backup. **(Not re-run here because reproducing it means deliberately issuing a malformed backup command; the correct form is the one executed above, and it works.)** ⚠️ **The filename must carry THIS plan's id** — `data/backups/` already holds **15 `.db` files** (measured at authoring) matching generic `pre-*` prefixes and a non-unique glob resolves to a stale snapshot from another cycle. ⚠️ **The id is DERIVED from your own in-progress filename, not hardcoded** — the daemon mints it on claim, so the Planner cannot know it at authoring time. ⚠️ **The derivation ASSERTS EXACTLY ONE MATCH and HALTs otherwise**, matches all four live lifecycle prefixes, and uses `grep -E` rather than a shell glob — **the runtime shell is zsh** — ⚠️ **[EXECUTED HERE — 2026-07-31]** confirmed `zsh`, with the shipped `grep -E` form returning correct results in all five states — **and an unmatched glob in a multi-glob `ls` errors the WHOLE command** (⚠️ **[INHERITED FROM 284 — NOT RE-EXECUTED]**: the glob-form failure was not reproduced here, because the `grep -E` form that replaced it is what ships and that form is measured). A00 carries its own single-match guard because it runs BEFORE the isolation check.
>
> ⚠️ **Why the id-in-filename is worth these lines, stated so a future clone inherits the reason and not just the machinery.** **Nothing in this plan ever reads the backup back** — there is no resume ladder that globs for it, and the **Output Receipt (item 6) records its absolute path**, which is the primary retrieval route. The filename matters in exactly one narrow case: **the step dies after the backup and before the deposit**, so the receipt does not exist and a human must pick the right file out of the fifteen-plus in `data/backups/`. That case is real (plan 283 hit it) but it is the *only* one — a cheap hedge on a narrow window, not a load-bearing mechanism. **Do not grow it further.**
>
> **Then VERIFY, IN THIS ORDER.** ⚠️⚠️ **FIRST, A NON-ZERO SIZE CHECK — `ls -la "$BK"` and assert the byte count is > 0.** **[EXECUTED HERE — 2026-07-31]: `PRAGMA integrity_check` returns `ok` on a 0-BYTE FILE and on a path that does not exist at all** (exit 0 both times), so the integrity check ALONE certifies the exact failure mode this task documents three paragraphs above. **A green `integrity_check` is not evidence a backup exists.** Then: `PRAGMA integrity_check` must return `ok`, AND the backup's `COUNT(*)` for `lesson_entries` / `lesson_proposals` must equal the LIVE DB's counts at backup time (expected **198 / 206** — confirm against live, not against these literals). Any failure → HALT before any write. State the absolute path in the deposit.
>
> **Task A0-iso — ISOLATION pre-flight, from the tree where the state lives.** Assert no other writer. **Read by ABSOLUTE MAIN-TREE path — a worktree-relative `ls` passes VACUOUSLY.** Run `ls /Users/marklehn/Developer/GitHub/lessons-forge/knowledge/decisions/` and assert the POSITIVE signal: **your own plan file MUST appear** (as `in-progress-*`, `verdict-pending-*`, `halted-*` **or** `parked-*` — ⚠️ **all FOUR, bellows' live prefix tuple; a two-prefix check false-HALTs every resume of a halted or rate-limit-parked step and reports the flatly wrong cause**) AND no OTHER `in-progress-*`/`verdict-pending-*` lessons plan. If your file is absent you are reading the wrong tree → HALT (fail-closed: write nothing). Then read the **route-NOT-NULL count** and the **status distribution** **twice, with at least 5 seconds between the two reads — state the interval you actually used in the deposit**; confirm both unchanged. ⚠️ **This is a cheap sanity read, NOT the concurrency guard** — two statements issued back-to-back sample a window of milliseconds and would detect almost nothing, and an unspecified "a moment apart" invites exactly that. **The real detection is Task C1 and QA row 2.** Do not report this check as evidence of isolation; report it as what it is. ⚠️ **Do NOT use `get_unclassified_entries()` as the quiescence signal — it filters on `status`, never `route`, so a route conflict is invisible to it.** Any concurrent writer possible → HALT.
>
> **Task A0-pre — PRECONDITION: verify the targets match the disposition table BEFORE writing.** `SELECT id, entry_id, status, category, confidence, route, target_layer, target_artifact FROM lesson_proposals WHERE id BETWEEN 201 AND 206 ORDER BY id` and assert, **per row**:
> - exactly **6 rows**;
> - the id→entry_id mapping is exactly `201→193, 202→194, 203→195, 204→196, 205→197, 206→198` — ⚠️ **a contiguous mapping is easy to eyeball as correct; check each pair, because an off-by-one shifts every disposition by one row**;
> - each `status='proposed'`;
> - each `route` is **NULL or already `codify`** (idempotent re-run tolerated);
> - `target_layer='governance'`, `category='governance_rule'`, and `confidence='high'` on all 6;
> - **`target_artifact` matches the disposition table PER ROW** — `PLANNER_TEMPLATE.md` for 201/203/205, `DRAFTING_CYCLE.md` for 202/204/206.
>
> ⚠️⚠️ **AND ONE ASSERTION THIS PLAN'S PARENT DID NOT NEED — VERIFY THE `proposed` SET IS EXACTLY THESE SIX.** Run `SELECT id FROM lesson_proposals WHERE status='proposed' ORDER BY id` and assert it returns **exactly `201, 202, 203, 204, 205, 206` and nothing else.** **HALT on any seventh row.**
>
> ⚠️ **This is the load-bearing premise of the whole plan, and it is stated here because it is what LICENSES the deletions.** This plan removes four guards its parent carried — the parked-pair before-item, its Task-C consumer, its QA row, and QA row 7's reconciliation path — on the measured ground that no Gate-2-bound bystander exists. **A seventh `proposed` row makes every one of those premises false at once**, and the deleted guards are precisely what would have caught the consequences. The rest of A0-pre verifies that the six rows are *right*; this assertion verifies that they are *all there are*. **Before the write, nothing else can detect a seventh proposal** — QA row 4 would count its route only if it had one, and a `proposed` row typically does not. ⚠️ **After the write, QA rows 1 ∧ 2 DO detect one, and that is deliberate, not incidental** — see row 2, which owns the in-window half of this premise and must not be narrowed. **An earlier draft said "nothing else in either step can detect a seventh proposal", which contradicted row 2 and invited exactly the narrowing row 2 warns against.** **Fail closed: on a seventh row, HALT before any write and report the id.**
>
> ⚠️⚠️ **THE CATEGORY EXPECTATION IS UNIFORM HERE, AND THAT IS A REVERSAL FROM THIS PLAN'S PARENT — DO NOT "CORRECT" IT BACK.** 284 carried a prominent warning *not* to assert a uniform category, because two of its eight were `instrumentation`. **This batch is uniformly `governance_rule` / `high` / `governance`, measured at authoring.** ⚠️ **But verify PER ROW, never by an aggregate:** a `SELECT DISTINCT` or a `GROUP BY` returning one row proves the set is uniform and proves nothing about which id holds which `target_artifact`. The expectation is uniform; the verification is row-by-row.
>
> **HALT on genuine drift** (missing id, wrong entry_id, a status other than `proposed`, a route other than NULL/`codify`, or a category/confidence/target_artifact that disagrees with the table). ⚠️ **A disagreement here means the disposition table is describing a corpus that no longer exists — the CEO's routing decision was made against the table, so writing anyway would apply a decision to rows it was not made about.**
>
> **Task A0-snap — CAPTURE THE FOUR BEFORE-SNAPSHOTS, pre-write.** Record and KEEP, each on its OWN labelled line:
> 1. the full status distribution
> 2. the **TOTAL** route-NOT-NULL count
> 3. `get_unclassified_entries(conn)`
> 4. the **OUTSIDE-RANGE** route-NOT-NULL count — `SELECT COUNT(*) FROM lesson_proposals WHERE route IS NOT NULL AND id NOT BETWEEN 201 AND 206`
>
> 4b. **THE OUTSIDE-RANGE ROW IMAGE** — `SELECT id||':'||route FROM lesson_proposals WHERE route IS NOT NULL AND id NOT BETWEEN 201 AND 206 ORDER BY id`, as RAW output (**70 rows at authoring; report the actual**). ⚠️⚠️ **This is a VALUE-level image, and it is the only thing in the plan that can detect a route VALUE change outside the target range.**
>
> ⚠️⚠️ **RETRACTION — AN EARLIER DRAFT SAID "There is no item (4b) in this plan" AND DISMISSED 284's AS A BYSTANDER ARTEFACT. THAT WAS WRONG, AND THE PREMISE UNDER IT WAS FALSE.** The deletion rationale claimed the blast-radius question was *"carried, in full, by Task C1 / QA row 4."* **Measured against live data: it is not.** C1 and row 4 count rows where `route IS NOT NULL`; **a foreign row moving from one non-NULL route to another leaves that count unchanged.** Verified at authoring: **seven foreign rows carry non-`codify` routes (140, 141, 146, 161, 164, 169, 183); flipping any one of them to `codify` leaves the outside-range count at 70, and C1, QA row 4, B3's identity, QA row 3, QA row 2 and B1 ALL still pass.** ⚠️ **284's deleted row asserted route VALUES on the rows it watched; the count that replaced it asserts only membership.**
>
> ⚠️ **This was the one deletion premise never measured against live data — in a plan whose CEO Context quotes §2.7's per-item subtractive-trim mandate in the same paragraph, and which routes proposal 195 (verify a guard's necessity against the runtime).** The generalisation 284's 4b deserved was a row image of **everything outside the range**, not of two named bystanders. **It is restored here in that generalised form, and it also supplies the missing ANCHOR for the on-mismatch capture at C1 and QA row 4 — which previously dumped current rows with no before-image to diff against.**
>
> ⚠️⚠️ **(2) AND (4) ARE DIFFERENT MEASUREMENTS THAT ARE BOTH 70 BEFORE THE WRITE — capture and report them SEPARATELY.** They coincide only because the 6 targets are route-NULL and excluded from both. **After the write they diverge: (2) → 76, (4) stays 70.** Crossing them passes for the wrong reason on a clean run. **Authoring-measured expectations, to be confirmed not assumed:** status distribution `implemented 147, superseded 28, rejected 15, reference 7, proposed 6, stale 3` (total 206); total route-NOT-NULL **70**; `get_unclassified` `[]`; outside-range **70**. Report ACTUALS. **Do NOT re-read "before" after Task A's write.**
>
> ⚠️⚠️ **AND IF THIS IS A RESUME, SAY SO — these values are then post-partial-write and the "(pre-write)" label would be false.** A0-pre explicitly tolerates *k* targets already `codify`; in that case item (2) reads **70+k** and item (4) still 70. **Label the block `resume — snapshot taken post-partial-write`, state k, and do NOT treat the 70→70+k delta as premise drift** — Checklist #29's "a mismatch is a finding, not a number to adjust to" applies to an UNEXPLAINED mismatch, and this one is explained by A0-pre's own observation. ⚠️⚠️ **AND THE HONEST STATEMENT IS NOT "NO DOWNSTREAM CHECK BREAKS" — AN EARLIER DRAFT SAID THAT, AND IT WAS LITERALLY TRUE AND MATERIALLY FALSE.** The checks do not break; **several become VACUOUS.** On a resume the snapshot is taken from the same post-write DB the later checks read, so **rows 2, 4(a), 4(b) and 5 compare the corpus against an anchor drawn from itself and pass unconditionally** — including **4(b), the plan's only value-level guard on foreign rows**, which an earlier enumeration of this very clause omitted entirely. **B3/row 3 remain sound (same-instant identities) and row 1 remains sound (absolute).**
>
> ⚠️⚠️ **SO A RESUME MUST BE DECLARED WHERE STEP 2 CAN SEE IT — Receipt item 0b.** Record `RESUME: yes/no`, the value of *k*, and the sentence **"before-items (1), (3), (4) and (4b) are POST-WRITE; rows 2, 4(a), 4(b) and 5 are NOT independently verified on this run."** ⚠️ **Without that slot the degradation is invisible: Step 2 reads a clean eight-row table and the CEO concludes the blast radius was checked when nothing outside the range was actually compared.** **On a resume, the pre-write state is gone and no instruction in either step can recover it — the only honest remedy is to say so.**
>
> **Task A — record the six routes** with `set_proposal_route(conn, proposal_id, 'codify')` from `src.lessons_forge` (NOT hand-written SQL), for **201, 202, 203, 204, 205, 206**. **No status is written — leave all six `status='proposed'`.** **Then a SINGLE `conn.commit()`.**
>
> **⚠️ TASKS B AND C ARE READ-ONLY AND INDEPENDENT — RUN ALL OF THEM BEFORE HALTING ON ANY ONE.** No check depends on another having passed, and none can alter the DB further. **The write has already happened**, so an early halt does not protect the corpus — it only decides how much the CEO knows about a corpus that is already changed. Halting at B1 hands over "a route looks wrong" while leaving unanswered whether a status moved (B2), whether an out-of-range route changed (C1), and whether classification staled (C2) — the questions that decide whether this is a one-row anomaly or a corrupted corpus, and therefore what the recovery is. Run every check, record every measured value, THEN halt and report all findings together.
>
> ⚠️ **AND DO NOT ATTEMPT TO REPAIR ANYTHING YOU FIND — report it, do not fix it.** This is the one step holding an open read-WRITE handle, so unlike QA you *can* act on what you discover, and continuing past a detection is exactly when that becomes tempting. Do not re-run `set_proposal_route`, do not hand-write corrective SQL, do not restore from the backup, do not "tidy" a value so the next check passes. A discrepancy at B or C means a PREMISE of this plan is wrong, and a plan operating on a wrong premise must stop and be adjudicated, not self-correct. **The restore point exists for the CEO's decision, not for yours.**
>
> ⚠️⚠️ **HALT DURABILITY COMES IN TWO HALVES. READ BOTH — THEY COVER DIFFERENT POINTS IN THE STEP AND NEITHER IMPLIES THE OTHER.**
>
> **(1) A PRE-WRITE HALT DEPOSITS A SHORT NOTE AND COMMITS IT — *EXCEPT* WHEN THE TREE IS UNCONFIRMED, WHERE IT MUST WRITE NOTHING.**
>
> ⚠️⚠️ **THE EXCEPTION IS LOAD-BEARING AND IS STATED FIRST, BECAUSE THE GENERAL RULE CONTRADICTS A0-iso's "fail-closed: write nothing" AND A0-iso's CASE IS THE ONE WHERE DEPOSITING IS ACTIVELY HARMFUL** — if your own plan file is absent you are in the WRONG TREE, and a deposit written there puts the note somewhere no one will look while leaving the real tree untouched. **The discriminator is whether your own plan file was FOUND:**
> - **Tree UNCONFIRMED** — A00 derived zero matches, or A0-iso found your plan file absent → **write nothing, commit nothing, halt and report.** The gate failure IS the signal, and it is the correct one.
> - **Tree CONFIRMED** — **A0-pre found genuine drift** (A0-iso has by then positively identified your own plan file) → **deposit the note and commit it** (the deposit path is fixed by this plan, not derived from the failed derivation, so it is writable).
> - ⚠️⚠️ **A00's two-match halt is TREE UNCONFIRMED — write nothing.** An earlier draft classified it CONFIRMED on the reasoning that the deposit path is *writable*; **that answers the wrong question.** A00 runs FIRST, before A0-iso, and its guard *"asserts that exactly ONE lifecycle-prefixed file exists — it does NOT assert the file is YOURS"* — **a count of two identifies your file no better than a count of one.** A wrong tree holding two foreign plan files would take the deposit branch and commit a halt note where no one will look, **which is precisely the harm the exception exists to prevent.** **Nothing before A0-iso establishes ownership, so nothing before A0-iso may write.**
>
> **When the tree is CONFIRMED, the note is required:** ⚠️ **These are the three guards most likely to actually fire, and they sit entirely outside the post-commit rule below.** Nothing has been mutated, so there is no audit trail to preserve and **no restore is needed** — but a step that deposits nothing fails the `deposit_exists` gate and hands the CEO a bare gate failure instead of a reason. **So write a short deposit to the Step-1 path naming: which guard fired; the value it measured against the value it expected; an explicit statement that **NO DB WRITE occurred**; **`#### Files Created or Modified`** (row 0 clauses (i)/(ii) iterate it, and a receipt with no deliverable list gives them nothing to read); **the backup's absolute path and verification result IF A00 already ran** — ⚠️ **a halt at A0-iso, A0-pre or A0-snap happens AFTER A00 has written a real restore point, so "no restore point exists" is FALSE on those branches; say which case you are in**; and `Status: Partial — HALTED at <task>, <reason>` as the Receipt's opening line.** Then commit by explicit pathspec. ⚠️ **This also makes the halt legible to Step 2, whose opener is written to read a `Partial — HALTED` receipt — a pre-write halt would otherwise produce no receipt at all for it to read.**
>
> **(2) IF YOU HALT AT ANY POINT AFTER TASK A's `conn.commit()`, YOU MUST STILL DEPOSIT AND COMMIT BEFORE STOPPING.** This is the mutating step, and "report and halt" does not otherwise say *write a file*. Once the commit has landed the corpus is changed, and **the four before-snapshots exist only in your context** — the pre-write state cannot be re-measured, and **QA rows 2, 4(a), 4(b) and 5 mark themselves `❌ (unverifiable)` without them** (row 2 → item (1), row 4 → item (4), row 5 → item (3)). So before you stop: write the dev-log with **item 0 and** before-items **(1)–(4) AND (4b)** — ⚠️ **(4b), the outside-range row image, is the one that cannot be reconstructed after the fact and is the plan's only value-level guard on foreign rows** — the backup path and its verification result, whatever read-backs completed, and an explicit statement of which task halted and why; then **`git commit` by explicit pathspec**. ⚠️ **Mark any item you could not reach as `NOT CAPTURED — halted at <task>`** rather than omitting it — ⚠️ **and item (5) is the EXCEPTION: mark it PER SUB-CHECK, not wholesale.** Item (5) spans FIVE sub-checks (B1/B2/B3 + C1/C2), so one blanket marker would either discard read-backs you actually produced or claim ones you did not. Write a line per sub-check, each either its raw output or its own `NOT CAPTURED — halted at <task>`. **A halt with a committed record is a clean outcome; a halt with nothing on disk leaves a mutated production corpus with no audit trail.**
>
> **Task B — verify the ABSOLUTE post-state.**
> ⚠️ **B1 IS THE ONLY THING THAT OBSERVES THE WRITE'S EFFECT — it is not redundant with A0-pre.** Verified in code at authoring: `set_proposal_route` (`src/lessons_forge.py:256`) executes a bare `UPDATE lesson_proposals SET route = ? WHERE id = ?` and **never checks `rowcount`**, so a call naming an id that does not exist is a **SILENT NO-OP** — no exception, no signal, and the helper returns normally. (It validates the *route value* and raises on a bad one, but nothing validates the *id*.) A0-pre proves the 6 rows existed **before** the write; only B1's read-back proves the write **landed**. Neither substitutes for the other.
>
> - **B1** — read back all 6 rows: `SELECT id, entry_id, status, route, category, target_artifact FROM lesson_proposals WHERE id BETWEEN 201 AND 206 ORDER BY id`. Each `route='codify'`, each `status='proposed'`, category and target_artifact unchanged from A0-pre. **Absolute assertion — no before-anchor.**
> - **B2** — the full status distribution is **byte-identical to A0-snap item (1)**. No status moved; `proposed` is still 6. ⚠️ **This is a FLAT assertion here, and that is a deliberate strengthening over 284**, which had to split it into a hard half and a reconciled half because its row 5 blessed an in-window Gate-2 ship of two bystanders. **No such event is reachable in this corpus** (premise (ii) above), so nothing legitimate moves any status during this plan and the flat form is correct.
> - **B3** — the TOTAL route-NOT-NULL count. ⚠️ **Assert a SAME-INSTANT IDENTITY, not a delta and not a cross-time comparison: measure both counts NOW and assert total == outside-range(now) + 6.** ⚠️⚠️ **"Same-instant" must be a MECHANISM, not an intention — compute BOTH counts in ONE statement**, e.g. `SELECT (SELECT COUNT(*) FROM lesson_proposals WHERE route IS NOT NULL), (SELECT COUNT(*) FROM lesson_proposals WHERE route IS NOT NULL AND id NOT BETWEEN 201 AND 206);` — **two separate `sqlite3` invocations are two separate read transactions and can straddle a concurrent writer**, which is exactly the condition the identity is meant to be immune to. One statement, one snapshot. ⚠️ **Adjudicate on the IDENTITY, never on a literal** — at authoring the identity resolves to `76 == 70 + 6`, recorded as *illustrative only*. A foreign routed proposal arriving before dispatch raises both sides equally and the identity still holds, so an agent checking for the literal 76 would false-HALT a correct corpus. Report the actuals. Anchoring to before-item (4) mixes a now-value with a then-value, so an externally-added out-of-range route would fail this check for a reason that belongs to Task C1. Same-instant keeps the fault localized: B3 answers "did exactly my 6 land?", C1 answers "did anything outside move?" This is resume-invariant, because after this write all 6 targets are non-NULL regardless of how many already were. **A delta form — "before-item (2) + 6" — is WRONG on the exact resume A0-pre explicitly tolerates.** **Also report the rise over item (2) as a measured value — it must be ≤ 6** — but adjudicate on the identity.
>
> **Task C — confirm the blast radius.** Measure and report actuals (do NOT hardcode):
> - **C1** — `SELECT COUNT(*) FROM lesson_proposals WHERE route IS NOT NULL AND id NOT BETWEEN 201 AND 206` equals **A0-snap before-item (4)** — the outside-range count, **NOT item (2), the total** (equal before the write, divergent after) — and is **UNCHANGED**. ⚠️ **Adjudicate against before-item (4) as captured this run, not against a literal**; the authoring measurement **70** is *illustrative*. Report the actual. The 6 targets were route-NULL and already excluded from item (4); this count does **NOT** rise by 6. Any change means a route outside 201–206 moved → HALT. ⚠️ **With the bystander row deleted, C1 is now the SOLE outside-range guard** — it carries the whole blast-radius question alone, which is why it is absolute and unadjudicatable.
>
>    ⚠️⚠️ **C1 HAS TWO PARTS. THE COUNT IS NOT SUFFICIENT ON ITS OWN.**
>    **C1(a) — the count**, as above, equal to before-item (4).
>    **C1(b) — the ROW IMAGE, byte-identical to before-item (4b).** Re-run `SELECT id||':'||route FROM lesson_proposals WHERE route IS NOT NULL AND id NOT BETWEEN 201 AND 206 ORDER BY id` and compare to the captured image. ⚠️⚠️ **(a) CANNOT SEE A ROUTE VALUE CHANGE — a foreign row moving between two non-NULL routes leaves the count identical, and seven such rows exist. (b) is the only check in this plan that detects it.** Any difference → HALT.
>
>    **On a mismatch of either part, DIFF the two images and deposit the differing lines** — before-item (4b) against the image you just took — RAW output into the deposit. ⚠️ **This check reports a COUNT, and a count cannot say WHICH row moved.** 284 had a second row naming its parked pair directly; this plan deleted it because no bystander exists, and that deletion is correct — **but it removed the only thing that could identify a specific drifted id.** ⚠️⚠️ **This capture is mandated at BOTH C1 and QA row 4, deliberately.** `pause_for_verdict: always` means a C1 halt reaches the CEO **before Step 2 ever runs** — so the mutating step's halt path is the one where a bare integer is least useful, and it is the path an earlier draft left uncovered while fixing the later, non-mutating one. **Checklist #26: weight the sweep toward the step that MUTATES.**
> - **C2** — `get_unclassified_entries(conn)` UNCHANGED from A0-snap item (3) (expected `[]`; a delta signals an unrelated staling → HALT).
>
> **Deposit:** `knowledge/development/gate-1-route-session-13-14-captures-2026-07-31.md` — the six `set_proposal_route` calls made, and the read-back of all 6 rows as **RAW command output, not a summary**, plus an Output Receipt.
>
> **The Output Receipt MUST OPEN with an explicit status line from this CLOSED SET:** `Status: Complete` (every task ran) · `Status: Partial — HALTED at <task>, <reason>` (you stopped). **The first is the only proceed-value.** Step 2's first action reads this line. A halted status is a legitimate value — do NOT write `Complete` to make the step look tidy. It MUST then carry each of the following on its OWN labelled line — **QA rows 2, 4(a), 4(b) and 5 mark themselves `❌ (unverifiable)` if before-items (1), (4) and (3) respectively are not reported separately, so a summarized "snapshots captured" is a plan-halting deposit:**
> 0. **A0-pre's SET-IDENTITY assertion** — the raw output of `SELECT id FROM lesson_proposals WHERE status='proposed' ORDER BY id`, taken pre-write. ⚠️⚠️ **This item is numbered 0 rather than inserted into the snapshot sequence because it comes from A0-pre, not A0-snap, and because renumbering items (1)–(8) would break every downstream reference to them.** ⚠️ **It exists because the set-identity premise LICENSES this plan's four guard deletions, and without a Receipt slot nothing downstream can tell whether A0-pre ran at all.** A Step 1 that skipped A0-pre entirely and happened to write correctly would otherwise close clean — the same unauditable-precondition shape row 0(iv) exists to prevent for the POST-write checks. **Row 0(iv) reads this item.**
> 0b. **RESUME DECLARATION** — `RESUME: yes/no`; if yes, the value of *k* (targets already `codify` at A0-pre) and the verbatim sentence **"before-items (1), (3), (4) and (4b) are POST-WRITE; rows 2, 4(a), 4(b) and 5 are NOT independently verified on this run."** ⚠️ **Its reader is the clause immediately above Step 2's `## Verification Table`, which governs rows 2, 4(a), 4(b) and 5 — stated once there rather than copied into four rows.** ⚠️⚠️ **An earlier draft asserted here that those rows "READ THIS ITEM FIRST" while NO row mentioned item 0b anywhere — a mandated capture with no reader, which is CL2, in the fold that was fixing the resume gap.** **Numbered 0b so items 1–8 keep their existing references.**
> 1. before-item **(1)** — the full status distribution (pre-write)
> 2. before-item **(2)** — the **TOTAL** route-NOT-NULL count (pre-write)
> 3. before-item **(3)** — `get_unclassified_entries(conn)` (pre-write)
> 4. before-item **(4)** — the **OUTSIDE-RANGE** route-NOT-NULL count (pre-write) ⚠️ **distinct from (2) even though both read the same number at authoring**
> 4b. before-item **(4b)** — the **OUTSIDE-RANGE ROW IMAGE** (`id:route` per row, pre-write), as RAW output. ⚠️⚠️ **QA row 4(b) marks itself `❌ (unverifiable)` without this, and it is the ONLY value-level guard on rows outside the target range** — the count in item (4) is blind to a foreign row moving between two non-NULL routes. **It is also the anchor the on-mismatch diff at C1 and QA row 4 needs; without it those captures dump current rows against nothing.**
> 5. the after-values for B1 / B2 / B3 and C1 / C2, **each labelled with its anchor**: B2 → before-item (1); B3 → same-instant identity; C1 → before-item **(4)**; C2 → before-item (3). **B1 is an ABSOLUTE assertion of final state — label it "absolute (no before-anchor)".** Do not invent a before-item for it.
> 6. the absolute backup path **plus its verification result** — the `PRAGMA integrity_check` output and the backup-vs-live count match, as RAW output. ⚠️ **The restore point is a belt-and-braces artefact, NOT this plan's recovery instrument.**
>
>    ⚠️⚠️ **RECOVERY FOR THIS PLAN IS SURGICAL, and that makes whole-DB-restore machinery unearned (Rule 56: resume machinery is justified only when the interrupted work is NOT reproducible).** The entire mutation is `route='codify'` on six known ids, so its inverse is one statement: **`UPDATE lesson_proposals SET route = NULL WHERE id BETWEEN 201 AND 206`.** ⚠️ **And note this plan's corpus makes the case STRONGER than 284's:** 284 had to forbid a whole-DB restore because one taken after an in-window CEO Gate-2 disposition would revert it. **Here no in-window disposition is reachable**, so the restore is merely *unnecessary* rather than *hazardous* — but it stays unused either way, because the surgical UPDATE is exact and a whole-DB restore is not.
>
>    ⚠️ **On a resume, record BOTH backup paths** — this run's and any earlier one's. ⚠️⚠️ **RANK BY SIZE AND CONTENT, NOT BY TIMESTAMP.** An earlier draft said *"the EARLIER UTC stamp is the pre-mutation snapshot (the 0-byte corpse having been `rm`ed at A00)"* — **the `rm` was struck at A00 because it would have destroyed the backup, and this ranking silently kept depending on it.** ⚠️ **On the reachable path where two files exist under this plan id — run 1 hit the sidecar-less branch, wrote a 0-BYTE file, and died before its re-run; run 2 wrote a good backup at a LATER stamp — the earlier-stamp rule names the EMPTY file as the restore point.** **So: `ls -la` every candidate, DISCARD any with size 0, and among the survivors take the earliest.** State which you discarded. do not rebuild selection machinery — the recovery is the surgical UPDATE above, not a file.
> 7. **`#### Files Created or Modified`** — every **git-tracked** file this step wrote, by path (for this step: the single deposit). ⚠️⚠️ **Do NOT list the backup `.db` or the canonical DB here.** `.gitignore` matches `*.db`, so for those `git status --porcelain` is empty (passes) while `git log -1` is empty (never committed → fails row 0's "exists on disk AND is committed"), and row 0's repair narrowing cannot reach a path that is not a declared deposit — a literal reading would manufacture a Critical `❌` on a clean run. **The backup is recorded under item 6, and row 0's clause (iii) is its reader.** ⚠️ Rule 17's literal string is **`Files Created or Modified (Code)`**; the `(Code)` suffix is deliberately dropped here because this plan's deliverables are a markdown deposit and a database mutation, not code, and row 0 greps the un-suffixed heading. **Declared so the divergence from the template's literal string is not read as an error.**
> 8. any flags **or HALT conditions encountered** — including a condition you hit and cleared, which the `Status:` line alone cannot record
>
> ⚠️ **Every measured value is deposited as RAW COMMAND OUTPUT, not a summary or a re-typed figure.** "Raw" means the output is PRESENT, not that it is unlabelled — annotate freely, but the annotation accompanies the raw output, it never replaces it. **QA rows 2, 4(a), 4(b) and 5 ADJUDICATE against these values — 4(b) against the row image, which is the only value-level guard on foreign rows; row 3 REPORTS against item (2) without adjudicating on it; and row 0(iv)/(iv-b) read items 0 and 8 — the pre-write state no longer exists to re-measure.**
>
> Canonical Python file-write — no heredoc. Commit by explicit pathspec. `#### Prompt Feedback` in `### Ledger Updates`.
>
> **Deposits:**
> - `lessons-forge/knowledge/development/gate-1-route-session-13-14-captures-2026-07-31.md`

---
---

## STEP 2 — QA

---

> **Before starting, read the Step 1 deposit — `knowledge/development/gate-1-route-session-13-14-captures-2026-07-31.md`, project-relative to YOUR OWN working tree (the `lessons-forge/…` form in the Deposits block is root-joined; do not prepend it here — the plan-225 trap) — and confirm its Output Receipt opening status line reads `Status: Complete`. Anything else — including `Status: Partial — HALTED at …` — stops this step.** ⚠️⚠️ **"Stops" does NOT mean "writes nothing" — it is governed by the HALT-MUST-LEAVE-A-COMMITTED-RECORD rule below, and the two must not be read as contradicting each other.** Produce and commit the QA report anyway, stating the halted Step-1 status and marking as `❌ (Step 1 halted — not verifiable)` **only the rows whose anchors genuinely do not exist — rows 1, 2, 3, 4(a), 4(b) and 5.** ⚠️⚠️ **RUN EVERYTHING ELSE FOR REAL: row 0 clauses (i), (ii), (iii) and (v), row 6, and row 7.** On a **pre-write** halt the corpus is untouched and every one of those is fully executable — **row 0(i)/(ii)/(iii) read the deliverable list and backup record Step 1 was specifically instructed to deposit for them; row 6 runs the suite and the `src/` check; row 7 is the ONLY guard on the three root doctrine files.** ⚠️ **An earlier draft marked "every verification row" `❌`, which (a) put false statements in the CEO's report, (b) discarded the doctrine guard on the very run the CEO authorised to obtain a record, and (c) had Step 1 producing a deliverable list for a reader ordered not to read it — a direct CL10 violation.** **Run (v) and record its real result; the Rule 46 obligation it guards is owed on a halted run exactly as on a clean one.** ⚠️ **Stated because row 0 mixes two deliverable sets, and a blanket "every row" would falsely fail the plan's declared primary Rule 46 check on the very run where the CEO most needs the register populated.** ⚠️⚠️ **AND STATE WHICH HALT CLASS IT WAS, IN ONE LINE AT THE TOP, BECAUSE THE TWO MEAN OPPOSITE THINGS ABOUT THE CORPUS AND PRODUCE AN IDENTICAL ALL-`❌` TABLE.** Read Step 1's note: **a PRE-WRITE halt (a guard fired before Task A) means the corpus is UNMUTATED and no restore is owed** — say so plainly, or a table of eight `❌`s reads as catastrophe when nothing was touched. **A POST-COMMIT halt means the six routes DID land and the corpus is changed.** ⚠️ **The row verdicts are the same either way; the CEO's decision is not.** ⚠️ **Note the sequencing that makes this coherent: the header declares `pause_for_verdict: always`, so a halted Step 1 reached the CEO BEFORE you were dispatched — if you are running at all, the CEO has seen the halt and authorised a continue.** They authorised it to obtain a record; stopping with zero deposits hands them a bare `deposit_exists` gate failure instead, which is strictly less than they asked for. ⚠️ **An ALLOWLIST, not a prefix match.** ⚠️ **This is a DELIBERATE NARROWING of `PLANNER_TEMPLATE.md:433`, declared so you do not "correct" it back:** the template permits proceeding on *"Complete or Partial with known acceptable scope"*, but every halt condition in Step 1 concerns whether the write landed correctly, and this step's whole job is certifying that write.
>
> Post a short visible chat message confirming you are starting Step 2 (QA). You are Lessons Forge QA (`agents/FORGE_LESSONS_AGENT.md`). Same absolute-DB rule. **Verification and reporting only — no product-code changes, and no DB writes; if a check fails, report it, do NOT fix it.** ⚠️ **"No DB writes" means NO ROW MUTATION AND NO SCHEMA CHANGE** — a read-only handle is the mandate because it makes that structural, but **creating WAL sidecars by opening a connection is not a row mutation**, and row 5 defines the one narrow fallback where that is permitted and must be recorded. **Stated here as well as at row 5 so the step's own top-level declaration and its single exception do not read as contradicting each other.** Do NOT use Monitor. Do NOT edit PROJECT_STATUS directly.
>
> **MANDATORY — Rule 20 self-check (canonical block, Checklist #4 — the exact template with all four placeholders filled, NOT a paraphrase).** ⚠️ **Form selection:** a **DB-mutating plan with a real pytest suite** → the **full canonical block with real evidence files**, not a simple banner. Run the block from `/Users/marklehn/Developer/GitHub/RULE_20_SELF_CHECK_BLOCK.md` (ABSOLUTE path — it lives at the governance root, not in the lessons-forge worktree). Fill ONLY the four placeholders:
> - `plan_slug`: `gate-1-route-session-13-14-captures-2026-07-31`
> - `qa_report_path`: `<your-own-tree-abs>/knowledge/qa/gate-1-route-qa-2026-07-31.md`
> - `evidence_dir`: `<your-own-tree-abs>/knowledge/qa/evidence/gate-1-route-session-13-14-captures-2026-07-31/` — **derive from `pwd` (your OWN working tree), NOT a hardcoded main-tree path** (the plan-225 trap).
> - `required_evidence_files`: the two entries `"db-invariants.txt"` and `"pytest_targeted.txt"` — ⚠️ **Python string literals, QUOTED.** Unquoted they raise `NameError` before any check runs, and the gate then reports "no Rule 20 banner" — a misleading cause for a transcription slip. ⚠️ **[EXECUTED HERE — 2026-07-31] This placeholder is a THREE-LINE structure, not one line:** the block defines `required_evidence_files = [`, then a `# PLACEHOLDER —` comment line, then `]`. **The placeholder IS the inner comment line — replace only it, and leave both bracket lines intact.** The file's own instruction is *"Replace ONLY the four placeholder lines marked `# PLACEHOLDER —`. Do not modify any other line"*, so collapsing all three onto one line violates the contract of the artifact you are copying.
>
> ⚠️⚠️ **RUN THE BLOCK LAST — AFTER EVERY OTHER LINE OF THE REPORT IS WRITTEN, INCLUDING ITS OWN STDOUT PLACEHOLDER, THE `❌`-NAMING LINE, AND ROW 0's DELIVERABLE SUB-TABLE.** **[EXECUTED HERE — 2026-07-31]:** a block run that PASSES, followed by the plan's own mandated additions, then re-run → **FAILED, CRITICAL: a hedging keyword in a positive-status row** — because those additions are exactly where `assumed` / `inferred` / `not run` live. ⚠️ **And the gate would not catch it: `gates.py` greps only for the banner and the PASSED line, so a report carrying a stale `PASSED` from before its own final edits passes.** **This is CL7 — a recorded tool result is valid only for the version it ran against — applied to the Rule 20 block, which an earlier draft swept only to `plan_lint`.** **Write everything, THEN run the block, THEN paste its stdout as the last edit; if pasting the stdout is itself the last edit, re-run once more and confirm the verdict is unchanged.**
>
> Deposit both evidence files BEFORE running the block — it `sys.exit(1)`s if either is missing or empty. **Include the block's literal stdout in the QA report; the banner `## Rule 20 — QA Self-Check Results` and the line `PASSED — SELF-CHECK PASSED` must appear VERBATIM** — including the em-dash (—, U+2014, not a hyphen) — **byte-enforced by `bellows/gates.py::_gate_rule_20_self_check`.**
>
> ⚠️ **Those two strings are quoted HERE, in an instruction, and deliberately NOT in the Cycle Log.** `plan_lint` check (c) scans the whole plan for both literals and FAILs without them — measured at authoring — while proposal 206, which this plan routes, forbids gate-matching strings in the **Cycle Log** specifically, because the final step's gate span absorbs the Log and evaluates a quoted string as if the step had said it. **A step prompt is an instruction; the Log is a record. Only the record is governed by 206.** Do not "apply 206" by deleting these. **If the block prints a failure, do not proceed with closure — halt and report to CEO.** ⚠️ **Writing the verification table does NOT satisfy this gate** — the gate reads the deposited report for the banner and PASSED line only. End with a self-grep confirming the banner.
>
> **⚠️ WHAT THE BLOCK DOES AND DOES NOT VERIFY.** It reads **evidence-file presence and hedging keywords only. It cannot see your verification verdicts.** A table with a genuine `❌` and both evidence files present still prints the PASSED line and exits 0. So **expect a pass even on an honest halt** — the gate that catches a failure is `gates.py::_gate_rule_22_verification`. **You must NOT flip a row to ✅, soften its wording, drop it, or re-scope a check to make the block pass.** If any row is `❌`, add one line under the stdout naming EVERY `❌` row and stating the step is expected to fail at the rule_22 gate.
>
> **⚠️ Rule 19 — REQUIRED VERBATIM IN THIS PROMPT** (`PLANNER_TEMPLATE.md:561`): *"If you cannot complete a check, mark it ❌ with a reason. Do NOT mark it ✅ and explain why you couldn't verify. Any ✅ row containing hedging keywords will auto-fail during the self-check in Rule 20."*
>
> ⚠️ **Read that mandate BEFORE what follows.** Hedging language in a ✅ row signals a check that was not performed. The guidance below teaches you to avoid tripping the scanner **with honest measurements** — it does NOT license rewording a check you did not run. **A check you did not execute is `❌`, whatever words you use.**
>
> **⚠️⚠️ DO NOT QUOTE THE HEDGING-KEYWORD LIST — OR THIS PLAN'S PROSE — INTO ANY LINE CONTAINING A `|`.** The block detects a positive-status row by cell equality but scans it for hedging by **whole-line substring**, so any `|`-bearing line pairing a positive token with a hedging keyword — even as a quotation — emits CRITICAL and `sys.exit(1)`. Keep table cells SHORT; put narrative below the table.
>
> ⚠️⚠️ **A HEDGING KEYWORD IS FATAL EVEN AS A MEASURED VALUE, including a ZERO count.** The word for a not-run test is on the keyword list, so the natural pytest summary cell CRITICALs a clean run. **Write row 6's value as `<N> passed` and nothing else** — measure `<N>`; Planner's measured collection at authoring: **55**; report the actual.
>
> ⚠️⚠️ **THE FULL LIST, MEASURED AT AUTHORING FROM `RULE_20_SELF_CHECK_BLOCK.md`, BECAUSE SEVERAL ENTRIES ARE LIVE HAZARDS FOR THIS PLAN'S OWN HONEST PROSE:** `pending`, `inferred`, `extrapolated`, `estimated`, `approximate`, `skipped`, `assumed`, `close enough`, `should pass`, `would pass`, `not run`. ⚠️ **Note what that means here specifically: rows 2, 4 and 5 each have a fail-closed clause whose natural wording reaches for "assumed", "inferred" or "not run", and row 4's `❌ (unverifiable)` narrative is exactly where those words appear.** Any of them on a `|`-bearing line that also carries a positive token is a CRITICAL `sys.exit(1)`. **The mitigation is the one already stated — keep table cells short and put every narrative sentence BELOW the table** — but it only works if you know the whole list, so it is given in full rather than by reference.
>
> **⚠️ On a FAILED block run, route its raw stdout to `db-invariants.txt` and describe the failure in the report WITHOUT reproducing the offending row.** The block echoes the offending line verbatim; pasting that into the report makes the echo re-trip the scan permanently, foreclosing Rule 20's own recovery option (a). **This is a declared, narrow deviation on the FAILING path only** — on a PASSING run paste the stdout as Rule 20 mandates. **This step takes EXACTLY THREE declared deviations, enumerated here so the list is CLOSED and CHECKABLE:** (i) the `Status: Complete`-only allowlist, a declared narrowing of `PLANNER_TEMPLATE.md:433`; (ii) **this one** — Rule 20's unconditional "include the literal stdout in the QA report", narrowed on the FAILING path only; and (iii) row 0's narrowing of Rule 17's repair permission to RE-COMMITTING a declared deposit path. **No other rule may be narrowed on any reasoning.**
>
> **⚠️⚠️ STEP-WIDE SEARCH RULE — applies to EVERY text search in this step, not only the one in row 4.** Measured on this machine at authoring: **`grep` is a `ugrep` shim** (`grep --version` → `ugrep 7.5.0`). ⚠️⚠️ **THE EXIT CODE IS NOT A SAFE DISCRIMINATOR — AN EARLIER DRAFT'S RULE WAS INVERTED FOR THE EXACT PATTERN CLASS IT NAMED, AND IS CORRECTED HERE.** **[EXECUTED HERE — 2026-07-31]** against a file where the searched line **is present**:

| command | result |
|---|---|
| `grep 'before-item **(4)**' <file>` | **exit 1, prints NOTHING** — the line is there |
| `grep -F 'before-item **(4)**' <file>` | exit 0, line returned |
| `grep -E 'before-item **(4)**' <file>` | exit 0, **but also matches `(4b)`** |

⚠️ **So a `**`-bearing search WITHOUT `-F` fails as a SILENT EXIT 1 — the very code an earlier draft whitelisted as "genuinely absent".** An agent applying that rule would report a present anchor as missing and mark a row `❌` against a correct receipt. The loud case (exit 2, a true regex-syntax abort) is the one that already announces itself on stderr. **And `-E` is not a safe fallback: it over-matches, silently confirming `(4b)` when you searched for `(4)`.**

**THE RULE, corrected:** **`-F` is MANDATORY for every literal-string search — it is not a preference and there is no fallback.** Run `grep -F '<literal>' <file>; echo "GREP-EXIT=$?"`. **A search not run with `-F` is INVALID EVIDENCE regardless of its exit code — do not record its result at all, re-run it with `-F`.** With `-F`: exit 0 = found; exit 1 = genuinely absent; any other exit = tooling failure. ⚠️ **Never substitute `-E` or a bare pattern.** ⚠️ **This governs row 0(v)'s heading check, row 4's receipt-line search, row 6's pytest-summary grep, and the evidence-file self-greps.** ⚠️ **This defect class is proposal 204, which this plan routes** — a command whose failure output and zero-result output are the same bytes.
>
> **Evidence-source rule.** Every SQL row states which DB it ran against (canonical = `sqlite3 "file:/Users/marklehn/Developer/GitHub/lessons-forge/lessons-forge.db?mode=ro" "<query>"`). ⚠️ **`?mode=ro` NON-HALT FALLBACK:** the DB is WAL-mode and a read-only handle needs its `-shm`/`-wal` sidecars; their absence yields `Error: unable to open database file` on a perfectly healthy DB. **On that specific error, re-run the same command against the same absolute path WITHOUT the `?mode=ro` URI, note that you did, and continue** — only a failure of the fallback is a real failure. Deposit **RAW command output, never a summary of it**.
>
> **Scope:**
> - `knowledge/qa/gate-1-route-qa-2026-07-31.md`
> - `knowledge/qa/evidence/gate-1-route-session-13-14-captures-2026-07-31/db-invariants.txt`
> - `knowledge/qa/evidence/gate-1-route-session-13-14-captures-2026-07-31/pytest_targeted.txt`
>
> ⚠️ **The verification table MUST sit under a top-level `## ` heading containing "Verification"** — write exactly `## Verification Table`, with columns `| # | Claim | Status (✅/❌) | Measured value | DB source | Evidence |` (**the Evidence column is mandated by Rule 18 `:558` — cite the evidence file BY PATH**; Rule 17 `:555` fixes the `Status (✅/❌)` spelling). `gates.py:657` tracks sections via `startswith("## ")` only, so a `###` heading puts the table OUTSIDE the scanned region and an `❌` would go undetected.
>
> ⚠️⚠️ **AND THE SECTION NEVER CLOSES — close it yourself.** `in_verification_section` is never cleared by `###`/`####`, so it runs to end-of-file and every later `|`-bearing block is graded as if it were the verification table. **Immediately after the table, write exactly `## Evidence and Narrative`**, and keep the Rule 20 stdout, the Output Receipt and `### Ledger Updates` under `## `-level headings.
>
> ⚠️ **Use ONLY `✅` or `❌` — no third value.** A table where no row carries a positive token and none carries `❌` produces **zero** failures at BOTH gates; a table of hedged non-verdicts passes silently. If an outcome feels like neither, it IS an `❌`.
>
> **⚠️ A FAILING ROW DOES NOT LICENSE SKIPPING THE REST.** This step has **eight checks — rows 0, 1, 2, 3, 4, 5, 6, 7** — mutually independent. **Run all eight, record every measured value, and halt only after the table is complete.** Inside a row, "→ halt" means *mark it `❌`, record why, and keep going*. **A HALT MUST STILL LEAVE A COMMITTED RECORD:** finish the report, write the evidence files, and commit whatever deposits exist by explicit pathspec.
>
> ⚠️⚠️ **READ RECEIPT ITEM 0b BEFORE YOU ADJUDICATE ROWS 2, 4(a), 4(b) OR 5 — THIS IS THE READER FOR THAT ITEM, STATED ONCE HERE RATHER THAN COPIED INTO FOUR ROWS THAT WOULD THEN DRIFT.** If Step 1's Receipt declares `RESUME: yes`, its before-items (1), (3), (4) and (4b) were captured **AFTER** the write, so those four rows compare the corpus against an anchor drawn from itself and **cannot fail**. ⚠️⚠️ **ON `RESUME: yes` THE STATUS CELL STAYS EXACTLY `✅` — THE VACUITY GOES IN THE *MEASURED VALUE* COLUMN AS `vacuous — post-write anchor`, PLUS ONE LINE BELOW THE TABLE.** ⚠️ **An earlier draft of this clause said to write `✅ (vacuous — post-write anchor)` INTO THE STATUS CELL. That would have been a silent pass:** the Rule 20 block's `is_positive_row` matches positive tokens **by CELL EQUALITY, not substring**, so an annotated cell is **not** a positive row — it escapes the hedging scan entirely — while carrying no `❌` for `rule_22` either. **The row would be neither passing nor failing and both gates would ignore it**, which is the hedged-non-verdict failure this step warns about and a direct breach of its own **"Use ONLY `✅` or `❌` — no third value."** ⚠️ **Keep the annotation OUT of the Status column, and keep every hedging keyword from the list given earlier in this step out of any table row — write "the anchor post-dates the write, so this row did not independently verify the corpus" instead.** ⚠️ **The keywords are described here rather than repeated, per the discipline this plan routes: an earlier draft of this very sentence QUOTED two of them on a row-shaped line, making the warning an instance of the thing it warns against.** **If the Receipt carries no item 0b at all, treat it as an undeclared resume and mark those four rows `❌ (unverifiable)`** — the same fail-closed rule their anchors already carry. ⚠️ **Rows 1, 3, 6 and 7 are unaffected: row 1 is absolute, row 3 is a same-instant identity, and rows 6/7 read neither the DB anchors nor Step 1.**
>
> ## Verification Table
>
> 0. **Deliverable verification (Rule 17 — MANDATED in the final step, BEFORE the regression check).** ⚠️ **This row's scope is BOTH deliverable sets: (A) every file Step 1's Receipt lists, and (B) the mandated blocks of THIS step's own report.** Clauses (i)–(iv) cover (A); clause (v) covers (B). **Stated explicitly because a row that reads "Step 1's Receipt" in its opening and then checks this step's own output is a scope contradiction an agent resolves by dropping the clause it did not expect.** Read Step 1's Receipt `#### Files Created or Modified` section and verify **EVERY** listed deliverable — **not sampling.** For each: confirm it exists on disk and is committed **in its CURRENT state — two commands, both required**: `git log --oneline -1 -- <path>` **AND** `git status --porcelain -- <path>` which must be EMPTY.
>
>    ⚠️⚠️ **USE THE PROJECT-RELATIVE PATH, NOT THE ROOT-JOINED `lessons-forge/…` FORM THE RECEIPT LISTS — THIS IS THE PLAN-225 TRAP, AND IT IS SILENT HERE.** **[EXECUTED HERE — 2026-07-31]** from inside the submodule: `git log --oneline -1 -- lessons-forge/knowledge/qa/<file>` returns **empty with exit 0**, and `git status --porcelain` on the same wrong path **also returns empty**. ⚠️ **So the two halves disagree with no error and no non-zero exit: `git log` reads as "never committed" → Critical `❌`, while `git status` reads as PASS — on a perfectly committed deposit.** **Strip the `lessons-forge/` prefix before running either command**, and if `git log` comes back empty, **re-run with the stripped path before concluding anything**. The Step-2 opener carries this warning for the deposit it reads; it was not swept here. ⚠️ **`git log` alone is insufficient** — a file committed then modified returns its old commit, so the check passes while the content is uncommitted. **Any ❌ here is a Critical finding that blocks Done.** ⚠️ Rule 17 permits "re-create the file"; **this plan narrows that to RE-COMMITTING a deposit path it already declares.**
>
>    ⚠️⚠️ **AND CHECK THREE THINGS NO OTHER ROW COVERS:**
>    - **(iii)** the Receipt records the **absolute path of the restore point AND its verification result** (`PRAGMA integrity_check` + the backup-vs-live count match).
>    - **(iv)** the Receipt contains **item 0 (A0-pre's set-identity assertion) AND Step 1's own B1/B2/B3 and C1/C2 read-backs**. ⚠️ **Item 0 is checked here because it is the PRECONDITION half of the same auditability problem:** without it, a Step 1 that never verified the `proposed` set is exactly the six — the premise licensing this plan's four guard deletions — is indistinguishable from one that did. ⚠️ **Rows 1–5 re-measure CURRENT DB state, so they prove the OUTCOME is right but cannot show Step 1 verified its own write.** Without (iv), a Step 1 that skipped Tasks B and C entirely and happened to write correctly closes clean, and Step 1's "run all of B and C before halting" bound becomes unauditable. **You are checking PRESENCE, not values — the values are rows 1–5's job.**
>    - **(iv-b)** the Receipt's **item 8** — flags and HALT conditions encountered, including any condition hit and cleared. ⚠️ **Presence check only, and it exists because item 8 otherwise had NO downstream reader at all** — a mandated deposit with no reader is the defect shape this plan flags in three other places. **A Receipt whose item 8 is absent is incomplete even when every other item is present**, because a condition that was hit and cleared is invisible to the `Status:` line by construction.
>    - **(v)** ⚠️ **THIS STEP's own `### Ledger Updates` carries a populated `#### Forward Register` block** naming the `gates.py:449` span defect and the three `plan_lint` items (see the Ledger Updates instruction below). ⚠️⚠️ **ORDERING — TWO MANDATES BOTH CLAIMED "LAST", AND CL7 MAKES THE LOSER'S RESULT STALE. THE RESOLUTION IS FIXED HERE, NOT LEFT TO THE AGENT.** **(1)** finish every other line of the report; **(2)** run row 0(v)'s greps and write their results in; **(3)** run the Rule 20 block; **(4)** paste its stdout as the FINAL edit; **(5)** re-run the block once and confirm the verdict is unchanged, recording that you did. ⚠️ **Row 0(v) goes BEFORE the block because its output is report text the block must scan; the block goes last because it is the thing whose verdict must describe the shipped file.** **Check row 0(v) MECHANICALLY, at step (2):** `grep -F '#### Forward Register' <your qa report path>; echo "GREP-EXIT=$?"` must exit 0, **and** the block must carry **six** item lines — count them and record the count. ⚠️ **A self-check on your own output is weak by construction; a mechanical one is what makes it worth anything.**
>
>      ⚠️⚠️ **AND THIS CLAUSE'S JOB IS EXACTLY AND ONLY "THE BLOCK IS IN THE DEPOSIT" — SAID PLAINLY, BECAUSE TWO EARLIER DRAFTS CLAIMED MORE.** Verified at authoring: **plan 288 mandated this block and its QA agent DID emit it** (`lessons-forge/knowledge/qa/cycle-qa-2026-07-30.md:152`, three correctly formatted items). **[EXECUTED HERE — 2026-07-31]** the daemon then resolved the append destination to `lessons-forge/knowledge/FORWARD.md`, **which does not exist**, logged `no FORWARD.md in project, skipping forward append`, and returned — **exactly as `bellows.py:1417` is written.** ⚠️ **So no row was ever going to appear anywhere, `bellows/knowledge/FORWARD.md` was never the destination, and there is no daemon defect to detect.** **This clause therefore verifies the block reached the deposit and claims NOTHING about delivery — which is now the whole and accurate truth of what it does.** ⚠️ **Recording a `✅` here means the block is present; it has never meant a register row exists, and no check in this plan asserts one.**
>
>    ⚠️ **RECORD THIS ROW IN RULE 17's MANDATED FORM.** Rule 17 (`:555`) requires *"Output as a verification table: `| Deliverable | Expected | Status (✅/❌) | Evidence |`. This is not sampling — every listed deliverable is checked."* One row of the main table cannot give "every deliverable" a structural home. **So: keep this as row 0 of the main table, and put its evidence under `## Evidence and Narrative` as a `| Deliverable | Expected | Status (✅/❌) | Evidence |` sub-table with ONE ROW PER FILE listed in Step 1's Receipt item 7.** ⚠️ That sub-table is `|`-bearing prose outside the verification section — keep its cells short and free of hedging keywords. ⚠️ **A `NOT CAPTURED — halted at <task>` marker is recorded verbatim as this row's evidence but does NOT satisfy the clause** — the record IS incomplete, and an `❌` the CEO expects is an accurate report, not a false failure.
> 1. **All 6 routed.** `SELECT id, entry_id, status, route, category, confidence, target_artifact FROM lesson_proposals WHERE id BETWEEN 201 AND 206 ORDER BY id` — 6 rows, **each `confidence='high'`** (the Rule 46 carve-out premise, re-verified at run time), each `route='codify'`, each `status='proposed'`, each `category='governance_rule'`, and each `target_artifact` matching the plan's disposition table **per row** (`PLANNER_TEMPLATE.md` for 201/203/205, `DRAFTING_CYCLE.md` for 202/204/206). ⚠️ **Verify per row, not by an aggregate** — a uniform-set query proves uniformity and says nothing about which id holds which artifact. **Also assert the id→entry_id mapping** (`201→193 … 206→198`): it is already in the result set, a route write cannot alter it, so any drift means foreign corruption — which is precisely a class no other row covers. Absolute assertion.
>
>    ⚠️⚠️ **THIS ROW IS HALF OF THE SET-IDENTITY GUARANTEE — DO NOT WEAKEN IT WITHOUT READING ROW 2.** Row 2 confirms the `proposed` count is still 6; **this row confirms all six of those are THIS plan's targets.** Together — count == 6 **and** the six targets all `proposed` — they prove **the proposed set is exactly the six**, which is the premise licensing this plan's four guard deletions at Step-2 time. ⚠️ **Neither row proves it alone**, and row 2's note says so from the other side.
> 2. **No status moved — a FLAT assertion.** The full distribution is **byte-identical to Step 1's before-item (1)**, and all 6 targets are still `status='proposed'`. ⚠️ **284 split this row into a hard half and a reconciled half; that split is deliberately NOT cloned.** Its purpose was to bless an in-window CEO Gate-2 ship of two parked bystanders — **and this corpus has no bystander: `proposed` is exactly this plan's six.** No legitimate event moves a status during this plan, so the flat form is both simpler and stronger. ⚠️ **Do not re-introduce a reconciliation path here; there is no event for it to bless, and one would only create a route to excuse a real failure.** ⚠️ **If Step 1's Receipt does not report item (1) separately, mark this row `❌` (unverifiable).**
>
>    ⚠️⚠️ **THIS ROW IS ALSO THE ONLY IN-WINDOW GUARD ON THE SET-IDENTITY PREMISE — STATED, BECAUSE IT WAS OTHERWISE TRUE ONLY BY ACCIDENT.** A0-pre asserts the `proposed` set is exactly the six **before the write**; the Step-1→Step-2 approval gate is hours to days, and a seventh proposal created inside that window would falsify the premise licensing this plan's four guard deletions **after** A0-pre can see it. **A byte-identical distribution is what detects it** — a new `proposed` row changes the `proposed` count and this row fails. ⚠️⚠️ **BUT THIS ROW DOES NOT PROVE IT ALONE, AND THE CONJUNCTION IS WHAT ACTUALLY CLOSES IT.** A foreign proposal created **and** another moved terminal inside the same window would leave the count at 6 and this row byte-identical while the SET had changed. **Row 1 closes that: it asserts all six targets are still `proposed`.** Count == 6 (this row) **∧** the six targets all `proposed` (row 1) **⟹ the proposed set is exactly the six.**
>
>    ⚠️⚠️ **AND THE PROOF IS ONLY CLOSED IF BOTH HALVES COME FROM ONE READ — RUN IT AS A SINGLE STATEMENT, HERE, IN THIS ROW:**
>    `SELECT (SELECT COUNT(*) FROM lesson_proposals WHERE status='proposed'), (SELECT COUNT(*) FROM lesson_proposals WHERE status='proposed' AND id BETWEEN 201 AND 206);`
>    **Both values must read 6.** ⚠️ **Row 1 and row 2 are separate `SELECT`s in a table this step declares "mutually independent — run all eight" with NO ordering, so as two reads they can straddle a writer minutes apart: a proposal 207 inserted between them leaves row 2 reading `proposed 6` and row 1 reading all six targets `proposed` — BOTH PASS while the set is seven.** **This plan already mandates the one-statement mechanism at row 3 on exactly that reasoning; it was not applied to the pair called a closed proof.** ⚠️ **Report this single command and its raw output. The two-read form does not establish the premise and must not be recorded as if it did.**
>
>    ⚠️⚠️ **AND THE CONCLUSION NEEDS AN OWNER — THIS ROW IS IT, BECAUSE OTHERWISE THE PROOF IS EXPLAINED TWICE AND RECORDED NOWHERE.** Rows 1 and 2 each describe the conjunction; **neither states what to do when one of them fails.** So: **if EITHER row 1 or row 2 is `❌`, record one explicit line in this row's evidence — *"the set-identity premise is UNPROVEN for this run"* — naming which row failed.** ⚠️ **That premise is what licenses this plan's four guard deletions, so its status is a finding in its own right, not an inference the CEO should have to reconstruct from two separate `❌`s.** **Do not mark this row `✅` on the reasoning that the other row is the one that failed.** ⚠️ **That coverage is a side effect of the flat form, not something the row was written to do, so it would be silently lost by any later "simplification" narrowing this row to the six targets this plan owns.** It is recorded here so the narrowing is recognisable as a removal of a guard rather than a tidy-up. **Do not narrow this row.**
> 3. **Blast radius — total.** ⚠️ **Adjudicate on a SAME-INSTANT identity, not a cross-time one:** measure both counts now and assert **total route-NOT-NULL == outside-range route-NOT-NULL (measured NOW) + 6**. ⚠️⚠️ **Compute BOTH in ONE `sqlite3` statement** (the two-subquery form given at Step 1's B3) — **two invocations are two read transactions and can straddle a writer**, which defeats the whole point of a same-instant identity. Report the single command and its raw output. ⚠️ **Do NOT anchor this to before-item (4)** — that mixes a now-value with a then-value, so an externally-added route outside the range fails BOTH this row and row 4, and this row fails for a reason that is not its own. **Keeping it same-instant means row 3 answers "did exactly my 6 land?" and row 4 answers "did anything outside move?" — one fault, one failing row, correct localization.** At authoring both resolve to **76 == 70 + 6** — ⚠️ *illustrative only; adjudicate on the identity and report the actuals.* Resume-invariant. **A `before-item (2) + 6` form false-FAILs any partial resume.** Report the rise over item (2) as a measured value (**must be ≤ 6**), but do not adjudicate on it. ⚠️ **If Step 1's Receipt does not report item (2) separately, say so explicitly in this row's evidence and report the rise as `NOT COMPUTABLE — item (2) absent`. Do NOT mark the row `❌` on that ground alone** — item (2) is a REPORTING anchor here, not an adjudicating one, and this row's verdict rests on the same-instant identity. **The distinction is deliberate: a missing reporting anchor must be visible without failing a row whose actual check succeeded.**
> 4. **Blast radius — outside range.** `SELECT COUNT(*) FROM lesson_proposals WHERE route IS NOT NULL AND id NOT BETWEEN 201 AND 206` equals Step 1's before-item **(4)**, **UNCHANGED**. ⚠️ **Adjudicate against the reported before-item, NOT against a literal** — the authoring measurement was **70**, recorded here as *illustrative*; if a foreign routed proposal landed before dispatch the before-item legitimately reads higher, and the anchor is still what decides. Report the actual. ⚠️⚠️ **Anchor each after-value to its OWN before-item — (2)↔total, (4)↔outside-range. Both before-items read the same number at authoring, so crossing them silently passes while checking nothing.**
>
>    ⚠️ **If Step 1's deposit does not report item (4) separately, this row is `❌ (unverifiable)`** — and append the verbatim receipt lines you searched, plus your search command AND its exit code, to `db-invariants.txt` BEFORE marking it.
>
>    ⚠️⚠️ **THE STEP-WIDE SEARCH RULE APPLIES HERE AND IS PARTICULARLY LIVE:** the Receipt lines you must search for contain `**`, which is a regex error under this machine's `ugrep` shim — the command prints nothing and exits non-zero, and this row's own wording (*"its empty result"*) would otherwise invite recording that as evidence of absence. **Use `grep -F`, capture `GREP-EXIT`, and treat any exit other than 0 or 1 as a tooling failure.** The rule is stated once at the top of this step; it is referenced here rather than restated, so the two cannot drift apart.
>
>    ⚠️⚠️ **AND THIS ROW HAS A SECOND PART THE COUNT CANNOT SUBSTITUTE FOR — 4(b), THE ROW IMAGE.** Re-run `SELECT id||':'||route FROM lesson_proposals WHERE route IS NOT NULL AND id NOT BETWEEN 201 AND 206 ORDER BY id` and assert it is **byte-identical to Step 1's before-item (4b)**. ⚠️ **The count in 4(a) is blind to a route VALUE change: a foreign row moving between two non-NULL routes leaves it unchanged, and seven such rows exist (140, 141, 146, 161, 164, 169, 183 — measured at authoring).** **4(b) is the only check in either step that sees it.** ⚠️ **If Step 1's Receipt does not report item (4b), mark this row `❌ (unverifiable)`** — the same fail-closed rule as its siblings.
>
>    ⚠️ **ON A MISMATCH, DIFF THE IMAGES BEFORE HALTING.** Compare before-item (4b) against the image you just took and write the differing lines, RAW, to `db-invariants.txt`. ⚠️ **This row is the SOLE outside-range guard, and it reports a COUNT — a count cannot say WHICH row moved.** 284 had a second row naming its parked pair directly; that row is deleted here because the pair does not exist, and the deletion is correct — **but it removed the only check that could identify a specific drifted id.** Without this capture the CEO adjudicates a blast-radius failure from a single changed integer. The capture costs one query and is required only on the failing path.
> 5. **Classification unchanged across the approval window.** `get_unclassified_entries(conn)` **UNCHANGED from Step 1's before-item (3)**. ⚠️ **This helper needs a live `sqlite3.Connection`, and this step declares NO DB writes — so open it READ-ONLY: `sqlite3.connect("file:/Users/marklehn/Developer/GitHub/lessons-forge/lessons-forge.db?mode=ro", uri=True)`.** The natural `sqlite3.connect(<path>)` is read-WRITE on the live production DB and touches its WAL sidecars, contradicting this step's own no-writes declaration. ⚠️⚠️ **THIS ROW GETS ITS OWN FALLBACK — DO NOT APPLY THE STEP-WIDE ONE HERE, BECAUSE IT INSTRUCTS THE EXACT ACTION THIS ROW FORBIDS.** The step-wide `?mode=ro` fallback says *re-run without the URI*; for a Python `sqlite3.connect` that opens the live production DB **read-WRITE** — two sentences after this row mandates read-only. **Taking that path silently, under cover of a generic rule written for the `sqlite3` CLI, is the one thing this row must never do.**
>
>    ⚠️ **What "no DB writes" means here, stated precisely so the fallback can be judged against it: NO ROW MUTATION AND NO SCHEMA CHANGE.** A read-only handle is the mandate because it makes that guarantee structural. **Creating WAL sidecars by opening a connection is not a row mutation** — so if `?mode=ro` fails on absent sidecars, the fallback is: **open read-write, run the SELECT-only helper, and RECORD in the row's evidence that you did and why.** ⚠️ **This is NOT a fourth entry in this step's closed list of three declared deviations** — those are narrowings of rules external to this plan, whereas this is a clarification of this plan's own no-writes scope. **The list of three stays accurate; do not "correct" it to four.** ⚠️ **The recording is the whole point** — it converts a silent contradiction into a declared, bounded one. **If you cannot open the DB at all, mark the row `❌` with the tooling failure captured; do not infer the value.** And anchor to the recorded before-value, NOT to a hardcoded `[]`. ⚠️⚠️ **And if Step 1's Receipt does not report item (3) separately, mark this row `❌` (unverifiable)** — the same fail-closed rule rows 2 and 4 apply to their anchors. **Without this clause the row states a prohibition with no consequence:** its only instruction on a missing anchor would be *"do not use the hardcoded value"*, leaving the hardcoded value as the agent's sole remaining path — so the guard would be defeated exactly when the record it depends on is absent. ⚠️ **Without this row, Step-1 Receipt item (3) is a mandated deposit with NO downstream reader.** It matters because the Step-1→Step-2 window is hours-to-days and Step 1's own C2 closes only *within* Step 1. Planner's measured value at authoring: `[]`.
> 6. **Suite** — `python3 -m pytest src/ -v` to an explicit pass/fail, raw tail shown. ⚠️ **The repo's only test module is `src/test_lessons_forge.py`** — named explicitly because `plan_lint` warns when a step mentions tests without naming a test file or `tests/` prefix, and because it is the fact that makes targeted ≡ full here. Compute the baseline from `--collect-only` and reconcile against the most recent prior QA in `knowledge/qa/`. Confirm 0 regressions. ⚠️ **Also assert `src/` is untouched** — `git status --porcelain -- src/` run from YOUR OWN working tree (not `-C <root>`; `src/` lives in this submodule) must be EMPTY. This plan writes no code, so any `src/` delta is out of scope.
> 7. **The Gate-2 target artifacts are UNCHANGED by this plan.** ⚠️ `lessons-forge` is a submodule; these files are tracked by the ROOT repo and do NOT exist in your worktree — a plain `git diff` passes VACUOUSLY. One invocation, capturing the exit code:
>    `git -C /Users/marklehn/Developer/GitHub status --porcelain -- DRAFTING_CYCLE.md PLANNER_TEMPLATE.md RULE_20_SELF_CHECK_BLOCK.md; echo "PORCELAIN-EXIT=$?"`
>
>    ⚠️⚠️ **THIS ROW DOES TWO DIFFERENT JOBS AND THEY ADJUDICATE DIFFERENTLY — AN EARLIER DRAFT RAN BOTH AS ONE ABSOLUTE, AND THAT WAS A MEASURED FALSE-FAIL GENERATOR.**
>
>    **JOB 1 — DID *THIS PLAN* TOUCH DOCTRINE? (fail-closed, absolute, unadjudicatable.)** The instrument is **`git status --porcelain`**, plus `scope_check`. **A non-empty porcelain is `❌`, full stop** — see below.
>
>    **JOB 2 — HAS DOCTRINE MOVED SINCE AUTHORING? (drift detection, reconcilable.)** The instrument is the **`shasum` pins**. ⚠️ **Measured at authoring: these three files are edited by plans in the GOVERNANCE project — 59 commits in 91 days, 11 in the last 23 — and NEITHER concurrency precondition excludes a governance-project plan.** Over an hours-to-days verdict gate an ambient doctrine commit is likely, and it says nothing whatever about this plan's correctness. **So: a pin mismatch with an EMPTY porcelain, attributable by `git -C <root> log --oneline -- <path>` to a commit that is NOT this plan's, is `✅ (reconciled — ambient doctrine drift)`. ⚠️⚠️ **THE ATTRIBUTION IS MECHANICAL, NOT A JUDGEMENT — doctrine commits in this shop carry a bracketed plan id in the subject** (measured: `[287] Step 2: codify proposals 191–200…`, `[270] codify the Drafting Cycle…`, `[260] register READONLY_AUDIT_CONTRACT…`). **Read the id off the commit subject: a tag that is NOT this plan's own id reconciles; this plan's own id, or NO id you can read, is `❌`.** ⚠️ **Stated because "not this plan's" without a mechanism leaves the agent adjudicating by feel — which is exactly what the porcelain half of this row forbids.**** Record the commit, capture the `log` and `diff` into `db-invariants.txt`, and state that Gate 2 must re-derive its anchors against the moved file. ⚠️ **A mismatch you CANNOT attribute to an identified foreign commit is `❌`.**
>
>    ⚠️⚠️ **WHY THE SPLIT IS PRINCIPLED AND NOT A WEAKENING — IT IS PROPOSAL 201, WHICH THIS PLAN ROUTES.** 201's whole content is that *"unchanged by THIS plan" (fail-closed) is a different job from "unchanged since AUTHORING" (drift detection), and one pin cannot serve both.* ⚠️ **The historical trap in this lineage is weakening the PORCELAIN half — 282 warned "there is no other guard; this row IS the guard", and three consecutive plans then softened it. That half is untouched here and stays absolute.** What is reconciled is only the drift half, which never was a guard on this plan's behaviour.
>
>    ⚠️⚠️ **AND RE-MEASURE THE CONTENT PINS — porcelain ALONE CANNOT SEE A COMMITTED EDIT, which is exactly why Job 2 exists.** Run `shasum -a 256 /Users/marklehn/Developer/GitHub/{DRAFTING_CYCLE.md,PLANNER_TEMPLATE.md,RULE_20_SELF_CHECK_BLOCK.md}` and compare against the Planner's authoring pins:
>    - `DRAFTING_CYCLE.md` → `3951bcf8bc2d9e5f`
>    - `PLANNER_TEMPLATE.md` → `0c53222fbacdc89c`
>    - `RULE_20_SELF_CHECK_BLOCK.md` → `3accbce0c8d2b445`
>
>    ⚠️ **The pins above are TRUNCATED to their leading 16 hex characters — compare only that prefix of your measured digest.** A full 64-char digest will never equal a 16-char string, and an unstated comparison rule manufactures a `❌` on a clean tree. ⚠️ These are the *authoring-time* values — report the measured value, do not adjust to the literal (Checklist #29).
>
>    ⚠️⚠️ **THIS ROW IS FULLY ABSOLUTIST, AND THAT IS A DELIBERATE HARDENING OVER 284 — NOT AN OMISSION.** 284 carried one narrow `Gate-2 shipped in-window` reconciliation, because its parked pair 191/192 targeted two of these three files and a legitimate CEO codification would move them mid-flight. **That trigger is unreachable here: `proposed` is exactly this plan's six, so no Gate-2 ship of any other proposal can occur during this plan.** The reconciliation is therefore deleted, and plan 282's rule stands unqualified: ***"There is no other guard; this row IS the guard."*** ⚠️ **Do NOT re-derive a bless-path by analogy to the parent — there is no event to bless, and adding one would only create a route to excuse a real failure.**
>
>    ⚠️⚠️ **NON-EMPTY PORCELAIN IS `❌`. FULL STOP. YOU DO NOT ADJUDICATE IT.** `scope_check` is cwd-scoped and cross-repo-blind, so nothing else can see an edit to a root doctrine file. **You may not mark this ✅ on the reasoning that the change looks unrelated, pre-existing, or someone else's — that judgment is not yours to make.** On any non-empty result, or any pin mismatch: capture `git -C <root> log --oneline` and `git -C <root> diff` for those paths into `db-invariants.txt` **before** halting, mark `❌`, and let the CEO adjudicate at the verdict gate. **A non-zero exit code is likewise `❌`.**
>
>    ⚠️⚠️ **ONE FORENSIC NOTE — THIS IS DIAGNOSIS FOR THE CEO, NOT AN ADJUDICATION YOU MAY MAKE.** There is a specific benign-looking pattern: **this row failing on `DRAFTING_CYCLE.md` and/or `PLANNER_TEMPLATE.md` at the same time as row 2 shows 201–206 moved `proposed → implemented`.** That signature means **the dispatch precondition was violated** — a Gate-2 plan for this batch shipped inside the approval window (see the concurrency clause; Task A is what makes these six eligible). ⚠️ **It is STILL `❌`, and you still halt** — the plan's premises no longer hold and that is a real finding, not a false alarm. **But say so explicitly in the row's evidence**, naming both rows, so the CEO can distinguish a violated precondition from genuine corruption in one look rather than reconstructing it from two unrelated-looking failures. ⚠️ **Do not extend this note to any other combination, and do not use it to mark anything `✅`.** ⚠️ **Why `bellows/scripts/plan_lint.py` and `bellows/gates.py` are PROHIBITED in scope but deliberately NOT pinned here:** a prohibition is inert and costs nothing, but a content pin on an actively-churning bellows file could only ever false-HALT this plan on an unrelated change. The asymmetry is intentional — do NOT "complete" the pin set. **Also record root HEAD** (`git -C <root> rev-parse --short HEAD`; authoring value `d430234`) — a HEAD delta alone is a reconcile-note, but paired with a pin mismatch it identifies the commit.
>
> **Write rows 0–5 and 7 raw output to `db-invariants.txt`; row 6's pytest tail to `pytest_targeted.txt`.** ⚠️ **Non-empty is ALL the Rule 20 block checks — a one-byte file passes it.** Before running the block, **self-grep each file for markers proving the content landed.** ⚠️⚠️ **A single `PORCELAIN-EXIT=` marker is VACUOUS for 6 of the 7 rows routed to `db-invariants.txt`:** that string comes from row 7's `echo`, which prints unconditionally, so an agent that writes only row 7's block and drops rows 0–5 satisfies both the Rule 20 non-empty check AND the grep. So: **prefix each row's block in `db-invariants.txt` with a literal `ROW-<n>:` line** (`ROW-0:` … `ROW-5:`, `ROW-7:`), then assert **`grep -c '^ROW-' db-invariants.txt` == 7** and record the count.
>
> ⚠️⚠️ **THE MARKER COUNT PROVES SEVEN LINES EXIST — IT DOES NOT PROVE ANY HAS OUTPUT BENEATH IT.** Seven bare `ROW-n:` lines satisfy both the count and the Rule 20 non-empty check, which is the same vacuity the marker scheme replaced (a single unconditional `PORCELAIN-EXIT=` echo). **So also assert the file's total line count is at least 21** — seven markers plus a floor of two captured lines each — **and record `wc -l`.** ⚠️ **Then spot-check TWO markers by eye**, naming which two in the report, and confirm each is followed by actual command output rather than a bare heading. **A floor is not a proof; it raises the cost of a vacuous file above the cost of writing a real one, which is what a cheap check can honestly buy.** Do not report this as proving every row landed — it does not.
>
> For `pytest_targeted.txt`, grep the pytest summary line. **Record every search's command AND its exit code**, per the `grep -F` discipline in row 4 — a search that errors prints nothing and must never be recorded as a negative result.
>
> **Deposit:** the QA report — table under `## Verification Table` with the mandated columns, raw output, the Rule 20 banner + PASSED line + literal stdout (passing runs only), Output Receipt — plus the two evidence files. Canonical Python file-write — no heredoc. **Commit by EXPLICIT PATHSPEC** — `git add <exact paths>` then `git commit -m "…" -- <exact paths>`. **NEVER `git commit -a`, never an unscoped `git add -A`.** ⚠️ **`git add` FIRST — load-bearing for NEW files:** `git commit -- <untracked path>` fails with `pathspec … did not match`.
>
> In `### Ledger Updates` include:
> - **`#### Project Status`** — one milestone, **SCOPED to this plan's six**: Gate 1 complete for the session-13/14 batch (entries 193–198 → proposals 201–206) — 6 codify / 0 backlog / 0 reference; all 6 remain `proposed` and Gate-2-bound. ⚠️ **State it scoped, never as a bare corpus-wide `proposed = N`** — an unscoped milestone deposits a permanent status record that a later foreign proposal falsifies.
> - **`#### Forward Register`** — ⚠️ **THIS BLOCK IS MANDATORY AND ROW 0(v) CHECKS IT EXISTS.** ⚠️ **Write it as EXACTLY SIX `- ` bullet lines, one per item, so row 0(v)'s count of six is checkable** — a prose paragraph carrying all of them would satisfy the heading grep while failing the count, and a count assertion against an unspecified format is not a check. **Six items:** **(a)** the `gates.py:449` per-step span regex — the final step's span runs to end-of-file and absorbs the trailing Drafting Cycle block, so a gate-matching string quoted in a record is evaluated as if the step had said it (Rule 46's routing obligation for proposal 206, which this plan codifies rather than rejects); **(b)** the `plan_lint` §4 zero-expectation-class check; **(c)** the `plan_lint` §4 T2 panel check, which matches a line's opening and never its content; **(d)** the `plan_lint` §4 closing check, whose negation strip is defeated by one intervening word — **(b), (c) and (d) are carried forward from plan 288's cycle.** ⚠️ **Enumerated as SEPARATE labels, one per bullet, because the format mandate above requires SIX bullet lines and row 0(v) mechanically counts SIX** — a grouped enumeration produces fewer bullets and marks that row `❌` on an otherwise clean run. **(e)** `generate_lessons_report` at `src/lessons_forge.py:593` writes with no explicit `encoding=` argument — a portability gap. ⚠️ **Carried because it was item 1 of plan 288's own deposited register and appears in NO other forward destination; an earlier draft of this plan dropped it while claiming to carry 288's items forward.** **(f)** ⚠️ **`lessons-forge/knowledge/FORWARD.md` DOES NOT EXIST, so `bellows.py:1417` skips the append for every lessons-forge plan and every Rule 46 routing from this project is discarded by design** — a CEO decision is owed (create the project's register file, or route bellows-side tooling items through a bellows-project plan). ⚠️ **Write SIX `- ` bullet lines; row 0(v) counts six.**

⚠️ **All six are recorded here for Rule 46 compliance and as part of this plan's record — NOT as a delivery mechanism.** **[EXECUTED HERE — 2026-07-31]** plan 288's QA deposit emitted its block correctly and no row appeared anywhere, because the destination file for this project does not exist; **`bellows/knowledge/FORWARD.md` was never that destination.** ⚠️ **Even with the file present, `bellows.py:1430` keeps only `lines[0]` — one row, first line only — so a multi-item block cannot survive the channel.** **The durable record of all six is the session baton at wrap.**
>
>   ⚠️⚠️ **THE LIMIT OF THIS BLOCK'S REACH, STATED FROM THE CODE PATH RATHER THAN INFERRED.** **[EXECUTED HERE — 2026-07-31]** `bellows.py:1417` resolves the append destination to **`<this plan's project>/knowledge/FORWARD.md`** — for a lessons-forge plan, **`lessons-forge/knowledge/FORWARD.md`, which does not exist** — so the daemon logs `no FORWARD.md in project, skipping forward append` and returns. **Confirmed in plan 288's run log.** ⚠️ **So NO row from this block reaches any register file, and `bellows/knowledge/FORWARD.md` was never its destination.** ⚠️⚠️ **Even were the file created, `bellows.py:1430` keeps only `lines[0]` — ONE row, first line only — and `parser.py:75` truncates the capture at the first blank line, so four bullets could never yield four rows.**
>
>   **Therefore: this block is emitted for Rule 46 compliance and as part of this plan's own record. It is NOT a delivery mechanism, and this plan claims no delivery.** ⚠️ **An earlier draft designated a post-close read of `bellows/knowledge/FORWARD.md` as the primary remediation and framed the absence as a "live, evidenced daemon defect" — that was WRONG on both counts and is struck: the file is not this project's register, and the daemon behaved exactly as written.**
>
>   ⚠️⚠️ **AND THAT OBLIGATION NEEDS A DESTINATION THAT OUTLIVES THIS PLAN — NAMED, BECAUSE THE OBVIOUS ONE IS THE BROKEN CHANNEL ITSELF.** This plan closes to `Done/` and is archived; an obligation recorded only in its prose dies with it. **The Forward Register cannot carry it — measured above, it reaches no file for this project.** ⚠️ **So ALL SIX items — (a) through (f) exactly as enumerated in the Ledger Updates block, no fewer — are written into the session baton (`shop_next_session.md`) at wrap, which is the one artifact read at the start of every session.** ⚠️⚠️ **Item (e), the `encoding=` gap, is the one an earlier draft of this very plan dropped while claiming to carry 288's items forward — count six at the wrap, do not re-derive the list from memory.** ⚠️ **Two earlier attributions in this plan's drafting were wrong and are retracted: that the obligation "lived in a fold record and never in the artifact", and that it "died at the append."** **It reached the artifact and the deposit; the destination was simply never configured.**
> - **`#### Prompt Feedback`**
>
> **Deposits:**
> - `lessons-forge/knowledge/qa/gate-1-route-qa-2026-07-31.md`
> - `lessons-forge/knowledge/qa/evidence/gate-1-route-session-13-14-captures-2026-07-31/` (multiple files per Rule 20 self-check)
>
> ⚠️ **The evidence DIRECTORY is one bullet; the filenames are deliberately NOT listed.** `PLANNER_TEMPLATE.md:808` (Rule 26): *"Evidence files MUST be represented by the evidence directory as a single bullet… Do NOT list individual evidence files… duplicating them in `**Deposits:**` is forbidden."* It matters because this step may halt mid-table and commit only the deposits that exist — a declared-but-unwritten file would produce a false gate failure on an honest halt.
>
> **Do NOT move this plan to `Done/`.** The close path is owned by Bellows on continue-verdict consumption — **never by the agent** (`PLANNER_TEMPLATE.md:513`, Rule 8: *"Either way, the agent is not the actor"*). This is unconditional; an `unauthorized_done_move` force-recovers the file and FAILs this step's gates.

---

## Drafting Cycle

**This section is a RECORD of the drafting cycle, NOT instructions for any step.** Nothing here modifies the steps above. ⚠️ It sits inside the final step's extracted span — the final step's match runs to end-of-file with no later step to stop at — a parsing artifact, not scope. Per §3 "compact" the per-fold narrative is NOT here; it lives in `scratchpad/`.

⚠️ **No BLOCKING gate string is quoted in this block** — the Rule 20 banner pair and the deposit/scope markers are described, never reproduced, which is the discipline proposal 206 proposes and this plan routes. ⚠️⚠️ **The claim is stated with that qualifier because an unqualified version would be FALSE: this block contains the `❌` glyph, which `rule_22` matches.** It is harmless here for a reason worth recording rather than rediscovering — **`_gate_rule_22_verification` scans the deposited QA REPORT for that glyph and uses the step text only to extract deposit paths**, so a glyph in the plan's own record is never evaluated as a verdict. **An earlier draft asserted the absolute; a cold reader found the counter-example and the gate-internals fact that makes it safe.**

**Tier:** T2 — trigger fired: **T-2** (production-data mutation — writes `route` on 6 canonical proposals). T-6 does NOT fire (routing metadata, no doctrine edit — that is Gate 2). **T-8 does not fire on novelty**, but the tier is NOT reduced on clone provenance: a proven clone is where cold-panel value is highest, and this clone deletes an entire subsystem of its parent, which is the highest-risk edit a clone can make.

**Walks:** v0 → v10. **Five walks**, each run as four independent proposal lenses → one culmination → ACID alone against the merged draft, then a sequential cold panel. ⚠️ **Per-walk detail lives in `scratchpad/walk{1,2,3,4,5}-proposal-register.md`, NOT here (§3).**

- Weak spots:          w1 4 folded; w2 3 folded; w3 3 folded; w4 3 folded; w5 2 folded; cold-1 10 folded. (Recurring class: **claims about work done**. Three inherited execution attestations were worn in this plan's voice — an authoring self-check, a forward-diff, and A00's derivation; all discharged by execution or struck. Cold-1 refuted the reason given for NOT executing one of them.)
- Destruction:         w1 3 folded; w2 1 folded; w3 1 folded; w4 dry; w5 1 folded; cold-2 6 folded. (⚠️ **The deletion of the parent's bystander subsystem rested on four premises; the ONE never measured was false** — an outside-range COUNT cannot see a foreign row moving between two non-NULL routes, and seven such rows exist. Repaired by restoring a generalised value-level row image at before-item (4b), C1(b) and row 4(b).)
- Vulnerabilities:     w1 2 folded + 2 negatives; w2 2 folded; w3 1 folded + 2 verified; w4 1 folded; w5 1 folded; cold-3 7 folded. (⚠️ **Every finding here came from EXECUTION, not reading**: the search-rule exit-code discriminator was inverted for the exact pattern it named; `integrity_check` returns ok on a 0-byte and a nonexistent file; the mandated re-run overwrites the corpse so the `rm` instruction would have destroyed the restore point; `git log`/`git status` on a root-joined path both return empty with exit 0.)
- Integration-record:  w1 2 folded + 3 verified; w2 2 folded + 1 verified; w3 1 folded + 2 verified; w4 dry; w5 dry; cold-4 9 folded. (⚠️ **The Rule 46 diagnosis was wrong THREE TIMES and is now taken from the code path**: the daemon resolves the register to `<project>/knowledge/FORWARD.md`, which this project lacks, so the routing is discarded by design — no daemon defect exists. Cold-4 also caught the batch mislabel baked into six path literals.)


- ACID:                w1 5 folded; w2 5 folded; w3 3 folded; w4 2 folded; w5 2 folded; w6 2 folded; run ALONE against each merged draft. **(w6, against the confirming-walk merge: the resume clause written one pass earlier told the agent to annotate the Status cell — but the block matches positive tokens by CELL EQUALITY, so an annotated cell is neither positive nor failing and BOTH gates ignore the row; the annotation moved to the measured-value column. And the sentence warning against putting hedging keywords on a table row QUOTED two of them on a row-shaped line, becoming an instance of itself.)** ⚠️ **Dominated on every pass by defects the CULMINATION introduced — none existed in the drafts the four lenses read.** (Representative: a capture with no Receipt slot and no reader; a fail-closed set enumerated at three sites and swept at none; the pre-write-halt rule contradicting A0-iso's “write nothing”; a provenance convention that over-swept a dozen measured claims; a forbidden running tally replaced by a false qualifier.)





**Cold panel (T2):** **COMPLETE — all 5 rounds run.** Sequential fresh-context readers, each on the prior's folded draft, each handed 284 as the comparison artifact; every finding author-verified before folding. **r1 weak-spots 10 · r2 destruction 6 · r3 vulnerabilities 7 · r4 integration-record 9 · r5 ACID 9 — all folded.** ⚠️ **Per-round counts only; no total, because §3 forbids a separate running tally in this block — a prior draft carried one and it is struck.** ⚠️ **r5 (cold ACID, system level) found what four single-lens rounds could not: the rows-1∧2 "closed proof" was two read transactions in an explicitly unordered table; CL6 was declared satisfied against a destination no step writes to; row 7's absolutism rested on an invariant nothing established (the three pinned files are edited by GOVERNANCE-project plans at ~0.65 commits/day, excluded by neither precondition) — resolved by splitting the row's two jobs per proposal 201, which this plan routes; and a post-commit resume silently degrades rows 2, 4(a), 4(b) and 5 to tautologies, including the value-level guard restored in r2.** ⚠⚠ **The panel found the two premise failures five warm walks did not**, and each of its four rounds overturned something the warm cycle had asserted as settled. ⚠️ **Round detail in `scratchpad/`; not restated here per §3.**

**Conflicts:** CL1 — a check anchored to a captured before-value may never adjudicate on an authoring literal (weak spots w1; swept to four sites). CL2 — every mandated capture needs a Receipt slot, every slot needs a reader, and every reader needs a fail-closed clause for that item's absence (weak spots w1; **widened by ACID w1 to cover PRE-condition captures, not only snapshots — the clause that caught item 0**). CL3 — a mandated search must distinguish a tooling failure from a genuine zero result (vulnerabilities w1). CL4 — deleting a guard inherited from a parent obliges an explicit statement of the capability lost and where it is now covered (destruction w1). **CL5 — an invariant that closes a gap only as a side effect must be named as that gap's guard, or a later simplification removes it without anyone noticing a guard was removed (ACID w1).** No cross-lens contradiction remains unresolved.

**CL6 — a remediation may not be recorded solely in the channel it is remediating, nor in an artifact that is archived at close; name a destination that is read after this plan ends (ACID w2).** **CL7 — a recorded tool result is valid only for the artifact version it ran against; any check whose output is quoted in the plan must be re-run as the last pre-deposit action (ACID w2).** **CL8 — an execution claim inherited from a parent plan may not be stated in this plan's voice; it is either re-executed here or marked as inherited with the reason it was not (weak spots w3, after the third instance).** **CL9 — a newly added rule must be checked against the clauses already in the plan before it is folded: three of this cycle's own folds contradicted or over-swept an existing clause on the pass that introduced them (ACID w3).** **CL11 — SCOPE AN ASSERTION TO THE IDS THIS PLAN OWNS; A WHOLE-CORPUS PREDICATE IS A LIABILITY, NOT A GUARD.** ⚠️ **This is the previous session's C9, and it is encoded here because that session paid the most for it and this plan did not cite it:** an ACID fold there asserted *"no proposal in the corpus carries `category='duplicate'`"* — **19 already existed**, the assertion was false by construction, and it would have halted every resume. It was caught by the LIVE RUN after ten walks and a five-reader panel missed it. ⚠️ **This plan's A0-pre set-identity assertion is deliberately whole-corpus and therefore deliberately in tension with CL11** — the trade is declared at premise (iv) and in the concurrency clause, and it is accepted because the premise it protects licenses four guard deletions. **CL11 exists so that the NEXT such assertion is argued against the precedent rather than from first principles, and so the tension is visible rather than rediscovered.**

**CL10 — RESTORED FROM 284, WHICH THIS CLONE DROPPED WITHOUT STATING IT (a CL4 violation in its own right): every cross-row assertion must agree with every other on the SAME event — a check that blesses an outcome obliges its siblings to bless it too.** ⚠️ **The clone dropped it because the two bless-paths CL3 originally policed were deleted — but the constraint is GENERAL, and its class recurred twice this cycle with no standing owner: the pre-write-halt rule contradicting A0-iso, and QA row 5's fallback instructing what row 5 forbade. CL9 covers new-versus-existing only, so both were caught by which lens happened to run.** The pairs it now polices: rows 1 ∧ 2 (the set-identity proof), C1(a) ∧ C1(b) and row 4(a) ∧ 4(b) (count versus value image), row 7 ∧ row 2 (the forensic signature), and Step 2's opener ∧ row 0(v).

**⚠️ LEDGER STATUS AT COLD-PANEL ROUND 1 — re-checked against the instructions AS THEY NOW STAND, not against what they were meant to say.** ⚠️⚠️ **This re-check is restored from 284, which carried it and which this clone had DROPPED — and restoring it immediately found CL2 VIOLATED.** **CL1 SATISFIED** — no adjudicating check rests on an authoring literal; all four sites carry the illustrative marker. **CL2 VIOLATED, now repaired** — Receipt item (2) had a slot and a reporting reader but no absence clause, and **item 8 had NO reader at all**; the constraint is refined to distinguish adjudicating anchors (fail-closed) from reporting anchors (must be visibly marked absent), and item 8 now has row 0(iv-b). **CL3 SATISFIED** — the step-wide search rule is stated once and referenced, not duplicated. **CL4 SATISFIED** — every deleted guard names its lost capability, and the blast-radius capture is now at BOTH sites. **CL5 SATISFIED** — the rows-1∧2 conjunction is named in both rows with an owner for its conclusion. ⚠️⚠️ **CL6 NOT SATISFIED — DECLARED OPEN, because an earlier draft marked it satisfied against a destination NO STEP IN THIS PLAN WRITES TO.** The session baton lives in the **root repo**, outside both steps' scope; it appears in no `**Deposits:**` block, no Receipt item, and no QA row, and no gate observes it. **CL6 requires "a destination that is read after this plan ends"; the baton qualifies, but nothing in this plan puts the items there.** ⚠️ **The obligation is therefore the PLANNER's at session wrap, explicitly and by name — not this plan's — and it is recorded as OPEN so that closing it is a deliberate act rather than an assumption.** **The six items are enumerated in the Ledger Updates block precisely so the wrap has a checkable list to copy.** **CL7 OPEN BY DESIGN** — discharged only by the final pre-deposit lint run. **CL8 VIOLATED, now repaired** — its first application carried a false reason for not re-executing; the work was done instead. **CL9 SATISFIED** — this re-check is itself CL9's mechanism.

⚠️ **CL9 is the ledger's own summary of itself.** CL2 was broken by the culmination that opened it; CL1's sweep left a literal unhedged; CL8's convention over-swept on the pass that stated it; and the pre-write-halt rule contradicted a clause sitting forty lines above it. **Four constraint-violations by their own authors across three walks** — the same count, and the same shape, as the three the previous session measured. ⚠️ **This is the direct evidence behind 205's Gate-2 mechanization requirement, and it is now this plan's own data rather than a citation to someone else's.**

⚠️ **CL2 was violated by the culmination itself, one pass after being opened** — the fold that added the set-identity assertion created a capture with no slot and no reader. ⚠️ **And CL1's own sweep left one literal unhedged, caught by ACID w1.** Recorded rather than quietly repaired: **the ledger's value is precisely the record that its own authors broke it, three times across two walks.** ⚠️ **This is the measured argument behind 205's Gate-2 mechanization requirement** — every mechanism built to remember has itself required remembering.

**Closing:** ⚠️ **§2's closing condition is NOT MET, and this is stated rather than papered over.** §2 requires the last event before deposit to be a **lens pass**; the last event here is a **fold** (ACID w6, against the confirming-walk merge). **The confirming walk RAN and was NOT dry** — it returned three material findings, which under §2 re-opens the walk; those were folded, and the ACID pass on that merge folded two more. **The cold panel is COMPLETE (all five rounds).** ⚠️ **No walk in this cycle has ever closed dry: every culmination has produced defects that the following ACID pass caught, including the culminations that were repairing defects — which is this plan's own measured evidence for the Gate-2 mechanization requirement it carries for proposal 205.** **Deposited on explicit CEO direction under the same fail-closed standard as plans 282, 284 and 288, each of which shipped with this condition unmet and said so here.** `plan_lint` closing WARN fires and is CORRECT; it is not to be worded away.
