# Executable: the §2 rewrite — the doneness bar reads TOUCH (cluster A of the 2026-08-10 gate: 275/278/292/302/308): DRAFTING_CYCLE v2.3 → v2.4, then flip all 12 cluster-A rows (both vintages) to `implemented|codify`

**Type:** Executable
**Project:** lessons-forge
**Depends on:** **executable-346** (lessons-forge, Done — the newest same-class Gate-2 plan and mechanism origin: block-edit builder, all-or-nothing, two-value timestamp exclusion), **halted-executable-334** (lessons-forge, HALTED — Step 1 complete (v2.0 doctrine), Step 2 the held seven-row flip THIS plan completes on CEO release 2026-08-11; its Task-M clause mapping and 258/259 PARTIAL records are inherited AND re-verified, never assumed), executable-342 (Gate-1 routing that stamped the five new cluster-A rows), executable-326 (stamped the seven old rows), the 2026-08-10 gate-1 packet Group 1 (cluster-A routing decision, Planner 2026-08-10; provenance, no step reads it), DRAFTING_CYCLE.md at v2.3 (precondition, checked at A0)
**Created:** 2026-08-11
**Author:** Planner
**Slug:** `s2-rewrite-2026-08-11`
**dispatch_mode:** bellows
**pause_for_verdict:** always
**Priority:** 5
**cycle_tier:** T2
**qa_steps:** 2
**Test Scope:** targeted (Rule 21 — no source-code change; single test module verified 2026-08-11; QA row 7 re-derives; baseline 55 passed / 0 skipped)

⚠️ **ID NOTE:** id read from `id_sequence` at deposit (`next_id` read **347** at authoring — a PREDICTION).

---

## Why this exists — the bar contradicts itself, and the replacement is measured

DRAFTING_CYCLE §2's doneness bar requires findings **"record-class only AND predominantly fold-introduced."** bellows **Forward row 53** records the defect: *the fold-introduced fraction is simultaneously the bar's convergence condition and the section's own noise-floor signature* — measured 28% → 68% → 67% → 75% across four walks of one cycle, with **both readings applying from walk 2 onward.** A cycle steered by that number cannot distinguish finishing from circling.

**The replacement is measured, not argued** (LESSONS 2026-08-10, *A walk's convergence is told by what its findings TOUCH*): classifying each finding by the surface it touches separated the two cases cleanly — walk 3 changed instructions in roughly ten of fifteen findings, walk 4 in two of eight; the instruction surface had converged while the commentary surface had not, **and structurally cannot**, because every fold adds explanation and the explanation is itself reviewable.

**Cluster (A) of the 2026-08-10 gate** (proposals 275, 278, 292, 302, 308 — entries 267, 270, 284, 294, 300), routed `reference|backlog` **as one unit into this rewrite** (Planner decision 2026-08-10, no cherry-picking): 308 is the centerpiece (the TOUCH bar); 302 adds the restructuring-clock reset; 275 the by-class confirming-pass yield; 278 the untargeted-confirming-pass half plus the tracking-line sweep; 292's static-artifact correction is confirmed already carried by v2.0's text (the flag-D measurement in the packet), with its ratio-reporting discipline retained.

**The plan ALSO completes plan 334's held Step 2** — the seven-row 2026-08-09 cluster-A flip (233, 238, 246, 247, 258, 259, 271, content shipped as v2.0), **released by CEO decision 2026-08-11**: the hold's condition ("let v2.0 prove itself on a real cycle or two") is met many times over — specimen 1, two qualifying closes (330/342-era), and the three-batch 344/345/346 arc. ⚠️ **258 and 259 flip PARTIAL per 334's own Task-M mapping, recorded rather than silently absorbed:** 258's phase-labelling quarter and 259's cross-plan-measurement middle ask (the packet's D5, funnel-routed instrumentation) remain unbuilt; the History row names both. ⚠️ D5's absence still means the bar's three-cycle re-measurement has no instrument — that dependency transfers to the NEW bar unchanged and stays on the funnel's queue.

**Routing:** corpus path proper, no §6 deviation. **Held-draft disposition (panel seat 2):** `scratchpad/draft-executable-dc-s4-prose-2026-08-09.md` (FORWARD 41's §4-prose plan, pre-v2.1) does NOT ride this plan; its re-base debt moves 2.3→2.4, re-stated at wrap beside FORWARD 41 — scheduled, not discovered. **This cycle runs REAL WALKS under v2.3** — the zero-walk batch form was scoped to evidence-backed batch codification, and this plan's subject is the bar itself. The cycle closes under the PRIOR (v2.3) bar; v2.4 governs only after it ships. The T2 cold panel carries the **prose-debt brief extension** (CEO decision 2026-08-11): seats read Rules 65–94 (v4.86) and the 36 v2.3 bullets AS DOCTRINE while diffing this rewrite against them — one panel discharges the standing debt and this cycle's own review.

## Scope — one doctrine file, ten script-applied edits; one scoped 12-row flip; nothing else

- **Edits exactly ONE existing file:** `/Users/marklehn/Developer/GitHub/DRAFTING_CYCLE.md` via the GIVEN builder (§ APPENDIX A): E1 the bar rewrite (the single 3,593-char paragraph → seven paragraphs), E2 §2.8's oscillation clause re-base (`finding count stops trending toward dry` → `instruction-class count stops trending toward zero`), E3 §2.7's tracking-line sweep bullet (278), E4 §3's judged-stop exception re-base (both splits), E5 §3's worked judged-stop form (both splits), E5b the §3 worked-Closing exhibit re-based to the bar's own form (seat 5 — the third exhibit on the line E4/E5 edited, unswept until the capstone), E8 the §3 dry-form example carrying the per-class number, E9 the §3 gate-span sentence's Forward-45 currency marker, E6 version 2.3 → 2.4, E7 History row. **All-or-nothing; an anchor failure writes zero bytes.**
- **One DB write:** a single scoped `UPDATE` flipping all 12 ids `reference|backlog → implemented|codify` — one predicate serves both vintages (same prior status/route, same target values); per-group sentinels name a mismatch's vintage.
- **No code edit; no LESSONS.md touch; no FORWARD register touch by any step.** FORWARD 53's status flip is Planner-direct Rule 42 at wrap.
- ⚠️ **§6 coordinate-doctrine-and-gate — A REAL COUPLING EXISTS THIS TIME AND IS DEFERRED EXPLICITLY:** `plan_lint`'s §4 checks match per-lens lines and fold tokens, not the bar's wording — measured: `fold-introduced`, `instruction-class`, `origin split` all return 0 in `plan_lint.py` and `gates.py` — **but the WARN text at `plan_lint.py` check (f) describes the closing condition in prose that after this rewrite understates it** (the 332-precedent class: an understatement, not a false check). The gate half is deliberately deferred per §6's "explicitly defer and say so": a bellows FORWARD row is owed for the check-(f) prose refresh plus any `instruction 0 / record N` line-form support — **emitted at wrap by PLANNER-DIRECT edit to `bellows/knowledge/FORWARD.md`** (the 334/45/50 precedent — Rule 42 authorizes status flips only and Rule 44's channel is receipt-bound, so a new wrap row is Planner-direct; the Rules-42/44 new-row gap itself is a wrap corpus item, panel seat 4). **The row text, pinned verbatim so the wrap copies rather than composes:** *"DRAFTING_CYCLE v2.4 re-based §2's bar to touch-classification (s2-rewrite plan): plan_lint check (f)'s WARN prose now UNDERSTATES the closing condition, and the `instruction N / record M` line form has no lint support. Owed: check-(f) prose refresh + optional per-class line-form check. Owner: bellows plan_lint. | deferred-work | <plan-id> | open"* §4's check SET is unchanged; nothing false fires on a correct v2.4 plan (verified: the closing-fold WARN keys on fold tokens, which the new Closing-line form still carries).
- ⚠️ **THE STALE-HAZARD GUARD RE-KEYS FOR THIS BATCH:** these rows are `reference|backlog`, not `accepted` — `accepted|codify` is 0 and stays 0. G1 asserts **`PRE=12`**, **`PRE_OLD=7`**, **`PRE_NEW=5`**, and corpus-wide **`RB=18`** (`reference|backlog` total, queried 2026-08-11: the 12 plus exactly `161, 169, 291, 294, 299, 301`). Any below its pin → HALT naming the group.
- ⚠️⚠️ **FREEZE CHECKLIST — executed at the deposit path, in order, before the copy (the last-edit-least-reviewed class, mechanized):** (1) inject § APPENDIX A programmatically from the COMMITTED builder at `governance/knowledge/research/builder-s2-rewrite-2026-08-11.py` (per-phase committed beside the draft — seat 3's HIGH: the mechanism must not live only in session tmp) and prove the round-trip extract byte-identical; (2) re-token the OPERATIVE placeholder sites — the A1 pin value and the appendix stub — to the live post-final-fold doctrine sha and the injected builder respectively; then assert by REGION, not file-wide (the checklist and the walk-0 record legitimately mention the placeholder tokens by name — a file-wide zero is unearnable, panel seat 2): the A1 task's line contains a 64-hex sha and no angle-bracket token, the appendix fence contains a python shebang and no stub token, **and the Closing line contains `instruction` and `/ record` with no italic-parenthetical placeholder form (seat 5: earnable — fails against the placeholder today; without it a freeze executed exactly as written deposits an unclosed record)**; (3) re-read `id_sequence` (authoring prediction 347) **and SUBSTITUTE the read id at the two `<id>` sites — the bootstrap line and TASK F's `-m` string — then assert by region: each contains the numeric id and no angle-bracket token (seat 5: the read had no consumer). When copying the pinned FORWARD row at wrap, fill its `<plan-id>` token likewise;** (4) final `plan_lint` at the deposit-shaped path, WARN set matching the Conformance paragraph exactly; (5) re-run the builder dry-run against the then-live doctrine file and confirm numstat and every post-condition pin unchanged — a foreign in-window doctrine edit shows up here, not at dispatch.
- ⚠️ **Doctrine lands in the REAL governance root; QA doc-integrity rows are the only guard; absolute operands; placement = script, verification = Bash.** Verdict-window posture: v2.4 governs from the Step-1 commit; HALT holds it live; rollback is CEO's.
- ⚠️ **Expected `plan_lint` state at deposit:** recorded in the Cycle Log's Conformance paragraph, measured at the deposit-shaped path.
- **Rule 93 (mandate-observer pairing) — declared blanket form:** Step-1 mandates are observed collectively by QA row 2's every-post-condition-probe re-run plus the named per-row observers (G3→row 6(a), capture→row 6(c), commit→rows 1/4); the inline-tag form is waived with this line as the declaration, per the rule's visible-at-writing-time purpose; **the constructed-violation half discharged at authoring for the named pairs** — the category-baseline violation was constructed live (row 6(a) without G3's column is unfalsifiable — measured before the three-site fold), the capture-loss violation is 334's measured crash class row 6(c)'s fallback exists for, and the wrong-repo commit violation is the thrice-measured cd-drift class Rule 85's assert observes.

### ⚠️ Environment facts — observed, not predicted

1. `grep` is a ugrep shim: **`-F` for every literal**; a zero-match `grep -c` prints `0` and exits 1 — **the printed count is the assertion; never `&&`-chain zero-count probes** (the SCRIPT post-condition `stops trending toward dry` → 0 is exactly such a probe: it prints 0 and exits 1 on SUCCESS).
2. Shell state does NOT persist between commands — create and use scratch dirs in the same invocation.
3. zsh aborts on an unmatched glob — use `find`, never a glob.
4. The DB is **gitignored and absent from any worktree**: a bare relative `sqlite3 lessons-forge.db` silently CREATES an empty file. Canonical absolute path only.
5. ⚠️ **Both prior-value exclusions again, and BOTH vintages are present in ONE flip:** the seven old rows carry `2026-08-09T01:20:01Z` (Z-form, **GLOB-matching** — the exclusion is what guards them) and the five new rows carry `2026-08-11T13:42:09+00:00` (offset, structurally non-matching, excluded uniformly). Re-measured on the actual 12 rows 2026-08-11.

---

## The edit content — GIVEN in § APPENDIX A; measured deltas

**Dry-run [EXECUTED 2026-08-11, re-run after every culmination — current as of panel seat 4's folds — swept each culmination]: numstat `21 7` after the seat-1 folds (was `19 5` through walk 5 — the panel added E8/E9).** Coherence sweep on the current dry-run result: `stops trending toward dry` 1 → 0; `predominantly fold-introduced` 2 → 2, both survivals correct (the bar's described-retirement; the immutable v2.0 row); earnable probes all 0 pre-edit (`zero instruction-class findings`, `DEMOTED to a diagnostic`, `convergence clock RESETS`, `converged before its account of itself`, `sweeps the record lines that track it`, `14 of 19 confirming-pass findings at exec-330`, `**Version:** 2.4 (2026-08-11)`); carried tokens `N of M fold-introduced` 1 → 1 and `instruction 0 / record` 0 → 4 occurrences (3 lines); `2.3 (2026-08-11)` 2 → 1; §2-intro paragraph blocks 3 → **9**; History bullets 13 → **14**.

## Conflict Ledger — run-time constraints

- **C1** — every anchor script-asserted before mutation; all-or-nothing. **C2** — version swap surgical; the retired-condition phrase is quoted in exactly ONE new location — the BAR's resolution parenthetical (§2 ¶3), NEVER the History row — so QA row 3's exactly-2 (bar + immutable v2.0 row) is earnable and correct (panel seat 2 disambiguation). **C3** — doctrine before DB. **C4** — backup adjacent, `BK=12` restorability on every path reaching the flip (`?immutable=1` read — the 334 lesson: `-readonly` FAILS on a WAL backup with error 14 and reads as corruption). **C5** — flip scoped `WHERE id IN (<the 12>) AND status='reference' AND route='backlog'`. **C6** — `CHANGES=12`, `GLOBOK=12`. **C7** — in-statement timestamp. **C8** — in-transaction capture; QA baseline is the deposited capture. **C9** — path-scoped commit + name-only assert. **C10** — busy_timeout; locked = HALT. **C11** — per-edit post-conditions. **C12** — order: A0 → A1 → SCRIPT → E0 → DOC_SHA → F → F2 → B → G1 → G2 → G3 → deposits. **C13** — History earns its past tense at G2; A0 state 2 completes the half-state. **C14** — serialized dispatch stated. **C15** — the re-keyed guards: `PRE=12 / PRE_OLD=7 / PRE_NEW=5 / RB=18`. **C16** — the 12-id list immutable.

---

## How to Run This Plan

**Bootstrap prompt:**
```
Read the plan at knowledge/decisions/in-progress-executable-348.md (the daemon renames the deposited placeholder on claim). Execute Step 1 ONLY. After completing Step 1, STOP and wait for my confirmation.
```

Step 1 (DEV) → verdict gate → Step 2 (QA). ⚠️ **HALT ROUTING:** Step 1 reads this plan, the live doctrine file, the canonical DB. Step 2 reads this plan, the dev-log, the live doctrine, the DB (read-only), the merged captures, `RULE_20_SELF_CHECK_BLOCK.md`. Unreadable capture = HALT (vs the row-6 DECLARED FALLBACK).

---
---

---

## Drafting Cycle

**Tier:** T2 — triggers fired: T-6 (governance surface: the doctrine file + the corpus), T-8 (novel: the bar's own semantics change). Clone lineage: mechanism from **executable-346** (newest same-class; builder + two-value exclusion), flip machinery cross-checked against **halted-executable-334** (the immutable-URI backup lesson, the PARTIAL mapping, the route-write form). **This cycle runs REAL WALKS under the v2.3 bar** — the zero-walk deviation does not apply to a rewrite of the bar itself.

**Walk 0 (context pin, §2.0):** committed at `governance/knowledge/research/walk-register-s2-rewrite-2026-08-11.md`. Walk-0 pinned sha `98c9c255…` — ⚠️ relabelled at panel seat 1: that blob is v2.2 (commit 983136a), not the v2.3 the register first claimed; every non-sha walk-0 measurement was re-confirmed against live v2.3 (`87126289…`) by the seat and the author, and the dispatch pin `<S2_PIN_SHA>` at A1 is re-tokened from the live file at freeze; the bar is ONE 3,593-char line (the 710-char-line class at 5×, itself an argument for the rewrite's paragraph split); anchors unique per the builder's asserts; the 12 rows' two vintages re-measured; provenance: line 38 last written by 343/334 (both closed).

**Walks:** 5 (this line swept at every walk-adding culmination from walk 5 on — its own staleness at four walks was walk 5's only finding, the tracked-structure class 278's sweep clause names, on the plan that ships the clause).
- Weak spots:          w1 3 folded — instruction 3 / record 0, all pre-existing (a QA constant carried as measured without measurement — true values 9/3 vs the claimed 7/2; the RB survivor set narrated not queried; freeze placeholders unguarded → the FREEZE CHECKLIST).
- Destruction:         w1 2 folded — instruction 1 / record 1, all pre-existing (the old bar's measured noise-floor citations dropped, restored; the origin split's numeric form re-mandated).
- Vulnerabilities:     w1 0 folded — check-(f)'s matcher executed at source; the deferred-gate claim verified (token profile unchanged by E5).
- Integration-record:  w1 1 folded — record, pre-existing (walk-0 register row 3 restated from the site list).
- ACID:                w1 dry (post-fold re-derivation of atomicity, order, and the C15/survivor-set consistency).

**Walk-1 split: instruction 4 / record 2; origin 6 of 6 pre-existing** (all v0-authoring, zero fold-introduced — expected on a first walk). Under the governing v2.3 bar the walk is not record-class-only, so the cycle continues; walk 2 owed.

**Direction verdict (after walk 1): PROCEED** — no forcing finding; reasoning in the walk register.

**Walk 2** (whole artifact; new surface = walk 1's folds):
- Weak spots:          w2 1 folded — instruction, fold-introduced (the Step-1 probe table lacked probes for walk-1's fold-added text; the earnable exec-330-citation probe added, `N of M` noted carried-not-earnable).
- Destruction:         w2 dry (walk-1 folds relax nothing; freeze checklist adds guards; the in-paragraph additions leave the 9-block shape and numstat intact — re-measured).
- Vulnerabilities:     w2 dry as findings — the full pin sweep re-EXECUTED on the post-fold dry-run: all twelve pins stable, paragraphs 9, History 14, numstat `19 5` across three runs.
- Integration-record:  w2 1 folded — record, fold-introduced (the measured-deltas paragraph had gone stale against walk-1's additions; refreshed to the current dry-run with the carried/earnable distinction stated).
- ACID:                w2 dry (fold interactions checked: the probe addition rides QA row 2's re-run-independently reference automatically; the freeze checklist's step-5 re-run is the standing guard against builder/draft drift).

**Walk-2 split: instruction 1 / record 1; origin 2 of 2 fold-introduced** — the noise-floor profile at minimal counts. Under the v2.3 bar an instruction-class finding re-opens the walk; walk 3 owed.

**Walk 3** (whole artifact; attention rotated to the step bodies and QA rows within full coverage):
- Weak spots:          w3 2 folded — instruction 2 / record 0, both pre-existing (QA row 6(a)'s "category preserved" had NO BASELINE anywhere in the evidence chain — the capture excludes the 12 and G3's projection lacked the column; fixed as one three-site constraint: G3 carries `category`, the read-back format pins all 12 `governance_rule` against the authoring measurement, row 6(a) asserts it. And row 2's 9/3 paragraph pin lacked its extraction command — v4.86 Rule 61 ext., proposal 240, codified this morning and applied to this plan; the shipped one-liner VALIDATED against the dry-run, prints 9).
- Destruction:         w3 dry (the projection widening relaxes nothing; the fallback path reads no category and is unaffected).
- Vulnerabilities:     w3 executed — the 12 rows' categories measured (12/12 `governance_rule`), the entry-id mapping re-derived exact (225 230 238 239 250 251 263 267 270 284 294 300), the row-2 command run against the dry-run.
- Integration-record:  w3 dry as findings (Cycle Log and register synced in the same culmination per the tracking-line sweep clause this plan ships).
- ACID:                w3 dry (the three-site constraint verified consistent: projection, read-back format, QA row — one fold, all sites named).

**Walk-3 split: instruction 2 / record 0; origin 2 of 2 pre-existing.** Under the v2.3 bar the walk re-opens; walk 4 owed. *(Specimen note: the new bar would read this walk identically — instruction-class > 0 re-opens under both bars.)*

**Walk 4** (whole artifact; new surface = walk 3's folds; the §5 first run):
- Weak spots:          w4 3 folded — instruction 2 / record 1, all pre-existing (⚠️ HIGH: both Scope blocks in a form the parser reads as ZERO entries — caught by the linter's FIRST run, not by three walks of lenses; G3's invocation described rather than spelled, now verbatim per lineage; the 286-cadence breach itself, repaired by running the battery and recording it).
- Destruction:         w4 dry (the Scope conversion is content-identical and strictly tighter — step 2 now enumerates the four files it previously gestured at; nothing relaxed).
- Vulnerabilities:     w4 executed — lint run twice (fail → earned pass); builder dry-run re-run, numstat `19 5` stable, all pins unchanged.
- Integration-record:  w4 dry as findings (Conformance paragraph filled with the real history including the breach; log and register synced same-culmination).
- ACID:                w4 dry (the Scope form change proven compatible with the deposit gates by the lint's own (b)/(d) PASSes).

**Walk-4 split: instruction 2 / record 1; origin 3 of 3 pre-existing.** The walk re-opens under both bars; walk 5 owed. ⚠️ **The honest read: walks 3 and 4 keep finding PRE-EXISTING instruction defects — the artifact is not yet at the bar, and the linter found in one command what the lenses missed in three walks.**

**Walk 5** (whole artifact; new surface = walk 4's folds; battery per culmination):
- Weak spots:          w5 dry as instruction — G3's exact command EXECUTED against the live DB (12 rows, 6-field shape, pre-flip values as expected); the row-5 tail probe verified earnable (0 in the live doctrine); header/routing/bootstrap re-read clean.
- Destruction:         w5 dry (walk-4 folds relax nothing; the Conformance paragraph replaced a placeholder and lost nothing).
- Vulnerabilities:     w5 executed — the full battery: lint EXIT 0 stable, builder dry-run numstat `19 5` stable.
- Integration-record:  w5 1 folded — record, fold-introduced (the Cycle Log's walk tracker read "1 so far" at four walks — the tracked-structure lag on the very plan shipping the sweep clause; swept, with the sweep obligation now stated on the line itself).
- ACID:                w5 dry (freeze-checklist/placeholder interplay re-derived; no fold interactions).

**Walk-5 split: instruction 0 / record 1; origin 1 of 1 fold-introduced.** ⚠️⚠️ **THE v2.3 BAR IS MET: record-class only ✓, predominantly fold-introduced (1 of 1) ✓, origin split stated as a number ✓.** The walk phase closes on a qualifying walk. Per v2.3: the closing-record re-read is owed at this close, and the T2 cold panel is NOT waived — both follow before deposit.

**Conformance (§5) — first run at walk 4, and the record of WHY it was late is part of the record:** the 286 residue-battery cadence (doctrine since v2.3, this morning) mandates lint after every culmination; walks 1–3 ran none — a process breach flagged by walk 4's own lens 1 and repaired by running it. **First run → EXIT 1, TWO FAILs: both step Scope blocks (the double-starred scope marker the deposit gates parse) parsed to ZERO entries** (v0 used a compact inline form the scope parser cannot read) — a deposit-blocking defect that survived three full walks and fell to one command, the exact measured lesson §5 carries. Post-fold → **EXIT 0**, TWO warnings, both classified: (1) missing cold-panel line — EARNED, the panel has not yet run (and the missing-lens WARN does NOT fire: the per-lens lines exist because the walks are real — the first cycle in this arc to clear that check earned); (2) `(o1)` on the conditional `resume-sweep.txt` — the lineage's standing benign class. ⚠️ **Then at panel seat 2, measured by execution: EXIT 0, ONE warning — the cold-panel WARN DISAPPEARED when seat 1's record entry landed.** The clear is EARNED (the panel is actually running; the line-anchored panel line now exists) — the §3/309 disappearance rule demands the explanation and this is it. The one standing warning is the `(o1)` conditional-path class. **Freeze step (4) matches THIS one-warning set.** Re-run after every culmination; the exit code at deposit rides the freeze checklist.

**Cold panel (T2):** convened after the walk-5 qualifying close and the closing-record re-read (re-read: clean — splits/register arithmetic verified; one probe-representation misfire in the re-read's own instrument, recorded). Sequential, author-verified and folded between seats; every seat carries the prose-debt extension.
- **Seat 1 (weak spots + evidence-attack): 12 findings — 3 HIGH / 4 MED / 5 LOW; touch split instruction 8 / record 4; all author-verified, all folded or dispositioned.** The HIGHs: (1) QA row 10 carried `13` for the heading census — the SAME wrong constant plan 346's QA corrected this morning, reproduced by clone (live value 11; eighth predicted-number instance); (2) the shipped bar's zero-instruction form silently resolved a split in its own evidence (308 says "nearly all", 275 says record-only) while its rationale asserted convergence at a value the rule rejects — the resolution is now STATED in the bar's parenthetical and the History row; (3) the plan's own record sections TRAILED after the last step, violating the 313 placement rule its sibling batch codified this morning — the Drafting Cycle block now sits above STEP 1, and the Conformance paragraph describes rather than quotes the scope marker. MEDs: 302's restructured-nothing clause restored to the bar (a bar-meeting walk containing a restructuring fold does not close); the §3 dry-form example re-based to carry the per-class number (E8); the walk-0 sha relabelled (it was the v2.2 blob mislabelled as v2.3 — non-sha measurements re-confirmed live); Rule-85 toplevel asserts added to all three commit compounds. LOWs: absolute gate paths (the seat's own probe mis-fired on a scripts/-relative gates.py — measured); the "roughly" hedge restored; the Rule-93 blanket waiver declared; §3's gate-span sentence gains its Forward-45 currency marker (E9); Rule 85's cd-first-vs-git-C doctrine tension recorded as a corpus item for wrap.
- **Seat 2 (destruction + clone-diff): 24 findings — 3 HIGH / 12 MED / 9 LOW; touch split instruction 14 / record 10; clone-diff verdict 11 DROPPED / 4 MIS-ADAPTED across 23 regions, 0 re-added-after-deletion; all author-verified, all folded or dispositioned.** The HIGHs: the freeze checklist's placeholder probes were UNEARNABLE (they exhibited their own literals — the 263 class inside the last-edit guard; now region-scoped); the seven 334 Task-M keys were claimed re-verified but never probed post-rewrite — two keys MEASURABLY VANISH under E1 (238, 247) while the flip stamps their proposals implemented (seven-key probe table added, two keys re-derived); the four INHERITED environment facts were compressed to an inexpandable label — the section carries a fifth, plan-specific one (restored verbatim — the draft's own `stops trending toward dry → 0` probe is the exit-1-on-success case fact 1 exists for). Majors: G1 now deposits the 12-row pre-flip state (the only raw record of a route-writing flip's pre-state); the EXACT-projection copy mandate (G2/G3 tails are swapped — a re-typed projection fails all 302 lines); the route-consumer half of QA row 9 restored; E0's five-name denylist restored; the survivor-set added-vs-missing asymmetry; receipt sentinels by name (eight, not "six"); the Conformance record refreshed to the MEASURED one-WARN state with the disappearance explained and the seat-1 claim struck by execution; register rows 3/5 strike-corrected; "culmination" now defined in the bar (prose-debt); Rule-85's tautology half and the final-ACID ambiguity routed to wrap as corpus items.
Builder now NINE edits, numstat `21 7`, all new probes earnable, lint EXIT 0 — ~~with the WARN set unchanged across the record edits~~ **struck (seat 2, by execution): the cold-panel WARN legitimately cleared when this very panel entry landed; the claim was written before its own edit's effect was re-measured — the class §3's re-lint rule exists for.**
- **Seat 3 (vulnerabilities, register-plain): 10 findings — 1 HIGH / 5 MED / 4 LOW; touch split instruction 7 / record 3; ~60 commands executed, EVERY plan-pinned expectation reproduced (zero mismatches), the full G1→G2→G3 sequence run on a scratch snapshot with constructed failure states (a pre-flipped row correctly fires G1 at PRE=11/PRE_OLD=6 AND G2's second net at CHANGES=11/GLOBOK=11; the missing-evidence-dir abort verified pre-UPDATE).** The HIGH: **the builder — the plan's entire mutating mechanism — existed only in volatile session tmp, committed nowhere**, while its record was per-phase committed; now committed at `governance/knowledge/research/builder-s2-rewrite-2026-08-11.py` and named as freeze step (1)'s source. MEDs: an unbalanced bold token in the shipped ¶5 (a seat-1 fold inserted `**…**` inside an already-bold sentence — 7 tokens; invisible to every count pin, found by a per-paragraph balance census; fixed in the builder, balance now even across all nine paragraphs); the History awk was cited as "pinned" while pinned nowhere (shipped verbatim, validated 13→14); two QA row-3 probes were described not spelled (spelled, validated); row 9(a) CANNOT observe the flip — `reference` is already terminal, so the entries are absent in both states (relabelled a preservation check; row 6(a) is the flip observer); the SCRIPT task's probe walls converted to ordered sub-items (Rule 94). LOWs: the Closing-line/check-(f) freeze interaction flagged (no change — step 4 is the guard); the Rule-93 blanket-waiver gap and Rule-85 assert-vacuity-under-`-C` confirmed as DOCTRINE defects → wrap corpus items; row 10's fenced-decoy annotation added. **The register-plain seat's fifth consecutive panel producing a HIGH no aimed brief found.**
- **Seat 4 (integration-record + clone-diff-record): 10 findings — 1 HIGH / 5 MED / 4 LOW; touch split instruction 5 / record 5; every record claim it tested reproduced by execution except the four defects it filed.** The HIGH: **the walk register declared schema 0.1 while its fold tables were UNCONFORMANT with the schema this very plan codifies conformance to** (6 columns vs the mandated 8; no `pre_fold_text`; unnoticed by five walks and three seats) — converted to the 8-column form with uncaptured pre-fold cells declared honestly and the per-phase commits named as the recovery source. MEDs: the panel record itself had spliced seat 3 mid-line into seat 1's bullet, presenting order 1-3-2 against the proven 1-2-3 (unspliced, execution order restored); the Task-M sub-item 7 had FUSED the AssertionError abort mandate onto its expected-count in the Rule-94 conversion (unfused, 346's standalone form restored); the deferred-gate FORWARD row cited Rule 42 which authorizes no new rows — the real mechanism (Planner-direct edit, 334 precedent) now named and the row text PINNED verbatim; the deltas currency marker was one culmination stale again (re-stamped, sweep obligation extended through the Closing). LOWs: the no-rename guard restored to both steps (dropped from both parents' FIRST paragraphs); 246's extended key flagged as modified; cd-first added to the worktree compounds (Rule 85's other half); the absolute Rule-20 path restored at the callout; the builder's ¶2 gate-span rationale given the same Forward-45 currency hedge as ¶7 (committed-builder edit, numstat stable at `21 7`). The Rules-42/44 new-row wrap gap joins the corpus items.
- **Seat 5 (ACID + system, the capstone — last by design, on the artifact as folded by all four): 9 findings — 0 HIGH / 5 MED / 4 LOW; touch split instruction 5 / record 4; 30+ mechanical pins re-reproduced, ZERO pin failures; never-landed-fold sweep CLEAN (every claimed fold of ~56 exists at its claimed site); cross-seat contradictions NONE. SYSTEM VERDICT: SAFE TO FREEZE AND DEPOSIT after the named fixes — all applied this culmination.** The fold-interaction pair only the last seat could see: §3's worked Closing exhibit still carried the v2.0-era form the new bar rejects (the THIRD exhibit on the very line E4/E5 edited — swept as E5b, builder now TEN edits, numstat stable `21 7`); and seat 1's 302 restoration beside resident 275 text left close-vs-reset precedence unstated (302 now takes precedence explicitly). The freeze-execution gaps: step 3's id read had NO CONSUMER (substitution + region asserts added; the pinned FORWARD row's `<plan-id>` fill noted) and nothing proved the Closing was written (an earnable Closing-form assert added — fails against today's placeholder by construction). The closure disposition (§2.6: panel findings re-open the walk) is stated in the Closing per the seat's own framing. Plus: the register's panel-fold gap and value-domain deviations declared honestly; A0 states 1/2 hardened (foreign-only-flip HALT; C11 re-run before the state-2 flip path); the seat-2 bullet's four-vs-five fact count fixed; and the ripple pin corrected to the OCCURRENCE form — `-c` was blind to two hits sharing a line, the 270 bullet applied to this plan's own pin.


**Closing:** judged stop under the governing v2.3 bar, taken at the panel's capstone and DECLARED with its reasoning (the §2.6 disposition seat 5 framed): the panel's findings re-opened the walk on the same terms as any lens pass, each seat read the artifact as folded by all prior seats, and seat 5's nine verified folds are the final culmination — every one applied from the seat's own explicit fixes and re-proven by the post-fold battery (dry-run numstat `21 7` stable, all pins reproduced in the occurrence-correct form, bold balance even, lint EXIT 0 at the one-WARN pin, never-landed-fold sweep clean across ~56 panel folds). Final culmination split: instruction 5 / record 4; origin 9 of 9 panel-introduced or panel-found (diagnostic). Residue enumerated by class: the seat-5 folds themselves are the deposit's unreviewed-by-a-fresh-reader edits — instruction 5: the E5b exhibit sweep, the 302-precedence clause, the freeze id-consumer, the Closing-form assert, the A0 hardenings; record 4: the register gap declarations, the occurrence-form pin, the count fix, this Closing. Each is execution-verified where a probe exists; none touches the flip machinery's guard chain, which seat 5 walked path-by-path unchanged. Closing-record re-read run on the final record: seat arithmetic 12/24/10/10/9 all internally consistent, register carries all five seat rows, walk splits recompute from the fold rows. Judged stop, fold-and-deposit exactly once.

---

## STEP 1 — DEV (run the builder, commit, flip the 12)

---

> **FIRST — post a short visible chat message confirming the start.** Do NOT rename this plan file (the daemon manages the `in-progress-` name; the RUN-EXE rename protocol does not apply to bellows dispatch — panel seat 4). You are the Developer. The doctrine edit lands at the ABSOLUTE root path; a HALT after edits leaves the tree as-is, reported loudly.
>
> **⚠️ TASK A0 — STATE CLASSIFICATION, first match wins:** (1) all 12 `implemented` → verify commit-by-slug `s2-rewrite-2026-08-11` — **NOT FOUND → HALT, a foreign-only flip state (seat 5)**, check deposits survived; a lost `outside-range-ids.txt` is unreconstructible — RECOVERY dev-log naming what is missing, **produce the G3 read-back FRESH (that one IS reproducible), and state in the receipt that QA row 6 takes its DECLARED FALLBACK** (without the fresh read-back, QA's any-❌-HALT dead-ends the recovery branch after the irreversible write — panel seat 2); report complete. (2) docs committed + 12 still `reference` → **re-run the C11 probe set against the live committed doctrine first (seat 5: this path reaches the irreversible flip on content never re-verified)**, then skip to B then G; DOC_SHA from the commit; reuse any `pre-s2-rewrite-` backup via prefix-only `find`; `BK=12` assert mandatory. (3) docs modified-uncommitted → HALT + probe-sweep table to `knowledge/qa/evidence/s2-rewrite-2026-08-11/resume-sweep.txt`; a PARTIAL signature means a foreign edit — say so. (4) fresh-with-unexplained-backup → HALT. (5) fresh — porcelain clean, version reads `2.3`, no backup, `COUNT(*) WHERE id IN (<the 12>) AND status='reference' AND route='backlog'` = **12** → A1. Version cross-check on every path (2.3, or 2.4-first-bullet-names-THIS-slug); no-match → HALT with the observed triple.
>
> **⚠️ TASK A1 — RE-VERIFY THE PIN:** `shasum -a 256 /Users/marklehn/Developer/GitHub/DRAFTING_CYCLE.md` == `87126289f1f0ea1c150e2b412ec53b53ae2dd7c75c00f8ff8e3630ef4f77cb07`. Mismatch → HALT. Anchor re-proof is the script's asserts.
>
> **TASK SCRIPT** — write § APPENDIX A VERBATIM to `knowledge/development/s2-rewrite-edits.py`, run exactly: `python3 <your-tree-abs>/knowledge/development/s2-rewrite-edits.py /Users/marklehn/Developer/GitHub/DRAFTING_CYCLE.md /Users/marklehn/Developer/GitHub/DRAFTING_CYCLE.md` (SRC and DST BOTH the live absolute path — single read, all asserts, single write). Assert exit 0 + `OK — 10 edits applied` naming all ten labels. ⚠️ **THE SEVEN 334 TASK-M KEYS, re-proven against the POST-rewrite doctrine as ORDERED SUB-ITEMS (Rule 94 — seat 3; two keys live in the line E1 replaces, and the flip stamps their proposals implemented). `grep -cF` each; a zero on ANY → HALT before Task F — the flip would stamp a proposal implemented while deleting its only implementing clause:**
> 1. 233 → `twenty-seven sections for roughly the token budget of one targeted round` → 1
> 2. 238 → **re-derived v2.4 key** `A pass whose findings are mostly its predecessor's fold damage remains` → 1
> 3. 246 → `the aimed pass returned 3 findings leaving twelve regions unreached` → 1 (334's pinned literal EXTENDED by the leading clause — flagged as modified, panel seat 4; both forms measure 1)
> 4. 247 → **re-derived v2.4 key** `ten of ten ACID passes each catching a culmination-introduced defect` → 1
> 5. 258 → `The record is rewritten more often than any other region` → 1
> 6. 259 → `a normal outcome, not a deviation` → 1 (survives verbatim)
> 7. 271 → `certifies everything except the paragraph that records it` → 1
>
> **Any AssertionError from the builder → HALT and quote it verbatim — the file is UNTOUCHED by construction; do not edit the script, do not retry with modifications** (standalone mandate, 346's form — panel seat 4 unfused it from key 7). Post-conditions (C11), all `grep -cF` on the live file: `zero instruction-class findings` → 2; `DEMOTED to a diagnostic` → 2; `convergence clock RESETS` → 1; `converged before its account of itself` → 1; `sweeps the record lines that track it` → 1; `stops trending toward dry` → **0**; `instruction 0 / record` → **4 occurrences via `grep -Fo 'instruction 0 / record' <file> | wc -l`** (line-count `-c` prints 3 — two hits share the worked-example line; the 270 occurrence-form bullet applied to this plan's own pin at seat 5); `**Version:** 2.4 (2026-08-11). Amended only through the Iteration Protocol` → 1; `2.3 (2026-08-11)` → **1**; `predominantly fold-introduced` → **2** (the described retirement + the v2.0 row — NOT zero, by design); `14 of 19 confirming-pass findings at exec-330` → **1** (the restored noise-floor citations, earnable — 0 pre-edit); `N of M fold-introduced` → **1** (carried token, NOT earnable — the prior bar also contains it, so this probe proves presence, never placement); History bullets via exactly `awk '/^## History/{f=1;next} f&&/^- \*\*/{n++} END{print n}' /Users/marklehn/Developer/GitHub/DRAFTING_CYCLE.md` → **14** (validated: prints 13 on live v2.3, 14 on the built v2.4 — seat 3).
>
> **TASK E0** — porcelain: expect `DRAFTING_CYCLE.md` modified. **HALT iff any of the OTHER governance doctrine files is dirty: `PLANNER_TEMPLATE.md`, `RULE_20_SELF_CHECK_BLOCK.md`, `READONLY_AUDIT_CONTRACT.md`, `SPECIALIST_TEMPLATE.md`, `INTERMEDIATE_DECISION_PHRASES.md`** (the enumerated set — 330 deliberately narrowed from all-root-md, which false-HALTs under parallel-terminal topology; the label form re-opened that judgement, panel seat 2). Any other root dirt reported, never a HALT. **TASK DOC_SHA** — pin before commit. **TASK F** — `git -C /Users/marklehn/Developer/GitHub add DRAFTING_CYCLE.md && git -C /Users/marklehn/Developer/GitHub commit -m "[348] s2-rewrite(s2-rewrite-2026-08-11): the bar reads TOUCH — cluster A landed, 334 Step 2 completed — doctrine 2.3 -> 2.4" -- DRAFTING_CYCLE.md && git -C /Users/marklehn/Developer/GitHub rev-parse --show-toplevel` (the toplevel print is Rule 85's assert — expect exactly the governance root; wrong or missing → NOT COMMITTED); numstat expect **`21	7`**, different → HALT. **TASK F2** — commit sha == DOC_SHA; name-only exactly the one file.
>
> **TASK B — BACKUP:** `.backup` to `/Users/marklehn/Developer/GitHub/lessons-forge/pre-s2-rewrite-$(date -u +%Y%m%d_%H%M%S).db`; locate via prefix-only `find`. ⚠️⚠️ **RESTORABILITY BY VALUE, every path reaching the flip, via IMMUTABLE URI** (the 334 seat-3 lesson — `-readonly` on a WAL `.backup` fails error-14 and masquerades as corruption): `sqlite3 -bail "file:<found-backup>?immutable=1" ".timeout 5000" "SELECT 'BK='||COUNT(*) FROM lesson_proposals WHERE id IN (233,238,246,247,258,259,271,275,278,292,302,308) AND status='reference' AND route='backlog';"` → **`BK=12`**, else HALT before the flip.
>
> **TASK G — rehearsal → flip → read-back.** No heredocs; `.sql` via file-WRITE; `-bail` + exit-0 + empty-stderr on every invocation (same-invocation scratch dir outside git trees); captures in YOUR tree — **`mkdir -p` the evidence dir FIRST** (sqlite `.output` cannot create directories; under `-bail` a missing dir aborts G2 BEFORE the UPDATE — that pre-commit exit-1 is the ONE state where fixing the dir and re-running G2 is correct; the never-re-run rule scopes to post-COMMIT states) — `.output` absolute from `pwd`.
>
> **G1 (`knowledge/development/s2-rewrite-flip-rehearsal.sql`), content exactly:**
> ```
> BEGIN IMMEDIATE;
> SELECT 'PRE='||COUNT(*) FROM lesson_proposals WHERE id IN (233,238,246,247,258,259,271,275,278,292,302,308) AND status='reference' AND route='backlog';
> SELECT 'PRE_OLD='||COUNT(*) FROM lesson_proposals WHERE id IN (233,238,246,247,258,259,271) AND status='reference' AND route='backlog';
> SELECT 'PRE_NEW='||COUNT(*) FROM lesson_proposals WHERE id IN (275,278,292,302,308) AND status='reference' AND route='backlog';
> SELECT 'RB='||COUNT(*) FROM lesson_proposals WHERE status='reference' AND route='backlog';
> SELECT 'MAXID='||MAX(id) FROM lesson_proposals;
> .output <your-tree-abs>/knowledge/qa/evidence/s2-rewrite-2026-08-11/pre-flip-state.txt
> SELECT id||'|'||category||'|'||status||'|'||COALESCE(route,'')||'|'||COALESCE(status_updated_by,'')||'|'||COALESCE(status_updated_at,'') FROM lesson_proposals WHERE id IN (233,238,246,247,258,259,271,275,278,292,302,308) ORDER BY id;
> .output stdout
> ROLLBACK;
> ```
> Assert **PRE=12, PRE_OLD=7, PRE_NEW=5, RB=18, MAXID=314**, plus the pre-flip capture holding **12 rows** all `governance_rule|reference|backlog` (the RAW pre-state record for a route-writing flip — the G2 capture excludes the flipped ids by design, so this is their only raw record; panel seat 2) (C15; any low count → HALT naming the group; RB>18 or MAXID>314 = in-window work, reported, bounds unchanged).
>
> **G2 (`knowledge/development/s2-rewrite-flip.sql`), content exactly** (`.output` from `pwd`):
> ```
> BEGIN IMMEDIATE;
> .output <your-tree-abs>/knowledge/qa/evidence/s2-rewrite-2026-08-11/outside-range-ids.txt
> SELECT id||'|'||category||'|'||status||'|'||COALESCE(route,'')||'|'||COALESCE(status_updated_at,'')||'|'||COALESCE(status_updated_by,'') FROM lesson_proposals WHERE id <= 314 AND id NOT IN (233,238,246,247,258,259,271,275,278,292,302,308) ORDER BY id;
> .output stdout
> UPDATE lesson_proposals SET status='implemented', route='codify', status_updated_at=strftime('%Y-%m-%dT%H:%M:%SZ','now'), status_updated_by='ceo' WHERE id IN (233,238,246,247,258,259,271,275,278,292,302,308) AND status='reference' AND route='backlog';
> SELECT 'CHANGES='||changes();
> SELECT 'GLOBOK='||COUNT(*) FROM lesson_proposals WHERE id IN (233,238,246,247,258,259,271,275,278,292,302,308) AND status_updated_at GLOB '20[0-9][0-9]-[0-9][0-9]-[0-9][0-9]T[0-9][0-9]:[0-9][0-9]:[0-9][0-9]Z' AND status_updated_at NOT IN ('2026-08-09T01:20:01Z','2026-08-11T13:42:09+00:00');
> COMMIT;
> ```
> **Sentinels: CHANGES=12, GLOBOK=12; either off → HALT with numbers.** Capture **302 lines** (314 − 12); the count is read post-commit — mismatch = record + HALT with the flip landed, never re-run. The 12-id list is IMMUTABLE (C16). ⚠️ This UPDATE writes `route` as well as `status` (backlog → codify) — 334's form, unlike the 344/345/346 flips; the read-back asserts all four columns.
>
> **G3 — READ-BACK** (`-readonly` is fine on the LIVE DB — the immutable-URI caveat applies only to the backup), the exact invocation: `sqlite3 -bail -readonly /Users/marklehn/Developer/GitHub/lessons-forge/lessons-forge.db ".timeout 5000" "SELECT id||'|'||category||'|'||status||'|'||COALESCE(route,'')||'|'||COALESCE(status_updated_by,'')||'|'||COALESCE(status_updated_at,'') FROM lesson_proposals WHERE id IN (233,238,246,247,258,259,271,275,278,292,302,308) ORDER BY id;"` → RAW to `flip-readback.txt` — all 12 `governance_rule|implemented|codify|ceo|<Z-form>`, none equal to either prior value. ⚠️ `category` rides the projection because QA row 6(a) asserts it against the AUTHORING pin (all 12 measured `governance_rule` 2026-08-11) — without it in the read-back, "category preserved" has no baseline anywhere in the evidence chain (walk-3 fold; the sites this constraint spans: this projection, the flip-readback format, QA row 6(a)).
>
> **Output Receipt** — DOC_SHA, the commit hash, the numstat pair, the sentinels BY NAME: PRE, PRE_OLD, PRE_NEW, RB, MAXID, CHANGES, GLOBOK, BK (a bare count is the count-in-prose class — an earlier revision said "all six" of what are eight, panel seat 2), every file deposited. **End with `### Ledger Updates` and `#### Prompt Feedback` — and `#### Forward Register`: the word `NONE`** (the owed FORWARD row is BELLOWS-owned and wrap-emitted; a habitual Step-1 emission files it into lessons-forge's register — the 334-measured wrong-register mechanism, panel seat 2). **FINAL ACTION — commit deposits in the worktree**, pathspec on the COMMIT, name-only assert, **with `cd <your-tree-abs>` as the compound's FIRST token and `git rev-parse --show-toplevel` printing your worktree root as its LAST (Rule 85, both halves)**. DB and backup never staged.
>
> **Scope:**
> - `knowledge/development/dev-log-s2-rewrite-step-1-2026-08-11.md`
> - `knowledge/development/s2-rewrite-edits.py`
> - `knowledge/development/s2-rewrite-flip-rehearsal.sql`
> - `knowledge/development/s2-rewrite-flip.sql`
> - `knowledge/qa/evidence/s2-rewrite-2026-08-11/outside-range-ids.txt`
> - `knowledge/qa/evidence/s2-rewrite-2026-08-11/flip-readback.txt`
> - `knowledge/qa/evidence/s2-rewrite-2026-08-11/pre-flip-state.txt`
>
> **Deposits:**
> - `lessons-forge/knowledge/development/dev-log-s2-rewrite-step-1-2026-08-11.md`
> - `lessons-forge/knowledge/development/s2-rewrite-edits.py`
> - `lessons-forge/knowledge/development/s2-rewrite-flip-rehearsal.sql`
> - `lessons-forge/knowledge/development/s2-rewrite-flip.sql`
> - `lessons-forge/knowledge/qa/evidence/s2-rewrite-2026-08-11/outside-range-ids.txt`
> - `lessons-forge/knowledge/qa/evidence/s2-rewrite-2026-08-11/flip-readback.txt`
> - `lessons-forge/knowledge/qa/evidence/s2-rewrite-2026-08-11/pre-flip-state.txt`
>
> ⚠️ `resume-sweep.txt` joins Scope on A0 state 3 only.

## STEP 2 — QA

---

> **FIRST — Do NOT rename this plan file. Deliverable Verification (Rule 8 / 17)**, table with ✅/❌, any ❌ → HALT. **Rule 20 self-check** (canonical block from `/Users/marklehn/Developer/GitHub/RULE_20_SELF_CHECK_BLOCK.md`, exact template; `plan_slug`: `s2-rewrite-2026-08-11`; `qa_report_path`: `<tree>/knowledge/qa/s2-rewrite-qa-2026-08-11.md`; `evidence_dir`: `<tree>/knowledge/qa/evidence/s2-rewrite-2026-08-11/`; `required_evidence_files`: `[doc-integrity.txt, db-invariants.txt, gate-neutrality.txt, pytest_targeted.txt]`; all four deposited BEFORE the block; literal stdout including the banner `Rule 20 — QA Self-Check Results` and the `PASSED — SELF-CHECK PASSED` line, byte-exact). Then `## Evidence and Narrative`. RAW evidence; ONE read-only DB form.
>
> **1. DOC INTEGRITY** — independent commit discovery by slug; three-way sha (commit == live == DOC_SHA; `e3b0c442…` = show-failed); porcelain empty; name-only exact. → `doc-integrity.txt`
> **2. THE REWRITE LANDED WHOLE** — every Step-1 post-condition probe re-run independently, PLUS the bar's structure: `grep -cE '^Walk the lenses'` → 1 (the rewrite's first paragraph starts where the old line started) and the paragraph shape asserted over the §2-intro span (between the `## 2. The Lens Register` heading and `### 2.0`): via exactly `python3 -c "import io;t=io.open('/Users/marklehn/Developer/GitHub/DRAFTING_CYCLE.md',encoding='utf-8').read();span=t.split('## 2. The Lens Register'+chr(10))[1].split('### 2.0')[0];print(len([p for p in span.split(chr(10)+chr(10)) if p.strip()]))"` (the pin ships its extraction command — v4.86 Rule 61 ext., proposal 240) → expect **9** post-edit, from **3** pre-edit (the bar's seven paragraphs replacing one line, beside the RE-DRAFT paragraph and the each-lens paragraph — **both sides MEASURED on the executed dry-run 2026-08-11**; an earlier revision of this row carried 7/2 as an unmeasured claim labelled measured, caught by walk 1 lens 1 running the count). → `doc-integrity.txt`
> **3. COHERENCE SWEEP** — `predominantly fold-introduced` → exactly **2** with locations CLASSIFIED (the §2 described-retirement + the v2.0 History row; any third location → HALT); `stops trending toward dry` → 0; `instruction 0 / record` → **4 occurrences via `grep -Fo 'instruction 0 / record' <file> | wc -l`** (line-count `-c` prints 3 — two hits share the worked-example line; the 270 occurrence-form bullet applied to this plan's own pin at seat 5); the §2.0/§2.7/§3 cross-references intact, probes SPELLED (seat 3 validated each against the built file): `grep -cF '### 2.0 Walk 0'` → 1; `grep -cF 'A walk covers, it does not target.'` → 1; `grep -cF 'alongside the per-class split (and the origin split as its diagnostic)'` → 1. → `doc-integrity.txt`
> **4. NUMSTAT** — exactly `21	7`. → `doc-integrity.txt`
> **5. VERSION + CHANGELOG** — v2.4 line probed; `2.3 (2026-08-11)` → 1; new row FIRST, names the slug, prior 2.3 row intact below; **tail probe** `grep -cF 'stated for the audit trail'` → 1 (measured 0 pre-edit — earnable); **PARTIAL-clause probe** `grep -cF "259's cross-plan-measurement middle ask"` → 1 (measured 0 pre-edit — earnable; proves the History row CARRIES the 258/259 PARTIAL naming the Why section promises, panel seat 2); History via the same shipped awk → 14. → `doc-integrity.txt`
> **6. FLIP + BLAST RADIUS** — (a) all 12 `implemented|codify|ceo`, Z-GLOB AND not-in both priors, **`category` = `governance_rule` on every row — asserted against the AUTHORING pin (all 12 measured 2026-08-11), read from G3's projection which carries the column for exactly this row's benefit**, and **route flipped backlog → codify on all 12** (the five-column read; this flip writes route, unlike 344/345/346). (b) `reference|backlog` corpus-wide → **6**, survivor set EXACTLY `161, 169, 291, 294, 299, 301` (queried 2026-08-11): assert the ids against this list — **an ADDED id is REPORTED with its value, never a halt** (G1 tolerates in-window `RB>18` as benign; halting QA on the same legitimate concurrency after the irreversible flip is the DEV-tolerates/QA-halts asymmetry, panel seat 2); **a MISSING id is a HALT** (a row leaving `reference|backlog` outside this plan's 12 is corpus interference). (c) re-run **the EXACT capture SELECT from G2's block — COPY it, never re-type it: G2's projection ends `…status_updated_at` then `…status_updated_by` while G3's ends the other way round, and a re-typed adjacent projection makes ALL 302 lines differ with a plausible wrong diagnosis (334's measured class, panel seat 2)**; first-line shape control (six `|`-fields), then diff vs the deposited 302-line file; partition (impossible-id → HALT; deleted-row → HALT; else concurrent, named, no halt). → `db-invariants.txt` ⚠️ DECLARED FALLBACK (capture lost): degraded sweep, `DEGRADED` label, co-stamp check on id 233's stamp including all 12.
> **7. TARGETED TESTS + PREMISE** — single-module premise re-derived; pytest vs 55/0 baseline; delta reported not asserted. → `pytest_targeted.txt`
> **8. GATE-NEUTRALITY + THE DEFERRED HALF** — (a) `fold-introduced`, `instruction-class`, `instruction 0` → 0 in both `/Users/marklehn/Developer/GitHub/bellows/scripts/plan_lint.py` and `/Users/marklehn/Developer/GitHub/bellows/gates.py` (⚠️ gates.py is at the bellows ROOT, not beside plan_lint — a scripts/-relative probe returns a confident false absence, measured by panel seat 1); (b) POSITIVE CONTROL `Drafting Cycle` in `plan_lint.py` → 11; (c) ⚠️ **confirm the closing-fold WARN still fires correctly on a v2.4-form plan**: the check keys on fold tokens in per-lens lines, which the new Closing-line form retains — quote the check-(f) region lines that read the Closing/lens lines and state that the key tokens are unchanged by this rewrite; **the check-(f) prose refresh is the DEFERRED gate half, owed via the wrap-emitted FORWARD row, and its absence today is declared, not silent.** → `gate-neutrality.txt`
> **9. CONSUMER SEMANTICS — BOTH written fields (this flip writes status AND route, panel seat 2):** (a) **preservation check, not a flip observer (seat 3: `reference` is ALREADY in `_TERMINAL_STATUSES`, so these entries are absent from the work list in BOTH states — the flip-observing assert is row 6(a)):** `lessons_forge.py:31` quoted; `get_unclassified_entries` → all 12 source entries (225, 230, 238, 239, 250, 251, 263, 267, 270, 284, 294, 300) ABSENT before AND after — guards linkage deletion only. (b) **the route consumer:** grep `route` in `/Users/marklehn/Developer/GitHub/lessons-forge/src/lessons_forge.py`, quote the reading site, and execute one route-reading query against the live DB confirming the 12 read `codify` where backlog-readers no longer see them. → `db-invariants.txt`
> **10. DOCTRINE PARSES** — `^## ` count unchanged (9 — one of which is the fenced `## Drafting Cycle` decoy inside §3's example, noted per the 253 strip-fences rule: a future edit inside that fence shifts this baseline); `^### ` count unchanged at **11** (measured live on v2.3 by panel seat 1 AND re-measured by the author — an earlier revision carried 13, the same wrong constant plan 346's QA corrected this morning, reproduced by clone; the rewrite adds paragraphs and bullets, zero headings). → `doc-integrity.txt`
>
> `## Evidence and Narrative`, Output Receipt, `### Ledger Updates` (Write/Edit, once, blank-line-terminated, one physical line per item). **`#### Forward Register`: the word `NONE`** — ⚠️ deliberate: the check-(f) FORWARD row is WRAP-emitted by the Planner because its owner is bellows and this plan's Receipt appends to lessons-forge's register (the emitter-owner match rule); FORWARD 53's status flip is Rule 42 Planner-direct. **FINAL ACTION — commit deposits**, pathspec + name-only assert, **cd-first and toplevel print per Rule 85, both halves**.
>
> **Scope:**
> - `knowledge/qa/s2-rewrite-qa-2026-08-11.md`
> - `knowledge/qa/evidence/s2-rewrite-2026-08-11/doc-integrity.txt`
> - `knowledge/qa/evidence/s2-rewrite-2026-08-11/db-invariants.txt`
> - `knowledge/qa/evidence/s2-rewrite-2026-08-11/gate-neutrality.txt`
> - `knowledge/qa/evidence/s2-rewrite-2026-08-11/pytest_targeted.txt`
>
> **Deposits:**
> - `lessons-forge/knowledge/qa/s2-rewrite-qa-2026-08-11.md`
> - `lessons-forge/knowledge/qa/evidence/s2-rewrite-2026-08-11/doc-integrity.txt`
> - `lessons-forge/knowledge/qa/evidence/s2-rewrite-2026-08-11/db-invariants.txt`
> - `lessons-forge/knowledge/qa/evidence/s2-rewrite-2026-08-11/gate-neutrality.txt`
> - `lessons-forge/knowledge/qa/evidence/s2-rewrite-2026-08-11/pytest_targeted.txt`

## APPENDIX A — `s2-rewrite-edits.py` (GIVEN; injected from the verified builder at freeze)

```python
#!/usr/bin/env python3
# Section-2 rewrite builder - DRAFTING_CYCLE v2.3 -> v2.4.
# All-or-nothing: single read, every anchor asserted, single write.
import io, sys

SRC = sys.argv[1]
DST = sys.argv[2]
s = io.open(SRC, encoding='utf-8').read()
edits_applied = []

def rep(old, new, expect=1, label=""):
    global s
    got = s.count(old)
    assert got == expect, f"ANCHOR COUNT {label}: expected {expect}, got {got} for {old[:60]!r}"
    s = s.replace(old, new, expect)
    edits_applied.append(label)

# ---------------- E1: the bar rewrite - the whole single-line paragraph at line 38,
# anchored by its unique opening (the full 3,593-char line is matched by prefix+suffix
# via the two unique fragments; the replacement is composed as SEVEN paragraphs).
lines = s.split('\n')
idx = next(i for i, l in enumerate(lines) if l.startswith("Walk the lenses **in order, one pass per lens per walk.**"))
OLD_LINE = lines[idx]
assert len(OLD_LINE) > 3000, f"bar line unexpectedly short: {len(OLD_LINE)}"
assert s.count(OLD_LINE) == 1

NEW_BAR = """Walk the lenses **in order, one pass per lens per walk.** Fold all accepted findings after each pass — that fold set is a **culmination**, the unit the residue-battery and newest-constraint cadences (§2.6, §2.8) key on. Re-run a lens only on a **subsequent** walk — a fold's defect is usually caught by a *different* lens on different evidence.

**The cycle is DONE when a full walk's findings, classified by the surface each TOUCHES, are all record-class — zero instruction-class findings.** An **instruction-class** finding changes what an executing agent DOES: a step's command, a guard, a post-condition, a branch, a value a gate acts on. A **record-class** finding changes only the artifact's account of itself: the Cycle Log, ledger rows, fold commentary, changelog prose. **Classify by the edit the fold would make, not by the sentence's location** — a Cycle-Log line a gate matches is instruction-class (the gate acts on it — §3's gate-span absorption, which holds until bellows Forward row 45's regex fix ships), and a rationale sentence inside a step that no agent or gate reads is record-class. The walk's per-class split is stated as numbers in the Cycle Log (`instruction 0 / record 5`). *(Measured, and the reason the bar reads TOUCH: one cycle's walk 3 changed instructions in roughly ten of fifteen findings, walk 4 in two of eight — the instruction surface was closing while the commentary surface was not, and structurally cannot, because every fold adds explanation and the explanation is itself reviewable by the next walk. A cycle steered by any whole-artifact count keeps finding work forever, all of it real, none of it changing what an agent does. Proposal 308; entry 300. ⚠️ A stated resolution, not an oversight: 308's remedy reads "nearly all record-class is done"; the bar adopts sibling 275's stricter record-ONLY form because "nearly" is the unauditable judgement the auditable-stop clause below exists to remove — under this bar the measured walk 4, at two instruction findings, signals the transition and does not itself close.)*

**The origin split (pre-existing vs fold-introduced) is DEMOTED to a diagnostic — it is no longer a condition of the bar.** Report it as numbers alongside the per-class split (`N of M fold-introduced` — the declared-once numeric form §2.7's count-in-prose rule requires), because it still says where findings come from — but it cannot tell a finishing cycle from a circling one: **the prior bar's "predominantly fold-introduced" condition and the section's own noise-floor warning were the same number read in opposite directions** (measured: 28% → 68% → 67% → 75% across four walks of one cycle, both readings applying from walk 2 on; bellows Forward row 53, resolved by this rewrite). A pass whose findings are mostly its predecessor's fold damage remains the **noise-floor signature** — a reason to suspect circling, never a licence to close (measured: 14 of 19 confirming-pass findings at exec-330 were the cycle's own fold damage, walk 4 running 0-for-3 pre-existing; 3 of 4 at exec-332; ten of ten ACID passes each catching a culmination-introduced defect).

⚠️ **A falling total finding-count is NOT the convergence signal, and no pass may be justified by naming an "unexamined region"** — after walk 1 there is none; a walk is every lens over the whole artifact (§2.7). A pass instead names the **new surface the last culmination created** and reports both splits of what it found. Budget for a cold panel's yield staying **flat**: a five-lens sequential panel returned 11 / 12 / 12 / 12 / 12 with no decay, roughly a third of each round being defects the immediately preceding round's folds introduced.

**After a RESTRUCTURING pass — a collapse, a promotion, a sub-step split — the convergence clock RESETS: the next walk is a FIRST pass over the new arrangement, never a confirming pass, and its finding count is not a convergence signal — and the bar is met only by a walk that restructured nothing: a bar-meeting walk that contains a restructuring fold does not close.** *(Proposal 302; entry 294.)* **A confirming pass is UNTARGETED by construction** — record decay hides from aimed passes precisely because attention follows what changed *(proposal 278; §2.7's covers-not-targets rule is the general form)* — and **its yield is reported by class: a confirming pass returning record-class only is the signature that the artifact converged before its account of itself did.** Close on it (absent any restructuring fold — the clock-reset rule above takes precedence); do not schedule another walk to confirm the confirmation. *(Proposal 275; entry 267.)*

**The last event before deposit is either a dry lens pass or a declared judged stop meeting the bar above — a judged stop is a normal outcome, not a deviation,** recorded with its reasoning. ⚠️ **An instruction-class finding RE-OPENS THE WALK:** the bar is unmet and the cycle continues. Folds made on a closing walk that DOES meet the bar are record-class by the bar's own condition; those landing in the closing record are read by the closing-record re-read (§2.7), and **any that land elsewhere are enumerated individually in the residue list** — the re-read covers the record, not the whole artifact, and must not be cited as though it did. ⚠️ **This is a stated relaxation, not an oversight:** a qualifying close may deposit with record-class edits no lens has read. On T2 the cold panel supplies that reader (§2.6 — the panel is not waived by a judged stop). **On T1 there is no such reader, so a T1 judged stop rests on the residue enumeration and the closing-record re-read alone.**

⚠️ **A judged stop is auditable or it is not a stop.** The touch classification is the author's own judgement, and the author is the party who wants to finish, so a bare assertion of record-class-ness is not a close — show the work. **The Closing line carries the per-class split as numbers and NAMES each residue finding's class in a clause apiece** (`instruction 0 / record 3: two count-word lags, one stale label`); the per-finding detail — what each was, where, which fold produced it, and the classification's reasoning wherever it is not obvious — lives in the committed walk register, which the closing-record re-read reads. ⚠️ **This is the ONE bounded exception to §3's compact-form rule, and §3 states it** — the bar cannot be audited from a log that may not name what it stopped on. Fold-and-deposit **exactly once**."""

rep(OLD_LINE, NEW_BAR, 1, "E1-bar-rewrite")

# ---------------- E2: section 2.8 oscillation clause re-base (stale against the new bar)
rep("or the per-lens finding count stops trending toward dry",
    "or the per-lens instruction-class count stops trending toward zero",
    1, "E2-s28-rebase")

# ---------------- E3: section 2.7 bullet - 278's tracking-line sweep clause
rep("- **Per-phase commits.**",
    "- **Any phase that completes a tracked structure sweeps the record lines that track it, in the SAME culmination.** A count, a status word, or a tally that tracks a structure decays the moment the structure moves — and the phase that moved it is the only one that knows. Sweeping later means a finding-driven pass must rediscover the lag, and record decay hides from aimed passes. *(Proposal 278; entry 270 — the sweep half; the untargeted-confirming-pass half lives in §2's bar.)*\n- **Per-phase commits.**",
    1, "E3-s27-tracking-sweep")

# ---------------- E4: section 3 judged-stop exception - re-base "alongside the origin split"
rep("names each residue finding's CLASS in a clause apiece on the Closing line, alongside the origin split.",
    "names each residue finding's CLASS in a clause apiece on the Closing line, alongside the per-class split (and the origin split as its diagnostic).",
    1, "E4-s3-exception-rebase")

# ---------------- E5: section 3 worked judged-stop form - both splits
rep("each per-lens line carries its origin split (`w4 3 folded — 1 pre-existing, 2 fold-introduced`)",
    "each per-lens line carries its per-class split with the origin split as diagnostic (`w4 3 folded — instruction 0 / record 3; 1 pre-existing, 2 fold-introduced`)",
    1, "E5-s3-worked-form")

rep("`**Closing:** w4 met the bar — 3 findings, all record-class (two count-word lags, one stale label), 2 of 3 fold-introduced;",
    "`**Closing:** w4 met the bar — instruction 0 / record 3: two count-word lags, one stale label; 2 of 3 fold-introduced (diagnostic);",
    1, "E5b-s3-closing-exhibit")

# ---------------- E6/E7: version + History
rep("- Weak spots:          w1 2 folded; w2 dry; w3 dry.",
    "- Weak spots:          w1 2 folded — instruction 1 / record 1; w2 dry; w3 dry.",
    1, "E8-s3-dry-form-example")

rep("the final QA step's gate span absorbs it, so a gate-matching string quoted in the log is evaluated as if the QA step had said it.",
    "the final QA step's gate span absorbs it (until the gate-span regex fix ships — bellows Forward row 45; the record-placement rule below is the authoring-side guard meanwhile), so a gate-matching string quoted in the log is evaluated as if the QA step had said it.",
    1, "E9-s3-gatespan-currency")

rep("**Version:** 2.3 (2026-08-11). Amended only through the Iteration Protocol",
    "**Version:** 2.4 (2026-08-11). Amended only through the Iteration Protocol", 1, "E6-version")

HIST = "## History\n"
ROW = ("- **2.4 (2026-08-11):** slug s2-rewrite-2026-08-11; the §2 doneness-bar REWRITE — cluster (A) of the 2026-08-10 gate "
"(proposals 275, 278, 292, 302, 308; entries 267, 270, 284, 294, 300), routed as one unit, landed as one coherent rewrite; corpus path proper, no §6 deviation. "
"THE BAR NOW READS TOUCH: done = a full walk with zero instruction-class findings, per-class split stated as numbers; the origin split is DEMOTED to a diagnostic, "
"resolving the self-contradiction bellows Forward row 53 recorded (the fold-introduced fraction was simultaneously the convergence condition and the noise-floor "
"signature — measured 28/68/67/75% with both readings applying). Restructuring passes reset the convergence clock (302); confirming passes are untargeted and "
"report by class, record-only yield = converged-before-its-account (275); §2.7 gains the tracking-line sweep clause and §2's bar the untargeted half (278); "
"292's static-artifact correction confirmed already carried by v2.0 text, ratio-reporting discipline retained; the record-ONLY form is 275's, adopted over 308's nearly-all wording as the auditable of the two (stated resolution, panel seat 1); the §3 dry-form example carries the per-class number and the gate-span sentence its Forward-45 currency marker. §2.8's oscillation clause re-based (finding-count-"
"toward-dry → instruction-class-toward-zero); §3's judged-stop exception and worked form re-based to carry both splits. "
"DB: proposals 275/278/292/302/308 flipped reference|backlog → implemented|codify, AND the seven rows of the 2026-08-09 cluster (A) — 233, 238, 246, 247, 258, 259, 271 — "
"flipped reference|backlog → implemented|codify, completing plan 334's held Step 2 on CEO release 2026-08-11 (the hold's condition, a real cycle or two under v2.0+, met "
"many times over; 258 and 259 flip PARTIAL per 334's mapping — 258's phase-labelling quarter and 259's cross-plan-measurement middle ask (D5, funnel-routed) remain "
"unbuilt and are recorded here rather than silently absorbed). Inheritors: every future cycle; the in-flight s2-rewrite cycle itself closed under the PRIOR bar (v2.3), "
"stated for the audit trail.\n")
rep(HIST, HIST + ROW, 1, "E7-history")

io.open(DST, 'w', encoding='utf-8').write(s)
print(f"OK — {len(edits_applied)} edits applied: {', '.join(edits_applied)}")
```
