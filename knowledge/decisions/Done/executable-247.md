# Lessons Forge — Cycle Run 2026-07-21 (ingest + classify the 12-entry 07-20/07-21 batch)
**Date:** 2026-07-21 | **Tier:** Medium | **Dispatch Mode:** bellows | **Test Scope:** both | **Execution:** Step 1 (Lessons Agent) → Step 2 (DEV) → Step 3 (QA) | **qa_steps:** 3 | **pause_for_verdict:** always

## CEO Context

**This is the first plan of a three-plan arc whose Gate 2 would produce PLANNER_TEMPLATE v4.77** (v4.76 → v4.77, *if* Gate 1 routes at least one entry to `codify` — not presupposed here). This plan runs the cycle only: ingest the un-ingested LESSONS.md entries and classify them into proposals. Gate 1 (route disposition) and Gate 2 (codification) are separate plans with CEO decisions between. Precedent: 243 → 244 → 246, which ran this exact shape one batch ago.

**The payload of this arc is the conflict-serializability lens** — whether the Drafting Cycle gains a **sixth** named lens or has its existing ACID/Isolation clause **widened** to cover multi-step schedules. That is a Gate 2 authoring call, not this plan's. This plan's job is to get the entry into the corpus with a `reasoning` field Gate 1 can dispose of.

**⚠️ THIS BATCH WAS PARTIALLY CONSUMED AS EVIDENCE BEFORE IT WAS INGESTED — the single most important thing about this cycle.** The prior Gate 2 (plan 246, commit `0a6932d`) cited **entry 99** ("Walk the lens list, don't re-run one lens to dry") as the evidence for proposal 159, and codified its substance into `## The Drafting Cycle` at v4.76. Verified against the live template: `PLANNER_TEMPLATE.md:341` now reads *"Walk the full lens list in order — one pass per lens per walk. A lens is re-run only on a subsequent walk of the entire list…"* — that IS entry 99's content, already shipped. The v4.76 changelog row names it explicitly ("evidence entry 99/C1"). **So this cycle will ingest and classify at least one entry whose substance is already law.** That is not a defect and not a reason to skip it — the corpus is the record, and an entry that shipped early still belongs in it. But it means **Gate 1's dedup-against-the-live-template is load-bearing this cycle in a way it has not been before**, and the likely disposition of entry 99 is `reference` (already-codified) rather than `codify`.

**Do NOT let Step 1 pre-dedup.** Classification does not dedup against PLANNER_TEMPLATE; Gate 1 dedups against the LIVE template (the 2026-06-07 discipline). Step 1's job is to classify entry 99 on its merits and **flag in the synthesis** that it may already be codified, so Gate 1 arrives at the question already holding it. A Step 1 that silently downgrades or skips entry 99 has made a Gate 1 decision without a CEO.

**Verified NOT already codified: entry 95, the conflict-serializability lens.** `grep -ic 'conflict-serializ' PLANNER_TEMPLATE.md` → **0** at authoring time. The only two `serializ` hits in the template are at `:1465-1467` in the Bellows dispatch model ("Serialize same-project plans") — a different concept, outside the Drafting Cycle section (`:314–:346`). The closest existing coverage is one clause inside the codified ACID lens — *"Isolation: what does a concurrent actor observe mid-operation?"* — which is scoped to a **single operation**, where conflict serializability examines the **multi-step schedule** across verdict-gate windows. **Record this in the synthesis as an open Gate 2 question; do not resolve it here.**

**The other ten entries' template coverage is UNMEASURED and is Gate 1's to establish.** I checked two entries against the template because the arc turns on them. I deliberately did not sweep the remaining ten — that is dedup, and dedup is Gate 1's. Do not read the two verified entries as implying anything about the other ten.

**Batch size is 12 — the largest this corpus has ingested in one cycle** (243 ran 5, verified from its Step-1 dev log; prior cycles ran 5–6). Parser-verified at authoring time: LESSONS.md holds **106** entries, exactly **12** have headings not present in `lesson_entries`, and `get_unclassified_entries()` returns **`[]`** — so post-ingest the work list is exactly these 12. Six are dated 2026-07-20 (the Gate-1 drafting experiment: conflict-serializability, plan-shape skip-rules, fix-sweep-siblings, open-lens-set, walk-the-list/C1, vacuous-git/C2) and six are dated 2026-07-21 (the Gate-2 cycle: parallelism-within-not-across, guards-observe-the-live-tree, resume-only-for-non-reproducible-work, manual-era-boilerplate, novel-lens-broken-mechanism, context-saturation).

**⚠️ A NUMBERING DISAMBIGUATION, because this plan uses "entry N" two ways.** In this CEO Context, **"entry 95 / 99 / 106" are LESSONS.md file-position numbers** (the baton's numbering: 95 = conflict-serializability, 99 = walk-the-list, 106 = context-saturation) — narrative pointers only. **"entry id 151" and "`entry_id > 151`" are DB row ids**, a different namespace (DB id 99 is an unrelated old orphan row). **The agent never uses the file-position numbers as DB ids** — Step 1 derives every DB id from `needs_classification` and refers to entries by heading/title, never by a hand-typed number. Keep the two straight when reading.

**⚠️ OPEN DESIGN QUESTION FOR THE CEO — batch size vs. context saturation.** Twelve classifications in one agent step is 2.4× the largest prior batch, and **the context-saturation entry is itself in this batch** ("when late walks go quiet, rotate the reviewer"). A single Step 1 that degrades over twelve entries would produce thin `reasoning` on the last few — and `reasoning` is the only thing Gate 1 reads. This plan keeps a single Step 1 with a mandatory mid-step checkpoint (below) plus a QA depth spot-check on the last-classified three (row 9). **The alternative is splitting classification across two steps at a verdict gate.** Flagged for the decision, not unilaterally settled — the single-step-with-backstop choice is defensible (243 classified 5 in one step cleanly) but 12 is untested; say the word to split it.

**⚠️ The `updated_count` watch — the plan-204 hash trap's live test, again.** Appending this batch gave the previously-last entry a trailing `---`, the exact trigger that used to flip a `content_hash` over whitespace and silently demote an `implemented` proposal. Verified at authoring time: LESSONS.md uses `^---$` between entries (**107** of them for 106 entries), so the trigger condition genuinely occurred. The hash-trap's target is **entry id 151** — *"2026-07-20: A drafting-cycle lens is not done at one pass…"*, `content_hash` `4f138100a107794c11563113a1833a05defe8190662d3af0239de1b4e28116e3`. **Expectation: `updated_count == 0` and `terminal_proposals_flagged` empty. Verify and report the ACTUAL values; do not force the number.** Non-zero either way is a loud finding — halt and report.

**A note on that target entry, because it is a trap of its own.** Entry 151 is the *within-lens iterate-to-dry* model, which v4.76 **REPLACED** with the walk-the-list form. Its DB proposal is `implemented`. **A superseded model is still `implemented`, not `stale` or `rejected` — do not "correct" it.** The corpus records what was codified when; supersession is Gate 2's changelog job, already done at `0a6932d`. If anything about entry 151 moves during this cycle, that is the 204 regression, not housekeeping.

**⚠️ The plan-154 dedup advisory is RETIRED — do NOT expect advisory lines.** Plan 207 removed `detect_recently_implemented_overlaps`, its helper, both call sites, the rendering, and 7 tests. Verified absent from `lessons-forge/src/` at authoring time (`grep -rn` → no hits). **Any `Recently-implemented overlap:` line in the generated report is a regression — halt and flag.**

**Specialist path-rot is FIXED — the in-plan path guard plan 243 carried is retired.** `lessons-forge/agents/FORGE_LESSONS_AGENT.md` was corrected at `7ce4218`; it now names `lessons-forge/src/lessons_forge.py` and `lessons-forge/lessons-forge.db` (verified by grep at authoring time). `Project: forge` remains and is intentional (a mode inside Forge), not rot. **One residual subtlety survives and is stated in Step 1: the specialist's paths are relative to the GitHub ROOT, and the agent runs from a lessons-forge worktree, where `lessons-forge/lessons-forge.db` resolves to nothing.** The absolute-path rule is what carries, not a correction to the specialist.

**No diagnostic precedes this plan, deliberately — surfaced for CEO veto.** The diagnostic-first workflow asks what is unknown; every unknown this plan would have asked about was measured directly at authoring time against live data — the parser delta (12), the G1 precondition (0 non-terminal proposals), the G3 duplicate expectation (0, by simulating the shipped detector over the actual batch), the suite baseline (55), the hash-trap target (id 151), and the route-scoping trap (130 pre-existing NULL-route proposals). This matches the shop's established practice for a recurring cycle: plan 243 — the direct precedent, one batch ago — also measured inline and deposited no separate diagnostic. A diagnostic here would re-derive numbers this plan already carries with its measurement method stated. If you would rather see a diagnostic deposit first, say so and I will split it out.

**Scope discipline:** cycle run only. Routes stay `NULL` at insert — the CEO assigns at Gate 1 via `set_proposal_route()`. **Do NOT edit PLANNER_TEMPLATE.md.** Codification is Gate 2 and is the one thing the record explicitly forbids doing outside the governed route. Do not pre-draft template wording in any deposit; a classification is not a codification.

**⚠️ Standing caution for the session: do NOT append to LESSONS.md while this plan is deposited-but-un-run.** The cycle parser-pins its batch at 12; a mid-flight append makes every count in this plan wrong and turns G6 into a halt.

**Deposit-once discipline:** deposited exactly once. If a second copy appears, that is a claim-dedup bug — do not double-claim.

**Authoring self-check (for the verdict gate).** `bellows/scripts/plan_lint.py` was run against this plan at authoring time: **exit 0**, checks (a)–(d) all PASS (header parsed, dispatch_mode bellows, pause_for_verdict always, all three steps' Deposits and Scope resolved, QA banner pair present). It emits the **same two known-benign `scope_check`-on-tests WARNs** plan 243 carried — "step 1 mentions tests but declares no test scope" (Step 1 quotes a "named test" from a lesson entry it classifies) and "step 3 mentions tests…" (Step 3 is verification-only and names no `test_*.py`, because modifying one would be wrong). Exit 0 is the pass; the WARNs are expected. **Do NOT add test files to any step's scope to silence them** (memory `benign-gate-failure-classes`).

## How to Run This Plan

**Bootstrap prompt:**
```
Read the plan at lessons-forge/knowledge/decisions/executable-lessons-forge-cycle-2026-07-21.md. Execute Step 1 ONLY. After completing Step 1, STOP and wait for my confirmation. Do NOT proceed to Step 2.
```

---
---

## STEP 1 — Lessons Agent

---

> **FIRST — before any reads or work: post a short visible chat message (1-2 sentences) confirming you are starting this plan and your immediate next action.** Do NOT rename the plan file.
>
> You are the Forge Lessons Agent. Read your specialist file at `agents/FORGE_LESSONS_AGENT.md` first (ADR-002 six-value taxonomy). **Its module and DB paths are CORRECT as of `7ce4218` — but they are written relative to the GitHub root, and you are running in a worktree.** `lessons-forge/lessons-forge.db` resolves to nothing from where you stand. **Every canonical-DB access in this plan uses the ABSOLUTE path** `/Users/marklehn/Developer/GitHub/lessons-forge/lessons-forge.db`. **`forge/forge.db` is a REAL but DIFFERENT database (the forge prompt-workshop) — never open or write it.**
>
> **Single-writer assumption.** This plan writes to the one canonical DB and is the only thing that should. Before Step 1a, confirm no daemon or other session is mid-cycle: `get_unclassified_entries` should be stable across two reads a moment apart, and no `in-progress-*lessons*` plan should exist in `knowledge/decisions/`. If either check suggests a concurrent writer, HALT — a second cycle running against the same corpus is exactly how a proposal gets staled out from under you.
>
> **Working location — read carefully, this is the plan-225 trap.** Run commands from **your own working tree** and write every file there. **The ONLY exception is canonical-DB access, which uses the ABSOLUTE path above** — the worktree has no DB copy and that is never a reason to substitute. Do NOT `cd` to the main tree to run the cycle: `generate_lessons_report` and the deposit writes all resolve RELATIVE paths, so a main-tree cwd puts output in main while you commit in the worktree, which is exactly the untracked-file collision that tore down plan 225's Step 3.
>
> **Scope:**
> - `knowledge/development/classifications-summary-2026-07-21.md`
> - `knowledge/development/dev-log-cycle-step-1-2026-07-21.md`
>
> **The expected batch is the twelve un-ingested LESSONS.md entries** — the six dated 2026-07-20 and the six dated 2026-07-21. **Parser-verified at authoring time** (`parse_lessons_md` on the live file vs the canonical DB): the file holds 106 entries, exactly **12** have headings not yet in the DB, and `get_unclassified_entries()` returns **`[]`** — so post-ingest the work list is exactly these 12.
>
> **⚠️ The corpus row count is 151, NOT 106 — do not read that as corruption.** `lesson_entries` holds **151** rows while the file has 106 entries; the surplus are orphan rows from headings reworded over the project's history (a heading change inserts a new row and leaves the old one). All 151 are classified (that is why `get_unclassified_entries()` is `[]`). **After this cycle expect 163 rows (151 + 12), not ~118.** A reader who assumes "106 + 12" will see 163 and cry corruption — it is expected.
>
> **Confirm all of this against what the ingest actually returns; if `ingested_count` is not 12, or the work list is not exactly these twelve headings, halt and explain the delta rather than adjusting to match this text.**
>
> **Step 1a — take a restore point, then capture the pre-cycle baseline. Before touching anything.**
>
> **Back up the canonical DB first — with `.backup`, NOT `cp`.** The canonical DB has a live WAL (`lessons-forge.db-wal` exists at authoring time), so a filesystem copy of the `.db` file alone would silently miss any un-checkpointed pages and produce a subtly stale restore point. Use SQLite's own backup, which checkpoints correctly.
> **⚠️ Write the backup to the MAIN tree, by ABSOLUTE path — a worktree-local backup is destroyed by teardown, which is when you would need it.** This plan runs worktree-isolated (lessons-forge cycles do — plan 225), so a relative `data/backups/` resolves inside your worktree and vanishes at teardown along with the restore point. Target the main tree explicitly and `mkdir -p` that same absolute directory: `mkdir -p /Users/marklehn/Developer/GitHub/lessons-forge/data/backups && sqlite3 "file:/Users/marklehn/Developer/GitHub/lessons-forge/lessons-forge.db?mode=ro" ".backup '/Users/marklehn/Developer/GitHub/lessons-forge/data/backups/lessons-forge-pre-cycle-<UTC timestamp>.db'"` (colon-free stamp). The `.gitignore` matches `*.db` (verified at `.gitignore:6`), so it will not be committed — confirm with `git -C /Users/marklehn/Developer/GitHub/lessons-forge status --porcelain` showing the backup absent. **State the absolute backup path in your dev log.**
>
> **Then capture the baseline.** Read from canonical and record verbatim in your dev log: proposals grouped by `status`, proposals grouped by `category`, total `lesson_entries` count, and the id + `content_hash` of **the entry that is currently last in LESSONS.md among those already ingested** (identify it by file position, then look it up — do not take `MAX(id)` and assume the two agree). **Authoring-time measurement, to be confirmed not assumed: that entry is id 151**, *"2026-07-20: A drafting-cycle lens is not done at one pass…"*, hash `4f138100a107794c11563113a1833a05defe8190662d3af0239de1b4e28116e3`. That entry is the hash-trap's target: appending this batch gave it a trailing `---`. Step 3 verifies the 204 watch against these figures and cannot do so if you skip this. **Capture BEFORE the ingest — after is worthless.**
>
> **⚠️ ALSO capture two FIXED pre-cycle constants, in the same baseline block: `E0 = SELECT MAX(id) FROM lesson_entries` and `P0 = SELECT MAX(id) FROM lesson_proposals`.** Authoring-time values (confirm, do not assume): **`E0 = 151`, `P0 = 159`.** If either differs from the stated value on a fresh run, the corpus changed since authoring — **HALT and report** (every downstream count in this plan is derived from these). **These are FIXED constants for the whole cycle, not values to re-derive.** ⚠️ **On a RESUME, use the recorded constants — read `E0`/`P0` from the prior dispatch's Step-1 deposit, or if that deposit never landed (the G5 deposit-completion resume) fall back to this plan's stated `E0 = 151` / `P0 = 159`. Do NOT re-run `MAX(id)` on a resume,** which after a partial run returns the post-ingest/partial-classification maxima (e.g. 163 / 165) and would silently exclude this cycle's own rows.
>
> **Why these matter: `entry_id > E0` (=151) is the canonical, resume-invariant handle for "the twelve entries this cycle ingested."** Entry ids are assigned once at ingest and never change; the twelve batch entries permanently occupy ids 152–163 no matter how many dispatches the classification takes. **`route IS NULL` is NOT a usable handle and is a trap:** at authoring time **130 pre-existing proposals already carry `route IS NULL`** (routes were only ever assigned to the last two cycles' 29 proposals), so `WHERE route IS NULL` selects 142 rows, not this cycle's 12 — `route IS NULL` is only ever asserted as a *property* of an already-scoped set, never used to scope. **State `E0` and `P0` on their own line in the Output Receipt** — Step 3 reads them from there; a missing baseline makes rows 3, 8, and 9 unverifiable.
>
> **⚠️ PRECONDITION (G1) — the fresh-run safety.** The ingest's update path stales **non-terminal** proposals when an entry's hash changes; plan 204's `_TERMINAL_STATUSES` guard protects only `implemented`/`rejected`/`superseded`/`reference`. So `proposed`, `accepted`, and `ambiguous` proposals are destructible when their entry is updated. **Measured at authoring time: ZERO of those three statuses in the corpus** (status distribution: implemented 110, superseded 28, rejected 15, reference 3, stale 3) — which on a fresh run makes the ingest provably non-destructive. **Confirm from your own baseline. On a FRESH run, any `proposed`/`accepted`/`ambiguous` proposal voids the safety argument — HALT before the ingest.** The one exception is a **resume**; see G1 for the full disambiguation. Do not proceed on the strength of this plan's measurement; the number is from authoring time, and the corpus is the thing that may have changed.
>
> **Step 1b — run the ingest.** Open canonical **read-WRITE** — a plain `sqlite3.connect("/Users/marklehn/Developer/GitHub/lessons-forge/lessons-forge.db")`. **Do NOT reuse a `?mode=ro` handle here** — those are only for the Step 1a baseline snapshot and the `.backup`; the ingest INSERTs, and a read-only connection makes it fail. Then call `run_full_lessons_cycle(conn)` (its `lessons_md_path` defaults to `/Users/marklehn/Developer/GitHub/LESSONS.md` — confirm that is the path it read, do not assume). **`conn.commit()` after it returns**, before classifying — the commit-after-cycle pattern proven on plans 205/226/228/243.
>
> **⚠️ "Commit" means `conn.commit()` — the SQLite transaction — NOT a git commit.** `run_full_lessons_cycle` does not commit internally (its docstring says the caller is responsible), and the DB is gitignored, so there is nothing to `git commit` and no `.md` deposit exists yet. Skip `conn.commit()` and a step death LOSES the ingest.
>
> **⚠️ Understand what you are running: this is a WHOLE-CORPUS operation, not a tail append.** `run_full_lessons_cycle` parses **all** of LESSONS.md and re-hashes **every** entry, then upserts. The twelve new entries are merely the expected delta. Any edit anywhere in that file since the last cycle — a typo fix, a reflow, a separator shift — lands in this same write and shows up as `updated_count`. That is why G4 is a real integrity watch, not a formality.
>
> **Record and report the ACTUAL returned values** for `ingested_count`, `updated_count`, `unchanged_count`, `duplicates_marked_count`, `terminal_proposals_flagged`, and `needs_classification`. Do not paraphrase them into a narrative — print the dict.
>
> ## Step 1 gates — G1 through G6
>
> **These are GATES, not advice.** Report **every** gate as a row in a table in your dev log, each with its measured value and `PASS` or `HALT` — including the ones that pass. A gate you do not report is a gate you did not run. **Rule 54, codified at v4.76 from this corpus, is why these are numbered gates rather than prose:** every instruction bound to a numbered row, a named test, or a gate is honoured; every instruction living only in prose is dropped.
>
> **Gate timing:** G2 is **pre-ingest** (Step 1a). G1 is pre-ingest **on a fresh run**; if the baseline is non-empty G1 defers its verdict until after the (no-op) ingest so it can distinguish a fresh-run violation from an expected resume. G3–G6 read the Step 1b return dict on the already-committed result and are HALT-and-report watches: they surface an unexpected outcome for the CEO, not a rollback.
>
> **G1 — non-terminal proposals precondition (FRESH-run gate; exempt on resume).** G1's real guarantee is that the ingest stales no live proposal — which holds when no *updated* entry has a non-terminal proposal. On a **fresh** run: the Step 1a baseline must show zero `proposed`/`accepted`/`ambiguous` proposals; any such proposal → **HALT before the ingest**. **On a RESUME, G1 is satisfied by construction, NOT violated.** A resume (G5 `PASS (resume)`) means the batch is already ingested and this run's ingest updates nothing — G4's `updated_count == 0` proves the ingest staled nothing — so `proposed` proposals left by an interrupted classification are EXPECTED and do not trip G1. Concretely: if the baseline shows non-terminal proposals, do not halt immediately — run the (no-op) ingest and check G5/G4 first; halt on G1 only if this is a fresh run (`ingested_count == 12`) with unexpected pre-existing non-terminal proposals.
>
> **G2 — LESSONS.md provenance.** Before the ingest, confirm the source file is committed and pin what you read: `git -C /Users/marklehn/Developer/GitHub status --porcelain -- LESSONS.md` must be **empty**, and record `git -C /Users/marklehn/Developer/GitHub rev-parse HEAD`. **Use `git -C <absolute root>` — do NOT rely on cwd.** `lessons-forge` is a submodule and LESSONS.md does not exist inside it; a bare `git status -- LESSONS.md` from your worktree reports clean because the file is *absent*, which is a vacuous pass, not a verification. **This batch contains the entry that names this exact failure class** (2026-07-20, `qa-discipline`: a `git status/diff -- <file>` from a submodule or worktree passes VACUOUSLY) — do not let this plan's own gate fall to the lesson it is ingesting. A dirty LESSONS.md means the corpus would record content that exists in no commit → **HALT**. Expected at authoring time: clean, root HEAD `41d63c4`; report the actual.
>
> **G3 — `duplicates_marked_count` is zero.** This is the silent-drop guard. `run_full_lessons_cycle` inserts duplicate proposals at `status='proposed'` and only THEN computes `needs_classification` via `get_unclassified_entries`, which excludes any entry holding a non-stale proposal — so a duplicate-marked entry **disappears from the work list with no error**. **Measured at authoring time by simulating the shipped detector over this exact batch:** criterion 1 (tag overlap) is a no-op because PLANNER_TEMPLATE.md contains no `**Tag:**` lines, and criterion 2 (case-insensitive substring of the heading text after the first ` — `) is clean on all twelve. Expected value: **0**. Non-zero → **HALT** and name exactly which entry IDs were marked and why — a dropped amendment entry would silently defeat this whole arc.
>
> **G4 — `updated_count` is zero and `terminal_proposals_flagged` is empty.** A pure-append cycle expects both. Non-zero either way → **HALT** and diagnose — do NOT prejudge it as a regression. Report **which entry IDs updated** and show each diff, classifying it as one of: **(a) whitespace-only** — the plan-204 hash-trap has regressed (a trailing-`---` flip should be normalized away); or **(b) substantive** — someone edited an existing entry's body, which is a *legitimate* re-ingest but is unexpected for this cycle and the CEO must confirm it was intended. Either way HALT with the evidence.
>
> **G5 — there is work to do.** `ingested_count == 0` **AND** `needs_classification` empty → *probably* nothing to do — but **before halting, disambiguate two states that look identical here.** (a) **Truly nothing to do:** no proposals exist for this cycle's twelve batch entry IDs → **HALT** and report. (b) **Classification done, deposits not landed:** proposals for the twelve DO exist AND the Step 1 deposits are absent from the tree → this is a **deposit-completion resume**: do NOT re-classify and do NOT halt as nothing-to-do; **re-generate the Step 1 deposits from the committed DB state and commit them**, then STOP for verdict. **If `ingested_count` is 0 but `needs_classification` is NON-empty, that is a legitimate resume** — record it as `PASS (resume)` and proceed. **A `PASS (resume)` here is also what clears G1's deferred verdict.**
>
> **G6 — work-list reconciliation.** The derived work list matches the entries the ingest just inserted; any divergence is reconciled per the DB-wide note below. `needs_classification` unexpectedly larger than the batch → **HALT** (an older entry surfacing means a prior proposal went `stale` — the plan-204 signature). **On that HALT the CEO chooses one of: (i) classify the batch AND the extra entry this cycle; (ii) classify the batch only and DEFER the extra entry to a later cycle; (iii) investigate the stale first, classify nothing.** If the CEO chooses (ii), record the deferred entry ID(s) explicitly in the Step 1 deposit under a heading `### Deferred entries (CEO-approved)` — Step 3 reads exactly that list and will otherwise treat any residual unclassified entry as a failure. **Re-entry mechanics:** because Step 1 STOPs at this HALT, the CEO decision arrives on a re-dispatch; the ingest is already committed, so the resumed Step 1 hits G5's `PASS (resume)` path, classifies only the batch, and writes the deferred-entries heading — it does not re-ingest.
>
> **After the gate table: if every gate reads PASS (or `PASS (resume)` on G5), continue to classification. Any HALT — stop and report; the ingest stays committed (G1 made it safe) and the CEO decides.**
>
> **Derive the work list yourself** from `needs_classification` (Orchestration Rule #47 — never a hand-copied list). Do NOT hand-type entry IDs from this plan; this plan deliberately names none.
>
> **⚠️ `needs_classification` is DB-WIDE, not scoped to what this cycle ingested** — its docstring says so, and it returns any entry whose only proposals are `stale` as well as any entry with none. So the work list can legitimately be LARGER than the batch. **Reconcile the two explicitly and report the reconciliation:** list the entry IDs the ingest inserted, list the work list, and name any entry present in the work list but NOT in this batch. Do **not** silently classify such an entry as part of this cycle, and do **not** silently skip it — **halt and report it** under the halt discipline above.
>
> ### Classification contract
>
> For each entry ID in the work list: read `id, source_heading, raw_content, tags, entry_date` from `lesson_entries`, apply the ADR-002 taxonomy, and call `insert_proposal(conn, ...)`. **All FOUR of these are REQUIRED positional args (the function has no defaults for them) — supply every one:**
> - `category` — one of `structural`/`instrumentation`/`governance_rule`/`language`/`narrative` per ADR-002 (do NOT hand-assign `duplicate` — the cycle function owns that).
> - `suggested_action` — a concrete natural-language recommendation (what should change and where). Omitting it is a `TypeError`.
> - `reasoning` — cites specific `raw_content` text from that entry (see the evidence rule below).
> - `confidence` — one of `low`/`medium`/`high`.
>
> **Set `target_layer` and `target_artifact` per the specialist file** — a `governance_rule` amendment gets `target_layer='governance'`, `target_artifact='PLANNER_TEMPLATE.md'`, exactly as proposals 149–159 do (DB-verified). **Only `route` and `subcategory` stay `None`:** `route` is the Gate 1 CEO disposition, and `subcategory` is Phase-2-reserved. Use `status='ambiguous'` (the default is `proposed`) only for a genuine no-fit. ⚠️ Do not conflate the two: leaving `target_layer`/`target_artifact` NULL would contradict both the specialist file and every prior cycle's proposals. **Do NOT dedup against PLANNER_TEMPLATE** — Gate 1 dedups against the LIVE template; your job is classification only.
>
> **⚠️ COMMIT INCREMENTALLY, because this is the largest batch this corpus has run.** A single `conn.commit()` after all twelve inserts means a step death at entry 11 loses eleven classifications; twelve is the most work this shop has ever had at risk in one classification step. **`conn.commit()` at the mid-step checkpoint (after the 6th `insert_proposal`) and again after the LAST classification** (whatever the work-list length — do not hard-code "12th"; a resume classifies only the remainder), before the git-commit of the `.md` deposits. This is safe and composes with the resume ladder: `get_unclassified_entries` excludes any entry that already holds a non-stale proposal, so a resume after a partial commit re-derives a work list of only the un-classified remainder (G5 `PASS (resume)`; G1's deferred verdict is cleared by `updated_count == 0`). Incremental commit strictly reduces worst-case loss and changes none of the gate logic. **Then git-commit the two `.md` deposits** once classification is complete.
>
> **After the final commit, self-report the proposals this cycle created.** Print `SELECT id, entry_id, status, category FROM lesson_proposals WHERE entry_id > <E0>` (the pre-cycle max entry id = 151 you fixed in Step 1a — the resume-invariant handle) ordered by `id`. Expected: exactly twelve rows, one per batch entry, every `entry_id` in 152–163. **Also** run `SELECT COUNT(*) FROM lesson_proposals WHERE id > <P0>` as a cross-check — it should equal 12 in the normal case; a value >12 means a proposal was created for an older entry (a G6-approved older-entry classification), which must already be named in your summary. Put both in the Output Receipt — this is the stated set Step 3 rows 3 and 9 reconcile against rather than re-deriving one.
>
> **⚠️ MANDATORY MID-STEP CHECKPOINT — after the 6th classification (and its commit), before the 7th.** This batch is twelve entries, 2.4× the largest prior cycle. Post a short visible chat message stating how many you have classified, and re-read this classification contract before continuing. **The reason is in the batch itself:** one of the entries you are classifying is *"Context saturation is a reviewer failure mode — when late walks go quiet, rotate the reviewer, not the lens."* A degraded second half produces thin `reasoning`, and `reasoning` is the only field Gate 1 reads. **If at any point you cannot cite specific `raw_content` text for an entry's `reasoning`, STOP and report rather than writing a generic justification** — a thin proposal is worse than a halted step, because it passes every gate.
>
> **`reasoning` is quoted evidence, not free narration.** Every proposal's `reasoning` must cite specific text from that entry's own `raw_content` — quote it, and name the entry ID it came from. Gate 1 disposes proposals by reading this field and does not re-read the source entry; a `reasoning` that paraphrases, embellishes, or imports a claim the entry does not make becomes a false premise wearing the authority of a completed classification. **Do not carry any assertion from this plan's CEO Context into a `reasoning` field as if the entry said it** — the entry is the only source. This matters more than usual here, because the CEO Context above makes substantive claims about two of the entries.
>
> **If any entry classifies `ambiguous`, call it out by ID at the TOP of your summary with the specific reason.** An ambiguous proposal carries no route, so Gate 1 has nothing to dispose and the arc stalls on it.
>
> **Disk-verify every filesystem claim in your SUMMARY (Rule 52).** This applies to the analytical claims in your classification summary and synthesis — distinct from the `reasoning` DB field, which quotes `raw_content` only and by construction makes no filesystem claim. If any summary claim asserts a file, function, or section exists, does not exist, or moved, verify it against disk with `ls`/`grep`/`git log` BEFORE writing it, and state in the summary that you did.
>
> ### Three things to surface in the synthesis, none of them to resolve
>
> **1. The already-codified entry.** One entry in this batch ("Walk the lens list, don't re-run one lens to dry") had its substance codified into `PLANNER_TEMPLATE.md:341` at v4.76 (`0a6932d`), where it was cited as the evidence for proposal 159. **Classify it on its merits anyway; do not skip or downgrade it.** Flag in the synthesis that Gate 1 must dispose of it against the LIVE template, and that `reference` is a plausible route. **Verify the codification claim yourself against the live template before repeating it** — do not assert it on this plan's say-so.
>
> **2. The conflict-serializability lens placement question.** One entry proposes a new Drafting Cycle lens that models the plan's DB operations as a transaction schedule. **The open question — sixth named lens, or a widening of the existing ACID lens's Isolation clause — is a Gate 2 authoring call.** Record it in the synthesis as an open question with the evidence on both sides drawn from the entry's own text. Do not decide it.
>
> **3. The batch's internal cluster structure.** Several entries amend the same template section (`## The Drafting Cycle`). Name the cluster so Gate 1 can route them coherently rather than as scattered edits — the same service plan 243's synthesis provided for the 155-159 cluster.
>
> **Deposit:** `knowledge/development/classifications-summary-2026-07-21.md` — the raw cycle dict, count, category/confidence distribution, per-entry reasoning, and cluster synthesis for CEO Gate 1, including the three items above. Plus `knowledge/development/dev-log-cycle-step-1-2026-07-21.md` with an Output Receipt containing, each as its own labelled line so Step 3 can read them mechanically: (1) the cycle dict verbatim; (2) total classified and the category/confidence distribution; (3) the derived work list; (4) the full G1–G6 gate table; (5) the **pre-cycle baseline** — proposals by status, proposals by category, `lesson_entries` count, and the recorded boundary entry's id + `content_hash`; (6) **`E0` and `P0`** (the two fixed pre-cycle maxima); (7) the **self-reported created-proposal list** (`entry_id > E0` rows) and the `id > P0` cross-check count; (8) the absolute backup path; (9) any flags. **Items 5, 6, and 7 are load-bearing for Step 3 rows 3/4/8/9 — a receipt missing them makes those rows unverifiable.** Canonical Python file-write pattern — no heredoc. Commit both. In `### Ledger Updates` include `#### Prompt Feedback`.
>
> **Deposits:**
> - `lessons-forge/knowledge/development/classifications-summary-2026-07-21.md`
> - `lessons-forge/knowledge/development/dev-log-cycle-step-1-2026-07-21.md`
>
> **STOP. Do NOT proceed to Step 2. Wait for CEO verdict.**

---
---

## STEP 2 — DEV

---

> **Before starting, read the Step 1 deposits and confirm Output Receipt status Complete; otherwise halt and report.** Post a short visible chat message confirming you are starting Step 2.
>
> You are the Forge Developer (`/Users/marklehn/Developer/GitHub/forge/agents/FORGE_DEVELOPER.md` — verified present at authoring time; skip with a note if absent).
>
> **Working location — same rule as Step 1.** Run commands from **your own working tree**; canonical DB by ABSOLUTE path is the only exception. Do NOT `cd` to the main tree. **Open your own connection** — this is a fresh step, no `conn` carries over from Step 1. `generate_lessons_report` only reads the DB and writes a `.md` file, so open read-only: `sqlite3.connect("file:/Users/marklehn/Developer/GitHub/lessons-forge/lessons-forge.db?mode=ro", uri=True)`.
>
> **Scope:**
> - `reports/lessons-report-2026-07-21.md`
> - `knowledge/development/dev-log-cycle-step-2-2026-07-21.md`
>
> **Before generating: check whether `reports/lessons-report-2026-07-21.md` already exists. If it does, HALT and report — do not overwrite it.** `generate_lessons_report` writes that path unconditionally, so a Step 2 re-run after a partial failure silently destroys the first run's report, which is the only record of what that run produced. Verified at authoring time: no 07-21 report exists (the most recent is `lessons-report-2026-07-20.md`), so the expected state is "absent".
>
> Run `generate_lessons_report(conn, "2026-07-21")` against the canonical DB.
>
> **Two halt conditions, both regressions rather than noise:**
> 1. All route values are NULL this cycle — the report must render without route lines (plan-128 conditional render). If any `- **Route:**` line appears, **halt and flag**.
> 2. **If any `Recently-implemented overlap:` advisory line appears, halt and flag.** Plan 207 retired that detector entirely; its output reappearing means the retirement regressed. Do NOT tune or reinstate it.
>
> `generate_lessons_report` resolves `output_dir="reports"` RELATIVE to cwd — which is why the working-location rule above is load-bearing here rather than boilerplate. Run `pwd` before the call, capture the **path the function RETURNS**, and state that resolved absolute path in your dev log. **Confirm the returned filename matches the file named in Scope above; if the function's naming convention differs, report the actual name rather than renaming the file to match this plan.**
>
> Print the report head (~80 lines) for the transcript.
>
> **Deposit:** the report plus `knowledge/development/dev-log-cycle-step-2-2026-07-21.md` with an Output Receipt (report length, proposals surfaced, route-line count, advisory-line count). Canonical Python file-write pattern — no heredoc. Commit both. In `### Ledger Updates` include `#### Prompt Feedback`.
>
> **Deposits:**
> - `lessons-forge/reports/lessons-report-2026-07-21.md`
> - `lessons-forge/knowledge/development/dev-log-cycle-step-2-2026-07-21.md`
>
> **STOP. Do NOT proceed to Step 3. Wait for CEO verdict.**

---
---

## STEP 3 — QA

---

> **Before starting, read the Step 1 and Step 2 deposits and confirm both Output Receipt statuses Complete; otherwise halt and report.** Post a short visible chat message confirming you are starting Step 3 (QA).
>
> You are Lessons Forge QA. Read `agents/FORGE_LESSONS_AGENT.md` first. **Working location — same rule as Steps 1 and 2:** run commands from your own working tree; canonical DB by ABSOLUTE path is the only exception. **Verification + reporting only — no product-code changes.** If a test fails, **report it — do NOT fix it**. If you find a blocker, STOP and report it. Do NOT use Monitor. Do NOT edit PROJECT_STATUS directly; route it via the receipt.
>
> **MANDATORY — Rule 20 self-check banner.** Your deposit MUST contain, verbatim, a section headed exactly `## Rule 20 — QA Self-Check Results` followed (anywhere below it) by a line reading exactly `**PASSED — SELF-CHECK PASSED**`. End with a self-grep confirming the banner is present in your deposited report.
>
> **Evidence-source rule.** Every SQL/PRAGMA row states which DB it ran against. Canonical reads use `sqlite3 "file:/Users/marklehn/Developer/GitHub/lessons-forge/lessons-forge.db?mode=ro" "<query>"`. Worktree DB absence is never a substitution reason. Deposit **RAW command output, never a summary of it** — a hand-written summary is not acceptable evidence.
>
> **Scope:**
> - `knowledge/qa/cycle-qa-2026-07-21.md`
>
> Verification table, one row per claim, each with a DB-source column:
> 1. **Full suite** — `python3 -m pytest src/ -v` (`python3 -m pytest`, NOT the `timeout` binary — unavailable on macOS) to an explicit pass/fail, raw tail shown. **Compute the baseline yourself from `--collect-only` and reconcile it against the most recent prior QA report in `knowledge/qa/`; do NOT carry a number forward from any plan text, including this plan's.** (For reconciliation only: the 2026-07-20 cycle QA recorded 55.) Report the actual and confirm 0 regressions.
> 2. `get_unclassified_entries(conn)` on canonical returns `[]`. **The one legitimate non-empty result is a G6 deferral:** if Step 1's deposit contains a `### Deferred entries (CEO-approved)` heading, the row passes iff the residual list equals exactly those deferred IDs — quote both the deposit heading and the query output. Any residual entry NOT on that list, or a non-empty result with no such heading, is a **FAIL**.
> 3. **Invariants** on canonical. First fix the scope: **"proposals created this cycle" = `WHERE entry_id > <E0>`, reading `E0` (=151 at authoring) from Step 1's Output Receipt.** This is the resume-invariant handle — entry ids never move, so it selects exactly this cycle's twelve batch-entry proposals regardless of how many dispatches ran. ⚠️ Do NOT scope by `route IS NULL` — 130 pre-existing proposals already carry NULL routes, so that predicate selects ~142 rows, not this cycle's 12; quote the `E0` value you used. Over exactly that scoped set (expect 12 rows): dangling proposals 0, invalid category 0, invalid confidence 0, and every one has `route IS NULL` (asserting the property, having scoped by entry_id). **Also verify the target fields WERE set:** every non-`ambiguous` proposal in scope has `target_layer IS NOT NULL` and `target_artifact IS NOT NULL`, and any `governance_rule` proposal carries `target_layer='governance'` + `target_artifact='PLANNER_TEMPLATE.md'`. A non-ambiguous proposal with NULL target fields is a **FAIL** — the requirement went unenforced. **This `entry_id > E0` scope is exactly 12, always** (each of the twelve batch entries is classified once; no batch entry gets two proposals, and a G6-approved older-entry classification has `entry_id ≤ E0` and so falls entirely OUTSIDE this scope — it never inflates or deflates the 12). **A scoped count ≠ 12 is a FAIL:** <12 means a batch entry went unclassified or the ingest dropped one; >12 means a batch entry got a duplicate proposal. Reconcile against Step 1's self-reported list. (Any G6 older-entry proposal shows up only in the separate `id > P0` cross-check reading 13+, and must be named in Step 1's summary.)
> 4. **The 204 fix held (the hash-trap regression watch).** Read the **pre-cycle baseline table Step 1 deposited in its dev log** — proposals by status, proposals by category, entry count, and the recorded entry's `content_hash`. If that baseline is missing from the Step 1 deposit, **halt**: this row is unverifiable without it and a post-hoc count proves nothing. Re-read the same figures from canonical now and diff them. Confirm `stale` has NOT grown, no proposal moved off a terminal status, and the pre-existing entry hash Step 1 recorded is unchanged. Report `updated_count` and `terminal_proposals_flagged` as Step 1 recorded them. If anything was demoted, halt loudly.
> 5. **Report** exists, proposal counts match DB, zero `- **Route:**` lines, **zero `Recently-implemented overlap:` lines**, and `detect_recently_implemented_overlaps` is still absent from `src/` (plan 207 retirement intact).
> 6. **No schema drift** — `.schema lesson_entries` / `.schema lesson_proposals` on canonical vs `src/db.py` DDL. This plan changes no schema. Any delta is a FAIL.
> 7. **PLANNER_TEMPLATE.md is UNCHANGED by this arc.** ⚠️ **This row is the ONE place you must reach outside your working tree, and it is a deliberate second exception to the working-location rule.** `lessons-forge` is a **submodule**; `PLANNER_TEMPLATE.md` is tracked by the **root** repo and does **not exist** in your worktree — a plain `git diff -- PLANNER_TEMPLATE.md` from where you are will find nothing and pass vacuously. **This batch contains the lesson that names that exact failure** (2026-07-20, `qa-discipline`, vacuous submodule git). Run it against the root repo explicitly: `git -C /Users/marklehn/Developer/GitHub diff --exit-code -- PLANNER_TEMPLATE.md`, and show the exit code. Exit 0 is the pass; any diff is a FAIL. (Self-contained deliberately: do NOT assert what version the header "should" read — an eyeballed version string is not evidence.) Codification is Gate 2; this cycle must not have touched the template.
> 8. **Post-cycle DB counts** — entries total, proposals by status and category, stated as actuals. **Expected entries total is 163** (151 pre-cycle + 12 this batch); the 151 includes orphan rows from historically-reworded headings and is NOT the file's 106-entry count — do not flag 163 as anomalous. Report the actual; a value other than 163 is worth a note, not necessarily a fail (a concurrent reword could shift it), but explain it.
> 9. **Classification depth spot-check (the context-saturation watch).** This batch is twelve entries and Step 1 carried a mandatory mid-step checkpoint. Scope "this cycle's proposals" the same way as row 3 (`WHERE entry_id > <E0>`), take the **three highest proposal ids** in that set (the last three classified — the most exposed to saturation), and for each confirm its `reasoning` genuinely quotes its own entry's `raw_content`. **⚠️ Use the EXTRACTION-FREE check below — do NOT try to parse quoted spans out of the `reasoning`.** A regex that extracts quoted spans breaks on the nested quotation marks these entries routinely contain (e.g. `detects "agent moved to Done/ without authorization"`, `A "dry" verdict`), and `raw_content` carries markdown emphasis markers (asterisks, underscores, backticks) that a rendered quote drops — together those produce FALSE FAILs on genuine citations. **This was measured, not theorized: the extraction approach false-FAILed 2 of the 3 last-classified proposals on this cycle's real data, both of which are verbatim-correct.** The check: (a) `canon(s)` = normalize curly quotes to straight, strip asterisk/underscore/backtick characters, collapse whitespace runs to single spaces, strip, lowercase; (b) compute the **longest common substring** between `canon(reasoning)` and `canon(raw_content)` via `difflib.SequenceMatcher(None, a, b, autojunk=False).find_longest_match(0, len(a), 0, len(b))`; (c) the proposal PASSES if that match is **>= 40 characters**. **Planner-validated against all twelve of this cycle's proposals at the Step-1 verdict gate: every one passes, range 97-300 characters** — so a result anywhere near 40 is itself worth a note, and a genuine sub-40 result means the `reasoning` does not quote its entry. **Report the measured longest-match length per proposal as a number** (not just a verdict word), plus PASS/FAIL, and name the proposal ids + entry ids checked. **This row is the mechanical backstop for the mid-step checkpoint** — a prose instruction the agent could skip — catching the *outcome* regardless of whether the checkpoint fired (Rule 54: a requirement needs a structural home).
>
> If any row fails, report and halt — do not pass a broken deliverable.
>
> **Deposit:** `knowledge/qa/cycle-qa-2026-07-21.md` — verification table with DB-source column, raw full-suite tail, the mandatory Rule 20 banner + PASSED line, and an Output Receipt with status. Canonical Python file-write pattern — no heredoc. Commit it. In `### Ledger Updates` include: `#### Project Status` — one milestone paragraph (cycle 2026-07-21 complete: the twelve-entry 07-20/07-21 batch ingested and classified, report deposited, corpus integrity held; Gate 1 route disposition pending, including the Drafting-Cycle cluster, the already-codified walk-the-list entry, and the open conflict-serializability lens-placement question); `#### Prompt Feedback` — standard.
>
> **Deposits:**
> - `lessons-forge/knowledge/qa/cycle-qa-2026-07-21.md`
>
> On full completion, move the plan file to `lessons-forge/knowledge/decisions/Done/` as the absolute last operation.
