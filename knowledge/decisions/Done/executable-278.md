# Lessons Forge — Gate 2 Codification (DRAFTING_CYCLE.md N1–N6 refinements) → DRAFTING_CYCLE.md v1.1
**Date:** 2026-07-25 | **Tier:** Medium | **Dispatch Mode:** bellows | **Test Scope:** none | **Execution:** Step 1 (SA) → Step 2 (DEV) → Step 3 (QA) | **qa_steps:** 3 | **pause_for_verdict:** always
**cycle_tier:** T2

## CEO Context

**Gate 2 Plan A — the finish of the DRAFTING_CYCLE.md hardening arc.** Gate 1 (plan 275) routed proposals 187–190 all `codify`. Gate 2 diagnostic (276) scoped the cross-repo architecture → **S2 (split by repo)** and committed the edit map. Gate 2 **Plan B (277) already shipped** — the `plan_lint.py` §4 refinements for 189/N5 (last-lens-line status read) and 190/N6 (`^T([012])\b` collapsed-T0 regex) are **LIVE on bellows main**. This plan is the governance half: it **codifies all four proposals (187–190) into `DRAFTING_CYCLE.md`, bumps v1.0 → v1.1, updates the changelog, and transitions the four to `implemented`.**

**Depends on Plan B (277, bellows — Done).** The plan_lint code Plan A's §4 doc text describes is already on bellows main; Plan A's QA verifies the doc text matches that shipped code. No live sequencing constraint remains (B is closed). *(Kept as prose context, NOT a header `**Depends on:**` field — that field resolves against THIS repo's `Done/`, but Plan B lives in `bellows/…/Done/`, not `lessons-forge/…/Done/`; a cross-repo Depends-on field would mis-resolve. Since B is already Done, no dispatch-time gate is needed — WS Lens-1 w1.)*

**Authored FROM the edit map (Rule 27 — do not re-derive from source):** `governance/knowledge/research/gate2-architecture-edit-map-2026-07-25.md` (diag-276). The SA re-derives every live line number and anchor against HEAD, but the edit POINTS and INTENT come from the map.

### The edit map — four proposals, five doc edits + two mechanical edits (all CEO-ratified via diag-276)

| # | Region | Proposal | Edit |
|---|---|---|---|
| E1 | §2.2 Destruction — append sub-question **2.4** | **187 (N1)** | diagnostic-mode residue: how the Destruction lens / its skip-condition applies to a non-mutating read-only plan (a diagnostic) |
| E2 | §2.5 ACID — append sub-question **5.5** | **187 (N2–N3)** | diagnostic-mode residue: what ACID analysis is meaningful for a single-step read-only plan (the multi-step Isolation schedule is structurally empty) |
| E3 | §2.7 Cross-cutting rules — new bullet | **188 (N4)** | explicit no-batch rule: each lens runs against the draft AS FOLDED by all prior lenses this walk; an EXTENSION cross-referencing §2.7:79 + §2.6:73, NOT a competing rule (plan 275's CI1) |
| E4 | §4 Self-Check — replace the closing-check bullet (:125) | **189 (N5)** | the check reads the **last lens line's** status (structured `- <Lens>: … wN dry`), not a keyword match on the closing prose; codifies the already-in-use last-lens-line format |
| E5 | §4 Self-Check — optional clarifying note | **190 (N6)** | §4 notes the self-check accepts the collapsed T0 header form (`T0 (no trigger); …`); **NO §3 edit** — Plan B's `\b` regex fix already makes §3's example pass |
| M1 | Version `:5` | — | surgical date-swap `1.0 (2026-07-23)`→`1.1 (2026-07-25)`, **preserving** the trailing "Amended only through the Iteration Protocol (§6)." clause |
| M2 | History section | — | new `- **1.1 (2026-07-25):** …` row naming 187–190, the units changed, and that the lens count stays five |

**⭐ E2/187 note:** proposal 187 (N1–N3) yields TWO sub-questions — 2.4 (Destruction) and 5.5 (ACID) — both diagnostic-mode residues, the same shape as the existing 1.4 (diagnostic-tailored weak-spots sub-question). Append-only per §6; new ids **2.4** and **5.5**; existing 2.1–2.3 and 5.1–5.4 unchanged.

**⭐ E3/188 is an EXTENSION, not a competing rule (plan 275's CI1).** §2.7 already carries "Parallelism within a lens, never across" (:79) and §2.6 carries "cumulation lives in the draft" (:73). The new bullet makes the AS-FOLDED sequencing explicit and names the rationalization "this pass is just confirmation, so cumulation doesn't matter here" as a self-check target — the same false reasoning that applies to expected-dry confirming walks. Word it to reinforce, not restate or contradict, :79/:73.

**⭐ E4/189 codifies what Plan B already shipped — the WHOLE-LINE rule (grounded in the committed code, Lens-1 w1).** Plan B's plan_lint finds the last lens result line (the last `- <Lens>: …` line in the region BEFORE the `**Closing:**` line) and WARNs iff that line contains a fold-token (`'fold'`) but not `'dry'`; the closing-line prose check is kept only as a legacy fallback. **Plan B's cold panel (CB1) replaced the edit map's Q2 "segment-after-`;`" design with this whole-line rule** — so the doc must describe the SHIPPED whole-line behavior, NOT the superseded segment design (the trap my draft-0 E4 fell into). SA authors E4 from `git -C /Users/marklehn/Developer/GitHub/bellows show HEAD:scripts/plan_lint.py`; QA cross-checks the doc wording against that same committed source.

**⭐ THE LENS COUNT STAYS FIVE — load-bearing.** None of 187–190 adds a lens (2.4 and 5.5 are sub-questions of existing lenses 2 and 5). The three count phrases must stay "five" and unchanged:

| Line | Phrase |
|------|--------|
| 29 | "run the **full five-lens walk** (§2.1–§2.5)" |
| 73 | "run the five lenses **cold**" |
| 123 | "one result line per **required** lens (all five for T1/T2, ACID included)" |

**⚠️ Load-bearing order (259 / edit map Q5): the DOC edits (E1–E5 + M1 + M2) land BEFORE the status flip.** This order governs the INTRA-Step-2 crash: die between the doc edit and the DB flip → doc-edited / DB-still-`proposed`, the recoverable direction (obvious at the gate). DB-first-then-doc-fails → four false `implemented` claims no later gate re-checks. (Precision, ACID-cold F2: a *separate* transient state — DB `implemented` + doc uncommitted — genuinely exists across the W2/W3 verdict gates; it is recovered NOT by this edit order but by the committed Step-1 blueprint + the wrap shasum/last-commit re-verify. Two distinct windows, two distinct guards — they cohere, they do not overlap.)

**Dispatch = lessons-forge (259 clone).** This plan writes the lessons-forge DB (the 187–190 status flip), so it clones 259 exactly: dispatched to lessons-forge, DEV edits root-tracked `DRAFTING_CYCLE.md` by ABSOLUTE path and leaves it UNCOMMITTED (the Planner commits it cross-repo at wrap; scope_check is cross-repo-invisible by design — `git diff --stat --relative -- .` is cwd-scoped, edit map Q1a). `lessons-forge.db` is gitignored, so the status flip produces no commit — QA reads it back to verify.

**Isolation (ACID w1) — stated, not lucky.** Two shared stores. The **DB** write is isolated by Bellows same-project serialization (no second lessons-forge plan runs concurrently) plus the C0 precondition (asserts `proposed`+`codify` immediately before the flip). The **root doc** has no cross-repo serialization (a governance-dispatched plan is a different project), so its guard is A0's commit-pin (a concurrent doc change between SA-read and DEV-apply → A0 HALT). Precondition: **run with nothing else in flight** — queues empty at authoring, re-confirm at dispatch. No between-step window is left unguarded.

**Test Scope: none** — a governance edit with no suite; Planner verification is the safety net. **Do NOT predict the plan_lint WARN set** (Integration w1 — plan 277's CB3: the "mentions tests but declares no test scope" WARN is SUPPRESSED when a step's text contains a run token like `pytest tests/`, so it may or may not fire — the predicted-number class, memory `predicted-numbers-need-verify-clause`). The §5 authoring self-check RUNS `plan_lint` on the finalized draft and reports the ACTUAL output; expect **exit 0** (all (f) and tests-mention checks are non-blocking WARNs). Never add a test file to this none-scope governance plan to silence a tests-mention WARN. (Plan B carried the plan_lint tests; Plan A is doc + DB only.)

**Scope discipline:** this plan edits ONE file (`/Users/marklehn/Developer/GitHub/DRAFTING_CYCLE.md`) and transitions FOUR proposal statuses (187,188,189,190). No `src/`, no schema, no other proposal, no PLANNER_TEMPLATE edit (the template's pointer to DRAFTING_CYCLE.md does not change).

**Deposit-once discipline:** deposited exactly once. **Wrap-commit protocol (binds the PLANNER):** between QA certification and the wrap commit, `DRAFTING_CYCLE.md` sits modified-uncommitted in the root tree. Before committing, (i) re-run `shasum -a 256` on it and match the Step-2 dev-log hash, AND (ii) re-verify DRAFTING_CYCLE.md's last-touching commit (`git -C <root> log -1 --format=%H -- DRAFTING_CYCLE.md`) still equals the DEV A0 pin — no concurrent governance commit landed on the root doc during the W2/W3 verdict gates (the shasum re-match covers only Plan A's OWN uncommitted bytes, NOT a concurrent COMMITTED change; ACID-cold F1). Both hold → commit at root + bump the submodule pointers + push. If the last-touching commit advanced → a concurrent change landed → reconcile (rebase Plan A's edit onto it), do NOT blind-overwrite. **Mismatch → do NOT commit blindly** (clone-drift guard, ACID w1 — 259 carried this branch): a post-QA edit or loss landed. The DB `implemented` is already durable and correct; only the doc commit is owed — reconstruct the intended v1.1 bytes by RE-APPLYING the committed Step-1 blueprint's edits and VERIFY against the Step-2 dev-log hash (a hash verifies bytes, it cannot reconstruct them), then commit — or re-dispatch DEV. Integrity chain: **SA read (commit pin) → DEV write (post-edit hash) → QA read (hash row) → wrap commit (re-match).** Per DRAFTING_CYCLE.md's own "When this file changes" (:152), the wrap records **which in-flight plans inherit v1.1 — expected NONE** (queues empty at authoring; re-confirm empty at wrap). The v1.1 edits are additive (2 sub-questions, 1 §2.7 bullet, 1 corrected §4 description) and invalidate no v1.0-drafted plan even if one were in flight (Destruction w1).

## How to Run This Plan

**Bootstrap prompt:**
```
Read the plan at lessons-forge/knowledge/decisions/in-progress-executable-<id>.md (daemon renames the deposited placeholder on claim). Execute Step 1 ONLY. After completing Step 1, STOP and wait for my confirmation. Do NOT proceed to Step 2.
```

---
---

## STEP 1 — SA (Solution Architect)

---

> **FIRST — post a short visible chat message (1-2 sentences) confirming you are starting this plan and your immediate next action.** Do NOT rename the plan file.
>
> You are the Solution Architect. Run commands from your own working tree. The target is governance-root `/Users/marklehn/Developer/GitHub/DRAFTING_CYCLE.md` — **READ-ONLY this step; you blueprint, DEV applies.**
>
> **Read the four proposals VERBATIM from canonical, with their source lessons, by DB join (never by ordinal counting):**
> `sqlite3 "file:/Users/marklehn/Developer/GitHub/lessons-forge/lessons-forge.db?mode=ro" "SELECT p.id, p.entry_id, p.status, p.route, p.suggested_action, p.reasoning, e.source_heading, e.raw_content FROM lesson_proposals p JOIN lesson_entries e ON e.id=p.entry_id WHERE p.id IN (187,188,189,190) ORDER BY p.id"`
> `raw_content` IS the lesson body. **Confirm exactly four rows, and that `p.status`/`p.route` (now in the projection) read `proposed`/`codify` for all four** (the projection carries these columns precisely so this confirmation is performable, not asserted — Cold-panel F1).
>
> **Read the edit map (authoritative — Rule 27):** `/Users/marklehn/Developer/GitHub/governance/knowledge/research/gate2-architecture-edit-map-2026-07-25.md` — the Q4 section gives the edit points + intent for E1–E5, M1, M2.
>
> **Read the live doc regions before blueprinting (re-derive EVERY line number; those in this plan are authoring-time):** `## 1. The Rigor-Tier Gate` (`:11`), §2.2 Destruction (`:48`, sub-questions 2.1–2.3, `**Evidence:**` at `:51`), §2.5 ACID (`:66`, sub-questions 5.1–5.4, `**Evidence:**` at `:69`), §2.6 (`:72` heading; the "cumulation lives in the draft" clause is on `:73`), §2.7 Cross-cutting rules (`:75`, the bullet list `:76–:81`, "Parallelism within a lens, never across" at `:79`), §3 Cycle Log (`:96`, the T0 collapsed form at `:114`), §4 Self-Check (`:118`, the closing-check bullet at `:125`, "warn-first" posture at `:127`), the version line (`:5`), the History section (`:155`, the 1.0 row at `:156`). **Pin your read:** record `git -C /Users/marklehn/Developer/GitHub rev-parse HEAD` and the doc's last-touching commit (`git -C /Users/marklehn/Developer/GitHub log -1 --format=%H -- DRAFTING_CYCLE.md`). DEV re-checks the latter before applying.
>
> **Blueprint FIVE doc edits + TWO mechanical edits — for each: the exact final text, an exact anchor (a verbatim substring occurring EXACTLY ONCE in the live file — verify uniqueness by grep and state the count), and whether it is an insertion or replacement.** DEV is a faithful applicator and will not improvise.
>
> **E1 — §2.2 append sub-question 2.4 (187/N1).** Diagnostic-mode residue for the Destruction lens: the lens is executable-primary — its core ("what existing functionality could this harm") and skip-condition ("only a pure-additive plan touching no existing behaviour") do not map to a read-only diagnostic, which mutates nothing. State what Destruction examines for a diagnostic: the harm surface is the DOWNSTREAM plans the diagnostic's findings authorize (a finding a later plan builds on without re-verification per T-7 / Rule 27 can license a guard-relaxing or destructive change) — so aim the lens at whether any finding over-claims certainty a downstream plan would act on. **2.4 must also make explicit that §2.2's skip-condition does NOT license skipping Destruction for a diagnostic** — a diagnostic is not "pure-additive touching no existing behaviour" in effect, precisely because its findings authorize downstream change (Cold-panel D-F2; this is the skip-condition-application point the edit map frames 2.4 around). Keep it within 2.4's wording — do NOT separately edit the :52 skip line (out of edit-map scope). Model the wording on the existing 1.4 (the diagnostic-tailored weak-spots sub-question). **APPEND as a new inline parenthetical `(2.4) …` at the END of §2.2's existing single `- **Sub-questions:**` line** (which currently ends with the 2.3 clause `…before it runs?`) — it extends that one line, NOT a new bullet; §2.2's sub-questions are all inline on one line, like 1.4's model (Cold-panel F3).
> **E2 — §2.5 append sub-question 5.5 (187/N2–N3).** Diagnostic-mode residue for the ACID lens: for a single-step read-only plan, the multi-step Isolation schedule (5.3) is structurally empty and Atomicity/Durability degenerate. State what ACID analysis remains meaningful — e.g. Consistency of the FINDINGS as a set (do two findings contradict?), and Durability in the record sense (is the findings artifact enough to reconstruct the diagnostic's basis for a later author, per the read-the-record floor). **APPEND as a new inline parenthetical `(5.5) …` at the END of §2.5's existing single `- **Sub-questions:**` line** (which currently ends with the 5.4 Durability clause) — extends that one line, NOT a new bullet (Cold-panel F3).
> **E3 — §2.7 new bullet (188/N4).** The explicit no-batch rule, as an EXTENSION of the existing cumulation clauses (§2.7:79 "Parallelism within a lens, never across"; §2.6:73 "cumulation lives in the draft, so sequential preserves it") — NOT a competing rule (plan 275's CI1). State: run each lens against the draft AS FOLDED by all prior lenses in this walk; never analyze all lenses against one draft and batch the folds (a batched fork is never seen by the later lenses). Name the rationalization "this pass is just confirmation, so cumulation doesn't matter here" as a self-check target — the same false reasoning applies to an expected-dry confirming walk (you do not know it is dry until you have run it lens-by-lens). Cross-reference :79/:73 explicitly. Append after the last existing bullet ("Sketch one real block…", `:81`). Assign a citation key (e.g. "§2.7 sequential-fold rule") in the changelog for future reference; §2.7 bullets are unnumbered so no new stable id is required. **Anti-watering-down gate (Destruction w1; pinned per Rule 57, Destruction w2):** before blueprinting E3, QUOTE :79 and :73 verbatim, then confirm E3 carries BOTH pinned propositions that :79/:73 lack — **(a)** the AS-FOLDED sequencing of the *folds* (each lens reads the draft as folded by all prior lenses; distinct from :79, which forbids concurrent *execution* of lenses, a different failure), and **(b)** the "this pass is just confirmation, so cumulation doesn't matter" rationalization named as a self-check target (absent from both :79 and :73). If E3 reduces to only :79/:73's content — i.e. it carries neither (a) nor (b) — it is a competing/duplicate rule → HALT and report. Do NOT judge "merely restates" freehand; the (a)/(b) test is the criterion.
> **E4 — §4 replace the closing-check bullet (189/N5).** Replace `:125` ("the **closing** line asserts a dry lens pass as the last event (not a fold).") with text describing the check Plan B SHIPPED. **Read the committed code FIRST — `git -C /Users/marklehn/Developer/GitHub/bellows show HEAD:scripts/plan_lint.py`** (the authoritative shipped version, not a working-tree copy) — and word the bullet to match it. The shipped mechanism (verified at authoring): the check finds the **last lens result line** (the last `- <Lens>: …` line in the region BEFORE the `**Closing:**` line; ACID is that line for T1+) and WARNs iff that line, whole, contains a fold-token (`fold`) but not `dry`. It reads the last lens line's **whole-line status**, NOT a keyword match on the closing prose, and NOT a per-segment (`;`-split) parse. The legacy closing-line prose check is retained only as a fallback when no structured lens line is parseable. **Do NOT describe a "segment-after-`;`" mechanism** — the edit map's Q2 design was superseded by Plan B's cold-panel fix (CB1) to the whole-line rule, and describing the superseded design would make the doc contradict the shipped code.
> **E5 — §4 optional clarifying note (190/N6).** Add a brief note in §4 that the self-check accepts the collapsed T0 header form (`**cycle_tier:** T0 (no trigger); …`) as a valid tier declaration — matching Plan B's shipped `^T([012])\b` regex. **NO §3 edit** — §3's T0 example (`:114`) stays unchanged (the `\b` fix already makes it pass). If the SA judges E5 redundant with E4's paragraph, it may fold the note into E4's replacement or omit it (flag the choice for QA) — E5 is explicitly optional per the edit map.
>
> **Two mechanical edits:** M1 — a **SURGICAL date-swap** at `:5`: change only `1.0 (2026-07-23)` → `1.1 (2026-07-25)` and **PRESERVE the trailing sentence**, so the full line reads `**Version:** 1.1 (2026-07-25). Amended only through the Iteration Protocol (§6).` — do NOT drop "Amended only through the Iteration Protocol (§6)." (Cold-panel D-F1: it exists only on `:5`; a whole-line replacement would destroy it and QA row 1 / B4 would not catch the within-hunk loss). Anchor on the date substring, not the whole line. M2 — a new History row, APPENDED after the 1.0 row (`:156`), never rewriting it: `- **1.1 (2026-07-25):** …` naming the four proposals (187–190), the units changed (§2.2 sub-question 2.4, §2.5 sub-question 5.5, §2.7 sequential-fold rule, §4 last-lens-line closing check + collapsed-T0 acceptance), that this pairs with Plan B's shipped plan_lint edits (189/190), and that **the lens count deliberately stays five**.
>
> **Dedup EVERY edit against live v1.0 before blueprinting it** (state the grep + count for each): confirm no diagnostic-mode sub-question already in §2.2/§2.5, no explicit no-batch clause in §2.7, no last-lens-line wording in §4, no collapsed-T0 note in §4. Already-present substance → halt and report rather than adding a competing statement. **Confirm the three count phrases at `:29`/`:73`/`:123` read "five" and that no edit touches them.**
>
> **Deposit:** `knowledge/development/gate-2-plan-a-blueprint-2026-07-25.md` — the five edits + two mechanical edits with exact text + grep-verified unique anchors, the dedup greps with counts, the pinned HEAD + doc last-touching commit, the count-phrase confirmation, and an Output Receipt. Canonical Python file-write — no heredoc. Commit it. `#### Prompt Feedback` in `### Ledger Updates`.
>
> **Deposits:**
> - `lessons-forge/knowledge/development/gate-2-plan-a-blueprint-2026-07-25.md`
>
> **STOP. Do NOT proceed to Step 2. Wait for CEO verdict.**

---
---

## STEP 2 — DEV

---

> **Before starting, read the Step 1 blueprint and confirm Output Receipt status Complete; otherwise halt and report.** Post a short visible chat message confirming you are starting Step 2.
>
> You are the Forge Developer. Apply the Step-1 blueprint to `/Users/marklehn/Developer/GitHub/DRAFTING_CYCLE.md` (governance root, ABSOLUTE path, in place) **VERBATIM** — a faithful applicator, not a re-author. If the blueprint and the live doc disagree on an anchor, **halt and report**.
>
> **⚠️ Encoding (Vuln w1, 3.1).** DRAFTING_CYCLE.md contains non-ASCII (em-dashes `—`, arrows `→`, `§`, `⭐`). ALL file I/O on it uses explicit `encoding='utf-8'` on BOTH read and write. A wrong-encoding round-trip silently corrupts non-ASCII in UNTOUCHED regions — and the shasum chain does NOT catch it (DEV's and QA's hashes agree on the same corrupted bytes). The real guard is the diff check in Task B4.
>
> **Scope:**
> - `/Users/marklehn/Developer/GitHub/DRAFTING_CYCLE.md` (edit in place; leave **UNCOMMITTED** — the Planner commits at wrap)
> - `knowledge/development/gate-2-plan-a-dev-2026-07-25.md`
>
> **Task A0 — pre-edit cleanliness gate, with resume disambiguation.** `git -C /Users/marklehn/Developer/GitHub status --short -- DRAFTING_CYCLE.md` must be **empty**, AND the doc's last-touching commit must **match the Step-1 blueprint's**. A clean tree with a different last commit → the doc moved after SA read it → **HALT.** **Use `git -C <root>`** — the doc is tracked by the ROOT repo; a bare `git status` from your worktree passes vacuously (this is proposal 184/E11, codified in PLANNER_TEMPLATE v4.78). **If DIRTY, disambiguate:** grep the modified doc for this plan's own anchors (`Version:** 1.1`; `(2.4)`; `(5.5)`; `1.1 (2026-07-25)`). Any present → this plan's own prior work → (1) snapshot the dirty file aside to a durable gitignored MAIN-tree path (`bellows/.bellows-cache/`), record the path; (2) attribute every hunk via `git -C <root> diff` — every hunk must match blueprint content; ANY unattributable hunk = MIXED dirt → HALT, never restore. All attributable → `git -C <root> restore DRAFTING_CYCLE.md`, verify clean + last-commit match, reapply. **No anchor present → foreign modifications → HALT, do NOT restore.**
>
> **Recovery for a botched apply — SCOPED to this plan's own work:** the pre-edit doc is committed (A0 verifies) → `git -C <root> restore DRAFTING_CYCLE.md` → HALT and report. **Never hand-patch over a bad apply** (the retry belongs to a fresh dispatch, per Rule #56).
>
> **Execute in label order: A0 → A → B → B2 → B3 → B4 → C0 → C.**
>
> **Task A — version bump FIRST** (`:5` — surgical date-swap `1.0 (2026-07-23)`→`1.1 (2026-07-25)`, **PRESERVING** the trailing `Amended only through the Iteration Protocol (§6).` sentence; the full line must read `**Version:** 1.1 (2026-07-25). Amended only through the Iteration Protocol (§6).`), so even an early death leaves `1.1` for A0's anchor test on re-dispatch. Grep-confirm the trailing clause survived (Cold-panel D-F1).
> **Task B — apply E1 through E5 exactly as blueprinted.** After each, grep-confirm the new text landed and is in the right section: E1's `(2.4)` appended INLINE on §2.2's `**Sub-questions:**` line (before its `**Evidence:**` line, NOT a standalone bullet); E2's `(5.5)` appended INLINE on §2.5's `**Sub-questions:**` line (before its `**Evidence:**`); E3's bullet inside §2.7 (after "Sketch one real block"); E4's replacement inside §4 (the old `:125` closing bullet GONE, the last-lens-line text present); E5's note inside §4 (or folded into E4 per the blueprint).
> **Task B2 — apply the CHANGELOG row (M2)** (append after the 1.0 row; never rewrite it). Must name **v1.1**, the four proposals, the units changed, the Plan-B pairing, and the lens-count-stays-five decision.
> **Task B3 — the count guard.** Confirm `:29`/`:73`/`:123` still read **five** ("full five-lens walk" / "five lenses cold" / "all five for T1/T2") and were NOT altered. Report all three as explicit greps. **No count sweep — 2.4/5.5 are sub-questions, not lenses.**
> **Task B4 — diff-is-only-intended-hunks (Vuln w1, 3.2 observe-the-effect).** Run `git -C /Users/marklehn/Developer/GitHub diff -- DRAFTING_CYCLE.md` and confirm it shows ONLY the intended hunks (E1–E5, M1, M2) and NO collateral change to any untouched line — a spurious diff on an untouched `—`/`→`/`§` line is encoding corruption. Any unexplained hunk → `git -C <root> restore DRAFTING_CYCLE.md` and HALT. This is the guard the shasum cannot be (the shasum only proves DEV and QA read the same bytes, not that those bytes are the intended ones). Paste the full diff into the dev-log.
> **⚠️ TASK ORDER IS LOAD-BEARING — the doc edit (A/B/B2) MUST complete before the DB transition (C).** Doc lands, DB fails → corpus says `proposed` while the doc carries the rule (recoverable, obvious at the gate). DB first, doc fails → four false `implemented` claims with no codification behind them, which no later gate re-checks.
> **⚠️ Canonical DB path (Vuln w1, 3.3).** ALL DB ops (C0 read, the restore-point copy, the Task-C UPDATE, the read-back) target the CANONICAL DB by ABSOLUTE path — `/Users/marklehn/Developer/GitHub/lessons-forge/lessons-forge.db`. `lessons-forge.db` is **gitignored**, so if this step runs in a bellows worktree a RELATIVE connection would open an absent/empty worktree-local DB and the flip would silently write nothing (or the wrong store). This is E11/proposal-184: a WRITE to shared state takes an absolute path. Never a bare `lessons-forge.db`.
> **Task C0 — DB PRECONDITION, immediately before the write.** Against the canonical absolute DB, read + assert 187,188,189,190 are each `status='proposed'` AND `route='codify'` (already `implemented` → prior dispatch, idempotent, proceed; any other status/route → HALT). Report as RAW CLI output.
> **Task C — transition the FOUR to `implemented`.** Take a restore point first, using the proven plan-275 pattern (Integration w2): **use `sqlite3 ".backup"`, NOT `cp`** — the DB runs in WAL mode and a `cp` can capture an un-checkpointed, inconsistent snapshot. Write to a **durable MAIN-tree absolute path that survives worktree teardown** (NOT the ephemeral dispatch worktree — a backup lost to teardown is no restore point): `mkdir -p /Users/marklehn/Developer/GitHub/lessons-forge/data/backups` then `sqlite3 "file:/Users/marklehn/Developer/GitHub/lessons-forge/lessons-forge.db?mode=ro" ".backup '/Users/marklehn/Developer/GitHub/lessons-forge/data/backups/lessons-forge-preA-<UTC-timestamp-colon-free>.db'"`. `.gitignore` matches `*.db` — confirm the backup is ABSENT in `git -C /Users/marklehn/Developer/GitHub/lessons-forge status --porcelain` and is non-zero-byte. Record the absolute path in the dev-log. **HALT and write nothing if the backup fails for ANY reason** (non-zero exit, missing/zero-byte file). Then, on the canonical absolute DB, `UPDATE lesson_proposals SET status='implemented', status_updated_at=<UTC now>, status_updated_by='ceo' WHERE id IN (187,188,189,190) AND status='proposed'` — parameterised, **never a bare UPDATE without WHERE**. `conn.commit()` once.
>
> **Deposit:** `knowledge/development/gate-2-plan-a-dev-2026-07-25.md` — the applied-edit confirmations (section-scoped greps), the B3 count-guard results, before/after status distributions as RAW output, the **post-Task-C per-id read as RAW `sqlite3` CLI output** (`SELECT id, status, status_updated_at, status_updated_by FROM lesson_proposals WHERE id IN (187,188,189,190) ORDER BY id`), the backup path, and the **post-edit doc hash** (`shasum -a 256 /Users/marklehn/Developer/GitHub/DRAFTING_CYCLE.md`). Canonical Python file-write — no heredoc. **Commit the dev-log only — NOT the doc.** `#### Prompt Feedback` in `### Ledger Updates`.
>
> **Deposits:**
> - `lessons-forge/knowledge/development/gate-2-plan-a-dev-2026-07-25.md`
>
> **STOP. Do NOT proceed to Step 3. Wait for CEO verdict.**

---
---

## STEP 3 — QA

---

> **Before starting, read the Step 1 + Step 2 deposits and confirm both Output Receipts Complete; otherwise halt and report.** Post a short visible chat message confirming you are starting Step 3 (QA). You are Lessons Forge QA. **Verification + reporting only — no doc edits, no DB writes.** If a check fails, report it — do NOT fix it. Do NOT use Monitor. Do NOT edit PROJECT_STATUS directly.
>
> **MANDATORY — Rule 20 self-check (canonical block, Checklist #4 — the exact template with all four placeholders FILLED, NOT a paraphrase; the M1 lesson, session 9; 275's machinery, adapted for a doc+DB plan — Cold-Integration Rule-20 fold).** Run the canonical Rule 20 self-check from `/Users/marklehn/Developer/GitHub/RULE_20_SELF_CHECK_BLOCK.md` (ABSOLUTE path — the block lives at the governance root, NOT in the lessons-forge worktree; a bare-name read fails there, the same vacuous-tree trap rows 0b/11 guard against). Copy the Python block verbatim and fill ONLY the four placeholders:
> - `plan_slug`: `gate-2-plan-a-2026-07-25`
> - `qa_report_path`: `<your-own-tree-abs>/knowledge/qa/gate-2-plan-a-qa-2026-07-25.md`
> - `evidence_dir`: `<your-own-tree-abs>/knowledge/qa/evidence/gate-2-plan-a-2026-07-25/` — the block runs in YOUR process and checks `os.path.isdir`, so derive this from `pwd` (your OWN working tree), NOT a hardcoded main-tree path (the plan-225 trap).
> - `required_evidence_files`: `[db-invariants.txt, doc-integrity.txt]`
>
> **Deposit both evidence files BEFORE running the block — it `sys.exit(1)`s if either is missing or empty.** Plan A has NO pytest (`Test Scope: none`), so the two evidence files are the DB read-back and the doc-integrity greps — NOT 275's `full-suite.txt`, which is uncloneable here. Include the block's literal stdout in the QA report; the banner `## Rule 20 — QA Self-Check Results` + the `PASSED — SELF-CHECK PASSED` line must appear verbatim (byte-enforced by `bellows/gates.py`). If it prints `FAILED`, halt and report — do NOT close. End with a self-grep confirming the banner.
>
> **Evidence-file contents:** write the RAW output of rows 9–10 (the DB per-id status read-back + the full status distribution showing `proposed`=0) to **`db-invariants.txt`**, and the RAW output of rows 0/0b/1–8 (the doc shasum, the independent diff, and the per-edit + count-phrase + §3:114-unchanged greps) to **`doc-integrity.txt`** — these are the two `required_evidence_files` the block verifies non-empty. The QA report still carries the full verification table.
>
> **Evidence rule.** Deposit **RAW command output, never a summary.** Every DB row states which DB.
>
> **Scope:**
> - `knowledge/qa/gate-2-plan-a-qa-2026-07-25.md`
> - `knowledge/qa/evidence/gate-2-plan-a-2026-07-25/db-invariants.txt`
> - `knowledge/qa/evidence/gate-2-plan-a-2026-07-25/doc-integrity.txt`
>
> Verification table, one row per claim:
> 0. **Doc integrity — FIRST.** `shasum -a 256 /Users/marklehn/Developer/GitHub/DRAFTING_CYCLE.md` byte-compared against the Step-2 dev-log hash. Mismatch → halt (every row below would certify bytes DEV never produced).
> 0b. **Independent diff cross-check (Cold-Vuln F2).** Re-run `git -C /Users/marklehn/Developer/GitHub diff -- DRAFTING_CYCLE.md` YOURSELF and confirm the diff contains ONLY the intended E1–E5 + M1 + M2 hunks — **no collateral change on any untouched line, especially non-ASCII (`—`/`→`/`§`/`⭐`) lines**. This is the QA-side observation of the effect the shasum (row 0) structurally cannot give (DEV's and your hashes agree on the same bytes, corrupted or not); B4 was otherwise the sole guard, single-point at DEV. Any unexplained hunk → FAIL.
> 1. **Version 1.1** on `:5`, **FULL line intact**: `**Version:** 1.1 (2026-07-25). Amended only through the Iteration Protocol (§6).` — the trailing amendment-discipline clause MUST be preserved (Cold-panel D-F1); a truncated line missing "Amended only through the Iteration Protocol (§6)." is a FAIL.
> 2. **E1 — sub-question 2.4 present in §2.2 Destruction** — quote it. Confirm it addresses diagnostic-mode / read-only plans and is a SUB-QUESTION of Destruction (not a new lens). **Also confirm the PRE-EXISTING sub-questions 2.1/2.2/2.3 remain intact on that same line** — 2.4 was appended, nothing dropped or mojibaked (the D-F1 within-hunk preservation principle applied to the inline append; ACID-cold F3).
> 3. **E2 — sub-question 5.5 present in §2.5 ACID** — quote it. Confirm it addresses diagnostic-mode ACID and is a SUB-QUESTION of ACID. **Also confirm the PRE-EXISTING sub-questions 5.1/5.2/5.3/5.4 remain intact on that same line** (ACID-cold F3).
> 4. **E3 — the §2.7 sequential-fold (no-batch) bullet present** — quote it. Confirm it cross-references the cumulation clauses (:79/:73) as an EXTENSION and does not restate/contradict them.
> 5. **E4 — the §4 closing-check bullet now describes the shipped last-lens-line check** — quote it; confirm the old keyword-on-closing-prose wording is GONE. **Cross-check against the shipped code:** `git -C /Users/marklehn/Developer/GitHub/bellows show HEAD:scripts/plan_lint.py` and confirm the doc text matches the ACTUAL mechanism (last lens line in the region before `**Closing:**`; WARN iff that whole line has a fold-token and no `dry`; closing-line prose kept only as fallback). A doc/code mismatch — including describing a "segment-after-`;`" parse the code does not implement — is a FAIL.
> 6. **E5 — collapsed T0 acceptance** (one of three per the SA's flagged choice, Cold-panel F2): (a) a standalone §4 note → quote it; (b) folded into E4's replacement → quote the clause; (c) **SA omitted E5 and flagged it in the blueprint** → confirm §4/E4 already conveys the collapsed T0 form is accepted (Plan B's `\b` regex handles it) and record the omission as INTENDED, not a FAIL. In ALL three cases confirm **§3's T0 example at :114 is UNCHANGED**.
> 7. **The count phrases at `:29`/`:73`/`:123` are INTACT** — quote all three; each still reads "five". A sixth lens or altered count is a FAIL.
> 8. **New changelog row (M2)** for v1.1 naming the four proposals, the units changed, the Plan-B pairing, and the lens-count-stays-five decision; the 1.0 row is intact (not rewritten).
> 9. **The four proposals are `implemented`.** Against the canonical absolute DB (`/Users/marklehn/Developer/GitHub/lessons-forge/lessons-forge.db`), re-run the Step-2 per-id query as raw CLI and byte-compare against the Step-2 block: 187,188,189,190 → `implemented`, `status_updated_by='ceo'`.
> 10. **Corpus totals:** report entries + proposals; **`proposed` is now 0** (the four were the last). Report actuals via `SELECT status, COUNT(*) FROM lesson_proposals GROUP BY status`. If `proposed` > 0, list which ids remain (`SELECT id FROM lesson_proposals WHERE status='proposed'`) — a non-187–190 id at `proposed` is a surprise to surface, not silently pass.
> 11. **No `src/` change, no schema drift.** Check from your **OWN working tree** (in-place or worktree) — do NOT hardcode `-C <main lessons-forge>`, which passes VACUOUSLY on a worktree run (proposal 184's class; 275's QA row 7 proved this fix): `git status --porcelain -- src/` empty (a secondary guard for a lingering *uncommitted* src/ edit; Bellows scope_check is the primary guard against a *committed* src/ change). (Cold-Vuln F1 — reverts a 259-clone regression back to 275's own-tree form.)
>
> If any row fails, report and halt.
>
> **Deposit:** the QA report `knowledge/qa/gate-2-plan-a-qa-2026-07-25.md` (verification table with a DB-source column, raw output, the Rule 20 banner + PASSED line + the block's literal stdout, Output Receipt) **plus the two evidence files** (`db-invariants.txt` = rows 9–10 raw; `doc-integrity.txt` = rows 0/0b/1–8 raw). Canonical Python file-write — no heredoc. **Commit all three.** In `### Ledger Updates` include `#### Project Status` (one milestone paragraph: Gate 2 Plan A complete — DRAFTING_CYCLE.md **v1.1**, proposals 187–190 implemented, the two diagnostic-mode sub-questions [2.4/5.5] + the §2.7 sequential-fold rule + the §4 last-lens-line check codified, lens count deliberately five, `proposed` now 0; the DRAFTING_CYCLE.md hardening arc is COMPLETE) and `#### Prompt Feedback`.
>
> **Deposits:**
> - `lessons-forge/knowledge/qa/gate-2-plan-a-qa-2026-07-25.md`
> - `lessons-forge/knowledge/qa/evidence/gate-2-plan-a-2026-07-25/db-invariants.txt`
> - `lessons-forge/knowledge/qa/evidence/gate-2-plan-a-2026-07-25/doc-integrity.txt`
>
> **Do NOT move this plan to `Done/`.** The close path is owned by Bellows on continue-verdict consumption (Rule 8) — never by the agent.

---
---

## Drafting Cycle
**Tier:** T2 — triggers fired: T-6 (governance surface — edits DRAFTING_CYCLE.md doctrine), T-2 (production-data mutation — flips 187–190 in the LF DB), T-7 (authored-from diagnostic — builds on diag-276's edit map), T-8 (novel — not a byte clone; new edit set).
**Walks:** 3 (2 warm + cold panel + confirming). w1 12 (4+3+2+1+2); w2 5 (2+1+1+1+0); cold 12 (3+2+2+2+3); w3 dry on plan logic (Cycle-Log housekeeping only). Sequential+cold+confirming cycle CLOSED — last event = ACID w3 dry lens pass. ~34 folds total.
- Weak spots:          w1 4 folded (E4 mechanism mismatch [whole-line vs segment, grounded in committed plan_lint.py]; E4/QA-5 committed-code pin; Depends-on-as-prose note; QA-10 proposed>0 diagnosis); w2 2 folded (precision on w1 fold-text: wrap-mismatch "hash reconstructs"→"blueprint reconstructs, hash verifies"; self-conformance "closes with dry"→"contains dry"); w3 dry (plan logic sound — SA→DEV→QA flow, Rule-20 evidence-row mapping, task order, self-conformance all trace clean; cleaned 3 Cycle-Log housekeeping items).
- Destruction:         w1 3 folded (E3 anti-watering-down diff-gate vs :79/:73; :152 in-flight-inherit note; self-conformance recursion — Plan A's own ACID line must close dry). w2 1 folded (pinned E3 gate's (a)/(b) criteria per Rule 57 — E3 confirmed distinct from :79/:73, gate is passable). w3 dry — no fold across the ~29 relaxed a guard (all added or strengthened); E5 optionality is edit-map-ratified; extended the Conflicts ledger to span the full cycle.
- Vulnerabilities:     w1 2 folded (3.1/3.2 UTF-8 + Task-B4 diff-only-intended-hunks [shasum can't catch mojibake in untouched regions]; 3.3 canonical absolute DB path for the flip [gitignored DB + worktree = silent no-op]). w2 1 folded (3.1/3.3 restore-point pinned to a durable main-tree path — a worktree `.backup` is lost on teardown). Verified-holds: `git -C <root>` resolves from any worktree cwd; DEV editing the root doc outside the worktree is the 259 pattern; C0→C has no gate + `AND status='proposed'` atomic guard. w3 dry — no new env/isolation/degenerate vuln from the folds; Rule 20 `evidence_dir` pwd-derived == deposit location (cold-ACID-confirmed); evidence files are governance metadata (no leak, local commit); wrap last-commit re-verify is a clean absolute read.
- Integration-record:  w1 1 folded (CB3/predicted-number: authoring self-check is a verify-clause, not a WARN prediction). Verified-holds: cycle_tier on its own line parses (275 precedent + `_parse_plan_header` docstring — collects consecutive bold lines); 277 in bellows Done/; both deposit dirs exist; SA-query JOIN columns all live in schema; 259 protections all carried; M1/CB1/CI1 session-9 lessons folded (Lenses 1/1/plan). w2 1 folded (⭐ record CORRECTED the Vuln-w2 restore-point guess: 275's proven pattern = `sqlite3 ".backup"` not `cp` [WAL-safe] + `lessons-forge/data/backups/` not `bellows/.bellows-cache/`; gitignored via `*.db`). w3 dry — re-verified load-bearing premises STILL HOLD post-cycle: 187–190 still proposed+codify (count 4); DRAFTING_CYCLE.md still v1.0 (commit 2502159, trailing clause intact, tree clean); nothing in flight. Rule 20 clone faithful to 275; edit-map decisions (S2/fix-i/status/count-pin/load-order) + §6 all honored.
- ACID:                w1 2 folded (clone-drift: restored 259's wrap-mismatch branch [QA→wrap doc-loss window; DB already durable]; Isolation stated — bellows serialization + C0 for DB, A0 commit-pin for doc, nothing-in-flight precondition). Atomicity/Durability verified-holds (load-bearing order + A0 disambiguation + `AND status='proposed'` idempotency + DB backup + committed pre-doc-state + blueprint). w2 dry — folds cohere as a system; restore-point Vuln→Integration was a correction (converged on 275's `.backup` pattern), not oscillation; atomicity/isolation/durability intact + strengthened. w3 dry — ACID-cold F1/F2/F3 cohere; isolation now COMPLETE (F1 closed the concurrent-committed-root-change wrap window); closing state correct (warm ACID line contains "dry" + is the last `^-` lens line before Closing → plan_lint (f) no-WARN, confirmed empirically at §5).
**Conflicts:** none across the full cycle (walks 1–3 + cold panel) — no two lenses' constraints required joint resolution. Both candidate tensions were benign: the restore-point evolution (Vuln→Integration) was a correction from the record, not a ping-pong; and load-order↔wrap-recovery / absolute-path↔own-tree were confirmed COMPLEMENTARY by the cold ACID reader, not conflicting.
**Cold panel (T2):** COMPLETE — 5 cold readers, sequential fresh-context subagents, author-verified; 12 folded (3+2+2+2+3). ⭐ Standout: cold Integration caught the Rule 20 QA step was UNSATISFIABLE (full form mandated, no evidence files → canonical block sys.exit(1) → plan would halt at QA); CEO chose full-form-+-real-evidence. Two clone-drift reversions vs 275 caught (own-tree src-check; and the warm-Walk-2 `.backup`-not-cp). Cold ACID confirmed the whole fold set coheres (no cross-fold conflict; atomicity/consistency/durability guarded not lucky; late Rule 20 fold adds no new hazard).
  - Weak spots (cold): 3 folded — F1 SA query omitted status/route so its "confirm proposed+codify" was unperformable (my 259-adaptation error; added p.status/p.route); F2 QA row 6 had no branch for a permitted E5 omission (added it); F3 E1/E2 "insert item" → clarified inline-append (sub-questions are inline, not bullets). Cold reader independently CONFIRMED: DB-safety chain, E4↔shipped-code (plan_lint HEAD cc0777c), unique anchors, count phrases untouched, cycle_tier parse, gitignored backup.
  - Destruction (cold): 2 folded — ⭐ D-F1 [MEDIUM] M1 whole-line version replacement would DESTROY the ":5 Amended only through the Iteration Protocol (§6)." clause (exists nowhere else; QA-1 + B4 both blind to the within-hunk loss) → surgical date-swap + QA-1 asserts full line; D-F2 2.4 must state the §2.2 skip-condition doesn't apply to diagnostics (edit-map-framed, in 2.4's wording). Cold reader CLEARED the prime suspects: E4 doesn't weaken §2:38's invariant (lives in §2, untouched; plan_lint WARNs cite §2 not §4); E3 distinct from :79/:73; no doc consumer parses the wording at runtime.
  - Vulnerabilities (cold): 2 folded — ⭐ V-F1 [clone-drift] QA row 11 `git -C <main>` src/-check is worktree-VACUOUS (I cloned 259's old form, reverting 275's proven own-tree fix, proposal-184's class) → own-tree `git status --porcelain -- src/`; V-F2 mojibake guard was single-point at DEV-B4 → added QA row 0b independent diff cross-check. Cold reader VERIFIED live: bellows DOES worktree lessons-forge plans (bellows.py:1045), so hardening is load-bearing; DB has no triggers/no status↔route coupling; all cross-repo/DB/backup paths absolute; no false-implemented path exists.
  - Integration (cold): 1 MATERIAL folded (CEO-decided) — ⭐⭐ Rule 20 clone-drift: QA mandated M1's FULL form but supplied no evidence files → the canonical block would sys.exit(1) (unsatisfiable). Verified: enforcement gate needs only banner+PASSED (gates.py); the full block hard-requires evidence_dir + non-empty files (RULE_20_SELF_CHECK_BLOCK.md:66–74,95); 259 [same doc-only class] used the simple banner; 275 used the full form justified by pytest evidence Plan A can't produce. **CEO chose the full form + real evidence** → cloned 275's machinery, evidence set adapted to db-invariants.txt (rows 9–10) + doc-integrity.txt (rows 0/0b/1–8, replacing uncloneable full-suite.txt); own-tree evidence_dir (plan-225), Scope+Deposits+commit-all-three updated. Plus 1 trivial (§2.6 :72→:73). Cleared: Depends-on-prose (correct), DB-isolation-guard (covered), all domain premises HOLD (Plan B shipped, E4↔code, 187–190 proposed, count phrases, self-conformance).
  - ACID (cold): 3 folded (all minor) — F1 wrap adds a prevention-side last-touching-commit re-verify (concurrent committed root change the own-bytes shasum can't see); F2 load-order rationale precision (intra-Step-2 crash vs the transient implemented+uncommitted-doc window — two windows, two guards); F3 extended D-F1's within-hunk preservation to E1/E2 (QA rows 2/3 confirm 2.1–2.3 / 5.1–5.4 survive the inline append). ⭐ CONFIRMED the fold set coheres: load-order↔wrap-recovery complementary, absolute-path↔own-tree = the two halves of E11, DB-flip atomicity + doc↔DB wrap-consistency guarded not lucky, late Rule 20 fold no new hazard.
**Self-conformance (recursion, Destruction w1):** Plan A is T2 and carries this very block, so plan_lint's (f) check runs on Plan A at deposit. It passes iff — all five lens lines present ✓, the `**Cold panel (T2):**` line present ✓, and the **last lens line (ACID) CONTAINS "dry"** (from its closing dry walk) so the whole-line rule (`fold`-token present AND `dry` absent → WARN) does not fire — a line like `w1 2 folded; wN dry` passes because `dry` is present even though `folded` also appears. The closing walk MUST leave ACID dry; verify plan_lint exit 0 on the finalized draft before deposit (§4 authoring self-check).
**Closing:** Walk 3 closed dry (last event = ACID w3 dry lens pass; plan logic dry, only Cycle-Log housekeeping remained). **§5 COMPLETE** — plan_lint on the finalized draft: **exit 0**; the (f) drafting-cycle self-check PASSES (recursion confirmed empirically — cycle_tier T2 parsed, all 5 lens lines found, cold-panel line present, ACID closes "dry", zero (f) WARN); the only WARNs are 3 benign "mentions tests but declares no test scope" (CB3 / `benign-gate-failure-classes` — non-blocking, do NOT add a test file to silence). Checklist-by-scope conformant. **READY TO DEPOSIT** (exactly once, to lessons-forge/knowledge/decisions/).
