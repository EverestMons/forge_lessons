# Executable: Gate 2 — the PLANNER_TEMPLATE three (316/324/326): v4.86 → v4.87, then flip the three to `implemented`

**Type:** Executable
**Project:** lessons-forge
**Depends on:** executable-350 (Done — wrote the routing; stamped all three `2026-08-12T00:29:31Z`), executable-353 + executable-345 (Done — the builder-mechanism lineage; 353's panel is this clone's hardening source, cloned FULL per its compression lesson), the 2026-08-11 packet (`d08110b`; provenance), PLANNER_TEMPLATE.md at v4.86 (precondition, A0)
**Created:** 2026-08-12
**Author:** Planner
**Slug:** `gate2-pt3-2026-08-12`
**dispatch_mode:** bellows
**pause_for_verdict:** always
**Priority:** 5
**cycle_tier:** T2
**qa_steps:** 2
**Test Scope:** targeted (Rule 21 — single test module, session-verified ×9; QA row 5 re-derives; baseline 55 passed / 0 skipped)

⚠️ **ID NOTE:** id read at deposit (`next_id` was **354** at authoring — CONSUMED BY THE PANEL-SEAT-1 INCIDENT (a lint copy staged at the real decisions path was claimed AND dispatched same-second; contained pre-flip: template restored byte-exact, corpus untouched, stop-verdicted halted-354, duplicate removed); 351/352 went to the parallel terminal, and 355 followed IN-WINDOW at panel seat 3 (invoice-pulse) — FOUR consumed ids around this one plan (seat 4 keeps this count honest); the freeze reads fresh).

## Why
The Gate-2 queue's last three rows (packet `d08110b`): **316** the wrap corpus-freeze obligation (Session Wrap step-7 guard (a) generalized, with hold-and-record), **324** new Rule 95 — a step body has exactly ONE reader, **326** the panel's four doctrine defects, of which THREE land here (Rule 85's `-C` amendment + fail-able assert; Rule 93's declared-blanket form; Rule 44's owner-mismatch wrap channel) and **the FOURTH — the final-ACID ambiguity — is SPLIT to a future DRAFTING_CYCLE clarify and NOT codified here: 326 flips `implemented` with the split DECLARED (the PARTIAL convention), stated in the changelog row and here — and SCHEDULED, not just declared (seat 1: declared-in-prose-scheduled-nowhere is the very defect quarter 3 codifies): the row text, pinned verbatim for the wrap's Rule-44 owner-mismatch channel into the GOVERNANCE register: *DRAFTING_CYCLE's closing-line-ordering bullet (252) is ambiguous between a stand-alone post-walk ACID phase (334's practice) and §2.5's in-walk lens 5 (current practice); both models have shipped. Owed: one clarifying clause naming the referent. Owner: governance/DRAFTING_CYCLE. | deferred-work | 356 | open*.** Eight builder edits, one scoped 3-row flip. Corpus path proper, no §6-equivalent deviation; real walks under the v2.5 doctrine (T2 — governance surface: the template + corpus).

⚠️ **Sentinel exposure: the three's prior stamp `2026-08-12T00:29:31Z` is Z-form and GLOB-MATCHING** (350's write) — the one-value `NOT IN` exclusion carries the guard; re-measured, not inherited. **Categories: all three `governance_rule` (measured per id 2026-08-12 — the 353 seat-3 lesson applied at authoring).**

## Scope
- **One doctrine file** via the COMMITTED builder `governance/knowledge/research/builder-gate2-pt3-2026-08-12.py` (all-or-nothing; anchors count-asserted; dry-run numstat **`13 6`** (re-measured after seat 2's E8), all probes earnable 0→1, `**Version:** 4.86` 1→0, `**Last Updated:** 2026-08-11 (v4.86)` 1→0, rules census 94→**95** monotonic-unique inside the section, H2/H3 **30/190 → 30/191**).
- **One DB write:** the three `accepted|codify → implemented`, `status_updated_by='ceo'`, in-statement Z-stamp.
- **No code edit; no LESSONS/FORWARD touch by any step.** Gate coupling: none — the four distinctive clause tokens measure 0 in both `/Users/marklehn/Developer/GitHub/bellows/scripts/plan_lint.py` and `/Users/marklehn/Developer/GitHub/bellows/gates.py`; rule-number couplings exactly `Rule 20/22/26` and zero `:NN` template line-cites in bellows `*.py` — the new Rule 95's number must stay absent (all measured; QA row 7 re-sweeps everything with positive control — seat 2 restored 345's two sweeps: the authoring measurement is a prediction by QA time).
- Environment facts, VERBATIM (the twice-paid lesson — never compressed again):
  1. `grep` is a ugrep shim: **`-F` every literal**; a zero-match `grep -c` prints `0` and EXITS 1 — the printed count is the assertion; never `&&`-chain zero-count probes (QA row 7's expected values are all 0 — load-bearing there).
  2. Shell state does NOT persist — same-invocation scratch dirs.
  3. `find`, never a glob; `--` before dash-leading grep literals.
  4. The DB is gitignored and worktree-absent: a bare relative name CREATES an empty file — canonical absolute path only.
- Verdict-window posture: v4.87 governs from the Step-1 commit; HALT holds; rollback is CEO's.

**Rule 93 — declared blanket form (per the rule this very plan codifies):** Step-1 mandates are observed collectively — the builder asserts by QA row 2's probe re-run, F/F2 by rows 1/3, CHANGES/GLOBOK by row 4(a), **BK and MAXID by the Receipt's named-sentinel mandate itself** (no QA row reads the backup — the receipt is their observer); the category-baseline violation is constructed by row 4(a)'s per-id pin, the commit-drift violation by the Rule-85 asserts. This line is the declaration.

## Freeze checklist (deposit path — items 1–4 BEFORE the copy, item 5 immediately AFTER it; walk 4: item 5 arrived post-copy at seat 5 and the v0 header's promise had not been re-read against it)
1. Substitute the read id at the bootstrap `<id>` site AND in TASK F's `-m` string AND at the Why's pinned FORWARD row's `<plan-id>` token (seat 2: the Rule-44 verbatim-copy channel would otherwise carry the placeholder into the governance register); probe exactly, OCCURRENCE form: `grep -oF -- '<id' <deposit-path> | wc -l` → **2** post-substitution (both residual tokens on this checklist line; the 353 seat-5 form), and `grep -oF -- '<plan-id' <deposit-path> | wc -l` → **2** (both on this checklist line).
2. Final `plan_lint` at a NON-CLAIMABLE deposit-shaped path — ⚠️ **NEVER stage any `executable-*.md` under the real `lessons-forge/knowledge/decisions/` while the daemon is up: the claim is same-second and it DISPATCHES (proven live at this plan's panel seat 1 — the incident); lint from a scratchpad mirror (`<scratch>/stage/lessons-forge/knowledge/decisions/…`), which resolves the identical project_root** — and the mirror must be FAITHFUL: copy the real `src/test_lessons_forge.py` into `<scratch>/stage/lessons-forge/src/` first (seat 2's spelled QA row 5 makes the path an (o1) existence candidate, resolved against the MIRROR root — the file exists at the real root; mirror fidelity, not suppression). WARN set matches the Conformance paragraph.
3. A0-fresh re-check: the three still `accepted|codify` stamped `2026-08-12T00:29:31Z`, categories all `governance_rule`; template sha still the A1 pin; builder re-run numstat still `13 6` (the post-E8 measured pair).
4. The Closing line contains `instruction` and `/ record` with no italic-parenthetical placeholder (earnable — fails the placeholder, passes only the real Closing).
5. Post-copy, same minute: `ls` the real `decisions/` — the daemon's claim must read `in-progress-executable-<READ-ID>.md` carrying the id substituted at item 1 (seat 5: the id-consumption window is seconds wide and FOUR ids were consumed around this plan; a mismatched name = a foreign in-window consumption — report it, never re-copy).

## Conflict Ledger
**C1** the three-id set immutable. **C2** builder all-or-nothing; anchors count-asserted. **C3** doctrine before DB; A0 completes the half-state. **C4** backup adjacent, `BK=3` by value via `?immutable=1` against THE FOUND BACKUP's absolute path. **C5** flip scoped `id IN (316,324,326) AND status='accepted'`. **C6** sentinels BY NAME — PRE, ACC, MAXID, BK, CHANGES, GLOBOK — SIX, counted; `CHANGES=3`, `GLOBOK=3` with the one-value exclusion. **C7** capture inside the flip txn: `id <= 326 AND id NOT IN (316,324,326)` → **323 rows** (326 − 3; contiguity measured). **C8** commits cd-first + pathspec + name-only + toplevel (Rule 85 both halves — and this plan SHIPS the amendment it obeys). **C9** serialized dispatch stated; non-dependent guards named at A0/QA.

## How to Run This Plan
**Bootstrap:**
```
Read the plan at knowledge/decisions/in-progress-executable-356.md (the daemon renames on claim). Execute Step 1 ONLY. After completing Step 1, STOP and wait for my confirmation.
```
⚠️ HALT ROUTING: Step 1 reads this plan, the live `/Users/marklehn/Developer/GitHub/PLANNER_TEMPLATE.md`, the COMMITTED builder, the canonical DB. Step 2 reads this plan, the dev-log, the live template, the DB (read-only), the merged captures, `/Users/marklehn/Developer/GitHub/RULE_20_SELF_CHECK_BLOCK.md`.

---

## Drafting Cycle

**Tier:** T2 — governance surface (the template + corpus). Clone lineage: 353's FINAL panel-hardened form cloned FULL (its compression lesson is this clone's first law); 345 the template-side grandparent.

**Walk 0 (context pin, §2.0):** template sha `886cfaca…` (live v4.86 — the same blob 345 shipped, porcelain clean); all seven anchors count-1 (E1/E2/E3/E4 unique body phrases; E5 the Rule-94 Source line; E6 the version line `**Version:** 4.86` = 1; E7 the v4.86 changelog row head; **+E8 the Last-Updated header line, count-1 measured — EIGHT anchors from seat 2's fold on**); the three's prior stamp Z-GLOB-MATCHING → one-value exclusion load-bearing; categories measured per id (all `governance_rule`); capture bound 326−3=**323** (contiguity measured); dry-run executed: numstat `12 5` at walk 0 (**→ `13 6` re-measured after seat 2's E8 — the standing pin**), probes earnable, rules 94→95 monotonic-unique, census 30/190→30/191. Register: `governance/knowledge/research/walk-register-gate2-pt3-2026-08-12.md`.

**Direction verdict (after walk 1): PROCEED** — origin sound (353's final form, cloned full), mechanism sound (committed builder, dry-run stable at `12 5`), licence sound (packet + 350's routing re-verified; categories per id at the pin).

**Walks:** 3 (swept per culmination). ⚠️ **This cycle runs under v2.5 — per-lens commits govern its own folds, and walk 1's lens-1 fold carries its own commit (the rule's first live exercise, on the batch that shipped it to the sibling file).**
- Weak spots:          w1 1 folded — instruction, pre-existing (A0 branch-2's probe-retrieval pipe cannot feed probe 7's file-reading python census — materialize-first form added, same-invocation; committed per-lens).
- Destruction:         w1 dry (E1's tail-preservation verified by neighbour probes both sides; all three neighbour literals 1→1).
- Vulnerabilities:     w1 executed — the row-scoped tail pipe VALIDATED on the dry-run (returns 1; `v4.87` total post = 1, the row only — the version line carries no `v` prefix, checked) *(superseded seat 2: E8 adds a second `v4.87` line — total post = 2, measured; the ROW-SCOPED pipe is unaffected, still 1 — seat 5 closed the pointer walk-0's twin got at seat 4)*; battery: three `accepted`, categories 3/3 `governance_rule`; lint EXIT 0.
- Integration-record:  w1 dry (register conformant from birth with deviations pre-declared; synced this culmination).
- ACID:                w1 dry (E1 preserves Rule 85's tail by anchor construction; flip machinery is 353's verified clone at 3-row scale).

**Walk-1 split: instruction 1 / record 0; origin 1 of 1 pre-existing.** Re-opens; walk 2 owed.

**Walk 2** (whole artifact; the systematic both-sides sweep run EARLY — the 353 walk-4 lesson as standing practice):
- Weak spots + Vulnerabilities: w2 executed — thirteen pins + the rules census (94→95 monotonic-unique) + H2/H3 (30/190→30/191) + the row-scoped tail, ALL CLEAN both sides; battery stable (three `accepted`, categories 3/3); lint EXIT 0 ~~at the three expected WARNs~~ (seat 1: TWO by then — see Conformance).
- Destruction:         w2 dry.
- Integration-record:  w2 1 folded — instruction, pre-existing ~~(measured 2 both sides — Rule 26's copy + the v4.68 row)~~ **struck same-walk: the fold wrote 2 from RECALL while the measurement in the same output printed 1 — the pin is 1, corrected before the culmination closed; the recalled sites were a fabrication (the Rule-26 copy uses different wording). The predicted-number class, caught at its shortest interval yet: one tool-call.**
- ACID:                w2 dry.

**Walk-2 split: instruction 1 / record 0; origin 1 of 1 pre-existing.** Re-opens; walk 3 owed — the tail is one measured-constant firming, the convergence shape.

**Walk 3** (whole artifact; new surface = walk 2's fold and its same-walk correction):
- Weak spots:          w3 dry (the corrected pin re-verified FROM THE ARTIFACT against a fresh measurement — reads 1, measures 1; the strike texts read honest and complete at both sites).
- Destruction:         w3 dry.
- Vulnerabilities:     w3 executed — builder re-run `12 5` stable; battery three `accepted` categories 3/3; template sha unchanged; lint EXIT 0 ~~at the three expected WARNs~~ (seat 1: TWO — see Conformance).
- Integration-record:  w3 dry (log/register consistent incl. the scars).
- ACID:                w3 dry.

**Walk-3 split: instruction 0 / record 0 — LITERAL DRY. The walk phase meets the v2.5 bar on the dry branch.** T2: the cold panel convenes on this artifact before the Closing.

**Conformance (§5) — from birth: v0 → EXIT 0, THREE WARNs (missing-lens, cold-panel, the benign (o1) conditional); post-walk-1 → EXIT 0, TWO (the missing-lens cleared EARNED as the walk lines landed); stable at TWO through walk 3** — ⚠️ the walk-2/3 log lines said THREE from staleness, struck in place (seat 1). **Post-panel expected set: ONE — the (o1) conditional — once the panel line lands; freeze item 2 pins THAT set and fails on this paragraph ever reading as a placeholder.** Last run: walk 5 (the bar-meeting close) at the FAITHFUL mirror — EXIT 0, ONE WARN after the close landed (the transient mid-close fold-as-last-event WARN cleared EARNED) (seat 2's spelled row-5 path made `src/test_lessons_forge.py` an (o1) candidate resolved against the mirror root; freeze item 2 now mandates copying the real file in — a bare mirror shows a second, artifact-only WARN).

**Cold panel (T2):** sequential, author-verified and folded between seats; per-lens commits govern the folds.

**Panel meter (§2.6's 286 convention — added at seat 4, and the meter's own decay is already visible: seat 1 ran before the meter existed and its session closed):** seat 1 UNMETERED (recorded as LOST, not estimated); seat 2 ≈141k; seat 3 ≈135k; seat 4 ≈198k; seat 5 ≈125k; panel total ≈599k + seat 1 (LOST); findings 7+5+3+7+3 = 25; baseline 563k / 45 (the first metered panel). COMPLETE.
- **Seat 1 (weak spots + evidence-attack): 7 findings — 1 HIGH / 4 MED / 2 LOW; instruction 5 / record 2 — PLUS ONE LIVE INCIDENT, caused by the seat's sanctioned lint step and contained by it.** The incident: a lint copy staged at the REAL `decisions/` path was claimed AND DISPATCHED by the daemon within a second; a real agent ran the builder against the live template. Containment (seat, then Planner-verified at source): agent killed pre-commit/pre-backup/pre-flip; the dirty template proven byte-identical to the scratch builder output, restored to the A1 pin `886cfaca…`, porcelain clean; the corpus NEVER touched (three still `accepted` at the prior stamp); stop-verdicted as halted-354, the duplicate removed; **id 354 consumed-by-incident — the same-second-claim memory upgraded: it also dispatches.** The HIGH: the lint procedure itself (now a freeze-item-2 mandate: scratchpad-mirror only). MEDs: the walk-0 tail-literal claim was FALSE (0 pre-edit, not 1 — recalled-not-read; struck in the register, corrected-with-narrative at QA row 2 — seat 4: one true strike, one rewrite; the verb "struck at both sites" was wrong at one of them); 326's fourth quarter was declared-but-UNSCHEDULED — the very defect quarter 3 codifies — its DC-clarify FORWARD row now PINNED verbatim for the Rule-44 channel; Rule 95's "executed as one" overstated its evidence (corrected to the sources' permission-granting); the stale three-WARN log lines struck (TWO, enumerated in Conformance with the post-panel expected set). LOWs: two uncited numbers cut from doctrine text (builder wording, numstat stable `12 5`). Evidence-attack clean on everything else: both Rule-85 halves faithful, all pins/census/probes reproduced, DB exact, gate-neutrality with control.

- **Seat 2 (destruction + clone-diff): 5 findings — 1 HIGH / 1 MED / 3 LOW; instruction 5 / record 0; clone-diff across 35 hunks: 2 DROPPED (both vs grandparent 345), 3 mis-adapted, 30 faithfully carried, 0 re-added-after-deletion; all author-verified and folded.** The HIGH: **the builder bumped `**Version:**` but never touched the header's companion `**Last Updated:** 2026-08-11 (v4.86)` — 345 carried a dedicated edit (E9-lastupdated) for exactly this line, the DC-side hop (353) legitimately had none (DC's date lives in its Version line), and the re-adaptation lost it; nothing in the plan could see it, and the `12 5` numstat pin certified the defective builder.** Folded as builder E8 (count-1 anchor, dry-run re-measured: `OK — 8 edits applied`, numstat **`13 6`**, header coherent 4.87/(v4.87)) + the pin swept at every forward-looking site (Scope, freeze 3, TASK F, BUILDER assert) + QA row-3 probes (new form → 1, old → 0); E8 adds a second `v4.87` line — the row-scoped tail pipe re-measured, still 1. The MED (fold-introduced, seat 1 — the last-edit-least-reviewed class): the pinned FORWARD row's plan-id placeholder token had NO substitution site — the Rule-44 verbatim-copy channel this plan ships would have carried the literal placeholder into the governance register; freeze item 1 extended with its own occurrence probe (→ 2, both on the checklist line). LOWs: A0 branch-4's defining predicate restored (353 spelled it; an undefined state in a first-match ladder forces improvisation); 345's two gate-coupling sweeps restored to QA row 7 — rule-number coupling (exactly 20/22/26; Rule 95's number must stay absent) and the `:NN` line-cite sweep (deletion premise verified true TODAY, but the in-window half had no run-time observer); QA row 5's literal `…` spelled to `<tree-abs>/src` + 345's report-not-halt branch restored (the adjacent forge project's 8+ test modules break "exactly one" on a one-level-high find). Everything else verified clean against live data: A1 sha = 345's shipped blob (incident restore byte-exact), all probes/censuses/neighbours reproduced, DB exact (ACC=3, MAXID=326, CAP=323, GLOBPRIOR=3), consumer semantics at `:31` both sides, gate-neutrality 0/0×4 control 11, mirror lint EXIT 0 at the declared ONE WARN, freeze id-token occurrence geometry as pinned, no incident residue.

- **Seat 3 (vulnerabilities, register-plain): 3 findings — 0 HIGH / 2 MED / 1 LOW; instruction 3 / record 0; all pre-existing (2 clone drops vs 345, 1 origin-carried); all author-verified and folded. The lens's six-for-six HIGH streak ENDS — earned by 36 scratch-only rehearsals reproducing every stated pin (the honest reading: the machinery is clone-verified, the drops were in what the clone REFERENCED but didn't spell).** MED 1: the load-bearing rules census named the end boundary `## Lifecycle DB Read Protocol` but the live heading is `## Lifecycle DB Read Protocol (Planner)` — an exact-line boundary raises a confident false absence ON THE VERIFICATION PROBE at both steps, and the plan's "the spelled form" spelled nothing (345 carried the verbatim `startswith` one-liner; restored, remapped, re-earned: `RULES 95 1 95 True True` on the built scratch copy). MED 2: TASK F's numstat and F2's name-only asserts had no spelled command — a bare git form in a separate invocation reads the WORKTREE repo from the step's cwd (measured: `191 0 …executable-353.md`); spelled with `-C` + **CAPTURE_COMMIT (the TASK-F-printed hash, never `HEAD`) — upgraded mid-fold by LIVE evidence: a parallel terminal's foreign commit landed on root HEAD between panel seats 2 and 3** (id 355 consumed by an invoice-pulse deposit; this plan's DB battery re-verified unchanged: MAXID=326, ACC=3). LOW: A0 branch 2's "then B → G" after a reused backup literally re-ran the `.backup` creation, leaving the prefix-only `find` with two matches and no tiebreak — B gains the skip-on-reuse clause and a >1-match HALT. Adversarial rehearsals all fail-safe: G2 re-run CHANGES=0, in-window half-flip CHANGES=2, `.output`-to-missing-dir aborts BEFORE the UPDATE under `-bail`; builder all-or-nothing proven by re-run assert (file untouched); incident containment re-verified at every reachable surface.

- **Seat 4 (integration-record + clone-diff-record): 7 findings — 0 HIGH / 2 MED / 5 LOW; instruction 1 / record 6 — the record-heavy split this seat exists for; all author-verified and folded. Zero phantom folds: ~20 register resolutions all probed LANDED against the artifact and the per-phase diffs; the number audit ran 30 MATCH / 1 MISMATCH / 1 STALE / 1 UNVERIFIABLE; the whole Conformance WARN history (3→2→2→1→1) reproduced by linting the per-phase commits at the mirror.** MED 1 (the mismatch): register row 4's origin cell blanketed all seat-1 findings "pre-existing" — the Conformance history itself proves the stale three-WARN lines were walk-2/3-born (v0 measured THREE, post-walk-1 TWO; the lines were stale AT WRITE) — blanket corrected in place, 6-of-7 + 1 walk-born. MED 2 (the unverifiable, and the meter's own lesson): no §2.6 panel meter existed through seat 3, and seat 1's session closed unmetered — the meter line now added ABOVE the seat lines records seat 1 as LOST, not estimated (in-window decay, measured). LOWs: the register Deviations line completed (commit range spelled; compound lens cells and the one-row-per-seat coarsening now declared); walk-0 rows 2/5 gained the supersession pointers their draft twin already had (asymmetric sweep, seat 2); the ID NOTE's "three consumed ids" was stale by seat 3's own account — now FOUR (355 in-window); seat 1's "struck at both sites" verb corrected (one true strike, one rewrite-with-narrative); and the one INSTRUCTION finding (origin-carried, 4.4): E4's shipped freeze rule meets `halted-executable-334.md` sitting in live `decisions/` ON DAY ONE with no parked-plan boundary — the doctrine text now names it (a `halted-*` artifact is PARKED, not pending — no freeze, its resume re-verifies; `in-progress-*` DOES freeze, deliberately), dry-run re-measured stable (`OK — 8 edits`, numstat `13 6`, all probes unchanged). Incident audit clean: 354's stop-verdict disposition matches the draft clause-for-clause; no residue; 351/352/355 all as recorded; the foreign commit range fully disjoint.

- **Seat 5 (ACID + system capstone, ran LAST on the artifact as folded by seats 1–4): 3 findings — 0 HIGH / 1 MED / 2 LOW; instruction 1 / record 2. VERDICT: SAFE-AS-WRITTEN — zero blocking findings; all folds are hardening or record repair.** Execution: 27 MATCH / 1 MISMATCH (the mismatch was a stale record line, not machinery) — builder, both censuses via the plan's own spelled forms, the full copy-DB flip rehearsal (CHANGES=3/GLOBOK=3/capture 323/re-run 0), all four freeze items rehearsed including the Closing assert's earnability (it FAILS the current placeholder), faithful-mirror lint at the declared ONE WARN. Isolation: 9 windows STATED / 2 LUCKY / 0 MISSING; fold-interaction matrix 6 seat-pairs, 0 CONFLICT; all half-states classified by A0 first-match-wins with correct branch ordering; durability loss-combinations all owned. The MED (instruction, origin-carried — 353 dropped 345's spelled form): the discovery-by-slug command was spelled at NONE of its three sites while seat 3's sweep fixed only TASK F/F2 — a bare `git log` from the QA cwd reads the worktree repo → false-HALT on the happy QA path and both resume branches (fail-safe, never false-pass); folded: spelled once at QA row 1 (`-C`-pinned, one-line `--grep` form, proven against 345's own commit), A0 branches 1/2 now reference it. LOW 1 (record, the asymmetric-sweep tail): walk-1's "`v4.87` total post = 1" was stale post-E8 (measured 2) — the supersession pointer walk-0's twin got at seat 4, closed here. LOW 2 (record, structural): the register Deviations recovery range couldn't name seat 4's own commit — extended with `823eb0b` and an open tail. Plus one hardening adopted: freeze item 5, the post-copy `ls` verify closing the seconds-wide id-consumption window (four ids consumed around this plan).

**Walk 4 — the post-panel bar-meeting close, pass 1** (whole artifact; new surface = the panel's ~22 folds):
- Weak spots:          w4 1 folded — instruction, origin-carried (QA row 1's `name-only exact` was the ONE git assert the seat-3/5 spelling sweeps missed — a bare form reads the worktree repo, fail-safe direction; now references F2's spelled `-C` + `<commit>` form).
- Destruction:         w4 dry (E4's anchor asserts the OLD text — the amendment rides the replacement; B/freeze additions all additive; nothing relaxed).
- Vulnerabilities:     w4 executed — builder re-run `OK — 8 edits` numstat `13 6` stable; sha at the A1 pin; DB battery PRE=3/ACC=3/MAXID=326; tail pipe 1; token geometry 4/3; lint at the faithful mirror EXIT 0, ONE WARN.
- Integration-record:  w4 dry (meter arithmetic ✓; seat lines ↔ register rows 4–8 consistent; supersession-pointer policy uniform: standing pins annotated, dated event records left historical).
- ACID:                w4 1 folded — instruction, fold-introduced (seat 5): freeze item 5 is a POST-copy check but the checklist header still promised "(deposit path, before the copy)" — the header now states the 1–4/5 boundary. Fold-interaction class: the panel's last fold vs the v0 header, exactly the least-reviewed-edit seam.

**Walk-4 split: instruction 2 / record 0; origin 1 origin-carried + 1 fold-introduced (seat 5).** Re-opens; walk 5 owed on the two edits.

**Walk 5 — the bar-meeting close** (whole artifact; new surface = walk 4's two edits):
- Weak spots:          w5 dry (both walk-4 folds re-verified FROM THE ARTIFACT against fresh measurement: row-1 spelled form 1, new header 1, old header text 0 in instructions — the probe's one hit is walk 4's own record QUOTING it, checked).
- Destruction:         w5 dry.
- Vulnerabilities:     w5 executed — lint EXIT 0; ⚠️ the mid-close state showed a TRANSIENT second WARN (closing-check: fold-as-last-event, correct while walk 4's folds were the last recorded event) — cleared EARNED by this walk's dry lines and the real Closing below, re-lint verified at ONE WARN; token geometry 4/3 stable; `<commit>` ×6 all run-time placeholders.
- Integration-record:  w5 dry on instructions; 1 record fold — the Conformance last-run line refreshed to this walk (landing OUTSIDE the closing record: enumerated as residue, re-read below).
- ACID:                w5 dry (header boundary ↔ item semantics coherent; row-1 form ↔ F2 consistent; no new interactions).

**Walk-5 split: instruction 0 / record 1 (the Conformance refresh, residue-enumerated). THE BAR IS MET — zero instruction-class findings on a full untargeted walk.**

**Closing:** the bar is met at walk 5 — **instruction 0 / record 1** (the Conformance last-run refresh, the sole residue outside this record, re-read). Walk 4 folded the panel's two remaining seams (both instruction: the one unspelled git assert both spelling sweeps missed; the seat-5 post-copy item vs the v0 header's promise — the least-reviewed-edit class, caught by the close the doctrine prescribes for it). Cycle totals: 5 walks + a 5-seat sequential cold panel; panel 25 findings (2 HIGH, both seats 1–2), ≈599k tokens + seat 1 LOST; two honest strikes and one origin-blanket correction stand in the record; the closing-record re-read (§2.7) done on this line, the walk-4/5 blocks, and register rows 9–10. Deposit rides the 5-item freeze.

---

## STEP 1 — DEV (run the builder, commit, flip the three)

> **FIRST — visible chat message; do NOT rename this plan file.** The template edit lands at the ABSOLUTE root path; a HALT after edits leaves the tree as-is, reported loudly.
> **A0 (first match wins):** (1) three `implemented` → verify commit-by-slug `gate2-pt3-2026-08-12` via the QA row-1 SPELLED discovery (NOT FOUND → HALT), deposits survived, RECOVERY receipt if the capture is lost + fresh G3 + row-4 DECLARED FALLBACK named; report complete. (2) docs committed (the QA row-1 spelled discovery returns the commit) + three still `accepted` → **assert the template path's porcelain clean ON TOP of the commit (post-commit dirt → report LOUDLY, never sweep)**, DOC_SHA FROM THE COMMIT, reuse any `pre-pt3-*` backup via prefix-only `find` (BK assert mandatory either way), re-run the SEVEN probes against the committed content — probes 1–6 via `git -C /Users/marklehn/Developer/GitHub show <commit>:PLANNER_TEMPLATE.md | grep -cF …`; **probe 7 (the python census) reads a FILE, so materialize first: `S=$(mktemp -d) && git -C /Users/marklehn/Developer/GitHub show <commit>:PLANNER_TEMPLATE.md > "$S/committed.md"` and census that path, same invocation (walk 1: the pipe form silently cannot feed it)** — then B → G. (3) **docs modified-UNCOMMITTED** (porcelain dirty for the template path) → **HALT**; sweep the seven probes against the live file, DEPOSIT the landed/not-landed table at `knowledge/qa/evidence/gate2-pt3-2026-08-12/resume-sweep.txt` (joins Scope on this path only), report — CEO directs. (4) **fresh-with-unexplained-backup** (a `pre-pt3-*` backup exists, no template commit naming the slug, three `accepted`) → **HALT**. (5) fresh — porcelain clean FOR THE TEMPLATE PATH AND THE E0 DENYLIST FILES ONLY (the root carries near-permanent benign dirt), version reads `4.86`, three `accepted|codify` stamped `2026-08-12T00:29:31Z` with categories all `governance_rule`, no `pre-pt3-` backup → A1. Version cross-check every path (4.86, or 4.87-changelog-head-names-THIS-slug). ⚠️ **A state matching NO branch → HALT with the observed triple (porcelain + version/changelog-head + per-id read-back) — STATE-level, distinct from the version check.**
> **A1 — pin:** `shasum -a 256 /Users/marklehn/Developer/GitHub/PLANNER_TEMPLATE.md` == `886cfaca36cd5f4e0e0150400220fcd98aff148b9109ff969e7fdf401d1b041e`. Mismatch → HALT.
> **BUILDER:** run exactly `python3 /Users/marklehn/Developer/GitHub/governance/knowledge/research/builder-gate2-pt3-2026-08-12.py /Users/marklehn/Developer/GitHub/PLANNER_TEMPLATE.md /Users/marklehn/Developer/GitHub/PLANNER_TEMPLATE.md` → exit 0 + `OK — 8 edits applied` naming all eight. **AssertionError → HALT, quote verbatim — the file is UNTOUCHED by construction; never patch, never retry modified.** Post-conditions, `grep -cF` (`--` where dash-leading) on the live file:
> 1. `An equally blessed form` → 1
> 2. `A DECLARED BLANKET form is equally valid` → 1
> 3. `Owner-mismatch at wrap` → 1
> 4. `a CORPUS FREEZE on its pinned file` → 1
> 5. `### 95. A step body has exactly ONE reader` → 1
> 6. `**Version:** 4.87` → 1; `**Version:** 4.86` → 0; `**Last Updated:** 2026-08-12 (v4.87)` → 1; `**Last Updated:** 2026-08-11 (v4.86)` → 0 (E8, the header's companion — seat 2; zero-expectations per env fact 1); `the PARTIAL convention` → 1
> 7. Rules census via python — run exactly (seat 3: the live end-boundary heading is `## Lifecycle DB Read Protocol (Planner)`, so the boundary MUST be `startswith`, never an exact-line match; 345's spelled form restored, remapped): `python3 -c "import io,re;L=io.open('/Users/marklehn/Developer/GitHub/PLANNER_TEMPLATE.md',encoding='utf-8').read().split(chr(10));s=L.index('## Orchestration Plan Rules');e=next(i for i,l in enumerate(L) if l.startswith('## Lifecycle DB Read Protocol'));n=[int(re.match(r'### (\d+)\.',l).group(1)) for l in L[s:e] if re.match(r'### \d+\.',l)];print('RULES', len(n), min(n), max(n), n==sorted(n), len(set(n))==len(n))"` → must print `RULES 95 1 95 True True` (at A0 branch 2, substitute the materialized `$S/committed.md` path). H2/H3 census `grep -cE '^## '` → **30 unchanged**, `grep -cE '^### '` → **191** (was 190 — the one new rule heading).
> **E0 denylist:** porcelain — `PLANNER_TEMPLATE.md` modified; HALT iff any of `DRAFTING_CYCLE.md`, `RULE_20_SELF_CHECK_BLOCK.md`, `READONLY_AUDIT_CONTRACT.md`, `SPECIALIST_TEMPLATE.md`, `INTERMEDIATE_DECISION_PHRASES.md` dirty; other root dirt reported, never a HALT.
> **DOC_SHA** pinned before commit. **TASK F:** `cd /Users/marklehn/Developer/GitHub && git add PLANNER_TEMPLATE.md && git commit -m "[356] gate2(gate2-pt3-2026-08-12): the PT three — Rule 85 -C amendment, Rule 93 blanket form, Rule 44 wrap channel, the wrap freeze-check, Rule 95 — template 4.86 -> 4.87" -- PLANNER_TEMPLATE.md && git rev-parse HEAD && git rev-parse --show-toplevel` (BARE rev-parses per the very amendment this plan ships — the compound mixes cd-first with -C-less commands; expect the new commit hash then the governance root; wrong/missing → NOT COMMITTED). **The printed hash is CAPTURE_COMMIT — every post-commit assert below names it, NEVER `HEAD`: a parallel terminal commits to this root in-window (proven live during this cycle's panel — a foreign commit landed on root HEAD between two panel seats).** Numstat, spelled (seat 3: a bare git form in a separate invocation reads the WORKTREE repo from the step's cwd — measured): `git -C /Users/marklehn/Developer/GitHub show <CAPTURE_COMMIT> --numstat --format=` → exactly the one line `13	6	PLANNER_TEMPLATE.md`; different → HALT. **F2:** the committed CONTENT's sha — `git -C /Users/marklehn/Developer/GitHub show <CAPTURE_COMMIT>:PLANNER_TEMPLATE.md | shasum -a 256` — == DOC_SHA; name-only spelled: `git -C /Users/marklehn/Developer/GitHub show <CAPTURE_COMMIT> --name-only --format=` → exactly `PLANNER_TEMPLATE.md`.
> **B — backup** (seat 3: if A0 branch 2 already reused a found `pre-pt3-*` backup, SKIP the `.backup` creation — ONE backup ever, the assert runs against the found file; the prefix-only `find` returning MORE THAN ONE match → HALT, the state is unexplained), exactly: `sqlite3 -bail /Users/marklehn/Developer/GitHub/lessons-forge/lessons-forge.db ".timeout 5000" ".backup /Users/marklehn/Developer/GitHub/lessons-forge/pre-pt3-$(date -u +%Y%m%d_%H%M%S).db"`; locate via prefix-only `find`; assert against THE FOUND BACKUP's absolute path: `sqlite3 -bail "file:<found-backup-abs>?immutable=1" ".timeout 5000" "SELECT 'BK='||COUNT(*) FROM lesson_proposals WHERE id IN (316,324,326) AND status='accepted';"` → **BK=3**; else HALT.
> **G1 — rehearsal** (runner: `sqlite3 -bail /Users/marklehn/Developer/GitHub/lessons-forge/lessons-forge.db ".timeout 5000" ".read <abs>"`, exit-0 + empty-stderr per invocation, same-invocation scratch outside git trees; file `knowledge/development/pt3-rehearsal.sql`, exactly):
> ```
> BEGIN IMMEDIATE;
> SELECT 'PRE='||COUNT(*) FROM lesson_proposals WHERE id IN (316,324,326) AND status='accepted' AND route='codify';
> SELECT 'ACC='||COUNT(*) FROM lesson_proposals WHERE status='accepted' AND route='codify';
> SELECT 'MAXID='||MAX(id) FROM lesson_proposals;
> ROLLBACK;
> ```
> Assert **PRE=3, ACC=3, MAXID=326** (fewer → HALT naming ids; ACC>3 → in-window routing → HALT; MAXID>326 reported-benign, the capture bound stays).
> **G2 — the flip** (`mkdir -p` the evidence dir FIRST; file `knowledge/development/pt3-flip.sql`, exactly; `.output` absolute from `pwd`):
> ```
> BEGIN IMMEDIATE;
> .output <tree-abs>/knowledge/qa/evidence/gate2-pt3-2026-08-12/outside-range-ids.txt
> SELECT id||'|'||category||'|'||status||'|'||COALESCE(route,'')||'|'||COALESCE(status_updated_at,'')||'|'||COALESCE(status_updated_by,'') FROM lesson_proposals WHERE id <= 326 AND id NOT IN (316,324,326) ORDER BY id;
> .output stdout
> UPDATE lesson_proposals SET status='implemented', status_updated_at=strftime('%Y-%m-%dT%H:%M:%SZ','now'), status_updated_by='ceo' WHERE id IN (316,324,326) AND status='accepted';
> SELECT 'CHANGES='||changes();
> SELECT 'GLOBOK='||COUNT(*) FROM lesson_proposals WHERE id IN (316,324,326) AND status_updated_at GLOB '20[0-9][0-9]-[0-9][0-9]-[0-9][0-9]T[0-9][0-9]:[0-9][0-9]:[0-9][0-9]Z' AND status_updated_at NOT IN ('2026-08-12T00:29:31Z');
> COMMIT;
> ```
> **Sentinels CHANGES=3, GLOBOK=3** (the prior stamp is Z-form GLOB-MATCHING — the `NOT IN` carries the guard); either off → HALT with numbers. Capture **323 lines**, read post-commit — mismatch = record + HALT with the flip landed, never re-run. The three-id set is IMMUTABLE.
> **G3 — read-back**, exactly: `sqlite3 -bail -readonly /Users/marklehn/Developer/GitHub/lessons-forge/lessons-forge.db ".timeout 5000" "SELECT id||'|'||category||'|'||status||'|'||COALESCE(route,'')||'|'||status_updated_by||'|'||status_updated_at FROM lesson_proposals WHERE id IN (316,324,326) ORDER BY id;"` → RAW to `flip-readback.txt`: per id, all `governance_rule|implemented|codify|ceo|<Z ≠ 2026-08-12T00:29:31Z>`.
> **Receipt** with SIX named sentinels — PRE, ACC, MAXID, BK, CHANGES, GLOBOK (the receipt IS BK/MAXID's observer) — + DOC_SHA + the commit hash + the numstat pair · `### Ledger Updates` · `#### Prompt Feedback` · `#### Forward Register`: `NONE`. **FINAL ACTION** — commit deposits: `cd <your-tree-abs>` first token, pathspec on the COMMIT, name-only assert, bare `git rev-parse --show-toplevel` last.
>
> **Scope:**
> - `knowledge/development/dev-log-gate2-pt3-step-1-2026-08-12.md`
> - `knowledge/development/pt3-rehearsal.sql`
> - `knowledge/development/pt3-flip.sql`
> - `knowledge/qa/evidence/gate2-pt3-2026-08-12/outside-range-ids.txt`
> - `knowledge/qa/evidence/gate2-pt3-2026-08-12/flip-readback.txt`
>
> **Deposits:**
> - `lessons-forge/knowledge/development/dev-log-gate2-pt3-step-1-2026-08-12.md`
> - `lessons-forge/knowledge/development/pt3-rehearsal.sql`
> - `lessons-forge/knowledge/development/pt3-flip.sql`
> - `lessons-forge/knowledge/qa/evidence/gate2-pt3-2026-08-12/outside-range-ids.txt`
> - `lessons-forge/knowledge/qa/evidence/gate2-pt3-2026-08-12/flip-readback.txt`

## STEP 2 — QA

> **FIRST — do NOT rename this plan file. Deliverable Verification (Rule 8/17)**, ✅/❌ table, any ❌ → HALT. **Rule 20 self-check** (canonical block from `/Users/marklehn/Developer/GitHub/RULE_20_SELF_CHECK_BLOCK.md`; `plan_slug`: `gate2-pt3-2026-08-12`; `qa_report_path`: `<tree>/knowledge/qa/gate2-pt3-qa-2026-08-12.md`; `evidence_dir`: `<tree>/knowledge/qa/evidence/gate2-pt3-2026-08-12/`; `required_evidence_files`: `[doc-integrity.txt, db-invariants.txt, gate-neutrality.txt, pytest_targeted.txt]`, all four BEFORE the block; literal stdout: the banner `Rule 20 — QA Self-Check Results` and the `PASSED — SELF-CHECK PASSED` line, byte-exact). `## Evidence and Narrative` after the table. ONE read-only DB form; RAW evidence.
> **1. DOC INTEGRITY** — commit discovered by slug, spelled (seat 5: a bare `git log` from the QA cwd reads the WORKTREE repo — the same class seat 3 measured): `git -C /Users/marklehn/Developer/GitHub log -n 1 --format='%H %s' --grep='gate2-pt3-2026-08-12' -- PLANNER_TEMPLATE.md` → exactly one line naming the slug (empty output = NOT FOUND → HALT); the committed CONTENT's sha (`git -C /Users/marklehn/Developer/GitHub show <commit>:PLANNER_TEMPLATE.md | shasum -a 256` — the `e3b0c442…` output means the show FAILED) == live == DOC_SHA; porcelain empty for the template path; name-only exact via F2's spelled form with `<commit>` (`git -C /Users/marklehn/Developer/GitHub show <commit> --name-only --format=` → exactly `PLANNER_TEMPLATE.md` — walk 4: the one git assert the seat-3/5 spelling sweeps missed). → `doc-integrity.txt`
> **2. THE CLAUSES LANDED** — the seven Step-1 post-condition probes re-run independently, incl. the python rules census (95/1/95/monotonic/unique) and H2/H3 30/**191**; **tail probe** `grep -cF 'Numbering append-only.'` scoped to the NEW changelog row via `grep -F 'v4.87' <file> | grep -cF 'Numbering append-only.'` → 1 (the row's final clause — ⚠️ seat 1 measured the bare literal at **0 pre-edit** (the v4.86 row reads 'append-only per the 4.83 precedent', no period): the walk-0 claim of 1-pre-edit was recalled-not-read, the same class as the walk-2 strike; the bare `grep -cF 'Numbering append-only.'` → 1 is itself earnable, and the pipe-scoped form stays as belt-and-braces). → `doc-integrity.txt`
> **3. COHERENCE** — `**Version:** 4.86` → 0 with the v4.86 CHANGELOG row intact (`grep -cF 'v4.86: Gate 2 batch 2'` → 1); header companion coherent (seat 2's E8 — 345's E9 machinery restored): `grep -cF -- '**Last Updated:** 2026-08-12 (v4.87)'` → 1 AND `grep -cF -- '**Last Updated:** 2026-08-11 (v4.86)'` → 0 (zero-expectation: printed count is the assertion); Rule 94's Source line intact above the new Rule 95; the Rule-85/93/44 neighbour text intact (three spelled probes: `grep -cF 'never trust cwd persistence'` → 1, `grep -cF 'FORWARD 52, forge-owned'` → 1, `grep -cF 'computing next row number and date automatically'` → **1** (measured both sides at walk 2 — ⚠️ the fold that firmed this pin FIRST wrote 2 from recalled site-counting WHILE THE MEASUREMENT PRINTING 1 SAT IN THE SAME OUTPUT; struck and corrected within the same walk — the predicted-number class committed inside the fold claiming to fix a hedge)). → `doc-integrity.txt`
> **4. FLIP + BLAST RADIUS** — (a) per id: all three `governance_rule|implemented|codify|ceo`, Z-stamped ≠ the pinned prior (categories per the authoring measurement — all three `governance_rule`); (b) **`accepted|codify` → 0 — THE GATE-2 QUEUE FULLY DRAINED, again**; (c) re-run the EXACT capture SELECT (COPY from G2's block, never re-type), diff vs the deposited 323-line file; partition (impossible-id → HALT; deleted-row → HALT; else concurrent, named, no halt). DECLARED FALLBACK (capture lost to a crashed worktree): degraded co-stamp sweep, labelled. → `db-invariants.txt`
> **5. TESTS** — single-module premise re-derived: `find <tree-abs>/src -name 'test_*.py'` → exactly `src/test_lessons_forge.py` (a second module = report + run whole `src/`, never HALT — 345's branch, restored by seat 2; ⚠️ the path is spelled because a one-level-high find hits the adjacent forge project's 8+ test modules and breaks "exactly one"); pytest vs 55/0, delta reported never asserted. → `pytest_targeted.txt`
> **6. CONSUMER SEMANTICS** — quote `/Users/marklehn/Developer/GitHub/lessons-forge/src/lessons_forge.py:31` verbatim (`implemented` IS terminal, `accepted` is NOT); `get_unclassified_entries` (read-only) → the three source entries (308, 316, 318) ABSENT before and after (preservation; the terminal-boundary crossing is the intended meaning of codified). → `db-invariants.txt`
> **7. GATE-NEUTRALITY** — (a) `equally blessed`, `CORPUS FREEZE`, `exactly ONE reader`, `Owner-mismatch` → 0 in both `/Users/marklehn/Developer/GitHub/bellows/scripts/plan_lint.py` AND `/Users/marklehn/Developer/GitHub/bellows/gates.py` (gates.py at the bellows ROOT); positive control `Drafting Cycle` in plan_lint.py → 11. ⚠️ All four expected values are 0 — env fact 1: the printed count is the assertion, never `&&`-chain. (b) Rule-number coupling (345's sweep, restored by seat 2 — the in-window observer): `grep -rohE 'Rule [0-9]+' /Users/marklehn/Developer/GitHub/bellows/gates.py /Users/marklehn/Developer/GitHub/bellows/scripts/plan_lint.py | sort -u` → exactly `Rule 20`, `Rule 22`, `Rule 26` (anything new — including `Rule 95` — means a coupling landed in-window → HALT). (c) Line-citation sweep: `grep -rn -E 'PLANNER_TEMPLATE[^ ]*:[0-9]+' /Users/marklehn/Developer/GitHub/bellows --include='*.py'` → zero hits (prints nothing, exits 1 — env fact 1; (b) is the positive-output control for the instrument). → `gate-neutrality.txt`
> `## Evidence and Narrative` · Receipt · `### Ledger Updates` · `#### Forward Register`: `NONE`. **FINAL ACTION** — commit deposits, cd-first + pathspec + name-only + bare toplevel.
>
> **Scope:**
> - `knowledge/qa/gate2-pt3-qa-2026-08-12.md`
> - `knowledge/qa/evidence/gate2-pt3-2026-08-12/doc-integrity.txt`
> - `knowledge/qa/evidence/gate2-pt3-2026-08-12/db-invariants.txt`
> - `knowledge/qa/evidence/gate2-pt3-2026-08-12/gate-neutrality.txt`
> - `knowledge/qa/evidence/gate2-pt3-2026-08-12/pytest_targeted.txt`
>
> **Deposits:**
> - `lessons-forge/knowledge/qa/gate2-pt3-qa-2026-08-12.md`
> - `lessons-forge/knowledge/qa/evidence/gate2-pt3-2026-08-12/doc-integrity.txt`
> - `lessons-forge/knowledge/qa/evidence/gate2-pt3-2026-08-12/db-invariants.txt`
> - `lessons-forge/knowledge/qa/evidence/gate2-pt3-2026-08-12/gate-neutrality.txt`
> - `lessons-forge/knowledge/qa/evidence/gate2-pt3-2026-08-12/pytest_targeted.txt`
