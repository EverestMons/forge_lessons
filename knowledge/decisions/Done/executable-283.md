# Lessons Forge — Cycle Run 2026-07-29 (ingest + classify the 8-entry session-12 batch)
**Date:** 2026-07-29 | **Tier:** Small | **Dispatch Mode:** bellows | **Test Scope:** full-suite | **Execution:** Step 1 (Lessons Agent — ingest + classify all 8) → Step 2 (DEV — report) → Step 3 (QA) | **qa_steps:** 3 | **pause_for_verdict:** always
**cycle_tier:** T2

## CEO Context

Cycle run only: ingest the 8 un-ingested `LESSONS.md` entries (all dated 2026-07-28, captured from plan 282's 100-fold drafting cycle) and classify them into proposals. Gate 1 (route disposition) and Gate 2 (codification) are separate plans with CEO decisions between.

**Clone lineage.** Direct clone of **281** (the newest same-class cycle plan); its lineage is 274 → 257 → 247. Per entry 183's rule — *clone against the NEWEST same-class plan* — this draft ALSO diffs against **282** (the newest plan of any class on this corpus), which hardened QA mechanics 281 does not carry.

**⚠️⚠️ FOUR INHERITED CONDITIONS FROM 281 ARE FALSE HERE** and would false-HALT or false-PASS if cloned unchanged:

1. **`proposed` is NOT zero at baseline.** 281's G1 required *zero* non-terminal proposals and HALTed on any with `entry_id ≤ E0`. **Measured 2026-07-29: proposals 191 and 192 are `proposed`, on entry_ids 183 and 184 — both ≤ E0.** They are the Gate-2-pending pair from cycle 281, deliberately parked. Cloning 281's G1 verbatim HALTs this cycle on its own correct baseline.
2. **The report will surface 10 proposals, not 8.** `generate_lessons_report` is whole-corpus over `status IN ('proposed','ambiguous')`, so it surfaces this cycle's 8 **plus** 191/192.
3. **The report WILL render `- **Route:**` lines.** 191/192 carry `route='codify'` and `src/lessons_forge.py:584` renders the line under `if route is not None` (**verified in code at authoring**), so **exactly 2 route lines are EXPECTED**; zero would be the anomaly.
4. **Categories and targets are MIXED, not uniform.** 281 asserted `category='governance_rule'` on every proposal. This batch is 5 `planner-discipline` + 3 `bellows-integration`, and the latter describe live tooling defects where `instrumentation` is legitimate.

**⚠️⚠️ ITEMS 1–3 ALL REST ON ONE PREMISE: that Gate 2 has NOT run when this plan is dispatched.** If the CEO ships Gate 2 first, proposals 191/192 become `implemented` (terminal) and **all four of the following flip at once**: G1's expected non-terminal set becomes empty, the report surfaces 8 not 10, it renders 0 route lines not 2, and QA row 4b's id set becomes empty. ⚠️ **Row 4b does NOT invert** — it adjudicates `status` directionally (`proposed → terminal` is a `✅` with a recorded note), so a Gate-2 ship in any window passes it by design; only a move to `stale` fails it. That state is strictly *safer* than the one this plan is written for — nothing is staleable — but a plan hardcoding 10/2/`proposed` would **false-HALT on the safer baseline**. **Therefore no downstream step hardcodes those numbers: Step 1a captures the ACTUAL non-terminal set, and Step 2 and QA rows 4b/5 anchor to that capture.** The literals below are the Planner's measured expectation for the un-shipped-Gate-2 case, not gate values. ⚠️ **Note the Gate-2-shipped state is safer with respect to STALING only — see Step 1a-bis item 2b for a mutation it makes MORE likely, not less.**

**⚠️⚠️ NUMBERING — THREE NAMESPACES COLLIDE ON THE NUMERAL 192.**
- **`lesson_entries.id` 185–192** — THIS batch's 8 entries (after ingest).
- **`lesson_proposals.id` 193–200** — THIS batch's 8 proposals (after classification).
- **`lesson_proposals.id` 191–192** — the PRE-EXISTING Gate-2-pending pair (entry_ids 183/184). **Leave untouched.**

**Never write a bare "192".** File-position counts (135 `##` headings in `LESSONS.md`) are a fourth namespace: the corpus row count is 184, NOT 135 — the surplus are orphan rows from reworded headings, all classified, which is why `get_unclassified_entries()` is `[]` pre-cycle. **After this cycle expect 192 entry rows (184 + 8) and 200 proposals (192 + 8).**

**⚠️ E0 / P0 — the resume-invariant handles** (confirm at Step 1a; HALT if either differs on a fresh run): **`E0 = MAX(id) FROM lesson_entries = 184`, `P0 = MAX(id) FROM lesson_proposals = 192`.** Verified 2026-07-29: `sqlite_sequence` == `MAX(id)` for both, **no gap** → inserts land exactly at entries 185–192 / proposals 193–200; G6 still derives the range arithmetically. **`entry_id > 184` is the canonical handle; `route IS NULL` is NOT.** On a RESUME use the recorded `E0`/`P0` — do NOT re-run `MAX(id)`.

**⚠️ G1 precondition — REWRITTEN for this cycle.** The ingest's update path stales **non-terminal** proposals; the plan-204 `_TERMINAL_STATUSES` guard protects only the terminal set. **Measured 2026-07-29: exactly TWO non-terminal proposals — 191 (entry_id 183) and 192 (entry_id 184), both `proposed`, both `route='codify'`.** So the fresh-run ingest is **conditionally** non-destructive: it is safe precisely because **no entry's `content_hash` changes**. Any OTHER non-terminal proposal with `entry_id ≤ E0` voids the guarantee → HALT before the ingest.

**⚠️⚠️ G4 DETECTS THIS DAMAGE; IT CANNOT PREVENT IT — hence the pre-ingest guard (Step 1a-bis).** Verified in code: the staling `UPDATE lesson_proposals SET status='stale' … WHERE entry_id = ? AND status != 'stale' AND status NOT IN (<terminal>)` sits at `src/lessons_forge.py:187-193`, **inside the hash-changed branch of the ingest itself**. By the time `run_full_lessons_cycle` returns and G4 reads `updated_count`, any staling is already written. **And the instrument that would measure it is discarded:** `ingest_lesson_entries` computes `stale_proposals_marked` (`:135`) but `run_full_lessons_cycle` **omits that key from its return dict** (`:503-511`, which does retain `terminal_proposals_flagged`). This is invisible in 281, where nothing was staleable. **So the plan must check the parent entries' hashes BEFORE the ingest, and the verified `.backup` is the actual recovery path for 191/192.**

**⚠️ The hash-trap watch — PLANNER-PRE-VERIFIED, and it now protects 191/192.** **Target: entry id 184**, `content_hash` `f2cf892c30e3544e36b4c1e76766f52497ac3770918ee9fba3820fd398e103a2`. The Planner ran a read-only WHOLE-CORPUS re-hash and confirmed **8 would-INGEST, 0 would-UPDATE, 127 unchanged** over 135 file entries — **re-measured at cycle close, not carried from a prior session.** Entries 183 and 184 are the parents of proposals 191/192, so a hash flip on either stales a proposal the CEO is mid-arc on. **Expectation: `ingested_count == 8`, `updated_count == 0`, `terminal_proposals_flagged` empty. Verify and report ACTUAL values; do not force the number.**

**The 8 entries — 5 `planner-discipline` + 3 `bellows-integration`.**

**Placement scout** — the Planner's reading, verified against live doctrine at authoring. ⚠️ **Deliberately carries no ad-hoc rule citation.** Plans 281 and 282 both labelled this a "Rule 27" scout; **Rule 27 (`PLANNER_TEMPLATE.md:824`) is "Diagnostic-derived plans cite findings, never supplement with source reads"** — it does not govern `target_artifact` placement and does not apply here (this plan has no preceding diagnostic). Checklist #33 (`:1315`) is cross-repo audit/archival contracts, also not it. **The governing rule is Rule 58 (`:1093`), "Pre-stated conclusions require verification anchors and equal evidence burden"** — found only by opening it, after an earlier draft wrongly concluded no rule existed while reconstructing Rule 58's own phrase "explicit licence to disagree" verbatim. Rule 58 requires: (1) named verification anchors **and explicit licence to disagree**; (2) **a statement that the pre-resolutions are a fact about which items were investigated, NOT a distribution**; (3) equal evidence burden on every disposition.

**Rule 58(2), stated explicitly because both this plan and 281 omitted it:** ⚠️ **This table records where the Planner looked, not a distribution.** No claim is made about any entry not listed; a placement absent from it is not a placement rejected; the three artifacts named do not bound the answer — a fourth, well-reasoned target is a legitimate outcome. **Rule 58(3):** every disposition carries the same evidence burden — agreeing with the scout is not the low-effort path.

| # | entry (expected id) | substance | scouted `target_artifact` |
|---|---|---|---|
| 1 | 185 | a fold lands where the defect was NOTICED — sweep every site | `PLANNER_TEMPLATE.md` (Family names **Checklist #26**) |
| 2 | 186 | review attention follows CHURN, not RISK — rotate the TARGET | `DRAFTING_CYCLE.md` (§2.6) |
| 3 | 187 | verification granularity must match change granularity | `DRAFTING_CYCLE.md` (§2.4 / §2.7) |
| 4 | 188 | READ the cited rule, do not recall it | `PLANNER_TEMPLATE.md` — ⚠️ **surface to Gate 1:** the entry's Family line cites *"Rule 27 ('reference, don't re-derive')"*, but Rule 27 (`:824`) actually reads *"Diagnostic-derived plans cite findings, never supplement with source reads."* **The entry's gloss does not match the rule it names — an instance of the very failure the entry describes, inside the entry.** Gate 2 must place it against the rule's real text |
| 5 | 189 | §3's "compact" is load-bearing | `DRAFTING_CYCLE.md` §3 (*"first entry against §3 itself"*) |
| 6 | 190 | `plan_lint` §4 has four defects | `DRAFTING_CYCLE.md` §4 — **HARD `plan_lint.py` coupling** |
| 7 | 191 | an honest QA failure passes the Rule 20 self-check | `RULE_20_SELF_CHECK_BLOCK.md` — **`gates.py` dependency** |
| 8 | 192 | I recorded four lens passes as DRY without running them | `DRAFTING_CYCLE.md` (§2 / §4 residual-trust) |

**⚠️ Entries 190 and 191 DO propose gate/code changes — name the coupling.** 281 instructed the classifier *"do NOT name a `plan_lint.py` coupling"*; **that instruction is INVERTED here.** Entry 190 prescribes concrete code edits and entry 191 names a `gates.py:657` dependency. Per `DRAFTING_CYCLE.md` §6 ("Coordinate doctrine and gate", `:147`), a Gate-2 codification touching §1 or §4 **must pair with the `plan_lint` edit and its tests, or explicitly defer and say so** — flag that in the cluster synthesis.

**Cluster synthesis for Gate 1:** **"8 entries from plan 282's drafting cycle — 5 planner-discipline authoring refinements + 3 bellows-integration gate defects; MIXED targets across ≥3 artifacts, 2 with hard code couplings."** Do NOT skip or downgrade any. ⚠️ **One disposition note Gate 1 will reach for on this shape: Rule 46 (`PLANNER_TEMPLATE.md:1012-1014`) directs Gate 1 to REJECT a proposal that is a "Planner-side workaround for a daemon bug" and route the bug to `FORWARD.md` instead of codifying it as governance. It does NOT fire here** — entries 190 and 191 propose **fixes to the gate itself** (concrete `plan_lint.py` edits; a documented `gates.py:657` dependency), not workarounds around it. Named so a Gate-1 reader who reaches for Rule 46 on seeing "hard code couplings" finds the question already answered; §6's doc+gate pairing is the applicable rule.

**⚠️ ONE MORE THING FOR GATE 1 — the Drafting Cycle record at the end of this plan carries a `Codification-effect prediction`.** It names, per entry, what the NEXT plan drafted after Gate 2 should show if codification actually changed authoring behaviour. **Carry it forward into the Gate-2 plan's record so the following cycle can be measured against it rather than assessed from memory.** The reason it exists: entries 185, 187, 188, 190 and 192 each recurred *inside the drafting of this very plan*, several times each — so this plan is evidence that the lessons are real, and is structurally incapable of being evidence that they were absorbed.

**Do NOT dedup against `DRAFTING_CYCLE.md`, `PLANNER_TEMPLATE.md`, or `RULE_20_SELF_CHECK_BLOCK.md`.** Gate 1 dedups against live doctrine.

**Scope discipline:** cycle run only. Routes stay `NULL` at insert. **Do NOT edit `DRAFTING_CYCLE.md`, `PLANNER_TEMPLATE.md`, `RULE_20_SELF_CHECK_BLOCK.md`, or `bellows/scripts/plan_lint.py`.** **Do NOT touch proposals 191 or 192.** **⚠️ Do NOT append to `LESSONS.md` while this plan is deposited-but-un-run** — the cycle parser-pins its batch at 8 (memory `lessons-forge-cycle-pins-batch-by-parser-count`).

**⚠️ Concurrency — dispatch with NO other lessons-forge cycle in flight.** This cycle's 8 proposals insert `status='proposed'`, and 191/192 are already non-terminal, so a concurrent whole-corpus ingest could stale **ten** proposals. DETECTION is QA row 2 + row 4 + row 4b.

**No diagnostic precedes this plan, deliberately** (247/257/274/281 practice): every unknown was measured inline against live data — batch delta (8), the G1 baseline, E0/P0, `sqlite_sequence` no-gap, the hash-trap target, root HEAD (`881ec60`), `LESSONS.md` porcelain clean. **✅ `LESSONS.md` is ALREADY committed** (entries landed in root `5a7a1db`; HEAD now `881ec60`, porcelain clean).

**⚠️ Checklist #3 vs Checklist #4 — a rule-vs-rule conflict, resolved here so no reader "fixes" it in either direction.** Checklist #3 (`:1133`) says that for `dispatch_mode: bellows` plans one must grep for `**STOP.**`, `do not proceed`, `halt and report`, `wait for CEO confirmation`, `wait for my confirmation` and *"strip all matches."* **Applied literally, that deletes the sentence "If the block prints FAILED, do not proceed with closure — halt and report to CEO" — the canonical Rule 20 template text Checklist #4 (`:1137`) mandates VERBATIM**, and which was restored to Step 3 precisely because it had been dropped. Resolution, on Checklist #3's own rationale: it exists because *"these patterns are ignored by Bellows — the daemon dispatches steps end-to-end unless `pause_for_verdict` is set… the only mechanism for inter-step pauses."* Its target is prose that pretends to pause BETWEEN steps. **The three step-terminal STOP blocks have been stripped.** What remains is intra-step failure handling and one Checklist-#4-mandated closure instruction — neither claims an inter-step pause, and the more specific mandate governs. The bootstrap prompt's *"wait for my confirmation"* is generated by the daemon itself (`bellows.py:638`). Precedent for resolving a rule contradiction explicitly: `PLANNER_TEMPLATE.md:1944`.

**Residual risk register — what is verified, what is not, and how a remaining unknown fails.** The closing test applied to this plan is **fail-closed, not dry** (see Closing). Recorded so the verdict-gate reader adjudicates against the real confidence distribution rather than a uniform one.
- **Best verified — the corpus-protection path.** G1's by-id probe of 191/192 including the `stale` check, Step 1a-bis's pre-ingest parent-hash guard, G4's direct `stale` query, and QA row 4b were each independently re-verified against live code and the read-only DB by five separate cold readers. Every number this plan asserts (E0/P0, the 8/0/127 dry run, the entry-183/184 hashes, the tag split and its id mapping, `stale`=3, 55 tests) was reproduced from source more than once.
- **Least verified — the failure-path scaffolding.** The resume ladder, the `NT`/`NT-original`/`NT-now`/`NT-post` anchoring, the pristine-backup reconstruction, and the dispatch-state probe carry the majority of this cycle's late folds and have never executed. **A defect remaining there costs a dispatch, not the corpus** — every branch halts rather than mutates, and DO-NOT-REPAIR plus the verified restore point bound the blast radius.
- **Known-unclosed by choice.** The cold walk on v16 folded on all five lenses, so this draft has not had a walk return only-minor findings. The trim the cold-integration reader identified (collapsing the four anchor labels and the dual before-anchor) is deliberately NOT taken before deposit — restructuring is what produced the two worst defects of this cycle.
- **Declared exception to C5** — QA row 7 fails closed on an in-window CEO doctrine edit; the `❌` is an escalation channel, not a verdict.

**Codification-effect prediction — written now so the next cycle MEASURES whether Gate 2 landed, rather than recalling it.** ⚠️ **Surface this to Gate 1 with the cluster synthesis.** If these entries are codified, the NEXT plan drafted after Gate 2 should show:
- **185 (sweep every site)** — zero folds that are repairs of an earlier fold's unswept sibling. This cycle had at least eight, two of them repairs-of-repairs.
- **188 (read the cited rule)** — zero citation corrections from its cold panel. This cycle had six, plus one mislabel inherited from both parents.
- **190 (`plan_lint` §4)** — the closing WARN tracks the actual disposition. On this plan it flipped in both directions on prose wording alone, disposition unchanged.
- **192 (unrun attestation)** — every lens line written after its pass ran, with no retraction needed.
- **187 (verification granularity)** — presence audits assert what was KEPT, not only that what was removed is gone. This cycle's 51-artifact audit passed while five real seams existed.

**Deposit-once discipline:** deposited exactly once. **Authoring self-check:** `plan_lint.py` run at authoring — exit 0. ⚠️ **Per entry 190, a clean `plan_lint` exit is NOT evidence the §4 block ran** (three of its sub-checks cannot fail; the block is warn-only). Expect the known-benign WARN *"step 3 mentions tests but declares no test scope"* — the QA-runs-pytest-without-modifying-tests class (memory `benign-gate-failure-classes`); do NOT add test files to Step 3's scope to silence it.

⚠️⚠️ **A SECOND WARN — "closing indicates fold as last event" — appears while the cycle is open, and ITS PRESENCE OR ABSENCE IS NOT A VERDICT. This plan demonstrated the defect on itself, twice, in both directions: record it for Gate 1.** The §4 closing check tests `has_fold and not has_dry` by SUBSTRING, so a last lens line containing the string "NOT dry" satisfies `has_dry` and passes, and a line containing "DRY" does the same. The WARN therefore vanished once while the cycle was still open, and returned later purely because incidental prose wording changed — with the actual disposition unchanged throughout. **The plan ingesting entry 190 was falsely passed, then falsely warned, by the exact bug entry 190 documents.** ⚠️ **Read the Cycle Log's closing line, which states the disposition directly; never infer readiness from this WARN.**

## How to Run This Plan

Bellows dispatches this plan automatically when deposited; no manual bootstrap required.

⚠️ **The inherited manual bootstrap block has been REMOVED, and the removal is declared because both parents carry it.** `PLANNER_TEMPLATE.md:892` (Rule 35) states that for `dispatch_mode: bellows` the *"'How to Run This Plan' section either is omitted or replaced with a short note"* — the sentence above is Rule 35's own wording. The block 274/281/282 all carry — a bootstrap prompt instructing the agent to run only the first step and then pause for confirmation — contains **three of Checklist #3's five strip-patterns** (paraphrased rather than reproduced, so this rationale does not reintroduce the very strings it explains removing) and would hand a `pause_for_verdict: always` plan one more inter-step-pause signal that Bellows ignores — the exact class the Checklist #3 resolution above removes from the step bodies. 281's conformance record reads `## How to Run (Rule 35) ✓`, i.e. the parent read Rule 35 as *requiring* the section; it says the inverse for bellows mode.

---
---

## STEP 1 — Lessons Agent (ingest whole corpus + classify all 8)

---

> **FIRST — post a short visible chat message (1-2 sentences) confirming you are starting this plan and your immediate next action.** Do NOT rename the plan file.
>
> You are the Forge Lessons Agent. Read `agents/FORGE_LESSONS_AGENT.md` first (ADR-002 six-value taxonomy). **Its DB paths are relative to the GitHub root and you may run in a worktree** — so **every canonical-DB access uses the ABSOLUTE path** `/Users/marklehn/Developer/GitHub/lessons-forge/lessons-forge.db`. **`forge/forge.db` is a REAL but DIFFERENT database — never open it.**
>
> **Working location — the plan-225 trap.** Run commands from **your own working tree** and write every file there. **The ONLY exception is canonical-DB access, by the ABSOLUTE path above.** Do NOT `cd` to the main tree: report/deposit writes resolve RELATIVE paths, so a main-tree cwd puts output in main while you commit in the worktree.
>
> **⚠️ EXECUTION ORDER — run these in exactly this sequence. The gate definitions are documented AFTER Step 1b for readability, but two of them run BEFORE it; do not execute this step in reading order.**
> 0. **DETERMINE DISPATCH STATE — do this FIRST; four checks in Step 1a branch on it and none of them can be evaluated until it is settled.** Run `git -C <your worktree> show HEAD:knowledge/development/dev-log-cycle-step-1-2026-07-29.md` and also check the working tree for that path. ⚠️ **Probe THREE places, not one — a hard death routes the record somewhere the obvious probe cannot see.** If a prior dispatch died without teardown (a session-cap exit, a daemon kill), `_create_worktree` moves its unmerged commits to a **`bellows-preserved/<slug>-<ts>`** branch and rebuilds the worktree from `HEAD` (`bellows.py:~1064-1096`, verified) — so the stub is committed, but neither `HEAD:` nor the working tree shows it. Check: (i) `git -C <worktree> show HEAD:knowledge/development/dev-log-cycle-step-1-2026-07-29.md`; (ii) the working tree; **(iii) `git -C <worktree> log --all --oneline -- knowledge/development/dev-log-cycle-step-1-2026-07-29.md` and `git -C /Users/marklehn/Developer/GitHub/lessons-forge branch --list 'bellows-preserved/*'`**. **A hit on ANY of the three is a RESUME** — recover the stub with `git show <ref>:<path>`. **Absent from all three → FRESH DISPATCH**, and its opening `Status:` line selects the branch. On a RESUME its recorded values — `E0`, `P0`, `NT` (as `NT-original`), the pristine backup path, both parent-entry hashes, the `stale` baseline, and any recorded ingest dict — are **authoritative for the whole step** and override anything you re-measure now. State the determination and the evidence for it as the first line of your dev log.
> 1. **Step 1a** — restore point, verify it, capture the baseline + `E0`/`P0` + the `NT` set.
> 2. **Step 1a-ter** — write + `git commit` the pre-ingest anchor stub. ⚠️ **The durability fix; NOT optional and NOT skippable** — every later resume path depends on it existing. ⚠️⚠️ **BUT IT IS CONDITIONAL — READ THE FILE BEFORE YOU WRITE IT.** If `dev-log-cycle-step-1-2026-07-29.md` already exists and its opening line is either PROCEED-value (`Status: Complete` / `Status: Complete (idempotent re-dispatch …)`), **do NOT write a stub at all** — this is a re-dispatch onto completed work; leave the file untouched and proceed to G5(a), which owns that case. ⚠️ **But CONFIRM THE WORK IS ACTUALLY THERE FROM THE DB FIRST, not from the file's status line** — `SELECT COUNT(*) FROM lesson_proposals WHERE entry_id > E0` must be **8** and `get_unclassified_entries()` must be `[]`. **If either differs, do NOT take the G5(a) shortcut** — fall through to the normal path (append-only to the existing file). A `Status: Complete` receipt describes a corpus that may since have been **restored from the pristine backup** (this plan names that recovery three times), in which case the work is gone while the record still claims it landed; taking the shortcut would then release the write handle without re-ingesting and let Step 2 report over an un-ingested corpus. If it exists with a stop-value, **preserve every recorded value verbatim and only append.** An unconditional whole-file write here would destroy the completed Receipt — item 1's ingest dict, item 6's proposal list, item 7's backup paths, item 9's divergences — **before G5(a)'s do-not-clobber rule is ever evaluated at position 7**, defeating the durability fix in exactly the state it exists for.
> 3. **Step 1a-bis** — pre-ingest parent-hash guard (read-only), and the `detect_duplicates` pre-check.
> 4. **G2** then **G1** — both pre-ingest. **G1 is the last thing before the mutation; if it HALTs, the ingest never runs.**
> 5. **Step 1b** — the ingest (the only mutation), then `conn.commit()`, then append the returned dict to the stub and commit it again.
> 6. **G3, G4, G5, G6** — these read the Step-1b dict and are post-mutation detectors.
> 7. **Classification** → per-proposal commit → deposits → commit.
>
> **Single-writer assumption.** Before Step 1a, confirm no concurrent cycle: (a) `get_unclassified_entries` stable across two reads a moment apart, **and** (b) no other in-progress plan. ⚠️ **Glob `in-progress-*.md` in the MAIN TREE by absolute path — `ls /Users/marklehn/Developer/GitHub/lessons-forge/knowledge/decisions/in-progress-*.md` — NOT the inherited `in-progress-*lessons*`/`in-progress-*cycle*`, and NOT your worktree's copy.** Your worktree's `knowledge/decisions/` is a frozen HEAD snapshot (the daemon renames the plan with an uncommitted `shutil.move` in the main tree at `bellows.py:585`, then creates the worktree from HEAD at `:642`), so a cycle claimed after your worktree existed could never appear there and the check would be vacuous. ⚠️ **ZERO matches is a NORMAL, NON-HALTING result**; one match — this plan's own file — is equally fine. **Only this plan's own file may appear.** For any OTHER match, read its title line and HALT if it is a lessons/cycle plan.
>
> **⚠️ HALT DURABILITY — applies to every HALT in this step.** This step holds the only write handle to the canonical corpus. **Before you stop, commit whatever deposit files exist by EXPLICIT PATHSPEC** (`git add <exact paths>` then `git commit -m "…" -- <exact paths>`; never `-a`, never unscoped `add -A`). Record which gate halted, its measured value, and whether the ingest had already committed.
>
> **⚠️ DO NOT REPAIR. You hold the write handle.** If a gate fails, report it — do not "fix" the corpus, do not re-run the ingest to get a different number, do not adjust a proposal to make a count match. The only authorized writes are: the `.backup`, `run_full_lessons_cycle`, `insert_proposal` for entries `get_unclassified_entries` returns, and this step's two deposit files.
>
> **Scope:**
> - `knowledge/development/classifications-cycle-2026-07-29.md`
> - `knowledge/development/dev-log-cycle-step-1-2026-07-29.md`
>
> **Step 1a — restore point, then baseline.** Back up canonical with `.backup` (NOT `cp` — a live WAL exists), to the MAIN tree by ABSOLUTE path: **build the path in a shell variable first** (verified-working form):
> ```
> mkdir -p /Users/marklehn/Developer/GitHub/lessons-forge/data/backups
> BK="/Users/marklehn/Developer/GitHub/lessons-forge/data/backups/lessons-forge-pre-cycle-283-$(date -u +%Y%m%dT%H%M%SZ).db"
> sqlite3 "file:/Users/marklehn/Developer/GitHub/lessons-forge/lessons-forge.db?mode=ro" ".backup '$BK'"
> ```
> ⚠️ Do NOT inline `$(date …)` between single-quoted parts of the `.backup` argument — sqlite3 MISPARSES that and errors with NO backup written. **Use the COMPACT UTC stamp.** `.gitignore` matches `*.db` — confirm absent in `git -C /Users/marklehn/Developer/GitHub/lessons-forge status --porcelain`.
>
> **Then VERIFY the restore point is real, not ceremony.** ⚠️ **In this cycle the backup carries more weight than it did in 281:** because the ingest stales live proposals inside the same call that updates their parent entry, G4 can only report damage already written — so **this file is the only route back to the pre-cycle state of proposals 191/192.** Verify: `sqlite3 '<backup-path>' 'PRAGMA integrity_check;'` must return `ok`, AND the backup's `COUNT(*)` for both tables must equal **the LIVE DB's counts at backup time**. ⚠️ Match to the live DB **NOW**, not a hardcoded number: fresh run = 184/192; **on a RESUME the live DB is already mutated (≈192/≈200) and the fresh `.backup` correctly snapshots that — do NOT assert 184/192 or you false-HALT every resume.** Any failure → HALT before the ingest.
>
> ⚠️⚠️ **THE GLOB MUST BE CYCLE-UNIQUE — the inherited pattern is NOT, and it fails UNSAFE.** `data/backups/` **already contains five files matching `lessons-forge-pre-cycle-*.db`** (verified: 2026-07-20, 20260721, 20260723, 20260724, 20260728). "Earliest matching the pattern" therefore resolves to a **nine-day-old corpus snapshot**, and the instruction "derive the date from the actual backup filename or the receipt" is circular in the one state this fallback exists for — no receipt means nothing identifies this cycle's stamp. Reconstructing `NT-original` from that file yields ids that have since gone terminal and **does not contain proposals 191/192 at all** (P0 was smaller then), so QA row 4b would query the wrong ids, see `proposed → implemented`, mark itself `✅ Gate-2 shipped in-window`, and **pass green having never looked at the pair it exists to protect.** So: **the backup filename carries this plan's id — `lessons-forge-pre-cycle-283-<UTC-stamp>.db` — and the resume glob matches on the `-283-` token.** ⚠️ **And PROVE the file is this cycle's pristine snapshot before accepting it:** `SELECT MAX(id) FROM lesson_entries` must be **184** and `SELECT MAX(id) FROM lesson_proposals` must be **192** (this plan's own `E0`/`P0`). Anything else → do NOT use it; row 4b is `❌ (unverifiable)`, which is the honest answer. ⚠️ **Take the EARLIEST `-283-` match — end the glob in `.db`**: SQLite writes `-shm`/`-wal` sidecars alongside every backup and `sqlite3` errors on a `-wal` file. NOT the directory's overall earliest (prior cycles persist there). **Derive the date from the ACTUAL backup filename or the receipt, never from the local dispatch date** (`date -u` after ~17:00 local stamps the next day). Prefer the exact path recorded in the Step-1 Receipt (**item 7**, the backup paths — NOT item 6, the created-proposal list).
>
> **Then capture the baseline** (read-only), verbatim: proposals by `status`, proposals by `category`, total `lesson_entries`, and the id + `content_hash` of **BOTH parent entries of the pre-existing proposals — entry 183 (`553e9493df9bc289af7bdca4013eee328c4673e857f19d805104eaeba97412a2`) and entry 184 (`f2cf892c30e3544e36b4c1e76766f52497ac3770918ee9fba3820fd398e103a2`, the hash-trap target) — named directly and NOT derived from `MAX(id)`.** ⚠️ **Both are required and neither may be sourced from `NT`:** QA row 4b compares against both, and `NT` is legitimately EMPTY in the `PASS (Gate 2 shipped)` branch, which would leave that comparison with no operand. ⚠️ **Confirm BOTH against your own baseline read and report the actuals; do not copy these literals forward** (Checklist #29 — a stated figure is the Planner's measurement, never a substitute for yours). A mismatch on either is a HALT, not a correction. ⚠️ On a FRESH run 184 is also the last-ingested entry; **on a RESUME it is not (the last-ingested id is 192) — do NOT halt on that difference.**
>
> **Also capture `E0 = MAX(id) FROM lesson_entries` and `P0 = MAX(id) FROM lesson_proposals`. Confirm `E0 = 184`, `P0 = 192`; if either differs on a fresh run, HALT. ⚠️ **But do NOT halt with the wrong diagnosis:** a "fresh" determination that finds `E0 = 192` almost certainly means **a prior dispatch's ingest DID land and its record is on a `bellows-preserved/*` branch** (step 0, probe iii). **Search those branches for the stub before reporting the first-dispatch ingest dict as lost** — that dict is the only unreproducible value in this plan, and reporting it lost while it sits recoverable on a branch defeats the durability fix in the exact failure mode it was built for.**
>
> **⚠️ Additionally capture THE NON-TERMINAL SET — one authoritative query, selected by STATUS PREDICATE, never by hardcoded id.** This single result is the before-anchor that Step 1a-bis, G1, Step 2 and QA rows 4b/5 all read; none re-derives it:
> ```sql
> SELECT p.id, p.entry_id, p.status, p.route, p.target_artifact,
>        e.source_heading, e.content_hash
> FROM lesson_proposals p JOIN lesson_entries e ON p.entry_id = e.id
> WHERE p.status IN ('proposed','accepted','ambiguous')
> ORDER BY p.id;
> ```
> Label it **`NT`** and deposit it as RAW output. Planner's measured expectation on a FRESH run: **2 rows** — proposals 191/192 on entries 183/184, both `proposed`, both `route='codify'`. ⚠️ **A hardcoded `WHERE id IN (191,192)` would miss precisely the unexpected third non-terminal proposal G1 exists to catch**, and omits the `source_heading` + `content_hash` columns Step 1a-bis needs.
>
> **⚠️⚠️ ON A RESUME, `NT` FROM A RE-RUN IS NOT A VALID BEFORE-ANCHOR — read the ORIGINAL.** (i) Benign: this cycle's own committed proposals are `proposed`, so a re-run returns ~10 rows rather than 2 — expected growth. (ii) **Dangerous: if the first attempt already staled a pre-existing proposal, a resume captures it as `stale`, and QA row 4b then compares `stale` against `stale` and PASSES — the corruption goes invisible exactly when a resume is happening.** Therefore: **on a RESUME the authoritative before-anchor is the ORIGINAL committed capture, read from the 1a-ter stub or the prior Receipt — never a fresh re-run.** Re-run the query too, label the results `NT-original` and `NT-now`, deposit both; Step 1a-bis and QA row 4b compare against `NT-original`.
>
> **⚠️ AND WHEN NO PRIOR RECORD EXISTS — RECONSTRUCT `NT-original` FROM THE PRISTINE BACKUP.** Run the `NT` query verbatim against the pristine backup:
> ```
> sqlite3 "file:<pristine-backup-path>?mode=ro" "<the NT query above>"
> ```
> That reconstruction **is** `NT-original` — a byte-faithful snapshot taken before any mutation, whose integrity this step already proved. Label it `NT-original (reconstructed from <path>)`. Only if the pristine backup is ALSO absent or fails integrity is row 4b genuinely unverifiable. ⚠️ **This is a fallback derivation of the SAME value, not a second capture** — it does not license re-deriving `NT` when the recorded one exists.
>
> **⚠️ Why this step carries resume machinery at all — Rule 56's required ask, answered.** `PLANNER_TEMPLATE.md:1081` (Rule 56, *"Resume machinery is justified only when the interrupted work is not reproducible"*) requires that **before** building resume machinery the Planner ask whether the work could be regenerated from a recipe the plan already carries, and prefer **restore-and-redo** if so. Asked and answered: **restore-and-redo was considered and rejected.** A verified pristine restore point exists, so a blind re-run is technically available — but Rule 56's own caveat is that *"restore is only safe on your own dirt,"* and this step **cannot prove sole ownership** (its single-writer check is explicitly non-halting on zero matches), so a restore could silently discard a concurrent writer's work. Rule 56 also names committed classifications that cannot be blindly re-run without manufacturing duplicates as its own carve-out — exactly what `get_unclassified_entries` guards. **The machinery stays, and this paragraph is why.** ⚠️ This also settles the §2.8 oscillation signal: the before-anchor was re-folded four times, and this is the step-back §2.8 prescribes rather than a fifth patch.
>
> **⚠️ Step 1a-ter — COMMIT THE BEFORE-ANCHOR BEFORE THE INGEST. This is the durability fix; not bookkeeping.** Every anchor this plan depends on otherwise lives only in the Output Receipt, written at the END of the step — so in exactly the resume states that matter (ingest committed, step died before deposit) **none of it exists**, and the plan is reduced to filesystem archaeology over backup filenames, which fails on a resume crossing a UTC-day boundary.
>
> Immediately after the restore point is verified and **before** `run_full_lessons_cycle` runs, write and **`git commit`** a stub `knowledge/development/dev-log-cycle-step-1-2026-07-29.md` containing:
> - `Status: Partial — in flight (pre-ingest stub; superseded by the final Receipt)` ⚠️ **This value must NEVER satisfy a downstream precondition** — Steps 2 and 3 proceed ONLY on an enumerated Complete value.
> - the **absolute pristine backup path**
> - `E0`, `P0`
> - the raw `NT` capture (or `NT-original` if this is itself a resume — **never overwrite an existing stub's `NT`**)
> - the `stale` baseline count
> - both parent-entry hashes (183, 184)
>
> A later resume reads this file instead of guessing from filenames, and the final Receipt overwrites it in place. ⚠️⚠️ **THE OVERWRITE MUST PRESERVE THE FIRST-DISPATCH INGEST DICT — otherwise it destroys the durability fix it supersedes.** The final Receipt rewrites this whole file, so a resume that re-runs the ingest (and correctly gets `updated_count == 0`, because the entries are already ingested) would replace a FIRST-dispatch dict recording `updated_count == 1` with its own all-zero one, and QA row 4 would positively report a clean ingest that never happened. **If the stub carries a recorded ingest dict, the final Receipt MUST carry it forward verbatim under `#### First-dispatch ingest dict`.** **If the stub already exists on a resume, its recorded values are authoritative over anything you re-measure now.**
>
> **Step 1a-bis — PRE-INGEST parent-hash guard (preventive; no analogue in 281; do not skip as boilerplate).** The ingest stales a non-terminal proposal *inside* the same call that updates its parent entry, so every post-ingest gate is a detector, not a guard. Before calling `run_full_lessons_cycle`, do this **read-only**:
> 1. From your working tree's repo root: `import sys; sys.path.insert(0, "src")` then `from lessons_forge import parse_lessons_md`; `entries = parse_lessons_md("/Users/marklehn/Developer/GitHub/LESSONS.md")` — the same parser `run_full_lessons_cycle` calls, so hashes are computed identically. ⚠️ **Compare against the `content_hash` column of `NT-original`**, never a hash re-read from the DB now — on a resume the live row may already carry the damage you are checking for. **Verified by execution at authoring:** the parser returns dicts keyed `content_hash`, `entry_date`, `raw_content`, `source_heading`, `tags` over 135 file entries, and the computed hashes for entries 183/184 **equal** their stored values.
>
>    ⚠️⚠️ **WHILE YOU HOLD ALL 135 PARSED ENTRIES, TALLY THE WHOLE-CORPUS DRY RUN — three more lines, and it converts G3/G4/G5 from post-mutation DETECTORS into pre-mutation GUARDS for the entire corpus rather than for two rows** — ⚠️ **for the interval between this check and `run_full_lessons_cycle`, which re-reads BOTH `LESSONS.md` and (via `cat`) `PLANNER_TEMPLATE.md` in a fresh read. G3/G4/G5 remain the backstop for anything that changes inside that window**, and this plan cannot prove sole ownership (its single-writer check is non-halting on zero matches). Stated because an earlier draft over-claimed the same conversion for the batch and had to be corrected. For every parsed entry, look up `(source_file='LESSONS.md', source_heading)` in `lesson_entries` and count: **`would_insert`** (no row), **`would_update`** (row exists, `content_hash` differs), **`unchanged`**. ⚠️⚠️ **BRANCH THIS ASSERT ON THE STEP-0 DISPATCH DETERMINATION — an unconditional `would_insert == 8` makes EVERY RESUME PATH IN THIS PLAN UNREACHABLE.** A landed ingest puts all 135 parsed headings into `lesson_entries`, and the ingest's lookup key is `(source_file, source_heading)` — so on any resume `would_insert` is **arithmetically certain to be 0**, not merely likely. An unconditional assert therefore HALTs pre-mutation on every resume, which silently deletes G1's `PASS (resume)`, G5(a)'s idempotent re-dispatch (which this plan states in bold **must NOT be turned into a halt**), G5(b)'s deposit-completion path with its first-dispatch-dict carry-forward, G6's RESUME subset branch, and the entire Step-1a-ter durability architecture — leaving a half-classified corpus with no legal recovery, since Step 2 rejects `Partial — HALTED` and DO-NOT-REPAIR forbids restoring the backup. So:
>    - **FRESH dispatch → assert `would_insert == 8` AND `would_update == 0`.** Any deviation → HALT pre-mutation.
>    - **RESUME → assert `would_update == 0`** (still the staling guard, valid on every dispatch) **and `would_insert ∈ {0, 8}`.** ⚠️ **Anything in `1..7` is a HALT** — the ingest is one transaction, so a partially-landed insert set is impossible from this plan and signals a foreign writer.
>
>    Report the actuals either way, naming any entries responsible. This is the same measurement the Planner made at authoring (8 / 0 / 127) — the recipe is handed to you rather than only its result. ⚠️ **It matters because a commit to root `LESSONS.md` between authoring and dispatch is explicitly PERMITTED** (G2 treats a root-HEAD delta as a reconcile-note): a 9th appended lesson would otherwise be discovered only by G5, *after* `run_full_lessons_cycle` had already written it.
> 2. For **every row in `NT-original`** (on a first dispatch that is simply the Step-1a `NT` capture; on a RESUME it is the stub's capture or the pristine-backup reconstruction — ⚠️ **never `NT-now`**, because a row this cycle already staled is ABSENT from `NT-now` and iterating it would make this guard vacuous in exactly the state it exists for), find the parsed entries whose `source_heading` equals that row's, and compare hashes. Adjudicate on the number of file matches:
>    - **exactly 1 match, hash EQUAL** → PASS. *(Measured at authoring for both 183 and 184.)*
>    - **exactly 1 match, hash DIFFERENT** → **HALT BEFORE THE INGEST.** Running the ingest would stale that live proposal. Report the entry id, both hashes, and whether the difference is whitespace-only. Do not "fix" `LESSONS.md`, and do not proceed to see what G4 says — G4 would be reporting damage already written.
>    - **ZERO matches** → **PASS, and this is SAFE — record it, do not halt.** The DB holds 184 entry rows against 135 file entries (orphans from reworded headings), so a parent may legitimately be absent. `ingest_lesson_entries` looks up by `(source_file, source_heading)` and only updates entries it MATCHES, so an unmatched entry cannot be updated and its proposals cannot be staled. Note it as `orphan — not reachable by the ingest`.
>    - **MORE than 1 match** → **HALT.** Two file entries sharing one heading is a corruption signal (the lookup would be ambiguous).
> 2b. ⚠️ **ALSO pre-check the duplicate detector — it reads a file the CEO is expected to edit, and that read produces WRITES.** `run_full_lessons_cycle` calls `detect_duplicates`, which defaults its reference file to `/Users/marklehn/Developer/GitHub/PLANNER_TEMPLATE.md` (read via `subprocess.run(["cat", …])` — `src/lessons_forge.py:297`+, verified) and, on a heading-substring hit, **INSERTS a `duplicate` proposal with `status='proposed'`**. Its idempotency guard is only `WHERE entry_id = ? AND category = 'duplicate'`, so an entry already carrying a real proposal is **not** skipped. ⚠️ **So a Gate-2 edit to `PLANNER_TEMPLATE.md` is NOT neutral for this cycle.** ⚠️⚠️ **SCOPE THIS HONESTLY — the pre-check CANNOT cover this cycle's own 8 entries, and an earlier draft of this plan wrongly claimed it "converts G3 into a guard."** `detect_duplicates(conn, entry_ids, reference_files=None)` takes **ids, not headings**, and resolves each via `SELECT source_heading, tags FROM lesson_entries WHERE id = ?`, skipping ids that return no row (`src/lessons_forge.py:362-369`, verified). **Pre-ingest, entries 185–192 do not exist and have no ids** — so calling the detector now can only ever cover ids ≤ 184. For the batch, G3 remains a **post-mutation detector**; nothing converts it.
>
> So do BOTH, and report them separately:
> - **(a) Pre-existing ids — run the detector directly, scoped to the ids the ingest ACTUALLY passes it:** `run_full_lessons_cycle` builds `candidate_ids` only from entries whose `source_heading` matches a **parsed file** heading (127 today, not all 184), so the ~57 orphan DB rows are never handed to the detector. **Mirror that construction** rather than sweeping `1..E0`: a heading hit on an orphan would HALT a cycle holding the canonical write handle for something the real path cannot insert. Run `detect_duplicates(conn, <the parsed-and-matched ids>)` read-only. **Non-empty → HALT.** If you also sweep the wider set for information, record an orphan-only hit as a note, not a halt. This is the case that is FORESEEABLE if Gate 2 has already codified an older lesson into `PLANNER_TEMPLATE.md`. ⚠️ **That is a direct consequence of the Gate-2-shipped state this plan elsewhere calls "strictly safer" — safer with respect to STALING, and not with respect to this detector.**
> - **(b) This cycle's 8 entries — replicate the detector's logic read-only against the PARSED entries**, since they have no ids yet. Apply **both** of its criteria, not just one: (1) the post-em-dash heading title as a case-insensitive substring of `PLANNER_TEMPLATE.md`; and (2) **tag overlap** — each comma-split tag against the `**Tag:**`/`**Tags:**` lines in that file (`:390-398`). ⚠️ **Criterion (2) is easy to miss and the parsed tags carry literal backticks**, the same trap that voids an equality check elsewhere in this plan. **Any hit → HALT** — a lesson you are about to ingest already exists in doctrine, which is a genuine classification finding.
>
> Planner's measured expectation at authoring: **zero hits on both paths** — none of the 135 heading titles substring-matches the current `PLANNER_TEMPLATE.md`, and that file contains **0** `**Tag:**`/`**Tags:**` lines, so criterion (2) is inert today. Report the actuals.
> 3. **If `NT-original` is EMPTY** — and ONLY `NT-original`, never a merely-empty `NT-now` — record `Step 1a-bis: N/A — no non-terminal proposals to protect` and continue. That is a legitimate, safer state, not a defect.
>
> Record the comparison as raw output, one line per row.
>
> **Step 1b — run the ingest (ONCE, this step only).** Open canonical **read-WRITE** (plain `sqlite3.connect("/Users/marklehn/Developer/GitHub/lessons-forge/lessons-forge.db")` — NOT `?mode=ro`). **Call `run_full_lessons_cycle(conn, lessons_md_path="/Users/marklehn/Developer/GitHub/LESSONS.md")` — pass the path EXPLICITLY and print the argument.** ⚠️ The inherited instruction was "it defaults to that path — confirm that is what it read", which **cannot be discharged**: the returned dict carries no path key (verified `:503-511`), so the only way to "confirm" is to re-read the default, which proves nothing. Passing it explicitly makes the claim checkable from your own transcript. **`conn.commit()` after it returns** (the DB is gitignored; skip it and a step death LOSES the ingest). **This is a WHOLE-CORPUS re-hash.**
>
> ⚠️ **Then IMMEDIATELY append the verbatim returned dict to the Step-1a-ter stub and `git commit` it (a second commit, same pathspec), BEFORE anything else.** Ledger C4 requires a resume to anchor on the original committed capture, and **the ingest dict is the ONLY value in this plan that is genuinely unreproducible** — every other anchor can be rebuilt from the pristine backup, but a post-ingest resume re-runs the cycle over already-ingested entries and gets `updated_count == 0` / `terminal_proposals_flagged == []` no matter what the FIRST run returned. **Never overwrite an already-recorded dict.** **Record and PRINT the actual returned dict:** `ingested_count`, `updated_count`, `unchanged_count`, `duplicates_marked_count`, `terminal_proposals_flagged`, `needs_classification`.
>
> ## Step 1 gates — G1 through G6 (report EVERY one as a table row with its measured value + PASS/HALT)
>
> **Timing:** G2 pre-ingest; **G1 pre-ingest ALWAYS**; G3–G6 read the Step-1b dict. **Run every gate before halting** — a one-row gate table forces the CEO to adjudicate blind.
>
> - **G1 — non-terminal precondition (REWRITTEN; do NOT clone 281's zero-tolerance form).**
>   - ⚠️⚠️ **FIRST, CHECK `stale` EXPLICITLY — it belongs to NEITHER partition.** `_TERMINAL_STATUSES` is `{implemented, rejected, superseded, reference}` (`src/lessons_forge.py:31`, verified) and the non-terminal set is `{proposed, accepted, ambiguous}`. **`stale` is in neither**, so a proposal a PRIOR FAILED ATTEMPT already staled vanishes from the non-terminal query — and the EMPTY branch below would read that corruption as "the strictly safer baseline". **Absence from the non-terminal set is NOT evidence of terminality.**
>   - ⚠️⚠️ **ON A FIRST DISPATCH THIS IS NOT TRIVIAL — query the plan's stated baseline ids, NOT the current capture.** There is no `NT-original` yet, so deriving the id list from `NT` makes the check circular: **if something staled 191/192 between authoring and dispatch, those ids are already ABSENT from `NT`, the check has nothing to look at, and the EMPTY branch is VACUOUSLY TRUE over an empty set** — the plan would positively assert "Gate 2 shipped" about a damaged corpus, and a half-staled state would match no branch at all. So on **every** run: `SELECT id, status FROM lesson_proposals WHERE id IN (191, 192)`. **Fewer than 2 rows, or any status `stale`, or any status outside `{proposed}` ∪ terminal → HALT before the ingest**, report suspected pre-dispatch staling, and name the pristine backup as the recovery point. (C3-compatible: C3 governs *capturing* an unknown set; re-verifying a *known* id set by id is the correct instrument.)
>   - Non-terminal set == exactly proposals 191/192 → **fresh run → PASS.** The ingest is non-destructive *conditional on* `updated_count == 0` (G4), because those two are the only staleable rows.
>   - Those two **plus** proposals with `entry_id > E0` → **RESUME** → `PASS (resume)`.
>   - **ANY other non-terminal proposal with `entry_id ≤ 184`** → the guarantee is VOID → **HALT before ingest.**
>   - **Every id in the before-capture — `NT` on a FRESH dispatch, `NT-original` on a RESUME; equivalently the two baseline ids 191/192 — positively confirmed at a TERMINAL status** (e.g. Gate 2 shipped) → **`PASS (Gate 2 shipped)`.** ⚠️ **Both labels are named because the Gate-2-shipped-before-dispatch case is a FRESH-run scenario** (see the CEO Context premise note), where `NT` is empty and no `NT-original` exists — keyed on `NT-original` alone, no branch of this ladder would fire. Rows 4, 4b and 5 use the same both-labels wording. The genuinely safer baseline; 281's original clean case. **Do NOT halt.** Record it prominently — Step 2 and QA rows 4b/5 read it and adjust (10→8 surfaced, 2→0 route lines).
>   - **A MIXED or partial state** — the `NT-original` ids do not all share one disposition, or any carries a route other than `codify`, or any carries a non-terminal status other than `proposed` → **HALT.** ⚠️ **A half-staled state is caught by the explicit `stale` check above, not by this branch.**
>
>   ⚠️ **Report the ACTUAL non-terminal set as raw output regardless of which branch fires** — it is the anchor Step 2 and QA rows 4b/5 compare against.
> - **G2 — LESSONS.md provenance. THE GATE is: `git -C /Users/marklehn/Developer/GitHub status --porcelain -- LESSONS.md` is EMPTY.** Use `git -C <absolute root>` — a bare `git status` from the submodule worktree passes VACUOUSLY. ⚠️ **Empty stdout alone does NOT prove the check ran** — a mistyped `-C` exits 128 with empty stdout. Capture the exit code: `git -C … status --porcelain -- LESSONS.md; echo "PORCELAIN-EXIT=$?"`. **A non-zero exit is a HALT.** ⚠️⚠️ **AND NON-EMPTY OUTPUT IS ALSO A HALT — before the ingest.** The Planner committed the `LESSONS.md` append before dispatch, so a dirty `LESSONS.md` means an unexpected edit: **do NOT ingest an uncommitted corpus.** This is stated explicitly because the two nearest adjudications in this plan both say *don't* halt on a working-tree signal — G2's own HEAD clause below, and QA row 7's reconcile-note treatment — and neither precedent applies here. Row 7 measures files this cycle cannot write, across a window the plan does not own; **G2 measures the exact input the ingest is about to read.** Separately RECORD `git -C <root> rev-parse --short HEAD` (Planner's expected: `881ec60`) — **a mismatch is a reconcile-note, NOT a halt.** **Also record `shasum -a 256 /Users/marklehn/Developer/GitHub/{DRAFTING_CYCLE.md,PLANNER_TEMPLATE.md,RULE_20_SELF_CHECK_BLOCK.md}`** — ⚠️ **working-tree content hashes, NOT `git rev-parse HEAD:<path>`**, which resolves HEAD's tree and is blind to an uncommitted edit. QA row 7 re-measures these and diffs.
> - **G3 — `duplicates_marked_count == 0`** (silent-drop guard). Non-zero → HALT and name which entry ids + why.
> - **G4 — `updated_count == 0` AND `terminal_proposals_flagged` empty** (hash-trap watch on entry 184). Non-zero either way → HALT, show the diff, classify whitespace-only (204 regression) vs substantive. ⚠️ **G4 is a DETECTOR, not a guard — Step 1a-bis is the guard.** The staling UPDATE runs inside the ingest (`:187-193`), so a non-zero `updated_count` means a live proposal may ALREADY be `stale`. ⚠️ **And you cannot measure how many:** `run_full_lessons_cycle` drops `stale_proposals_marked` from its return dict (`:503-511`, which does retain `terminal_proposals_flagged`). **On a G4 failure, query directly:** `SELECT id, entry_id, status FROM lesson_proposals WHERE status='stale'` and diff against the stub's baseline. If any `NT-original` proposal is now `stale`, say so and name the `.backup` as the recovery point.
> - **G5 — there is work to do.** `ingested_count == 0` AND `needs_classification` empty → disambiguate: **(a)** truly nothing to do → HALT. ⚠️⚠️ **EXCEPT the re-dispatch of an ALREADY-COMPLETE Step 1** (ingest done, all 8 classified, both deposits committed with `Status: Complete`) — a legitimate, uncorrupted state that must NOT be turned into a halt: **do NOT overwrite the existing `Status: Complete` receipt with a halt record.** APPEND a `### Re-dispatch note` section recording that this dispatch found the work already done, leave the status line reading **`Status: Complete (idempotent re-dispatch — no work required)`**, commit, and stop. Rationale: the HALT-DURABILITY rule mandates committing a halt record to this same file, so writing `Partial — HALTED` would destroy the only durable evidence the cycle completed and would then be rejected by Step 2's narrowed precondition — deadlocking a clean run. **(b)** proposals exist but deposits absent → deposit-completion resume, **re-generate deposits**, then END THIS STEP (deposit and commit first). ⚠️⚠️ **"Re-generate from the committed DB" is NOT sufficient — regenerate from the DB *and from the 1a-ter stub*:**
>   - **The first-dispatch ingest dict is NOT in the DB and cannot be recomputed** — re-running the ingest now returns all zeros. **Copy it verbatim from the stub as `#### First-dispatch ingest dict`.** If the stub has no dict, say so explicitly — QA row 4 then marks itself `❌ (unverifiable)`, which is correct, not something to paper over.
>   - **The `#### Scout dispositions` section IS derivable from the DB** — compare each proposal's stored `category` and `target_artifact` against the scout table and re-emit the fixed-format lines. **Do this; do not omit the section because the original reasoning is gone.** ⚠️ QA row 3 marks a divergence with no recorded line as `❌`, so dropping this section converts every licensed placement into a failure. Where the reason is unrecoverable, write `reason: not recorded (regenerated on deposit-completion resume)`.
>   - `E0`, `P0`, the pristine backup path, both parent-entry hashes and the `stale` baseline likewise come from the stub, never a live re-read (C4).
>
>   `ingested_count == 0` but `needs_classification` non-empty → `PASS (resume)`. Fresh run expects `ingested_count == 8`. **Any `ingested_count` ∉ {0, 8} → HALT.**
> - **G6 — work-list reconciliation.** The batch range is `E0+1 .. E0+8` (= 185–192), computed ARITHMETICALLY from the confirmed `E0=184` — **do NOT derive it FROM `needs_classification`** (self-referential and vacuous). **THE INVARIANT: every id in `needs_classification` is `> E0` and `≤ E0+8`.** HALT on any id OUTSIDE it → the CEO chooses (i) classify batch + extra, (ii) batch only with deferred ids under `### Deferred entries (CEO-approved)`, or (iii) investigate. **FRESH (`ingested_count==8`)** → `needs_classification` must be EXACTLY the 8; FEWER → HALT. **RESUME (`ingested_count==0`)** → a SUBSET is EXPECTED → `PASS (resume)`.
>
> **After the gate table: if all PASS, continue to classification. Any HALT — stop and report (having run the remaining gates); the ingest stays committed.**
>
> ### Classification — every entry `get_unclassified_entries` returns
>
> **`get_unclassified_entries` is AUTHORITATIVE — classify EXACTLY what it returns, in ascending `id` order; NEVER hand-derive "185–192" or anchor on a literal "8".** On a RESUME it returns only the un-classified remainder — **this exclusion is the SOLE mechanism preventing a double-insert.** For each: read `id, source_heading, raw_content, tags, entry_date`, apply ADR-002, and call `insert_proposal(conn, entry_id, category, suggested_action, reasoning, confidence, ...)` — **SIX required positional args** (`src/lessons_forge.py:202`; `entry_id` FIRST after `conn`); `status`/`target_layer`/`target_artifact` are keyword args:
> - `entry_id` — the FK, taken from `get_unclassified_entries` (Rule #47 — never hand-typed).
> - `category` ∈ `structural`/`instrumentation`/`governance_rule`/`language`/`narrative` (never hand-assign `duplicate`). ⚠️ **Do NOT assume a uniform category.** The 5 `planner-discipline` entries are expected `governance_rule`. The 3 `bellows-integration` entries (expected 189/190/191) describe **live tooling defects with code couplings** — `instrumentation` is legitimate for 190 and 191. Classify on merits; call out any genuine no-fit as `ambiguous` by id.
> - `suggested_action` — concrete recommendation. **⚠️ DO name the code coupling where one exists** (this INVERTS 281's instruction): entry 190 prescribes specific `plan_lint.py` edits, entry 191 names a `gates.py` dependency. Per §6, say whether Gate 2 must pair the gate edit or explicitly defer.
> - `reasoning` — **quoted evidence** citing specific `raw_content` from THAT entry, naming its id. Do NOT carry any claim from this plan's CEO Context into a `reasoning` field. If you cannot cite specific `raw_content`, STOP and report rather than writing generic justification.
> - `confidence` ∈ `low`/`medium`/`high`.
>
> **Target fields — SET BOTH on every non-`ambiguous` proposal; never leave either NULL.** `target_layer='governance'` expected for all 8. `target_artifact` **SPLITS across at least three artifacts** per the scout table. Only `route`, `subcategory` and `duplicate_of` stay `None`.
>
> **⚠️ The scout is guidance, NOT a mandate — you have explicit LICENCE TO DISAGREE (Rule 58).** Derive each target INDEPENDENTLY from the entry's own `raw_content` (each entry carries a `**Family:**` line naming its lineage — read it). If your reading diverges, set what the entry supports and **RECORD the divergence in the Receipt's `#### Scout dispositions` section** — do NOT silently conform, and do NOT silently override. **An 8-row scout table costs more under anchoring than 281's 2-row one:** map each entry by its content, not its position in the table.
>
> **`conn.commit()` after each `insert_proposal` (or after the 8th at latest). Then git-commit the two `.md` deposits.**
>
> **Self-report:** print `SELECT id, entry_id, status, category, target_artifact FROM lesson_proposals WHERE entry_id > 184 ORDER BY id` — expect **8 rows** (entry_ids 185–192, proposal ids 193–200). **Separately re-run the Step-1a `NT` query VERBATIM and LABEL THE RESULT `NT-post`** — ⚠️ **`NT-post` is an AFTER-value and is NEVER a valid before-anchor**; on a resume the Receipt may hold `NT-original`, `NT-now` and `NT-post`, and mistaking this one for a before-capture reintroduces the after-as-before substitution. ⚠️ **The re-run will legitimately return MORE rows than the capture** (this cycle's 8 new proposals satisfy the same predicate); that growth is expected. What must not change is the pre-existing rows — compare by `id`, and report any that vanished or changed a field. Confirm `get_unclassified_entries()` is now `[]`.
>
> **Deposit:** `knowledge/development/classifications-cycle-2026-07-29.md` (the cycle dict, the 8 classifications' per-entry reasoning, the cluster synthesis for Gate 1, the narrative reasoning for any scout divergence — ⚠️ **the AUTHORITATIVE machine-checkable record of divergences is the Receipt's `#### Scout dispositions` section (item 9), which QA row 3 reads; this deposit carries the explanation, not the anchor** — and any `ambiguous` by id) + `knowledge/development/dev-log-cycle-step-1-2026-07-29.md` with an Output Receipt.
>
> **The Receipt MUST OPEN with an explicit status line, and it must be ONE OF THIS CLOSED SET:** `Status: Complete` (every task ran) · `Status: Complete (idempotent re-dispatch — no work required)` (G5(a)'s already-complete branch) · `Status: Partial — HALTED at <gate>, <reason>` (you stopped) · `Status: Partial — in flight (pre-ingest stub; superseded by the final Receipt)` (the 1a-ter stub only). **The first two are proceed-values; the last two are stop-values.** (The `Complete`/`Partial` vocabulary is the template's, `PLANNER_TEMPLATE.md:433`.) A halted status is a legitimate value — do NOT write `Complete` to make the step look tidy. It MUST then carry each of the following on its OWN labelled line:
> 1. the cycle dict verbatim (THIS dispatch's). ⚠️ **Plus, if the 1a-ter stub recorded an ingest dict from an earlier dispatch, reproduce it verbatim as `#### First-dispatch ingest dict` — QA row 4 reads THAT one, not item 1, whenever a resume is in evidence.**
> 2. the G1–G6 gate table
> 3. the pre-cycle baseline (status + category distributions, entry count, BOTH parent-entry hashes)
> 4. `E0` and `P0`
> 5. **the `NT` capture — labelled literally `NT`** (and `NT-original` / `NT-now` on a resume), as RAW output. This is the before-anchor Step 2 and QA rows 4b and 5 all read, and each fails closed if it cannot find a line labelled `NT`. ⚠️ **An EMPTY `NT` is a legitimate value** — write the label and record it as empty; do not omit the item.
> 6. the self-reported 8-row created-proposal list, `NT-post`, and `get_unclassified_entries() == []`
> 7. **the absolute backup paths, LABELLED SEPARATELY** — `pristine (pre-cycle)` and, on a resume only, `this-dispatch (mid-cycle)`. QA row 4 and the reconstruction need the PRISTINE one specifically.
> 8. **`#### Files Created or Modified`** — every file this step created or wrote, by path. ⚠️ **QA row 0 reads this section and asserts it checked EVERY entry, so an omitted file is an unverifiable deliverable.** (Rule 17 `:555` names this list — the same string at `:515` sits inside Rule 8 — ⚠️ **its exact wording is `Files Created or Modified (Code)`; the `(Code)` suffix is deliberately dropped here** because this cycle's deliverables are markdown deposits and a database mutation, not code, and row 0 greps the un-suffixed heading. Declared so the divergence from the template's literal string is not read as an error.) Step 2's Receipt must carry the same section.
> 9. any flags, and — as its own labelled section — **`#### Scout dispositions`**, carrying **ONE LINE FOR EVERY PROPOSAL, not only the divergent ones.** ⚠️ **This is Rule 58(3) given a structural home.** Rule 58 requires *"equal evidence burden on every disposition, so the cheap/default one is not the low-effort path"* — and with a divergence-only section, **agreeing costs nothing while diverging costs a formatted line plus an existence proof**, which is precisely the bias 58(3) names. Rule 54 (`:1069`) is the reason prose cannot carry this: an instruction anchored to a numbered QA row or a mechanical gate survives execution; one living only in a prose paragraph is dropped. (Paraphrased rather than quoted — the rule's own wording contains a keyword that trips `plan_lint`'s step-scope scan, producing a spurious WARN on this step.) So both dispositions cost one line with a reason drawn from the entry's own `raw_content` / `**Family:**` line. ⚠️ **QA row 3 matches this section MECHANICALLY, so the format is fixed:** `- proposal <id> | entry <id> | agreed | reason: <text>` for a placement matching the scout, and `- proposal <id> | entry <id> | diverged | field: <category|target_artifact> | scouted: <value> | set: <value> | reason: <text>` where it departs. Write the values **bare — no backticks, no bold, no quotes** (this corpus has already been bitten by a stored value carrying literal backticks that silently voided an equality check). ⚠️ **That line contains `|` characters: it MUST live in the Step-1 dev log, NEVER pasted into the QA report.** ⚠️ **A divergence recorded here is a `✅` at row 3; the same divergence unrecorded is a `❌`.** ⚠️ **QA row 3 COUNTS these lines** (see its opening clause) — that count is what makes Rule 58(3) mechanical rather than aspirational. ⚠️ **The section always carries exactly as many lines as there are proposals you classified — 8 on a fresh run.** There is no "none" case: a run with zero divergences still has 8 `agreed` lines. A missing line is an unrecorded disposition, and QA row 3 counts them.
>
> 10. **`#### Doctrine pins`** — the raw `shasum -a 256` output for `DRAFTING_CYCLE.md`, `PLANNER_TEMPLATE.md` and `RULE_20_SELF_CHECK_BLOCK.md`, as recorded at G2. ⚠️ **QA row 7 re-measures and diffs against these, and marks itself `❌ (unverifiable)` if this section is absent** — the pins have no other durable home, and a G2 gate-table cell is not one.
>
> ⚠️ **Every measured value is deposited as RAW COMMAND OUTPUT, not a summary or re-typed figure** (memory `qa-evidence-raw-output`). "Raw" means the output is PRESENT, not unlabelled — annotate freely, but the annotation accompanies the raw output, never replaces it. QA rows 3, 4 and 4b each anchor to these values, and the pre-write state cannot be re-measured later.
>
> Canonical Python file-write — no heredoc. Commit both by explicit pathspec. `#### Prompt Feedback` in `### Ledger Updates`.
>
> **Deposits:**
> - `lessons-forge/knowledge/development/classifications-cycle-2026-07-29.md`
> - `lessons-forge/knowledge/development/dev-log-cycle-step-1-2026-07-29.md`

---
---

## STEP 2 — DEV (generate the report)

---

> **Before starting, read the Step 1 deposits and confirm the Output Receipt's opening status line is one of the two PROCEED-values — `Status: Complete` or `Status: Complete (idempotent re-dispatch — no work required)`. Anything else — including `Status: Partial — HALTED at …` and the 1a-ter stub's `Status: Partial — in flight` — stops this step; report and do not proceed.** ⚠️ **Treat this as an ALLOWLIST, not a prefix match**: a stub-only receipt means the ingest never finished, and proceeding on it would generate a report over a half-mutated corpus. ⚠️ **This is a DELIBERATE NARROWING of `PLANNER_TEMPLATE.md:433`, declared so you do not "correct" it back.** The template permits proceeding when status is *"Complete **or Partial with known acceptable scope**"*. This plan does not accept the Partial branch, because every halt condition in Step 1 concerns the integrity of the ingest itself — and this step's whole output is a report *derived from* that ingest.
>
> Post a short visible chat message. You are the Forge Developer (`/Users/marklehn/Developer/GitHub/forge/agents/FORGE_DEVELOPER.md` — skip with a note if absent). Same working-location + absolute-DB rules. **Open read-only:** `sqlite3.connect("file:/Users/marklehn/Developer/GitHub/lessons-forge/lessons-forge.db?mode=ro", uri=True)`.
>
> **Scope:**
> - `reports/lessons-report-2026-07-29.md`
> - `knowledge/development/dev-log-cycle-step-2-2026-07-29.md`
>
> **Before generating: if `reports/lessons-report-2026-07-29.md` already exists AND this step's dev-log deposit is committed → HALT** (`generate_lessons_report` overwrites unconditionally). If the report exists but the deposit is ABSENT → deposit-completion resume (regen is deterministic + read-only). Verified at authoring: no 07-29 report exists.
>
> Run `generate_lessons_report(conn, "2026-07-29")`. **The report is WHOLE-CORPUS** — it surfaces every `proposed`/`ambiguous` proposal; the date argument is only the filename/title.
>
> **⚠️ TWO INHERITED HALT CONDITIONS ARE FALSE FOR THIS CYCLE — do NOT clone 281's expectations. ⚠️ And do NOT hardcode the replacements either: read Step 1's recorded non-terminal set (`NT`, or `NT-original` on a resume) and derive both numbers from it. ⚠️ If Step 1's Receipt does not carry a parseable `NT`, STOP and report — do NOT fall back to the literals 10 and 2.** Those literals are the Planner's authoring-time measurement, not gate values.
> 1. **Expect `8 + |NT still non-terminal AT THIS MOMENT|` surfaced proposals.** With the expected `NT = {191, 192}` still `proposed` that is **10**. If Step 1 recorded `PASS (Gate 2 shipped)` it is **8**.
>    ⚠️⚠️ **AND `NT` CAN CHANGE BETWEEN STEP 1 AND HERE — this is the CEO's own documented workflow, not an anomaly.** The approval gate before this step is hours-to-days, and the CEO may ship Gate 2 in it, flipping 191/192 `proposed → implemented`. The report (`status IN ('proposed','ambiguous')`) then surfaces 8 with 0 route lines while Step 1 recorded `|NT| = 2` — and a plan comparing against the recorded literal halts on a legitimate action it elsewhere blesses. **So re-read each `NT` id's live status before computing the expectation:** an id now at a TERMINAL status is expected to be ABSENT — record `Gate-2 shipped in-window` and reduce the expectation accordingly; an id now `stale`, or absent while still non-terminal, is the real anomaly → HALT. Report both the recorded `|NT|` and the live one.
> 2. **Expect exactly `|NT|` `- **Route:**` lines** (**2** expected, **0** if Gate 2 shipped). `src/lessons_forge.py:584` emits the line under `if route is not None` — verified in code. **281 HALTed on ANY route line; cloned verbatim that would false-HALT this cycle.** The real anomaly is a route line correlating to a proposal with `entry_id > 184` — **that** is a HALT.
>    ⚠️ **The report prints NEITHER proposal id NOR entry_id** — verified: it renders `### {source_heading}` per proposal and, although `entry_id` is SELECTed (`:538`), it is never written to output. So you cannot attribute a route line to an id from the report alone. **Attribute by `source_heading`**, correlating against `SELECT p.id, p.entry_id, p.route, e.source_heading FROM lesson_proposals p JOIN lesson_entries e ON p.entry_id = e.id WHERE p.status IN ('proposed','ambiguous')`. Report the route-line count, the headings carrying them, and the ids they correlate to.
>
> **The one inherited halt condition that STILL HOLDS:** any `Recently-implemented overlap:` line → HALT (plan 207 retired `detect_recently_implemented_overlaps`; its reappearance is a regression).
>
> `generate_lessons_report` resolves `output_dir="reports"` RELATIVE to cwd — run `pwd` before the call, capture the path the function RETURNS, state that absolute path, and confirm the filename matches Scope. **Note the latent `encoding=` gap** (`src/lessons_forge.py:593` writes with no explicit `encoding=`; safe on the Mac/Bellows UTF-8 default; a Forward-Register item). Print the report head (~80 lines).
>
> **Deposit:** the report + `knowledge/development/dev-log-cycle-step-2-2026-07-29.md`. **The Output Receipt MUST carry an opening `Status:` line** — ⚠️ **Step 3's precondition reads this line, so it is not optional; without it that check has nothing to read.** Use `Status: Complete` or `Status: Partial — HALTED at <point>, <reason>`; the closed set's other two values are Step-1-only. **It MUST also carry a `#### Files Created or Modified` section** listing every file this step wrote, by path — ⚠️ **QA row 0 reads that section from THIS step's Receipt as well as Step 1's, and asserts it checked every entry.** Then: report length, proposals surfaced with entry_ids, route-line count + which ids, and the count of `Recently-implemented overlap:` lines — expected **zero**, since plan 207 retired the detector; any occurrence is a regression and a HALT. Canonical Python file-write — no heredoc. Commit both by explicit pathspec. `#### Prompt Feedback`.
>
> **Deposits:**
> - `lessons-forge/reports/lessons-report-2026-07-29.md`
> - `lessons-forge/knowledge/development/dev-log-cycle-step-2-2026-07-29.md`

---
---

## STEP 3 — QA

---

> **Before starting, read the Step 1–2 deposits and confirm both Output Receipt status lines are PROCEED-values — `Status: Complete` or `Status: Complete (idempotent re-dispatch — no work required)`. Anything else — `Status: Partial — HALTED at …`, or the 1a-ter stub's `Status: Partial — in flight` — stops this step; report and do not proceed.** ⚠️ **An ALLOWLIST, not a prefix match.** Naming the required VALUE is load-bearing: an instruction that merely says "confirm the status lines" is satisfied by observing a halted one.
>
> Post a short visible chat message confirming you are starting Step 3 (QA). You are Lessons Forge QA (`agents/FORGE_LESSONS_AGENT.md`). Same working-location + absolute-DB rules. **Verification + reporting only — no product-code changes; if a test fails, report it, do NOT fix it.** Do NOT use Monitor. Do NOT edit PROJECT_STATUS directly.
>
> **MANDATORY — Rule 20 self-check (canonical block, Checklist #4 — the exact template with all four placeholders filled, NOT a paraphrase).** ⚠️ **Form selection:** this is a **DB-mutating plan with a real pytest suite** → the **full canonical block with real, plan-appropriate evidence files**, NOT a simple banner. Run the canonical block from `/Users/marklehn/Developer/GitHub/RULE_20_SELF_CHECK_BLOCK.md` (ABSOLUTE path — the block lives at the governance root, NOT in the lessons-forge worktree). Copy the Python block verbatim and fill ONLY the four placeholders:
> - `plan_slug`: `cycle-session-12-captures-2026-07-29`
> - `qa_report_path`: `<your-own-tree-abs>/knowledge/qa/cycle-qa-2026-07-29.md`
> - `evidence_dir`: `<your-own-tree-abs>/knowledge/qa/evidence/cycle-session-12-captures-2026-07-29/` — the block runs in YOUR process and checks `os.path.isdir`, so **derive this from `pwd` (your OWN working tree), NOT a hardcoded main-tree path** (the plan-225 trap; in a worktree run this resolves under `.bellows-worktrees/<id>/`).
> - `required_evidence_files`: `["full-suite.txt", "invariants.txt", "hash-trap.txt", "schema.txt"]` — ⚠️ **Python string literals, QUOTED.** Copied unquoted into the block they raise `NameError` before any check runs, so it emits neither `FAILED` nor the banner, and `_gate_rule_20_self_check` then reports *"no QA deposit contains Rule 20 self-check banner"* — a misleading cause for what is really a transcription slip.
>
> Deposit all four evidence files BEFORE running the block — it `sys.exit(1)`s if any is missing or empty. Include the block's literal stdout in the QA report; the banner `## Rule 20 — QA Self-Check Results` + the `PASSED — SELF-CHECK PASSED` line must appear verbatim (byte-enforced by `bellows/gates.py`). **If the block prints `FAILED`, do not proceed with closure — halt and report to CEO** (the canonical template's own closing sentence, `RULE_20_SELF_CHECK_BLOCK.md:22` — it governs WHETHER THE STEP CLOSES and is NOT affected by this plan's declared deviation, which governs only where the stdout is written). ⚠️ **Writing the verification table does NOT satisfy this gate** — `_gate_rule_20_self_check` reads the deposited report for the banner and the `PASSED` line only, and is blind to the table entirely. (Rule 48, `:1024-1026`, requires a gate-enforced callout to state this explicitly; it is absent from plan 281, so a clone-diff would not surface it.) End with a self-grep confirming the banner.
>
> **⚠️ WHAT THE RULE 20 BLOCK DOES AND DOES NOT VERIFY — this batch's entry 191 documents it; act on it.** The block reads **evidence-file presence and hedging keywords only. It cannot see your verification verdicts.** A table containing a genuine `❌`, with all four evidence files present, prints **`PASSED — SELF-CHECK PASSED`, exit 0**. So:
> - **Expect `PASSED` even on an honest halt.** That is NOT a contradiction and NOT a reason to doubt your `❌`. The gate that catches a failure is `gates.py::_gate_rule_22_verification`.
> - **You must NOT flip a row to ✅, soften its wording, drop it from the table, or re-scope a check to make the block print `PASSED`.** A `PASSED` banner obtained by adjusting the rows it summarizes is a false close — strictly worse than an honest halt.
> - If any row is `❌`, add one line immediately under the Rule 20 stdout: *"Rule 20 PASSED reports evidence-file presence and hedging only — it does not read verification verdicts. Row(s) &lt;list EVERY ❌ row&gt; are ❌; this step is expected to fail at the rule_22 verification gate."* Name every failing row, not just the first.
>
> **⚠️ Rule 19 — REQUIRED VERBATIM IN THIS PROMPT** (`PLANNER_TEMPLATE.md:561` mandates that the QA prompt state this explicitly; **absent from plan 281, present in 282, and it must be read BEFORE the avoidance guidance below**): *"If you cannot complete a check, mark it ❌ with a reason. Do NOT mark it ✅ and explain why you couldn't verify. Any ✅ row containing hedging keywords will auto-fail during the self-check in Rule 20."*
>
> ⚠️ **Read that mandate and the guidance that follows in the right order.** Hedging language in a ✅ row is a signal that the check was not performed — Rule 19 exists because QA was observed marking rows ✅ with evidence like *"full suite pending"*. The instructions below teach you to avoid tripping the scanner **with honest measurements**. They do **not** license rewording a check you did not run. **The keyword list is a backstop with known gaps: an unverified ✅ phrased without those words survives it. A check you did not execute is ❌, whatever words you use.**
>
> **⚠️⚠️ DO NOT QUOTE THE HEDGING-KEYWORD LIST — OR PASTE THIS PLAN'S PROSE — INTO ANY LINE CONTAINING A `|`.** The block detects a positive-status row by cell equality but scans that row for hedging keywords by **whole-line substring**. So any `|`-bearing line pairing a positive-status token with "pending", "inferred", "assumed", "should pass", "not run" — even as a quotation or negation — emits CRITICAL and `sys.exit(1)`, halting a clean plan. **The hazard is ANY line containing a `|`.** Keep every table cell SHORT (claim / status glyph / measured value / evidence path); put narrative below the table or in an evidence file, and never combine a `|` with a positive-status word there.
>
> ⚠️⚠️ **A HEDGING KEYWORD IS FATAL EVEN AS A MEASURED VALUE — including a ZERO count.** The keyword list at `RULE_20_SELF_CHECK_BLOCK.md:41` contains **`skipped`**, **`not run`**, **`pending`** and **`assumed`**, and the scan is whole-line. So the most natural cell for row 1 — `55 passed, 0 skipped` — emits CRITICAL, `sys.exit(1)`, a `FAILED` banner, and then fails the Rule 20 gate for a missing `PASSED` line, **on a completely clean run.** This is not the "quoting the keyword list" hazard; it is an ordinary measurement using an ordinary word. **Write row 1's value as `<N> passed` and nothing else — measure `<N>`, do not copy it from this plan.** Planner's measured collection at authoring: **55**; report the actual and reconcile any difference (Checklist #29). The same trap applies to any cell reading "route disposition pending" or "assumed unchanged".
>
> **⚠️ On a FAILED block run, route its raw stdout to `invariants.txt` and describe the failure in the report WITHOUT reproducing the offending row.** The block echoes the offending line verbatim; pasting that stdout into the report makes the echo re-trip the scan permanently, foreclosing Rule 20's own documented recovery option (a) *"re-run after fixing the deficient evidence."* Markdown fences do not protect it. **This is a declared, narrow deviation from Rule 20's unconditional "include its literal output in the QA report" — on the FAILING path only.** The literal output is still captured, committed, and auditable. On a PASSING run, paste the stdout into the report as Rule 20 mandates. **This step is authorized to take this ONE declared deviation and no others** — if you believe you have found another, that is a finding to REPORT, not a licence to act on.
>
> **Evidence-source rule.** Every SQL/PRAGMA row states which DB it ran against (canonical = `sqlite3 "file:/Users/marklehn/Developer/GitHub/lessons-forge/lessons-forge.db?mode=ro" "<query>"`). Deposit **RAW command output, never a summary of it**.
>
> **Scope:**
> - `knowledge/qa/cycle-qa-2026-07-29.md`
> - `knowledge/qa/evidence/cycle-session-12-captures-2026-07-29/full-suite.txt`
> - `knowledge/qa/evidence/cycle-session-12-captures-2026-07-29/invariants.txt`
> - `knowledge/qa/evidence/cycle-session-12-captures-2026-07-29/hash-trap.txt`
> - `knowledge/qa/evidence/cycle-session-12-captures-2026-07-29/schema.txt`
>
> ⚠️ **The verification table MUST sit under a top-level `## ` heading containing the word "Verification"** — write exactly `## Verification Table`, and give it these columns: `| # | Claim | Status (✅/❌) | Measured value | DB source | Evidence |` — **the Evidence column is mandated by Rule 18 (`:558`): the row cites its evidence file BY PATH, "not by paraphrase, not by summary, not by 'see above'"**, and Rule 17 (`:555`) fixes the `Status (✅/❌)` spelling. `gates.py::_gate_rule_22_verification` tracks section transitions via `line.startswith("## ")` only, so a `###` sub-heading puts your table OUTSIDE the scanned region and an `❌` row would go undetected.
>
> ⚠️⚠️ **AND THE SECTION NEVER CLOSES — you MUST close it yourself with another `## ` heading.** `in_verification_section` is set only on a `## ` line and is **never cleared by `###` or `####`** (verified at `gates.py:657`), so it stays open to end-of-file and **every later `|`-bearing block is scanned as if it were the verification table.** Gate (c) defers "missing status" failures per table and flushes them if that table has at least one positive-status row — so your evidence-marker grep table, or a commit table with a single `OK` cell, would flag each of its other rows as a `(c) missing status` failure on an otherwise clean run. **Immediately after the verification table, open a new top-level section — write exactly `## Evidence and Narrative`** — and keep the Rule 20 stdout, the Output Receipt and `### Ledger Updates` beneath `## `-level headings.
>
> ⚠️ **Use ONLY `✅` or `❌` in the status column — no third value.** Not "PARTIAL", not "N/A", not "⚠️", not blank. A table where no row carries a positive-status token and none carries `❌` produces **zero** failures at BOTH gates — a table of hedged non-verdicts passes silently. If an outcome feels like neither, it IS an `❌`.
>
> **⚠️ A FAILING ROW DOES NOT LICENSE SKIPPING THE REMAINING ROWS.** This step has **eleven checks — rows 0, 1, 2, 3, 4, 4b, 5, 6, 7, 8, 9** — mutually independent. **Run all eleven, record every measured value, and halt only after the table is complete.** Inside a row, "→ halt" means *mark this row `❌`, record why, and keep going*. **A HALT MUST STILL LEAVE A COMMITTED RECORD:** finish the report with failing rows marked, write the evidence files, and commit whatever deposit files exist by explicit pathspec.
>
> ## Verification Table
>
> 0. **Deliverable verification (Rule 17 — MANDATED in the final step, BEFORE the regression check, hence row 0).** ⚠️ **Absent from plan 281 and from its parent 274; added here because the rule was opened and read, not because a clone-diff surfaced it** — a mandated element missing from both parents is invisible to any clone-diff (this batch's entry 188). Rule 17 (`:555`; the `Files Created or Modified (Code)` string also appears at `:515`, which belongs to **Rule 8**, not Rule 17): read the prior steps' Output Receipt **`#### Files Created or Modified`** sections and verify **EVERY** listed deliverable — **not sampling.** For each Step-1 and Step-2 deposit: confirm it exists on disk, and is committed **in its CURRENT state — two commands, both required**: `git log --oneline -1 -- <path>` **AND** `git status --porcelain -- <path>` which must be EMPTY. ⚠️ **`git log` alone is insufficient**: a file committed then modified still returns its old commit, so the check would pass while the current content is uncommitted — exactly the state this row detects. ⚠️ **Do NOT re-verify deposit CONTENTS here** — rows 3, 4, 4b and 5 do that and fail closed. **Any ❌ here is a Critical finding that blocks the plan from moving to Done.** ⚠️ Rule 17 permits "re-run the commit, re-create the file"; **this plan narrows that to RE-COMMITTING a deposit path it already declares — never re-creating content you did not produce.**
> 1. **Full suite** — `python3 -m pytest src/ -v` to an explicit pass/fail, raw tail shown. **Compute the baseline yourself from `--collect-only` and reconcile against the most recent prior QA in `knowledge/qa/`.** Confirm 0 regressions.
> 2. `get_unclassified_entries(conn)` on canonical returns `[]` (or exactly a Step-1 `### Deferred entries (CEO-approved)` list — quote both heading and query output).
> 3. **Invariants** on canonical, scope = `WHERE entry_id > 184` (read `E0`=184 from Step 1's receipt; do NOT scope by `route IS NULL`). Over exactly that set (**expect 8 rows**): dangling 0, invalid category 0, invalid confidence 0, every one `route IS NULL`, **and every one `status IN ('proposed','ambiguous')`.** ⚠️ **The status assertion is load-bearing for the Step-1→Step-3 isolation window:** a concurrent cycle's ingest between steps would stale THIS cycle's proposals, and a staled row is still 8 rows with `route IS NULL` — so without this clause the row PASSES on corrupted state.
>
>    Target fields — **for each NON-`ambiguous` proposal**: `target_layer='governance'`, and **`target_artifact` ∈ {`PLANNER_TEMPLATE.md`, `DRAFTING_CYCLE.md`, `RULE_20_SELF_CHECK_BLOCK.md`}** — a membership bound, not merely NOT NULL. ⚠️ **281 asserted this kind of membership set and it must NOT be reduced to a NOT-NULL check.** The premise that changed is that this batch's scout spans **three** artifacts rather than two — a reason to WIDEN, not delete. The bound is load-bearing because **`target_artifact` is bare `TEXT` with no CHECK** (`src/db.py:42`, verified) and the live corpus already contains free-text drift — `runner.py` AND `bellows/runner.py` both appear.
>
>    ⚠️ **FIRST, COUNT THE DISPOSITION LINES.** Step 1's `#### Scout dispositions` section must carry **exactly one line per proposal with `entry_id > 184` (expect 8)**, each matching one of the two fixed formats. **A count below the proposal count is `❌`, naming the proposal ids with no line.** ⚠️ **Without this count the Rule 58(3) remedy is not installed at all** — the rest of this row is divergence-triggered, so an agent emitting only divergence lines and no `agreed` lines would pass every check, leaving intact the exact "agreeing is the low-effort path" bias the section exists to remove.
>
>    ⚠️⚠️ **THEN: THE DISPOSITION TURNS ON WHETHER THE DIVERGENCE WAS RECORDED, NOT ON MEMBERSHIP — otherwise this row contradicts ledger constraint C5 and traps the agent.** Step 1 grants an explicit Rule-58 licence to place an entry outside the scouted set and record it. A row that marks any fourth artifact `❌` **gate-fails the plan on an outcome it just licensed** (`_gate_rule_22_verification` fires on an explicit `❌` unconditionally). So adjudicate:
>    - `target_artifact` **inside** the three-artifact set → `✅`.
>    - **outside** it, **AND** the `#### Scout dispositions` section of Step 1's **dev-log Output Receipt** (item 9) names that exact proposal id and that exact target, **AND the named target actually exists in the root repo** — `git -C /Users/marklehn/Developer/GitHub cat-file -e HEAD:<target>` must succeed → **`✅`**. ⚠️ **The existence check is what keeps this from being self-authorizing:** the same agent makes the placement and writes the divergence line, so without it a typo or an invented artifact passes on the strength of its own log entry — and `target_artifact` is the one column with no schema constraint. A named target that does not resolve → **`❌`**, reported prominently for CEO review at Gate 1. ⚠️ **That Receipt section is the single anchor; the classification deposit's prose narrative is NOT a substitute and NOT a second source of truth.**
>    - outside it with **no recorded divergence** → **`❌`.** *That* is the defect this bound exists to catch — indistinguishable from a typo.
>    - **The same three-way rule governs the category bound below.**
>
>    **⚠️ CATEGORY — assert a per-entry EXPECTED SET, never bare ADR-002 validity.** 281's cold panel added an explicit category assertion (fold FV1) on a proved premise: **`instrumentation` ALSO pairs with `target_layer='governance'`** (8 such rows live in this corpus), so the layer check does **not** police category. ⚠️ **This bound applies ONLY to proposals whose `status` is NOT `ambiguous`** — `ambiguous` is a **status**, not a category (`src/db.py:40`), so such a proposal still carries one of the five real categories.
>    - the **5 `planner-discipline`** entries (expected 185, 186, 187, 188, 192) → `category` MUST be `governance_rule`.
>    - the **3 `bellows-integration`** entries (expected 189, 190, 191) → `category` ∈ {`governance_rule`, `instrumentation`}.
>
>    Derive each entry's tag from its `lesson_entries.tags` column, not from this list's position. ⚠️ **The stored values carry LITERAL BACKTICKS** — the column holds `` `planner-discipline` `` (verified: `quote(tags)` returns `'`planner-discipline`'`). **An equality predicate returns ZERO rows and makes this whole bound vacuous.** Use `WHERE tags LIKE '%planner-discipline%'` and report the matched row count so a zero-match derivation is visible.
>
>    ⚠️ **Note what this row does and does not police:** it enforces the membership + recorded-divergence rule, but does **not** verify that an entry was mapped to the RIGHT one of the permitted artifacts — a 189↔190 swap is two in-set values and passes by design. That mapping is a Gate-1 CEO decision, informed by the recorded divergences and by row 9.
>
>    An **`ambiguous` proposal PASSES this row — exempt from BOTH the target checks AND the category bound** (NULL targets allowed; any valid ADR-002 category allowed). ⚠️ **The exemption must cover the category bound, not only the targets:** Step 1 permits `ambiguous` as a legitimate no-fit, so a rule reading "anything else is a FAIL" unconditionally would halt on a permitted result — the shape 281's cold panel caught as CB1. Report any `ambiguous` by id. **A scoped count ≠ 8 is a FAIL.** Cross-check `SELECT COUNT(*) FROM lesson_proposals WHERE id > 192` == 8.
> 4. **The 204 fix held.** Read the pre-cycle baseline from Step 1's dev log — **if missing, mark this row `❌` (unverifiable)**: it is the backstop for the documented staling-corruption signature and MUST fail closed. Re-read from canonical and diff: `stale` not grown. ⚠️ **The `stale` baseline is a before-anchor and C4 governs it: on a RESUME take it from the stub or the prior Receipt, NEVER from this dispatch's live re-capture** — a resume that re-reads an already-damaged corpus records the inflated count as its own baseline and compares it to itself. Planner's measured pre-cycle value: **3**. Also confirm no proposal moved off a terminal status, and **entry id 184's `content_hash` unchanged** (still `f2cf892c…`). Report `updated_count` + `terminal_proposals_flagged` **from the Receipt's `#### First-dispatch ingest dict` if that section is present** (a resume is then in evidence, and item 1 holds only the resume's all-zero re-run); otherwise from item 1. ⚠️ **If a resume is in evidence and NO first-dispatch dict was preserved, this cell is `❌ (unverifiable)`** — the ingest dict is the one value that cannot be reconstructed from the pristine backup. **Write the raw output to `hash-trap.txt`.**
> 4b. **⚠️ THE PRE-EXISTING PROPOSALS SURVIVED — no analogue in 281.** ⚠️ **The before-capture you compare against is Step 1's recorded one — labelled `NT` on a FRESH dispatch and `NT-original` on a RESUME.** Read whichever the Receipt carries; **do not look only for `NT-original`**, because Receipt item 5 mandates that label only on a resume, so on a clean fresh run it does not exist and a literal reader would mark this row `❌ (unverifiable)` — hard-failing the gates on a completely clean execution (`gates.py:687` fires on an explicit `❌` unconditionally). Rows 4 and 5 already use the both-labels wording; this row must match them.
>
>    ⚠️ **Query BY ID, not by the status predicate** — `SELECT p.id, p.entry_id, p.status, p.route, p.target_artifact, e.content_hash FROM lesson_proposals p JOIN lesson_entries e ON p.entry_id = e.id WHERE p.id IN (<the ids in that before-capture>)`. **Re-running the status-predicate form here would make this row self-defeating: a staled proposal has `status='stale'` and DROPS OUT of that result, so a comparison over the returned rows finds nothing changed and PASSES on the corruption it exists to detect.** ⚠️ This does not violate C3: C3 governs *capturing* an unknown set; re-verifying a **known** id set is correctly done by id.
>
>    ⚠️ **Adjudicate `status` DIRECTIONALLY — "unchanged" alone false-FAILs the CEO's own workflow.** The approval gates are hours-to-days, and the CEO may legitimately ship Gate 2 *during* one, flipping `proposed → implemented`. **A move to `stale` is corruption; a move to a `_TERMINAL_STATUSES` member is a CEO disposition.** So: `status` unchanged, **or** moved from `proposed` to a terminal status → **`✅`**, with a `Gate-2 shipped in-window` note. `status` → `stale` → **`❌`** always. A change to `route` or `target_artifact` → **`❌` when `status` is still `proposed`** (nothing legitimate moves those mid-flight); **but when `status` ALSO moved to a terminal value, treat it as part of the same Gate-2 disposition** — a codification that re-places a proposal is a coherent CEO action in the same window this row already accommodates — so record it as a `Gate-2 shipped in-window` note rather than a false `❌`. Any change to the parent entry's `content_hash` → **`❌`** regardless; that is the staling trigger and no CEO action produces it. Beyond that, **for every id in `NT-original` those remaining fields must be UNCHANGED** — a field-wise comparison, not a text diff. **Any `NT-original` id ABSENT from the result is `❌`** (nothing in this cycle deletes). **If Step 1's receipt does not carry the before-capture, mark this row `❌` (unverifiable).** **Also confirm entry ids 183 and 184 still carry their baseline `content_hash`es** — both are captured by name in Step 1a precisely so this comparison has operands even when `NT` is empty. Raw output to `hash-trap.txt`.
> 5. **Report** exists; **its surfaced proposals are EXACTLY this cycle's 8 (`entry_id > 184`) plus those `NT` ids that are STILL NON-TERMINAL when you measure** — ⚠️ **apply row 4b's directional rule here too, or this row false-`❌`s a Gate-2 ship that happened in an approval window** (an `NT` id now at a terminal status is expected to be absent from the report — record `Gate-2 shipped in-window` and reduce the expected count; an id now `stale`, or absent while still non-terminal, is `❌`). Otherwise: **`8 + |NT|` in total, and no others** (**10** expected; **8** if Step 1 recorded `PASS (Gate 2 shipped)`). ⚠️ **Read `|NT|` from Step 1's receipt (`NT-original` on a resume) — do NOT hardcode 10. If the receipt carries no parseable `NT`, mark this row `❌` (unverifiable) rather than falling back to the literal.** A surfaced proposal in neither set means unexpected non-terminal state → FAIL. **Exactly `|NT|` `- **Route:**` lines**; a route line correlating to any `entry_id > 184` proposal → FAIL.
>    ⚠️ **The report prints neither proposal id nor entry_id** (`entry_id` SELECTed at `:538`, never written). **Use the report's own `**Total proposals:** N` line for the count**, and correlate every `### ` heading and route line back to ids via the DB join in Step 2. State the correlation in the evidence file — a bare "10 surfaced, 2 routes" without the heading→id mapping does not distinguish the right ten from a wrong ten. Zero `Recently-implemented overlap:` lines; `detect_recently_implemented_overlaps` still absent from `src/`.
> 6. **No schema drift** — compare the live schema of `lesson_entries` / `lesson_proposals` against `src/db.py`'s DDL **SEMANTICALLY** (column names + types + NOT NULL + PK via `PRAGMA table_info`, plus the CHECK/UNIQUE constraint set), **NOT a literal `.schema`-text diff**. ⚠️ Cosmetic artifacts of a past RENAME migration — a quoted `"lesson_proposals"`, `IF NOT EXISTS` differences, whitespace, index ordering — are EXPECTED and NOT drift. A FAIL is a real column/type/constraint difference. **Deposit the raw `.schema` output for both tables to `schema.txt`** (Rule 18 names schema-verification queries as mandatory evidence).
> 7. **The Gate-2 target artifacts are UNCHANGED by this cycle.** ⚠️ `lessons-forge` is a submodule; these files are tracked by the ROOT repo and do NOT exist in your worktree — a plain `git diff` passes VACUOUSLY. Run against root, ONE invocation, capturing the exit code:
>    `git -C /Users/marklehn/Developer/GitHub status --porcelain -- DRAFTING_CYCLE.md PLANNER_TEMPLATE.md RULE_20_SELF_CHECK_BLOCK.md; echo "PORCELAIN-EXIT=$?"`
>    ⚠️⚠️ **NON-EMPTY PORCELAIN IS `❌`. FULL STOP. THIS ROW FAILS CLOSED AND YOU DO NOT ADJUDICATE IT.** An earlier draft of this plan downgraded this to "a reconcile-note, not automatically a ❌, unless attributable to your worktree." **That was a safety regression and it is reversed here.** Plan 282 states the reason in terms this plan must not soften: ***"There is no other guard; this row IS the guard"*** — `scope_check` is cwd-scoped and cross-repo-blind (`files_changed` comes from `_parse_diff_stat(…, wt_path)`), so nothing else in the system can see an edit to a root-repo doctrine file. 282 also forbids the exact reasoning that downgrade used: ***"THE ROW IS `FAIL` REGARDLESS OF WHAT THE LOG SHOWS… You may not mark this row ✅ on the reasoning that the commit looks unrelated, pre-existing, benign, or someone else's — that judgment is not yours to make."*** **The legitimate concern that motivated the downgrade — a CEO editing `DRAFTING_CYCLE.md` in-window to draft Gate 2 — is handled by ATTACHING EVIDENCE, not by grading your own attribution:** capture `git -C /Users/marklehn/Developer/GitHub log --oneline 881ec60..HEAD -- <the three files>` and `git -C … diff -- <the three files>` into `invariants.txt` **before halting**, mark the row `❌`, and let the CEO adjudicate at the verdict gate — which is exactly where an in-window CEO edit is waved through in one sentence. **The stale reasoning that follows in this paragraph is superseded by this clause; read it as context for why the row exists, not as licence to downgrade it.** This row asserts something about *this cycle's* behaviour but measures the *root working tree's global state* across a multi-day approval window the plan does not own — and the CEO is expected to open `DRAFTING_CYCLE.md` to draft the Gate-2 codification during exactly that window. The plan applies this reasoning twice already: G2 records a root-HEAD delta as a reconcile-note because *"the root repo legitimately moves between authoring and dispatch"*, and this row excludes `plan_lint.py` because *"this cycle has no write path to the bellows repo"* — which holds verbatim for the three root files. **So ALSO re-measure the pins and actually perform the comparison** — `shasum -a 256` on the same three files — and diff against the values G2 recorded. ⚠️ **Without this the comparison has one operand and the recorded pins are dead data:** `status --porcelain` is empty on a clean tree, so a *committed* `DRAFTING_CYCLE.md` edit during the Gate-1 window passes the porcelain check and is caught only by the pin. ⚠️⚠️ **AND PIN WORKING-TREE CONTENT, NOT THE HEAD BLOB — `git rev-parse HEAD:<path>` IS BLIND TO THE CASE THAT MATTERS MOST.** It resolves the blob in HEAD's **tree**, not the file on disk (verified), so an **uncommitted** edit to `DRAFTING_CYCLE.md` — precisely what a misbehaving agent in this cycle would produce — passes a HEAD-blob pin. Combined with the downgrade above, that let this cycle mutate doctrine and close clean. **Use `shasum -a 256` on all three files, recorded at G2 in Receipt item 10 (`#### Doctrine pins`) and re-measured here, and diff. Any mismatch → `❌`** with the same evidence-then-halt procedure. ⚠️ **If Step 1's Receipt carries no `#### Doctrine pins` section, this row is `❌ (unverifiable)` — an empty porcelain does NOT discharge it.** Without that clause the row's one enumerated failure mode on a clean tree is a mismatch it cannot compute, so a *committed* doctrine edit in the Gate-1 window — the exact case the pin was added to catch — would pass green on a one-operand comparison. **A non-zero exit code is still a `❌`, not a pass.** ⚠️ **`RULE_20_SELF_CHECK_BLOCK.md` is in this check because entry 191 targets it. `bellows/scripts/plan_lint.py` is deliberately NOT checked** even though entry 190 targets it: this cycle has no write path to the bellows repo. Raw output to `invariants.txt`.
> 8. **Post-cycle DB counts** — entries total (**expect 192** = 184 + 8; the 184 includes orphan rows, NOT the file's 135), proposals total (**expect 200**), proposals by status + category, stated as actuals. Raw output to `invariants.txt`.
> 9. **Classification depth spot-check.** Scope `entry_id > 184`; for each of the 8 confirm its `reasoning` genuinely quotes its entry's `raw_content`. **Use the EXTRACTION-FREE check**: (a) `canon(s)` = normalize curly→straight quotes, strip `*_`` ` chars, collapse whitespace, lowercase; (b) longest common substring via `difflib.SequenceMatcher(None, a, b, autojunk=False).find_longest_match(...)`; (c) PASS if match **>= 40 chars**. **Report the measured longest-match length per proposal as a number** + PASS/FAIL + the ids. ⚠️ With 8 proposals this is the row most likely to surface a thin classification from a saturated tail — report all 8 measurements, not a summary. (Measured at authoring: the 8 batch entries' `raw_content` is 1408–1986 chars, so the 40-char floor cannot false-FAIL on length.)
>
> **Write the raw output of rows 0, 2, 3, 5, 7, 8 and 9 to `invariants.txt`; rows 4 and 4b to `hash-trap.txt`; row 6 to `schema.txt`; row 1's pytest tail to `full-suite.txt`.** These are the four `required_evidence_files` the Rule 20 block verifies non-empty. ⚠️ **Non-empty is ALL the block checks** — a one-byte file passes it (entry 191 documents the block's limits). So the block's `PASSED` is not evidence your evidence landed. **Before running it, self-grep each of the four files for a marker proving the intended content reached it** — `PORCELAIN-EXIT=` in `invariants.txt`, the hash-trap entry's `content_hash` prefix in `hash-trap.txt`, `CREATE TABLE` in `schema.txt`, and the pytest summary line in `full-suite.txt` — and record each grep's result. A missing marker means a truncated or misdirected write: fix it before the block runs, and if it cannot be fixed, mark the owning row `❌`.
>
> **Deposit:** `knowledge/qa/cycle-qa-2026-07-29.md` — the table under `## Verification Table` with the mandated columns, raw full-suite tail, the Rule 20 banner + PASSED line + literal stdout (passing runs only), Output Receipt — plus the four evidence files. Canonical Python file-write — no heredoc. **Commit all deposits by EXPLICIT PATHSPEC** — `git add <the exact paths>` then `git commit -m "…" -- <the exact paths>`. **NEVER `git commit -a`, and never an unscoped `git add -A` / `git add .`.** ⚠️ **`git add` FIRST — load-bearing for NEW files:** `git commit -m "…" -- <untracked path>` fails with `error: pathspec … did not match any file(s) known to git`. Your evidence files are new. **If you hit that error the fix is to `git add` the path and retry — NEVER to fall back to `git commit -a`.**
>
> In `### Ledger Updates` include `#### Project Status` (one milestone: cycle 2026-07-29 complete — the 8-entry session-12 batch ingested + classified, report deposited, corpus integrity held, the pre-existing proposals 191/192 unharmed; Gate 1 route disposition pending for the 8), `#### Forward Register` (the `generate_lessons_report` `encoding=` gap at `src/lessons_forge.py:593`), and `#### Prompt Feedback`.
>
> **Deposits:**
> - `lessons-forge/knowledge/qa/cycle-qa-2026-07-29.md`
> - `lessons-forge/knowledge/qa/evidence/cycle-session-12-captures-2026-07-29/` (multiple files per Rule 20 self-check)
>
> ⚠️ **The evidence DIRECTORY is one bullet; the four filenames are deliberately NOT listed here.** `PLANNER_TEMPLATE.md:808` (Rule 26): *"Evidence files (Rule 18) MUST be represented by the evidence directory as a single bullet… Do NOT list individual evidence files… duplicating them in `**Deposits:**` is forbidden."* Both parent plans violate this. It matters here because this step may halt mid-table and commit only the deposits that exist — a declared-but-unwritten `schema.txt` would produce a false gate failure on an honest halt. The filenames remain in `**Scope:**` and in `required_evidence_files`, where Rule 26 says they belong. (The 2026-04-19 lesson at `:1923` forbidding directory bullets is STALE — superseded per `:1942`, with `:1944` recording the canary-diagnostic resolution; directories resolve via `isdir()` at every gate strategy.)
>
> **Do NOT move this plan to `Done/`.** The close path is owned by Bellows on continue-verdict consumption — **never by the agent** (`PLANNER_TEMPLATE.md:513`, Rule 8: *"Either way, the agent is not the actor"*). ⚠️ This is unconditional: there is no post-verdict branch in which the agent may move it. An `unauthorized_done_move` force-recovers the file and FAILs this step's gates.

---

## Drafting Cycle

**This section is a RECORD of the drafting cycle, NOT instructions for any step.** Nothing here modifies the steps above. ⚠️ It sits inside the final step's extracted span — `gates.py:449` extracts the final step with a regex whose lookahead has no later step to stop at, so the span runs to end-of-file. That is a parsing artifact, not scope. The full per-fold narrative is deliberately NOT here (§3, "compact"): it lives in `scratchpad/cycle-narrative-283-FULL.md`.

**Tier:** T2 — trigger fired: T-2 (production-data mutation of the canonical lessons corpus, which has a documented silent-corruption history). Proven clone of 281/274/257, so T-8 does not fire; T-2 sets the floor. The corpus holds a live non-terminal pair (proposals 191/192) the ingest can stale, so the cold panel is not skippable.
**Walks:** 1 sequential walk → 5-lens cold panel → confirming walk → reconstruction-confirming walk; readers rotated, and the target rotated to the mutating step first (entry 186).
- Weak spots:          w1 5 · cold 8 · confirming 3 · recon-confirming 1 · cold-v16 5.
- Destruction:         w1 5 · cold 6 · confirming 2 · recon-confirming 1 · cold-v16 6 (incl. a hard doctrine-file guard found INVERTED into a self-adjudicated note, reversed).
- Vulnerabilities:     w1 5 · cold 7 · confirming 2 · recon-confirming 1 · cold-v16 5 (incl. a pristine-backup glob that resolved to a 9-day-old snapshot, failing unsafe).
- Integration-record:  w1 4 · cold 11 · confirming 2 · recon-confirming 1 · cold-v16 6 (incl. a header test-scope claim, and Rule 35/48 elements absent from this plan AND its parents).
- ACID:                w1 2 · cold 9 · confirming 1 · recon-confirming dry · cold-v16 5 (incl. two folds from ONE walk that cancelled each other, deadlocking every resume path — the fold-interaction failure this lens exists for).
**Cold panel (T2):** COMPLETE — all 5 lenses run as sequential fresh-context readers, each on the prior's folded draft; every finding author-verified against live code or DB before folding.
**Conflicts:** C1 pre-existing non-terminal proposals survive unchanged on status/route/target/parent-hash, except a CEO-driven transition to a terminal status, which must be recorded. C2 no step hardcodes a count derived from their status. C3 `NT` is **captured once by predicate; re-verified only by id** (the by-id carve-out is invoked five times — G1's baseline probe, row 4b, Step 2's live re-read, row 5, and the pristine-backup reconstruction — each declared; "captured once" is true of the capture, not of the reads, and is restated this way so a future clone does not read the five as drift). C4 a resume anchors on the ORIGINAL committed capture, never a live re-read. **C5 a permitted outcome is never a FAIL — with ONE RECORDED EXCEPTION: QA row 7 fails closed on any change to the three root doctrine files, including a legitimate in-window CEO edit, because no other instrument in the system can see that change (`scope_check` is cross-repo-blind; 282: "this row IS the guard"). The `❌` is an escalation channel to the verdict gate, not a verdict on the cycle. Declared explicitly so a future clone does not re-derive the downgrade this cycle already reversed once.** **Resolved:** A1's backup reconstruction appeared to violate C3 — resolved as a bounded fallback derivation of the same value. **§2.8 oscillation signal FIRED and settled** via the Rule 56 determination recorded in Step 1.
**Ledger status at cycle close:** C1–C4 SATISFIED; **C5 SATISFIED with one declared exception (row 7), stated in C5 itself.** ⚠️ **This line has been wrong before and was corrected by the lens that checks it:** during the v16 cold walk the ledger read "C1–C5 all SATISFIED" while C5 was violated three ways — a permitted resume, the idempotent re-dispatch, and an expected CEO doctrine edit all produced failures. **A constraint recorded but unenforced is the failure the ledger exists to prevent, and a ledger that misreports its own status is the same failure one level up.** Earlier in the cycle C1, C4 and C5 were likewise each found violated by the plan's own instructions and repaired.
**Residual risk register and codification-effect prediction: see the CEO Context**, where their audience is and where they sit outside the final step's extracted span. Kept out of this log per §3 "compact" — entry 189 of this very batch.

**Recovery note:** the working draft was destroyed TWICE by authoring errors (truncating whole-file writes). This document was rebuilt from the surviving CEO-Context fragment plus the recorded fold set. A dedicated fidelity reader audited the first rebuild and found 5 seams, all repaired; this second rebuild has been presence-audited but the reconstruction itself has not been independently re-read.

**Closing:** **NOT DRY, and deposited on a fail-closed test rather than a dry-walk test — a deliberate, declared deviation from §2's closing condition, made by the CEO at the deposit gate.** The cold walk on v16 ran all five lenses cold and sequentially, and **every one folded** (5 / 6 / 5 / 6 / 5 = 27). §2's "done" test — a full walk returning zero or only-minor findings — is therefore NOT met, and the last event before deposit is a fold, not a lens pass.

**Why deposit is nonetheless the call, stated so it can be judged rather than assumed.** (i) Continued folding is now producing defects as well as removing them: the two worst findings of the cycle — a resume architecture deadlocked by an assert added in the same walk that repaired it, and a hard guard inverted into a self-adjudicated note — were both introduced by folds, not found in the original clone. (ii) The confidence distribution is not uniform: the corpus-protection path is the most-verified thing in this plan and the unclosed risk is concentrated in failure-path scaffolding that halts rather than mutates (see the residual register above). (iii) **This plan cannot validate the lessons it carries.** Entries 185, 187, 188, 190 and 192 each recurred inside this very cycle, several times each; the artifact that tests whether they were absorbed is the NEXT plan drafted after Gate 2, not this one — hence the codification-effect prediction recorded above.

⚠️⚠️ **THE `plan_lint` CLOSING WARN DOES NOT FIRE ON THIS PLAN, AND THAT SILENCE IS THE BUG — NOT A PASS.** Measured at deposit: exit 0, one WARN ("step 3 mentions tests but declares no test scope"), and **no closing WARN** — because this very line contains the string "NOT DRY", and entry 190's defect (b) is that the §4 check tests `has_fold and not has_dry` by **substring**, so a line declaring the cycle *not* dry satisfies `has_dry` and passes. **The gate reports this cycle closed while this line states in its first sentence that it is not.** Nothing was engineered to achieve that; the phrasing is the honest one and the gate reads it wrong. This is now the **third** time on this plan the §4 check has reported a disposition it cannot actually see — it fired, cleared, and fired again on prose wording alone. **Read this line, never the WARN**, and carry this to Gate 1 as live corroboration for entry 190. Precedent for depositing with §2's closing condition unmet and declared: plan 282, which shipped the same way with the CEO informed and its Closing line saying so.

**Cycle totals:** 103 folds — walk 1: 21 · cold panel: 41 · confirming walk: 10 · reconstruction-confirming walk: 4 · cold walk on v16: 27. Plus 5 seam repairs from the post-loss fidelity audit. Two working drafts were destroyed by authoring errors and rebuilt; this is the second rebuild.
