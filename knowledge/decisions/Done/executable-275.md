# Lessons Forge — Gate 1 Route Disposition (DRAFTING_CYCLE.md refinements cycle, 2026-07-24)
**Date:** 2026-07-24 | **Tier:** Small | **Dispatch Mode:** bellows | **Test Scope:** targeted | **Execution:** Step 1 (DEV) → Step 2 (QA) | **qa_steps:** 2 | **pause_for_verdict:** always
**cycle_tier:** T2

## CEO Context

Gate 1 for the 4 proposals from cycle 274 (the DRAFTING_CYCLE.md-refinement batch — entries 179–182, all dated 2026-07-24). **CEO dispositions are final and embedded below: ALL 4 → codify (0 reference, 0 backlog).** The CEO walked each proposal individually (2026-07-24) and routed every one to `codify`.

**Disposition table (authoritative for both steps):**

| Proposal | Entry | Note | Topic | Route | Status after |
|---|---|---|---|---|---|
| 187 | 179 | N1–N3 | §2.2 (Destruction) + §2.5 (ACID) need diagnostic-mode residues | **codify** | `proposed` (unchanged) |
| 188 | 180 | N4 | make §2's no-batch / run-each-lens-on-the-prior-fold rule EXPLICIT | **codify** | `proposed` (unchanged) |
| 189 | 181 | N5 | §4 self-check reads the last lens line's status, not a prose substring (+ `plan_lint.py`) | **codify** | `proposed` (unchanged) |
| 190 | 182 | N6 | fix the live §3-vs-§4 T0 `cycle_tier` regex contradiction (+ `plan_lint.py`) | **codify** | `proposed` (unchanged) |

**⚠️ THIS GATE 1 WRITES `route` ONLY — NO status change (the inversion of 258).** All four are `codify` → Gate-2-bound → every one KEEPS `status='proposed'`. There is **no hand-written SQL** in this plan: every write is `set_proposal_route(conn, id, 'codify')` (src/lessons_forge.py:256 — parameterised `UPDATE … WHERE id=?`, validates the route value, no internal commit). Because no status moves, the **status distribution is byte-identical before/after** — this is an *invariant to CHECK*, the exact opposite of 258 (which changed one status and warned NOT to carry a byte-identical invariant). Do not clone 258's "predicted delta, not identity" language: here identity holds on status; the only delta is `route` NULL→`codify` on exactly four rows.

**Authoring facts — MEASURED against canonical 2026-07-24 (not predicted; confirm, do not assume):**
- The four targets: `187→179, 188→180, 189→181, 190→182`, each `status='proposed'`, `category='governance_rule'`, `route` NULL, `target_layer='governance'`, `target_artifact='DRAFTING_CYCLE.md'`.
- **The only four `proposed`-with-NULL-`route` rows in the corpus ARE these four** (`SELECT COUNT(*) WHERE status='proposed' AND route IS NULL` = 4) — no collateral proposed proposal exists.
- **route-NOT-NULL = 56** now → **60** after (a rise of exactly 4 on a clean run). Status distribution now: `implemented 133, superseded 28, rejected 15, reference 7, proposed 4, stale 3` (total **190**) — **unchanged** after this gate.
- `get_unclassified_entries(conn)` = `[]` (post-274) — must stay `[]` (route is orthogonal to classification; a non-empty result means an unrelated staling, → FAIL).

**Dedup against LIVE `DRAFTING_CYCLE.md` v1.0, done at Gate-1 authoring (the 2026-06-07 discipline) — all four are NOT codified, so `codify` stands for each:**
- **187 (N1–N3):** §2.1 carries the only diagnostic-tailored sub-question (1.4, `:44`); **§2.2 (`:48–52`) and §2.5 (`:66–70`) carry NO diagnostic-mode sub-question** → the residue does not exist → codify.
- **188 (N4):** §2 (`:38`) states "one pass per lens per walk, fold after each pass, re-run a lens only on a subsequent walk" and the closing-pass rule — the sequential-fold behaviour is **implied but never stated as an explicit no-batch rule naming the "just confirmation" rationalization** → partial overlap, codify as an explicit extension. **Nearest existing clauses: §2.7 (`:79`) "concurrent lenses sever cumulation" + §2.6 (`:73`) "cumulation lives in the draft, so sequential preserves it" — but those forbid CONCURRENT/parallel lenses, whereas N4 forbids BATCHING all lenses against one un-folded draft (sequential-but-no-fold) and names the rationalization; distinct, so codify stands.**
- **189 (N5):** §4 (`:125`) says only "the **closing** line asserts a dry lens pass as the last event"; **how the gate reads it (last lens line's status vs the `'fold'/'dry'` substring in `plan_lint.py`) is unspecified in the doc** → codify (doc primary + `plan_lint.py` coupling).
- **190 (N6):** the contradiction is **LIVE in the shipped doc** — §3's T0 example (`:114`, `**cycle_tier:** T0 (no trigger); integration-vs-record pass: <result>.`) trips §4's `re.match(r'^T([012])$', …)` gate → not "already fixed" → codify (doc primary + `plan_lint.py` coupling).

**⚠️ Notes for Gate 2 (NOT this plan's job — routing only here):**
- **187 (N1–N3)** and **188 (N4)** are **pure-doc** amendments to `DRAFTING_CYCLE.md` (§2.2/§2.5 sub-questions; an explicit §2 no-batch clause). Keep the five-lens count per §6's "add a sub-question" amendment shape. **⚠️ For 188: place the new no-batch clause as an EXTENSION that cross-references the existing cumulation clauses (§2.7:79, §2.6:73), NOT a competing §2 rule — matching 258's 178/184 partial-overlap convention ("extend/cross-reference, don't add a competitor").**
- **189 (N5)** and **190 (N6)** each carry a **`bellows/scripts/plan_lint.py` coupling** — §6's "coordinate doctrine and gate" / "keep the self-check in lockstep": Gate 2 must pair the §4/§3 doc edit with the `plan_lint.py` edit **and its tests**, or explicitly defer the gate change and say so. 190 is a live doc-vs-gate bug; 189 hardens a gameable heuristic.
- Gate 2 bumps `DRAFTING_CYCLE.md`'s version + appends a dated changelog row naming the units it changed (§6).

**⚠️ Concurrency — dispatch with NO other lessons-forge plan in flight (ACID/isolation).** This gate routes four NON-terminal `proposed` proposals; a concurrent whole-corpus ingest in the Step-1→Step-2 window could stale one (`status='stale'`), leaving a routed-but-stale row. **PREVENT:** the daemon serialises same-project plans + the Step-1 A0 single-writer check. **DETECT:** QA row 2 (each target still `proposed`), row 3 (distribution stale-count identity vs the Step-1 before-snapshot), row 5 (`get_unclassified` unchanged). Do NOT dispatch a second lessons plan until this one closes.

**Scope discipline: `route` writes ONLY — no status change, no `src/` edit, no doctrine edit.** Do NOT edit `DRAFTING_CYCLE.md`, `PLANNER_TEMPLATE.md`, or `plan_lint.py` — codification is Gate 2. Do NOT touch `src/`. Do not pre-draft any codification wording in a deposit.

**Deposit-once discipline:** grep `knowledge/decisions/` for an existing Gate-1 plan first, then deposit exactly once (memory `bellows-deposit-once-discipline`). **Authoring self-check:** run `bellows/scripts/plan_lint.py` on the FINAL post-cycle draft (the `## Drafting Cycle` block complete — all five lens lines, the T2 cold-panel line, a dry closing) — expect exit 0; the §4 check reads the bare `**cycle_tier:** T2` + the block. The one WARN — *"step 2 (QA) mentions tests but declares no test scope"* — is the known-benign QA-runs-pytest-without-modifying-tests class (memory `benign-gate-failure-classes`); do NOT add a test file to Step 2's scope to silence it.

## How to Run This Plan

**Bootstrap prompt:**
```
Read the plan at lessons-forge/knowledge/decisions/in-progress-executable-<id>.md (daemon renames the deposited placeholder on claim). Execute Step 1 ONLY. After completing Step 1, STOP and wait for my confirmation. Do NOT proceed to Step 2.
```

---
---

## STEP 1 — DEV

---

> **FIRST — before any reads or work: post a short visible chat message (1-2 sentences) confirming you are starting this plan and your immediate next action.** Do NOT rename the plan file.
>
> You are the Lessons Forge Developer (`/Users/marklehn/Developer/GitHub/forge/agents/FORGE_DEVELOPER.md` — skip with a note if absent). Run commands from **your own working tree**. **All canonical-DB access uses the ABSOLUTE path** `/Users/marklehn/Developer/GitHub/lessons-forge/lessons-forge.db` — never substitute or skip a verification because a relative path resolves to nothing. `forge/forge.db` is a REAL but DIFFERENT database — never open it. **This step changes the canonical DB `route` column only; it MUST NOT touch `src/` and MUST NOT change any `status`.**
>
> **Open canonical read-WRITE** (`sqlite3.connect(<abs path>)`; do NOT reuse a `?mode=ro` handle for the writes). The four `set_proposal_route` calls are ONE transaction: a SINGLE `conn.commit()` after all four (the helper does not commit internally; the DB is gitignored so this is a SQLite commit, not a git commit).
>
> **Scope:**
> - `knowledge/development/gate-1-route-drafting-refinements-2026-07-24.md`
>
> **Task A00 — TAKE A RESTORE POINT BEFORE ANY WRITE.** Defense-in-depth (there is no hand-SQL here, but a restore point is cheap and standard). **Use `.backup`, NOT `cp`** (live WAL). **Write to the MAIN tree by ABSOLUTE path** (a worktree-local backup dies at teardown): `mkdir -p /Users/marklehn/Developer/GitHub/lessons-forge/data/backups && sqlite3 "file:/Users/marklehn/Developer/GitHub/lessons-forge/lessons-forge.db?mode=ro" ".backup '/Users/marklehn/Developer/GitHub/lessons-forge/data/backups/lessons-forge-pre-gate1-drafting-<UTC-timestamp-colon-free>.db'"`. `.gitignore` matches `*.db` — confirm absent in `git -C /Users/marklehn/Developer/GitHub/lessons-forge status --porcelain`. **State the absolute path in your dev log. If the backup fails for ANY reason (non-zero exit, missing/zero-byte file) — HALT and write nothing.**
>
> **Task A0 — ISOLATION pre-flight, from the tree where the state lives.** This is an `R(before) → W → R(after)` schedule; assert no other writer. **Read the lifecycle state by ABSOLUTE MAIN-TREE path — a worktree-relative `ls` passes VACUOUSLY** (this is proposal 184's class, and entry 180/N4 of the batch this plan routes names the batch-vs-vacuous trap). Run `ls /Users/marklehn/Developer/GitHub/lessons-forge/knowledge/decisions/` and assert the POSITIVE signal: **your own plan file MUST appear** (as `in-progress-*` or `verdict-pending-*`; the daemon claims this marker by renaming in the MAIN canonical `decisions/` it watches — BEFORE any per-step worktree spin — so this absolute main-tree `ls` is correct in both in-place and worktree modes) AND no OTHER `in-progress-*`/`verdict-pending-*` lessons/cycle plan. If your file is absent, you are reading the wrong tree — HALT (fail-closed: write nothing). Then read the **route-NOT-NULL count** and the **status distribution** twice a moment apart; confirm both unchanged. **Do NOT use `get_unclassified_entries()` as the quiescence signal — it filters on `status`, never `route`, so a route conflict is invisible to it; it is captured (A0 before-snapshot item 3) only as an orthogonal before/after check.** Any concurrent writer possible → HALT. (A concurrent whole-corpus ingest could stale a `proposed` proposal mid-window — the daemon serialises same-project plans, so this is belt-and-suspenders; the QA staling detectors are rows 2 [target still `proposed`], 3 [distribution stale-count identity], and 5 [`get_unclassified` unchanged] — NOT row 6, which is the doctrine-unchanged check.)
>
> **Task A0 — PRECONDITION: verify targets match the disposition table BEFORE writing.** `SELECT id, entry_id, status, category, route FROM lesson_proposals WHERE id BETWEEN 187 AND 190 ORDER BY id` and assert all four: the id→entry_id mapping is exactly `187→179, 188→180, 189→181, 190→182`; each `status='proposed'`; each `category='governance_rule'`; each `route` is **NULL or already `codify`** (idempotent re-run tolerated). **HALT on genuine drift** (missing id, wrong entry_id, a status other than `proposed`, or a route other than NULL/`codify`).
>
> **Task A0 also CAPTURES the "before" snapshots — pre-write.** Record and KEEP: (1) the full status distribution, (2) the route-NOT-NULL count, (3) `get_unclassified_entries(conn)` (measured `[]` at authoring — route is orthogonal to classification, so this gate must leave it UNCHANGED; capture it so the post-check compares to the before-value, not a hardcoded `[]`). **Authoring-measured expectation, to be confirmed not assumed:** status distribution `implemented 133, superseded 28, rejected 15, reference 7, proposed 4, stale 3` (total 190); route-NOT-NULL **56**; `get_unclassified` `[]`. Report the ACTUAL; a mismatch is a finding, not a number to adjust to. Do NOT re-read "before" after Task A's write.
>
> **Task A — record the four routes** with `set_proposal_route(conn, proposal_id, 'codify')` from `src.lessons_forge` (NOT hand-written SQL), for **187, 188, 189, 190**. No status is written — leave every `status='proposed'`. **Then a SINGLE `conn.commit()`.**
>
> **Task B — verify the ABSOLUTE post-state.**
> **B1 (primary, resume-invariant):** `SELECT id, entry_id, status, route FROM lesson_proposals WHERE id BETWEEN 187 AND 190 ORDER BY id` — assert every row `route='codify'` AND `status='proposed'` STILL (the trap: a route written but a status accidentally moved is a FAIL). Raw output.
> **B2 (distribution identity):** read the status distribution now and assert it is **byte-identical to the A0 before-snapshot** — `proposed 4, reference 7, implemented 133, superseded 28, rejected 15, stale 3`, total **190**. Any status movement → HALT (this gate must not move a status).
> **B3 (route delta):** route-NOT-NULL rose by **≤4** over the A0 before-count (exactly 4 on a clean run → 60; fewer only on a resume where some were already `codify`).
>
> **Task C — confirm the blast radius.** Measure and report actuals (do NOT hardcode): (1) the Task-B read-back of all four; (2) `SELECT COUNT(*) FROM lesson_proposals WHERE route IS NOT NULL AND id NOT BETWEEN 187 AND 190` equals its **A0 before-value** and is **unchanged** — measured **56** on a clean run (the four targets are route-NULL, so already excluded from the before route-NOT-NULL total; this count does NOT drop by 4). Assert equality to the before-snapshot; any change means a route outside 187–190 moved → HALT. (3) `get_unclassified_entries(conn)` UNCHANGED from the A0 before-snapshot (expected `[]`; a delta signals an unrelated staling → HALT).
>
> **Deposit:** `knowledge/development/gate-1-route-drafting-refinements-2026-07-24.md` — the four `set_proposal_route` calls made, the A0 pre-write snapshots + the after-values proving the route delta and the status identity, and the read-back of all four rows as **RAW command output, not a summary** (qa-evidence-raw-output), plus an Output Receipt. Canonical Python file-write — no heredoc. Commit the deposit. In `### Ledger Updates` include `#### Prompt Feedback`.
>
> **Deposits:**
> - `lessons-forge/knowledge/development/gate-1-route-drafting-refinements-2026-07-24.md`
>
> **STOP. Do NOT proceed to Step 2. Wait for CEO verdict.**

---
---

## STEP 2 — QA

---

> **Before starting, read the Step 1 deposit and confirm Output Receipt status Complete; otherwise halt and report.** Post a short visible chat message confirming you are starting Step 2 (QA). You are Lessons Forge QA. Run commands from your own working tree; canonical DB by ABSOLUTE path is the only exception. **Verification + reporting only — no product-code changes and no DB writes.** If a check fails, report it — do NOT fix it. Do NOT use Monitor. Do NOT edit PROJECT_STATUS directly.
>
> **MANDATORY — Rule 20 self-check (canonical block, Checklist #4 — the exact template with all four placeholders filled, NOT a paraphrase).** Run the canonical Rule 20 self-check from `/Users/marklehn/Developer/GitHub/RULE_20_SELF_CHECK_BLOCK.md` (ABSOLUTE path — the block lives at the governance root, NOT in the lessons-forge worktree; a bare-name read fails there, the same vacuous-submodule trap rows 6/7 guard against). Copy the Python block verbatim and fill ONLY the four placeholders:
> - `plan_slug`: `gate-1-route-drafting-refinements-2026-07-24`
> - `qa_report_path`: `<your-own-tree-abs>/knowledge/qa/gate-1-route-drafting-refinements-qa-2026-07-24.md`
> - `evidence_dir`: `<your-own-tree-abs>/knowledge/qa/evidence/gate-1-route-drafting-refinements-2026-07-24/` — the block runs in YOUR process and checks `os.path.isdir`, so derive this from `pwd` (your OWN working tree), NOT a hardcoded main-tree path (the plan-225 trap).
> - `required_evidence_files`: `[db-invariants.txt, full-suite.txt]`
>
> Deposit both evidence files (below) BEFORE running the block — it `sys.exit(1)`s if either is missing/empty. Include the block's literal stdout in the QA report; the banner `## Rule 20 — QA Self-Check Results` + the `PASSED — SELF-CHECK PASSED` line must appear verbatim (byte-enforced by `bellows/gates.py`). If it prints `FAILED`, halt and report — do NOT close. End with a self-grep confirming the banner.
>
> **Evidence-source rule.** Every SQL row states which DB it ran against (canonical = `sqlite3 "file:/Users/marklehn/Developer/GitHub/lessons-forge/lessons-forge.db?mode=ro" "<query>"`). Deposit **RAW command output, never a summary of it** (memory `qa-evidence-raw-output`).
>
> **Scope:**
> - `knowledge/qa/gate-1-route-drafting-refinements-qa-2026-07-24.md`
> - `knowledge/qa/evidence/gate-1-route-drafting-refinements-2026-07-24/db-invariants.txt`
> - `knowledge/qa/evidence/gate-1-route-drafting-refinements-2026-07-24/full-suite.txt`
>
> Verification table, one row per claim, DB-source column (HALT on any FAIL). **Write the RAW output of rows 1–6 (the DB read-backs + the doctrine-unchanged exit codes) to `db-invariants.txt`, and row 7's `pytest` output to `full-suite.txt`** — these are the two `required_evidence_files` the Rule 20 block verifies (non-empty). The QA report still carries the full table.
> 1. **All four routes applied.** `SELECT id, entry_id, status, route FROM lesson_proposals WHERE id BETWEEN 187 AND 190 ORDER BY id` — raw output. Assert `route='codify'` on all four. Any mismatch → FAIL.
> 2. **All four are STILL `status='proposed'`** (Gate-2-bound; this gate writes route only). Any at a non-`proposed` status → FAIL.
> 3. **Status distribution is byte-identical to the Step-1 A0 before-snapshot** — `proposed 4, reference 7, implemented 133, superseded 28, rejected 15, stale 3`, total **190**. Any movement → FAIL (this gate must not change a status). Before-snapshot missing from Step 1's deposit → halt (unverifiable).
> 4. **Blast radius.** Total proposals still **190**; route-NOT-NULL = **60** (rose by ≤4 over whatever Step-1's A0 route-NOT-NULL before-count recorded — **56 on a clean run, higher on a resume** where some targets were pre-routed; mirror B3, do NOT assert a literal "before-count == 56"); `SELECT COUNT(*) FROM lesson_proposals WHERE route IS NOT NULL AND id NOT BETWEEN 187 AND 190` equals its **Step-1 A0 before-value (56), unchanged** — the four targets were route-NULL, so already excluded from the 56; there is NO −4 delta. Report actuals; any differing → FAIL (unscoped write).
> 5. **`get_unclassified_entries(conn)` UNCHANGED from the Step-1 A0 before-snapshot** (expected `[]`). Route is orthogonal to classification, so this gate cannot change it; a delta signals an unrelated staling → FAIL. Anchor to the before-value, not a hardcoded `[]`, so a pre-existing non-quiescent corpus does not false-FAIL this gate.
> 6. **`DRAFTING_CYCLE.md` + `PLANNER_TEMPLATE.md` + `plan_lint.py` UNCHANGED by this gate.** ⚠️ `lessons-forge` is a submodule; these files are tracked by the ROOT/bellows repos and do NOT exist in your worktree — a plain `git diff` passes VACUOUSLY (**this is proposal 184's class; entry 180/N4 of this batch names it**). Run `git -C /Users/marklehn/Developer/GitHub diff --exit-code -- DRAFTING_CYCLE.md PLANNER_TEMPLATE.md` and `git -C /Users/marklehn/Developer/GitHub/bellows diff --exit-code -- scripts/plan_lint.py`; show all exit codes (0 = pass; any diff = FAIL). Codification is Gate 2.
> 7. **`src/` untouched and suite green.** Check from your **OWN working tree** (in-place or worktree) — do NOT hardcode `-C <main lessons-forge>`, which passes VACUOUSLY on a worktree run (proposal 184's class, named by entry 180/N4 of this batch): `git status --porcelain -- src/` empty (a secondary guard for a lingering *uncommitted* edit; Bellows scope_check is the primary guard against a *committed* src/ change), and `python3 -m pytest src/ -q` passes. Compute the baseline from `--collect-only` and reconcile against the most recent prior QA in `knowledge/qa/` (the 2026-07-24 cycle QA recorded 55 — reconciliation only). Raw tail shown.
>
> If any row fails, report and halt.
>
> **Deposit:** the QA report `knowledge/qa/gate-1-route-drafting-refinements-qa-2026-07-24.md` (verification table with DB-source column, raw query output, the Rule 20 banner + PASSED line + the block's literal stdout, Output Receipt) **plus the two evidence files** (`db-invariants.txt` = rows 1–6 raw; `full-suite.txt` = row 7 pytest tail). Canonical Python file-write — no heredoc. Commit all three. In `### Ledger Updates` include `#### Project Status` (one milestone paragraph: Gate 1 complete for the DRAFTING_CYCLE.md-refinement cycle 2026-07-24 — 4 codify / 0 reference / 0 backlog; all four remain `proposed` and Gate-2-bound; Gate 2 owes the §2.2/§2.5 diagnostic residues [187/N1–N3], the explicit §2 no-batch clause [188/N4], and the two `plan_lint.py`-coupled edits [189/N5 last-lens-status, 190/N6 T0 regex]) and `#### Prompt Feedback`.
>
> **Deposits:**
> - `lessons-forge/knowledge/qa/gate-1-route-drafting-refinements-qa-2026-07-24.md`
> - `lessons-forge/knowledge/qa/evidence/gate-1-route-drafting-refinements-2026-07-24/db-invariants.txt`
> - `lessons-forge/knowledge/qa/evidence/gate-1-route-drafting-refinements-2026-07-24/full-suite.txt`
>
> **Do NOT move this plan to `Done/`.** The close path is owned by Bellows on continue-verdict consumption — never by the agent (a Mode-A `unauthorized_done_move` violation force-recovers the file and FAILs this step's gates).

---

## Drafting Cycle
**Tier:** T2 — triggers fired: T-2 (production-data mutation — writes the `route` column of the canonical lessons-forge corpus, the same DB with a documented silent-corruption history). Structure-clone of Gate 1 258 (route disposition) + the 274 cycle_tier/block format. (T-8 arguable — route-only is a simplification/subset of 258's route+status, not clearly novel — but IMMATERIAL: T-2 fixes the floor at T2 regardless, and the full cold panel is running.) T-6 does NOT fire — this gate sets DB routing metadata and touches no doctrine file (that is Gate 2).
**Walks:** Walk 1 complete (v0 → v4): 6 folds (W1; V1, V2; R1; A1, A2). Walk 2 (confirming) COMPLETE — all 5 lenses dry (v4 stable). Sequential phase done → T2 cold panel.
- Weak spots:          w1 → v1: 1 folded (W1 1.1/1.2 — Task C(2) + QA row 4 mis-derived the outside-range route-NOT-NULL invariant as `52 = 56−4`; correct is **56, unchanged** — the four targets are route-NULL and already excluded from the 56, so the bad number would false-HALT a clean run). Verified clean (no fold): route delta 56→60 (B3); status-distribution identity (B2); precondition mapping + HALT-on-drift; restore-point + isolation logic. **w2 dry** — re-checked all 6 Walk-1 folds hold + introduced no new weak spot (§2.7); category-assertion judged a defensible drift-canary, not a defect; `Tier: Small` vs `cycle_tier: T2` orthogonal.
- Destruction:         w1 dry — clones 258's proven corpus-mutation guards (restore-point `.backup`; scoped `set_proposal_route` WHERE-id write; precondition HALT-on-route-drift). The one dangerous 258 op (hand-SQL `UPDATE … SET status`) is REMOVED (route-only) → smaller harm surface. (2.1) nothing breaks — route NULL→codify is a forward transition, orthogonal to `get_unclassified` (keys on status). (2.2) no guard relaxed vs 258 — QA row 3's full-distribution identity is a GLOBAL status guard subsuming 258's scoped `id<172` status check; W1 tightened the blast-radius check. (2.3) bounded (4 literal ids) + proven ours (precondition verifies the 274 batch) + fully reversible (restore point + revertible metadata); no reset/delete/rewrite. **w2 dry** — no Walk-1 fold relaxed a guard: W1/V1/A1 STRENGTHENED (correct blast-radius anchor / own-tree src-check / right detectors), A2 additive (concurrency guard), V2 correctly SCOPES the staling check to this gate's delta without harmful relaxation (A0 still verifies the 4 targets clean); no fold added a destructive op.
- Vulnerabilities:     w1 → v2: 2 folded (V1 3.1/3.3 — QA row 7's `git -C <main> status -- src/` is worktree-vacuous → OWN-TREE `git status --porcelain -- src/`, scope_check still primary for committed changes; V2 3.4 — `get_unclassified` hardcoded `[]` → anchored to the A0 before-snapshot [before==after], no false-FAIL on a pre-existing non-quiescent corpus). Verified clean: rest of the cross-tree surface closed (DB absolute, row-6 `git -C root/bellows` non-vacuous, RULE_20 block absolute, backup main-tree absolute); guards observe DB-state effects not non-throw; resume idempotent; no cp1252/interpreter risk (local governance write). **w2 dry** — re-ran V1/V2 on their fixes (§2.7): own-tree src-check holds on in-place + worktree-per-step (committed → scope_check, uncommitted → porcelain); `get_unclassified` is `ORDER BY id` so before==after is order-stable; no fold added a vacuous cross-tree ref (A2's row-refs are internal); degenerate ids doubly guarded (precondition HALT + WHERE-id affects 0 rows).
- Integration-record:  w1 → v3: 1 minor folded (R1 4.1 — authoring self-check now names the benign Step-2 QA-runs-pytest plan_lint WARN [memory `benign-gate-failure-classes`; do NOT add a test file to scope] + runs on the FINAL post-cycle draft; deposit-once strengthened to grep-first). Verified: routing dedup re-confirmed (188/N4 not explicit in §2:38 → codify stands, all four hold); memories honored (predicted-number verify-clauses incl. W1, qa-raw-output, deposits-block); §6-aligned (target DRAFTING_CYCLE.md, codify→Gate 2); re-trips nothing (batch-pin is cycle-only; V2 covers corpus-drift); NOT trivial (T2 right-sized — anti-under-escalation); premises hold + A0 runtime-re-verifies. ⚠️ OWED to cold panel: a line-level clause diff vs `executable-258` — warm passes miss clone-drift (memory `cold-panel-catches-clone-drift`; 274's cold panel found 3 dropped 257 clauses). **w2 dry** — R1's benign-WARN claim accurate (actual plan_lint run correctly deferred to §5 execute-against-real-data); A2 matches 274's note (mine broader: "plan" not just "cycle"); the V1 own-tree src-check is an INTENTIONAL improvement on 258's `-C <main>` (traceable when the cold panel diffs vs 258 — not clone-drift). CARRY to §5: run plan_lint on the final draft, confirm §4 passes + only the benign WARN.
- ACID:                w1 → v4: 2 folded (A1 5.3 — A0 parenthetical cited the WRONG staling detector [said "rows 2+6"; row 6 is doctrine-unchanged] → corrected to rows 2/3/5 [the CA3 class from 274]; A2 5.3 — added the dispatch-time concurrency note [no other lessons plan in flight] mirroring 274, since this gate routes non-terminal `proposed` proposals a concurrent ingest could stale). Sound: 5.1 atomicity (single-commit boundary + idempotent resume), 5.2 consistency (each gap has a stated invariant), 5.4 durability (committed DB + restore point; resume-before is ≤4/before-anchored). **w2 dry** — A1/A2 hold; folds cohere as a system (atomicity intact — all folds are reads/docs, one write-txn unchanged; consistency strengthened; isolation gives complete temporal staling coverage [Task C3 intra-Step1 + row 5 Step1→Step2]; durability intact). Ledger C1 holds; no fold re-violates another; resume coheres. **Walk 2 ALL-DRY → sequential phase done.**
**Cold panel (T2):** lens-by-lens, fresh-context reader subagents (each on the prior's folded draft), author-verified. Carries the R1 line-level clause diff vs `executable-258`.
- Cold weak-spots:      → v5: 1 minor (CB1 — restored 258's dropped "`get_unclassified` is NOT the quiescence signal" clarification, now more relevant since V2 added a get_unclassified ref to A0). ⭐ Fresh reader INDEPENDENTLY re-verified the whole numeric backbone vs live DB — **0 mismatches** (187–190 mapping/status/route/targets; route-NOT-NULL 56→60 [dist NULL 134/codify 49/ref 5/backlog 2]; outside-range 56 unchanged — W1's folded value CONFIRMED correct; distribution 190; `set_proposal_route` no self-commit; `get_unclassified` status-keyed=[]; §4 regex; all DRAFTING_CYCLE.md line refs land). Verified NOT drift: the `id NOT BETWEEN` predicate is a correct improvement on 258's `id<172` (MAX(id)=190, nothing above range).
- Cold destruction:     dry (0 folds) — exhaustive guard-by-guard diff vs 258: every fail-closed/HALT/restore-point/precondition/blast-radius clause PRESERVED (many strengthened) or correctly-dropped-with-its-op (the hand-SQL status guards G6/QA-R3, gone with the status change). Top dropped-guard suspect (258's scoped `status='reference' AND id<172`==6) RESOLVED — R3 full-distribution byte-identity strictly SUBSUMES it (no auto-un-stale path → no compensating pair). Numbers re-verified live AGAIN (0 mismatches; W1's 56-unchanged reconfirmed); `set_proposal_route` scoped+reversible; restore-point fail-closed. ⭐ Discharges the guard-diff half of the R1/258 debt.
- Cold vulnerabilities: → v6: 2 folded, both LOW (CV1 F1 — A0 isolation self-signal's worktree behavior rested on an implicit "marker is main-tree" assumption → made explicit [daemon claims in main `decisions/` before any worktree spin]; kept the fail-closed HALT, did NOT soften it; CV2 F2 — QA row 4's "before-count of 56" is clean-run-only → made dynamic like B3 [56 clean / higher on resume], removing a resume false-FAIL for a literal QA agent — the W1 class). ⭐ Full worktree×resume trace: every path correct in BOTH modes (abs DB / abs backup / abs RULE_20 / `git -C` root+bellows non-vacuous / own-tree src); resume sound at all 3 extremes (0/partial/all-4 pre-routed). Numbers re-verified a 3rd time (0 mismatches); no `from config import`-class isolation bypass (DB via `conn`); no cp1252/path-sep (local ASCII write).
- Cold integration:     → v7: 2 folded (CI1 LOW — 188's dedup/Gate-2 note omitted the NEAREST cumulation clauses §2.7:79 + §2.6:73; added the cross-ref + a "extend-don't-compete" Gate-2 instruction per 258's 178/184 convention [codify still stands: those forbid CONCURRENT lenses, N4 forbids BATCHING one un-folded draft]; CI2 INFO — softened "proven-clone→structure-clone", noted T-8 arguable but immaterial [T-2 sets T2]). ⭐ Convention diff vs 258/274: NO convention mis-copied/dropped (the route-only inversion + QA-row re-map are conscious, correct). **DECISIVE dedup proof:** DRAFTING_CYCLE.md is v1.0 with ZERO amendment rows since extraction → no refinement CAN be pre-codified → all 4 `codify` right, none `reference`. plan_lint couplings verified in code (`:201` fold/dry heuristic [189], `:165` `^T([012])$` [190]); taxonomy coherent; §6 aligned; no re-trip. Numbers confirmed live (4th).
- Cold ACID:            dry (0 material folds) — full system analysis: (5.1) all 5 crash points resumable, no double-apply/false-HALT/false-PASS (single-commit rollback confirmed via sqlite3 default isolation; atomicity is defense-in-depth, not load-bearing — the 4 route writes are independent NULL→codify transitions); (5.3) every isolation window guarded, incl. the lock-impossible verdict-gate window — a `proposed` target staling is TRIPLY detected (rows 2/3/5), independently confirmed VALID (each target entry has exactly 1 proposal → staling makes get_unclassified non-empty); (5.4) durable + reconstructable from the `.backup` diff. ⭐ Fold-coherence: CV2/V2/B3 cohere across all 3 completion extremes (0/partial/all-4 pre-routed); no fold conflict; C1 consistent. ⭐ Consistency UPGRADE: status-identity is GUARANTEED not just stated — `set_proposal_route` writes route only + ZERO triggers on lesson_proposals + no route↔status CHECK coupling (all verified live) → mechanically incapable of moving a status.
**Cold panel COMPLETE — 5 cold folds (CB1, CV1, CV2, CI1, CI2; all minor/low/info).** Independent value delivered: the numeric backbone re-verified 4× (0 mismatches every time), the guard-by-guard diff vs 258 (0 dropped guards), the convention diff (0 dropped conventions), the full worktree×resume trace (sound in both modes, all 3 extremes), and the ACID system analysis (clean). Cold panel materially touched the draft → warm confirming **Walk 3** owed before §5.
**Walk 3 (warm confirming, on v7):**
- Weak spots:          → dry. The 5 cold folds introduce no new weak spot + are internally consistent (CB1 refs V2's A0 before-snapshot item 3; CV2 mirrors B3; CI1/CI2 are routing-rationale/Gate-2-guidance with no Step-1/2 exec logic). Scope↔Deposits match in both steps.
- Destruction:         → dry. No cold fold relaxed a guard: CB1 RESTORED a clause, CV1 KEPT the fail-closed HALT (+reinforced "write nothing"), CV2 removed a false-FAIL without weakening the real guard (row 1 still catches an under-write; `≤4` is resume-tolerance not a hole), CI1/CI2 are documentation. Harm surface unchanged (route-only, scoped, reversible); no cold fold added a destructive op.
- Vulnerabilities:     → dry. CV1 is documentation (worst case if its daemon-order assumption were wrong = a fail-closed HALT, not a hole; the HALT is preserved); CV2's dynamic anchor opens no degenerate edge (a route-removal is caught by outside-range=56; the all-4-pre-routed extreme was verified by cold ACID). Cross-tree surface unchanged by the cold folds (no path touched); the cold Vulnerabilities worktree×resume trace stands.
- Integration-record:  → dry. CI1 accurate (§2.7:79 + §2.6:73 verified present; "extend-don't-compete" matches 258's 178/184 convention; reaffirms codify — 188 distinct from the concurrent-lens clauses) + consistent with the routing; CI2 accurate (T-2→T2 regardless of T-8). All 5 cold folds align with record/memories/§6; routing unchanged, re-confirmed decisively (v1.0 doc, 0 amendments).
- ACID:                → dry. All 11 folds cohere as a system: atomicity/isolation/durability untouched by the cold folds; CB1 is CONSISTENT with A2/row5 (get_unclassified is not the A0 quiescence signal but IS the row-5 before/after staling detector — different roles); CV2/V2/B3 coherence verified by cold ACID; C1 holds. No fold conflict.
**Conflicts:** none (§2.8 ledger: C1 — outside-range route-NOT-NULL count is 56 and must stay unchanged; the four targets were NULL, no −4; C1 re-confirmed consistent by every cold reader).
**§5 Mechanical conformance:** plan_lint EXECUTED on the final draft — exit 0, (a)-(d) PASS, §4 Drafting-Cycle check passes NON-vacuously (cycle_tier T2 recognized; all 5 lens regexes + cold-panel line matched; closing not a bare-fold), only the benign QA-tests WARN (R1 confirmed by execution). Rules 1–58 + Checklist 1–33 walked by scope — all satisfied/N-A except two findings: **(A) Checklist #3 (STOP-prose)** flagged but TOLERATED (both proven clones 258/274, authored AFTER #3's 2026-05-27 source, carry it; pause_for_verdict governs pauses; plan_lint doesn't enforce it; kept for clone-fidelity — inert). **(B) Checklist #4 + Rule 20** — QA step paraphrased instead of filling the four placeholders → **fold M1 → v8:** filled plan_slug/qa_report_path/evidence_dir/required_evidence_files + added 2 evidence files (db-invariants.txt, full-suite.txt) with own-tree evidence_dir (plan-225), matching 274's proven full form; plan_lint re-run PASS (step 2 now 3 deposits/scope). ⭐ Live corroboration for the routed proposals: my closing passes §4 ONLY because 'dry' co-occurs with 'fold' (189/N5), and bare `T2` passes the regex where the §3 T0 form wouldn't (190/N6).
**M1 confirming pass (v8):** re-checked the M1 change through all five lenses — dry (evidence-file flow matches 274's Rule-20 pattern: deposit files → write report → run block → include stdout; own-tree evidence_dir worktree-safe; strengthens verification, relaxes nothing; no Step-1 write touched).
**Closing:** last event before deposit = the dry M1 confirming pass (a lens pass, not a fold). **Adversarial phase COMPLETE:** Walk 1 (6) → Walk 2 (dry) → cold panel (5; numbers re-verified 4×) → Walk 3 (dry) → §5 (M1) → M1 confirming (dry). **12 folds, 8 revisions (v0→v8).** Ready to deposit once (pending CEO approval); the closing dry lens pass stands.
