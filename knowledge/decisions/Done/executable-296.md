# Lessons Forge — Cycle Run 2026-08-03 (ingest + classify the 16-entry session-16/17 batch)
**Date:** 2026-08-03 | **Tier:** Small | **Dispatch Mode:** bellows | **Test Scope:** targeted | **Execution:** Step 1 (Lessons Agent — ingest + classify all 16) → Step 2 (DEV — report) → Step 3 (QA) | **qa_steps:** 3 | **pause_for_verdict:** always
**cycle_tier:** T2

## CEO Context

Cycle run only: ingest the 16 un-ingested `LESSONS.md` entries (nine dated 2026-08-01 from session 16's plan-289 cycle, seven dated 2026-08-03 from session 17's plans 291–295) and classify them into proposals. Gate 1 (route disposition) and Gate 2 (codification) are separate plans with CEO decisions between.

**This is the largest batch the cycle has ever run — 16, against a prior maximum of 8 (plan 283) and a most-recent 6 (plan 288).** The mechanism is unchanged; the scale is not, and the places where scale actually bites are named individually below rather than left to a general caution.

⚠️ **`Test Scope: targeted` — carried from 288/287/284, and the justification is re-verified here rather than inherited.** Measured this session: `find . -name "test_*.py"` returns exactly ONE file, `src/test_lessons_forge.py`, so `python3 -m pytest src/` is simultaneously the targeted run and the full run — the same command over the same tests. Declaring `full-suite` would mandate two evidence files (`pytest_targeted.txt` AND `pytest_full.txt`) for two identical executions. Rule 21 (`### 21.` at `:604`) requires a written justification for `targeted`, and this is it. The contract-change carve-out does NOT fire — this plan changes no code at all; its only writes are a DB mutation and markdown deposits. **`--collect-only` measured 55 tests at authoring** — report the actual.

⚠️ **TRACKING, continued from 288 (CEO, 2026-07-31): `targeted` on a single-module repo is a precedent under observation.** What would falsify it: a defect reaching `Done/` that a broader run would have caught. **288's cycle reported 55; this plan's row 1 reports its own count, and the pair is what a later session compares.** This is the fourth data point.

**Clone lineage.** Direct clone of **288** (the newest same-class cycle plan); its lineage is 283 → 281 → 274 → 257 → 247. Per entry 183's rule — *clone against the NEWEST same-class plan* — this draft ALSO diffs against **291** (the newest plan of any class on this corpus), whose cold panel hardened machinery 288 does not carry.

---

### ⚠️⚠️ SEVEN INHERITED FACTS FROM 288 ARE FALSE HERE

Every one would false-HALT, false-PASS, or silently mis-scope if cloned unchanged. Each is measured, not reasoned.

**1. THE ANCHORS MOVED. `E0 = 198`, `P0 = 206`** (288: 192 / 200). Verified 2026-08-03: `sqlite_sequence` == `MAX(id)` for both tables (`lesson_entries=198`, `lesson_proposals=206`), **no gap** → inserts land at **entries 199–214 / proposals 207–222**. G6 still derives the range arithmetically rather than trusting these literals.

**2. TWO OF THE THREE DOCTRINE PINS MOVED, AND THE THIRD DID NOT — THAT IS THE TRAP.** Measured with `shasum -a 256` against the live working tree at authoring:

| file | pin | vs 288 |
|---|---|---|
| `DRAFTING_CYCLE.md` (v1.3) | `2d5cf9ab7c3a87ed825a1e1edcafea9a9b2598f30b29a7e48868558874a18ee0` | **MOVED** (288: `3951bcf8…`, v1.2) |
| `PLANNER_TEMPLATE.md` (v4.82) | `e8289d50f28711fdbf7c5319d812c229d3e7d2e255fc77f7ce868c19c01b6783` | **MOVED** (288: `0c53222f…`, v4.81) |
| `RULE_20_SELF_CHECK_BLOCK.md` (no version line) | `3accbce0c8d2b44586edc6c5f95582f775d0bce14276966b7ca90724e0ca5644` | **UNCHANGED** |

⚠️ **A clone that carried 288's three pins forward would diff two of them and match the third.** The match is what makes it dangerous: a partial-failure pattern reads as "the pins are being checked" rather than as "the pins are stale." Plan 291 moved the first two four days ago; `RULE_20_SELF_CHECK_BLOCK.md` has not moved since session 14 — **`3accbce0…` is a correct inherited value and is the only one of the three that is.**

**3. THE HASH-TRAP SENTINEL IS ENTRY 198, NOT 192.** `content_hash` = `28e19e1b7dc460f37f49c4d35ec52150e96e01ee6cc718aa4f4a30e18d906fc7`, heading `2026-07-30: The final step's gate span absorbs the Drafting Cycle block…`. It is the highest-id entry — the one an append most recently sat behind, i.e. where a trailing-separator hash flip would land if the plan-204 `_normalize_for_hash` fix ever regressed. **A regression sentinel, not a proposal guardian.** Named by id, so `MAX(id)` moving under a resume cannot silently retarget it.

**4. THE TAG SET IS FIVE VALUES, NOT TWO — AND THREE OF THEM HAVE NO CATEGORY PRECEDENT IN THE CORPUS.** Measured over the 16 parsed entries: `bellows-integration` **7**, `planner-discipline` **5**, `drafting-cycle` **2**, `verification` **1**, `process-discipline` **1**. Every prior cycle partitioned on two tags. Measured against the whole corpus's `lesson_entries.tags`:
- **`drafting-cycle` — ZERO prior entries.** No precedent exists.
- **`verification` — ZERO prior entries.** No precedent exists.
- **`process-discipline` — ONE prior entry** (id 134, *"Live-canary every daemon-write activation"*), classified **`instrumentation`**. That entry is the direct parent of this batch's canary entry.

⚠️ **So QA row 3's category bound CANNOT be a uniform tag→category map, and widening it is a measurement, not a relaxation** — see row 3 for the exact three-part form. **Do NOT assert `governance_rule` uniformly, and do NOT invent a bound for a tag the corpus has never classified.**

**5. `duplicates` ALREADY EXIST — 288'S RESUME ASSERTION IS FALSE BY CONSTRUCTION AND IS NOT CLONED.** 288's G3 resume branch reads *"assert no proposal in the corpus carries `category='duplicate'`."* **Measured 2026-08-03: `DUP_COUNT=19`.** That assertion would HALT every resume of this plan. This is the defect 288's own live run surfaced and its cycle recorded as candidate 9 — *every reviewer reasoned about what it would check; only the agent ran it.* **G3's resume clause here is scoped to this cycle's own entry ids and stated as a delta, not a whole-corpus predicate** (ledger C9 — the constraint 288 opened and then violated).

**6. THE BACKUP GLOB POPULATION IS SEVEN, NOT SIX.** `data/backups/lessons-forge-pre-cycle-*.db` matches **7** files at authoring (2026-07-20, 20260721, 20260723, 20260724, 20260728, `-283-`, `-288-`). **288 states six; that figure was correct when 288 was written and is now stale — which is exactly why this plan re-counts rather than inherits, and why the count is not the guard.** The guard is the id token: this cycle's backup is `lessons-forge-pre-cycle-296-<UTC-stamp>.db` and any resume glob matches on `-296-`.

**7. THE EM-DASH ASYMMETRY IS NINE-OF-SIXTEEN, NOT ONE-OF-SIX.** `detect_duplicates` extracts a heading's descriptive title with `split(_EM_DASH_SEP, 1)[1]` on a literal SPACE-EM-DASH-SPACE (`src/lessons_forge.py:294`, constant read at authoring), falling back to the **whole heading including its date prefix** when the separator is absent. **Measured: 7 of the 16 headings contain the separator (entries 199, 200, 202, 203, 205, 206, 207); 9 do NOT.** For those nine the detector tests the entire dated heading, which is far less likely to match. **Report that asymmetry rather than a uniform "no hits."**

---

### ⚠️⚠️ NUMBERING — FOUR NAMESPACES COLLIDE, AND TWO OF THEM ARE THIS PLAN'S OWN

This is sharper than 288's three-way collision and every downstream assertion must name its namespace.

- **`lesson_entries.id` 199–214** — THIS batch's 16 entries (after ingest).
- **`lesson_proposals.id` 207–222** — THIS batch's 16 proposals (after classification).
- **`lesson_proposals.id` 199–206** — PRE-EXISTING proposals, all terminal (`implemented`), codified by plans 287 and 291. **The numerals 199–206 therefore name both a NEW entry and an OLD proposal.** Leave the proposals untouched.
- **⚠️⚠️ THE NUMERALS 207–214 NAME BOTH A NEW ENTRY AND A NEW PROPOSAL — BOTH THIS PLAN'S OWN, AND THEY ARE NOT PAIRED.** The pairing is `entry 199+k → proposal 207+k`, so **entry 207 pairs with proposal 215**, not with proposal 207. A bare "207" is ambiguous between *the true-warning-silenced entry* and *the proposal created for the count-is-not-a-value-guard entry*. **288 had no equivalent of this and there is no inherited guard for it.**

**Never write a bare number in 199–222 without its namespace.** File-position counts are a fifth namespace: `parse_lessons_md` sees **157** `##` entries in `LESSONS.md`, while the corpus row count is **198** — the surplus is orphan rows from reworded or removed headings, all classified, which is why `get_unclassified_entries()` is `[]` pre-cycle. ⚠️ **Measured: `LESSONS.md` contains NO `## Archived` heading, so the parser's archived-stop branch never fires on this file.** **157 and 198 are both correct and neither is the other's baseline.**

---

### ⚠️ Preconditions measured at authoring (2026-08-03), read-only against live canonical

| what | measured | where it is re-checked at run time |
|---|---|---|
| non-terminal set `NT` | **EMPTY** — `NT_COUNT=0` | G1 (pre-ingest, HALTs) |
| `stale` count | **3** (proposals 98, 121, 130) | G1 (`STALE_BASE`) |
| whole-corpus dry run through `parse_lessons_md` | **16 would-INGEST / 0 would-UPDATE / 141 unchanged**, over 157 parsed entries | Step 1a-bis (pre-ingest, HALTs) |
| `E0` / `P0` | 198 / 206, `sqlite_sequence` agreement, no gap | Step 1a |
| entry-198 sentinel hash | `28e19e1b…` | Step 1a-bis item 2 |
| `LESSONS.md` provenance | porcelain **EMPTY**, `PORCELAIN-EXIT=0` | G2 — **HALT on non-empty or non-zero exit** |
| root HEAD | `b4b7bad` — ⚠️ **RECONCILE-NOTE ONLY, NEVER A HALT, and it is EXPECTED TO DIFFER** | G2 — see below |
| `detect_duplicates` path (a) | id list length **141**, **0 hits** | Step 1a-bis item 2b(a) |
| `detect_duplicates` path (b) | **0 / 16** substring hits; tag criterion **inert** (reference file has 0 `**Tag:**` / `**Tags:**` lines) | Step 1a-bis item 2b(b) |
| reference-file positive control | `PLANNER_TEMPLATE.md` read at the absolute root path: **373,176 bytes**, sentinel `Orchestration Plan Rules` **present** | Step 1a-bis positive control |
| collected tests | **55** | QA row 1 |
| `reports/lessons-report-2026-08-03.md` | **does not exist** | Step 2 pre-check |
| deposited plans of this class | **NONE** — `knowledge/decisions/` holds only `Done/`; no `in-progress-*` anywhere | deposit-once discipline |

⚠️ **Every figure above is the Planner's measurement, not a gate value** (Checklist #29, `:1329`): confirm each against your own read and HALT on a mismatch rather than adopting the literal — **except the root-HEAD row, which is reconcile-only.**

⚠️⚠️ **THE ROOT-HEAD ROW IS THE ONE ENTRY IN THIS TABLE EXPECTED TO BE WRONG BY DISPATCH TIME, AND AN EARLIER VERSION LISTED IT BESIDE THE HALT-BEARING ROWS WITH NO DISTINCTION.** G2's own text has always said a HEAD mismatch is a reconcile-note; **the table did not, and a reader consulting the table rather than G2 would halt a correct run.** ⚠️ **The mismatch is not merely possible but near-certain: depositing this plan commits to the `lessons-forge` submodule, and the root pointer bump moves root HEAD before this plan is ever dispatched.** Record the delta, name it, continue.

**⚠️ G1 precondition.** The ingest's update path stales **non-terminal** proposals; the plan-204 `_TERMINAL_STATUSES` guard (`src/lessons_forge.py:31`, read at authoring — `{implemented, rejected, superseded, reference}`) protects only the terminal set. With `NT` empty the ingest is non-destructive **by construction** on a fresh run — there is nothing staleable — **and it remains so precisely as long as no entry's `content_hash` changes.** Any non-terminal proposal with `entry_id ≤ E0` present at Step 1a voids the premise → HALT before the ingest.

**⚠️⚠️ G4 DETECTS HASH-TRAP DAMAGE; IT CANNOT PREVENT IT — hence the pre-ingest hash guard (Step 1a-bis).** Verified in code at authoring: the staling `UPDATE lesson_proposals SET status='stale' … WHERE entry_id = ? AND status != 'stale' AND status NOT IN (<terminal>)` sits at `src/lessons_forge.py:187-193`, **inside the hash-changed branch of the ingest itself**. By the time `run_full_lessons_cycle` returns and G4 reads `updated_count`, any staling is already written. **And the instrument that would measure it is discarded:** `ingest_lesson_entries` computes `stale_proposals_marked` (`:135`) but `run_full_lessons_cycle` **omits that key from its return dict** (`:503-511`, which does retain `terminal_proposals_flagged`). ⚠️ **With `NT` empty the BLAST RADIUS is zero on this corpus — but the DETECTOR is retained**, because a non-zero `updated_count` also means the file and the corpus disagree about an entry's content, which is a defect in its own right.

**⚠️⚠️ THE `NT` TRIM — inherited from 288, re-verified rather than re-inherited.** With `NT` empty, 288's already-trimmed `NT-original`/`NT-now` reconstruction ladder stays trimmed. **KEPT:** a single pre-ingest assertion that the non-terminal set is empty; non-empty → HALT before the mutation, reporting the ids. **THE PREMISE IS VERIFIED, NOT ASSUMED** — `NT_COUNT=0` was measured against live canonical this session, and **G1 re-verifies it at run time, so if the premise is false this step HALTs instead of proceeding without the machinery.** ⚠️ **Rule 62 (`:1125`, shipped by 291 from this lineage) is the governing rule and it is satisfied in the direction it actually requires:** the state the removed ladder recovers from (a populated `NT` staled by our own ingest) is measured unreachable, while the state the *retained* resume ladder recovers from (a mid-step death) is reachable under the actual dispatch path. **Those two calls are deliberately asymmetric and the reasoning is recorded so a lens can attack either.**

---

### The 16 entries — placement scout

**The governing rule is Rule 58 (`:1093`), "Pre-stated conclusions require verification anchors and equal evidence burden."** ⚠️ **Deliberately carries no Rule 27 citation** — Rule 27 (`:824`) is *"Diagnostic-derived plans cite findings, never supplement with source reads"* and does not govern `target_artifact` placement; this plan has no preceding diagnostic.

**Rule 58(2), stated explicitly:** ⚠️ **This table records where the Planner looked, not a distribution.** No claim is made about any entry not listed; a placement absent from it is not a placement rejected; the three artifacts named do not bound the answer — a fourth, well-reasoned target is a legitimate outcome. **Rule 58(3):** every disposition carries the same evidence burden — agreeing with the scout is not the low-effort path.

| # | entry (expected id) | substance | scouted `target_artifact` |
|---|---|---|---|
| 1 | **entry 199** | a COUNT over a scope cannot see a row moving between two non-NULL values; a trim replacing a value assertion with a count is a capability loss | `DRAFTING_CYCLE.md` **§2.7** (subtractive-trim verification) — the Family line names the 2026-07-25 subtractive-trim lesson, which §2.7 already carries. ⚠️ **Expect a PARTIAL-OVERLAP dedup, not a clean-slate placement** |
| 2 | **entry 200** | an `INHERITED` marker makes a claim honest, not true — the reason for not re-running is itself an unverified claim | `DRAFTING_CYCLE.md` **§2.7** (lens attestation integrity) — ⚠️ **alternative worth weighing: `PLANNER_TEMPLATE.md` Rule 52 (`:1053`), "Re-verify inherited claims before dispositions and routing decisions"**, which is the same subject named almost verbatim. The Family line points at the attestation lesson; the rule text points at Rule 52. Gate 2 owns the choice |
| 3 | **entry 201** | non-arrival has three candidate causes and only the DELIVERY code distinguishes them | `PLANNER_TEMPLATE.md` — ⚠️ **no existing rule is a clean fit; this reads as a NEW rule near 55/62.** Alternative: Checklist #32 (`:1347`, observe a behaviour change through the real entry point), which covers the confirm-the-channel half but not the diagnose-before-remediating half |
| 4 | **entry 202** | falling severity across walks is not convergence; rotation to an unexamined region is | `DRAFTING_CYCLE.md` **§2.6** (review-target rotation) **AND §2's doneness sentence** — ⚠️ **the conflict is with §2's PARENTHETICAL GLOSS only, not its criterion; see flag (A), which an earlier draft over-scoped** |
| 5 | **entry 203** | an annotated status cell is neither passing nor failing — both gates ignore it | `RULE_20_SELF_CHECK_BLOCK.md` (the block's `is_positive_row` cell-equality contract) — ⚠️ **alternative: `PLANNER_TEMPLATE.md` Rule 19 (`:560`).** ⚠️ **The entry proposes a lint ("every status cell is exactly one of the two permitted tokens") — a tooling REQUEST; flag the coupling for Gate 1** |
| 6 | **entry 204** | a guard's exit-code semantics must be executed against the failure it NAMES; `-F` is mandatory and a non-`-F` search is invalid evidence regardless of exit code | `DRAFTING_CYCLE.md` **§2.7** (the command-output rule) — ⚠️ **NAMESPACE TRAP: §2.7's command-output clause was shipped by PROPOSAL 204 (plan 291). This is ENTRY 204. They are different objects that happen to share a numeral and land on the same section** |
| 7 | **entry 205** | the edit phase manufactures defects even when the edit is a REPAIR, and the Conflict Ledger records that rather than preventing it | `DRAFTING_CYCLE.md` **§2.8** (the ledger's own limits) — ⚠️ **alternative: §2.7's "re-run the finding lens on its own fix".** The entry's third clause is an argument for MECHANIZING a constraint rather than writing it down, which is a Forward-Register-shaped consequence rather than a doctrine edit |
| 8 | **entry 206** | aim the cold panel at the premises that LICENSE a deletion | `DRAFTING_CYCLE.md` **§2.6** (cold panel) — a targeting rule appended to the existing clone-against-newest guidance. Clean placement |
| 9 | **entry 207** | a true warning silenced by wording is not a cleared warning; re-run the check after every edit to the block it reads | `DRAFTING_CYCLE.md` **§3** — ⚠️⚠️ **PARTIALLY CODIFIED ALREADY, MEASURED CLAUSE BY CLAUSE IN FLAG (B): all three core claims are present; three sub-claims are absent** (earned-phrasing is necessary-but-not-sufficient; the trigger is EVERY edit to the block the check reads, not only compaction; the prohibition applies reflexively). ⚠️ **A narrow EXTENSION, not `reference`** — the residue is codified nowhere |
| 10 | **entry 208** | the cold panel's yield does not decay; a third of each round is the previous round's folds | `DRAFTING_CYCLE.md` **§2.6** — ⚠️ **and it lands on the SAME §2 parenthetical entry 202 does.** **Gate 2 must merge 202 and 208 rather than apply them independently**, or the second edit will re-open the sentence the first just settled |
| 11 | **entry 209** | seven folds on one region is evidence for deletion; count folds per REGION, not per plan | `DRAFTING_CYCLE.md` **§2.8 *and* §2.6 — THIS ENTRY SPLITS ACROSS TWO SECTIONS**, measured in flag (B). §2.8 already carries both of its core claims; the un-codified residue is **(a) count folds per REGION not per plan — §2.8 never says how to count — and (b) check whether a shipped sibling already DELETED the machinery, which belongs in §2.6 and is the INVERSE of the question §2.6 currently asks.** ⚠️ **A Gate-2 plan filing this wholly into §2.8 drops half of it** |
| 12 | **entry 210** | a check must assert on the POST-condition, never on the presence of the thing being changed; a scripted edit asserts `after != before` | `PLANNER_TEMPLATE.md` **Checklist #32** (`:1347`) — the observed-delta rule, of which this is the verification-instrument form. ⚠️ **alternative: `DRAFTING_CYCLE.md` §2.7** |
| 13 | **entry 211** | a note-shaped verification row cannot live in a glyph-required table | `PLANNER_TEMPLATE.md` **Rule 17** (`:554`, which fixes the `Status (✅/❌)` spelling) — ⚠️ **SIBLING OF ENTRY 203: both are about what the status column can hold.** Gate 2 should consider merging. ⚠️ **This entry carries NO `**Family:**` line** |
| 14 | **entry 212** | a command containing `\|` cannot be quoted verbatim inside a markdown table cell — escaping it changes the semantics | `PLANNER_TEMPLATE.md` **Rule 18** (`:557`, QA evidence file requirement) — ⚠️ **alternative: a new rule; also a sibling of 211.** ⚠️ **This plan's OWN Step 1 Receipt format and QA row 3 both encode this rule already** (bare values, no `\|`, disposition lines confined to the dev log) |
| 15 | **entry 213** | daemon liveness is `ps -p` against a recorded PID; `pgrep` exit 1 is indistinguishable from "not running" | `PLANNER_TEMPLATE.md` **Rule 55(a)** (`:1075`) — ⚠️ **the strongest fit in this table, and stated as such because Rule 58(3) makes an under-argued AGREEMENT the same defect as an under-argued divergence.** Rule 55(a) is *"assert on a positive signal… never merely empty output, which absence also produces"*, argued there over **repo/tree state**; entry 213 is that identical thesis over **process state** — `pgrep` exit 1 conflates "no match" with "not running", so every failure mode presents as the answer the caller feared. **Gate 2 should codify it as an EXTENSION of 55(a) into a second domain, not as a new rule.** ⚠️ **This entry carries NO `**Family:**` line** |
| 16 | **entry 214** | a post-activation live canary can be paid for by the backlog it records; pair the live observation with an in-process prediction | `PLANNER_TEMPLATE.md` **Checklist #32** (`:1347`) — **named by the entry itself** (*"the constructive form of the Checklist #32 / Workaround #15 discipline"*). ⚠️ **Category precedent: its parent entry 134, same tag, was classified `instrumentation`** |

**⚠️⚠️ A THIRD-ARTIFACT CLUSTER GATE 2 MUST SEE AS ONE — entries 203, 211 and 212 all govern what may occupy a CELL of the QA verification table, and the scout sends them to three different homes.** 203 (an annotated status cell escapes both gates) → `RULE_20_SELF_CHECK_BLOCK.md`; 211 (a note-shaped row cannot live in a glyph-required table) → Rule 17; 212 (a command containing a pipe cannot be quoted in a cell) → Rule 18. ⚠️ **212's scouted home is the weakest of the three — Rule 18 governs the Evidence column citing a file BY PATH, not the construction of cells — and it is named first only because no better existing rule was found.** ⚠️⚠️ **Codified separately, ONE artifact's cell contract ends up specified in three places, which is the precondition every drift finding in this corpus starts from. Gate 2 should weigh a single consolidated cell-contract clause against three surgical edits.** **Naming the cluster is this plan's job; choosing the shape is Gate 2's.** ⚠️ **This plan's own Step 3 already encodes all three rules (C11) — one glyph per status cell, no pipe-bearing command in a cell, no note-shaped row — which is evidence the three belong together in practice, not merely in theory.**

**⚠️⚠️ TWO STANDING FLAGS FOR GATE 1, both named so they are decided deliberately rather than discovered mid-disposition.**

**(A) Entries 202 and 208 conflict with ONE PARENTHETICAL IN §2 — not with its criterion. ⚠️ An earlier draft of this section called it "the same class as the proposal-202 / §2.8 collision the CEO ruled on in session 17," and that framing was WRONG and is struck.** The CEO corrected it at authoring: *"we would consider a plan ready when it returns dry or minor issues. Falling counts are indications of moving in the right direction but it's not a signal of doneness."*

§2 reads: *"The cycle is **done** when a full walk returns zero or only-minor findings (that IS the diminishing-returns signal)."* **The main clause is a threshold on a walk's RESULT, and both entries agree with it.** The defect is the parenthetical gloss, which equates that absolute criterion with a falling-curve reading — **and it is exactly the falling-curve reading that entries 202 and 208 falsify with measurements** (202: a thinnest-so-far walk followed by four MEDIUMs in unexamined regions; 208: a five-round panel returning 11/12/12/12/12 with no decay at all).

**So the amendment is surgical, not contradictory: replace the parenthetical, and add entry 202's rotation qualifier — a dry walk over ALREADY-EXAMINED ground is not the signal either; the signal is a dry walk aimed at a region the previous round did not touch.** §2.6 already carries review-target rotation as a practice; what neither section carries is rotation as the *doneness* condition. ⚠️ **This is NOT a §2.8 escalation** — the two entries and §2's criterion are consistent; only the gloss conflicts. Named for Gate 1 so the routing is made with the correct, narrower scope in view.

**(B) Entries 207 and 209 are BOTH PARTIALLY CODIFIED ALREADY — measured at authoring, clause by clause, with `grep -F` and exit codes captured.** ⚠️ **Neither is a duplicate in the `detect_duplicates` sense** — that detector scans `PLANNER_TEMPLATE.md` only and returned 0 hits on both paths. **The overlap is against `DRAFTING_CYCLE.md`, which the detector never reads**, so nothing in the pipeline would have surfaced this. Gate 1 dedups against live doctrine; here is the measurement rather than a pointer to where to look.

**Entry 207 — its three core claims are ALL present in §3 (shipped by plan 291 from proposal 206). Three sub-claims are NOT:**

| entry 207 claim | in live §3? |
|---|---|
| re-run the gate and confirm the WARN/PASS set is unchanged | **PRESENT** |
| a WARN that disappears is the signature of the defect | **PRESENT** |
| describe gate-matching strings, never quote them | **PRESENT** |
| phrasing a line so it cannot match *until earned* is **necessary but not sufficient** | **ABSENT** |
| the trigger is **every edit to the block the check reads**, not only compaction | **ABSENT** — §3 scopes it to "after compacting or editing the log" |
| the prohibition applies **reflexively**, in the sentence that warns against quoting | **ABSENT** |

**Entry 209 — its two core claims are present in §2.8; FIVE sub-claims are not, and one of them belongs in a DIFFERENT SECTION:**

| entry 209 claim | in live doctrine? |
|---|---|
| three-or-more re-folds is the point at which to ask | **PRESENT** (§2.8) |
| every-patch-correct is evidence FOR deletion | **PRESENT** (§2.8) |
| **count folds per REGION, not per plan — a per-plan count hides this entirely** | **ABSENT** — §2.8 says "the same region has been re-folded repeatedly" but never says how to COUNT. ⚠️ **This is the mechanizable half and the highest-value residue in either entry** |
| ask what a version with **no branches** would look like | **ABSENT** — §2.8 says only "whether the record or the runtime already supplies a simpler method" |
| **check whether a shipped sibling already DELETED the same machinery** | **ABSENT — and it targets §2.6, not §2.8.** ⚠️⚠️ **§2.6 asks the INVERSE question: *"ask what the clone dropped or mis-adapted."* Entry 209 asks what the clone RE-ADDED that a parent deliberately removed. Those are different failure directions, and the session-17 record is the proof the current wording does not catch the second: §2.6 was live, and the clone re-added the deleted subsystem anyway** |

⚠️ **Consequence for Gate 1 and Gate 2, stated so neither has to re-derive it:** these are **extension routings, not clean-slate codifications, and entry 209 SPLITS ACROSS TWO SECTIONS.** A Gate-2 plan that files 209 wholly into §2.8 would drop its sibling-check half onto the floor. ⚠️ **And a `reference` routing on either entry would discard residue that is not codified anywhere** — the partial overlap is a reason to scope the codification, not to skip it.

**⚠️ Rule 46 (`:1012`) — does it fire on any entry?** Rule 46 directs Gate 1 to REJECT a *"Planner-side workaround for a daemon bug"* and route the bug to `FORWARD.md` instead. **The Planner's reading: it does NOT fire on any of the 16, and the two closest calls are named so Gate 1 can disagree.** Entry 211 (a gate that cannot express a note-shaped row) and entry 203 (a gate-invisible status cell) each describe real gate mechanics — but both entries' "how to apply" is an authoring rule about what a plan may write, not an avoidance of a defect that should be fixed. **Entry 203 additionally proposes a lint, which is the opposite of a workaround.** Gate 1 decides.

**Cluster synthesis for Gate 1:** *"16 entries from the session-16/17 cycles — 5 planner-discipline authoring refinements, 7 bellows-integration mechanics findings, 2 drafting-cycle measurements, 1 verification-instrument finding, 1 process-discipline canary pattern; MIXED targets across ≥3 artifacts; TWO entries (207, 209) MEASURED as partially codified already by plan 291, both routing as scoped extensions rather than `reference`, with 209 splitting across two sections; ONE surgical §2 amendment (entries 202/208 vs the doneness sentence's parenthetical gloss, NOT its criterion) that Gate 2 must apply as a single merged edit; and THREE tag values the corpus has never classified before."* Do NOT skip or downgrade any.

**Do NOT dedup against `DRAFTING_CYCLE.md`, `PLANNER_TEMPLATE.md`, or `RULE_20_SELF_CHECK_BLOCK.md`.** Gate 1 dedups against live doctrine.

---

### ⚠️ THE SCALE QUESTION — named for the CEO, with the Planner's default stated

**16 classifications in one step is 2× the largest batch this mechanism has run.** The risk is not correctness — `get_unclassified_entries` is authoritative and a resume classifies only the remainder — it is **quality decay on the tail**: 288's QA row 9 called itself *"the row most likely to surface a thin classification from a saturated tail"* at **six**.

**The Planner's default, taken here: ONE classification step, with row 9 hardened.** Reasoning: the ingest is a single transaction, `get_unclassified_entries` already provides the natural resume boundary, and splitting classification across a verdict gate would put two steps in sequence each holding the canonical write handle, widening the isolation window the plan's own concurrency guards exist to narrow. **Row 9 is strengthened accordingly** — all 16 longest-match lengths reported as numbers, and the FLOOR is raised to a per-proposal minimum rather than a set-level pass (see row 9).

**✅ CEO DECISION TAKEN (2026-08-03): ONE STEP.** The alternative — splitting Step 1 into 1a (ingest + classify entries 199–206) and 1b (classify entries 207–214) with a verdict gate between — **was offered and declined.** ⚠️ **Recorded as settled rather than left standing as an open question, because an un-struck alternative reads to a later author as a live fork and invites re-litigation of a decision already made.** The mitigation that carries the risk is QA row 9's per-proposal floor, not the step structure.

---

### Residual risk register

- **Best verified — the measured baseline.** Every number this plan asserts was produced this session by running the real code against live canonical, read-only: the **16 / 0 / 141** whole-corpus dry run through `parse_lessons_md` itself, the empty non-terminal set, `E0=198`/`P0=206` with `sqlite_sequence` agreement, entry 198's hash, `stale`=3, `DUP_COUNT=19`, **55** collected tests, the three doctrine shasums, the 5-value tag distribution and its corpus precedent, the em-dash asymmetry, and both `detect_duplicates` paths with their reference-file positive control.
- **Least verified — the failure-path scaffolding.** The resume ladder and the dispatch-state probe are inherited from 288 and **have never executed on this corpus.** ⚠️ **Rule 62 (`:1125`) is the standing objection and it is answered, not waived:** the ladder is retained because a mid-step death IS reachable under the actual dispatch path (a session-cap exit and a daemon kill are both observed in this shop's record), whereas the `NT` protection ladder was removed because its state is measured unreachable. A defect remaining in the resume path costs a dispatch, not the corpus — every branch halts rather than mutates, and DO-NOT-REPAIR plus the verified restore point bound the blast radius.
- **⚠️ Explicitly NOT verified.** Whether the 16 scouted `target_artifact` placements are correct — that is Gate 1/Gate 2's question, and rows 2, 3, 5, 7, 12 and 14 are flagged as genuinely two-answered. Whether the single-step classification holds quality across 16 entries — **row 9 measures it; nothing prevents it.** ⚠️ **The 207/209 dedup is NO LONGER on this list: it was measured clause by clause at authoring (flag B) rather than left as an open question, because `detect_duplicates` structurally cannot see it — that detector reads `PLANNER_TEMPLATE.md` only, and both overlaps are in `DRAFTING_CYCLE.md`.**
- **Declared exception to C5** — QA row 7 fails closed on an in-window CEO doctrine edit; the `❌` is an escalation channel, not a verdict.

**Scope discipline:** cycle run only. Routes stay `NULL` at insert. **Do NOT edit `DRAFTING_CYCLE.md`, `PLANNER_TEMPLATE.md`, `RULE_20_SELF_CHECK_BLOCK.md`, `bellows/scripts/plan_lint.py`, or `bellows/gates.py`.** **Do NOT touch proposals 199–206** (plans 287/291's codified set — note the numeral overlap with THIS batch's entry ids). **⚠️ Do NOT append to `LESSONS.md` while this plan is deposited-but-un-run** — the cycle parser-pins its batch at 16 (memory `lessons-forge-cycle-pins-batch-by-parser-count`).

**⚠️ Concurrency — dispatch with NO other lessons-forge cycle in flight.** This cycle's 16 proposals insert `status='proposed'`; a concurrent whole-corpus ingest between Step 1 and Step 3 could stale all sixteen. **With `NT` empty at baseline, this cycle's own proposals are the ONLY staleable rows in the corpus — the concurrency window protects them and nothing else.** DETECTION is QA row 2 + row 3's status clause + row 4.

**No diagnostic precedes this plan, deliberately** (247/257/274/281/283/288 practice): every unknown was measured inline against live data at authoring — see the preconditions table. **✅ `LESSONS.md` is ALREADY committed and porcelain-clean** — all 16 entries landed in root `b4b7bad` (the session-17 wrap), which is also current HEAD.

**Deposit-once discipline:** to be deposited exactly once (memory `bellows-deposit-once-discipline`; `knowledge/decisions/` was grepped for an existing plan of this class before authoring and holds only `Done/`). **Authoring self-check:** `plan_lint.py` RUN at authoring against draft v1. ⚠️ **A clean `plan_lint` exit is NOT evidence the §4 block ran** — the block is warn-only and exits 0 regardless; confirm the §4 lines appeared in stdout rather than inferring from the exit code. **That is entry 204's lesson applied to this plan's own self-check.** **Measured: exit 0, ten PASS lines, TWO WARNs — and both are CORRECT:**
1. **The known-benign one** — step 3 mentions tests while declaring no test scope; the QA-runs-pytest-without-modifying-tests class (memory `benign-gate-failure-classes`). **Do NOT add test files to Step 3's scope to silence it.**
2. **T2 plan missing its panel line** — **CORRECT: this plan has not run its cold panel.** ⚠️ **It fires only because the panel status line was re-phrased after the linter proved the canonical phrasing satisfied the check while declaring the panel ABSENT.** See the Drafting Cycle block for the measurement.

⚠️⚠️ **WARN 2 must NOT be "fixed" by re-wording. It is the plan telling the truth about itself.** It clears when — and only when — the panel actually runs. **If it goes quiet while that condition is still unmet, the wording has drifted back into suppressing it and that is a defect, not progress.**

⚠️⚠️ **AND A THIRD WARN THAT DID NOT FIRE IS ITSELF A MEASUREMENT: §4's closing check is SILENT on a plan that has had ZERO lens passes.** The check WARNs iff the last lens result line carries a fold-token and lacks a genuine dry-token; a line reading *"not yet run"* carries neither, and the `**Closing:**` presence check is satisfied by a Closing line whose content says the condition was never reached. **So on this draft §4 reports nothing about the fact that no lens has ever run.** This is a known recorded gap, re-measured here rather than asserted. ⚠️ **What it means for anyone reading this plan's linter output: a quiet §4 is NOT evidence the cycle closed, and NOT evidence a panel ran.** Read the Cycle Log's lens lines and the panel status line directly.

## How to Run This Plan

Bellows dispatches this plan automatically when deposited; no manual bootstrap required. (`PLANNER_TEMPLATE.md:886`, Rule 35 — for `dispatch_mode: bellows` this section *"either is omitted or replaced with a short note."*)

---
---

## STEP 1 — Lessons Agent (ingest whole corpus + classify all 16)

---

> **FIRST — post a short visible chat message (1-2 sentences) confirming you are starting this plan and your immediate next action.** Do NOT rename the plan file.
>
> You are the Forge Lessons Agent. Read `agents/FORGE_LESSONS_AGENT.md` first (ADR-002 six-value taxonomy). **Its DB paths are relative to the GitHub root and you may run in a worktree** — so **every canonical-DB access uses the ABSOLUTE path** `/Users/marklehn/Developer/GitHub/lessons-forge/lessons-forge.db`. **`forge/forge.db` is a REAL but DIFFERENT database — never open it.**
>
> **Working location — the plan-225 trap.** Run commands from **your own working tree** and write every file there. **The ONLY exception is canonical-DB access, by the ABSOLUTE path above.** Do NOT `cd` to the main tree: report/deposit writes resolve RELATIVE paths, so a main-tree cwd puts output in main while you commit in the worktree.
>
> **⚠️ EXECUTION ORDER — run these in exactly this sequence. The gate definitions are documented AFTER Step 1b for readability, but two of them run BEFORE it; do not execute this step in reading order.**
> 0. **DETERMINE DISPATCH STATE — do this FIRST; four checks in Step 1a branch on it and none can be evaluated until it is settled.** ⚠️ **C8 APPLIES TO ALL THREE PROBES.** `git show HEAD:<path>` prints nothing to stdout and exits non-zero **both** when the path is genuinely absent and when the invocation failed (a mistyped `-C`, a bad ref). **Capture and report each probe's exit code**; a FRESH determination read from silence alone is not a determination. ⚠️ **Probe THREE places, not one — a hard death routes the record somewhere the obvious probe cannot see.** If a prior dispatch died without teardown, `_create_worktree` moves its unmerged commits to a **`bellows-preserved/<slug>-<ts>`** branch and rebuilds the worktree from `HEAD`, so the stub is committed but neither `HEAD:` nor the working tree shows it. Check: (i) `git -C <your worktree> show HEAD:knowledge/development/dev-log-cycle-step-1-2026-08-03.md`; (ii) the working tree; (iii) `git -C <worktree> log --all --oneline -- knowledge/development/dev-log-cycle-step-1-2026-08-03.md` **and** `git -C /Users/marklehn/Developer/GitHub/lessons-forge branch --list 'bellows-preserved/*'`. **A hit on ANY of the three is a RESUME** — recover the stub with `git show <ref>:<path>`. **Absent from all three → FRESH DISPATCH.** On a RESUME its recorded values — `E0`, `P0`, `NT`, the pristine backup path, the sentinel hash, the `stale` baseline, and any recorded ingest dict — are **authoritative for the whole step** and override anything you re-measure now. State the determination and its evidence as the first line of your dev log.
> 1. **Step 1a** — restore point, verify it, capture the baseline + `E0`/`P0` + the `NT` set.
> 2. **Step 1a-ter** — write + `git commit` the pre-ingest anchor stub. ⚠️ **The durability fix; NOT optional and NOT skippable.** ⚠️⚠️ **BUT IT IS CONDITIONAL — READ THE FILE BEFORE YOU WRITE IT.** If `dev-log-cycle-step-1-2026-08-03.md` already exists and its opening line is a PROCEED-value (`Status: Complete` / `Status: Complete (idempotent re-dispatch …)`), **do NOT write a stub at all** — leave the file untouched and proceed to G5(a), which owns that case. ⚠️ **But CONFIRM THE WORK IS ACTUALLY THERE FROM THE DB FIRST, not from the file's status line** — `SELECT COUNT(*) FROM lesson_proposals WHERE entry_id > 198` must be **16** and `get_unclassified_entries()` must be `[]`. **If either differs, do NOT take the G5(a) shortcut** — fall through to the normal path (append-only to the existing file). ⚠️⚠️ **ONE DEVIATION HAS ITS OWN DESTINATION AND MUST NOT BE SWEPT INTO "fall through": a count of 17 or more WITH at least one `category='duplicate'` row among `entry_id > 198` is G3's state, not this one.** A prior dispatch's duplicate detector inserting a proposal produces exactly that, and G3 is written to HALT on it because the affected entry is then silently excluded from `get_unclassified_entries` and never classified. **So run the scoped duplicate count HERE, before choosing a branch** — `SELECT 'DUP_IN_BATCH=' || COUNT(*) FROM lesson_proposals WHERE category='duplicate' AND entry_id > 198` — **and if it is non-zero, go to G3 and HALT rather than re-ingesting.** ⚠️ **Without this the two instructions send the same corpus state to opposite destinations**, and the re-ingest path would run over a corpus carrying an unclassifiable entry. A `Status: Complete` receipt describes a corpus that may since have been restored from the pristine backup, in which case the work is gone while the record still claims it landed. If the file exists with a stop-value, **preserve every recorded value verbatim and only append.** ⚠️ **An unconditional whole-file write here would destroy a completed Receipt before G5(a)'s do-not-clobber rule is ever evaluated at position 7.**
> 3. **Step 1a-bis** — pre-ingest parent-hash guard (read-only), and the `detect_duplicates` pre-check.
> 4. **G2** then **G1** — both pre-ingest. **G1 is the last thing before the mutation; if it HALTs, the ingest never runs.**
> 5. **Step 1b** — the ingest (the only mutation), then `conn.commit()`, then append the returned dict to the stub and commit it again.
> 6. **G3, G4, G5, G6** — these read the Step-1b dict and are post-mutation detectors.
> 7. **Classification** → `conn.commit()` after EACH `insert_proposal` (a DB commit) → write the two deposits → `git commit` them ONCE by explicit pathspec. ⚠️ **The two commit verbs are named separately because they are different operations at different frequencies: sixteen DB commits, one git commit.** An earlier wording read "per-proposal commit", which reads as sixteen git commits inside a worktree.
>
> **Single-writer assumption.** ⚠️ **C8 applies: `ls <glob>` prints nothing both when there is no match and when the path is wrong, so capture the exit code and report the literal count.** ⚠️ **And note what a ZERO means under bellows dispatch: this plan's own file SHOULD be present** (the daemon renames it with an uncommitted `shutil.move` in the MAIN tree), so a zero match from the main-tree glob is itself evidence the glob is wrong, not evidence of no concurrency. Before Step 1a, confirm no concurrent cycle: (a) `get_unclassified_entries` stable across two reads a moment apart, **and** (b) no other in-progress plan. ⚠️ **Glob `in-progress-*.md` in the MAIN TREE by absolute path — `ls /Users/marklehn/Developer/GitHub/lessons-forge/knowledge/decisions/in-progress-*.md` — NOT your worktree's copy**, which is a frozen HEAD snapshot in which a cycle claimed after your worktree existed could never appear. ⚠️ **ZERO matches is a NORMAL, NON-HALTING result**; one match — this plan's own file — is equally fine. **For any OTHER match, read its title line and HALT if it is a lessons/cycle plan.**
>
> **⚠️ HALT DURABILITY — applies to every HALT in this step.** This step holds the only write handle to the canonical corpus. **Before you stop, commit whatever deposit files exist by EXPLICIT PATHSPEC** (`git add <exact paths>` then `git commit -m "…" -- <exact paths>`; never `-a`, never unscoped `add -A`). Record which gate halted, its measured value, and whether the ingest had already committed.
>
> **⚠️ DO NOT REPAIR. You hold the write handle.** If a gate fails, report it — do not "fix" the corpus, do not re-run the ingest to get a different number, do not adjust a proposal to make a count match. The only authorized writes are: the `.backup`, `run_full_lessons_cycle`, `insert_proposal` for entries `get_unclassified_entries` returns, and this step's two deposit files.
>
> **Scope:**
> - `knowledge/development/classifications-cycle-2026-08-03.md`
> - `knowledge/development/dev-log-cycle-step-1-2026-08-03.md`
>
> **Step 1a — restore point, then baseline.** Back up canonical with `.backup` (NOT `cp` — a live WAL exists), to the MAIN tree by ABSOLUTE path. **Build the path in a shell variable first** (verified-working form):
> ```
> mkdir -p /Users/marklehn/Developer/GitHub/lessons-forge/data/backups
> BK="/Users/marklehn/Developer/GitHub/lessons-forge/data/backups/lessons-forge-pre-cycle-296-$(date -u +%Y%m%dT%H%M%SZ).db"
> sqlite3 "file:/Users/marklehn/Developer/GitHub/lessons-forge/lessons-forge.db?mode=ro" ".backup '$BK'"
> ```
> ⚠️ Do NOT inline `$(date …)` between single-quoted parts of the `.backup` argument — sqlite3 MISPARSES that and errors with NO backup written. **Use the COMPACT UTC stamp.** `.gitignore` matches `*.db` — confirm absent in `git -C /Users/marklehn/Developer/GitHub/lessons-forge status --porcelain`.
>
> **Then VERIFY the restore point is real, not ceremony.** ⚠️ **What this backup protects is narrower than a mid-arc cycle's, and saying so honestly is the point:** with the non-terminal set empty there are no pre-existing proposals to stale, so it is **not** guarding parked proposals. **It is guarding against a botched or partial ingest of this cycle's own sixteen entries** — which, at sixteen, is the largest single-transaction corpus mutation this project has run. Verify: `sqlite3 '<backup-path>' 'PRAGMA integrity_check;'` must return `ok`, AND the backup's `COUNT(*)` for both tables must equal **the LIVE DB's counts at backup time**. ⚠️ Match to the live DB **NOW**, not a hardcoded number: a fresh run should see 198 entries / 206 proposals; **on a RESUME the live DB is already mutated (≈214/≈222) and the fresh `.backup` correctly snapshots that — do NOT assert 198/206 or you false-HALT every resume.** Any failure → HALT before the ingest.
>
> ⚠️ **READING A BACKUP: use `?immutable=1`, not `?mode=ro`.** `.backup` writes the snapshot file ALONE; it is **this plan's own mandated `PRAGMA integrity_check` — a read-WRITE open — that creates** the `-shm`/`-wal` companions beside it. **So the sidecars are an artifact of our verification, not of the backup.** With the sidecars present all three open forms work; **if they are absent or have been cleaned up, `?mode=ro` FAILS on the WAL header while `?immutable=1` succeeds.** `?immutable=1` is correct in both states and is the form this plan mandates. ⚠️ **Do not "simplify" it back to `?mode=ro`:** the two behave identically in the common case and differ only in the failure case, which is precisely when you are reading a backup.
>
> ⚠️⚠️ **THE GLOB MUST BE CYCLE-UNIQUE — a bare `lessons-forge-pre-cycle-*.db` is NOT, and it fails UNSAFE.** `data/backups/` **already contains SEVEN files matching that pattern** — re-counted at authoring: 2026-07-20, 20260721, 20260723, 20260724, 20260728, `-283-`, `-288-`. ⚠️ **Plan 288 states "six"; that figure was correct when 288 was written and is now stale, which is exactly why this plan re-counts rather than inherits — and why the COUNT is not the guard.** The guard is the id token: **the backup filename carries this plan's id — `lessons-forge-pre-cycle-296-<UTC-stamp>.db` — and any resume glob matches on the `-296-` token.** ⚠️ **And PROVE the file is this cycle's pristine snapshot before accepting it:** against that file, `SELECT MAX(id) FROM lesson_entries` must be **198** and `SELECT MAX(id) FROM lesson_proposals` must be **206** (this plan's own `E0`/`P0`; the `-288-` snapshot returns 192/200 and is thereby positively distinguishable, the `-283-` snapshot 184/192). Anything else → do NOT use it. ⚠️ **Take the EARLIEST `-296-` match — end the glob in `.db`**, since `sqlite3` errors on a `-wal` sidecar. NOT the directory's overall earliest. **Derive the date from the ACTUAL backup filename or the receipt, never from the local dispatch date** (`date -u` late in the local day stamps the next date — memory `resume-glob-utc-date-vs-local`). Prefer the exact path recorded in the Step-1 Receipt (**item 7**).
>
> **Then capture the baseline** (read-only), verbatim: proposals by `status`, proposals by `category`, total `lesson_entries`, and the id + `content_hash` of **the hash-trap target — entry 198 (`28e19e1b7dc460f37f49c4d35ec52150e96e01ee6cc718aa4f4a30e18d906fc7`), named directly and NOT derived from `MAX(id)`.** ⚠️ **Confirm it against your own baseline read and report the actual; do not copy this literal forward** (Checklist #29, `:1329`). A mismatch is a HALT, not a correction. ⚠️ **Also capture the `stale` count — Planner's measured pre-cycle value: 3** (proposals 98, 121, 130) — as its own labelled line; QA row 4 reads it and fails closed without it.
>
> ⚠️ **Why ONE named entry here.** **Entry 198 is the highest-id entry, i.e. the one an append most recently sat behind, which is where the trailing-separator hash flip would land if the plan-204 `_normalize_for_hash` fix ever regressed.** It is a regression sentinel, not a proposal guardian. Naming it by id keeps the check honest if `MAX(id)` moves under a resume.
>
> **Also capture `E0 = MAX(id) FROM lesson_entries` and `P0 = MAX(id) FROM lesson_proposals`. Confirm `E0 = 198`, `P0 = 206`; if either differs on a fresh run, HALT. ⚠️ But do NOT halt with the wrong diagnosis:** a "fresh" determination that finds `E0 = 214` almost certainly means **a prior dispatch's ingest DID land and its record is on a `bellows-preserved/*` branch** (step 0, probe iii). **Search those branches for the stub before reporting the first-dispatch ingest dict as lost** — that dict is the only unreproducible value in this plan.
>
> **⚠️ Additionally capture THE NON-TERMINAL SET — one authoritative query, selected by STATUS PREDICATE, never by hardcoded id.** This is G1's operand and the before-anchor Step 2 and QA row 5 read:
> ```sql
> SELECT p.id, p.entry_id, p.status, p.route, p.target_artifact,
>        e.source_heading, e.content_hash
> FROM lesson_proposals p JOIN lesson_entries e ON p.entry_id = e.id
> WHERE p.status IN ('proposed','accepted','ambiguous')
> ORDER BY p.id;
> ```
> Label it **`NT`** and deposit it as RAW output. **Planner's measured expectation on a FRESH run: ZERO rows.** ⚠️ **An empty `NT` is this cycle's EXPECTED and CORRECT baseline.** **Write the label and record it as empty; do not omit the item** — a missing `NT` label and an empty `NT` are different findings, and QA row 5 fails closed if it cannot find the label.
>
> ⚠️⚠️ **AND THIS IS THE ONE PLACE THE COMMAND'S OUTPUT IS AMBIGUOUS — entry 204's lesson, applied to the query this step depends on most.** `sqlite3` prints **nothing** for a zero-row result, and it also prints nothing if the query never ran, if the shell quoting swallowed it, or if you opened the wrong file. **Empty stdout is therefore NOT evidence of an empty set.** **Assert a positive signal** (Rule 55, `:1075`) — run the count form as well and record the printed number:
> ```
> sqlite3 "file:/Users/marklehn/Developer/GitHub/lessons-forge/lessons-forge.db?mode=ro" \
>   "SELECT 'NT_COUNT=' || COUNT(*) FROM lesson_proposals WHERE status IN ('proposed','accepted','ambiguous');"
> ```
> **That command prints a non-empty token on success in BOTH the empty and non-empty cases**, so silence now unambiguously means failure. Record the literal `NT_COUNT=<n>` line. ⚠️ **If it prints nothing at all, HALT** — you have a broken invocation, not an empty corpus.
>
> **⚠️ CAPTURE ONLY HERE — DO NOT ADJUDICATE. G1 owns the verdict, and it is BRANCHED.** Record `NT_COUNT`, `STALE_COUNT` and the raw query output, and move on. ⚠️⚠️ **An unconditional "`NT_COUNT` must be 0 → else HALT" at this position would be WRONG and would destroy every resume path in this plan:** on any resume this cycle's own 16 proposals are `proposed`, so `NT_COUNT` is legitimately **16**, and a flat assertion here — which executes at position 1, *before* the gate table at position 4 — HALTs pre-ingest on a completely healthy resume. **The expected value is 0 on a FRESH dispatch only.** **Report the numbers; let G1 judge them.**
>
> **⚠️⚠️ WHY THERE IS NO `NT-original` / `NT-now` / RECONSTRUCTION LADDER HERE, declared because plan 283 carries one and a silent omission is indistinguishable from clone-drift.** That ladder exists to protect **pre-existing non-terminal proposals from being staled**. On this corpus there are none, measured. **Every one of those branches would operate over an empty set, and an all-empty comparison PASSES vacuously** — which is worse than absent, because it reports green having examined nothing. ⚠️ **Rule 62 (`:1125`) governs and is satisfied in its own terms: the state the ladder recovers FROM is measured unreachable under the actual dispatch path.** ⚠️ **The premise is verified, not assumed** — the predicate above returned zero rows against live canonical at authoring — **and G1 re-verifies it at run time, so if the premise is false this step HALTs instead of proceeding without the machinery.** That is the whole safety argument for the removal: the trim is guarded by the very check that would detect its premise failing.
>
> **⚠️ Why this step carries resume machinery at all — Rule 56's required ask, answered.** Rule 56 (`:1081`) requires that **before** building resume machinery the Planner ask whether the work could be regenerated from a recipe the plan already carries, and prefer **restore-and-redo** if so. **Asked and answered: restore-and-redo was considered and rejected.** A verified pristine restore point exists, so a blind re-run is technically available — but Rule 56's own caveat is that *"restore is only safe on your own dirt,"* and this step **cannot prove sole ownership** (its single-writer check is explicitly non-halting on zero matches), so a restore could silently discard a concurrent writer's work. Rule 56 also names committed classifications that cannot be blindly re-run without manufacturing duplicates as its own carve-out — exactly what `get_unclassified_entries` guards. ⚠️ **And Rule 62's prior question is answered separately rather than folded into this one: a mid-step death IS reachable under the actual dispatch path** (a session-cap exit and a daemon kill are both in this shop's record), **which is the fact that licenses the machinery — not Rule 56's shape argument.** **The machinery stays, and these two paragraphs are why.**
>
> **⚠️ Step 1a-ter — COMMIT THE BEFORE-ANCHOR BEFORE THE INGEST. This is the durability fix; not bookkeeping.** Every anchor this plan depends on otherwise lives only in the Output Receipt, written at the END of the step — so in exactly the resume states that matter (ingest committed, step died before deposit) **none of it exists**, and the plan is reduced to filesystem archaeology over backup filenames.
>
> Immediately after the restore point is verified and **before** `run_full_lessons_cycle` runs, write and **`git commit`** a stub `knowledge/development/dev-log-cycle-step-1-2026-08-03.md` containing:
> - `Status: Partial — in flight (pre-ingest stub; superseded by the final Receipt)` ⚠️ **This value must NEVER satisfy a downstream precondition** — Steps 2 and 3 proceed ONLY on an enumerated Complete value.
> - the **absolute pristine backup path**
> - `E0`, `P0`
> - the raw `NT` capture **plus the printed `NT_COUNT=` line** (**never overwrite an existing stub's `NT`**)
> - the `stale` baseline count (`STALE_COUNT=`)
> - the entry-198 sentinel hash
> - ⚠️⚠️ **the three DOCTRINE PINS — and THIS IS THE ONLY PLACE THEY ARE EVER MEASURED.** Run `shasum -a 256` on `/Users/marklehn/Developer/GitHub/{DRAFTING_CYCLE.md,PLANNER_TEMPLATE.md,RULE_20_SELF_CHECK_BLOCK.md}` by absolute root path, and write the raw output into this stub. **G2 does NOT re-measure them and Receipt item 10 does NOT re-measure them — both cite this capture.** ⚠️ **HALT if fewer than three hashes are produced.** `shasum` on a missing or unreadable file prints nothing to stdout and exits non-zero, which is byte-identical on stdout to a value you failed to capture (C8): **run it once, count the output lines, and HALT unless all three are present with the expected filenames.** A stub carrying blank or partial pins is worse than one carrying none — it hands QA row 7b an operand that looks present.
>
>   ⚠️⚠️ **WHY ONE CAPTURE POINT AND NOT THREE — DECLARED, BECAUSE AN EARLIER DRAFT OF THIS PLAN HAD THREE AND THEY PRODUCED THREE SEPARATE DEFECTS FROM A SINGLE FOLD.** The pins were being taken at this position, recorded again at G2, and deposited at item 10, and the result was: **(i)** the resume path regenerated every other anchor and not these, **(ii)** item 10 still read *"as recorded at G2"* while the stub held its own copy, with no rule saying which won on a fresh run, and **(iii)** the new capture had no failure path. **Every individual patch would have been correct; the region kept producing defects because it carried three sources of truth for one fact.** ⚠️ **Collapsed to one — measured here, cited everywhere — which closes all three at once and makes the resume rule trivial: there is only one thing to recover.** **This is entry 209 of the batch this plan ingests, applied to this plan** (*seven folds on one region is not a patching problem, it is evidence for deletion*), and §2.7's subtractive-trim rule is satisfied per item: G2's re-measurement bought a seconds-wide drift signal no branch acted on, and item 10's re-measurement bought nothing at all.
>
> A later resume reads this file instead of guessing from filenames, and the final Receipt overwrites it in place. ⚠️⚠️ **THE OVERWRITE MUST PRESERVE THE FIRST-DISPATCH INGEST DICT — otherwise it destroys the durability fix it supersedes.** The final Receipt rewrites this whole file, so a resume that re-runs the ingest (and correctly gets `updated_count == 0`, because the entries are already ingested) would replace a FIRST-dispatch dict recording `updated_count == 1` with its own all-zero one, and QA row 4 would positively report a clean ingest that never happened. **If the stub carries a recorded ingest dict, the final Receipt MUST carry it forward verbatim under `#### First-dispatch ingest dict`.** **If the stub already exists on a resume, its recorded values are authoritative over anything you re-measure now.**
>
> **Step 1a-bis — PRE-INGEST hash guard (preventive; do not skip as boilerplate).** The ingest's update path writes inside the same call that updates a parent entry, so every post-ingest gate is a detector, not a guard. Before calling `run_full_lessons_cycle`, do this **read-only**:
> 1. From your working tree's repo root: `import sys; sys.path.insert(0, "src")` then `from lessons_forge import parse_lessons_md`; `entries = parse_lessons_md("/Users/marklehn/Developer/GitHub/LESSONS.md")` — the same parser `run_full_lessons_cycle` calls, so hashes are computed identically. **Verified by execution at authoring:** the parser returns dicts keyed `content_hash`, `entry_date`, `raw_content`, `source_heading`, `tags` over **157** file entries.
>
>    ⚠️⚠️ **WHILE YOU HOLD ALL 157 PARSED ENTRIES, TALLY THE WHOLE-CORPUS DRY RUN — three more lines, and it converts G3/G4/G5 from post-mutation DETECTORS into pre-mutation GUARDS for the whole corpus** — ⚠️ **but ONLY up to the interval between this check and `run_full_lessons_cycle`, which re-reads BOTH `LESSONS.md` and (via `cat`) `PLANNER_TEMPLATE.md` afresh. G3/G4/G5 remain the backstop for anything that changes inside that window**, and this plan cannot prove sole ownership. For every parsed entry, look up `(source_file='LESSONS.md', source_heading)` in `lesson_entries` and count: **`would_insert`** (no row), **`would_update`** (row exists, `content_hash` differs), **`unchanged`**. ⚠️⚠️ **BRANCH THIS ASSERT ON THE STEP-0 DISPATCH DETERMINATION — an unconditional `would_insert == 16` makes EVERY RESUME PATH IN THIS PLAN UNREACHABLE.** A landed ingest puts all 157 parsed headings into `lesson_entries`, and the ingest's lookup key is `(source_file, source_heading)` — so on any resume `would_insert` is **arithmetically certain to be 0**, not merely likely. An unconditional assert therefore HALTs pre-mutation on every resume, silently deleting G1's resume path, G5(a)'s idempotent re-dispatch, G5(b)'s deposit-completion path, G6's RESUME subset branch, and the entire Step-1a-ter durability architecture. So:
>    - **FRESH dispatch → assert `would_insert == 16` AND `would_update == 0`.** Any deviation → HALT pre-mutation.
>    - **RESUME → assert `would_update == 0`** (still the staling guard, valid on every dispatch) **and `would_insert ∈ {0, 16}`.** ⚠️ **Anything in `1..15` is a HALT** — the ingest is one transaction, so a partially-landed insert set is impossible from this plan and signals a foreign writer.
>
>    Report the actuals either way, naming any entries responsible. **This is the same measurement the Planner made at authoring — `16 / 0 / 141` over 157 parsed entries** — and the recipe is handed to you rather than only its result. ⚠️ **It matters because a commit to root `LESSONS.md` between authoring and dispatch is explicitly PERMITTED** (G2 treats a root-HEAD delta as a reconcile-note): a 17th appended lesson would otherwise be discovered only by G5, *after* `run_full_lessons_cycle` had already written it.
> 2. **The hash-trap sentinel — entry 198.** Find the parsed entry whose `source_heading` equals entry 198's, and compare its computed `content_hash` against the stored `28e19e1b…`. Adjudicate on the number of file matches:
>    - **exactly 1 match, hash EQUAL** → PASS. *(Measured at authoring.)*
>    - **exactly 1 match, hash DIFFERENT** → **HALT BEFORE THE INGEST.** Report both hashes and whether the difference is whitespace-only — a whitespace-only delta means the plan-204 `_normalize_for_hash` fix has regressed, which is a code defect, not a corpus defect. Do not "fix" `LESSONS.md`, and do not proceed to see what G4 says.
>    - **ZERO matches** → **HALT.** The sentinel is specifically `MAX(id)` — the most recently ingested entry — **so it cannot legitimately be an orphan**, and its absence from the file means the heading was edited between authoring and dispatch.
>    - **MORE than 1 match** → **HALT.** Two file entries sharing one heading is a corruption signal (the lookup would be ambiguous).
>
>    ⚠️ **Why this is one entry and not a loop over `NT`.** With `NT` empty there is no set of parent entries to protect. **A loop over an empty `NT` here would print nothing and pass — the vacuous-green shape this plan removes elsewhere for the same reason.**
> 2b. ⚠️ **ALSO pre-check the duplicate detector — it reads a file the CEO is expected to edit, and that read produces WRITES.** `run_full_lessons_cycle` calls `detect_duplicates`, which defaults its reference file to `/Users/marklehn/Developer/GitHub/PLANNER_TEMPLATE.md` (read via `subprocess.run(["cat", …])` — `src/lessons_forge.py:297-345`, read at authoring) and, on a hit, **INSERTS a `duplicate` proposal with `status='proposed'`**. Its idempotency guard is only `WHERE entry_id = ? AND category = 'duplicate'`, so an entry already carrying a real proposal is **not** skipped. ⚠️ **So a Gate-2 edit to `PLANNER_TEMPLATE.md` is NOT neutral for this cycle — and Gate 2 for the PREVIOUS batch shipped on 2026-08-02, moving that exact file to v4.82.** ⚠️⚠️ **SCOPE THIS HONESTLY — the pre-check CANNOT cover this cycle's own 16 entries.** `detect_duplicates(conn, entry_ids, reference_files=None)` takes **ids, not headings**, and resolves each via `SELECT source_heading, tags FROM lesson_entries WHERE id = ?`, **skipping ids that return no row**. **Pre-ingest, entries 199–214 do not exist and have no ids** — so calling the detector now can only ever cover ids ≤ 198. For the batch, G3 remains a **post-mutation detector**; nothing converts it.
>
> So do BOTH, and report them separately:
> - **(a) Pre-existing ids — run the detector directly, scoped to the ids the ingest ACTUALLY passes it:** `run_full_lessons_cycle` builds `candidate_ids` only from entries whose `source_heading` matches a **parsed file** heading (**141 today, not all 198**), so the **57** orphan DB rows are never handed to the detector. **Mirror that construction** rather than sweeping `1..E0`. Run `detect_duplicates(conn, <the parsed-and-matched ids>)` read-only. **Non-empty → HALT.** ⚠️⚠️ **PRINT THE LENGTH OF THE ID LIST BEFORE YOU CALL IT, and HALT if it is 0 or wildly off the Planner's measured 141.** The function's FIRST statement is an empty-list early return (`:331-332`), **ahead of the reference-file read** — so an empty or mis-built id list returns `[]` ("no duplicates") having examined nothing, **and the reference-file positive control would still be perfectly green**, since the file is fine and was simply never consulted. **The control proves the file was read; only the printed list length proves there was anything to check it against.**
> - **(b) This cycle's 16 entries — replicate the detector's logic read-only against the PARSED entries**, since they have no ids yet. ⚠️ **Read the reference file at the ABSOLUTE path `/Users/marklehn/Developer/GitHub/PLANNER_TEMPLATE.md`** — never a relative one; that file is in the ROOT repo and is absent from your worktree entirely, so a relative read yields nothing and "nothing" is exactly what a clean result looks like. ⚠️ **And derive the replication from the CURRENT source of `detect_duplicates`, not from this plan's description of it** — the code is authoritative. ⚠️ **Apply BOTH criteria, and note the code's own numbering:** **criterion 1 is TAG OVERLAP** (each comma-split tag against the tag set extracted from `**Tag:**`/`**Tags:**` lines in the reference file) and **criterion 2 is the HEADING SUBSTRING** match; the tag check runs FIRST and short-circuits. ⚠️ **The parsed tags carry literal backticks** (measured: all sixteen `tags` values are backtick-wrapped, e.g. `` `bellows-integration` ``), the same trap that voids an equality check elsewhere in this plan. **Any hit → HALT** — a lesson you are about to ingest already exists in doctrine, which is a genuine classification finding.
>
>    ⚠️⚠️ **THE HEADING-TITLE EXTRACTION SPLITS ON A LITERAL SPACE-EM-DASH-SPACE (`_EM_DASH_SEP`, `src/lessons_forge.py:294`), WITH A FALLBACK TO THE WHOLE HEADING WHEN THAT SEPARATOR IS ABSENT — and on this batch the fallback is the MAJORITY case.** Measured at authoring: **7 of the 16 headings contain the separator (entries 199, 200, 202, 203, 205, 206, 207); NINE do NOT** (entries 201, 204, 208, 209, 210, 211, 212, 213, 214). For those nine the detector tests the entire heading *including its date prefix*, which is far less likely to match. **Record that asymmetry rather than reporting a uniform "no hits".** ⚠️ **Plan 288 reported this as one-of-six; do not carry that ratio.**
>
> ⚠️⚠️ **BEFORE TRUSTING ANY "ZERO HITS" RESULT ON EITHER PATH — PROVE THE REFERENCE FILE WAS ACTUALLY READ. A failed read is INDISTINGUISHABLE from a clean check, and the failure is silent by construction.** Read at authoring: the reference read is `subprocess.run(["cat", ref_path], …, check=True)` wrapped in `except (subprocess.CalledProcessError, FileNotFoundError): continue`, followed by **`if not ref_contents: return []`**. **So if the reference file cannot be read, `detect_duplicates` returns an EMPTY LIST — "no duplicates found" — having scanned nothing**, `duplicates_marked_count` is 0, and **G3 PASSES on a detector that never ran.** ⚠️ **This is reachable in a way that matters here:** the default path is an ABSOLUTE path into the ROOT repo, and **`PLANNER_TEMPLATE.md` does not exist in the `lessons-forge` repo at all.**
>
> **So, as a POSITIVE CONTROL on both paths (Rule 55, `:1075`) — do this BEFORE reporting any count:** read the reference file yourself **by the absolute root path**, and from **that one read, held in memory**, record (i) its **byte length** and (ii) a **known-present sentinel** — the literal string `Orchestration Plan Rules`. ⚠️ **Planner's measured values at authoring: 373,176 bytes; sentinel PRESENT.**
>
> ⚠️⚠️ **BOTH FACTS MUST COME FROM THE SAME READ, AND AN EARLIER WORDING TOOK THEM FROM TWO.** It recorded the byte length from the agent's own read and then delegated the sentinel to a separate `grep` process. **A `grep` that succeeds proves the file exists on disk; it does NOT prove the read that feeds the check succeeded** — and "prove the reference file was ACTUALLY READ" is this control's entire stated purpose. **Two independent reads cannot discharge a single-read claim.** So: search the in-memory string for the sentinel and report both numbers from it. ⚠️ **If you additionally shell out for cross-confirmation, `grep -F` is mandatory** — per entry 204 of the batch you are ingesting, **a search not run with `-F` is invalid evidence regardless of its exit code**: on this machine `grep` is a ugrep shim and a `**`-bearing pattern run without `-F` exits **1, silently, on a file where the searched line is present.** **But that shell check is corroboration, never the control.**
>
> **A zero byte-length or a missing sentinel means the file was not read, and every "zero hits" result below is void → HALT.** Report the byte length and the sentinel result alongside the hit counts; **a hit count reported without them is unverifiable, not clean.**
>
> **Planner's measured expectation at authoring: ZERO hits on both paths — executed, not reasoned.** Path (a): id list length **141**, **0 hits**. Path (b) was run with the exact `_EM_DASH_SEP` split against the live `PLANNER_TEMPLATE.md`: **0/16 substring hits.** And that file contains **0** `**Tag:**` and **0** `**Tags:**` lines, so the tag criterion is **inert today** — ⚠️ **inert because the reference file has no tag lines, NOT because the tags fail to overlap.** If a future edit adds tag lines to that file, this criterion wakes up. Report the actuals.
> 3. **Record the non-terminal state as MEASURED, not as expected** — write `Step 1a-bis: NT_COUNT=<the value you captured>; sentinel check on entry 198 performed` and continue. ⚠️⚠️ **Do NOT write a pre-composed "set empty" string:** it is correct only on a FRESH dispatch, and on a resume this cycle's own 16 proposals make `NT_COUNT` legitimately non-zero — so a fixed string would enter a FALSE attestation into the receipt in exactly the state a later reader most needs it to be true. **Transcribe the number you actually read.** ⚠️ **Do NOT record this as "N/A"** — the sentinel check in item 2 ran and has a result.
>
> Record the comparison as raw output, one line per row.
>
> **Step 1b — run the ingest (ONCE, this step only).** Open canonical **read-WRITE** (plain `sqlite3.connect("/Users/marklehn/Developer/GitHub/lessons-forge/lessons-forge.db")` — NOT `?mode=ro`). **Call `run_full_lessons_cycle(conn, lessons_md_path="/Users/marklehn/Developer/GitHub/LESSONS.md")` — pass the path EXPLICITLY and print the argument.** ⚠️ The returned dict carries no path key, so "confirm it read the default" cannot be discharged; passing it explicitly makes the claim checkable from your own transcript. **`conn.commit()` after it returns** (the DB is gitignored; skip it and a step death LOSES the ingest). **This is a WHOLE-CORPUS re-hash.**
>
> ⚠️ **Then IMMEDIATELY append the verbatim returned dict to the Step-1a-ter stub and `git commit` it (a second commit, same pathspec), BEFORE anything else.** Ledger C4 requires a resume to anchor on the original committed capture, and **the ingest dict is the ONLY value in this plan that is genuinely unreproducible** — every other anchor can be rebuilt from the pristine backup, but a post-ingest resume re-runs the cycle over already-ingested entries and gets `updated_count == 0` / `terminal_proposals_flagged == []` no matter what the FIRST run returned. **Never overwrite an already-recorded dict.** **Record and PRINT the actual returned dict:** `ingested_count`, `updated_count`, `unchanged_count`, `duplicates_marked_count`, `terminal_proposals_flagged`, `needs_classification`.
>
> ## Step 1 gates — G1 through G6 (report EVERY one as a table row with its measured value + PASS/HALT)
>
> **Timing:** G2 pre-ingest; **G1 pre-ingest ALWAYS**; G3–G6 read the Step-1b dict. **Run every gate before halting** — a one-row gate table forces the CEO to adjudicate blind.
>
> - **G1 — non-terminal precondition.**
>   - ⚠️⚠️ **FIRST, CHECK `stale` EXPLICITLY — it belongs to NEITHER partition.** `_TERMINAL_STATUSES` is `{implemented, rejected, superseded, reference}` (`src/lessons_forge.py:31`, verified) and the non-terminal set is `{proposed, accepted, ambiguous}`. **`stale` is in neither**, so a proposal a prior failed attempt already staled **vanishes from the non-terminal query — and would make the empty result below look like a clean baseline.** ⚠️⚠️ **THIS IS THE ONE WAY THE EMPTY EXPECTATION CAN BE SATISFIED BY CORRUPTION.** So capture BOTH, as printed numbers: `NT_COUNT` (expected **0**) **and** `STALE_COUNT` (expected **3**). **`STALE_COUNT > 3` → HALT** — something staled rows between authoring and dispatch, and an empty `NT` is then evidence of damage rather than of health. **Absence from the non-terminal set is NOT evidence of terminality.**
>   ⚠️ **`STALE_BASE` — the value `STALE_COUNT` is compared against — is an ANCHOR, not a literal (C2 + C4).** On a **FRESH** dispatch `STALE_BASE` is the Planner's measured **3**, and a mismatch is a HALT rather than a correction. On a **RESUME** `STALE_BASE` is **the value recorded in the 1a-ter stub**, never the literal and never a live re-read — a resume that re-reads an already-damaged corpus would record the inflated count as its own baseline and compare it to itself. ⚠️ **If this is a resume and the stub carries no recorded `stale` baseline, do NOT substitute 3** — report the anchor as missing and treat the `stale` comparison as unverifiable, which is the honest result.
>   - **`NT_COUNT == 0` AND `STALE_COUNT == STALE_BASE`** → **fresh run → PASS.** Nothing in the corpus is staleable; the ingest is non-destructive by construction, and remains so conditional on `updated_count == 0` (G4).
>   - **`NT_COUNT == n` where every one of those `n` proposals has `entry_id > E0` AND `STALE_COUNT == STALE_BASE`** → **RESUME** → `PASS (resume)` ⚠️ **— but ONLY if step 0's dispatch determination also says RESUME (C7).**
>
>     ⚠️⚠️ **THE `STALE_COUNT` CONJUNCT IS LOAD-BEARING AND AN EARLIER WORDING OMITTED IT, LEAVING TWO BRANCHES MATCHING ONE STATE WITH OPPOSITE VERDICTS.** The fresh branch above guards itself with `AND STALE_COUNT == STALE_BASE`; this one did not, so **a resume whose corpus had ALSO been staled satisfied both this branch (`PASS (resume)`) and the `STALE_COUNT != STALE_BASE` branch below (`HALT`), with no precedence stated.** ⚠️ **That state is precisely what G1 exists to catch:** a staling of this cycle's own proposals is the inversion the classification section warns about, which puts all sixteen entries back on `get_unclassified_entries`'s work list and makes a re-dispatch insert **sixteen MORE proposals.** **Reading the resume branch first would PASS straight into a double insert.** **With the conjunct, the branches are disjoint and the staled-resume state falls through to HALT, which is correct.** **If step 0 said FRESH and this branch's condition holds, that is a CONTRADICTION → HALT and report both.** ⚠️ **Also bound `n`: more than 16 is a HALT regardless of dispatch state.** ⚠️ **This is the branch a flat "must be 0" assertion elsewhere would pre-empt — it is reachable and expected, not a fallback.**
>   - **ANY non-terminal proposal with `entry_id ≤ 198`** → the premise this plan is built on is VOID → **HALT before the ingest**, reporting every id, entry_id, status and route. ⚠️ **Do NOT attempt to protect it by improvising the removed ladder** — report and stop; the Planner re-authors against the real baseline.
>   - **`STALE_COUNT != STALE_BASE`** → **HALT**, in either direction. An INCREASE means something staled rows between authoring and dispatch. A DECREASE means the corpus is not the one this plan was authored against; nothing in this cycle un-stales a proposal.
>
>   ⚠️ **Report `NT_COUNT`, `STALE_COUNT` and the raw non-terminal query output regardless of which branch fires** — printed numbers, never inferred from silence. An empty result set and a failed query look identical on stdout.
> - **G2 — LESSONS.md provenance. THE GATE is: `git -C /Users/marklehn/Developer/GitHub status --porcelain -- LESSONS.md` is EMPTY.** Use `git -C <absolute root>` — a bare `git status` from the submodule worktree passes VACUOUSLY (Rule 55(a), `:1075`). ⚠️ **Empty stdout alone does NOT prove the check ran** — a mistyped `-C` exits 128 with empty stdout. Capture the exit code: `git -C … status --porcelain -- LESSONS.md; echo "PORCELAIN-EXIT=$?"`. **A non-zero exit is a HALT.** ⚠️⚠️ **AND NON-EMPTY OUTPUT IS ALSO A HALT — before the ingest.** The Planner committed the `LESSONS.md` append before dispatch, so a dirty `LESSONS.md` means an unexpected edit: **do NOT ingest an uncommitted corpus.** This is stated explicitly because the two nearest adjudications in this plan both say *don't* halt on a working-tree signal — G2's own HEAD clause below, and QA row 7's reconcile-note treatment — and neither precedent applies here. **G2 measures the exact input the ingest is about to read.** Separately RECORD `git -C <root> rev-parse --short HEAD` (Planner's measured: `b4b7bad`) — **a mismatch is a reconcile-note, NOT a halt.** ⚠️ **G2 does NOT measure the doctrine pins.** They were captured once in the Step-1a-ter stub (position 2) and G2 **cites that capture** — confirm the stub carries three hashes and record that you read them from it. ⚠️ **An earlier draft had G2 re-measure them, which created a second source of truth for one fact and no rule for a disagreement; see the stub's own note for why the region was collapsed to a single capture.** The pins are **working-tree content hashes, NOT `git rev-parse HEAD:<path>`**, which resolves HEAD's tree and is blind to an uncommitted edit (Rule 61, `:1115`). **They do DRIFT DETECTION (unchanged since AUTHORING) — a different job from QA row 7a's fail-closed unchanged-in-this-window check, and Rule 61 requires the job be named at the point the pin is taken.**
> - **G3 — `duplicates_marked_count == 0`.** Non-zero → HALT and name which entry ids + why. ⚠️⚠️ **A ZERO ON A RE-DISPATCH IS NOT THE SAME CLAIM AS A ZERO ON A FRESH RUN.** If a prior dispatch's detector fired, it INSERTED a `duplicate` proposal before halting; its idempotency guard (`WHERE entry_id = ? AND category = 'duplicate'`) then makes the count 0 on re-dispatch, **and that entry is now excluded from `get_unclassified_entries` because it carries a non-stale proposal — so it is silently never classified while the self-report still reads 16.**
>
>   ⚠️⚠️ **ON A RESUME, ASSERT THE SCOPED FORM — NOT A WHOLE-CORPUS ONE. PLAN 288's CLONE-SOURCE ASSERTION IS FALSE BY CONSTRUCTION ON THIS CORPUS AND IS DELIBERATELY NOT CARRIED.** 288's G3 reads *"assert no proposal in the corpus carries `category='duplicate'`"*. **Measured at authoring: `DUP_COUNT=19` — nineteen such proposals already exist**, all pre-dating this cycle. That assertion would HALT every resume of this plan. **This is C9's violation committed by 288's own author and caught only at run time; it is corrected here, not inherited.** The correct form scopes to this cycle's own entries:
>   ```
>   sqlite3 "file:/Users/marklehn/Developer/GitHub/lessons-forge/lessons-forge.db?mode=ro" \
>     "SELECT 'DUP_IN_BATCH=' || COUNT(*) FROM lesson_proposals WHERE category='duplicate' AND entry_id > 198;"
>   ```
>   **Record the printed token. Non-zero → HALT** even though `duplicates_marked_count` is 0. ⚠️ **A bare SELECT prints nothing both when there are none and when the query never ran (C8) — use the count form.** ⚠️ **`entry_id > 198` is legal HERE only because this branch runs inside Step 1, which holds the write handle and is where the ids are created (C9's scope).**
>
>   ⚠️⚠️ **A ZERO HERE IS NOT SELF-VALIDATING — G3 PASSES IDENTICALLY WHEN THE DETECTOR READ NOTHING.** `detect_duplicates` swallows a reference-file read failure and returns an empty list, so `0` means either "scanned and found none" or "never scanned". **Discharge this gate ONLY against Step 1a-bis's positive control** — the reference file's recorded byte length and sentinel hit. **If that control is absent or failed, report G3 as `HALT (unverified)`** — ⚠️ **the vocabulary matters: a bare `UNVERIFIED` is neither PASS nor HALT, so the closing rule ("if all PASS continue; any HALT stop") would have no branch for it and the agent would be left holding the canonical write handle with no instruction. `HALT (unverified)` stops.** ⚠️ **Do not resolve this by re-running the detector** — a second silent no-op reproduces the same zero.
> - **G4 — `updated_count == 0` AND `terminal_proposals_flagged` empty** (hash-trap watch on entry 198). Non-zero either way → HALT, show the diff, classify whitespace-only (plan-204 regression) vs substantive. ⚠️ **G4 is a DETECTOR, not a guard — Step 1a-bis is the guard.** The staling UPDATE runs inside the ingest (`:187-193`), so a non-zero `updated_count` means a live proposal may ALREADY be `stale`. ⚠️ **And you cannot measure how many:** `run_full_lessons_cycle` drops `stale_proposals_marked` from its return dict (`:503-511`). **On a G4 failure, query directly:** `SELECT id, entry_id, status FROM lesson_proposals WHERE status='stale'` and diff against the stub's baseline. If any proposal is now `stale` beyond the baseline count of 3, say so and name the `.backup` as the recovery point.
> - **G5 — there is work to do.** `ingested_count == 0` AND `needs_classification` empty → disambiguate: **(a)** truly nothing to do → HALT. ⚠️⚠️ **EXCEPT the re-dispatch of an ALREADY-COMPLETE Step 1** (ingest done, all 16 classified, both deposits committed with `Status: Complete`) — a legitimate, uncorrupted state that must NOT be turned into a halt: **do NOT overwrite the existing `Status: Complete` receipt with a halt record.** APPEND a `### Re-dispatch note` section, leave the status line reading **`Status: Complete (idempotent re-dispatch — no work required)`**, commit, and stop. **(b)** proposals exist but deposits absent → deposit-completion resume, **re-generate deposits**, then END THIS STEP. ⚠️⚠️ **"Re-generate from the committed DB" is NOT sufficient — regenerate from the DB *and from the 1a-ter stub*:**
>   - ⚠️⚠️ **REGENERATE RECEIPT ITEM 6 — THE SIXTEEN-ID CREATED-PROPOSAL LIST — FIRST, BEFORE ANYTHING ELSE IN THIS BRANCH. It is the anchor with the most readers and it was the one artifact this list did not name.** Ten sites in this plan consume it, and **QA rows 3, 5, 8 and 9 plus Step 2's route attribution ALL fail closed to `❌ (unverifiable)` without it**, by explicit design and with a standing prohibition on falling back to a predicate. **So a resume that rebuilds everything else and omits this one leaves the plan correctly refusing to verify a corpus that is in fact perfect.** It is fully derivable: `SELECT id, entry_id FROM lesson_proposals WHERE entry_id > 198 ORDER BY id` — ⚠️ **C9 explicitly permits that predicate INSIDE Step 1, which holds the write handle; this is the carve-out, not a breach.** **Re-emit it in item 6's exact fixed format (`- created proposal=<id> entry=<id>`), and HALT if it returns anything other than sixteen rows** — the same guard the original self-report carries, for the same reason.
>   - **The first-dispatch ingest dict is NOT in the DB and cannot be recomputed.** **Copy it verbatim from the stub as `#### First-dispatch ingest dict`.** If the stub has no dict, say so explicitly — QA row 4 then marks itself `❌ (unverifiable)`, which is correct, not something to paper over.
>   - **The `#### Scout dispositions` section IS derivable from the DB** — compare each proposal's stored `category` and `target_artifact` against the scout table and re-emit the fixed-format lines. **Do this; do not omit the section.** ⚠️ QA row 3 marks a divergence with no recorded line as `❌`. Where the reason is unrecoverable, write `reason: not recorded (regenerated on deposit-completion resume)`.
>   - ⚠️⚠️ **REGENERATE `#### Doctrine pins` (item 10) FROM THE STUB — this list previously omitted it, and QA row 7b fails closed without it.** The pins are measured exactly once, in the 1a-ter stub; **copy the three hashes forward verbatim.** ⚠️ **Do NOT re-run `shasum` here: a resume runs hours-to-days after authoring, so a live re-measurement would silently redefine the drift baseline as "whatever the files say now" and row 7b would compare a value to itself — the same self-comparison C4 forbids for the `stale` baseline, one anchor over.** **If the stub carries no pins, say so; row 7b is then `❌ (unverifiable)`, which is correct.**
>   - `E0`, `P0`, the pristine backup path, the entry-198 sentinel hash and the `stale` baseline likewise come from the stub, never a live re-read (C4).
>
>   `ingested_count == 0` but `needs_classification` non-empty → `PASS (resume)`. Fresh run expects `ingested_count == 16`. **Any `ingested_count` ∉ {0, 16} → HALT.**
> - **G6 — work-list reconciliation.** The batch range is `E0+1 .. E0+16` (= **199–214**), computed ARITHMETICALLY from the confirmed `E0=198` — **do NOT derive it FROM `needs_classification`** (self-referential and vacuous). **THE INVARIANT: every id in `needs_classification` is `> E0` and `≤ E0+16`.** ⚠️ **The upper bound is the BATCH SIZE, and getting it wrong is a live failure mode in this lineage** — 288 inherited `E0+8` from 283's eight-entry batch, which admitted two ids beyond its own batch and would have handed the classifier entries the plan did not own. **This plan's bound is 16 because this plan's batch is 16; it is not a carried literal.** ⚠️ **G5's `ingested_count ∉ {0,16}` backstops only the case where THIS plan ingested them; it does not cover foreign entries already ingested and merely unclassified.** HALT on any id OUTSIDE it → the CEO chooses (i) classify batch + extra, (ii) batch only with deferred ids under `### Deferred entries (CEO-approved)`, or (iii) investigate. **FRESH (`ingested_count==16`)** → `needs_classification` must be EXACTLY the 16; FEWER → HALT. **RESUME (`ingested_count==0`)** → a SUBSET is EXPECTED → `PASS (resume)`.
>
> **After the gate table: if all PASS, continue to classification. Any HALT — stop and report (having run the remaining gates); the ingest stays committed.**
>
> ### Classification — every entry `get_unclassified_entries` returns
>
> **`get_unclassified_entries` (`src/lessons_forge.py:267`) is AUTHORITATIVE — classify EXACTLY what it returns, in ascending `id` order; NEVER hand-derive "199–214" or anchor on a literal "16".** On a RESUME it returns only the un-classified remainder — **this exclusion is the SOLE mechanism preventing a double-insert, ⚠️⚠️ and it INVERTS if anything staled this cycle's proposals.** `get_unclassified_entries` excludes an entry only while it carries a NON-stale proposal, so a concurrent whole-corpus ingest that stales our sixteen would put all sixteen entries back on the work list, and a re-dispatch would insert sixteen MORE proposals. **Before classifying on any resume, confirm no proposal in the recorded sixteen-id set is `stale`** — as a COUNT with a printed token (C8): `SELECT 'STALE_IN_SET=' || COUNT(*) FROM lesson_proposals WHERE id IN (<the sixteen>) AND status='stale'`; **silence is not zero**. If any is, HALT — do not classify. The `.backup` is the recovery point.
>
> For each: read `id, source_heading, raw_content, tags, entry_date`, apply ADR-002, and call `insert_proposal(...)` (`src/lessons_forge.py:202`, signature read at authoring). ⚠️ **Count it by NAME, not by number — `conn` plus FIVE required arguments, in this order: `entry_id, category, suggested_action, reasoning, confidence`.** A sixth positional passed after `conn` binds to `status`, which is CHECK-constrained, so it fails. `status`/`target_layer`/`target_artifact`/`route` are keyword arguments:
> - `entry_id` — the FK, taken from `get_unclassified_entries` (Rule 47, `:1018` — never hand-typed).
> - `category` ∈ `structural`/`instrumentation`/`governance_rule`/`language`/`narrative` (never hand-assign `duplicate`). **Verified against the live CHECK constraint at authoring (`src/db.py:35`): those five plus `duplicate` are exactly the permitted values.** ⚠️ **Do NOT expect a uniform category.**
>
>    ⚠️⚠️ **ANCHOR ON THE ROW IN FRONT OF YOU, NEVER ON A PREDICTED ID.** You hold each entry's `tags` column when you classify it, so use it. **But this batch's tag set is FIVE values, and THREE of them the corpus has never classified — so the tag gives you LESS than it did on prior cycles, and this plan says so rather than pretending otherwise:**
>    - **`planner-discipline` (5 entries expected)** — the corpus's dominant tag; `governance_rule` is the strong expectation.
>    - **`bellows-integration` (7 entries expected)** — describes **live mechanics**; `instrumentation` is legitimate here alongside `governance_rule`.
>    - **`drafting-cycle` (2 expected)** — ⚠️ **ZERO prior corpus entries carry this tag. There is no precedent. Classify from the entry's substance alone.**
>    - **`verification` (1 expected)** — ⚠️ **ZERO prior corpus entries carry this tag. No precedent.**
>    - **`process-discipline` (1 expected)** — ⚠️ **ONE prior entry (id 134, the live-canary lesson this entry descends from), classified `instrumentation`.** That is a data point, not a mandate.
>
>    **WHICH entry is which is something you read, not something this plan numbers for you.** ⚠️ **Do NOT anchor a category to a predicted id** — Rule 58(3) is the reason: agreeing with a per-id expectation is cheaper than disagreeing with it, which is exactly the asymmetry the licence to disagree exists to remove. **Report the tag counts you measure; if they are not 5 / 7 / 2 / 1 / 1, that is the finding.**
>
>    ⚠️ **The id-ordering assumption, stated once so it can be checked rather than relied on silently:** ids are expected to follow `LESSONS.md` file order, because `parse_lessons_md` returns entries in file order and the ingest inserts them in that order into an AUTOINCREMENT table (`sqlite_sequence == MAX(id)`, no gap, measured at authoring). **This is a Planner measurement, not a gate (Checklist #29). Do not classify from it.**
>
>    Classify on merits; call out any genuine no-fit as `ambiguous` by id (`ambiguous` is a valid `status` — verified against `src/db.py:40`). ⚠️ **The tag is EVIDENCE for the category, not a synonym for it.**
> - `suggested_action` — concrete recommendation. **⚠️ DO name any code coupling where one exists:** entry 203 proposes a lint asserting every verification-table status cell is exactly one of the two permitted tokens. ⚠️ **State that as a QUESTION for Gate 1 (is it a tooling request, or does Rule 46 (`:1012`) fire?), not as a resolved obligation** — asserting a clause past its stated precondition is entry 200's and Rule 62's subject matter.
> - `reasoning` — **quoted evidence** citing specific `raw_content` from THAT entry, naming its id. Do NOT carry any claim from this plan's CEO Context into a `reasoning` field. If you cannot cite specific `raw_content`, STOP and report rather than writing generic justification. ⚠️⚠️ **BUT CITE, DO NOT TRANSCRIBE — this field is bounded at BOTH ends, and QA row 9 measures both.** A `reasoning` whose longest contiguous quotation is **under 40 characters** fails the floor (it cited nothing); one whose longest quotation is **80% or more of the field's own length** fails the ceiling (it is a paste, not an argument). **Measured range on the closest analogue — plan 288's six proposals — is 0.13–0.26, so an argued classification clears both bounds comfortably.** ⚠️ **Do NOT confuse this DB column with the dev-log disposition line's `reason:` (Receipt item 9), which is one line, MUST quote specific text, and carries NO ceiling. Different fields, different artifacts, different bounds — stated HERE, in the step that writes them both, because a rule that lives only in the verifying step cannot be complied with.**
> - `confidence` ∈ `low`/`medium`/`high`.
>
> ⚠️⚠️ **SIXTEEN IS TWICE THE LARGEST BATCH THIS MECHANISM HAS RUN, AND THE FAILURE MODE IS TAIL DECAY, NOT ERROR.** QA row 9 measures `reasoning` depth per proposal with a hard per-proposal floor, so a thin classification on the sixteenth entry fails the row rather than averaging out. **One obligation follows: `conn.commit()` after EACH `insert_proposal`, not once at the end — a mid-list death then costs the remainder, not the batch.**
>
> ⚠️⚠️ **THE MID-BATCH CHECKPOINT LINE THAT STOOD HERE IS DELETED, AND THE DELETION IS DECLARED BECAUSE IT WAS ADDED ONE PASS EARLIER IN THIS SAME CYCLE.** It instructed the agent to print, after the eighth insert, how many were done and which ids remained. **Measured against the artifact: the string appeared exactly ONCE in the entire plan — the instruction to print it. No Receipt item carried it, no QA row read it, no resume branch consumed it.** It was a capture with no reader, which is the standing ledger constraint it violated **and is verbatim what entry 205 of this batch describes** (*"the fold that added a load-bearing precondition gave it no Receipt slot and no reader"*).
>
> ⚠️ **The subsumption is verified PER JOB, not in aggregate (§2.7), because the checkpoint was claimed to do two things:**
> - **Resume anchor** — subsumed by `get_unclassified_entries`, which returns the exact unclassified remainder and is already declared AUTHORITATIVE for this step, plus the per-insert `conn.commit()` above that makes the landed prefix durable. **Strictly stronger than a printed line: it is a query against committed state rather than a claim in a transcript.**
> - **Tail visibility** — subsumed by QA row 9, which reports all sixteen measured lengths **in id order** with a per-proposal floor, so a decline across the list is visible and failable. **Strictly stronger: the checkpoint was unfailable.**
>
> **Both jobs have a named, failable owner; the checkpoint had neither. It is removed rather than given a reader** — adding a Receipt slot and a QA row to observe a line whose two purposes are already covered would be machinery built for a state the plan can already see.
>
> **Target fields — SET BOTH on every non-`ambiguous` proposal; never leave either NULL.** `target_layer='governance'` expected for all 16. `target_artifact` **SPLITS across at least three artifacts** per the scout table. Only `route`, `subcategory` and `duplicate_of` stay `None`.
>
> **⚠️ The scout is guidance, NOT a mandate — you have explicit LICENCE TO DISAGREE (Rule 58, `:1093`).** Derive each target INDEPENDENTLY from the entry's own `raw_content`. ⚠️⚠️ **Most entries carry a `**Family:**` line naming their lineage — read it — but TWO DO NOT: entries 211 and 213 have no `**Family:**` line at all** (measured at authoring). **For those two, derive the placement from the body alone and say so in the disposition line; do not report a Family line you did not find.** If your reading diverges, set what the entry supports and **RECORD the divergence in the Receipt's `#### Scout dispositions` section** — do NOT silently conform, and do NOT silently override. **A 16-row scout table costs more under anchoring than an 8- or 6-row one:** map each entry by its content, not its position in the table.
>
> **Then git-commit the two `.md` deposits.**
>
> **Self-report — this is where the sixteen-id anchor every later row depends on is CREATED, so its own premise is stated.** ⚠️ **C9 permits the predicate here because Step 1 holds the write handle; but this step CANNOT prove sole ownership** (its single-writer check is explicitly non-halting on zero matches), so the anchor is trustworthy only to the extent that check held. **If this query returns anything other than 16 rows, do NOT record an anchor and do NOT proceed to the deposits — HALT and report the actual rows**, because every downstream 'absolute' assertion inherits this set. Print `SELECT id, entry_id, status, category, target_artifact FROM lesson_proposals WHERE entry_id > 198 ORDER BY id` — expect **16 rows** (entry_ids 199–214, proposal ids 207–222). **Separately re-run the Step-1a `NT` query VERBATIM and LABEL THE RESULT `NT-post`** — ⚠️ **`NT-post` is an AFTER-value and is NEVER a valid before-anchor.** ⚠️ **`NT-post` should contain EXACTLY this cycle's 16 new proposals and nothing else** — the before-capture was empty, so on a clean run `NT-post` is a complete description of what this cycle added. **Any row in `NT-post` with `entry_id ≤ 198` is a foreign non-terminal proposal that appeared during the step → report it prominently.** Confirm `get_unclassified_entries()` is now `[]`.
>
> **Deposit:** `knowledge/development/classifications-cycle-2026-08-03.md` (the cycle dict, the 16 classifications' per-entry reasoning, the cluster synthesis for Gate 1, the narrative reasoning for any scout divergence — ⚠️ **the AUTHORITATIVE machine-checkable record of divergences is the Receipt's `#### Scout dispositions` section (item 9), which QA row 3 reads; this deposit carries the explanation, not the anchor** — and any `ambiguous` by id) + `knowledge/development/dev-log-cycle-step-1-2026-08-03.md` with an Output Receipt.
>
> **The Receipt MUST OPEN with an explicit status line, and it must be ONE OF THIS CLOSED SET:** `Status: Complete` · `Status: Complete (idempotent re-dispatch — no work required)` · `Status: Partial — HALTED at <gate>, <reason>` · `Status: Partial — in flight (pre-ingest stub; superseded by the final Receipt)`. **The first two are proceed-values; the last two are stop-values.** (The `Complete`/`Partial` vocabulary is the template's, `PLANNER_TEMPLATE.md:433`.) A halted status is a legitimate value — do NOT write `Complete` to make the step look tidy. It MUST then carry each of the following on its OWN labelled line:
> 1. the cycle dict verbatim (THIS dispatch's). ⚠️ **Plus, if the 1a-ter stub recorded an ingest dict from an earlier dispatch, reproduce it verbatim as `#### First-dispatch ingest dict` — QA row 4 reads THAT one whenever a resume is in evidence.**
> 2. the G1–G6 gate table
> 3. the pre-cycle baseline (status + category distributions, entry count, **the entry-198 sentinel hash**, and the `stale` baseline count)
> 4. `E0` and `P0`
> 5. **the `NT` capture.** ⚠️ **On a FRESH dispatch:** record `NT` and the printed `NT_COUNT=` / `STALE_COUNT=` lines, as RAW output; one label suffices. **On a RESUME:** carry the stub's pre-ingest capture forward **verbatim** as **`NT-original`**, **and** record this dispatch's re-read separately as **`NT-now`**. ⚠️ **`NT-original` is the before-anchor every downstream reader consumes; `NT-now` is diagnostic only.** ⚠️ **Downstream readers that need the BEFORE-anchor — Step 2's formula and QA row 5 — take `NT-original` when the Receipt carries it, otherwise `NT`. G1 IS EXPLICITLY EXCLUDED** and adjudicates the LIVE capture by design: that is exactly how it separates a fresh run from a resume. **Do not "sweep" G1 into this rule.** ⚠️ **An EMPTY value is still a legitimate value — write the label and record it as empty, and record `NT_COUNT=0` alongside it.**
> 6. **the self-reported created-proposal list — IN A FIXED, PARSEABLE FORMAT, because every QA row that scopes to "the sixteen recorded ids" reads THIS.** One line per proposal, values bare (no backticks, no bold, no quotes): **`- created proposal=<id> entry=<id>`**. ⚠️ **This shape is deliberately UNLIKE the scout-disposition line and shares no delimiter with it**, so QA row 3's disposition COUNT cannot match both formats. ⚠️ **It contains no `|` at all** (C11), which removes the paste-into-a-table hazard entirely rather than relying on a warning against it. Expect **16 lines** on a completed run, plus `NT-post` and `get_unclassified_entries() == []`.
> 7. **the absolute backup paths, LABELLED SEPARATELY** — `pristine (pre-cycle)` and, on a resume only, `this-dispatch (mid-cycle)`. QA row 4 and the reconstruction need the PRISTINE one specifically.
> 8. **`#### Files Created or Modified` — SPLIT INTO TWO LABELLED LISTS, because QA row 0 verifies one of them and would fail on the other.** Write `##### Committed deposits` (the tracked `.md` files this step wrote — row 0 verifies EVERY one) and `##### Untracked artifacts` (the `.backup`, and the canonical DB mutation — **gitignored and in the MAIN tree, so `git log` returns empty and `git status` errors from your worktree**). ⚠️⚠️ **With one list, listing the backup HONESTLY produces a `❌` while under-reporting passes — the instruction set would reward concealment.** Row 0 verifies the first list only; the second is recorded for the reader and cross-checked against Receipt item 7. **Both lists are still mandatory.** (Rule 17, `:554`, names this list; ⚠️ **its exact template wording is `Files Created or Modified (Code)` — the `(Code)` suffix is deliberately dropped here** because this cycle's deliverables are markdown deposits and a database mutation, not code, and row 0 greps the un-suffixed heading. **Declared so the divergence from the template's literal string is not read as an error.**) Step 2's Receipt must carry the same section.
> 9. any flags, and — as its own labelled section — **`#### Scout dispositions`**, carrying **ONE LINE FOR EVERY PROPOSAL, not only the divergent ones.** ⚠️ **This is Rule 58(3) given a structural home**; with a divergence-only section, agreeing costs nothing while diverging costs a formatted line, which is precisely the bias 58(3) names. Rule 54 (`:1069`) is why prose cannot carry it: an instruction anchored to a numbered QA row survives execution; one living in a prose paragraph is dropped. ⚠️ **QA row 3 matches this section MECHANICALLY, so the format is fixed:** `- proposal <id> | entry <id> | agreed | reason: <text>` for a placement matching the scout, and `- proposal <id> | entry <id> | diverged | field: <category|target_artifact> | scouted: <value> | set: <value> | reason: <text>` where it departs. Write the values **bare — no backticks, no bold, no quotes.**
>
>     ⚠️⚠️ **EVERY `reason:` IS DRAWN FROM THE ENTRY'S OWN `raw_content` (or its `**Family:**` line where one exists) — NOT from the tag, NOT from this plan's scout table, NOT from a general statement about the batch.** ⚠️ **This requirement was carried by plan 288's item 9 and was DROPPED IN CLONING here — measured, `grep -F` count 1 in the parent and 0 in this draft before the fold — and nothing detected it, because a dropped clause is an ABSENCE and no check in this plan looks for one.** **Restored, and it governs all sixteen lines, not a subset.**
>
>     ⚠️⚠️ **AND A STRICTER FORM BINDS FOUR OF THE SIXTEEN — STATED HERE, IN THE STEP THAT WRITES THE LINE, because QA row 3 enforces it and an obligation that lives only in the verifier cannot be complied with.** For every proposal whose entry carries **`drafting-cycle`**, **`verification`** or **`process-discipline`** (expected 4), the `reason:` must **quote or name specific text from that entry's `raw_content`** justifying the CATEGORY chosen — not merely the target. **A `reason:` that is empty, generic, or cites only the tag is an `❌` at QA row 3.** ⚠️ **Those three tags have no usable corpus precedent** (two have ZERO prior entries; one has a single one), **so these four rows establish the precedent for every future batch carrying them** — which is why they carry the higher burden and why the burden is written where the work happens rather than only where it is checked. ⚠️⚠️ **That line contains `|` characters: it MUST live in the Step-1 dev log, NEVER pasted into the QA report** (C11; entry 212 of the batch you are ingesting is exactly this defect). ⚠️ **A divergence recorded here is a `✅` at row 3; the same divergence unrecorded is a `❌`.** ⚠️ **The section always carries exactly as many lines as there are proposals you classified — 16 on a fresh run.** There is no "none" case: a run with zero divergences still has 16 `agreed` lines.
> 10. **`#### Doctrine pins`** — the raw `shasum -a 256` output for the three doctrine files, **carried forward VERBATIM from the Step-1a-ter stub, which is the single place they are measured. Do NOT re-run `shasum` for this item.** ⚠️ **The stub is the one source of truth; G2 cites it and this item reproduces it, so all three locations always agree by construction rather than by a rule about which one wins.** ⚠️ **QA row 7b diffs live hashes against THIS section and marks itself `❌ (unverifiable)` if it is absent or carries fewer than three hashes.** ⚠️ **Per Rule 61 (`:1115`), the JOB is named: these pins do DRIFT DETECTION (unchanged since AUTHORING), which is a different job from row 7a's fail-closed unchanged-in-this-window check.**
>
>     ⚠️ **Planner's measured values at authoring (2026-08-03, post-291) — confirm against your own `shasum` and report the actuals:**
>     - `DRAFTING_CYCLE.md` — `2d5cf9ab7c3a87ed825a1e1edcafea9a9b2598f30b29a7e48868558874a18ee0` (v1.3)
>     - `PLANNER_TEMPLATE.md` — `e8289d50f28711fdbf7c5319d812c229d3e7d2e255fc77f7ce868c19c01b6783` (v4.82)
>     - `RULE_20_SELF_CHECK_BLOCK.md` — `3accbce0c8d2b44586edc6c5f95582f775d0bce14276966b7ca90724e0ca5644` (no version line — CEO decision 2026-07-29; the `shasum` IS its version)
>
>     ⚠️⚠️ **TWO OF THE THREE MOVED SINCE PLAN 288 AND THE THIRD DID NOT — AND THE ONE THAT DID NOT IS THE TRAP.** Plan 291 rewrote `DRAFTING_CYCLE.md` and `PLANNER_TEMPLATE.md` on 2026-08-02; `RULE_20_SELF_CHECK_BLOCK.md` has been byte-stable since session 14. **A clone that carried 288's three pins forward would diff two and MATCH the third, and a partial match reads as "the pins are being checked."** All three above were re-measured this session with `shasum -a 256` against the live working tree.
>
> ⚠️ **Every measured value is deposited as RAW COMMAND OUTPUT, not a summary or re-typed figure** (memory `qa-evidence-raw-output`). "Raw" means the output is PRESENT, not unlabelled — annotate freely, but the annotation accompanies the raw output, never replaces it.
>
> Canonical Python file-write — no heredoc. Commit both by explicit pathspec. `#### Prompt Feedback` in `### Ledger Updates`.
>
> **Deposits:**
> - `lessons-forge/knowledge/development/classifications-cycle-2026-08-03.md`
> - `lessons-forge/knowledge/development/dev-log-cycle-step-1-2026-08-03.md`

---
---

## STEP 2 — DEV (generate the report)

---

> **Before starting, read the Step 1 deposits and confirm the Output Receipt's opening status line is one of the two PROCEED-values — `Status: Complete` or `Status: Complete (idempotent re-dispatch — no work required)`. Anything else — including `Status: Partial — HALTED at …` and the 1a-ter stub's `Status: Partial — in flight` — stops this step; report and do not proceed.** ⚠️ **Treat this as an ALLOWLIST, not a prefix match**: a stub-only receipt means the ingest never finished, and proceeding on it would generate a report over a half-mutated corpus. ⚠️ **This is a DELIBERATE NARROWING of `PLANNER_TEMPLATE.md:433`, declared so you do not "correct" it back.** The template permits proceeding when status is *"Complete or Partial with known acceptable scope"*. This plan does not accept the Partial branch, because every halt condition in Step 1 concerns the integrity of the ingest itself — and this step's whole output is a report *derived from* that ingest.
>
> Post a short visible chat message. You are the Forge Developer (`/Users/marklehn/Developer/GitHub/forge/agents/FORGE_DEVELOPER.md` — skip with a note if absent). Same working-location + absolute-DB rules. **Open read-only:** `sqlite3.connect("file:/Users/marklehn/Developer/GitHub/lessons-forge/lessons-forge.db?mode=ro", uri=True)`.
>
> **Scope:**
> - `reports/lessons-report-2026-08-03.md`
> - `knowledge/development/dev-log-cycle-step-2-2026-08-03.md`
>
> **Before generating: if `reports/lessons-report-2026-08-03.md` already exists AND this step's dev-log deposit is committed → HALT** (`generate_lessons_report` overwrites unconditionally). If the report exists but the deposit is ABSENT → deposit-completion resume ⚠️ **(regeneration is read-only with respect to the DB, but it OVERWRITES the report, and it is NOT deterministic across a verdict gate)** — the CEO may walk Gate 1 in this window, adding route lines the earlier report lacked. **Regenerating destroys the exact artifact the Gate-1 decision was made against, and unlike the corpus the report has no restore point. Copy the existing report aside FIRST, to the MAIN tree by absolute path — `/Users/marklehn/Developer/GitHub/lessons-forge/data/backups/lessons-report-pre-regen-296-<UTC-stamp>.md` — and record it in this step's `##### Untracked artifacts` list on its OWN labelled line, in exactly this form: `copy-aside (pre-regen): <absolute path>` — ⚠️ **the label is mandatory and fixed, because QA row 0 cross-checks against that exact token and this step's Receipt carries no numbered items for it to cite instead** — alongside the original report path.** ⚠️ **Deliberately OUTSIDE your worktree and outside `**Scope:**`:** a copy committed inside the worktree trips `scope_check` (it matches by basename against declared scope), and one left uncommitted does not survive teardown — so the only form that both preserves the artifact and passes the gates is a main-tree write alongside the DB backup. **Verified at authoring: no 2026-08-03 report exists in `reports/`.**
>
> Run `generate_lessons_report(conn, "2026-08-03")`. **The report is WHOLE-CORPUS** — it surfaces every `proposed`/`ambiguous` proposal; the date argument is only the filename/title.
>
> **⚠️ TWO EXPECTATIONS, BOTH DERIVED — NOT HARDCODED. Read Step 1's recorded `NT` capture and its `NT_COUNT=` line, and derive both numbers from that. ⚠️ If Step 1's Receipt does not carry a parseable `NT` label, STOP and report — do NOT fall back to a literal.**
> 1. **Expect `<pre-ingest NT_COUNT> + <this cycle's classified count>` surfaced proposals — and with the pre-ingest `NT` empty that is 16.** ⚠️ **The formula, not the 16, is what you evaluate**, and **both of its operands are PRE-INGEST or OWNED values read from Step 1's Receipt — neither is a live re-read.** ⚠️⚠️ **An earlier wording opened this bullet with `16 + |NT still non-terminal AT THIS MOMENT|` while the instruction below said to read the PRE-INGEST count. Those are two different quantities: on a fresh run both are 0, but on a RESUME they diverge by exactly 16, because this cycle's own proposals are `proposed` at that moment.** The headline named the operand the two-label Receipt exists to keep you away from, and the instruction named the right one — **a reader following the headline computes `16 + 16 = 32` against a correct 16.** Plan 288 fixed the LABEL question in the instruction and left the HEADLINE stating the other operand; corrected here at both. ⚠️⚠️ **NAME THE LABEL: take `NT-original` when the Receipt carries it, otherwise `NT`. NEVER `NT-now`.** On a RESUME the Receipt carries BOTH, and `NT-now` is legitimately **16** (this cycle's own proposals are `proposed`) — so reading it computes `16 + 16 = 32` against a correct 16.
>    ⚠️⚠️ **AND THE CORPUS CAN CHANGE BETWEEN STEP 1 AND HERE.** The approval gate before this step is hours-to-days. With nothing parked at baseline, the anomaly to watch for is an ADDITION — a proposal appearing with `entry_id ≤ 198`, which means a foreign writer. **Re-read live status before computing the expectation and report both the recorded `NT_COUNT` and the live one.** ⚠️⚠️ **A surfaced proposal outside this cycle's sixteen is a RECONCILE-NOTE, NOT a HALT — record its id and CONTINUE.** The report is whole-corpus and the gate before this step is hours-to-days, so a foreign proposal surfacing here is a legitimate in-window event. **A HALT here would deadlock a correct run: once Step 2 records a halted status, Step 3's relief clause is unreachable because Step 3 refuses to start.** **The genuine HALT is a surfaced proposal you cannot attribute at all.**
> 2. **Expect exactly ZERO `- **Route:**` lines.** `src/lessons_forge.py:584` emits the line under `if route is not None` — verified in code at authoring — and this cycle inserts every proposal with `route = NULL`. ⚠️ **One documented exception must not be turned into a halt:** **the CEO walking Gate 1 on this cycle's own sixteen proposals between Step 1 and here is legitimate.** ⚠️ **So: attribute before you halt.** A route line attributable to one of **the sixteen proposal ids this step recorded** (NOT `entry_id > 198` — C9) whose `status` is still `proposed` → **Gate 1 walked in-window: record it with the ids and CONTINUE.** A route line attributable to any `entry_id ≤ 198` proposal, or any route you cannot attribute → **HALT.**
>
>    ⚠️⚠️ **DO NOT COUNT THIS WITH A PLAIN `grep` — THE EXPECTED ANSWER AND A BROKEN COMMAND ARE THE SAME OUTPUT, AND THIS CYCLE'S ZERO EXPECTATION CANNOT TELL THEM APART.** On this machine **`grep` is a ugrep shim.** ⚠️ **Entry 204 of the batch you are ingesting is precisely this rule, in its corrected form: `-F` is MANDATORY, and a search not run with `-F` is invalid evidence REGARDLESS of its exit code** — a `**`-bearing pattern without `-F` exits **1, silently, on a file where the line is present**, which is the code an uncorrected rule whitelists as "absent". Therefore:
>    - **`grep -F` ALONE DOES NOT FIX THIS PATTERN.** `grep -Fc '- **Route:**' <file>` still fails — **empty stdout, exit 2** — because the pattern **begins with `-` and is parsed as an OPTION**. **Only an explicit `--` end-of-options marker fixes it:** `grep -Fc -- '- **Route:**' <file>` returns `0` and exits 1. **Use BOTH `-F` and `--`.**
>    - **The rule that holds: use `grep -F` for EVERY literal, and `--` for every pattern that could begin with `-`.** ⚠️ **A metacharacter-only rule is WRONG and misses the site it matters at most** — QA row 3's disposition-line count, whose lines begin `- proposal …`: a leading dash, no metacharacter.
>    - ⚠️ **NEVER pipe these to `head` — it masks the exit code**: `$?` then reports `head`'s status, so an errored check reads as a clean one.
>    - **Capture and report the exit code**: `grep -Fc -- '- **Route:**' <report>; echo "ROUTE-GREP-EXIT=$?"`. **Exit 0 = matches found (attribute, then decide). Exit 1 = zero matches (the expected result). Exit ≥2 = the command FAILED and the check did not run → HALT, do not record zero.**
>    - **Report the printed count, not the absence of output.**
>    ⚠️⚠️ **Apply `-F`, `--` and the exit-code capture to EVERY literal count in this plan — not to a metacharacter subset.**
>    ⚠️ **The report prints NEITHER proposal id NOR entry_id** — verified: it renders `### {source_heading}` per proposal and, although `entry_id` is SELECTed (`src/lessons_forge.py:538`), it is never written to output. So you cannot attribute a route line to an id from the report alone. **Attribute by `source_heading`**, correlating against `SELECT p.id, p.entry_id, p.route, e.source_heading FROM lesson_proposals p JOIN lesson_entries e ON p.entry_id = e.id WHERE p.status IN ('proposed','ambiguous')`. Report the route-line count, the headings carrying them, and the ids they correlate to.
>
>    ⚠️⚠️ **DO THE HEADING CORRELATION IN SQL OR PYTHON — NEVER BY INTERPOLATING A HEADING INTO A SHELL COMMAND. TWO OF THIS BATCH'S SIXTEEN HEADINGS ARE SHELL-HOSTILE, MEASURED AT AUTHORING.** Entry 208's heading contains an **apostrophe** (*"The cold panel's yield…"*) — inside a single-quoted shell argument it terminates the quote. Entry 213's heading contains **literal backticks** (*"Daemon liveness is `` `ps -p` ``…"*) — inside a double-quoted argument they invoke command substitution. **Neither failure is loud: the first is a quoting error or a silently truncated pattern, the second executes something.** ⚠️ **This plan warns about literal backticks elsewhere only for the `tags` column, where they void an equality predicate; that warning does not cover headings, and an earlier draft left this gap unnamed.** **Bind headings as query parameters; do not build shell strings from them.**
>
> **The one inherited halt condition that STILL HOLDS:** any `Recently-implemented overlap:` line → HALT ⚠️ **(C8 applies — a ZERO-expectation check: use `grep -Fc --`, report the printed count and the exit code, and never record "clean" from absent output.)** (Plan 207 retired `detect_recently_implemented_overlaps`; its reappearance is a regression.)
>
> `generate_lessons_report` resolves `output_dir="reports"` RELATIVE to cwd — run `pwd` before the call, capture the path the function RETURNS, state that absolute path, and confirm the filename matches Scope. ⚠️ **Note the latent `encoding=` gap:** `src/lessons_forge.py:593` writes with `open(output_path, "w")` and **no explicit `encoding=`** — safe on the Mac/Bellows UTF-8 default, unsafe on a cp1252 host. **This is the ONE remaining item on the lessons-forge Forward Register backlog and this plan's QA step emits it** (Rule 46, `:1012`). Print the report head (~80 lines).
>
> **Deposit:** the report + `knowledge/development/dev-log-cycle-step-2-2026-08-03.md`. **The Output Receipt MUST carry an opening `Status:` line** — ⚠️ **Step 3's precondition reads this line, so it is not optional.** Use `Status: Complete` or `Status: Partial — HALTED at <point>, <reason>`; the closed set's other two values are Step-1-only. **It MUST also carry a `#### Files Created or Modified` section** listing every file this step wrote, by path — ⚠️ **QA row 0 reads that section from THIS step's Receipt as well as Step 1's.** Then: report length, proposals surfaced with entry_ids, route-line count + which ids, and the count of `Recently-implemented overlap:` lines — expected **zero**. Canonical Python file-write — no heredoc. Commit both by explicit pathspec. `#### Prompt Feedback`.
>
> **Deposits:**
> - `lessons-forge/reports/lessons-report-2026-08-03.md`
> - `lessons-forge/knowledge/development/dev-log-cycle-step-2-2026-08-03.md`

---
---

## STEP 3 — QA

---

> **Before starting, read the Step 1–2 deposits and confirm both Output Receipt status lines are PROCEED-values — `Status: Complete` or `Status: Complete (idempotent re-dispatch — no work required)`. Anything else stops this step; report and do not proceed.** ⚠️ **An ALLOWLIST, not a prefix match.** Naming the required VALUE is load-bearing: an instruction that merely says "confirm the status lines" is satisfied by observing a halted one.
>
> Post a short visible chat message confirming you are starting Step 3 (QA). You are Lessons Forge QA (`agents/FORGE_LESSONS_AGENT.md`). Same working-location + absolute-DB rules. **Verification + reporting only — no product-code changes; if a test fails, report it, do NOT fix it.** Do NOT use Monitor. Do NOT edit PROJECT_STATUS directly.
>
> **MANDATORY — Rule 20 self-check (canonical block, Checklist #4 at `:1173` — the exact template with all four placeholders filled, NOT a paraphrase).** ⚠️ **Form selection (Rule 60, `:1105`):** this is a **DB-mutating plan with a real pytest suite** → the **full canonical block with real, plan-appropriate evidence files**, NOT a simple banner. Run the canonical block from `/Users/marklehn/Developer/GitHub/RULE_20_SELF_CHECK_BLOCK.md` (ABSOLUTE path — the block lives at the governance root, NOT in the lessons-forge worktree). Copy the Python block verbatim and fill ONLY the four placeholders:
> - `plan_slug`: `cycle-session-16-17-captures-2026-08-03`
> - `qa_report_path`: `<your-own-tree-abs>/knowledge/qa/cycle-qa-2026-08-03.md`
> - `evidence_dir`: `<your-own-tree-abs>/knowledge/qa/evidence/cycle-session-16-17-captures-2026-08-03/` — the block runs in YOUR process and checks `os.path.isdir`, so **derive this from `pwd` (your OWN working tree), NOT a hardcoded main-tree path** (the plan-225 trap).
> - `required_evidence_files`: `["pytest_targeted.txt", "invariants.txt", "hash-trap.txt", "schema.txt"]` — ⚠️ **Python string literals, QUOTED.** Copied unquoted they raise `NameError` before any check runs, so the block emits neither `FAILED` nor the banner, and the gate then reports *"no QA deposit contains Rule 20 self-check banner"* — a misleading cause for a transcription slip.
>
> ⚠️ **Rule 61 (`:1115`) applies to the block itself: it is a run-time-COPIED artifact.** Its authoring pin is `3accbce0c8d2b44586edc6c5f95582f775d0bce14276966b7ca90724e0ca5644` and **that pin does DRIFT DETECTION, not fail-closed guarding** — row 7b owns the comparison. If your `shasum` of the block differs, say so and continue to row 7b rather than improvising.
>
> Deposit all four evidence files BEFORE running the block — it `sys.exit(1)`s if any is missing or empty. Include the block's literal stdout in the QA report; the banner `## Rule 20 — QA Self-Check Results` + the `PASSED — SELF-CHECK PASSED` line must appear verbatim (byte-enforced by `bellows/gates.py`). **If the block prints `FAILED`, do not proceed with closure — halt and report to CEO** (the canonical template's own closing sentence, `RULE_20_SELF_CHECK_BLOCK.md:22`). ⚠️ **Writing the verification table does NOT satisfy this gate** — the gate reads the deposited report for the banner and the `PASSED` line only, and is blind to the table entirely (Rule 48, `:1024`, requires this callout to be stated explicitly). End with a self-grep confirming the banner.
>
> **⚠️ WHAT THE RULE 20 BLOCK DOES AND DOES NOT VERIFY.** The block reads **evidence-file presence and hedging keywords only. It cannot see your verification verdicts.** A table containing a genuine `❌`, with all four evidence files present, prints **`PASSED — SELF-CHECK PASSED`, exit 0**. So:
> - **Expect `PASSED` even on an honest halt.** That is NOT a contradiction and NOT a reason to doubt your `❌`. The gate that catches a failure is `gates.py::_gate_rule_22_verification`.
> - **You must NOT flip a row to ✅, soften its wording, drop it from the table, or re-scope a check to make the block print `PASSED`.** A `PASSED` banner obtained by adjusting the rows it summarizes is a false close — strictly worse than an honest halt.
> - If any row is `❌`, add one line immediately under the Rule 20 stdout: *"Rule 20 PASSED reports evidence-file presence and hedging only — it does not read verification verdicts. Row(s) &lt;list EVERY ❌ row&gt; are ❌; this step is expected to fail at the rule_22 verification gate."* Name every failing row, not just the first.
>
> **⚠️ Rule 19 — REQUIRED VERBATIM IN THIS PROMPT** (`PLANNER_TEMPLATE.md:560` mandates the QA prompt state this explicitly): *"If you cannot complete a check, mark it ❌ with a reason. Do NOT mark it ✅ and explain why you couldn't verify. Any ✅ row containing hedging keywords will auto-fail during the self-check in Rule 20."*
>
> ⚠️ **Read that mandate and the guidance that follows in the right order.** Hedging language in a ✅ row is a signal that the check was not performed. The instructions below teach you to avoid tripping the scanner **with honest measurements**. They do **not** license rewording a check you did not run. **The keyword list is a backstop with known gaps: an unverified ✅ phrased without those words survives it. A check you did not execute is ❌, whatever words you use.**
>
> **⚠️⚠️ DO NOT QUOTE THE HEDGING-KEYWORD LIST — OR PASTE THIS PLAN'S PROSE — INTO ANY LINE CONTAINING A `|`.** The block detects a positive-status row by cell equality but scans that row for hedging keywords by **whole-line substring**. So any `|`-bearing line pairing a positive-status token with "pending", "inferred", "assumed", "should pass", "not run" — even as a quotation or negation — emits CRITICAL and `sys.exit(1)`, halting a clean plan. **The hazard is ANY line containing a `|`.** Keep every table cell SHORT (claim / status glyph / measured value / evidence path); put narrative below the table or in an evidence file.
>
> ⚠️⚠️ **A HEDGING KEYWORD IS FATAL EVEN AS A MEASURED VALUE — including a ZERO count.** The keyword list contains **`skipped`**, **`not run`**, **`pending`** and **`assumed`**, and the scan is whole-line. So the most natural cell for row 1 — `55 passed, 0 skipped` — emits CRITICAL, `sys.exit(1)`, a `FAILED` banner, and then fails the Rule 20 gate for a missing `PASSED` line, **on a completely clean run.** **Write row 1's value as `<N> passed` and nothing else — measure `<N>`, do not copy it from this plan.** Planner's measured collection at authoring: **55**; report the actual and reconcile any difference (Checklist #29, `:1329`).
>
> ⚠️⚠️ **C11 / ENTRY 212 — NO COMMAND CONTAINING `|` GOES IN A TABLE CELL, AND ESCAPING IT IS NOT A FIX.** Escaping `|` to `\|` inside an ERE turns alternation into a literal pipe and the command silently matches nothing while exiting 1 — indistinguishable from a genuine negative. **Put every such command in a fenced block ABOVE the table and have the row cite its result.** ⚠️ **This is entry 212 of the batch you are ingesting, applied to the plan that ingests it.**
>
> ⚠️⚠️ **C11 / ENTRY 211 — THE STATUS COLUMN HOLDS EXACTLY ONE GLYPH. THERE IS NO THIRD VALUE.** Use ONLY `✅` or `❌` — not `NOTE`, not `PARTIAL`, not `N/A`, not `⚠️`, not blank, and **not a glyph with a parenthetical annotation appended** (entry 203: an annotated cell fails cell-equality, so it is neither a positive row for the hedging scan nor a failure for `rule_22` — **both gates ignore it**). **If an outcome feels like neither, it IS an `❌`.** ⚠️ **A row whose honest disposition is "note" does not belong in this table at all** — the two rows in this plan that carry reconcile-halves (5 and 8) are written so their reconcile outcome is still a `✅` with a note in the *measured-value* column, never a third glyph.
>
> **⚠️ On a FAILED block run, route its raw stdout to `invariants.txt` and describe the failure in the report WITHOUT reproducing the offending row.** The block echoes the offending line verbatim; pasting that stdout into the report makes the echo re-trip the scan permanently, foreclosing Rule 20's own documented recovery option (a). **This is a declared, narrow deviation from Rule 20's unconditional "include its literal output in the QA report" — on the FAILING path only.** On a PASSING run, paste the stdout into the report as Rule 20 mandates. **This step is authorized to take this ONE declared deviation and no others.**
>
> **Evidence-source rule.** Every SQL/PRAGMA row states which DB it ran against (canonical = `sqlite3 "file:/Users/marklehn/Developer/GitHub/lessons-forge/lessons-forge.db?mode=ro" "<query>"`). Deposit **RAW command output, never a summary of it**.
>
> **Scope:**
> - `knowledge/qa/cycle-qa-2026-08-03.md`
> - `knowledge/qa/evidence/cycle-session-16-17-captures-2026-08-03/pytest_targeted.txt`
> - `knowledge/qa/evidence/cycle-session-16-17-captures-2026-08-03/invariants.txt`
> - `knowledge/qa/evidence/cycle-session-16-17-captures-2026-08-03/hash-trap.txt`
> - `knowledge/qa/evidence/cycle-session-16-17-captures-2026-08-03/schema.txt`
>
> ⚠️ **The verification table MUST sit under a top-level `## ` heading containing the word "Verification"** — write exactly `## Verification Table` — with these columns: `| # | Claim | Status (✅/❌) | Measured value | DB source | Evidence |`. **The Evidence column is mandated by Rule 18 (`:557`): the row cites its evidence file BY PATH, "not by paraphrase, not by summary, not by 'see above'"**, and Rule 17 (`:554`) fixes the `Status (✅/❌)` spelling. `gates.py::_gate_rule_22_verification` tracks section transitions via `line.startswith("## ")` only, so a `###` sub-heading puts your table OUTSIDE the scanned region and an `❌` row would go undetected.
>
> ⚠️⚠️ **AND THE SECTION NEVER CLOSES — you MUST close it yourself with another `## ` heading.** `in_verification_section` is set only on a `## ` line and is **never cleared by `###` or `####`**, so it stays open to end-of-file and **every later `|`-bearing block is scanned as if it were the verification table** — your evidence-marker grep table, or a commit table with a single `OK` cell, would flag each of its other rows as a missing-status failure on an otherwise clean run. **Immediately after the verification table, open a new top-level section — write exactly `## Evidence and Narrative`** — and keep the Rule 20 stdout, the Output Receipt and `### Ledger Updates` beneath `## `-level headings.
>
> **⚠️ A FAILING ROW DOES NOT LICENSE SKIPPING THE REMAINING ROWS.** This step has **ten checks — rows 0 through 9** — mutually independent. **Run all ten, record every measured value, and halt only after the table is complete.** Inside a row, "→ halt" means *mark this row `❌`, record why, and keep going*. **A HALT MUST STILL LEAVE A COMMITTED RECORD.**
>
> ⚠️⚠️ **THE IN-WINDOW RECONCILIATION RULE — READ BEFORE ANY ROW BELOW; ROWS 5 AND 8 INHERIT IT RATHER THAN RESTATING IT.**
>
> **Step 1 ran behind a verdict gate of arbitrary wall-clock time — hours to days — and so did Step 2.** Several rows below measure **whole-corpus** quantities. **A lessons-forge cycle, or a CEO action, occurring inside that window legitimately changes those quantities, and this plan does not own the result.** Therefore every whole-corpus row is adjudicated in **two parts**:
> - **(a) HARD — the delta THIS plan owns, identified BY ID and not by a predicate.** ⚠️⚠️ **`entry_id > 198` is NOT "what this plan owns" — it is "everything ingested after authoring", and those are different sets.** An entry ingested by any other writer during the verdict gates receives an id **above** 214 and therefore satisfies `entry_id > 198`, landing inside the half this rule calls absolute — so a legitimate in-window event would produce a `❌` on the very rows `(b)` exists to protect. **The batch-pin discipline does not cover this**: it forbids appending while a cycle plan is deposited-un-run, and says nothing about the verdict-gate windows.
>   **So: take the SIXTEEN proposal ids and SIXTEEN entry ids from Step 1's Output Receipt (item 6's created-proposal list) and assert against THAT id set.** **This half is absolute. A failure here is `❌`.** ⚠️ **If the Receipt carries no parseable id list, mark the row `❌ (unverifiable)` — do NOT fall back to the predicate.**
> - **(b) RECONCILE — everything outside that scope.** The Planner measured **zero** foreign non-terminal proposals at authoring, so zero is the expectation — **but a proposal or entry created after Step 1 is a legitimate in-window event.** List the offending ids, record a reconcile-note naming them **in the measured-value column**, and **do NOT fail the row.** ⚠️ **A row failed on work this plan never owned is a false `❌`, and it costs a dispatch to re-run a cycle that was correct.** ⚠️ **The reconcile outcome is still a `✅` — C11 forbids a third glyph.**
>
> ⚠️⚠️ **THE RECORDED ID SET — ITS OPERAND, AND WHAT HAPPENS WHEN IT IS MISSING. Both halves below are inherited by every row that scopes to the sixteen ids (3, 5, 8, 9) and by Step 2's route attribution; neither is restated per row.**
>
> **(i) ENUMERATION — the reconcile half needs an operand.** When a reconcile half fires, obtain the complement **explicitly and read-only**. ⚠️⚠️ **VALIDATE THE ID LIST BEFORE YOU QUERY — `NOT IN` IS NULL-POISONED AND FAILS SILENTLY TOWARD "NOTHING FOUND".** Confirm **EACH list you are about to use** — the sixteen **proposal** ids AND, separately, the sixteen **entry** ids for the `lesson_entries` complement — is **exactly sixteen values, all integers, none blank or NULL**, and **HALT rather than query** if either is not. ⚠️ **They are DIFFERENT lists from different columns of the Receipt.** ⚠️ **Verified by execution: `WHERE id NOT IN (201,202)` over rows (201,202,207) returns `FOREIGN=1`; adding a single NULL to that list returns `FOREIGN=0` — the foreign row vanishes.** Because this is a ZERO-expectation check, `FOREIGN=0` is indistinguishable from a clean corpus, **and the printed token makes it worse by attesting that the check ran — which it did, on a poisoned predicate.** With the list validated: `SELECT 'FOREIGN=' || COUNT(*) FROM lesson_proposals WHERE id NOT IN (<the sixteen recorded proposal ids>) AND status IN ('proposed','ambiguous')` for a printed token first, then the row detail, and the analogous `lesson_entries WHERE id NOT IN (<the sixteen recorded entry ids>) AND id > 198`. ⚠️⚠️ **THE COMPLEMENT IS FOR IDENTIFICATION ONLY AND HAS NO `❌` PATH — EVER.** Report the ids and continue.
>
> **(ii) FAIL-CLOSED — the id set is a single point of failure for this entire step.** Rows 3, 5, 8 and 9, and Step 2's route attribution, all depend on Step 1's recorded created-proposal list. If that list is **absent, truncated, or unparseable**: mark **every dependent row `❌ (unverifiable)`** and say which artifact was missing. ⚠️⚠️ **DO NOT FALL BACK TO `entry_id > 198` OR ANY OTHER PREDICATE — C9 forbids it, and the fallback is precisely how the false-`❌` defect returns.** ⚠️ **And do NOT silently skip the row.**
>
> ⚠️⚠️ **AND ONE IN-WINDOW EVENT LANDS INSIDE THE *HARD* HALF — GATE 1.** The CEO may legitimately walk Gate 1 on THIS cycle's own sixteen proposals during a verdict gate; Gate 1 needs only the Step-2 report, which exists before Step 3 runs. Gate 1 sets **`route='codify'` while leaving `status='proposed'`**. Under an absolutist reading that makes **row 3's "every one `route IS NULL`" FAIL on a correct CEO action, inside the owned scope where `(b)` offers no relief.**
>
> **So `route` on this cycle's proposals is adjudicated DIRECTIONALLY:**
> - **`route IS NULL` on all sixteen** → the expected result → **✅**.
> - **A valid route (`codify`/`backlog`/`reference`) on one of the sixteen recorded proposal ids AND `status` still `proposed`** → **Gate 1 walked in-window** → **✅ with a `Gate-1 walked in-window` note naming the ids in the measured-value column.**
> - **A route on any proposal with `entry_id ≤ 198`** → **`❌`** — that is the foreign-mutation signal these checks exist for.
> - **`status` moved to `stale`, or route set alongside a status this plan cannot explain** → **`❌`**.
>
> ⚠️ **Note what is NOT being relaxed: `route IS NULL` remains the EXPECTATION, and every departure is reported with ids.** **This is C5's boundary** (*a permitted outcome is never a FAIL*). **Row 7 is the deliberate, declared exception to C5 and does NOT inherit this rule** — it fails closed on any doctrine change, in-window or not, because no other instrument in the system can see one.
>
> ## Verification Table
>
> 0. **Deliverable verification (Rule 17, `:554` — MANDATED in the final step, BEFORE the regression check, hence row 0).** Read the prior steps' Output Receipt **`#### Files Created or Modified`** sections and verify **every deliverable listed under `##### Committed deposits`** — **not sampling, and every one of them.**

>    ⚠️⚠️ **SCOPE THIS ROW TO THE COMMITTED-DEPOSITS SUB-LIST. AN EARLIER WORDING SAID "EVERY listed deliverable" WITH NO SUB-LIST NAMED, WHICH UNDOES THE SPLIT THAT EXISTS TO PROTECT IT.** Step 1's Receipt item 8 deliberately divides the section into `##### Committed deposits` and `##### Untracked artifacts`, and states that this row verifies the first only. **The `.backup` and the canonical DB mutation live in the second: both are gitignored and in the MAIN tree, so `git log --oneline -1 -- <path>` returns empty — which this row defines below as the FAILURE case.** ⚠️ **An unscoped reading therefore produces a `❌` on a completely clean run, and an agent that notices it is rewarded for OMITTING the backup from the Receipt — an instruction set that pays for concealment.** **Plan 288 found and repaired exactly this; the repair was reproduced here in the PRODUCER (item 8) and not in this CONSUMER, which is the same producer/verifier split this plan has now hit twice.** ⚠️ **DO cross-check each step's `##### Untracked artifacts` list and report the result — but never apply the commit tests to it, and never fail this row on it.** ⚠️⚠️ **THE OPERAND DIFFERS BY STEP, and an earlier wording named only one: for STEP 1 cross-check against Receipt item 7's labelled backup paths; for STEP 2 there IS no item 7 — its Receipt is not numbered.** **An unqualified "Receipt item 7" sends this row looking for a Step-2 item that does not exist.** ⚠️⚠️ **AND "cross-check against the Step-2 prose" — the first repair — IS NOT AN ANCHOR EITHER: Rule 54 (`:1069`) is explicit that an instruction pointing at unnamed prose is the shape that gets dropped at execution.** **So Step 2's Receipt carries a labelled line `copy-aside (pre-regen): <absolute path>` in its `##### Untracked artifacts` list, and this row cross-checks against THAT token.** ⚠️ **It is a ZERO-EXPECTATION check on the normal path — no prior report existed at authoring, so no copy-aside is written and the label is legitimately absent — and C8 therefore binds: `grep -Fc -- 'copy-aside (pre-regen):' <step-2 deposit>; echo "COPYASIDE-GREP-EXIT=$?"`, report the printed count and the exit code. **`0` with exit 1 is the expected normal-path result; `1` with exit 0 means a regeneration occurred; exit ≥2 means the check did not run.** **Record its absence, do not fail the row — but never record absence from silence.**
>
>    For each Step-1 and Step-2 **committed deposit**: confirm it exists on disk, and is committed **in its CURRENT state — two commands, both required**: `git log --oneline -1 -- <path>` **AND** `git status --porcelain -- <path>` which must be EMPTY. ⚠️ **`git log` alone is insufficient**: a file committed then modified still returns its old commit, so the check would pass while the current content is uncommitted.
>
>    ⚠️⚠️ **BOTH COMMANDS FAIL TO EMPTY STDOUT, AND FOR THIS ROW "EMPTY" MEANS OPPOSITE THINGS — capture the exit codes.** (i) `git status --porcelain -- <path>` prints nothing when the path is **clean** *and* when the invocation **failed** — so run it as `git status --porcelain -- <path>; echo "ROW0-PORCELAIN-EXIT=$?"` and treat **any non-zero exit as `❌`**, never as clean. (ii) `git log --oneline -1 -- <path>` prints nothing and **exits 0** for a path that was never committed — so here **empty output is the FAILURE case, not the pass case**: require a printed commit line and quote it. ⚠️ **Record both outputs verbatim per deposit.** ⚠️ **Do NOT re-verify deposit CONTENTS here** — rows 3, 4 and 5 do that and fail closed. **Any ❌ here is a Critical finding that blocks the plan from moving to Done.** ⚠️ Rule 17 permits "re-run the commit, re-create the file"; **this plan narrows that to RE-COMMITTING a deposit path it already declares — never re-creating content you did not produce.**
> 1. **Targeted suite** — `python3 -m pytest src/ -v` to an explicit pass/fail, raw tail shown. ⚠️ **This is the whole of `src/`, and under `Test Scope: targeted` that is the CORRECT and complete run** — the lessons-forge suite IS the module (measured at authoring: exactly one `test_*.py` file, in `src/`). **Do NOT add a second run to satisfy the word `full`.** **Compute the baseline yourself from `--collect-only` and reconcile against the most recent prior QA in `knowledge/qa/`.** Confirm 0 regressions. ⚠️ **Report the count as `<N> passed` and nothing else** — see the hedging-keyword trap above.
> 2. `get_unclassified_entries(conn)` on canonical returns `[]` (or exactly a Step-1 `### Deferred entries (CEO-approved)` list — quote both heading and query output).
>
>    ⚠️ **`[]` SURVIVES BOTH IN-WINDOW EVENTS THIS PLAN LICENSES, and that is checked rather than assumed:** the helper excludes an entry while it carries a **non-stale** proposal, so **Gate 1 setting `route` with `status='proposed'` leaves it excluded**, and **a Gate-2 flip to a terminal status leaves it excluded too.** Neither legitimate event can make this row fire.
>
>    ⚠️⚠️ **BUT A NON-EMPTY RESULT HAS EXACTLY ONE DIAGNOSIS AND IT IS NOT "WORK REMAINS" — IT IS THE STALING SIGNATURE.** The one state that returns ids here is a concurrent ingest **staling this cycle's proposals**, which un-excludes their entries and puts all sixteen back on the work list. ⚠️ **That is the same corruption row 3's status clause names, and this row previously offered no diagnosis for it** — a bare `❌` reading "sixteen ids returned" would send the verdict-gate reader hunting for unfinished classification work when the actual event is corpus damage. **So: report the ids, mark `❌`, and state explicitly that a non-empty result on a completed run indicates this cycle's proposals were staled — then cross-reference row 3's status clause and row 4's `stale` comparison, which measure the same event from the other side.** **The `.backup` is the recovery point.**
> 3. **Invariants** on canonical, scope = **the sixteen proposal ids recorded in Step 1's Receipt** (NOT `WHERE entry_id > 198` — C9). Over exactly that set (**expect 16 rows**): **dangling `entry_id` 0**, ⚠️ **`route IS NULL` — adjudicated DIRECTIONALLY per the in-window rule above** — **and every one `status IN ('proposed','ambiguous')` — ADJUDICATED DIRECTIONALLY, for the same reason `route` is.** A move to **`stale`** is corruption → **`❌`** always. A move to a TERMINAL status is a **CEO Gate-2 disposition landing in-window** → **`✅` with a note naming the ids.** ⚠️ **The status assertion is load-bearing for the Step-1→Step-3 isolation window:** a concurrent cycle's ingest between steps would stale THIS cycle's proposals, and a staled row is still 16 rows with `route IS NULL` — so without this clause the row PASSES on corrupted state.
>
>    Target fields — **for each NON-`ambiguous` proposal**: `target_layer='governance'`, and **`target_artifact` ∈ {`PLANNER_TEMPLATE.md`, `DRAFTING_CYCLE.md`, `RULE_20_SELF_CHECK_BLOCK.md`}** — a membership bound, not merely NOT NULL. ⚠️ **This must NOT be reduced to a NOT-NULL check.** The bound is load-bearing because **`target_artifact` is bare `TEXT` with no CHECK** (`src/db.py:42`, verified) and the live corpus already contains free-text drift — measured at authoring: `runner.py` AND `bellows/runner.py` both appear, alongside `bellows.py` (3) and `FORGE_QA.md` (1).
>
>    ⚠️⚠️ **"invalid category 0" AND "invalid confidence 0" ARE STRUCK FROM THIS ROW — THEY COULD NOT FAIL, AND C12 IS THE CONSTRAINT THEY VIOLATED.** Executed against a throwaway copy of canonical at authoring: `category='bogus'` and `confidence='bogus'` are **both rejected with `IntegrityError`** by the CHECK constraints at `src/db.py:35` and `:39`, so no input reaching this row can carry an invalid value in either column. **Asserting them consumed two of the row's operands and told the reader nothing.** ⚠️ **`dangling entry_id` is RETAINED and is genuinely failable: `PRAGMA foreign_keys` returns `0` on this database, so a dangling `entry_id` IS accepted by an insert.** ⚠️ **The first test of that appeared to reject it — the actual exception was `NOT NULL constraint failed: lesson_proposals.proposed_at`, a malformed test rather than an enforced foreign key. Reading the exception TEXT rather than the outcome is what separated a real guard from a struck one** (entry 210: print what matched). ⚠️ **These two were siblings of the schema-enforced category bound struck below, in the SAME SENTENCE, and the fold that opened C12 did not sweep them** — Checklist #26, committed by the fold that created the constraint it breaks.

>    ⚠️⚠️ **THE C12 SWEEP HAS NOW BEEN EXECUTED ACROSS EVERY COLUMN-LEVEL ASSERTION IN THIS PLAN, NOT MANDATED — and it comes back clean apart from the two struck above.** The test is whether an assertion is **strictly narrower than the column's CHECK** (or the column has none); anything not narrower cannot fail. Measured against `src/db.py` at authoring:
>
>    | assertion | column CHECK permits | narrower? |
>    |---|---|---|
>    | `target_layer = 'governance'` | NULL + 4 values (`:41`) | **YES — failable** |
>    | `status IN ('proposed','ambiguous')` | 8 values (`:40`) | **YES — failable** |
>    | `route IS NULL` | NULL + 3 values (`:44`) | **YES — failable** |
>    | `target_artifact` ∈ 3 artifacts | bare TEXT, **no CHECK** (`:42`) | **YES — failable** |
>    | ~~`category` valid~~ / ~~`confidence` valid~~ | exactly the asserted set (`:35`, `:39`) | **NO — struck above** |
>
>    ⚠️ **§2.7 requires a subtractive trim to carry the same whole-artifact sweep an addition would need; this is that sweep, and its result is recorded rather than promised.** **Four assertions survive with a demonstrated failure mode each; two were removed with a demonstrated absence of one.**
>
>    ⚠️ **FIRST, COUNT THE DISPOSITION LINES.** Step 1's `#### Scout dispositions` section must carry **exactly one line per proposal in the recorded sixteen-id set (expect 16)**, each matching one of the two fixed formats. **A count below the proposal count is `❌`, naming the proposal ids with no line.** ⚠️ **Without this count the Rule 58(3) remedy is not installed at all** — the rest of this row is divergence-triggered, so an agent emitting only divergence lines would pass every check, leaving intact the exact "agreeing is the low-effort path" bias the section exists to remove. ⚠️ **Count it with `grep -Fc --` and report the exit code** — the lines begin `- proposal`, a leading dash with no metacharacter, which a metacharacter-only rule misses entirely (entry 204).
>
>    ⚠️⚠️ **THEN: THE DISPOSITION TURNS ON WHETHER THE DIVERGENCE WAS RECORDED, NOT ON MEMBERSHIP — otherwise this row contradicts C5 and traps the agent.** Step 1 grants an explicit Rule-58 licence to place an entry outside the scouted set and record it. A row that marks any fourth artifact `❌` **gate-fails the plan on an outcome it just licensed.** So adjudicate:
>    - `target_artifact` **inside** the three-artifact set → `✅`.
>    - **outside** it, **AND** the `#### Scout dispositions` section of Step 1's **dev-log Output Receipt** (item 9) names that exact proposal id and that exact target, **AND the named target actually exists** — ⚠️⚠️ **but the root-repo `cat-file` form CANNOT resolve a submodule path.** Measured: `git -C <root> cat-file -e HEAD:bellows/gates.py` → **exit 128** (`bellows` is a gitlink, so its contents are not in the root tree), while a root file exits 0. **Resolve by repository: a root-repo target via `cat-file -e HEAD:<path>` from the root; a submodule-resident target via `cat-file -e HEAD:<path-within-submodule>` run with `-C` against THAT submodule.** A target that resolves in neither → **`❌`**; a target that resolves → **`✅`**. ⚠️ **The existence check is what keeps this from being self-authorizing:** the same agent makes the placement and writes the divergence line, so without it a typo or an invented artifact passes on the strength of its own log entry — and `target_artifact` is the one column with no schema constraint.
>    - outside it with **no recorded divergence** → **`❌`.** *That* is the defect this bound exists to catch — indistinguishable from a typo.
>
>    **⚠️⚠️ CATEGORY — A THREE-PART BOUND, AND THE THIRD PART IS A MEASURED WIDENING, NOT A RELAXATION.** Prior cycles asserted a two-tag partition. **This batch carries FIVE tag values and the corpus has classified only two of them before** (measured at authoring against `lesson_entries.tags`). The premise that changed is measured, so the bound changes with it:
>    - every entry whose `lesson_entries.tags` contains **`planner-discipline`** (expect **5**) → `category` MUST be `governance_rule`.
>    - every entry whose `tags` contains **`bellows-integration`** (expect **7**) → `category` ∈ {`governance_rule`, `instrumentation`}. **`instrumentation` ALSO pairs with `target_layer='governance'`** — **10** such rows live in this corpus, re-counted at authoring — so the layer check does **not** police category.
>    - every entry whose `tags` contains **`drafting-cycle`** (expect **2**), **`verification`** (expect **1**), or **`process-discipline`** (expect **1**) → `category` ∈ {`governance_rule`, `instrumentation`, `structural`, `narrative`} — **a four-value bound, and `language` and `duplicate` are both `❌`.**
>
>      ⚠️⚠️ **AN EARLIER WORDING HERE READ "ADR-002 validity only: `category` ∈ the five real categories" — AND THAT CLAUSE ASSERTED NOTHING.** `category` is **CHECK-constrained at `src/db.py:35`** to exactly those five plus `duplicate`, and the classification instructions separately forbid hand-assigning `duplicate` — **so "must be one of the five" is enforced by the schema before this row ever runs, and for 4 of 16 proposals the category check could not fail.** Its own closing sentence made that explicit by routing the result to Gate 1 *"rather than as a pass/fail"*. ⚠️ **That is entry 199 of the batch this plan ingests — trading a value-level assertion for a membership check that cannot see the value — committed in the row written to honour entry 199, and found by the walk-1 Destruction lens.**
>
>      **What replaces it is measured, not invented.** The corpus uses only FOUR of the five categories — `governance_rule` **162**, `instrumentation` **10**, `structural` **10**, `narrative` **5**, and **`language` has ZERO uses corpus-wide** (measured at authoring; `duplicate` **19** is detector-owned and never hand-assigned). **So excluding `language` and `duplicate` is a bound the corpus supports, and it is failable.**
>
>      ⚠️ **AND THE VALUE-LEVEL HALF, which a membership bound still cannot supply:** for each of these four proposals the `#### Scout dispositions` line MUST carry a `reason:` that **quotes or names specific text from that entry's own `raw_content`**. **A `reason:` that is empty, generic, or cites only the tag is `❌`, naming the proposal id.** ⚠️ **This is the part that actually polices the choice** — three of these four tags have no corpus precedent (`drafting-cycle` and `verification` have ZERO prior entries; `process-discipline` has exactly ONE, entry 134 → `instrumentation`), so the classification must be argued from the entry rather than pattern-matched, and **these four are the rows that establish the precedent for every future batch carrying these tags.** Report all four by id.
>    ⚠️ **This bound applies ONLY to proposals whose `status` is NOT `ambiguous`** — `ambiguous` is a **status**, not a category (`src/db.py:40`), so such a proposal still carries one of the five real categories.
>    ⚠️⚠️ **Partition BY THE TAG YOU READ FROM THE ROW, never by a predicted id.** **Report the tag counts you measure; if they are not 5 / 7 / 2 / 1 / 1, that is the finding.**
>
>    ⚠️ **The stored tag values carry LITERAL BACKTICKS** — the column holds `` `planner-discipline` `` (verified at authoring: every one of the sixteen is backtick-wrapped). **An equality predicate returns ZERO rows and makes this whole bound vacuous.** Use `WHERE tags LIKE '%planner-discipline%'` and **report the matched row count so a zero-match derivation is visible** (entry 210: print what matched).
>
>    ⚠️ **Note what this row does and does not police:** it enforces the membership + recorded-divergence rule, but does **not** verify that an entry was mapped to the RIGHT one of the permitted artifacts — a 208↔209 swap is two in-set values and passes by design. That mapping is a Gate-1 CEO decision, informed by the recorded divergences and by row 9.
>
>    An **`ambiguous` proposal PASSES this row — exempt from BOTH the target checks AND the category bound** (NULL targets allowed; any valid ADR-002 category allowed). Report any `ambiguous` by id. **A scoped count ≠ 16 is a FAIL.** Cross-check `SELECT COUNT(*) FROM lesson_proposals WHERE id IN (<the sixteen recorded proposal ids>)` == 16. ⚠️ **NOT `WHERE id > 206`** — C9.
> 4. **The plan-204 fix held.** Read the pre-cycle baseline from Step 1's dev log — **if missing, mark this row `❌` (unverifiable)**: it is the backstop for the documented staling-corruption signature and MUST fail closed. Re-read from canonical and diff: `stale` not grown. ⚠️ **The `stale` baseline is a before-anchor and C4 governs it: on a RESUME take it from the stub or the prior Receipt, NEVER from this dispatch's live re-capture.** Planner's measured pre-cycle value: **3**. Also confirm no proposal moved off a terminal status, and **entry id 198's `content_hash` unchanged** (still `28e19e1b…`). Report `updated_count` + `terminal_proposals_flagged` **from the Receipt's `#### First-dispatch ingest dict` if that section is present** (a resume is then in evidence); otherwise from item 1. ⚠️ **If a resume is in evidence and NO first-dispatch dict was preserved, this cell is `❌ (unverifiable)`.** **Write the raw output to `hash-trap.txt`.**
>
>    ⚠️⚠️ **THIS ROW CARRIES THE WHOLE STALING-DETECTION BURDEN, because the per-row comparison ladder is deliberately absent** (its operand, the non-terminal set, was empty at baseline; an empty-vs-empty comparison reports `✅` having examined zero rows). **So the `stale`-count check here is not a secondary backstop — it is the primary instrument.** ⚠️⚠️ **AND A COUNT IS NOT A VALUE GUARD — entry 199 of the batch you are ingesting says so, and this row is the site.** A `stale` count that is unchanged cannot see a proposal moving between two *other* statuses. **So state THREE things as printed numbers, not one:** (i) `stale` before, (ii) `stale` after, (iii) **the full `status` distribution before and after, with THIS CYCLE'S OWN DELTA SUBTRACTED before comparing.**
>
>    ⚠️⚠️ **STATE THE EXPECTED DELTA OR THE COMPARISON FLAGS EVERY CLEAN RUN.** This cycle legitimately adds **16 rows at `status='proposed'`** and changes nothing else. **So the expectation is exact and failable:** **`implemented` 153, `superseded` 28, `rejected` 15, `reference` 7 and `stale` 3 are each UNCHANGED** (Planner's measured pre-cycle distribution — confirm against Step 1's Receipt item 3 rather than adopting these literals, Checklist #29), **and `proposed` equals the classified count after.** **A change in ANY bucket other than `proposed`, or a `proposed` value other than the classified count, is `❌` and names the bucket.**
>
>    ⚠️⚠️ **`proposed` IS ABSENT FROM THE "BEFORE" CAPTURE — IT IS NOT A ZERO, AND AN EARLIER WORDING SAID `0 → 16`, WHICH NO OUTPUT WILL EVER SHOW.** Measured at authoring: `SELECT status, COUNT(*) FROM lesson_proposals GROUP BY status` returns **five rows and no `proposed` row at all**, because `GROUP BY` omits zero-count groups. **So the before/after comparison sees a bucket APPEAR, not a value change**, and a reader diffing bucket-by-bucket on the literal `0` would find nothing to compare it to. **Adjudicate as: `proposed` absent before, present after with the classified count; the other five buckets present in both and pairwise equal.** ⚠️ **Better still, remove the ambiguity at the source: capture the distribution with a form that emits zeros explicitly** — e.g. select over the enumerated status list with a `LEFT JOIN`/`COALESCE` so every legal status prints a number — **and record that printed set (C8: a bucket's absence and a failed query look identical).** ⚠️ **The `0 → 16` wording was itself a fold made one pass earlier to make this row passable, and it named a value the instrument does not produce.** ⚠️ **An earlier wording said only "compared row by row" with no expected delta — which reports a difference on every correct run, i.e. a check that cannot pass. It was added in the same session to honour entry 199 and was not itself checkable; that is entry 205's thesis (the edit phase manufactures defects even when the edit is a REPAIR) landing on the repair.** ⚠️ **Do NOT mark this row `✅` on an empty result set: state the count of proposals examined.** A row that cannot say how many rows it looked at has not run.
> 5. **Report** exists. ⚠️ **ADJUDICATE UNDER THE IN-WINDOW RECONCILIATION RULE ABOVE — this row is whole-corpus.** **(a) HARD:** all **16** of this cycle's proposals — **identified by the sixteen proposal ids recorded in Step 1's Receipt, NOT by `entry_id > 198`** — are present and surfaced. **(b) RECONCILE:** any surfaced proposal outside that set is listed by id as a reconcile-note in the measured-value column, **not a `❌`**. ⚠️ **Read the pre-ingest `NT_COUNT` from Step 1's receipt — specifically `NT-original` when the Receipt carries it, otherwise `NT`; NEVER `NT-now`, which on a resume is legitimately 16 — and confirm it is 0 before applying that expectation.** ⚠️ **If both labels are present and you cannot tell which is the pre-ingest capture, that is `❌ (unverifiable)`, not a guess**; if the receipt carries no parseable `NT` label, mark this row `❌ (unverifiable)`. ⚠️ **Route lines: adjudicate DIRECTIONALLY, exactly as Step 2 and row 3 do.** **Count route lines with `grep -Fc --` and the exit-code rule Step 2 mandates; a bare `grep` errors to empty output here.**
>    ⚠️ **The report prints neither proposal id nor entry_id.** **Use the report's own `**Total proposals:** N` line for the count**, and correlate every `### ` heading back to ids via the DB join in Step 2. State the correlation in the evidence file — a bare "16 surfaced, 0 routes" without the heading→id mapping does not distinguish the right sixteen from a wrong sixteen. Zero `Recently-implemented overlap:` lines; `detect_recently_implemented_overlaps` still absent from `src/`.
> 6. **No schema drift** — compare the live schema of `lesson_entries` / `lesson_proposals` against `src/db.py`'s DDL **SEMANTICALLY** (column names + types + NOT NULL + PK via `PRAGMA table_info`, plus the CHECK/UNIQUE constraint set), **NOT a literal `.schema`-text diff**. ⚠️ Cosmetic artifacts of a past RENAME migration — a quoted `"lesson_proposals"`, `IF NOT EXISTS` differences, whitespace, index ordering — are EXPECTED and NOT drift. A FAIL is a real column/type/constraint difference. **Deposit the raw `.schema` output for both tables to `schema.txt`.**
> 7. **The Gate-2 target artifacts are UNCHANGED by this cycle.** ⚠️ `lessons-forge` is a submodule; these files are tracked by the ROOT repo and do NOT exist in your worktree — a plain `git diff` passes VACUOUSLY (Rule 55(a), `:1075`). Run against root, ONE invocation, capturing the exit code. **⚠️ This command goes in a fenced block, not a table cell (C11):**
>    ```
>    git -C /Users/marklehn/Developer/GitHub status --porcelain -- DRAFTING_CYCLE.md PLANNER_TEMPLATE.md RULE_20_SELF_CHECK_BLOCK.md; echo "PORCELAIN-EXIT=$?"
>    ```
>
>    ⚠️⚠️ **REPORT THIS ROW AS TWO NAMED SUB-CHECKS, because it carries TWO DIFFERENT PIN JOBS and an undifferentiated `❌` tells the verdict-gate reader nothing about which fired.** **This is Rule 61 (`:1115`) — shipped by plan 291 from this very lineage — applied to the plan that ingests its successors.** **7a — UNCHANGED IN THIS WINDOW (fail-closed guard):** the porcelain probe above. **7b — UNCHANGED SINCE AUTHORING (drift detection):** `shasum -a 256` the three files and compare against **Step 1's Output Receipt item 10, `#### Doctrine pins`** — ⚠️ **which reproduces, verbatim, the ONE measurement taken in the Step-1a-ter stub at position 2. G2 does not measure the pins and neither does item 10; there is a single source of truth and this row reads its published copy.** ⚠️ **If item 10 is absent or unparseable, 7b is `❌ (unverifiable)` — NOT a pass; a missing baseline cannot demonstrate an absence of drift.** ⚠️ **And make the comparison self-evidencing (C8): print BOTH the three live hashes and the three recorded ones, and state the three pairwise verdicts.** `shasum` on a missing file prints nothing to stdout and exits non-zero, otherwise indistinguishable from a comparison you report as matching. **Both remain FAIL-CLOSED and neither is adjudicated by you** — but a `❌` must say **7a** or **7b**, because they demand opposite responses: 7b means the plan's own premises moved and the Planner must re-verify them; 7a means a file was touched mid-flight and the CEO must say whether it was theirs.
>
>    ⚠️⚠️ **BOTH HALVES OF 7a ARE PASS CONDITIONS: EMPTY OUTPUT *AND* `PORCELAIN-EXIT=0`.** A failed invocation prints **NOTHING to stdout** and returns non-zero, and empty stdout is byte-identical to the genuine no-change result this row treats as success. **So a non-zero exit is `❌ (check did not run)`, reported distinctly from `❌ (doctrine changed)`.** ⚠️ **The `-C` is REQUIRED and its absence is a SILENT PASS:** you are running from your lessons-forge worktree, where these three files **do not exist at all**, so without `-C` git reports nothing about them and exits 0.
>
>    ⚠️⚠️ **NON-EMPTY PORCELAIN IS `❌`. FULL STOP. THIS ROW FAILS CLOSED AND YOU DO NOT ADJUDICATE IT.** ***"There is no other guard; this row IS the guard"*** — `scope_check` is cwd-scoped and cross-repo-blind, so nothing else in the system can see an edit to a root-repo doctrine file. **You may not mark this row ✅ on the reasoning that the commit looks unrelated, pre-existing, benign, or someone else's — that judgment is not yours to make.** **The legitimate concern — a CEO editing `DRAFTING_CYCLE.md` in-window to draft Gate 2 — is handled by ATTACHING EVIDENCE, not by grading your own attribution:** capture `git -C /Users/marklehn/Developer/GitHub log --oneline b4b7bad..HEAD -- <the three files>` and `git -C … diff -- <the three files>` into `invariants.txt` **before halting**, mark the row `❌`, and let the CEO adjudicate at the verdict gate. ⚠️⚠️ **AND PIN WORKING-TREE CONTENT, NOT THE HEAD BLOB — `git rev-parse HEAD:<path>` IS BLIND TO THE CASE THAT MATTERS MOST.** It resolves the blob in HEAD's **tree**, not the file on disk, so an **uncommitted** edit passes a HEAD-blob pin. **Use `shasum -a 256` on all three files.** ⚠️ **Without 7b the comparison has one operand and the recorded pins are dead data:** `status --porcelain` is empty on a clean tree, so a *committed* `DRAFTING_CYCLE.md` edit during the Gate-1 window passes the porcelain check and is caught only by the pin. ⚠️ **`RULE_20_SELF_CHECK_BLOCK.md` is in this check because it is a scouted target for entry 203 AND because Step 3 copies it at run time (Rule 61). `bellows/scripts/plan_lint.py` AND `bellows/gates.py` are deliberately NOT checked:** this cycle has no write path to the bellows repo, so a change there could not be this cycle's doing. ⚠️ **That exclusion is an argument about WRITE PATHS, not about importance** — do not extend it to the three root files, which this cycle also cannot write but which no other instrument can see. Raw output to `invariants.txt`.
> 8. **Post-cycle DB counts.** ⚠️ **ADJUDICATE UNDER THE IN-WINDOW RECONCILIATION RULE ABOVE — these are whole-corpus aggregates.** **(a) HARD — the owned delta:** **assert over the SIXTEEN entry ids and SIXTEEN proposal ids recorded in Step 1's Receipt, by id** — `SELECT COUNT(*) FROM lesson_entries WHERE id IN (<the sixteen recorded entry ids>)` = **16** and the same by proposal id. **This is the part that is absolute.** ⚠️⚠️ **DO NOT use `WHERE id > 198` / `WHERE entry_id > 198` here** — the predicate means "everything ingested after authoring", so one foreign in-window entry makes both counts 17 and this absolute half a `❌` on a correct run. **(b) RECONCILE — the totals:** the Planner's derivation is `198 + 16 = 214` entries and `206 + 16 = 222` proposals — ⚠️ **both are Planner measurements, not gate values: verify each against your own count and EXPLAIN any difference rather than forcing it** (Checklist #29, `:1329`; these were the only two predicted numbers in this plan carrying no adjacent verify-and-explain clause, and every other one does). **Report the ACTUALS.** A total above the derivation **with the owned delta correct** means something landed in-window: name the foreign ids and record a reconcile-note, **do not `❌`**. A total that differs **while the owned delta is wrong** is a real failure. Also report proposals by status + category as actuals. Raw output to `invariants.txt`.
> 9. **Classification depth spot-check.** Scope **the sixteen recorded proposal ids** (NOT `entry_id > 198`); for each confirm its `reasoning` genuinely quotes its entry's `raw_content`. **Use the EXTRACTION-FREE check**: (a) `canon(s)` = normalize curly→straight quotes, strip `*_`` ` chars, collapse whitespace, lowercase; (b) longest common substring via `difflib.SequenceMatcher(None, a, b, autojunk=False).find_longest_match(...)`; (c) **PASS if the match is >= 40 chars AND the match is < 80% of `canon(reasoning)`'s own length — PER PROPOSAL, not on average and not on a majority.**

>    ⚠️⚠️ **TWO SIMILARLY-NAMED FIELDS PULL IN OPPOSITE DIRECTIONS AND THEY ARE DIFFERENT ARTIFACTS.** The **DB `reasoning` COLUMN** (measured by this row) must cite its entry but is penalised by the ceiling below if it is *mostly* quotation. The **dev-log disposition line's `reason:`** (Receipt item 9) must **quote or name specific text** from the entry, with a generic value an `❌` at row 3 — **and carries no ceiling at all**, because it is one line justifying a placement, not a paragraph arguing a classification. ⚠️ **They are one word apart and were mandated several folds apart; conflating them produces a heavily-quoting `reasoning` that satisfies the disposition rule and fails this one.** ⚠️ **This distinction is ALSO stated in Step 1, where both fields are written — a rule that lives only in the verifying step cannot be complied with, which is a class this cycle re-tripped three times.**
>
>    ⚠️⚠️ **THE CEILING IS AS LOAD-BEARING AS THE FLOOR, AND AN EARLIER VERSION HAD ONLY THE FLOOR.** A `reasoning` field that is *mostly* one long quotation scores a very high match and sails past a 40-char floor — **while being precisely the un-thought classification this row exists to catch.** The floor catches a field that cites nothing; **the ceiling catches a field that is nothing BUT a citation.** ⚠️ **Report the RATIO alongside the absolute length for all sixteen** (`match_len / len(canon(reasoning))`), **because the ratio is the number that distinguishes an argued classification from a paste** and the absolute length cannot. **A proposal failing either bound is `❌`, naming which bound and the measured pair.**
>
>    ⚠️ **THE MEASURED RANGE, recorded beside the threshold as §2.7 requires — a threshold without its range is an assertion, not a calibration.** Both bounds were executed at authoring against **the six real proposals of plan 288's batch (entries 193–198)**, the closest available analogue to what this cycle will produce: `len(canon(reasoning))` **391–493**, longest match **62–115 chars**, **ratio 0.13–0.26**. **0 of 6 would fail either bound.** The 40-char floor sits well below the observed minimum and the 80% ceiling roughly **three times above the observed maximum** (`0.80 / 0.26 = 3.08`) — deliberate margin, since the ceiling exists to catch a near-total paste rather than to police style. ⚠️ **The control is stronger than it looks: 288's proposals were written with NO ceiling in force, so they were not optimising for the bound they are being used to calibrate.** ⚠️ **Report your sixteen ratios against this range; a batch clustering near 0.80 is a finding about the classification work even if every row passes.**
>
>    ⚠️⚠️ **THIS ROW IS THIS PLAN'S ONLY INSTRUMENT FOR THE SCALE RISK, AND ITS FLOOR IS DELIBERATELY PER-ITEM.** Sixteen is twice the largest batch this mechanism has run, and the predicted failure mode is a thin classification in the TAIL, not an error anywhere. **A set-level or averaged pass would hide exactly that.** **Report all sixteen measured longest-match lengths as numbers, in id order** — the ordering matters because a monotone decline across the list is itself the finding, independent of whether any single value clears the floor. **Any single proposal below 40 chars is `❌`, naming that proposal id.** (Measured at authoring: the sixteen entries' `raw_content` is **854–1834 chars**, so the 40-char floor cannot false-FAIL on length.)
>
> **Write the raw output of rows 0, 2, 3, 5, 7, 8 and 9 to `invariants.txt`; row 4 to `hash-trap.txt`; row 6 to `schema.txt`; row 1's pytest tail to `pytest_targeted.txt`.** These are the four `required_evidence_files` the Rule 20 block verifies non-empty. ⚠️ **Non-empty is ALL the block checks** — a one-byte file passes it. So the block's `PASSED` is not evidence your evidence landed. **Before running it, self-grep each of the four files for a marker proving the intended content reached it** — `PORCELAIN-EXIT=` in `invariants.txt`, the entry-198 `content_hash` prefix in `hash-trap.txt`, `CREATE TABLE` in `schema.txt`, and the pytest summary line in `pytest_targeted.txt` — **using `grep -F`, and print what matched, not merely PRESENT/ABSENT** (entry 210 of the batch you are ingesting). A missing marker means a truncated or misdirected write: fix it before the block runs, and if it cannot be fixed, mark the owning row `❌`.
>
> **Deposit:** `knowledge/qa/cycle-qa-2026-08-03.md` — the table under `## Verification Table` with the mandated columns, raw suite tail, the Rule 20 banner + PASSED line + literal stdout (passing runs only), Output Receipt — plus the four evidence files. Canonical Python file-write — no heredoc. **Commit all deposits by EXPLICIT PATHSPEC** — `git add <the exact paths>` then `git commit -m "…" -- <the exact paths>`. **NEVER `git commit -a`, and never an unscoped `git add -A` / `git add .`.** ⚠️ **`git add` FIRST — load-bearing for NEW files:** `git commit -m "…" -- <untracked path>` fails with `error: pathspec … did not match any file(s) known to git`. Your evidence files are new. **If you hit that error the fix is to `git add` the path and retry — NEVER to fall back to `git commit -a`.**
>
> In `### Ledger Updates` include:
>
> `#### Project Status` ⚠️ **(state the milestone SCOPED to this cycle's sixteen — never as a bare corpus-wide `proposed = N`. QA row 5(b) explicitly permits a foreign in-window proposal to exist at close, so an unscoped milestone deposits a PERMANENT status record asserting something this plan's own guard allows to be false.)** One milestone: cycle 2026-08-03 complete — the 16-entry session-16/17 batch ingested + classified, report deposited, corpus integrity held, the non-terminal baseline confirmed empty at G1, and this cycle's sixteen the only non-terminal rows this plan owns at close; Gate 1 route disposition pending for the sixteen.
>
> `#### Forward Register` — ⚠️ **Per Rule 46 (`:1012`) a tooling defect or request is routed to the project's `FORWARD.md` through THIS section; the daemon appends the rows post-merge and agents never write `FORWARD.md` directly.** ⚠️⚠️ **THIS PLAN IS `lessons-forge`-DISPATCHED, SO THIS BLOCK REACHES `lessons-forge/knowledge/FORWARD.md` — which currently holds ONE row. That is why the first item below has waited: it belongs to this register and no lessons-forge plan has run since the register was created.** **Record THREE items, described and NOT quoted, ONE PER BULLET LINE.** ⚠️ **One bullet per item is a shipped capability: the daemon's item splitter emits one register row per bullet when a block carries two or more bullet lines, and falls back to first-line-only below that.**

> ⚠️⚠️ **BUT SAY WHAT IS AND IS NOT PROVEN HERE, BECAUSE THIS BLOCK IS A FIRST.** Measured at authoring: `lessons-forge/knowledge/FORWARD.md` holds **one row**, appended by the daemon at **2026-08-02T13:30:06** — i.e. **BEFORE the splitter shipped at 2026-08-03T07:33:24** (bellows `eefd2a9`). So that row was written by the pre-splitter code as a single item. **The splitter has been proven live on `bellows`'s register; it has NEVER run against THIS project's register.** The daemon now serving this plan (started after that commit) carries the change, so the capability is live — **but this three-bullet block is the first multi-item block this register will ever receive.**
>
> **State the before-count in your Receipt** — ⚠️ **and NAME THE TREE YOU READ IT FROM, because this plan pins every other cross-tree read to an absolute path and this one was left unstated.** Read `knowledge/FORWARD.md` **from your own worktree**, and record that you did. **The worktree copy is a frozen HEAD snapshot, and that is the CORRECT before-value here** — the daemon appends to the MAIN tree post-merge, i.e. strictly after this step ends, so the snapshot cannot already contain your own rows. ⚠️ **Say that reasoning out loud rather than relying on it:** Rule 55(b) (`:1075`) is this plan's own standing warning that a worktree is structurally blind to main-tree lifecycle state, and here that blindness is the property being used, not a hazard being tolerated. **Expected count: 1.** **Report the number you actually read** — entry 214 of the batch you are ingesting is precisely this discipline (*state the before-count before dispatching, or the after-count proves nothing*). ⚠️ **And note what a ONE-row outcome would mean: not a malformed block, but the splitter failing to apply on this project's path.** That is a diagnosable, reportable result, not a reason to hand-write rows — **agents never write `FORWARD.md` directly.** The three:
> 1. `generate_lessons_report` writes its output file with no explicit encoding argument — safe on this UTF-8 host, unsafe on a cp1252 one. **This is the last item of the original six-item backlog and the only one never emitted.**
> 2. `detect_duplicates` returns an empty list when its reference file cannot be read — the read is wrapped in an exception handler that continues, followed by an empty-contents early return — **so "no duplicates found" and "never scanned" are the same value**, and the cycle's duplicate gate passes identically in both cases. Verified by reading the function at authoring.
> 3. `run_full_lessons_cycle` omits the staled-proposal count from its return dict, although the ingest computes it internally — **so no caller can measure how many proposals an ingest staled**, which is why the cycle's hash-trap check is a post-mutation detector rather than a guard. Verified: computed inside the ingest, absent from the cycle's returned keys.
>
> ⚠️⚠️ **ALL THREE ARE `lessons-forge`-OWNED CODE, AND THAT IS DELIBERATE — AN EARLIER DRAFT'S ITEMS 2 AND 3 WERE `plan_lint` REQUESTS AND COULD NOT HAVE REACHED A REGISTER THAT WOULD SERVE THEM.** Read at authoring rather than assumed (**entry 201 of this batch is the rule**): the daemon calls `_append_forward_row(project_path, …)` and `project_path` is **this plan's project** — so every item in this block lands in `lessons-forge/knowledge/FORWARD.md`, unconditionally. **`plan_lint` lives in the bellows repo**, so those two items would have been filed where nobody working on that tool looks — **the same failure shape as the original six-item backlog: a correctly formatted emission to a destination that cannot serve it.** ⚠️ **The replacements are not filler: both were measured by this plan's own authoring, both are real defects in this project's code, and neither had ever been filed.**
>
> ⚠️ **The two `plan_lint` requests are NOT discarded — CEO decision 2026-08-03: DEFERRED TO THE SESSION WRAP for routing to a bellows-dispatched plan.** They are (a) a lint asserting every verification-table status cell is exactly one permitted glyph token, rejecting both a third value and an annotated cell — **two entries in this batch describe its two halves**; and (b) a lint warning when a plan mandates a zero-or-empty-expectation check that carries no printed token and no exit-code capture — ⚠️ **which must be filed as an explicit EXTENSION of bellows `FORWARD.md` row 25 (2026-08-02, plan 292), naming it.** Measured: row 25 covers a plan that declares no expected WARN/FAIL class; (b) covers an individual check lacking a positive signal. **They are genuinely distinct, and Rule 44 (`:1002`) requires the relationship be stated at filing or the next reader closes one as a duplicate of the other.**
>
> `#### Prompt Feedback`.
>
> **Deposits:**
> - `lessons-forge/knowledge/qa/cycle-qa-2026-08-03.md`
> - `lessons-forge/knowledge/qa/evidence/cycle-session-16-17-captures-2026-08-03/` (multiple files per Rule 20 self-check)
>
> ⚠️ **The evidence DIRECTORY is one bullet; the four filenames are deliberately NOT listed here.** Rule 26 (`:808`): *"Evidence files (Rule 18) MUST be represented by the evidence directory as a single bullet… Do NOT list individual evidence files… duplicating them in `**Deposits:**` is forbidden."* It matters here because this step may halt mid-table and commit only the deposits that exist — a declared-but-unwritten `schema.txt` would produce a false gate failure on an honest halt. The filenames remain in `**Scope:**` and in `required_evidence_files`, where Rule 26 says they belong. **Deposit resolution accepts a directory (`isfile() or isdir()` at every gate strategy), which is what makes the bullet safe.**
>
> **Do NOT move this plan to `Done/`.** The close path is owned by Bellows on continue-verdict consumption — **never by the agent** (Rule 8, `:512`: *"Either way, the agent is not the actor"*). ⚠️ This is unconditional: there is no post-verdict branch in which the agent may move it. An `unauthorized_done_move` force-recovers the file and FAILs this step's gates.

---

## Drafting Cycle
**Tier:** T2 — triggers fired: T-2 (mutates the canonical corpus), T-5 (irreversible ingest), T-6 (feeds a governance surface). A proven-clone framing does NOT down-tier this (§2.6).
**Walks:** 5 warm walks, each followed by its own culmination, plus **five ACID passes** run alone against the merged draft after each. **42 findings across ten passes (9 / 5 / 5 / 5 / 5 / 4 / 4 / 2 / 3).**
- Weak spots:          w1 5 folded; w2 2 folded + 1 verified negative; w3 2 folded (1 HIGH — a QA row undoing the split list built to protect it); w4 2 folded; **w5 DRY — 0 findings, 2 verified negatives.**
- Destruction:         w1 1 folded (HIGH) + 1 verified negative; w2 1 folded (the C12 sweep executed across every column assertion) + 1 verified negative; w3 1 verified negative; w4 1 verified negative (the restore-point block EXECUTED end to end — every claim true, including the failure case that licenses the `?immutable=1` mandate); **w5 DRY — 0 findings, 2 verified negatives.**
- Vulnerabilities:     w1 1 folded; w2 1 folded + 1 verified negative; w3 1 folded; w4 1 folded; w5 1 folded (LOW-MED).
- Integration-record:  w1 2 folded; w2 1 folded (HIGH — two register items addressed to a register that cannot serve them) + 1 verified negative; w3 1 folded + 1 verified negative; w4 1 folded; w5 1 folded (LOW).
- ACID:                w1 5 folded (2 HIGH); w2 5 folded (1 HIGH — the pins region, COLLAPSED to a single capture point rather than patched a fourth time); w4 4 folded (0 HIGH; Atomicity/Isolation/Durability all clean); w5 3 found, 1 HIGH — **AC1 and two others are NOT all folded; see the declared residuals in Closing.**

⚠️ **Attestation provenance (§2.7):** every lens result above was produced and recorded in `scratchpad/walk{1,2}-proposal-register.md` and `walk1-acid-register.md` in turns that ran **before** the folds they report; this block summarises those registers rather than being written alongside the edits.

⚠️ **The measured shape of this cycle so far — 19 findings across three passes, no decay.** Walk 1: 9. ACID on walk 1's merge: 5, of which **four were defects walk 1's own folds introduced.** Walk 2, aimed by rotation at regions no earlier pass had touched: 5, including 2 HIGH, of which **two were unswept siblings of walk-1/ACID folds.** ⚠️ **A severity curve read across walks 1–2 would suggest convergence; the REGION MAP says the opposite, and rotation is what produced walk 2's HIGHs.** Recorded because entries 202 and 208 of the batch being ingested are exactly this measurement, and this plan is now a third instance of it.
**Panel status (T2 cold reader panel):** NOT RUN — the panel has not been convened.

⚠️ **This line is deliberately phrased so §4's check cannot match it, and the phrasing was chosen by EXECUTION, not by reading.** §4's cold-panel check is the line-anchored pattern `^\*\*cold[\s-]+panel` (`plan_lint.py:193`, opened at authoring). ⚠️⚠️ **An earlier version of this block opened with the canonical `**Cold panel (T2):** not run …` and carried a note claiming it was phrased so the check could not match — the check matched it, silently, and the WARN this plan has earned did not fire. The claim was written from reading the rule and was false; running the linter is what found it.** **That is entry 208 of the batch this plan ingests** (*any sentence containing "measured" or "verified" is a claim to re-run, not to read*) **and entry 207** (*a true warning silenced by wording is not a cleared warning*), both committed by the plan that ingests them and both caught the way those entries prescribe. **Switch to the canonical opening ONLY when the panel has actually run.**
**Conflicts:** C1–C13 in the ledger below. **No cross-lens CONTRADICTION has arisen** — no fold has had to be reversed to satisfy a different lens, and nothing has needed joint resolution or escalation. ⚠️ **But entry 199's CLASS was violated by this plan twice, and the two halves land on DIFFERENT constraints — an earlier version of this line attributed the first to C10, which does not reach it.** **C10 as written covers a TRIM that replaces a value-level assertion with a count**; the schema-enforced category bound was an **ADDITION** that merely looked like a bound, and **C12 was opened in response to it** — so the bound violated the class, not C10. **C12 was then breached by two sibling assertions one clause away, inside the sentence the C12-opening fold had just edited.** ⚠️ **Both constraints were opened from the batch's own entries and both classes were breached by the author who opened them; that is a violation record, not a contradiction, and it belongs here rather than being reported as a clean ledger.** ⚠️ **The mis-citation is itself worth the line: it was "read the cited rule before citing it" committed against a constraint I wrote myself, in the sentence whose whole purpose is to report breaches honestly.** ⚠️ **An earlier version of this line read "no cross-lens conflict has arisen because no lens has run" — true when written, false from walk 1 onward, and it survived the Closing-line correction one screen below because that fold was applied where the defect was NOTICED and not swept to its sibling** (Checklist #26, committed inside the Cycle Log that records it).
**Closing:** ⚠️⚠️ **NOT MET, DECLARED, AND DEPOSITED ANYWAY ON CEO DIRECTION (2026-08-03) — the last event before deposit is a FOLD, not a dry lens pass, and `plan_lint` says so.** ⚠️ **Walk 5's four lenses came back with two LOW findings over an artifact with NO unexamined region** — the genuine convergence condition, since a falling count over already-read ground is not a signal (this batch's own entry 202). **But the walk-5 ACID pass then returned 3 findings including 1 HIGH, and they are not all folded.**

⚠️⚠️ **THE UNFOLDED RESIDUALS, NAMED SO THE VERDICT-GATE READER SEES THEM RATHER THAN DISCOVERING THEM.** Two were folded before deposit because they carry run risk; the rest are declared open:
- **FOLDED before deposit — AC2 (run risk):** the two-bound rule on the DB `reasoning` column lived only in QA row 9, so the step that WRITES the field never saw it. A heavily-quoting `reasoning` would have satisfied every instruction Step 1 was given and failed row 9's ceiling. **Now stated in Step 1 where the field is written.**
- **FOLDED before deposit — AC3:** QA row 0's copy-aside cross-check was a zero-expectation check with no printed token or exit code — C8's own subject. **Now carries the count-and-exit-code form.**
- **OPEN — AC1 (HIGH, record-only, NO RUN IMPACT):** ledger constraint **C13** had been formulated three times and was wrong in both directions. ⚠️ **The §2.8 step-back WAS taken — C13 now reads as a single principle with no list and no axis** — but the restatement has had no lens pass of its own. **C13 is a Conflict-Ledger record and addresses no executing agent, so nothing here can affect the run.**
- **OPEN — W5-5, W5-6 (hardening, not repair):** a sentence noting the Rule 20 block's `isdir` check would pass even on a main-tree `evidence_dir`; and marking G6's `199–214` parenthetical explicitly illustrative rather than authoritative.

⚠️⚠️ **AND ONE HONEST MARK AGAINST THE DRAFTING, RECORDED BECAUSE THE RECORD IS THE POINT: this draft was DESTROYED once during the deposit preparation and restored from backup.** A scripted whole-file edit anchored on `**Closing:**` matched an earlier occurrence in the CEO Context and spliced away every step body — 735 lines to 229. **Nothing outside `scratchpad/` was ever at risk, the backup was taken before the edit, and all eight subsequent edits were re-applied through anchored single-edit operations with a pre-write assertion.** ⚠️ **This is the third occurrence of this exact class in the shop's record and the method that caused it is the one already forbidden by memory `no-whole-file-rewrites-on-live-drafts`.**

⚠️ **`plan_lint` at deposit: exit 0, ten PASS, THREE WARNs — all three EARNED and none to be silenced.** (1) the known-benign test-scope class; (2) **the cold panel has NOT run** — this is a T2 plan and the panel is genuinely owed; (3) **the closing indicates a fold as the last event**, which is true. ⚠️ **Precedent for depositing with §2's closing condition unmet: plans 282 and 287 both did, both with the CEO told and the fact recorded in the plan's own Closing line rather than papered over.** **This is that record.**

⚠️ **Substantive result: 42 findings across ten passes.** The count never decayed while unexamined regions remained; **rotation, not severity, produced every HIGH after walk 1.** ⚠️ **In five of five ACID passes the immediately preceding culmination supplied the majority of what ACID found** — the edit phase is the unreviewed surface, measured again, which is this batch's entry 205. **Deposit-once discipline holds: deposited exactly once.**

**Superseded record of the prior closing state:** the last event on this draft is a FOLD (walk 5's ACID culmination), not a dry lens pass. ⚠️ **An earlier version of this line read "NOT REACHED — this draft has had no lens pass of any kind", which was true when written and became FALSE the moment walk 1 ran.** A record of what has not happened is falsified by doing it; **it must be updated in the same move that discharges it** — corrected here, one walk late.

⚠️⚠️ **`plan_lint` now emits THREE WARNs, and the third APPEARED as a direct result of editing this block — which is the healthy direction of §3's rule and is recorded as a measurement.** At authoring the check was silent on closure: the last lens line read "not yet run", carrying neither a fold-token nor a dry-token, so **a plan with zero walks passed the closing check silently.** With the ACID line now reading "5 folded", the check fires correctly. **§3 mandates re-running the gate after any edit to the log and confirming the WARN/PASS set is unchanged; here it CHANGED, and the change is right** — the artifact's state genuinely moved from "nothing has run" to "the last event was a fold". ⚠️ **A WARN that DISAPPEARS on a log-only edit is the defect §3 names; one that APPEARS because the artifact's state actually changed is the check working.** All three WARNs are earned: the benign test-scope class, the un-run cold panel, and this one.

⚠️ **Per §3 the full per-fold narrative will NOT live in this block** — it belongs in `scratchpad/`.

⚠️ **This block is a RECORD, not instructions** — nothing in it addresses an executing agent.

---

### Conflict Ledger (§2.8)

- **C1** — the empty non-terminal baseline is a *measured premise*, not an assumption: every guard that rests on it must either re-verify it at run time or halt.
- **C2** — no step hardcodes a count it can read from Step 1's recorded capture; literals in this plan are declared Planner measurements, never gate values.
- **C3** — a removal of inherited machinery must be declared in the artifact, with its premise and its run-time guard named.
- **C4** — a resume anchors on the ORIGINAL committed capture, never a live re-read.
- **C5** — a permitted outcome is never a FAIL, **with ONE RECORDED EXCEPTION:** QA row 7 fails closed on any change to the three root doctrine files, because no other instrument can see that change.
- **C6** — a fold that changes a convention is recorded here as a CONSTRAINT, not only applied at the site where it was found.
- **C7** — no pre-ingest instruction may state an unqualified fresh-run claim about a value that differs on a resume; every such instruction is qualified by the step-0 dispatch determination, or it captures and defers. **No enumerated site roster** (288's decayed twice).
- **C8** — every mandated check reports a POSITIVE token or an exit code; no check may be discharged by the absence of output. Binds hardest where the expectation is ZERO or EMPTY. **No enumerated site roster** — applied by inspection at fold time.
- **C9** — any assertion about "this cycle's rows" evaluated AFTER Step 1 names the sixteen recorded ids explicitly; `entry_id > 198`, `id > 198` and `id > 206` are FORBIDDEN as operands past Step 1 for any assertion about what this plan OWNS. **ONE CARVE-OUT:** permitted solely inside a complement (`NOT IN (<the sixteen>)`) whose result is REPORT-ONLY and has no pass/fail path.
- **C10 (new, opened at authoring from entry 199)** — **a check that replaces a value-level assertion with a count over the same scope is a capability loss, and any such trim must construct the change the surviving check is supposed to catch and confirm it fails.** Opened before the first walk because this batch contains the lesson and this plan carries several count-shaped guards.
- **C11 (new, opened at authoring from entries 211/212)** — **no verification row carries a third status value, and no command containing `|` is quoted inside a table cell.** Every status cell holds exactly one glyph; every qualifier lives in the measured-value or evidence column.

- **C12 (new, walk 1, from D1) — A BOUND MUST BE ABLE TO FAIL. Before adding any membership or validity check, name the concrete input that would fail it; if no such input exists because a schema constraint, an upstream instruction, or another check already excludes every disallowed value, the bound asserts nothing and must be replaced by a value-level assertion.** ⚠️ **This is C10 one level up:** C10 covers a *trim* that swaps a value assertion for a count; C12 covers an *addition* that looks like a bound and is already enforced elsewhere. **D1 was found by asking "what input fails this row?" and finding none** — the answer was pre-empted by `src/db.py:35`'s CHECK plus the never-hand-assign-`duplicate` instruction.
- **C13 (new, walk 1, from I1; RESTATED twice — see below) — A CAPTURE THAT A *LATER* STEP, QA ROW, OR RESUME BRANCH MUST READ IS DEPOSITED IN A COMMITTED ARTIFACT. A VALUE CONSUMED WITHIN THE STEP THAT PRODUCES IT NEEDS NO HOME.** A capture that claims a later reader and has no deposited home is **deleted, not given one**; whether the job has another owner is established per job against the artifact, not in aggregate (§2.7).
  ⚠️⚠️ **THIS CONSTRAINT WAS FORMULATED THREE TIMES AND WAS WRONG IN BOTH DIRECTIONS TWICE. The record is kept because the oscillation is the finding.** (1) Walk 1 opened it as a READER LIST — *"a Receipt item, a numbered QA row, or a resume branch"* — which **condemned Receipt item 2**, the G1–G6 gate table, a capture nothing downstream reads but whose reader is the CEO at the verdict gate. (2) Walk 4 widened the list with *"or a named human decision point"* — which **discriminated nothing**, since every Receipt item has that reader. (3) The walk-4 ACID pass restated it on a DEPOSITED-vs-PRINTED axis — which **condemned Step 1a-bis's mandated id-list-length print**, a transcript-only value that is correct, necessary, and consumed inside its own step. ⚠️⚠️ **Three formulations, each individually reasoned, each wrong — which is entry 209's signal (a region re-folded three or more times is a DELETION candidate, not a patch candidate), and entry 209 is in the batch this plan ingests.** **The form above is the step-back §2.8 prescribes rather than a fourth patch: one sentence, no list, no axis, nothing to keep in sync — the property C7's and C8's rosters lacked and the reason both were deleted.** **It resolves all three failing cases without exception: the deleted mid-batch checkpoint claimed a later reader and had no deposited home (condemned); Receipt item 2 is deposited (permitted); the id-list length is consumed within its own step (never in scope).**
  ⚠️ **THE CONSTRAINT WAS TESTED AGAINST THIS PLAN'S OWN CAPTURES.** Every anchor resolved to a named consumer — backup paths → row 4; `E0`/`P0` → G6; `NT` → Step 2's formula and row 5; `STALE_COUNT` → G1 and row 4; sentinel → row 4; doctrine pins → row 7b; created-proposal list → rows 3/5/8/9; scout dispositions → row 3; cycle dict → row 4 — **except Receipt item 2, the G1–G6 gate table, which nothing downstream reads.** ⚠️ **Item 2 is obviously correct and deleting it would be absurd: its reader is the CEO at the verdict gate, which is the Output Receipt's entire purpose, and this plan's own gate prose invokes exactly that reader when it says a one-row gate table "forces the CEO to adjudicate blind."** **The original enumeration listed only the readers the author happened to be looking at** — the identical decay that deleted C7's and C8's site rosters, **committed by a constraint written three turns ago to prevent reader-less captures.** **The teeth are intact: the deleted checkpoint had no reader of any kind, including this one.**

**Ledger status:** **C1–C13 OPEN.** ⚠️ **C10 and C11 were opened at authoring from the batch's own entries and BOTH were violated by this draft before walk 1 tested them** — C10 by D1's schema-enforced bound, C11 by nothing yet, and the C10 violation was written into the row added to honour the entry C10 came from. **C12 and C13 were opened by walk 1's findings and have not yet been tested by any lens.** None can be reported SATISFIED before the walks that would test them have run.
