# Lessons Forge — Gate 2 Plan A: codify proposals 191–200 (+195's parent) into three doctrine files, then flip all ten to `implemented`
**Date:** 2026-07-30 | **Tier:** Medium | **Dispatch Mode:** bellows | **Test Scope:** targeted | **Execution:** Step 1 (SA) → Step 2 (DEV) → Step 3 (QA) | **qa_steps:** 3 | **pause_for_verdict:** always | **cycle_tier:** T2

## CEO Context

**Depends on Plan B (286, bellows — Done).** The `plan_lint` code that this plan's §4 doc text describes is already on bellows main (`a59200b`, verified still live); Step 3 verifies the doc text matches that shipped code. This is the 278 form of the dependency declaration, deliberately: the diagnostic calls it "a bold-Markdown header field," but what actually shipped in 278 is this bold sentence in CEO Context, and the header line is parsed by the daemon — an unrecognised pipe-delimited key there is a risk with no upside (Checklist #13).

**Gate 2, Plan A — the governance half, and the LAST plan of this arc:** Plan B closed as plan **286** on 2026-07-30 (bellows `a59200b`, filed to `Done/executable-286.md`), shipping proposal 198's `plan_lint.py` code half with the full bellows suite at 834 passed / 0 regressions. **After this plan, `proposed` → 0** and the arc that began with the session-12 lessons batch closes.

**Author from diagnostic 285's findings (Rule 27 — cite, do NOT re-derive):** `/Users/marklehn/Developer/GitHub/governance/knowledge/research/gate2-architecture-edit-map-2026-07-29.md`. The **Gap Assessment (16 rows)** is the edit list; **§Q2** is the `DRAFTING_CYCLE.md` map (anchors + the lens-count guard + the version convention); **§Q3** is the `PLANNER_TEMPLATE.md` map (including the coupled edit); **§Q4** is `RULE_20_SELF_CHECK_BLOCK.md`; **§Q6** is 195's un-codified parent; **§Q7** is the status flip and its load-bearing ordering. If something needed is absent from the map, that is a return to a diagnostic — **not** a gap for the SA or DEV to fill from source.

**Scope: THREE doctrine files at the repo ROOT + the lessons-forge DB. Ten proposals, FIFTEEN owned gap rows (the sixteenth, 198's code half, shipped in plan 286), seven edits to `DRAFTING_CYCLE.md`.**

- `DRAFTING_CYCLE.md` — 191, 194 (§2.6); 195**+parent** (§2.7); 200 (§2.7 + §4); 197 (§3); 198-doc (§4); version `1.1`→`1.2` + History row.
- `PLANNER_TEMPLATE.md` — 192 (new rule); 193 (Checklist #26); 196 (new **Rule 59**); **192-coupled: Checklist #4**; version `4.80`→`4.81` + one Lessons Learned row.
- `RULE_20_SELF_CHECK_BLOCK.md` — 199 (prose section before `## Canonical Python Block`) + one `## History` row.
- `lessons-forge.db` — flip all ten `proposed` → `implemented`, LAST, after every doc edit.

**§6's doctrine-gate pairing clause governs this plan, and is SATISFIED — state it, do not leave it inferred.** `DRAFTING_CYCLE.md` §6 requires that *"the Gate-2 codification that touches §1 or §4 must pair with the `plan_lint` edit and its tests, or explicitly defer the gate change and say so."* This plan touches **§4** (198-doc). The pairing is **plan 286**, already shipped and closed, carrying the `plan_lint.py` fixes plus 8 new tests — which is exactly what the S2 split was for. Nothing is deferred. §1 is NOT touched: no proposal adds or changes a trigger (§Q2(d)).

**⚠️ §6 IS NOT THE ONLY AMENDMENT CLAUSE — `## When this file changes` (`:151–153`) governs this edit too, and BOTH its obligations apply.** (1) *"Any change is a governance edit: commit at the governance root; **note which in-flight plans inherit it**."* — the DEV records in the dev-log which deposited-but-unrun plans inherit the amended doctrine (at authoring: none, but re-check, since a plan deposited during a verdict gate would). (2) *"**Keep the `plan_lint` self-check (§4) in lockstep with §1/§3** — the gate's tests are part of the change."* — **197 amends §3**, so the SA must state explicitly whether that §3 change alters what `plan_lint` enforces and therefore whether a paired gate edit is owed; if it does, this plan is not chartered for it and the answer is to HALT and report, not to proceed quietly. Reading only §6's pairing bullet and declaring compliance is the failure proposal 196 — shipped by this plan — exists to prevent.

**⚠️ WHERE THE MAP IS SUPERSEDED, THE SHIPPED ARTIFACT WINS — and say so rather than silently deviating.** Rule 27 requires authoring from the map by citation, but the map was written 2026-07-29 and plan 286 shipped on the 30th, correcting one of its framings (see Task B on §4). **FIVE** declared deviations, each with its reason stated at the point of use — **(4) the `RULE_20_SELF_CHECK_BLOCK.md` History row PREPENDS** (the map says APPEND; that file has only one row so nothing establishes its order, and both sibling doctrine files are newest-first, so this edit sets the convention); **(5) the seven `DRAFTING_CYCLE.md` edits are enumerated as 191, 194, 195+parent, 197, 198-doc and 200's TWO sites** (§Q6 instead counts the parent as a seventh edit; both total seven, so no count check distinguishes them). And the first three: **§4 is a CORRECTION not an append** (286 supersedes map row 9); **the `## History` row PREPENDS** (the live table is newest-first; the map's "append after `:157`" would invert it); **the version line is dated `2026-07-30`** (ship date, per the 1.1 precedent; the map carries its own authoring date). If the SA finds any OTHER map/live disagreement, HALT and report — do not resolve it silently in either direction.

**Coverage: this plan owns 15 of the diagnostic's 16 Gap Assessment rows.** Row 10 (198's code half) shipped in plan 286. QA should confirm all 15 landed rather than trusting a per-file count — the failure mode is a silently dropped row, not a visibly wrong one.

**⚠️ FIVE CONSTRAINTS THE DIAGNOSTIC SETTLED — each has a QA row.**

1. **NO proposal adds a lens.** The count phrases at `:29`, `:73` and **`:124`** must still read "five" after every edit. **⚠️ `:124`, NOT `:123` — plan 278's Task B3 cites `:123` and is WRONG; do not clone that number.** (§Q2(b); the session-13 baton records the correction.)
2. **The `Checklist #4` COUPLED EDIT is mandatory.** 192 makes the Rule 20 form conditional on plan class; Checklist #4 currently mandates the full form unconditionally. Ship 192 without it and `PLANNER_TEMPLATE.md` simultaneously permits and forbids a simple banner. (§Q3(a).)
3. **195's parent is codified together with 195, MERGED into 195's own §2.7 bullet — it is not a separate seventh edit.** ⚠️ **The map calls it "the 7th edit" and I am deviating from that wording — declared, not hidden.** §Q6 says in full: *"This is the 7th edit to DRAFTING_CYCLE.md … The 7th edit does NOT get its own row **in the Gap Assessment** — it merges INTO row 6."* The elided phrase is what matters: §Q6 counts the parent AS a seventh edit while denying it a seventh Gap row. This plan instead enumerates the seven as 191, 194, 195+parent, 197, 198-doc and 200's TWO sites — a different seven that totals the same, so no count check distinguishes them. Use THIS enumeration, and expect the map to read otherwise. The seven `DRAFTING_CYCLE.md` edits QA counts are: 191, 194, 195+parent, 197, 198-doc, and 200's TWO sites (§2.7 and §4). An earlier draft called the parent "the seventh edit," which conflicts with that enumeration and would leave QA unsure whether to find seven distinguishing strings or eight. The 2026-07-25 subtractive-trim principle was never codified — §Q6 confirms zero matches for the concept. Without the parent, 195's refinements extend nothing.
4. **THREE FILES, THREE DIFFERENT VERSION CONVENTIONS.** `DRAFTING_CYCLE.md` has a `**Version:**` line AND `## History`. `PLANNER_TEMPLATE.md` has a version line and **NO History heading** (the Lessons Learned table is the changelog, newest-first PREPEND). `RULE_20_SELF_CHECK_BLOCK.md` has `## History` and **NO version line — and none is to be added.**
5. **Doc edits land BEFORE the status flip.** 259's reasoning, quoted in §Q7(b): if the DB flips first and a doc edit fails, the corpus asserts ten `implemented` with no codification behind them — a false permanent claim no later gate re-checks. The reverse is recoverable and obvious at the gate.

**✅ CEO DECISION 1 (2026-07-29) — NO version line on `RULE_20_SELF_CHECK_BLOCK.md`.** Nothing mechanical reads one, it only changes if someone remembers to bump it, and a stale one asserts something false; a `shasum` is strictly stronger. **199's `## History` row is the whole edit.** Do not "improve" the file by adding a version line.

**⚠️ THIS PLAN REWRITES THE RULES IT IS ITSELF GOVERNED BY — two self-references, both benign, both stated so no one "fixes" them mid-run.** (1) **Checklist #4:** this plan's own QA step cites it while the coupled edit rewrites it from an unconditional full-form mandate to a conditional one. The QA step uses the FULL form with two real evidence files, which is valid under BOTH the old and new wording — so nothing is invalidated mid-flight, and QA must not "adapt" its form because the checklist changed underneath it. (2) **`RULE_20_SELF_CHECK_BLOCK.md`:** the QA step reads it at run time to copy the canonical Python, and 199 edits that same file. The Python is unchanged by design; if the block QA copies differs from what this plan describes, that is a finding to REPORT, not a discrepancy to accommodate.

**⚠️ THE ROW-7 GUARD INVERTS FOR THIS PLAN — do not clone its absolutist form.** In plans 282/283/284 the doctrine files were pinned as UNCHANGED, because those were routing plans that must not touch doctrine. **Plan A edits all three by design**, so an absolutist "unchanged" pin would necessarily FAIL on a correct run — exactly the row-5-blesses-what-row-7-fails collision plan 284's cold panel caught. The guard here is **"changed ONLY in the intended ways"**: pre-edit shasums recorded, post-edit shasums expected to DIFFER for all three, and every hunk in the diff attributable to a named gap row. A plan that carries 282's language unmodified is broken before it runs.

**Authoring-time pins (pre-edit state, verified by the Planner 2026-07-30 — all three match the session-13 baton, so doctrine has not drifted):**
- `DRAFTING_CYCLE.md` — `d8f17394c08d…`
- `PLANNER_TEMPLATE.md` — `49b726447498…`
- `RULE_20_SELF_CHECK_BLOCK.md` — `c90ffb4bea00…`

**DB precondition (verified by the Planner 2026-07-30):** `SELECT count(*) FROM lesson_proposals WHERE id BETWEEN 191 AND 200 AND status='proposed' AND route='codify'` = **10**, and the corpus-wide `proposed|codify` count is also **10** — i.e. these ten are the entire `proposed` population. **⚠️ Per §Q7(d) the expected count is 10 BEFORE the flip and 0 AFTER; a row asserting 0 before the flip task runs would false-FAIL.**

**Test Scope: targeted** — this plan changes no code. The targeted run is the lessons-forge suite as a regression check that the plan's execution broke no existing behaviour (the 283/284 convention). ⚠️ **It is NOT a check on the canonical corpus** — the suite runs entirely on `:memory:` and tempfile fixtures and never opens `lessons-forge.db`. An earlier draft claimed it verified "the DB write left the corpus readable," which is false and would have been inherited by any clone. **Row 8's per-id read-back is the only genuine post-flip corpus verification in this plan.** Baseline: reconcile against the most recent prior lessons-forge QA report at QA time rather than asserting a number here. **⚠️ Declared explicitly, because Rule 21's `targeted` clause also says the QA step "must NOT run the full suite" and that prohibition is not satisfiable as a distinction here:** the lessons-forge suite IS the whole module, so targeted and full are the same command over the same tests. Plan 284 hit this identical tension and declared it rather than pretending to a distinction; the alternatives — no regression evidence, or two byte-identical runs — are both worse.

**Dispatch: lessons-forge**, because this plan writes the LF DB (the 259/278 precedent). The three doctrine files live at the **repo ROOT, outside this project** — edit them by ABSOLUTE path. Per §Q1(b) `scope_check` is cwd-scoped and cross-repo-blind, so root-file edits are structurally invisible to it; that is how 259 and 278 worked, and it means **the scope guard for the doctrine files is this plan's own QA rows, not the daemon.**

**⚠️ CARRY THIS INTO THE RECORD — the codification-effect measurement (baton instruction).** Plan 283's CEO Context recorded a prediction with baselines: the five defect classes in this batch each recurred *while the batch was being processed* — entry 185 (sweep every site) **8+ times**, entry 188 (read the cited rule) **6 times**, entry 190, entry 192. **Gate 2 is what codifies them, so the first plan drafted AFTER this one is the measurement** — if codification works, those recurrence counts fall. For the record, this plan's own cycle re-trips them again: the fold-sweep class recurred repeatedly (Rule 60 verified nowhere; the second corpus-sweep citation unscoped), and the resume-glob and `.backup`-inlining lessons were both re-tripped and caught. **That is baseline data, not failure** — it is precisely what the post-Gate-2 plan gets compared against.

**⚠️ TWO EXPECTED `plan_lint` WARNs — cause ESTABLISHED by reading the check, not predicted.** `step 2 / step 3 mentions tests but declares no test scope` fires because that check (`plan_lint.py:144–151`) triggers on the word "test" appearing in a step and is suppressed ONLY by a `test_*.py` file in Scope, a `tests/` scope prefix, or the literal `tests/` in the step text. This plan says "test" because it RUNS an existing suite, but it **modifies no test file** and its command is `pytest src/`. The WARN is therefore structurally true and semantically inapplicable — the check exists to catch "you are editing tests but did not scope them," which is not this plan. **Do NOT silence it by inserting a decorative `tests/` mention:** that games the gate rather than satisfying it, and is the Goodhart failure §3 names. **In the DEPOSITED state, expect exactly these two WARNs and exit 0.** ⚠️ While the drafting cycle is still open a THIRD WARN also fires — the `Drafting Cycle closing indicates fold as last event` check — because the last recorded event is a fold until the cycle closes on a dry lens pass. That one clears at close and is not a defect; if you lint this plan mid-cycle you will see three, and the count above is the post-close expectation, not the current one.

**⚠️ OWED AFTER THIS PLAN SHIPS — a new LESSONS entry, deliberately NOT appended yet.** This cycle established something proposal 197 does not cover: **the final step's gate span absorbs the trailing Cycle Log, so a gate-matching string QUOTED in that log is evaluated as if the step had said it.** Observed twice here — a path token silently suppressed a real `plan_lint` WARN on step 3, and the log briefly carried both byte-exact Rule 20 banner strings, which are checked by the one gate that HARD-FAILS. 197's concern is an AGENT misreading the log as instructions; this is a GATE matching it, which is a different mechanism and a worse failure because it is silent. Not appended now: `LESSONS.md` already carries five un-ingested entries, and growing the batch while a Gate 2 is in flight invites the batch-pin trap. Capture it once this plan is Done.

**Deposit-once** (grep `knowledge/decisions/` first).

## How to Run This Plan

**Bootstrap prompt:**
```
Read the plan at knowledge/decisions/in-progress-executable-<id>.md (daemon renames the deposited placeholder on claim). Execute Step 1 ONLY. After completing Step 1, STOP and wait for my confirmation.
```

---
---

## STEP 1 — SA (blueprint the exact edits; NO writes)

---

> **FIRST — post a short visible chat message (1-2 sentences) confirming you are starting this plan and your immediate next action.** Do NOT rename this plan file. You are the Solution Architect. **This step is READ-ONLY — you write one blueprint file and nothing else. No doctrine file is touched in this step.**
>
> Read the diagnostic findings at `/Users/marklehn/Developer/GitHub/governance/knowledge/research/gate2-architecture-edit-map-2026-07-29.md` (ABSOLUTE path — the Gap Assessment table plus §Q2, §Q3, §Q4, §Q6, §Q7), then the three live doctrine files by ABSOLUTE path: `/Users/marklehn/Developer/GitHub/DRAFTING_CYCLE.md`, `/Users/marklehn/Developer/GitHub/PLANNER_TEMPLATE.md`, `/Users/marklehn/Developer/GitHub/RULE_20_SELF_CHECK_BLOCK.md`.
>
> **Your job: convert each Gap Assessment row into an EXACT, ANCHORED before/after edit the DEV applies without judgement.**
>
> **⚠️⚠️ EVERY MODIFY EDIT MUST CARRY A MUST-SURVIVE LIST — this is the single largest destruction risk in the plan.** Five edits (197 → §3, 198-doc → §4, 193 → Checklist #26, the coupled edit → Checklist #4, and 200's §4 sentence) are anchored on SECTION HEADINGS, and the map's proposed replacement text does not reproduce everything those sections currently contain. Verified present today and **not named anywhere as must-survive**: §4's `**Landing posture — warn-first (deliberate).**` paragraph (the record that the gate does not block); §3's fenced canonical Cycle Log example that every plan copies; Checklist #26's "must explicitly include places that merely QUOTE the pattern" clause and its `**Worked example — convention changes.**` block; Checklist #4's "Grep the plan file for every step identified as QA" instruction and its "no agent-discretion language" clause. **For each MODIFY, enumerate in the blueprint the exact clauses that must still be present AFTER the edit, quoted, and hand the DEV a post-edit grep per clause.** A heading-anchored replacement that silently drops a paragraph is invisible to QA row 4 (which checks only that NEW strings appeared) and to row 1's per-hunk attribution (a deletion hunk attributes happily to the same gap row). ⭐ **Proposal 195 — which this plan is codifying into §2.7 — says "After any edit, assert the PRESENCE of retained material, not merely the absence of removed material." Apply it here, to the file it is being written into.** Anchor every edit on a QUOTED UNIQUE STRING from the live file, never on a line number — line numbers in the map are authoring-time and may have shifted. **⚠️ PROVE each anchor is unique before you blueprint it: `grep -Fc '<anchor>' <file>` must return exactly 1, and you must record that count per anchor.** An anchor matching two places does not fail loudly — the DEV edits whichever it finds first, producing a correct-looking edit in the wrong section, and every downstream grep for "the new text is present" still passes. If any anchor is not unique, lengthen it until it is, and blueprint the lengthened form.
>
> **⚠️ TWO EDITS ANCHOR ON TEXT THIS PLAN ITSELF CREATES — the pre-edit uniqueness grep cannot apply to them, and their ORDER is the thing at risk.** 194 lands after 191 at the SAME §2.6 paragraph, and 200's §2.7 bullet lands after 195's — a bullet that does not exist until 195 is applied. For these two, blueprint a SEQUENCED anchor: 194's anchor is the closing text of **191's newly-inserted clause**, and 200's is the closing text of **195's new bullet**, each stated explicitly as "re-derive from the post-<prior> text." Do not grep them against the pre-edit file and do not give 191 and 194 the same anchor: a DEV inserting both after one anchor yields **194 before 191**, the reverse of the mandated order — and every "new text is present" grep, plus QA row 4's "quote a distinguishing string from each," passes on the inverted result. **State the required final order explicitly in the blueprint** so QA can verify sequence, not just presence. For each edit produce: the gap row number, the target file, the anchor string, the exact BEFORE text, the exact AFTER text, and whether it is an APPEND or a MODIFY.
>
> **Task A — record the pre-edit state.** `shasum -a 256` all three doctrine files and record them. Confirm they match the authoring-time pins in CEO Context. **⚠️ Those pins are 12-hex PREFIXES followed by an ellipsis — compare by prefix, not by string equality**, or every comparison fails. Record the full 64-hex values you computed. If any prefix differs, STOP and report — doctrine drifted between authoring and execution and the blueprint's anchors may be stale.
>
> **Task B — blueprint the seven `DRAFTING_CYCLE.md` edits** (§Q2 + §Q6): 191 and 194 both APPEND after the §2.6 paragraph ending `"Author-verify cold findings; a cold reader can misread deliberate design as a defect."` (191 first, then 194 — they do not collide); 195**+parent** as a NEW §2.7 bullet after the last existing bullet (`"Sketch one real block."`), merging the parent principle and 195's refinements into ONE bullet; 200 as a further §2.7 bullet AFTER 195's, PLUS a sentence appended to §4's integrity paragraph anchored on `"The gate reads structure, not truth"`; 197 MODIFY §3 anchored on `"The Cycle Log (compact, in the plan)"`; 198-doc MODIFY §4 anchored on `"The Self-Check (mechanical, enforced by plan_lint)"`. **⚠️⚠️ §4 must be CORRECTED, not merely supplemented — and this is the one place the map is SUPERSEDED by what actually shipped.** Map row 9 frames §4 as "describes the self-check but does not document the four known defects," which reads as an append-only job. **Plan 286 — the sibling that shipped the code, closed 2026-07-30 — explicitly corrected that framing** (`executable-286.md:10`): *"An earlier draft claimed this plan 'changes only HOW the checks read their input, never what §4 mandates.' That claim was false and is corrected here."* Live `DRAFTING_CYCLE.md:126` MANDATES the pre-fix rule — *"it WARNs iff that line contains a fold-token (`fold`) but not `dry`"* — and **two shipped fixes contradict it**: (b) makes `- ACID: w1 NOT dry; folded elsewhere.` WARN where `:126` says it must not, and (c) adds a Closing-presence WARN class `:126` does not enumerate. **Blueprint `:126` as a must-CHANGE pin**, not merely a must-survive one: appending defect descriptions while leaving `:126` intact ships a §4 that documents the fix and mandates its opposite, and 198 then flips to `implemented` with the doc↔code window 286 says "closes when Plan A lands" still open — on a status change with no reverse transition. **⚠️ BOUND, from 286's own charter (`:14`): document the negation-aware `dry` handling ONLY. The fold side was deliberately left as the incumbent substring; narrowing it "appears in no proposal and Plan A is not authorised to document" it.**. **⚠️ 198-doc must describe the code as SHIPPED by plan 286, not as intended** — read the shipped behaviour from `git -C /Users/marklehn/Developer/GitHub/bellows show a59200b:scripts/plan_lint.py`, which is the authority for what §4 now says. **First confirm that SHA is still what is live:** `git -C /Users/marklehn/Developer/GitHub/bellows diff a59200b..HEAD --stat -- scripts/plan_lint.py; echo "exit=$?"` must be EMPTY. **⚠️ The same check is repeated in Step 2 and Step 3 — each carries its own copy, because a step never sees another step's prompt.** A `plan_lint.py` change landing on bellows main during either arbitrarily-long gate leaves §4 documenting behaviour that is no longer shipped, with 198 flipped to `implemented` and no reverse transition — and pinning verification to the SHA rather than to live HEAD would let every check pass anyway. (The Planner verified this at authoring; re-confirm, because if anything has landed on that file since, §4 would document behaviour that is no longer shipped — which is the precise failure Plan-B-first exists to prevent.)
>
> **Task C — blueprint the version + History edits for `DRAFTING_CYCLE.md`.** Version `:5` is a **SURGICAL SUBSTRING replacement**: `1.1 (2026-07-25)` → `1.2 (2026-07-30)`. **⚠️ THAT SUBSTRING IS NOT UNIQUE — `grep -Fc '1.1 (2026-07-25)'` returns 2** (the version line at `:5` AND the History row at `:157`), so a replace-all destroys the 1.1 changelog entry. Lengthen the anchor to include the trailing clause (`1.1 (2026-07-25). Amended only through the Iteration Protocol (§6).`), which IS unique, and blueprint the swap against that. **The same trap is worse in `PLANNER_TEMPLATE.md`: `grep -Fc '4.80'` returns 3** — `:5`, `:6`, and the v4.80 Lessons Learned row at `:1866` — and **no QA row observes that third one**, so a replace-all silently rewrites a governance changelog entry that nothing checks. Pin both version edits to unique, lengthened anchors; this plan already mandates `grep -Fc == 1` for prose anchors and must apply it to its own version strings. **⚠️ The map writes this as `1.2 (2026-07-29)` — that is the map's own authoring date, and the deviation to `2026-07-30` is DELIBERATE.** The convention is the SHIP date (1.1 shipped 2026-07-25 and is dated 2026-07-25), and this plan ships on the 30th. Use `2026-07-30` consistently in the version line, the History row, and the Lessons Learned row; QA should treat a `07-29` stamp as the error, not this note. ⚠️ **The trailing clause `Amended only through the Iteration Protocol (§6).` MUST survive** — plan 278's M1 nearly destroyed it with a whole-line replace. Blueprint it as a substring swap and say so explicitly. Then the History row, **PREPENDED — placed directly ABOVE the 1.1 row** (never rewriting it). ⚠️ **That `## History` table is NEWEST-FIRST**: live order is `:157` = 1.1, `:158` = 1.0, so "append after the last existing row" would file 1.2 beneath 1.0 and invert the convention. The diagnostic's "APPEND row after the last `## History` row at `:157`" means after the HEADING — when 1.1 actually shipped, it landed above 1.0. **⚠️ §6:148 also says a Gate-2 codification "**appends** a dated row" — that wording is stale against the file's own newest-first table, and you will find it when you open §6.** The live order is authority: `:157` = 1.1 above `:158` = 1.0. **PREPEND, and do not halt on §6's wording** — but record the discrepancy in your BLUEPRINT (you write no dev-log; the DEV carries it forward from there), because §6 is amendable only through this same Gate-2 route and a future batch should correct it. QA row 3 passes either way, so nothing downstream catches an inversion. Naming the six proposals + parent and stating that the lens count deliberately stays five. **Follow the 1.1 row's own convention and name the paired plan BY ID — it reads `Paired with Plan B (277, bellows)`, so the 1.2 row reads `Paired with Plan B (286, bellows)`.** A row that says only "Plan B" is unresolvable to a later reader, since every Gate 2 has one.
>
> **Task D — blueprint the four `PLANNER_TEMPLATE.md` edits** (§Q3): 192 as a NEW rule in the Orchestration Plan Rules; 196 as **NEW Rule 59**, placed after the END of Rule 58's BODY — ⚠️ **anchor on Rule 58's closing `Source:` line (`:1097`), NOT on its heading (`:1093`)**; an insert at the heading orphans Rule 58's body and `Source:` line under Rule 59, silently destroying a live rule while QA row 5 still finds the new headings and passes. The map pins this as "AUTHORING-TIME line after `:1097`" — ⚠️ **Rule 59 is a RULE, not Checklist #34; the two sections number INDEPENDENTLY** (§Q3(b)); 193 MODIFY Checklist #26 anchored on `"After fixing an anti-pattern instance, sweep the whole artifact for siblings"`; and **the COUPLED EDIT** — MODIFY Checklist #4 anchored on `"QA step includes exact canonical Rule 20 self-check reference"` to make the form conditional, cross-referencing 192's new rule by its assigned number. **⚠️ THE CONDITIONAL FORM RELAXES A BLOCKING GATE — carry a compensating clause or you ship a system-wide weakening.** `gates.py::_gate_rule_20_self_check` checks only that the banner and a PASSED line appear in the QA report; it cannot tell the block's real stdout from a hand-written line. Today Checklist #4 forbids that by mandating the full block unconditionally. The moment "simple-banner plans: the banner string and PASSED line" is codified, an entire plan class may emit an UNVERIFIED `PASSED` and clear the only blocking Rule 20 gate. **The codified text must therefore require that even the simple-banner form is the block's ACTUAL stdout** — run with an empty `required_evidence_files` and a real `evidence_dir` — never hand-authored. Blueprint that clause explicitly; the map's proposed wording omits it. **⚠️ THERE ARE TWO NEW RULES IN THIS BATCH, SO TWO NUMBERS ARE OWED — the map pins only one.** Both 192 and 196 are new Orchestration Rules and both APPEND after the highest existing rule (58), because §6-style numbering stability forbids inserting a mid-list number even where the content would read better there. §Q3(b) pins **196 → Rule 59**; therefore **192 → Rule 60**. Blueprint both numbers explicitly and state this derivation, because the Checklist #4 coupled edit must cite 192's number and cannot be written until it is fixed. If you conclude a different assignment is correct, HALT and report rather than silently choosing — a wrong number here propagates into the coupled edit and into the changelog rows. **⚠️ Pin POSITION as well as number: both new rules go at the END of the Orchestration Plan Rules, in ascending order — Rule 59 (196) then Rule 60 (192).** The map permits placing 192 "in the Rule 20 neighbourhood," but doing so while numbering it 60 would leave the file reading 20, **60**, 21, … 58, 59 — an out-of-order section that breaks reading order and any tooling that walks rules sequentially. Numbering stability and document order must agree here; append both. Plus version `4.80`→`4.81` and ONE Lessons Learned row **PREPENDED** after the table header (newest-first), not appended.
>
> **Task E — blueprint the `RULE_20_SELF_CHECK_BLOCK.md` edit** (§Q4): 199 is **PROSE-ONLY**. A new section between `"## How Plans Reference This Block"` and `"## Canonical Python Block"`, documenting **all FOUR points §Q4(a) enumerates — do not clone the three-point phrasing of the map's History-row (row 15), which is itself abbreviated**: (1) what the block verifies (evidence-file presence, hedging-keyword absence in positive-status rows) versus what it does NOT (verdicts, fail-glyphs); (2) the mandated `## Verification Table` heading; (3) the status column constrained to pass/fail glyphs; (4) the `## ` heading coupling with `gates.py` and the FAILED-run stdout placement. QA row 6 checks only that the prose section exists, so a dropped mandate is undetectable — and 199 is marked `implemented` either way, on a flip with no reverse transition. Plus ONE `## History` row — **PREPENDED, placed directly under the `## History` heading and ABOVE the existing 2026-05-10 row.** ⚠️ The map says "APPEND after the last row at `:122`", and that file has only ONE row so nothing establishes its order — which means **this row sets the convention for every future one**. Both sibling doctrine files are newest-first; filing this one oldest-first would make it the lone outlier. (The newest-first correction was made for the other two files and never swept to this one — the fold-sweep class proposal 193, shipped by this plan, exists to catch.) **⚠️ The executable Python block is UNCHANGED — every QA step in the system copies it verbatim at run time, so an edit there would break plans authored against the current block. And per CEO Decision 1, do NOT add a `**Version:**` line.**
>
> **Task F — the lens-count guard.** Locate and quote the three count phrases in `DRAFTING_CYCLE.md` (`"full five-lens walk"`, `"run the five lenses"`, `"all five"`) with their CURRENT line numbers as found by you. **Do not trust `:29`/`:73`/`:124` from the map or `:123` from plan 278 — 278's number is known wrong. Report what you actually find.** State explicitly that no blueprinted edit modifies any of the three.
>
> **Task G — blueprint the status flip** (§Q7): the parameterised UPDATE over `id IN (191,…,200) AND status='proposed'`, the `.backup` restore point taken FIRST, and the per-id read-back. **⚠️ Pin ALL THREE columns §Q7(a) specifies — `status='implemented'`, `status_updated_at`, `status_updated_by='ceo'` — not `status` alone. **⚠️ Pin the timestamp FORMAT, because the corpus is already inconsistent and `<UTC-now>` does not choose for you.** Measured across all rows: **ISO-Z 64, `+00:00` offset 58, other 49, space-separated 19, NULL 10.** **Use `YYYY-MM-DDTHH:MM:SSZ`** — the PLURALITY at ~34% of populated rows, not a majority; the corpus has no dominant convention, so this is a deliberate choice for a lexically-sortable, space-free form rather than a claim about what most rows do. (An earlier draft of this paragraph stated 23/5/4 and called it "the majority": those figures came from a query grouped by distinct timestamp VALUE, misread as format aggregates. The decision survives the correction; the stated basis did not.) **Compute it into a shell variable first** (`TS="$(date -u +%Y-%m-%dT%H:%M:%SZ)"`) and reference `$TS` in the statement — never inline a `$(date …)` bearing spaces into a quoted sqlite3 argument, which is the same construction that silently produced no backup in plan 281. `status_updated_by='ceo'` — ⚠️ **and the basis is a plurality plus the Gate-2 precedent, NOT "every row": measured `ceo` 121, `planner` 47, `auto` 22, NULL 10.** An earlier draft claimed every populated row was `'ceo'`, which is false; the VALUE is still right (every prior Gate-2 codification used it and the CHECK constraint admits it), but an SA told to verify a citation before relying on it — proposal 196, which this plan ships — would find the stated premise wrong and have no fallback. Write `'ceo'`; do not halt on the corrected census.** The audit fields are the only record of WHEN and BY WHOSE authority ten proposals became doctrine, this write has no supported reverse transition, and a status-only UPDATE succeeds silently while losing them permanently. Blueprint the statement in full rather than describing it. State the load-bearing ordering: **every doc edit lands before the flip.**
>
> **Scope:**
> - `knowledge/development/gate2-plan-a-blueprint-2026-07-30.md`
>
> **Deposit:** `knowledge/development/gate2-plan-a-blueprint-2026-07-30.md` — every edit as an anchored before/after pair, the three pre-edit shasums, the lens-count findings, 192's assigned rule number, **the two `## When this file changes` determinations — which in-flight plans inherit the amendment, and whether 197's §3 change requires a paired `plan_lint` edit (if it does, HALT and report; this plan is not chartered for a gate edit)** — the §6 append-vs-prepend discrepancy, and an Output Receipt. Canonical Python/MCP file-write — NO heredoc. Commit (NO push). `#### Prompt Feedback` in `### Ledger Updates`.
>
> **Deposits:**
> - `knowledge/development/gate2-plan-a-blueprint-2026-07-30.md`
>
> **STOP. Do NOT proceed to Step 2. Wait for CEO verdict.**

---
---

## STEP 2 — DEV (apply the blueprint faithfully, then flip)

---

> **Read the Step-1 blueprint at `knowledge/development/gate2-plan-a-blueprint-2026-07-30.md` (in your lessons-forge tree — Step 1 committed it, so the teardown merge has carried it into yours) and confirm its Output Receipt is Complete; else halt and report.** **⚠️ Record `shasum -a 256` of the blueprint you actually read, and quote it in your dev-log.** This plan pins three doctrine files and the Rule 20 block by hash, and the blueprint — the document every edit below is applied FROM — was the one input it never pinned. An arbitrarily-long verdict gate separates Step 1 from you, and the blueprint is a committed file like any other; recording what you read makes an altered or superseded blueprint visible after the fact instead of silently authoritative. (The SA cannot hash its own deposit — a file cannot contain its own hash — so this is the only place the pin can be taken.) Post a short visible chat message confirming you are starting Step 2. You are the Developer.
>
> **⚠️ IF YOU HALT AFTER TASK A HAS BEGUN, THE LIVE DOCTRINE IS SITTING MODIFIED-AND-UNCOMMITTED — say so, loudly, in the same breath as the halt.** These edits land in the real governance root, not a worktree; there is no teardown that cleans them up and no isolation between you and every future plan that reads these files. On ANY halt after editing starts: **leave the tree exactly as it is** (do NOT restore — the CEO may need to inspect what you did), and report three things explicitly — that `DRAFTING_CYCLE.md` / `PLANNER_TEMPLATE.md` / `RULE_20_SELF_CHECK_BLOCK.md` are currently modified and uncommitted, which tasks completed, and the output of `git -C /Users/marklehn/Developer/GitHub status --porcelain -- <the three absolute paths>`. A halt that does not say this reads as "nothing happened," and the next reader — human or agent — inherits a dirty rulebook without knowing it. **This does NOT contradict A0 state (3), which restores:** don't-restore governs the dispatch that is halting (preserve the evidence for inspection); restore-and-redo governs the NEXT dispatch, after the state has been classified and found attributable. Two different moments, two different correct actions.
>
> **⚠️⚠️ THREE ENVIRONMENT FACTS THAT BREAK NAIVE COMMANDS ON THIS MACHINE — every one was observed, not predicted.**
>
> **(1) `grep` here is a ugrep shim (`grep --version` → `ugrep 7.5.0`), and any pattern beginning with `**` is a REGEX ERROR.** `grep -c '**Version:**' <file>` prints `ugrep: error at position 4` to stderr and NOTHING to stdout; `grep -Fc '**Version:**'` correctly returns 1. **Use `grep -F` for every literal in this plan** — the bold-marker anchors, the must-survive clauses, the `**Version:**` absence check. This matters most for the ABSENCE checks: an errored grep produces empty stdout, which reads as "not found → PASS" having verified nothing. `/usr/bin/grep` fails the same way; `-F` is the fix, not a different binary. Never pipe these to `head`, which masks the exit code.
>
> **(2) Shell state does NOT persist between your separate commands.** `TS`, `BK` and `LF_TREE` must be assigned and USED **in the same invocation** (join with `&&` or a single heredoc-free block). ⚠️ An empty `$TS` is the dangerous one: the UPDATE writes `status_updated_at=''` and **exits 0**, the per-id read-back passes, a "populated" check passes because empty-string is not NULL — and only row 8's GLOB catches it, by which time `AND status='proposed'` makes repair impossible. An empty `$BK` is louder but not safe either: `sqlite3 … ".backup ''"` also exits 0 and writes nothing, which is why the `ls -la` guard is load-bearing.
>
> **(3) The shell is zsh, where an unmatched glob ABORTS the command** rather than passing the pattern through. The backup prefix-glob's expected fresh-run result is zero matches, so append `2>/dev/null || true` and treat "no output" as "no prior backup" — otherwise the correct case is indistinguishable from the glob breaking.
>
> **⚠️ TWO REPOS, TWO PATH DISCIPLINES — do not collapse them (plan 275 hardened this; plan 278, this plan's structural parent, reverted it and its cold panel caught the reversion).** For **lessons-forge** — the dev-log, and any git command in this project — you may be running in a worktree under `.bellows-worktrees/<id>/`, so set `LF_TREE="$(git rev-parse --show-toplevel)"` and use `git -C "$LF_TREE" …`; never assume `/Users/marklehn/Developer/GitHub/lessons-forge` is your tree. For the **three doctrine files and the DB**, the absolute paths under `/Users/marklehn/Developer/GitHub/` are CORRECT and must not be "fixed" to relative ones — those live at the repo root and in the main checkout, outside any worktree. Echo `LF_TREE` into the dev-log so the tree you actually used is on the record. **You are a faithful APPLICATOR, not a re-author** — apply the blueprint's anchored edits exactly. If an anchor does not match the live file, HALT and report; do NOT improvise a new anchor.
>
> **Task A0 — CLASSIFY THE STARTING STATE. A pin mismatch has FOUR possible causes and only one is external drift; a single "mismatch → HALT" would block this plan's own designed-safe resume.** ⚠️ **The three doctrine files live in the LIVE governance root, not a worktree** (verified: `/Users/marklehn/Developer/GitHub` has a real `.git` directory), so unlike a worktree plan, a prior dispatch that died mid-edit left partial edits in the REAL doctrine. Re-`shasum` all three and branch:
>
> **(1) All three match the blueprint's pre-edit pins, and `git -C /Users/marklehn/Developer/GitHub log --grep='\[<id>\]' --oneline -- <the three files>` is EMPTY** → fresh run. Proceed to Task A.
> **(2) Pins DIFFER and the working tree is CLEAN for those three files** (`git -C /Users/marklehn/Developer/GitHub status --porcelain -- <the three absolute paths>` is empty) → the edits are COMMITTED, so a prior dispatch already completed them. ⚠️ **Classify on the porcelain, not on the `[<id>]` tag.** The tag is the natural test and it fails open: an agent that omitted it leaves a committed state the tag-grep cannot see, which would fall through to (3), where `git diff` is empty, "only attributable hunks" is vacuously true, and a `checkout` restores from a HEAD that already contains the edits — leaving the pins still mismatched and the run unclassifiable. Use the `--grep` to IDENTIFY the commit for your report, not to decide the branch. **⚠️ And do not depend on the tag to identify it either — Task F needs this SHA to pick its diff.** The tag-independent form is `git -C /Users/marklehn/Developer/GitHub log -1 --format=%H -- <the three absolute paths>`: the most recent commit touching any of them. Confirm its diff matches the blueprint before trusting it (an unrelated newer commit to doctrine would surface here, and that is state (4), not state (2)). Record this SHA in the dev-log as `DOC_SHA` — Task F, the commit step, and QA row 1 all take it. **This is the plan's INTENDED safe intermediate state** (docs land before the DB by design), not a failure. Verify the committed diff against the blueprint, then **SKIP the EDIT tasks (A–C) AND Task F2 — the doctrine commit already exists, so F2 would stage nothing and hard-stop on "nothing to commit"; carry the prior `DOC_SHA` forward instead. Still RUN Task D's VERIFICATION half plus Tasks E, F, and G0 against the committed state** before going to Task G. ⚠️ **Do not skip A–F wholesale:** E is the lens-count guard and F is the per-hunk diff review — both are VERIFICATION, not edits, and skipping them would mean this dispatch flips the corpus while having verified nothing itself, trusting a prior dispatch whose evidence may have died with it. Re-applying an edit is the thing to avoid; re-verifying is cheap and is the whole reason the ordering is safe. **⚠️ Task D is HALF edit and half verification — skipping it wholesale drops the only PRE-FLIP proof that the canonical Rule 20 Python block is byte-identical**, which matters because that file is read at run time by every QA step in the system. On the resume path, skip Task D's prose/History insertion and still run its block-diff, its byte counts, its no-`**Version:**`-line grep, and Task A's check that `Amended only through the Iteration Protocol (§6).` survived — all against `<DOC_SHA>^:` since the edits are committed there. QA row 6 catches a corrupted block only AFTER the irreversible flip; this catches it before.
> **(3) Pins DIFFER, the working tree is DIRTY for those files, and `git -C /Users/marklehn/Developer/GitHub diff -- <the three files>` shows only hunks attributable to blueprint rows** → a prior dispatch died mid-edit. The work is fully reproducible from the blueprint, so per Rule 56 prefer restore-and-redo: **`git -C /Users/marklehn/Developer/GitHub checkout HEAD -- <the three absolute paths>`** — ⚠️ **the `HEAD` is REQUIRED: a bare `git checkout -- <path>` restores from the INDEX, so if a prior dispatch died between Task F2's `git add` and its `git commit`, the restore is a silent NO-OP.** Reproduced: with a staged edit, `status --porcelain` shows `M ` (so A0 reads DIRTY → this state), `git diff` returns ZERO lines (so "only attributable hunks" is vacuously true), and `git checkout --` leaves the edited content in place, while `git checkout HEAD --` restores it. Task F2's staging step is exactly what creates that window. **Then confirm the pins ARE back to pre-edit; if they are not, HALT** — do not proceed to Task A on a tree you failed to restore, or you re-apply every edit on top of already-edited LIVE doctrine. Never hand-patch a partial apply.
> **(4) Pins DIFFER with any unattributable hunk, or the `--grep` finds a commit whose diff does NOT match the blueprint** → external drift or foreign work. **HALT and report; restore NOTHING** — a `checkout` here would discard someone else's edit to live doctrine.
>
> **(5) ANY state not matching (1)–(4) → HALT and report it as unclassified.** Default-deny is deliberate: this classifier gates edits to live doctrine and an irreversible corpus flip, so an unrecognised starting state must stop the run rather than fall through to whichever branch happens to be written last. Describe what you observed — the three pins, the porcelain output, and the `--grep` result — so the state can be classified by the CEO and the classifier extended if it turns out to be legitimate.
>
> **Record which state you matched, and the evidence for it, in the dev-log.** Every later task branches on it (Task F picks its diff command from it; Task G's backup handling depends on it), so an unrecorded classification makes the rest of the step unauditable.
>
> Then confirm your lessons-forge tree is clean.
>
> **Task A — version bumps FIRST, both SURGICAL.** `DRAFTING_CYCLE.md :5` — substring-swap `1.1 (2026-07-25)` → `1.2 (2026-07-30)`, preserving `Amended only through the Iteration Protocol (§6).` verbatim; grep-confirm that clause still present after the edit. `PLANNER_TEMPLATE.md :5` — `4.80` → `4.81`, nothing else on that line. **⚠️ AND `:6` — `**Last Updated:** 2026-07-23 (v4.80)` → `2026-07-30 (v4.81)`.** The file's proven convention bumps BOTH lines together (the v4.79→v4.80 commit did); bumping only `:5` ships a doctrine header that contradicts itself — `Version: 4.81` beside `Last Updated: … (v4.80)` — and no QA row would have caught it. Constraint 4 above describes this file as "a version line and NO History heading"; that is incomplete, the version metadata is TWO lines.
>
> **Task B — apply the seven `DRAFTING_CYCLE.md` edits** exactly as blueprinted, then the History row (PREPEND directly above the 1.1 row — the table is newest-first; never rewrite the `1.1` row). After each edit, grep-confirm the new text is present AND the anchor's surrounding text survived.
>
> **Task C — apply the four `PLANNER_TEMPLATE.md` edits** (192, 196/Rule 59, 193/Checklist #26, and the Checklist #4 coupled edit), then PREPEND the Lessons Learned row after the table header.
>
> **Task D — apply the `RULE_20_SELF_CHECK_BLOCK.md` prose section + History row.** Then **prove the Python block is byte-identical to its pre-edit state**: extract the fenced block from BOTH versions and diff them; they must be identical. **⚠️ Get the "before" from git, not from a copy you were supposed to have kept** — this task edits the file first, so by the time you reach this check there is no earlier version in your tree. Use `git -C /Users/marklehn/Developer/GitHub show HEAD:RULE_20_SELF_CHECK_BLOCK.md` as the pre-edit source (HEAD still holds it: your edits are uncommitted at this point, **and on the A0(2) resume path you skip only this task's EDIT half — the prose/History insertion — while still running its VERIFICATION half, which A0(2) requires: the block-diff, the byte counts on both extractions, and the no-version-line grep. ⚠️ On that path the edits are COMMITTED, so extract the "before" from `git -C /Users/marklehn/Developer/GitHub show <DOC_SHA>^:RULE_20_SELF_CHECK_BLOCK.md`, NOT from `HEAD:` — `HEAD:` there is post-edit and would diff post-edit against post-edit, an empty diff certifying the system's most-copied artifact as unchanged having compared nothing**). Extract the ```python fence from that and from the working file, and diff. **An empty diff is only meaningful if BOTH extractions produced a non-empty block** — print the byte counts of each, because two failed extractions also diff clean. **No `**Version:**` line was added** (CEO Decision 1) — confirm by grep.
>
> **Task E — the lens-count guard.** Re-verify all three count phrases still read "five", quoting each line. If any changed, HALT — no proposal in this batch adds a lens.
>
> **Task F — diff review before any DB write. ⚠️ PICK THE DIFF THAT MATCHES YOUR A0 STATE — the default command is blind on the resume path.** On a **fresh or restored run (A0 states 1/3)** the edits are uncommitted, so use `git -C /Users/marklehn/Developer/GitHub diff -- DRAFTING_CYCLE.md PLANNER_TEMPLATE.md RULE_20_SELF_CHECK_BLOCK.md`. On the **A0(2) resume path the edits are already COMMITTED, so that command returns EMPTY and this review would pass having examined nothing** — there, diff the commit instead: `git -C /Users/marklehn/Developer/GitHub show <the doc commit A0(2) identified> -- <the three absolute paths>`. **An empty diff is never a pass for this task**: if the command you ran produced no hunks, you ran the wrong one for your state — say so and re-run, do not record a clean review. Then walk EVERY hunk, attributing each to a named gap row. **An unattributable hunk is a HALT.** Paste the RAW diffstat and the per-hunk attribution.
>
> **Task F2 — ⚠️ SKIP THIS ENTIRE TASK ON THE A0(2) RESUME PATH.** There, a prior dispatch already committed the doctrine files and your tree is clean for them: `git add` would stage nothing and `git commit` would error "nothing to commit", hard-stopping the run BEFORE the flip. Carry the prior `DOC_SHA` forward instead (A0(2) identified it) and go straight to Task G. On A0(1) and A0(3), run it. **Task F2 — COMMIT THE DOCTRINE FILES NOW, BEFORE ANY DB WRITE. This is the load-bearing ordering, and it governs the COMMIT, not merely the edit.** `git -C /Users/marklehn/Developer/GitHub add` the three absolute doctrine paths (explicit paths, never `-A`; verify the staged set is exactly those three via `git -C /Users/marklehn/Developer/GitHub diff --cached --name-only`), then commit tagged `[<id>]` with your minted id, and record the SHA as `DOC_SHA`.
>
> **⚠️ Why this task exists as its own step, ahead of Task G.** An earlier draft placed the doctrine commit AFTER the flip. That applied the ordering to the EDITS while leaving the only DURABLE artifact — the commit — on the far side of the irreversible write: die between the flip and the commit, and the corpus permanently asserts ten `implemented` while the codification is uncommitted working-tree state that a worktree teardown can destroy. That is verbatim the outcome §Q7(b) exists to prevent. **Plan 278 solved the same problem differently** — its DEV committed the dev-log only and never the doc, leaving the doc commit to the Planner at wrap, and it guarded the resulting "DB implemented + doc uncommitted" window with the committed blueprint plus a wrap re-verify. Committing here is the stronger form: it removes the window rather than guarding it, and it is what makes A0(2) ("docs committed, DB not yet flipped") a state this step can actually PRODUCE — under the old order that state was unreachable, so the classifier branched on a fiction.
>
> **Task G0 — RE-CONFIRM THE SHIPPED CODE IS STILL SHIPPED, immediately before the irreversible write.** `git -C /Users/marklehn/Developer/GitHub/bellows diff a59200b..HEAD --stat -- scripts/plan_lint.py; echo "exit=$?"` must be EMPTY. ⚠️ **EMPTY OUTPUT IS NOT ENOUGH — require `exit=0` TOO.** Verified: with a valid SHA this prints nothing and exits 0; with an unreachable one (wrong repo, typo, rewritten history) it prints nothing to stdout, fatals to stderr, and **exits 128** — byte-identical stdout, so "must be EMPTY" passes on a broken invocation. Treat `exit != 0` as a HALT in its own right, distinct from "the file moved". An arbitrarily-long verdict gate has just elapsed since the blueprint; if anything landed on that file, the §4 text you have applied documents behaviour that is no longer shipped, and flipping 198 to `implemented` would close the doc↔code window on a false premise with no reverse transition. **Non-empty → HALT and report; do not flip.** **Paste the actual `git -C … log --oneline a59200b..HEAD -- scripts/plan_lint.py` and the diff** — an unrelated change (a typo fix, a comment) may not affect what §4 documents, but that is a CEO adjudication and it cannot be made from "the check was non-empty" alone. Do not judge it yourself and do not proceed.
>
> **Task G1 — THE PRE-FLIP GATE. Restate these SIX conditions as a checklist, with the evidence for each, immediately before the UPDATE. All six must hold; any one failing is a HALT.** This step carries eleven tasks and sixteen separate HALT conditions, all guarding a single write with no reverse transition — a checklist at the point of no return is the only thing that catches a condition satisfied twenty minutes ago and silently invalidated since, or one skipped in the volume. **(1)** The A0 state you matched, and that you ran the task set it requires. **(2)** All seven `DRAFTING_CYCLE.md` edits, the four `PLANNER_TEMPLATE.md` edits, and the `RULE_20_SELF_CHECK_BLOCK.md` edits are applied — and every must-survive clause still greps present (`grep -F`). **(3)** The lens count still reads five at all three phrases. **(4)** `DOC_SHA` exists and the doctrine files are COMMITTED — the flip must never precede the durable record. **(5)** G0 was empty **AND exited 0** — an empty result with a non-zero exit means the command failed, not that the file is unchanged. **(6)** The backup exists at its absolute path and its read-back returned 10 via `?immutable=1`. **Quote the evidence, do not assert the conclusion** — "all six hold" without the six results is not this gate.
>
> **Task G — DB precondition, immediately before the write.** Against the canonical absolute DB path, confirm the ten targets are still `proposed` + `route='codify'` and the count is **10**. **If the count is 0 and all ten already read `implemented`, a prior dispatch completed the flip** — that is a resume past the write, not a corrupt corpus: verify the ten by id **INCLUDING their audit columns — `route='codify'`, a populated `status_updated_by`, and a `status_updated_at` matching the pinned `…Z` form — before ratifying that prior write.** ⚠️ If the prior dispatch ran a status-only UPDATE, this is the LAST moment repair is possible: once you ratify it, the `AND status='proposed'` guard makes the UPDATE permanently unable to fill in the missing provenance, and QA row 8 then FAILs on a state nothing can fix. **A prior flip missing its audit fields is a HALT here, not a clean resume.** Then record it and proceed to the commits without re-running the UPDATE — **but FIRST locate the prior `.backup` via the prefix-glob below and record its path and counts.** ⚠️ Do not skip past the backup paragraph on this branch: the prior dev-log died with the prior dispatch, so the glob is the only way to find that snapshot, and QA row 10 asserts unconditionally that a restore point exists with its counts recorded. Without this, a materially correct resume-past-flip fails two QA rows. Any other count (1–9) means a partial or concurrent change: HALT and report the per-id states. (The `AND status='proposed'` clause makes the UPDATE itself idempotent, but the precondition must not misread a completed run as a broken one.) Then take the `.backup` restore point — **written BESIDE the canonical DB at `/Users/marklehn/Developer/GitHub/lessons-forge/`, never inside your worktree.** **⚠️ COMPUTE THE PATH FIRST, INTO A VARIABLE, THEN USE IT — never inline a command substitution inside the `.backup '...'` argument.** Plan 281 shipped this exact form for this exact reason: `BK="/Users/marklehn/Developer/GitHub/lessons-forge/data/backups/lessons-forge-pre-gate2-flip-$(date -u +%Y%m%dT%H%M%SZ).db"` (the established `data/backups/` location, per 281) then `sqlite3 "file:/Users/marklehn/Developer/GitHub/lessons-forge/lessons-forge.db?mode=ro" ".backup '$BK'"`. Its cold panel found that inlining `$(date)` into that quoted argument makes sqlite3 **misparse, exit 1, and write NO backup** — a failure visible only by running it, which is why the next sentence is mandatory. **Then PROVE the backup exists before touching the corpus:** `ls -la "$BK"` shows a non-zero size, and `sqlite3 "file:$BK?immutable=1" "SELECT count(*) FROM lesson_proposals WHERE id BETWEEN 191 AND 200 AND status='proposed';"` returns **10**. **⚠️ SCOPE BY ID — an unscoped corpus-wide count here false-HALTs a correct run**, because a lessons-forge cycle during the arbitrarily-long Step-1→Step-2 gate legitimately adds proposals. This is the same split QA row 8 carries; it must apply to the DEV's own precondition too, or the step halts before the flip on a healthy corpus. **⚠️ `?immutable=1`, NOT `?mode=ro` — verified, `mode=ro` CANNOT read a fresh backup and would false-HALT every correct run.** The live corpus is WAL (`PRAGMA journal_mode` → `wal`), so a `.backup` destination is a WAL database with **no `-shm`/`-wal` sidecars beside it**; `mode=ro` cannot create them and returns `Error: in prepare, unable to open database file (14)`. Reproduced on a scratch copy: `.backup` exit 0 at 909,312 bytes, then `file:…?mode=ro` → error 14 while `file:…?immutable=1` → `10`. `immutable=1` is the correct read-only form for a snapshot: it reads without sidecars and leaves the restore point untouched, which is exactly the intent — a plain `sqlite3 <path>` read also works but CREATES `-wal`/`-shm` beside the artifact whose job is to be pristine. A restore point you have not read back is not a restore point. ⚠️ **The DB is gitignored (`.gitignore:6: *.db`), so it does not exist in your worktree at all** — every DB operation in this task uses the canonical ABSOLUTE path `/Users/marklehn/Developer/GitHub/lessons-forge/lessons-forge.db` (the 278/284 convention). A `.backup` written into the worktree would be destroyed by `git worktree remove --force` at teardown, leaving the flip with no restore point precisely when one is needed. Name the backup's absolute path in the dev-log, and record its pre-flip counts. **If a backup from a prior dispatch already exists at that path, what to do depends on which A0 state you are in — these two rules were written by different passes and would otherwise contradict.** On the **A0(2) resume path** an existing backup is EXPECTED (the prior dispatch took it): do NOT overwrite it and do NOT halt — verify it is readable, record its counts, and reuse it as the restore point. **⚠️ FIND it, do not RECONSTRUCT its name.** The filename carries a `date -u` stamp, so a resuming dispatch cannot rebuild it — and must not try: glob the prefix (`ls -t /Users/marklehn/Developer/GitHub/lessons-forge/data/backups/lessons-forge-pre-gate2-flip-*.db`) and, better, read the absolute path the prior dev-log recorded. **Never derive that timestamp from today's local date** — a UTC stamp taken after ~18:00 local rolls to the next day, so a locally-reconstructed name matches nothing and the resume concludes "no backup exists" while one is sitting right there. (This is a documented prior failure, not a hypothetical.) On a **fresh A0(1) run** an existing backup is unexplained: report it and HALT rather than overwriting, since it may be the only pre-flip snapshot of a state you did not create. **⚠️ To make that check able to FIRE, glob the PREFIX on every state, before computing `$BK`:** `ls -t /Users/marklehn/Developer/GitHub/lessons-forge/data/backups/lessons-forge-pre-gate2-flip-*.db`. `$BK` carries a fresh second-resolution UTC stamp, so "a backup already exists at that path" is false by construction on every run — the HALT could never trigger, and its pass was indistinguishable from its silent non-execution. The prefix glob is what actually detects a prior snapshot. Then run the parameterised UPDATE. Then read back each of the ten by id and confirm `implemented`. Then confirm `SELECT count(*) … WHERE status='proposed' AND id BETWEEN 191 AND 200` is **0** (the HARD assertion). Separately report the count OUTSIDE that range as a reconcile-note — it was 0 at authoring, but a proposal created during a verdict gate legitimately raises it and **must not fail this step**.
>
> **⚠️ ORDER IS LOAD-BEARING: Tasks A–F (all doc edits) complete and verified BEFORE Task G touches the DB.** If the docs land and the DB fails, the corpus says `proposed` while the doctrine carries the rules — recoverable and obvious at the gate. If the DB flipped first and a doc edit failed, the corpus asserts ten `implemented` with no codification behind them, which no later gate re-checks.
>
> **Commit.** **⚠️ `lessons-forge.db` is NOT git-tracked — verified: `git ls-files` returns nothing for it. DO NOT commit it, and do NOT `git add -f` it.** Its durability is the `.backup` restore point plus the per-id read-back evidence, not version control; plan 284's cold panel recorded a gitignored `.db` in a scope list manufacturing a Critical ❌ on a clean run. **⚠️ THE DOCTRINE COMMIT ALREADY HAPPENED IN TASK F2 — do not repeat it here.** This step commits only the lessons-forge dev-log. **HOW MANY COMMITS EXIST DEPENDS ON YOUR A0 STATE — on the resume path the root commit was made by the prior dispatch.** If you matched **A0(2)**, the doctrine files were already committed by the prior dispatch and your tree is clean for them: **make only the lessons-forge dev-log commit**, and carry the prior `DOC_SHA` forward into the dev-log unchanged. Attempting the root commit there stages nothing and `git commit` errors with "nothing to commit" — a hard stop, at the very end of a run whose corpus flip has already landed. On **A0(1) or A0(3)** you make both. **The commits, in two different repos:** (1) the three doctrine files at the ROOT repo (`git -C /Users/marklehn/Developer/GitHub add <the three absolute paths>`); (2) the dev-log in lessons-forge. Explicit paths, never `-A`, both tagged `[<id>]` with the minted id substituted. **⚠️ Do NOT stage or commit a submodule pointer.** The root repo tracks `bellows`, `lessons-forge` and `anvil` as gitlinks, and yours is a live lessons-forge dispatch — its pointer WILL show as modified while you work. `git add` with the three explicit absolute doctrine paths never picks a gitlink up, which is exactly why `-A` is forbidden here; verify with `git -C /Users/marklehn/Developer/GitHub diff --cached --name-only` that your staged set is EXACTLY those three files before committing. **The pointer bumps are the Planner's at session wrap, not yours** — bumping one mid-flight records a submodule state that the daemon's teardown merge has not produced yet — you do not mint it, the daemon does; read it from this plan file's own name. Record BOTH SHAs in the dev-log and label which repo each belongs to — **and record the ROOT one as `DOC_SHA`, the same name A0(2) uses on the resume path.** Task F, QA row 1 and QA row 9 all take `DOC_SHA`; on a fresh run it is the commit you just created, on a resume it is the one A0(2) identified. One name, two derivations, so a reader never has to guess which SHA a later row means. (Plan 286 accumulated four names for three SHAs across separate folds and needed a pass to untangle it — this is the same trap, pre-empted.) NO push.
>
> **Scope:**
> - `/Users/marklehn/Developer/GitHub/DRAFTING_CYCLE.md`
> - `/Users/marklehn/Developer/GitHub/PLANNER_TEMPLATE.md`
> - `/Users/marklehn/Developer/GitHub/RULE_20_SELF_CHECK_BLOCK.md`
> - `knowledge/development/gate2-plan-a-dev-2026-07-30.md`
>
> (`lessons-forge.db` is deliberately absent from this list — it is untracked, so it can never appear in `files_changed` and listing it would put an unsatisfiable path in scope.)
>
> **Deposit:** `knowledge/development/gate2-plan-a-dev-2026-07-30.md` — **and it is the ONLY crash-surviving narrative record of this step, so its required contents are enumerated here rather than scattered through the tasks above.** In order: (1) `LF_TREE`, **the blueprint's `shasum -a 256` as you read it**, and which **A0 state** you matched with its evidence (pins, porcelain, the `log -1` SHA); (2) the A0(iii)-equivalent precondition results, **and Task G1's six-condition pre-flip checklist with the evidence quoted for each**; (3) each applied edit with its anchor and a post-edit grep confirming BOTH the new text and every must-survive clause; (4) **the POST-EDIT `shasum -a 256` of all three doctrine files — QA row 0 byte-compares against these and halts on mismatch**; (5) the lens-count quotes; (6) Task F's per-hunk attribution, naming which diff command your A0 state required; (7) `DOC_SHA` (from F2, or carried forward on a resume) and the root-repo commit; (8) the backup's absolute path, whether it was newly taken or reused, and its read-back counts; (9) the UPDATE statement as executed, with `$TS` shown resolved, and the per-id read-back including `route` and both audit columns; (10) the `## When this file changes` determinations — which in-flight plans inherit the amendment, and whether 197's §3 change requires a paired `plan_lint` edit; (11) `#### Prompt Feedback` in `### Ledger Updates`; (12) an **Output Receipt** marked Complete — Step 3's first gate reads it and HALTs without it, after the flip has already landed. Canonical Python/MCP file-write — NO heredoc.
>
> **Deposits:**
> - `knowledge/development/gate2-plan-a-dev-2026-07-30.md`
>
> **STOP. Do NOT proceed to Step 3. Wait for CEO verdict.**

---
---

## STEP 3 — QA

---

> **FIRST — Deliverable Verification (Rule 8 / Rule 17).** Open the Step-2 dev-log, confirm its Output Receipt is Complete before relying on it, then verify every file it claims to have modified exists and carries the described change. Table: `| Deliverable | Expected | Status (✅/❌) | Evidence |`. Any ❌ → report and HALT; make no edits yourself.
>
> **MANDATORY — Rule 20 self-check (canonical block, Checklist #4 — the exact template with all four placeholders filled, NOT a paraphrase).** Run the canonical Rule 20 self-check from `/Users/marklehn/Developer/GitHub/RULE_20_SELF_CHECK_BLOCK.md` (ABSOLUTE path). Fill: `plan_slug`: `gate2-plan-a-2026-07-30`; `qa_report_path`: `<your-own-tree-abs>/knowledge/qa/gate2-plan-a-qa-2026-07-30.md`; `evidence_dir`: `<your-own-tree-abs>/knowledge/qa/evidence/gate2-plan-a-2026-07-30/` (derive from `pwd`, NOT hardcoded); `required_evidence_files`: `[doc-integrity.txt, db-invariants.txt, suite.txt]`. Deposit all three evidence files BEFORE running the block (it `sys.exit(1)`s if any is missing or empty). **Include the block's literal stdout in the QA report: the banner `Rule 20 — QA Self-Check Results` and the `PASSED — SELF-CHECK PASSED` line must both appear byte-exact (em-dash U+2014, not a hyphen).** If it prints FAILED, halt and report rather than proceeding to closure. End with a self-grep confirming the banner is present in the deposited report. **⚠️ You are reading the block from a file this plan just EDITED — 199 added a prose section and a History row. The Python block itself must be unchanged; if the block you copy differs from what the plan describes, report it rather than adapting.**
>
> **Evidence rule:** deposit RAW command output, never a summary.
>
> **⚠️ EVERY DB QUERY BELOW USES THE CANONICAL ABSOLUTE PATH** `/Users/marklehn/Developer/GitHub/lessons-forge/lessons-forge.db`, opened read-only (`file:…?mode=ro`) since this step only reads. The DB is gitignored (`.gitignore:6: *.db`) and therefore **does not exist in your worktree**: a bare `sqlite3 lessons-forge.db "…"` silently CREATES an empty file and fails `no such table: lesson_proposals` — or you "recover" by pointing at another store and verify a corpus that was never flipped. Read the `.backup` at the absolute path the dev-log records, likewise read-only.

Verification table, one row per claim (HALT on any FAIL):
> 0b. **The DEV applied the blueprint that Step 1 actually deposited.** `shasum -a 256` the committed blueprint and byte-compare against the hash the Step-2 dev-log records having read. Mismatch → HALT: every edit below was applied from a document other than the one the SA produced and the CEO reviewed at the Step-1 gate.
>
> 0. **DOC INTEGRITY — FIRST, before any other row.** `shasum -a 256` all three doctrine files and byte-compare each against the **post-edit hashes the Step-2 dev-log records**. **Any mismatch → HALT immediately: every row below greps the LIVE files, so all of them would certify bytes the DEV never produced.** ⚠️ Row 1's drift guard is COMMIT-based (`log <DOC_SHA>..HEAD`) and is structurally blind to an UNCOMMITTED working-tree edit during the arbitrarily-long Step-2 → Step-3 gate; this row is the only thing that sees one. Also run `git -C /Users/marklehn/Developer/GitHub status --porcelain -- <the three absolute paths>` and require it EMPTY. **Both parent plans (278 row 0, 259 row 0) open with exactly this check and this rationale; it was dropped in cloning and is restored here.**
>
> 1. **Doctrine changed ONLY in the intended ways.** All three files' shasums DIFFER from the pre-edit pins (they were edited by design — an "unchanged" assertion here would be wrong). Every hunk attributes to a named gap row; no unattributable hunk. **⚠️ Diff against the DOC COMMIT SHA the dev-log records, not `HEAD~1`** — the root repo may take unrelated commits between Step 2 and Step 3 (the verdict gate is arbitrarily long), and `HEAD~1` would then diff the wrong pair while still producing plausible output. Use `git -C /Users/marklehn/Developer/GitHub show <DOC_SHA> -- <the three files>`, and separately confirm `git -C /Users/marklehn/Developer/GitHub log --oneline <DOC_SHA>..HEAD -- <the three files>` is EMPTY — ⚠️ **the `-C` is REQUIRED and its absence is a silent PASS**: run from your lessons-forge tree, `<DOC_SHA>` is not a valid object there, git fatals to stderr and prints NOTHING to stdout, which is byte-identical to the genuine no-drift result this row treats as success so nothing landed on doctrine after this plan's own edit.
> 2. **Lens count still five** — quote all three phrases with the line numbers you find.
> 3. **`DRAFTING_CYCLE.md`** — version reads `1.2`, the `Amended only through the Iteration Protocol (§6).` clause SURVIVES on `:5`, `## History` gained exactly ONE row, the `1.1` row is untouched.
> 3b. **Nothing was silently DELETED from a MODIFY region.** For every clause the blueprint listed as must-survive, grep it in the post-edit file and quote the hit — specifically `**Landing posture — warn-first (deliberate).**` in §4, the fenced canonical Cycle Log example in §3, Checklist #26's "merely QUOTE the pattern" clause and its Worked example block, and Checklist #4's "Grep the plan file for every step identified as QA". **A missing must-survive clause is a FAIL even if every new string is present** — row 4 checks additions and cannot see a deletion, and row 1's per-hunk attribution will happily attribute a deletion hunk to the gap row that caused it.
>
> 4. **Seven `DRAFTING_CYCLE.md` edits present** — one per gap row (191, 194, 195+parent, 197, 198-doc, 200×2 sites). Quote a distinguishing string from each.
> 5. **`PLANNER_TEMPLATE.md`** — version `4.81`; **BOTH new rules exist in the RULES section and nowhere else — Rule 59 (196) AND Rule 60 (192), appended in ascending order after Rule 58**, neither of them landing in the Plan Authoring Checklist (the two sections number independently, so a new rule filed as "Checklist #34" is a FAIL); Checklist #26 amended; **Checklist #4 amended to the conditional form and cross-referencing 192's rule number** (a missing coupled edit is a FAIL — the template would otherwise permit and forbid a simple banner simultaneously); Lessons Learned gained exactly ONE row, PREPENDED.
> 6. **`RULE_20_SELF_CHECK_BLOCK.md`** — the prose section exists; **the canonical Python block is BYTE-IDENTICAL to pre-edit** — ⚠️ **extract the "before" from **`git -C /Users/marklehn/Developer/GitHub show <DOC_SHA>^:RULE_20_SELF_CHECK_BLOCK.md`** — the commit's PARENT, NOT `HEAD:`, and **with the `-C`**, since from your lessons-forge tree that SHA is not a valid object and the extraction comes back empty.** By the time you run, the edits are COMMITTED, so `HEAD:` returns the POST-edit file and you would diff post-edit against post-edit, get an empty diff, and report the system's most load-bearing artifact as verified-unchanged having compared nothing. (Step 2's Task D correctly uses `HEAD:` because its edits are still uncommitted at that moment — do not clone its command into this step.) **Print the byte count of BOTH extractions**: two failed extractions also diff clean; `## History` gained one row; **NO `**Version:**` line was added** (CEO Decision 1). **AND the block's approach path is INTACT AND ADJACENT** — `## Canonical Python Block`, then its "Copy the block below verbatim" instruction, then the opening ```python fence, in that order with nothing inserted between them. 199 adds a new `## ` section to a file that every QA step in the system reads at run time to find "the block below"; byte-identical block contents do not by themselves prove an agent can still locate it. Quote the three consecutive elements as evidence.
> 7. **198-doc describes SHIPPED behaviour, and §4 no longer mandates the opposite.** §4's text matches the SHIPPED code, not the pre-286 behaviour. **⚠️ Compare against LIVE bellows main, not against the pinned SHA — and first confirm they are the same thing:** `git -C /Users/marklehn/Developer/GitHub/bellows diff a59200b..HEAD --stat -- scripts/plan_lint.py; echo "exit=$?"` must be EMPTY. **and it must also EXIT 0** — an unreachable SHA prints nothing and exits 128, which is byte-identical to the pass. If the output is non-empty, `a59200b` no longer represents what is shipped and this row FAILs regardless of how well the text matches the pin; if the exit is non-zero, the check itself did not run and that is equally a FAIL, not a pass. **Specifically: `:126`'s "WARNs iff … a fold-token but not `dry`" MUST have been amended** — quote the replacement. Appending defect descriptions while leaving that clause intact leaves §4 documenting the fix and mandating its opposite, and is a FAIL for this row even though every new string is present. **Also confirm the fold side was NOT narrowed in the doc** (286's charter bound: that change appears in no proposal and Plan A is not authorised to document it).
> 8. **Status flip complete, and it changed ONLY what it should have** — each of the ten reads `implemented` (per-id read-back, not an aggregate), **each still reads `route='codify'`, and each carries a populated `status_updated_at` and `status_updated_by`. AND the timestamp FORMAT is asserted, not eyeballed:** `SELECT count(*) FROM lesson_proposals WHERE id BETWEEN 191 AND 200 AND status_updated_at GLOB '[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]T[0-9][0-9]:[0-9][0-9]:[0-9][0-9]Z'` must return **10**. A space-separated or offset-bearing stamp passes a non-NULL check while adding a fifth format to the corpus, on an operation with no reverse transition — 19 of the 200 existing rows already carry the space form, including the most recent. Route surviving proves the UPDATE hit the intended columns rather than a broader set; populated audit fields prove it was not a status-only write that silently dropped the provenance of ten proposals becoming doctrine. A row reading `implemented` with a NULL `status_updated_at` is a FAIL, not a cosmetic gap — there is no reverse transition to re-do it properly. **Then the corpus-wide count, in TWO parts, because a flat `proposed = 0` can false-FAIL a correct run:** (a) HARD assertion — `SELECT count(*) FROM lesson_proposals WHERE status='proposed' AND id BETWEEN 191 AND 200` = **0**; (b) RECONCILE — `... AND id NOT BETWEEN 191 AND 200` was **0** at authoring, so 0 is expected, but a proposal created after authoring (any lessons-forge cycle run during this plan's verdict gates) would legitimately raise it. If (b) is non-zero, list the ids and report them as a reconcile-note; **do NOT fail this row on proposals this plan never owned.** Only (a) is absolute. (Plan 284's cold panel caught the mirror of this — an aggregate assertion that a correct in-window event would break.)
> 8b. **The A0 starting state is recorded, and the rest of the step is consistent with it.** The dev-log must name which A0 state matched (1 fresh / 2 resume-after-docs / 3 restore-and-redo / 4 foreign / 5 unclassified) with its evidence. **Then check the step's behaviour AGREES with it:** state (2) means the doc edits were already committed, so Task F must have used `git show <DOC_SHA>` rather than `git diff`, states (1)/(3) mean `git diff` instead. **⚠️ Do NOT infer backup handling from the A0 state — they are INDEPENDENT.** A0 classifies only whether the DOC EDITS are committed; a prior dispatch could have committed the docs and died before Task G ran at all, leaving state (2) with NO backup, which must then be taken fresh. Judge the backup on its own evidence: the dev-log says whether one already existed, and reuse-vs-take must match THAT, not the A0 state. Likewise the flip is a third independent bit, classified by Task G's own precondition (count 10 = not yet flipped, count 0 = already flipped). **⚠️ Count commits by TOTAL, not "per dispatch" — a resume's commits carry the same `[<id>]` as the original, so "this dispatch's commits" is not a distinguishable set.** What IS evaluable, and what to assert: **exactly ONE root doc commit exists for this plan** — assert it on **`DOC_SHA`**, which the dev-log carries: `git -C /Users/marklehn/Developer/GitHub log --oneline <DOC_SHA>^..HEAD -- <the three files>` returns exactly that one commit. ⚠️ **Do NOT route this through the `[<id>]` tag.** A0(2) exists precisely because a prior dispatch may have committed WITHOUT the tag — and the previous Gate-2 doctrine commit is itself untagged (`afd5b11 docs(governance): DRAFTING_CYCLE.md v1.1 …`). A tag-grep would return zero rows on exactly the resume path A0(2) handles, hard-FAILing a correct run after the irreversible flip has landed — two would mean the doc edits were applied twice, which the classifier exists to prevent. **A dev-log that records no state is a FAIL for this row** — Tasks F and G both branch on it, so without it you cannot tell a correct resume from a run that silently skipped its verification, and row 9's ordering claim is unauditable.
>
> 8c. **Version metadata and changelog rows are intact — the three named-but-unguarded risks.** `PLANNER_TEMPLATE.md:6` reads `**Last Updated:** 2026-07-30 (v4.81)`, consistent with `:5` (the plan mandates this bump and nothing checked it). The **v4.80 Lessons Learned row at `:1866` still exists verbatim** — a `4.80` replace-all would have rewritten it and "gained exactly ONE row" cannot see a rewritten one. And Checklist #4 still carries its **"no agent-discretion language"** clause, which the must-survive enumeration names but row 3b's list omits. Use `grep -F` for all three.
>
> 9. **Ordering honoured — prove it from DURABLE artifacts, not only the narrative.** ⚠️ The dev-log is uncommitted working-tree state until Step 2's final action, so it is the most crash-prone thing in the plan and must not be the sole evidence for its most load-bearing invariant. **Cross-check two independently-stored timestamps: `DOC_SHA`'s commit date in the root repo (`git -C /Users/marklehn/Developer/GitHub show -s --format=%cI <DOC_SHA>`) against `status_updated_at` on the ten flipped rows.** The commit must precede the flip. That comparison survives any crash and is immune to a lost or edited dev-log. Then also confirm the dev-log's own sequence agrees with it.
>
> 9b. **Ordering, narrative check** — the doc edits were applied and verified BEFORE the DB write. **There is no "DB commit" to compare against — the DB is untracked**, so establish the ordering from the dev-log's own sequence: Tasks A–F evidence (including the diff review) precedes the Task G precondition, `.backup`, UPDATE and read-back. A dev-log that shows the flip before the diff review is a FAIL regardless of the end state.
> 9c. **The pre-flip gate actually ran.** The dev-log carries Task G1's six-condition checklist with EVIDENCE quoted per condition, not a summary assertion. **A dev-log that reaches the UPDATE without it is a FAIL** — sixteen HALT conditions guard this write and nothing else re-checks them at the point of no return.
>
> 10. **Restore point exists** — the `.backup` is present and its pre-flip counts recorded.
> 11. **Suite** — the lessons-forge targeted suite passes; reconcile the count against the most recent prior lessons-forge QA report rather than a number asserted here. Run it as `python3 -m pytest src/ --tb=short -q 2>&1 | cat`, foreground, and **write the RAW output to `suite.txt`** — Rule 18 makes an evidence file mandatory for a test-suite run, and this row previously had none, which would have left the Rule 20 block passing while the one row that executes something had no deposited output.
>
> **Scope:**
> - `knowledge/qa/gate2-plan-a-qa-2026-07-30.md`
> - `knowledge/qa/evidence/gate2-plan-a-2026-07-30/doc-integrity.txt`
> - `knowledge/qa/evidence/gate2-plan-a-2026-07-30/db-invariants.txt`
> - `knowledge/qa/evidence/gate2-plan-a-2026-07-30/suite.txt`
>
> **Deposit:** the QA report + the three evidence files. Canonical Python/MCP file-write — NO heredoc. Commit (NO push). In `### Ledger Updates` include `#### Project Status` (one milestone: Gate 2 complete — ten proposals codified across three doctrine files, `DRAFTING_CYCLE.md` v1.2, `PLANNER_TEMPLATE.md` v4.81, all ten `implemented`; **`proposed` = 0 WITHIN ids 191–200**) and `#### Prompt Feedback`. ⚠️ **State the milestone scoped that way, not as a bare "`proposed` = 0".** Row 8 explicitly permits a non-zero corpus-wide count — a lessons-forge cycle during a verdict gate legitimately adds proposals — so an unscoped milestone would deposit a PERMANENT status record asserting something this plan's own guard allows to be false.
>
> **Deposits:**
> - `knowledge/qa/gate2-plan-a-qa-2026-07-30.md`
> - `knowledge/qa/evidence/gate2-plan-a-2026-07-30/doc-integrity.txt`
> - `knowledge/qa/evidence/gate2-plan-a-2026-07-30/db-invariants.txt`
> - `knowledge/qa/evidence/gate2-plan-a-2026-07-30/suite.txt`
>
> **Do NOT move this plan to `Done/`.** The close path is owned by Bellows on continue-verdict consumption (Rule 8).

---


## Drafting Cycle

> **⚠️ THIS SECTION IS A RECORD, NOT INSTRUCTIONS.** Nothing below is addressed to any agent. It is kept short deliberately: the final step's gate span absorbs this block, so a gate-matching string QUOTED here is evaluated as if the QA step had said it — verified on this plan, where a quoted path token silently suppressed a real `plan_lint` WARN and this log briefly carried both byte-exact Rule 20 banner strings, which the only HARD-FAILING gate checks. **Describe such strings; never quote them.**

**Tier:** T2 — triggers fired: **T-2** (production-data mutation: flips `status` on ten canonical proposals) **and T-6** (governance surface: edits three doctrine files); also T-7 (authored from diagnostic 285) and T-1. Both T-2 and T-6 firing makes this the highest-risk class in the system. T-8 does not fire — structure-clone of 278 and 259.
**Walks:** 5 warm (v0 → v23): 18 / 12 / 11 / 8 / 6, descending. Then the mandatory T2 cold panel, then a confirming walk.
- Weak spots:          w1 5; w2 3; w3 3; w4 3; confirming 2; cold 13.
- Destruction:         w1 3; w2 2; w3 2; w4 1; confirming 2; cold 11.
- Vulnerabilities:     w1 3; w2 2; w3 2; w4 1; confirming 1; cold 8.
- Integration-record:  w1 4; w2 3; w3 3; w4 2; confirming 2; cold 7.
- ACID:                w1 3; w2 2; w3 1; w4 1; confirming 1; cold 11.
**Cold panel (T2):** RUN — all five lenses, sequential fresh readers, each on the prior's folded draft. **50 findings, 14 HIGH, every one author-verified by execution before folding.** It found more than five warm walks combined, and in a different class: the warm walks caught coherence, the panel caught defects invisible to reading — a restore point that could not be opened, a `grep` that errored into a pass, a `checkout` that silently no-opped, a liveness check satisfied by a fatal, and a QA guard both parent plans carried that this clone had dropped.
**Conflicts:** CL1 — the row-7 guard INVERTS here (doctrine changes by design), so absolutist "unchanged" language cloned from 282/283/284 would false-FAIL a correct run. CL2 — doc edits, and their COMMIT, strictly before the DB flip. CL3 — the Rule 20 Python block stays byte-identical while its own file is edited. CL4 — the three resume bits (docs committed / backup exists / flip done) are INDEPENDENT and must never be inferred from one another.
**Honest marks:** five factual errors of mine, four caught by cold readers (two blast-radius claims, two corpus censuses, one false rationale for what the suite verifies); one fold recorded in this log as applied that was NOT in the artifact — a false attestation of the 2026-07-28 class; and one index-based whole-file rewrite that my own standing rule forbids, which happened to be safe here and truncated plan 286's draft earlier the same day.
**Full narrative:** `scratchpad/cycle-narrative-planA-FULL.md` — session-local and ephemeral; this compact log is the durable record, and nothing in the plan cites a fold id that only the narrative defines.
**Confirming walk (after the panel):** 8 folds (2/2/1/2/1) — no mechanism redesigned; the yield was a cross-step instruction no step could act on, a fix that landed at a task but not at the branch routing to it, a liveness check satisfied by a fatal, a stale deviation count, this log's own extraction (per §3 and plan 286's RE1), and the blueprint — the one input every edit is applied FROM — never pinned by hash while three doctrine files and the Rule 20 block were.
**Closing:** ⚠️ **DEPOSITED WITH THE CYCLE OPEN, by explicit CEO decision — §2's closing condition is NOT met and this is recorded rather than papered over.** The last event was a fold (the unpinned-blueprint fold at confirming-ACID), not a dry lens pass, so no closing pass attests that the final draft is stable. **§5's mechanical conformance is also PARTIAL:** `plan_lint` was executed repeatedly throughout (final state: 2 expected WARNs, 0 FAIL, exit 0, both WARNs explained with their mechanism), but the by-scope walk of the Orchestration Rules and the Authoring Checklist was not run for this plan. **Note the closing WARN is silent for a STRUCTURAL reason, not because the cycle closed** — the compact log records fold counts rather than the token the check greps for. 113 folds across 5 warm walks, a 5-pass cold panel and a confirming walk; the draft has been stable in mechanism since the panel, and the residual risk is that no independent pass has read the final assembly end to end.
