# Lessons Forge — Cycle Run 2026-07-27 (ingest + classify the 2-entry planner-discipline authoring batch)
**Date:** 2026-07-27 | **Tier:** Small | **Dispatch Mode:** bellows | **Test Scope:** both | **Execution:** Step 1 (Lessons Agent — ingest + classify both) → Step 2 (DEV — report) → Step 3 (QA) | **qa_steps:** 3 | **pause_for_verdict:** always
**cycle_tier:** T2

## CEO Context

Cycle run only: ingest the 2 un-ingested LESSONS.md entries (both dated 2026-07-27) and classify them into proposals. Gate 1 (route disposition) and Gate 2 (codification) are separate plans with CEO decisions between. Direct clone: **274** ran this exact cycle shape (its 4-entry batch); its lineage is 257 (cycle, 15-entry split 8+7) → 258 (Gate 1) → 259 (Gate 2). **This batch is 2 — no split** (well under the proven-safe 12; a single classification step). Cloning the NEWEST same-class plan (274), not an older cycle, so the guards 274's own cold panel restored (fail-closed QA-row-4, regenerate-deposits-from-committed-DB, absolute RULE_20 path, pre-ingest entry_id G1) carry forward intact — this is exactly the discipline entry (a) of this very batch names.

**The 2 entries are planner-discipline authoring refinements** (captured from session 10, the DRAFTING_CYCLE.md hardening arc's close):
1. **[entry (a), id 183] Clone against the NEWEST same-class plan, not just the origin.** Gate 2 Plan A (278) cloned 259 but sibling 275 had hardened 3 things; the cold panel caught all 3 reversions. A **cold-panel / drafting-cycle** refinement.
2. **[entry (b), id 184] Choose the QA Rule 20 self-check FORM by plan class** (full canonical block + real evidence files for a doc/DB plan; simple banner for a move-only plan; a full-form mandate with no evidence files is an unsatisfiable, plan-halting QA step). A **QA-step-authoring / Plan Authoring Checklist** refinement.

**⭐ TARGET-ARTIFACT — the two SPLIT (this is the key delta from 274, where all 4 shared one target).** Both are `governance_rule` / `governance` layer, but they refine DIFFERENT artifacts, so there is **no blanket override**:
- **(a) id 183 → `target_artifact='DRAFTING_CYCLE.md'`** (override the specialist's `PLANNER_TEMPLATE.md` default): it sharpens the cold-panel / clone-review practice, a Drafting Cycle matter. **Verified in live `DRAFTING_CYCLE.md` 2026-07-27:** the surfaces this lesson refines are **§2.6 the cold-panel pass** (`:73` "rotate the reviewer, not the lens" — entry (a)'s named family) **and §1 the Rigor-Tier Gate** (proven-clone / don't-down-tier); §2.7 sequential-fold (`:82`) is only the warm-review that FAILS to catch clone-drift, NOT the surface amended. §6 says amend "into this file."
- **(b) id 184 → `target_artifact='PLANNER_TEMPLATE.md'`** (the specialist default is correct here): it refines how a QA step is authored (which Rule 20 form + which evidence files), a **QA-step-authoring** matter. **Verified in live `PLANNER_TEMPLATE.md` 2026-07-27:** Rule 18 "QA evidence file requirement" (`:557`) and the mandatory Rule 20 self-check block (`:565`) are where this doctrine lives; the M1 "carry the full Rule 20 form into every QA step" directive sharpens to form-by-class there. **(Pre-scouting these anchors is Rule 27 — Gate 1/Gate 2 author from them, not from a re-derivation.)**

Classify each on **merits from its own `raw_content`**; the above is guidance, not a substitute for reading the entry. If the classifier's reading of (b) lands on `DRAFTING_CYCLE.md` (§5 mechanical-conformance QA-authoring) instead, that is defensible — **set whatever the entry supports, but SET both target fields, and never leave either NULL.** Gate 1 dedups against live doctrine and routes; Gate 2 places. **Neither entry has a hard `plan_lint.py` code coupling** — both are pure doctrine (entry (b) explicitly says the gate "requires only the banner + PASSED line either way — the choice is about rigor, not gate-passing"), so there is no doc+gate pairing to name this cycle (unlike 274's N5/N6).

**Cluster synthesis for Gate 1:** flag as **"2 planner-discipline authoring refinements — SPLIT target"** (1 cold-panel → `DRAFTING_CYCLE.md`, 1 Rule-20-form → `PLANNER_TEMPLATE.md`); do NOT skip or downgrade either (that makes a Gate 1 decision without a CEO). Name the split so Gate 1 knows the two may route to different artifacts / different Gate-2 plans.

**⚠️ NUMBERING DISAMBIGUATION.** File-position counts (127 `##` headings) and DB row ids (`lesson_entries.id`, `lesson_proposals.id`) are different namespaces. The agent derives every DB id from `get_unclassified_entries` / the ingest return — never hand-types a batch id. **The corpus row count is 182, NOT 127 — do not read that as corruption** (surplus are orphan rows from headings reworded over the project's history; all classified, which is why `get_unclassified_entries()` is `[]` pre-cycle). **After this cycle expect 184 rows (182 + 2) and 192 proposals (190 + 2).**

**⚠️ E0 / P0 — the resume-invariant handles** (confirm at Step 1a, do NOT assume; HALT if either differs on a fresh run): **`E0 = MAX(id) FROM lesson_entries = 182`, `P0 = MAX(id) FROM lesson_proposals = 190`.** The 2 batch entries permanently occupy ids **183–184** (`sqlite_sequence == MAX(id) == 182` for `lesson_entries` and `== 190` for `lesson_proposals` — verified 2026-07-27, no gap — so inserts land exactly at entries 183–184 / proposals 191–192; G6 still derives the batch range dynamically as the safety net). **`entry_id > 182` is the canonical handle for this cycle's entries; `route IS NULL` is NOT** (pre-existing proposals already carry NULL routes). On a RESUME use the recorded `E0`/`P0` from the prior dispatch — do NOT re-run `MAX(id)` (post-ingest it returns 184/192 and would exclude this cycle's own rows).

**⚠️ G1 precondition (fresh-run safety) — measured 0 at authoring.** The ingest's update path stales **non-terminal** proposals (`proposed`/`accepted`/`ambiguous`); the plan-204 `_TERMINAL_STATUSES` guard protects the terminal set. Measured 2026-07-27: **ZERO** non-terminal proposals (137 implemented / 7 reference / 15 rejected / 3 stale / 28 superseded) → the fresh-run ingest is provably non-destructive. Confirm from your own baseline; any non-terminal proposal on a fresh run voids the safety → HALT before the ingest.

**⚠️ The `updated_count` / hash-trap watch (plan-204's live test) — ALREADY PLANNER-PRE-VERIFIED.** Appending this batch gave the previously-last-ingested entry a trailing `---` — the exact trigger that used to flip a `content_hash` over whitespace. **Target: entry id 182** — *"2026-07-24: DRAFTING_CYCLE.md §3's own T0 cycle_tier format example TRIPS the §4 plan_lint regex…"*, `content_hash` `75bf99cd741474217e3b800c3513ddc62b029620b683cff8382aad223d75de52` (confirm the full hash from your baseline). The Planner ran a read-only WHOLE-CORPUS re-hash post-append (`parse_lessons_md` over all 127 file entries vs the DB's stored `content_hash`es, 2026-07-27) and confirmed **exactly 2 would-INGEST (this batch), 0 would-UPDATE, 125 unchanged** (plus 57 orphan DB rows unmatched by the file — expected, reworded headings) — so entry 182's trailing-separator gain does NOT flip its hash (204 holds) AND no other untouched entry has drifted. **Expectation: `ingested_count == 2`, `updated_count == 0`, `terminal_proposals_flagged` empty. Verify and report the ACTUAL values; do not force the number.** Non-zero either way → HALT and diagnose (whitespace-only = a 204 regression; substantive = an unexpected edit the CEO must confirm).

**⚠️ The plan-154 dedup advisory is RETIRED — do NOT expect advisory lines** (plan 207 removed `detect_recently_implemented_overlaps`). Any `Recently-implemented overlap:` line in the report is a regression → HALT.

**Do NOT dedup against DRAFTING_CYCLE.md or PLANNER_TEMPLATE.** Classification classifies on merits; **Gate 1** dedups against the live doctrine. Both entries propose authoring refinements — FLAG them as the split cluster above for Gate 1, do NOT skip or downgrade either.

**Scope discipline:** cycle run only. Routes stay `NULL` at insert — the CEO assigns at Gate 1. **Do NOT edit `DRAFTING_CYCLE.md` or `PLANNER_TEMPLATE.md`** (this batch's Gate-2 targets; codification is Gate 2). **⚠️ Do NOT append to LESSONS.md while this plan is deposited-but-un-run** — the cycle parser-pins its batch at 2; a mid-flight append makes every count wrong and halts G6 (memory `lessons-forge-cycle-pins-batch-by-parser-count`).

**⚠️ Concurrency — dispatch with NO other lessons-forge cycle in flight (ACID/isolation).** This cycle's 2 proposals are inserted `status='proposed'` (NON-terminal); the plan-204 guard protects only terminal statuses, so a CONCURRENT cycle's whole-corpus ingest in the Step-1→Step-3 window COULD stale them. The Step-1 single-writer check + daemon serialization are the pre-flight guards; the DETECTION is **QA row 2** (`get_unclassified_entries` re-surfaces a staled entry → `!= []` → FAIL) **+ QA row 4** (`stale` not grown). ⚠️ NOT a row-3 count — staling is an UPDATE to `status='stale'`, not a DELETE, so `COUNT(entry_id>182)` is monotonic and can NEVER drop below 2; it cannot detect a staled proposal. The daemon serializes same-project plans, so this is belt-and-suspenders — but do NOT dispatch a second lessons cycle until this one closes.

**No diagnostic precedes this plan, deliberately** (recurring-cycle practice per 247/257/274): every unknown was measured inline at authoring against live data — batch delta (2), G1 precondition (0 non-terminal), E0/P0 (182/190), the hash-trap target (id 182, hash `75bf99cd…`, pre-verified stable), root HEAD. **✅ AUTHORING NOTE — LESSONS.md is ALREADY committed (root `1827337`, done before drafting this plan), so the batch-pin/commit-first ordering is satisfied; G2 confirms the clean porcelain.** The daemon claims a deposited plan within seconds; this plan is deposited only after this draft's cycle closes.

**Deposit-once discipline:** deposited exactly once. **Authoring self-check:** `bellows/scripts/plan_lint.py` run at authoring — expect exit 0, (a)-(d) PASS, §4 Drafting-Cycle check PASS (bare `cycle_tier: T2` + full block). The one WARN — *"step 3 mentions tests but declares no test scope"* — is the known-benign QA-runs-pytest-without-modifying-tests class (memory `benign-gate-failure-classes`); do NOT add test files to Step 3's scope to silence it.

## How to Run This Plan

**Bootstrap prompt:**
```
Read the plan at lessons-forge/knowledge/decisions/in-progress-executable-<id>.md (daemon renames the deposited placeholder on claim). Execute Step 1 ONLY. After completing Step 1, STOP and wait for my confirmation. Do NOT proceed to Step 2.
```

---
---

## STEP 1 — Lessons Agent (ingest whole corpus + classify both)

---

> **FIRST — before any reads or work: post a short visible chat message (1-2 sentences) confirming you are starting this plan and your immediate next action.** Do NOT rename the plan file.
>
> You are the Forge Lessons Agent. Read your specialist `agents/FORGE_LESSONS_AGENT.md` first (ADR-002 six-value taxonomy). **Its DB paths are written relative to the GitHub root, and you may run in a worktree** — so **every canonical-DB access in this plan uses the ABSOLUTE path** `/Users/marklehn/Developer/GitHub/lessons-forge/lessons-forge.db`. **`forge/forge.db` is a REAL but DIFFERENT database — never open it.**
>
> **Working location — the plan-225 trap.** Run commands from **your own working tree** and write every file there. **The ONLY exception is canonical-DB access, by the ABSOLUTE path above.** Do NOT `cd` to the main tree: report/deposit writes resolve RELATIVE paths, so a main-tree cwd puts output in main while you commit in the worktree — the untracked-file collision that tore down plan 225.
>
> **Single-writer assumption.** Before Step 1a, confirm no concurrent cycle: `get_unclassified_entries` stable across two reads a moment apart, and no `in-progress-*lessons*`/`in-progress-*cycle*` plan in `knowledge/decisions/`. If either suggests a concurrent writer, HALT.
>
> **Scope:**
> - `knowledge/development/classifications-cycle-2026-07-27.md`
> - `knowledge/development/dev-log-cycle-step-1-2026-07-27.md`
>
> **Step 1a — restore point, then baseline. Before touching anything.** Back up canonical with `.backup` (NOT `cp` — a live WAL exists), to the MAIN tree by ABSOLUTE path (a worktree-local backup is destroyed by teardown): **build the path in a shell variable first, then back up** (verified-working form):
> ```
> mkdir -p /Users/marklehn/Developer/GitHub/lessons-forge/data/backups
> BK="/Users/marklehn/Developer/GitHub/lessons-forge/data/backups/lessons-forge-pre-cycle-$(date -u +%Y%m%dT%H%M%SZ).db"
> sqlite3 "file:/Users/marklehn/Developer/GitHub/lessons-forge/lessons-forge.db?mode=ro" ".backup '$BK'"
> ```
> ⚠️ Do NOT inline the `$(date …)` directly between single-quoted parts of the `.backup` argument (`.backup '…pre-cycle-'$(date …)'.db'`) — sqlite3 MISPARSES that and errors `unknown database …pre-cycle-` with NO backup written (verified 2026-07-27). The `$BK`-variable form is verified to produce a valid backup. **Use exactly the COMPACT colon-free UTC stamp `$(date -u +%Y%m%dT%H%M%SZ)` → e.g. `20260727T143000Z`** (matches the existing backups' format `20260721T…`, so the resume-glob below finds it; NOT a hyphenated `2026-07-27T…` date). `.gitignore` matches `*.db`, so it is not committed — confirm absent in `git -C /Users/marklehn/Developer/GitHub/lessons-forge status --porcelain`. **Then VERIFY the restore point is real, not ceremony (corpus discipline — an unverified restore point cannot protect the mutation it precedes): `sqlite3 '<backup-path>' 'PRAGMA integrity_check;'` must return `ok`, AND the backup's `SELECT COUNT(*) FROM lesson_entries` / `FROM lesson_proposals` must equal **the LIVE canonical DB's counts at backup time** (the backup is a faithful, complete snapshot). ⚠️ Match the backup to the live DB **NOW**, not to a hardcoded number: on a FRESH run those counts are the pre-cycle 182 / 190; **on a RESUME the live DB is already mutated (≈184 / ≈192), and the fresh `.backup` correctly snapshots that — do NOT assert 182/190 or you false-HALT every resume.** If the `.backup` command errored, the file is missing, integrity_check ≠ `ok`, or a count differs from the live DB → HALT before the ingest.** State the absolute backup path in your dev log. ⚠️ On a RESUME the `.backup` runs again and captures MID-cycle state — the pristine pre-cycle restore point is **the earliest-timestamp backup matching THIS cycle's compact-stamp pattern `lessons-forge-pre-cycle-20260727T*`** (NOT the directory's overall earliest — `data/backups/` persists prior cycles' backups, e.g. a `2026-07-20T…` file; and NOTE the compact format `20260727T`, matching the pinned Step-1a stamp above — a hyphenated `2026-07-27T` glob would match nothing); if the Step-1 dev-log Output Receipt exists, prefer the exact backup path it recorded (item 6). Name which file is the pre-cycle one.**
>
> **Then capture the baseline** (read-only), verbatim in the dev log: proposals by `status`, proposals by `category`, total `lesson_entries` count, and the id + `content_hash` of the entry currently last-ingested by id (**confirm it is id 182**, *"2026-07-24: DRAFTING_CYCLE.md §3's own T0 cycle_tier format example TRIPS the §4 plan_lint regex…"*, hash `75bf99cd741474217e3b800c3513ddc62b029620b683cff8382aad223d75de52` — the hash-trap target). **Also capture the two FIXED constants: `E0 = SELECT MAX(id) FROM lesson_entries`, `P0 = SELECT MAX(id) FROM lesson_proposals`. Confirm `E0 = 182`, `P0 = 190`; if either differs on a fresh run, HALT.** On a RESUME read E0/P0 from the prior deposit — **or, if a mid-classification crash left no deposit, from this plan's stated constants (`E0=182`, `P0=190`)**; do NOT re-run `MAX(id)` (post-ingest it returns 184/192 and would exclude this cycle's own rows). **State `E0` and `P0` on their own line in the Output Receipt.**
>
> **⚠️ G1 fresh-run precondition:** the baseline must show zero `proposed`/`accepted`/`ambiguous` proposals; any such → HALT before the ingest (resume exempt).
>
> **Step 1b — run the ingest (ONCE, this step only).** Open canonical **read-WRITE** (plain `sqlite3.connect("/Users/marklehn/Developer/GitHub/lessons-forge/lessons-forge.db")` — NOT `?mode=ro`). Call `run_full_lessons_cycle(conn)` (its `lessons_md_path` defaults to `/Users/marklehn/Developer/GitHub/LESSONS.md` — confirm that is what it read). **`conn.commit()` after it returns** (the SQLite transaction — the DB is gitignored; skip it and a step death LOSES the ingest). **This is a WHOLE-CORPUS re-hash** — any edit anywhere in LESSONS.md since the last cycle lands here as `updated_count`. **Record and PRINT the actual returned dict:** `ingested_count`, `updated_count`, `unchanged_count`, `duplicates_marked_count`, `terminal_proposals_flagged`, `needs_classification`.
>
> ## Step 1 gates — G1 through G6 (report EVERY one as a table row with its measured value + PASS/HALT)
>
> **Timing:** G2 pre-ingest; **G1 pre-ingest ALWAYS** (its entry_id check distinguishes fresh vs resume without needing the post-ingest dict); G3–G6 read the Step-1b dict.
> - **G1 — non-terminal precondition (resume-aware, PRE-ingest — this breaks the resume circularity).** Examine the baseline non-terminal proposals (`proposed`/`accepted`/`ambiguous`) BY `entry_id`: **NONE** → fresh run → PASS. **All within the batch range (`entry_id > E0`=182)** → RESUME (they are this cycle's own already-committed classifications) → `PASS (resume)`; the re-ingest is a verified no-op that stales nothing. **ANY with `entry_id ≤ E0`** → a genuine pre-existing non-terminal proposal → the fresh-run non-destructive-ingest guarantee is VOID → HALT before ingest. (Do NOT gate on `ingested_count` — that is only known POST-ingest; use the `entry_id` of the baseline non-terminal proposals.) **Scope note:** this entry_id check governs INGEST SAFETY only; whether *classification* is fresh-2 or resume-subset is G5's `ingested_count` signal. In the narrow "ingested but nothing classified yet" resume, G1 reads "fresh" (0 non-terminal) while G5/G6 read "resume" (`ingested_count==0`) — both correctly PASS, and classification follows `get_unclassified` regardless.
> - **G2 — LESSONS.md provenance. THE GATE is: `git -C /Users/marklehn/Developer/GitHub status --porcelain -- LESSONS.md` is EMPTY** (use `git -C <absolute root>` — a bare `git status` from the submodule worktree passes VACUOUSLY because LESSONS.md is absent there — **the vacuous-submodule-diff failure class (an established corpus discipline: a bare git command in the submodule cannot see a root-tracked file, so it passes without checking anything)** — do not let this gate fall vacuous). Non-empty → HALT (the Planner committed the LESSONS.md 2-entry append before dispatch; a dirty LESSONS.md means an unexpected edit — do NOT ingest an uncommitted corpus). Separately RECORD `git -C /Users/marklehn/Developer/GitHub rev-parse --short HEAD` for the audit trail (Planner's expected value: `1827337` — a mismatch here is a note to reconcile, NOT a halt; the clean-porcelain check is the gate).
> - **G3 — `duplicates_marked_count == 0`** (silent-drop guard). Non-zero → HALT and name which entry ids + why.
> - **G4 — `updated_count == 0` AND `terminal_proposals_flagged` empty** (hash-trap watch on entry id 182; the Planner pre-verified this stable). Non-zero either way → HALT and show the diff, classifying whitespace-only (204 regression) vs substantive (unexpected edit, CEO confirms).
> - **G5 — there is work to do.** `ingested_count == 0` AND `needs_classification` empty → disambiguate: (a) truly nothing → HALT; (b) proposals for this cycle exist but deposits absent → deposit-completion resume, **re-generate deposits from the committed DB**, STOP. `ingested_count==0` but `needs_classification` non-empty → `PASS (resume)`. Fresh run expects `ingested_count == 2`. **Any `ingested_count` ∉ {0, 2} → HALT (unexpected ingest cardinality — a heading collision with an orphan row or a partial ingest; diagnose before classifying, do NOT fall through the run-type branches).**
> - **G6 — work-list reconciliation.** The batch range is `E0+1 .. E0+2` (= 183–184), computed ARITHMETICALLY from the confirmed `E0=182` — **do NOT derive the range FROM `needs_classification`** (that makes the invariant below self-referential and vacuous — a set is always a subset of itself). If a future `sqlite_sequence > MAX(id)` gap shifted the inserts, recompute `E0+1..E0+2` from the actual `E0`; don't trust the literal 183–184. **THE INVARIANT: every id in `needs_classification` is within that arithmetic batch range (i.e., `> E0` and `≤ E0+2`).** HALT on any id OUTSIDE it — an older entry surfaced (a prior proposal went `stale`, the 204 signature) → the CEO chooses (i) classify batch + extra, (ii) batch only + record deferred id(s) under `### Deferred entries (CEO-approved)`, or (iii) investigate. Then, **by run type** (the resume carve-out — mirrors G1/G5): **FRESH run (`ingested_count==2`)** → `needs_classification` must be EXACTLY the 2; FEWER → a batch entry is masked (cross-check G3 `duplicates_marked_count==0`) → HALT. **RESUME (`ingested_count==0`)** → a SUBSET of the batch range is EXPECTED (the un-classified remainder of a partial Step-1's incremental commits, NOT corruption) → `PASS (resume)`; only an out-of-range id is a HALT. Reconcile explicitly: list the ingested ids, list the work list, name any out-of-range id.
>
> **After the gate table: if all PASS (or `PASS (resume)`), continue to classification. Any HALT — stop and report; the ingest stays committed (G1 made it safe).**
>
> ### Classification — every entry `get_unclassified_entries` returns (the 2 on a fresh run; the un-classified remainder on a resume)
>
> **`get_unclassified_entries` is AUTHORITATIVE — classify EXACTLY what it returns, in ascending `id` order; NEVER hand-derive "183–184" or anchor on a literal "2".** On a fresh run it returns the 2 batch entries; on a RESUME it returns only the un-classified remainder (already-classified entries are excluded — this exclusion is the SOLE mechanism preventing a double-insert, so overriding it with a literal count would re-insert and corrupt). For each: read `id, source_heading, raw_content, tags, entry_date` from `lesson_entries`, apply ADR-002, and call `insert_proposal(conn, entry_id, category, suggested_action, reasoning, confidence, ...)` — **SIX required positional args** (re-verified live 2026-07-27 against `lessons-forge/src/lessons_forge.py:202` — also confirmed `get_unclassified_entries`:267, `run_full_lessons_cycle`:416, `generate_lessons_report`:514 exist as called; `entry_id` comes FIRST after `conn` — do NOT drop it, and do NOT hand-type it); `status`/`target_layer`/`target_artifact` are keyword args:
> - `entry_id` — the FK to `lesson_entries.id`, taken from `get_unclassified_entries` (Rule #47 — never hand-typed).
> - `category` ∈ `structural`/`instrumentation`/`governance_rule`/`language`/`narrative` (never hand-assign `duplicate`). **Both entries are expected `governance_rule`** (authoring-discipline doctrine); classify on merits from `raw_content` and call out any genuine no-fit as `ambiguous` by id.
> - `suggested_action` — concrete recommendation (what changes, where). **Do NOT name a `plan_lint.py` coupling** — neither entry proposes a gate change (entry (b) explicitly says the choice is about rigor, not gate-passing).
> - `reasoning` — **quoted evidence** citing specific `raw_content` text from THAT entry, naming its id. Do NOT carry any claim from this plan's CEO Context into a `reasoning` field — the entry is the only source. If you cannot cite specific `raw_content`, STOP and report rather than writing generic justification (a thin proposal passes every gate — worse than a halt).
> - `confidence` ∈ `low`/`medium`/`high`.
> **Target fields — SET BOTH on every proposal, but they SPLIT this batch (do NOT blanket to one artifact):** `target_layer='governance'` for both; **`target_artifact` per entry** — the cold-panel/clone entry (id 183) → **`DRAFTING_CYCLE.md`** (override the specialist `PLANNER_TEMPLATE.md` default); the Rule-20-form entry (id 184) → **`PLANNER_TEMPLATE.md`** (the default — a Plan Authoring Checklist refinement). Set whatever each entry's `raw_content` supports; **never leave `target_layer` or `target_artifact` NULL** on a non-`ambiguous` proposal. Only `route` and `subcategory` stay `None`.
> **⚠️ The a→`DRAFTING_CYCLE.md` / b→`PLANNER_TEMPLATE.md` split is the Planner's Rule-27 placement SCOUT (verified against live doctrine at authoring), NOT a mandate.** Derive each target INDEPENDENTLY from the entry's own `raw_content`; you have explicit **licence to disagree**. If your reading diverges from the scout, set what the entry supports and RECORD the divergence + why in the classification deposit — do NOT silently conform to the scout, and do NOT silently override it. This split costs more under anchoring than a uniform-target batch: a scout copied without reading could SWAP the two (which entry maps to which artifact), so map each by its content, not by position (the corpus lesson: *"a plan's pre-stated conclusions anchor the executing agent"*).
>
> **`conn.commit()` after each `insert_proposal` (or after the 2nd at latest). Then git-commit the two `.md` deposits.**
>
> **Self-report:** print `SELECT id, entry_id, status, category, target_artifact FROM lesson_proposals WHERE entry_id > 182 ORDER BY id` — expect **2 rows** (entry_ids 183–184, proposal ids 191–192; target_artifact split DRAFTING_CYCLE.md / PLANNER_TEMPLATE.md). Confirm `get_unclassified_entries()` is now `[]`. Put both in the Output Receipt.
>
> **Deposit:** `knowledge/development/classifications-cycle-2026-07-27.md` (the cycle dict, the 2 classifications' per-entry reasoning, the "2 planner-discipline authoring refinements — SPLIT target" cluster synthesis for Gate 1, any `ambiguous` by id) + `knowledge/development/dev-log-cycle-step-1-2026-07-27.md` with an Output Receipt containing, each on its own labelled line: (1) the cycle dict verbatim; (2) the G1–G6 gate table; (3) the pre-cycle baseline; (4) `E0` and `P0`; (5) the self-reported 2-row created-proposal list + `get_unclassified_entries()==[]`; (6) the absolute backup path; (7) any flags. Canonical Python file-write — no heredoc. Commit both. `#### Prompt Feedback` in `### Ledger Updates`.
>
> **Deposits:**
> - `lessons-forge/knowledge/development/classifications-cycle-2026-07-27.md`
> - `lessons-forge/knowledge/development/dev-log-cycle-step-1-2026-07-27.md`
>
> **STOP. Do NOT proceed to Step 2. Wait for CEO verdict.**

---
---

## STEP 2 — DEV (generate the report)

---

> **Before starting, read the Step 1 deposits and confirm the Output Receipt Complete; otherwise halt and report.** Post a short visible chat message. You are the Forge Developer (`/Users/marklehn/Developer/GitHub/forge/agents/FORGE_DEVELOPER.md` — skip with a note if absent). Same working-location + absolute-DB rules. **Open read-only:** `sqlite3.connect("file:/Users/marklehn/Developer/GitHub/lessons-forge/lessons-forge.db?mode=ro", uri=True)`.
>
> **Scope:**
> - `reports/lessons-report-2026-07-27.md`
> - `knowledge/development/dev-log-cycle-step-2-2026-07-27.md`
>
> **Before generating: if `reports/lessons-report-2026-07-27.md` already exists AND this step's dev-log deposit is committed → HALT** (a real re-run; `generate_lessons_report` overwrites unconditionally). If the report exists but the Step-2 deposit is ABSENT → deposit-completion resume (report regen is deterministic + read-only, so re-running is safe). Verified at authoring: no 07-27 report exists.
>
> Run `generate_lessons_report(conn, "2026-07-27")`. **Note the report is WHOLE-CORPUS** — it surfaces every `proposed`/`ambiguous` proposal (`WHERE status IN (...)`); the date argument is only the filename/title. Its accuracy as "this cycle's report" rests on G1 (0 pre-existing non-terminal → the only proposals it can surface are this cycle's 2). **Two halt conditions (regressions):** (1) any `- **Route:**` line appears (all routes NULL this cycle — plan-128 conditional render) → HALT; (2) any `Recently-implemented overlap:` line appears (plan 207 retired that detector) → HALT.
>
> `generate_lessons_report` resolves `output_dir="reports"` RELATIVE to cwd — run `pwd` before the call, capture the path the function RETURNS, state that absolute path in the dev log, and confirm the returned filename matches Scope (report the actual if it differs, do not rename). **Note the latent `encoding=` gap** (`src/lessons_forge.py:593` — re-verified 2026-07-27 — writes with no explicit `encoding=`; safe on the Mac/Bellows UTF-8 default; a Forward-Register item, not a blocker). Print the report head (~80 lines).
>
> **Deposit:** the report + `knowledge/development/dev-log-cycle-step-2-2026-07-27.md` (Output Receipt: report length, proposals surfaced, route-line count, advisory-line count). Canonical Python file-write — no heredoc. Commit both. `#### Prompt Feedback`.
>
> **Deposits:**
> - `lessons-forge/reports/lessons-report-2026-07-27.md`
> - `lessons-forge/knowledge/development/dev-log-cycle-step-2-2026-07-27.md`
>
> **STOP. Do NOT proceed to Step 3. Wait for CEO verdict.**

---
---

## STEP 3 — QA

---

> **Before starting, read the Step 1–2 deposits and confirm both Output Receipts Complete; otherwise halt and report.** Post a short visible chat message confirming you are starting Step 3 (QA). You are Lessons Forge QA (`agents/FORGE_LESSONS_AGENT.md`). Same working-location + absolute-DB rules. **Verification + reporting only — no product-code changes; if a test fails, report it, do NOT fix it.** Do NOT use Monitor. Do NOT edit PROJECT_STATUS directly.
>
> **MANDATORY — Rule 20 self-check banner.** Your deposit MUST contain, verbatim, `## Rule 20 — QA Self-Check Results` and a line `**PASSED — SELF-CHECK PASSED**`. End with a self-grep confirming the banner. Run the canonical block from `/Users/marklehn/Developer/GitHub/RULE_20_SELF_CHECK_BLOCK.md` (ABSOLUTE path — the block lives at the governance root, NOT in the lessons-forge worktree; a bare-name read fails there, the same vacuous-submodule trap G2/row-7 guard against) — values: `plan_slug`: `cycle-authoring-refinements-2026-07-27`; `qa_report_path`: `<abs>/knowledge/qa/cycle-qa-2026-07-27.md`; `evidence_dir`: `<abs>/knowledge/qa/evidence/cycle-authoring-refinements-2026-07-27/`; `required_evidence_files`: `[full-suite.txt, invariants.txt, hash-trap.txt, schema.txt]`. Include the literal stdout; if it prints FAILED, halt.
>
> **Evidence-source rule.** Every SQL/PRAGMA row states which DB it ran against (canonical = `sqlite3 "file:/Users/marklehn/Developer/GitHub/lessons-forge/lessons-forge.db?mode=ro" "<query>"`). Deposit **RAW command output, never a summary of it** (memory `qa-evidence-raw-output`).
>
> **Scope:**
> - `knowledge/qa/cycle-qa-2026-07-27.md`
> - `knowledge/qa/evidence/cycle-authoring-refinements-2026-07-27/full-suite.txt`
> - `knowledge/qa/evidence/cycle-authoring-refinements-2026-07-27/invariants.txt`
> - `knowledge/qa/evidence/cycle-authoring-refinements-2026-07-27/hash-trap.txt`
> - `knowledge/qa/evidence/cycle-authoring-refinements-2026-07-27/schema.txt`
>
> Verification table, one row per claim, each with a DB-source column (HALT on any FAIL):
> 1. **Full suite** — `python3 -m pytest src/ -v` to an explicit pass/fail, raw tail shown. **Compute the baseline yourself from `--collect-only` and reconcile against the most recent prior QA in `knowledge/qa/`** (reconciliation only — do NOT carry a number forward as expected). Confirm 0 regressions.
> 2. `get_unclassified_entries(conn)` on canonical returns `[]` (or exactly a Step-1 `### Deferred entries (CEO-approved)` list — quote both heading and query output).
> 3. **Invariants** on canonical, scope = `WHERE entry_id > 182` (read `E0`=182 from Step 1's receipt; do NOT scope by `route IS NULL`). Over exactly that set (**expect 2 rows**): dangling 0, invalid category 0, invalid confidence 0, every one `route IS NULL`; target fields — **for each NON-`ambiguous` proposal**: **`category='governance_rule'`** (assert this EXPLICITLY — both entries are authoring-discipline doctrine; the corpus's `instrumentation` category ALSO pairs with `target_layer='governance'`, so the layer check ALONE does NOT police category — a wrongly-`instrumentation` entry (b) would slip a layer-only check), `target_layer='governance'`, AND `target_artifact` ∈ (`DRAFTING_CYCLE.md`, `PLANNER_TEMPLATE.md`) (a membership sanity-bound — not NULL, not a wild third artifact; a NULL or out-of-set target on a non-`ambiguous` proposal is a FAIL). ⚠️ The membership check does NOT verify the entry→artifact MAPPING — a 183↔184 swap is two in-set values and PASSES here BY DESIGN; the mapping is a Gate-1 CEO decision (informed by VA1's recorded divergence + the row-9 reasoning↔`raw_content` check), not a QA gate. An **`ambiguous` proposal PASSES row 3** (EXEMPT from the category+target checks — NULL targets allowed) but MUST be reported by id in the Output Receipt for CEO adjudication — it is **NOT a row-3 FAIL** (Step 1 explicitly permits an `ambiguous` no-fit; don't HALT on a permitted outcome). Since both entries are expected `governance_rule`, any `ambiguous` OR non-`governance_rule` result is a deviation to SURFACE for the CEO (report-and-continue), not a silent pass. **A scoped count ≠ 2 is a FAIL.** Cross-check `SELECT COUNT(*) FROM lesson_proposals WHERE id > 190` == 2.
> 4. **The 204 fix held.** Read the pre-cycle baseline from Step 1's dev log — **if that baseline is missing from the receipt, HALT (unverifiable)**: this row is the QA-stage backstop for the corpus's documented staling-corruption signature and MUST fail closed. Re-read from canonical now and diff: `stale` not grown, no proposal moved off a terminal status, **entry id 182's `content_hash` unchanged** (still `75bf99cd…`). Report `updated_count` + `terminal_proposals_flagged` as Step 1 recorded.
> 5. **Report** exists; **its surfaced proposals are EXACTLY this cycle's 2 (`entry_id > 182`) and NO others** — the report is whole-corpus (all `proposed`/`ambiguous`), so a surfaced proposal with `entry_id <= 182` means pre-existing non-terminal state leaked past G1 → FAIL (do NOT rely on "counts match DB" alone — both sides move together and would hide an extra). Zero `- **Route:**` lines, zero `Recently-implemented overlap:` lines, `detect_recently_implemented_overlaps` still absent from `src/` (plan 207 intact).
> 6. **No schema drift** — compare the live schema of `lesson_entries` / `lesson_proposals` against `src/db.py`'s DDL **SEMANTICALLY** (column names + types + NOT NULL + PK, via `PRAGMA table_info`, plus the CHECK/UNIQUE constraint set), **NOT a literal `.schema`-text diff**. ⚠️ Cosmetic artifacts of a past `reference`-migration RENAME — a quoted `"lesson_proposals"` table name, `IF NOT EXISTS` present-or-absent on indexes, whitespace, index-DDL ordering — are EXPECTED and are NOT drift; a literal text diff would false-FAIL on them. A FAIL is a real column / type / constraint difference. **Deposit the raw `.schema` output for both tables to `schema.txt`** (Rule 18 names schema-verification queries as mandatory-evidence).
> 7. **DRAFTING_CYCLE.md + PLANNER_TEMPLATE.md UNCHANGED by this cycle** (this batch's two Gate-2 targets — prove the cycle classified WITHOUT prematurely codifying into them; codification is a SEPARATE plan). ⚠️ `lessons-forge` is a submodule; these files are tracked by the ROOT repo and do NOT exist in your worktree — a plain `git diff` from here passes VACUOUSLY (the vacuous-submodule-diff failure class). Run against root: `git -C /Users/marklehn/Developer/GitHub status --porcelain -- DRAFTING_CYCLE.md PLANNER_TEMPLATE.md` — must be EMPTY (matches G2's non-vacuous clean-check; strictly stronger than `git diff --exit-code`, which misses staged/untracked changes). Non-empty = FAIL. (**`plan_lint.py` is deliberately NOT checked** — unlike 274's N5/N6 batch, neither of this batch's entries touches it and the cycle has no write path to it, so the check could only false-HALT on an unrelated bellows change: the "cloned from 274 but irrelevant here" vestige entry (a) is about.)
> 8. **Post-cycle DB counts** — entries total (**expect 184** = 182 + 2; the 182 includes orphan rows, NOT the file's 127 — do not flag 184 as anomalous), proposals by status + category, stated as actuals.
> 9. **Classification depth spot-check.** Scope `entry_id > 182`; for each of the 2 proposals confirm its `reasoning` genuinely quotes its entry's `raw_content`. **Use the EXTRACTION-FREE check** (a quoted-span regex FALSE-FAILs on nested quotes + markdown markers): (a) `canon(s)` = normalize curly→straight quotes, strip `*_`` ` chars, collapse whitespace, lowercase; (b) longest common substring via `difflib.SequenceMatcher(None, a, b, autojunk=False).find_longest_match(...)`; (c) PASS if match **>= 40 chars**. **Report the measured longest-match length per proposal as a number** + PASS/FAIL + the proposal/entry ids.
>
> If any row fails, report and HALT — do not pass a broken deliverable.
>
> **Deposit:** `knowledge/qa/cycle-qa-2026-07-27.md` — the table with DB-source column, raw full-suite tail, the Rule 20 banner + PASSED line, Output Receipt — plus the three evidence files. Canonical Python file-write — no heredoc. Commit. In `### Ledger Updates` include `#### Project Status` (one milestone: cycle 2026-07-27 complete — the 2-entry planner-discipline authoring batch ingested + classified, report deposited, corpus integrity held; Gate 1 route disposition pending — the split cluster: cold-panel → DRAFTING_CYCLE.md, Rule-20-form → PLANNER_TEMPLATE.md), `#### Forward Register` (the `generate_lessons_report` `encoding=` gap at `src/lessons_forge.py` ~line 593 — a latent non-UTF-8-locale `UnicodeEncodeError`, safe on Mac/Bellows, worth an `encoding="utf-8"`), and `#### Prompt Feedback`.
>
> **Deposits:**
> - `lessons-forge/knowledge/qa/cycle-qa-2026-07-27.md`
> - `lessons-forge/knowledge/qa/evidence/cycle-authoring-refinements-2026-07-27/full-suite.txt`
> - `lessons-forge/knowledge/qa/evidence/cycle-authoring-refinements-2026-07-27/invariants.txt`
> - `lessons-forge/knowledge/qa/evidence/cycle-authoring-refinements-2026-07-27/hash-trap.txt`
> - `lessons-forge/knowledge/qa/evidence/cycle-authoring-refinements-2026-07-27/schema.txt`
>
> **STOP. Do NOT move the plan to Done until the CEO issues a verdict.**

---

## Drafting Cycle
**Tier:** T2 — triggers fired: T-2 (production-data mutation — writes lesson_entries + lesson_proposals to the canonical lessons corpus, which has a documented silent-corruption history [the hash-trap bug], CEO-confirmed class). Also a proven-clone of cycles 274/257/247 (T-8 does not fire) but T-2 sets the floor at T2. Down-tiering a 2-entry batch to skip the cold panel is precisely the trap entry (a) of THIS batch names — so T2 stands.
**Walks:** v0 → v10. **Walk 1 COMPLETE** (8 folds + 1 trim) → **Walk 2 COMPLETE** (all 5 DRY) → **COLD PANEL COMPLETE (13 folds)**: cold weak-spots (CB1-2), destruction (CD1; 204 guard verified in code), vulnerabilities (FV1-5; resume ladder robust), integration (CI1-2; plan_lint vestige trimmed, targets/category/§6 aligned), ACID (CA-cold-1/2/3; all 6 resume states + all fold-pairs verified coherent). Cold panel → **Walk 3 COMPLETE** (1 fold WC-walk3-1 + 4 dry) → **§5 mechanical conformance CLEAN** (plan_lint exit 0, sole WARN benign = 274's) → **CLOSING DRY PASS dry** (all 5 lenses dry on v11; LESSONS 94 satisfied). **ADVERSARIAL PHASE COMPLETE — v11 deposited.** Walk 1 (8 folds + 1 trim) → Walk 2 (all dry) → Cold panel (13 folds, backbone independently re-verified by all 5 cold readers) → Walk 3 (1 fold + 4 dry) → §5 clean → closing dry. **22 folds + 1 trim; nearly all at the clone seams** (my IR1 trim's false premise → CB1/FV1; my CA1/CA-cold-1/WC-walk3-1 folds breaking their own edges; the CI1 274-vestige) — lesson (a) demonstrated on the plan that codifies it.
- Weak spots:          w1 → v1: 5 folded. **WB1** whole-corpus hash pre-verify (read-only replicate of the ingest: 2 would-ingest / 0 would-update / 125 unchanged / 57 orphan) — grounds G3/G4/G5, upgrades the prior 1-entry check to all 127. **WB2** split-target GROUNDED in live doctrine (a→DRAFTING_CYCLE §2.6 cold-panel + §2.7:82 sequential-fold; b→PLANNER_TEMPLATE Rule 18:557 + Rule 20 self-check block:565) → Rule 27 placement pre-scout added. **WB3** cloned code refs re-verified live (insert_proposal:202 6-arg, get_unclassified:267, run_full_lessons_cycle:416, generate_lessons_report:514, encoding gap:593 — all accurate; lesson (a) applied, zero drift). **WB4** sqlite_sequence==182/190, no gap → inserts land exactly 183–184 / 191–192. **WB5** fixed a clone-drift FALSE self-reference (G2 claimed "my batch entry names the vacuous-check class" — carried from 274, but neither of my entries does; cited the class generically). ⏭ NOTE for integration: the vestigial "any `structural`→`structure`" QA-row-3 clause (neither entry is structural — over-build-trim candidate).
- Destruction:         w1 → v2: 1 folded. **DA1** — Step 1a created the `.backup` but never verified it (274's own gap vs the 247/248 "halt-if-it-fails restore point" discipline; a restore point you don't verify is ceremony) → added `PRAGMA integrity_check` + expected row-count (182/190) check with HALT-before-ingest. Verified un-weakened + cleared (no fold): G1 non-terminal precondition, G4 hash-trap HALT, plan-204 terminal-status guard, WB1 0-would-update pre-verify, WAL-safe main-tree gitignored `.backup`, INSERT-only classification + resume-ladder anti-double-insert, ingest-committed-before-classify (no loss on step death), no DELETE, no LESSONS.md write. **CD1 fail-closed clause (QA row 4) confirmed PRESENT — not re-dropped from the 274 clone (lesson (a)).** DA2 cleared: no 07-27 report/deposit exists to clobber.
- Vulnerabilities:     w1 → v3: 1 folded. **VA1** — the split target (a→DRAFTING_CYCLE / b→PLANNER_TEMPLATE) raises anchoring stakes vs 274's uniform target: a scout copied without reading could SWAP the two → added explicit licence-to-disagree + derive-independently-from-raw_content + record-divergence (operationalizes the "pre-stated conclusions anchor the agent" corpus lesson; the fold is on MY adaptation, not inherited from 274). Verified clean / no fold: vacuous checks all use `-C root`/absolute paths (G2, QA row 7, Rule 20 block); guards observe EFFECT not existence (G3/G4/G5 read the ingest dict, QA rows read live SQL); resume ladder intact (CV1 run-type G6 carve-out, CA1 pre-ingest entry_id G1, CA2 get_unclassified-authoritative, CA4 constant-fallback); ingest atomic (ingested_count ∈ {0,2}); CA3 correct staling-detector (rows 2+4, NOT the monotonic row-3 count); thin-proposal guarded (quote-or-STOP + row-9 LCS≥40); no new encoding risk (my entries are LESS exotic than the existing corpus's §/⭐/≠). Commit hygiene cleared: worktree isolation + scope_check backstop (274 same, no explicit-pathspec fold).
- Integration-record:  w1 → v4: 1 trimmed (subtractive). **IR1** — deleted the vestigial `structural→structure` QA-row-3 clause (a 274 clone artifact assuming a structural fallback; neither of my entries can be structural — both are governance_rule doctrine, no code coupling — and the `governance on both` assertion already catches any mis-categorization). **IR7 VERIFIED vs code** (lesson (a)): `run_full_lessons_cycle` docstring `:428` confirms it is ingest-only ("Classification… is NOT executed here… returns needs_classification") + inserts only idempotent `duplicate` proposals (G3-guarded) + "Does NOT call conn.commit()" → my manual-classify flow does NOT double-insert, and the `conn.commit()`-after is correct. Verified aligned: §6 doesn't govern (b) (a PLANNER_TEMPLATE amendment, not DRAFTING_CYCLE — no contradiction); premises hold vs record (2 un-ingested [W1], 0 non-terminal, DRAFTING_CYCLE single-source + template-pointer [W2]); right-sized T2 (mutation floor, batch-independent); no over-build towers (274 pre-trimmed; my folds are guards not towers); memories honored (batch-pin, hash-trap, deposit-once, qa-raw-output, benign-WARN).
- ACID:                w1 → v5: 1 folded (the signature fold-vs-fold catch). **CA1** — DA1 (destruction: verify `.backup` counts == baseline 182/190) CONTRADICTS the resume ladder (vulnerabilities): on a RESUME the ingest already committed, so the re-run `.backup` snapshots 184/192, and DA1 would false-HALT every resume → fixed DA1 to assert backup==LIVE-DB-counts-now (holds fresh AND resume), not a hardcoded number. Invisible to re-running destruction or vulnerabilities alone; only surfaced by modeling the plan as a schedule across the fresh/resume axis (the plan-239 pattern). Atomicity (ingest all-or-nothing+commit; per-proposal commit; resume ladder), Consistency (QA rows 2/3/4/6 invariants), Isolation (concurrent-cycle staling → daemon serialize + QA rows 2/4), Durability (committed DB survives step death; reproducible from committed LESSONS.md) all sound. Note (cleared): VA1-licence vs QA-row-3 2-artifact set — a genuinely-reasoned third artifact would HALT for CEO review (correct fail-closed, not a contradiction).
**Walk 2 (confirming, on v5):**
- Weak spots:          dry — traced the Walk-1 fold interactions: CA1 resume-aware backup-verify holds fresh AND resume (WAL-safe backup==live-committed); baseline-vs-E0/P0-vs-backup all consistent on resume; VA1↔row-3 cleared; IR1↔`governance on both` catches mis-category. No new weak spot. Noted (deferred to cold panel, not folded): WB2 CEO-Context anchors vs VA1 classification clause overlap slightly (acceptable context-vs-instruction layering).
- Destruction:         dry — CA1 (ACID) did NOT break DA1 (destruction) on its own edge: the backup-verify still catches a truncated/corrupt backup (integrity_check + counts==live), CA1 only removed the wrong 182/190 assertion. Core mutation-safety guards structurally untouched by any edit (G1, G4, 204 terminal guard, resume ladder, CD1 fail-closed row 4, INSERT-only classify, ingest-committed-before-classify). No guard relaxed; DA1/CA1 net-strengthened. No new destruction vector (backup-verify is read-only).
- Vulnerabilities:     dry — VA1's residual (a silent target swap QA row 3 wouldn't flag, both values valid) is fully backstopped: classify-time target is advisory → Gate 1 CEO review of the split-cluster synthesis → Gate 2 placement; a swap can't ship silently. Core vuln guards structurally intact: WB5 changed only G2's parenthetical (the `git -C <root>` mandate untouched); VA1 didn't remove get_unclassified-authoritative / quote-or-STOP / never-hand-type; WB4 kept G6 dynamic derivation authoritative over the literal 183–184. No new hole.
- Integration-record:  dry — IR1 trim confirmed lossless (`governance on both` covers mis-category; VA1 licence is artifact-only). Folds measure true to record (WB2 anchors, WB3 code refs, WB4 sqlite_seq re-verified; entry-178 anchoring + plan-248 verify-restore-point citations both real corpus). No removable tower (WB2/VA1 overlap is intentional context-vs-instruction locality). Minor NOTED for cold panel (not folded): QA row 7 still checks `plan_lint.py` unchanged — vestigial for this batch (274 targeted it via N5/N6; neither of my entries touches it, and the cycle can't reach that file) — fresh eyes to judge trim-vs-keep.
- ACID:                dry — CA1 resume-aware backup-verify traced coherent across the FULL resume schedule (R-a ingest-committed-pre-classify [the narrow "fresh G1 / resume G5" edge], R-b mid-classify [get_unclassified excludes the done one → no double-insert], R-c classified-deposits-absent [G5 regen-from-DB], R-d read-only Step 2/3 re-run) — backup==live-now holds at every point (single-writer, no drift). The 8 Walk-1 folds don't contradict as a system (only DA1/CA1 touched the schedule; CA1 resolved it). Isolation/durability unchanged. This is the resume-region joint-coherence re-check 274 flagged as owed on the confirming walk — passed.
- Vulnerabilities:     (pending)
- Integration-record:  (pending)
- ACID:                dry — v10/v11 folds cohere as a schedule: WC-walk3-1's `$BK` command writes a valid backup at the path the DA1/CA1 verify reads; across R0–R3 each resume writes a NEW `20260727T…` backup while the CA-cold-1 glob's "earliest" resolves to the original pristine (182/190) → rollback always targets true pre-cycle state. CA-cold-2 ambiguous-PASS doesn't contradict `count==2` (an ambiguous proposal is still one of the two `entry_id>182` rows, counted). No fold-vs-fold contradiction; cold-ACID's through-v9 coherence extends to v11.
**→ Walk 3 COMPLETE (1 real fold WC-walk3-1, then 4 dry — diminishing returns).**
**§5 mechanical conformance — CLEAN.** `plan_lint` exit 0, sole WARN is check (e) "step 3 mentions tests but declares no test scope" — VERIFIED benign: 274 (proven origin) emits the IDENTICAL WARN; the QA runs `pytest src/` (tests at `src/test_lessons_forge.py`) but modifies no test file → no test scope to declare; per corpus, don't contort the prompt to dodge the matcher. Structural: line-1 `# Title` ✓, `cycle_tier: T2` ✓, `## How to Run` (Rule 35) ✓, `## STEP 1/2/3` uppercase ✓, `## Drafting Cycle` ✓; (f) self-check passed silently; Rule 26 Deposits / Checklist 23 Scope / Rule 20 banner / qa_steps:3 all present; no stale marker counts in the plan body.
**CLOSING DRY PASS — dry.** Re-confirmed the last-folded lens (weak-spots) on v11: the WC-walk3-1 backup-command block coheres end-to-end (verified-working `$BK` form, accurate misparse warning, unchanged verify/resume-glob logic) → no new weak spot. With Walk-3's destruction/vuln/integration/ACID already dry on v11, **all 5 lenses are DRY on the final draft; the last event before deposit is a dry pass (LESSONS 94 satisfied).**
**Pre-deposit reconciliation (git ground truth):** the 2 LESSONS entries ARE appended + committed — root HEAD `1827337` "lessons: append 2 session-10 captures" (+24 lines), LESSONS.md 127 entries, both 2026-07-27 headings present (:2163/:2175), tree clean. G2's expected HEAD `1827337` is correct. **DRAFT COMPLETE — deposit-ready. Cycle totals: 22 folds + 1 trim (Walk1 8+1 / cold panel 13 / Walk3 1).**

**Cold panel (T2):** lens-by-lens, fresh-context reader subagents (each on the prior's folded draft), author-verified. Aimed at diff-against-274 per entry (a).
- Cold weak-spots:      → v6: 2 folded. **CB1** (MED) — QA row 3 self-contradicted on the `ambiguous` path: clause 1 exempts ambiguous from NOT-NULL, but "on both / a NULL target is a FAIL" applied unconditionally → a permitted ambiguous-NULL result false-HALTs. A defect **my own IR1 trim introduced** (rewrote 274's category-keyed row 3 to unconditional while Step 1 kept the ambiguous exemption) — invisible to the warm walk; the cold reader caught it by diffing against 274. Fixed: scoped target-value checks to non-`ambiguous`, exempt+flag ambiguous. **CB2** (LOW) — added `schema.txt` evidence for QA row 6 (Rule 18 names schema-verification as mandatory-evidence; fitting since this cycle carries lesson (b) about evidence-file completeness). ⭐ Backbone INDEPENDENTLY re-verified by the cold reader vs live DB/code (E0/P0 182/190, sqlite_seq no-gap, hash-trap 182/`75bf99cd`, whole-corpus 2/0/125/57, insert_proposal 6-arg :202, all fn line-numbers, detect_duplicates won't fire → no id-shift, 0 clone-drift stray numbers, all 274 guards present) — every number confirmed. Cleared (no fold): 2-set target licence-bound (deliberate fail-closed, HALT→CEO on a 3rd artifact — draft documents it); WB2 :565 anchor defensible.
- Cold destruction:     → v7: 1 folded (LOW), lens otherwise CLEAN (0 HIGH/MED). **CD1** — the resume "pristine backup = earliest-timestamp" heuristic (inherited from 274) is imprecise: `data/backups/` persists prior cycles' backups, so "earliest" points at a 2026-07-20 file → scoped it to this cycle's `…pre-cycle-2026-07-27T*` pattern + prefer the receipt's recorded path. ⭐ Cold reader INDEPENDENTLY verified the corpus-mutation safety in CODE: `_TERMINAL_STATUSES=frozenset(impl/rej/super/ref)` at `:31`; UPDATE path `:186-193` stales ONLY {proposed,accepted,ambiguous} = my G1 def exactly (not narrower); INSERT+UPDATE only, zero DELETE/DROP/TRUNCATE in the module; 0-would-update reproduced; hash-trap 182 stable under the trailing `---`; `.backup` WAL-safe+main-tree+verified; all 274 guards present (CD1 fail-closed row 4, G1/G3/G4, resume ladder) + `.backup` strengthened. Cleared (no fold): anti-double-insert rests solely on get_unclassified (v6 states this correctly; QA row-3 count==2 detects a double-insert) — the reader flagged that 274's cold-ACID log cited a PHANTOM "UNIQUE key" backstop (lesson_proposals has no UNIQUE), which v6 correctly did NOT inherit.
- Cold vulnerabilities: → v8: 5 folded (1 MED + 4 LOW). **FV1** (MED) — the SECOND fallout of my IR1 trim: I justified removing the `structural→structure` clause by "the `governance` layer check catches a mis-categorization," but the cold reader proved that FALSE against the live corpus — `instrumentation` ALSO pairs with `target_layer='governance'` (8 rows), and entry (b) [a QA-mechanism lesson] has `instrumentation` as its plausible wrong-neighbor → a wrongly-`instrumentation` (b) passes every row-3 clause. Fixed: row 3 now asserts `category='governance_rule'` EXPLICITLY. **FV2** (MED/LOW) — row 3 made honest that `target_artifact ∈ set` is a membership sanity-bound, NOT a mapping guard (a 183↔184 swap passes by design; the mapping is Gate-1's, per VA1 + row-9). **FV3** (LOW) — G6 range now ARITHMETIC from E0 (was "derive from needs_classification" → self-referential/vacuous invariant). **FV4** (LOW) — G5 totals the gate (`ingested_count ∉ {0,2}` → HALT). **FV5** (LOW) — row 7 → `git status --porcelain` (G2 pattern; catches staged/untracked `git diff --exit-code` misses). ⭐ Resume ladder INDEPENDENTLY verified ROBUST: all 4 partial-failure states double-insert-safe (`get_unclassified` `NOT EXISTS(status!='stale')` verified in code :284-289); vacuous-check surface closed (G2/row-7 `-C root`, Rule-20 absolute path — cold reader RAN them); concurrency detection reasoning holds (staling is UPDATE → row-3 count monotonic-blind, rows 2+4 catch it); the 3 live `stale` proposals sit on entries that also carry a terminal proposal → none can resurface. Finding 5 (resume post-commit staling on a FORBIDDEN mid-flight edit) — no fold (forbidden input, G4-detected, `.backup`-recoverable, inherent). ⚠️ META for a future LESSONS capture: my "subtractive" IR1 trim removed a REAL check on a FALSE premise (the layer check does not subsume the category check) → two cold folds (CB1+FV1); a subtractive trim justified by "another check covers it" must VERIFY that claim against live data, not assume it.
- Cold integration:     → v9: 2 folded (subtractive). **CI1** (LOW/MED) — TRIMMED `plan_lint.py` from QA row 7 + the Step-1 scope prohibition (resolves the Walk-2 deferred item): a 274 vestige (274 targeted plan_lint via N5/N6; this batch's 2 entries don't, and the cycle has NO write path to bellows/scripts/plan_lint.py) → the check could ONLY false-HALT on an unrelated bellows change. The fresh reader did exactly what the FV1/CB1 meta-lesson demands (verified "can't reach it" against reality, not assumed) — and named the irony: this is the "cloned from 274 but irrelevant" vestige entry (a) is ABOUT. **CI2** (LOW) — corrected entry (a)'s Rule-27 scout anchor from §2.7:82 to **§2.6 (`:73` "rotate the reviewer, not the lens") + §1 tier-gate** (§2.7 is only the warm-review that FAILS); helps Gate 2 land the amendment in the right section. ⭐ VERIFIED ALIGNED: target split BOTH correct vs live docs (a→§2.6/§1/§2.4; b→Rule 18:557 + Rule 20:563-565 + bonus Checklist #4:1137), category BOTH `governance_rule` per ADR-002 "documentary→governance_rule" (instrumentation wrong-neighbor for (b) correctly guarded by FV1's explicit category assert), §6 non-contradicted, backbone re-confirmed, WB5 fix sound, and "the plan PRACTICES what entry (b) preaches" (non-empty plan-adapted evidence set incl. the CB2 schema.txt — not cloned blindly). No other over-build survived (resume ladder/concurrency/schema.txt all load-bearing, not towers).
- Cold ACID:            → v10: 3 folded. **CA-cold-1** (MED) — my OWN CD1 fold broke on its edge: the pristine-backup resume glob hardcoded a HYPHENATED date `…2026-07-27T*`, but the last 3 cycles wrote COMPACT stamps (`20260721T…`) → the glob matches ZERO files on a no-deposit resume (reader ran `ls` → no matches). Fixed: pinned Step-1a to `$(date -u +%Y%m%dT%H%M%SZ)` compact + matched the glob to `…20260727T*`. **CA-cold-2** (LOW) — clarified an `ambiguous` proposal PASSES row 3 (report-for-CEO, not a HALT — immaterial under `pause_for_verdict:always` but precise). **CA-cold-3** (adjacent, real) — row 6 schema-drift now SEMANTIC (PRAGMA table_info + constraints), NOT a literal `.schema` text diff: the live table's past RENAME-migration emits a quoted name + `IF NOT EXISTS`-differences that would cosmetically false-FAIL. ⭐⭐ Reader wrote the full SCHEDULE, traced ALL 6 resume states clean (R0–R5), and confirmed EVERY fold-pair NON-contradictory (FV1↔CB1, FV4↔resume-0, FV3↔subset, DA1/CA1↔resume, VA1↔FV2, IR1↔FV1, G1/G5/G6 partition) — the plan is ACID-COHERENT as a system; the ingest is atomic (`ingested_count∈{0,2}`, code `:436` no internal commit).
**Cold panel COMPLETE (13 cold folds: CB1-2, CD1, FV1-5, CI1-2, CA-cold-1/2/3).** Materially changed the draft → warm confirming Walk 3.
**Walk 3 (warm confirming, on v10):**
- Weak spots:          → v11: 1 folded. **WC-walk3-1** — CA-cold-1's backup command was BROKEN: a `$(date …)` inlined between single-quoted parts of `.backup '…pre-cycle-'$(date …)'.db'` makes sqlite3 MISPARSE → `unknown database …pre-cycle-`, exit 1, NO backup written (VERIFIED by running it; the `$BK`-variable form works). The cold-panel's OWN fold (CA-cold-1) broke on its edge — caught only by EXECUTING the command, not re-reading it (run-the-procedure / §2.7 execute-against-real-data). Fixed to the verified-working `BK`-variable form. Other v10 folds cohere (CA-cold-2 ambiguous-PASS removes ambiguity; CA-cold-3 semantic-schema actionable; row 3's IR1→CB1→FV1→FV2→CA-cold-2 chain internally consistent).
- Destruction:         dry — restore-point guarantee intact end-to-end and, notably, TWO-LAYER: even CA-cold-1's broken backup command could not have caused a mutation-without-backup, because the DA1/CA1 verify fails CLOSED (HALT-before-ingest on a missing/errored/truncated backup) — so WC-walk3-1 was a wasted-dispatch bug, not a destruction hole. No v10/v11 fold relaxed a core guard (G1, G4, 204 terminal guard, INSERT-only classify, ingest-committed-before-classify all untouched; the edits only touched the backup command, ambiguous wording, schema-check semantics, glob format).
- Vulnerabilities:     dry — WC-walk3-1 + resume: a resume's 2nd `…20260727T…` backup is matched by the CA-cold-1 glob, "earliest" picks the pristine correctly (dispatch-past-midnight edge backstopped by the receipt-path fallback + seconds-latency claim). CA-cold-3 semantic schema still catches REAL column/type/constraint drift (non-vacuous the other way). Resume ladder + vacuous-check surface unchanged by any v10/v11 edit (gates/get_unclassified/`git -C root` untouched; cold-ACID's through-v9 verification holds). No new hole.
- Integration-record:  dry — ⭐ VERIFIED CA-cold-3's premise against live data (applying the FV1 meta-lesson): live `.schema lesson_proposals` IS quoted `"lesson_proposals"` + indexes without `IF NOT EXISTS`, vs db.py's unquoted + `IF NOT EXISTS` — caused by the REAL "table rebuild" migration `db.py:99 ALTER TABLE lesson_proposals_new RENAME TO lesson_proposals`. Columns/types/constraints byte-identical (NO real drift) → a literal diff WOULD false-FAIL, semantic comparison correctly passes. So CA-cold-3 rests on a TRUE premise (contrast: IR1 rested on a FALSE one → CB1/FV1). No new over-build; WC-walk3-1 (BK-var) + CA-cold-2 consistent with corpus idiom/report-continue. Everything measures true.
- ACID:                (pending)
- Vulnerabilities:     (pending)
- Integration-record:  (pending)
- ACID:                (pending)
**§5 mechanical conformance:** (pending — plan_lint on the final draft + Rules/Checklist walk)
**Closing:** (pending — must close on a dry lens pass, last event a lens pass not a fold)
