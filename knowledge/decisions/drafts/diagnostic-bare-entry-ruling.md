# lessons-forge — rule on the 14 bare `LESSONS.md` entries: grade them under the CEO's `history` ruling and emit the executable's input

**Date:** 2026-08-23 | **Project:** lessons-forge | **Tier:** Medium | **Dispatch Mode:** bellows | **cycle_tier:** T1 | **Test Scope:** none (read-only diagnostic) | **Execution:** Step 1 (READ-ONLY DIAGNOSTIC) | **Priority:** 1

**auto_close:** false
**pause_for_verdict:** after_step_1

**Depends on:** `Done/diagnostic-501.md` and its mapping `knowledge/research/annotation-mapping-2026-08-22.tsv` (the quarantine list); `Done/diagnostic-504.md` and `knowledge/research/promotion-corrected-2026-08-23.md` (the two grading rules); `/Users/marklehn/Developer/GitHub/governance/knowledge/decisions/Done/executable-502.md` (which quarantined these 14 by construction) and `/Users/marklehn/Developer/GitHub/governance/knowledge/decisions/Done/executable-505.md` (which set the current state) — ⚠️ **both are GOVERNANCE plans, not lessons-forge ones**, and a bare `Done/…` reference to either resolves to a file that does not exist. Newest same-class and clone origin: **`Done/diagnostic-504.md`**, shipped 2026-08-23.

## Why this exists

`executable-502.md` annotated 313 mapping rows and left **14 untouched by design** — its D2 row reads "rows QUARANTINED, untouched … `proposed_status` is `unknown`". Those 14 headings are the register's only bare ones. They were never a judgement the pipeline made and lost; they are a judgement it declined to make.

**CEO ruling, 2026-08-23:** `[status: history]` is a **fourth legal value** — for a register entry that RECORDS what happened rather than states an enforceable rule. It exists because the three-value taxonomy has no cell for such an entry: `pending` claims buildable work that does not exist, `codified` claims a target that does not exist, `learned` claims an enforcement that does not exist. Under the SESSION 59 ruling `learned` means COMPLETION, so all three would be false of the same entry.

This plan grades the 14 under that ruling and emits the mapping a companion executable applies.

## What walk 0 measured, and why it changes the plan's shape

The 14 quarantine under three stated causes. **All three dissolve, each for a different reason** — so this is not "grade 14 hard cases", it is "three quarantine classes, none of which is what its label says".

⚠️⚠️ **BE PRECISE ABOUT WHAT WAS VERIFIED AND HOW, because this plan is about an over-claim and must not commit one — and an earlier form of this section DID.** It said the three causes were *measured facts about the mapping and the corpus*; in truth all three were inferred from the mapping's `basis` STRINGS, and the cold panel then read `detect_learned.py` and falsified the CAUSE of two of them. **Per dissolution, this plan now states whether the cause was READ OUT OF THE DETECTOR SOURCE or INFERRED FROM ITS OUTPUT. An inferred cause is a corroboration and is labelled one.** The same discipline binds Q6, which sizes gaps from these causes.

1. **`target_layer = 'none'` occurs EXACTLY 5 times in the whole 378-proposal corpus, and those 5 entries are `59, 82, 88, 104, 112`** — INFERRED corroboration, not the cause. ⚠️ **Read from source, `target_layer` is causally INERT here: the `no_target` basis comes solely from an unresolvable `target_artifact` (`detect_learned.py:91–94`), and all NINE `no_target` entries have a NULL artifact while only five carry layer `none`.** So the layer did not produce the quarantine; it is a second column recording an earlier pass's record-not-rule judgement, which the annotation pipeline had no status value to express. ⚠️ **And it is ONE column, not two witnesses: `category='narrative'` returns the same five, so treating them as converging evidence double-counts a single judgement.** ⚠️ **This is a corroboration, not an authority** — it was produced by the same class of pass this plan is auditing, and Q2 tests the 5 from their BODIES. It is stated here because a reader must be able to tell a converging second measurement from a circular one. ⚠️ **If your body-reading DIVERGES from the column on any of the 5 — or finds a sixth RECORD outside them — your reading wins and the divergence is a REPORTED FINDING, not a discrepancy to reconcile away.** It would mean the column is incomplete or wrong, which is itself an answer Q6 needs; say which entry, which direction, and what in the body decided it.
2. **`conflicting-proposals-quarantined` is a misnomer — and READ FROM SOURCE, its cause is not a judgement at all.** `scripts/detect_learned.py:57` reads `CONFLICT_ENTRY_IDS = {93, 116, 123}` — a HARDCODED LITERAL — and `:226` short-circuits to the `conflicting-proposals-quarantined` basis before `detect_one` ever runs. ⚠️ **An earlier form of this pin said *the detector quarantined a pair with no live member*; that CAUSE was false and the cold panel caught it.** The CONCLUSION survives and is independently verified: each of the three carries exactly 2 proposals, both non-live — one `stale`, one `rejected`. **So these three were never graded by anything, which is a different and larger gap than a detector that graded them badly.**
3. **The 2 `threshold` entries are the only bare entries whose `target_artifact` is CODE** — `walk_register_lint.py` for both `330` and `331`. ⚠️⚠️ **An earlier form of this pin said they were the only bare entries with a `target_artifact` AT ALL, and that was FALSE — found by the cold scout, author-verified: `93`, `116` and `123` each carry `PLANNER_TEMPLATE.md` on BOTH proposals.** Five bare entries have a target, nine are NULL — which is what Q3 already said, so the plan contradicted itself for six walks. **The distinguisher is CODE-vs-DOC, not presence-vs-absence**, and it matters twice: the ratio test is meaningless against a `.py` file, and `93/116/123` arrive at Q3 with a target already proposed rather than needing one invented. ⚠️ **Weigh that, do not spend it: the proposed target (`PLANNER_TEMPLATE.md`) comes from the very `stale`+`rejected` pair this plan declares NON-LIVE in pin 2, and it is not a value the Fork-2 discriminator can produce. One fact cannot be dead evidence in pin 2 and live evidence in Q3 — treat it as a HINT to check, never as the answer.** The detector scores a prose-term ratio against the target's text. ⚠️ **An earlier form said that ratio *measures nothing* against a Python file — the plan's own cited data contradicts it: these two scored 0.40 and 0.29, against `ratio=0.00` on all nine `no_target` rows, and `330` missed the `learned` bar on the STRICT inequality `ratio > 0.4` at exactly 0.40.** The real defect is narrower and sharper: the thresholds and the stop list are tuned for prose, and the `phrase_hits >= 2` half is structurally dead against code. **That is what Q6(d) asks you to size — size the real one.** **The threshold class is a detector defect, not an undecidable entry.** ⚠️ **The EVIDENCE is that a guard shipped for both; the VERDICT is Q4's and this plan does not pre-judge it** — an earlier draft called them the set's strongest `learned` candidates, which primes the agent on the two entries whose grading is the most mechanically involved in the whole set. Grade them from the guard's behaviour and Rule One, not from this paragraph.

## What this plan does NOT do

- **It does NOT edit `LESSONS.md`, write to any `.db`, or re-label anything.** Read-only. Its output is the executable's input.
- **It does NOT re-open the 317 annotated entries.** They are not candidates. ⚠️ Say so explicitly in the findings rather than leaving it implied — a reader must be able to tell "not a candidate" from "considered and rejected". ⚠️ **The exclusion has a GROUND and this plan states it rather than asserting it, because `503`'s error ran in BOTH directions — it promoted entries that had not earned it AND left entries that had.** The ground: the record-not-rule signals this plan grades on (`target_layer='none'`, `category='narrative'`) return the same five entries, all inside `Q`. ⚠️ **That is a one-query claim — RE-RUN IT. If any entry OUTSIDE `Q` carries either marker, the exclusion is wrong and that is a REPORTED FINDING**, not a scope note.
- **It does NOT re-run the annotation detector over the corpus**, and it does not need to: the three sub-class causes above are measured facts about the mapping and the corpus, and Q2's question — is this entry a record or a rule — is answered by reading the entry, not by re-scoring it. ⚠️ **This bullet is about the DETECTOR only, and it is not licence to skip Q3's probes — which the folded plan mandates for every RULE entry.** Two kinds of work it does not exempt: the mechanism runs for `330`, `331` and `122`, where the verdict depends on whether a mechanism exists and fires (Q4); and the artifact probes behind every `codified`/`pending` split (Q3). Run both; assume neither.
- ⚠️ **It does NOT accept the corpus's `target_layer` as the answer to Q2.** See the warning in pin 1.

## The verdict derivation (the constraint Q2–Q5 share)

⚠️ **Sites: Q2, Q3, Q4, Q5, and Step 1's closing assert** (the post-conditions at the end of this section). Stated here rather than inside one question because it spans five, and a constraint carried by whichever question the author happened to be drafting is the half-carried-guard class.

Each entry's status is DERIVED, never chosen:

| row | class (Q2) | is the rule already written into a named artifact? (Q3) | does a mechanism REJECT a violation? (Q4) | status |
|---|---|---|---|---|
| **R1** | RECORD | n/a | n/a | `history` |
| **R2** | RULE | yes | yes, and the pair passes BOTH 504 rules | `learned` |
| **R3** | RULE | yes | no mechanism, or one failing either 504 rule | `codified` |
| **R4** | RULE | no — no artifact contains it yet | not reached | `pending` |
| **R5** | UNDECIDED (Q2) | — | — | `unknown` — surfaced, never auto-routed (Fork 3, SESSION 58b) |
| **R6** | RULE | **undecidable** — no artifact exists to probe (see `glossary.md` at Q3) | not reached | `unknown`, with the reason stated — never defaulted to `pending` |
| **R7** | RULE | yes | **undecidable** — the mechanism is ABSENT or RENAMED (you cannot find it to run) | `unknown` (guard-absence class, capped separately) |
| **R8** | RULE | **no** | yes — a mechanism rejects violations although no artifact states the rule | `codified`; the mechanism IS the target — name it as the artifact |

⚠️ **The rows carry STABLE IDS because ordinals rot: the cold panel appended three rows and every reference written before that silently shifted — one such reference was wrong when walk 8 checked it. Cite `R<n>`, never "row three".**

⚠️⚠️ **TWO COLLISIONS THIS TABLE HAD AND NO LONGER HAS — read both, because each decided a `[status:]` value.** (1) **An UNINVOKED mechanism is NOT "undecidable".** If you can find the mechanism and establish that nothing calls it, that is a determinate answer: it fails Rule Two's reject condition and the entry is **`codified`** — **`R3`**. `unknown` (**`R7`**) is only for a mechanism you cannot find or run at all. (2) **Q3=no + Q4=yes matches `R4` and `R8` both; `R8` governs** — ⚠️ an earlier form of this sentence named `R3`, which requires Q3=**yes** and cannot match; it was wrong, and it is the reason the rows now carry ids — when no artifact states the rule but a mechanism rejects violations of it, the mechanism IS the artifact and the verdict is `codified` by row 8's reasoning, not `pending` by row 4's.

⚠️ **`R6`–`R8` were added by the cold panel. The table had five rows, Q5 mandates stating which row each entry took, and three reachable cells had none** — so an agent would have been forced to mislabel or invent. It is now a partition; if you reach a cell with no row, that is a REPORTED FINDING, not a judgement call.

⚠️⚠️ **THE TWO MIDDLE COLUMNS ARE INDEPENDENT FOR A DOC TARGET AND COUPLED FOR A CODE ONE — read the table with that in mind or it will mislead you.** When the target artifact is a document, "written into it" and "a mechanism rejects a violation" are separate facts. When the target is CODE — as it is for `330` and `331`, whose target is `walk_register_lint.py` — writing the rule in IS the mechanism, so both columns read `yes` mechanically and the row lands on `learned` without anything having been judged. **Rule One is the whole discriminator there**, which is why Q4 makes those two verdicts turn on RUNNING the guard and then asking what the lesson's remaining clause is not covered by it. ⚠️ **`R3` and `R4` are the pair this plan exists to separate, and Q3 is where they separate.** `codified` and `pending` differ ONLY by whether the target artifact ALREADY CONTAINS the rule — so naming a target does not settle it, and Q3 must answer both halves. ⚠️ **The second half is an ABSENCE CLAIM against a 420KB file and it carries the §2.7 discipline in full:** derive the probe from the entry's OWN text rather than composing one from memory, and pair every zero with a positive control. A bare-word probe is worthless here and the Planner measured it — `canary` returns 17 hits in `PLANNER_TEMPLATE.md` and settles nothing about the live-canary entry.

⚠️⚠️ **TWO PROPERTIES MAKE THIS THE HARDEST QUESTION IN THE PLAN, AND BOTH ARE CODIFIED RULES THIS PLAN WOULD OTHERWISE RE-TRIP.**
1. **Decoy density is HIGHEST in exactly these files.** `PLANNER_TEMPLATE.md` and `DRAFTING_CYCLE.md` are documents ABOUT conventions, so they quote the conventions they govern. §2.7: anchor every structural search line-anchored, and **strip fenced blocks and blockquotes before matching any token that also appears in prose** — measured there at nine prose mentions causing four misfired measurements, one landing 251 lines early.
2. **A HIT IS NOT AN ANSWER — CLASSIFY IT.** A target artifact that QUOTES a lesson (in a History row, a changelog, a retraction, a worked example) is not an artifact that ENACTS it, and `codified` means enacted. **For every hit, say whether the surrounding text is the rule OPERATING on a reader or the rule being MENTIONED; zero OPERATIVE hits is the `pending` condition, and the total is meaningless.** This is the same discipline the record already carries for retraction text — a well-run artifact accumulates literal instances of the very thing being negated.

⚠️ **Every one of the `Q` entries receives a value.** `unknown` is the escape for a genuinely undecidable entry; **bare is not.** ⚠️ **Two post-conditions live here and only one is yours.** THIS plan's, checkable at Step 1 and asserted against your own deposits before the closing commit: (i) every one of the `Q` entries has at least one TSV row and a non-empty per-entry `verdict` — ⚠️ **UNLESS you took the deliberate-stop path (Q6's incremental-write clause), in which case (i) is replaced, not waived: every entry you GRADED has a row with a non-empty verdict, every entry you did NOT grade is named by `entry_id` under `### Flags for CEO`, and graded + ungraded = `Q`.** Without that substitution the two clauses contradict — a deliberate partial stop would fail a post-condition the plan itself authorises — and an agent facing the contradiction would either grade badly to fill rows or abandon the stop and die silently; (ii) **every `### <entry_id>` heading a `basis` cell names actually EXISTS in the findings document** — the two deposits reference each other, and an unresolvable pointer is a broken deposit that no gate will catch. ⚠️ **The pointer FORMAT is fixed so this assert can be mechanical rather than eyeballed: a `basis` cell ends with `→ ### <entry_id>`, and the findings document's section heading is `### <entry_id> — <heading>`.** ⚠️ **The heading text in that section MAY be abbreviated and the assert keys on the ID ALONE — deliberately, because `entry_heading` in the TSV is the single authoritative verbatim copy (C15) and a second verbatim copy in prose is a divergence waiting to happen.** Do not treat the section heading as a locator. ⚠️⚠️ **The instrument matters and the obvious one FAILS — the panel executed both.** An UNANCHORED grep for a broken pointer `→ ### 3300` resolves against the findings document's own fenced quotation of itself and reports PASS on a genuinely dangling pointer. And *report the two counts* passes a specimen carrying one dangling pointer AND one orphan section, because 3 = 3. **So: match the heading LINE-ANCHORED (`^### <id> `), compare SETS not counts, and report BOTH differences — pointers with no section AND sections with no pointer.** A prose pointer (*see the section on 330*) makes the post-condition unassertable and is not acceptable. The COMPANION's, which you cannot check and must not claim: bare reaches 0 in `LESSONS.md`. State the first as measured and the second as the executable's. ⚠️⚠️ **AND IF YOU TOOK THE DELIBERATE-STOP PATH, SAY THE CONSEQUENCE OUT LOUD: the companion CANNOT drive bare to 0 from a partial set, so the arc stays open until the ungraded remainder is graded.** A partial deposit that does not say this reads, to the next reader, as a complete one that simply failed — **name the residual explicitly as a second executable owed.**

## Numbers discipline

⚠️⚠️ **TWO DIFFERENT SETS IN THIS PLAN BOTH NUMBER 14 — `Q` (the bare headings, this plan's subject) and `D2` (the `learned`-labelled headings, which this plan does not touch). Every unqualified "the 14" in this document means `Q`.** Said once, here, rather than qualified at each site: `propagation_check` reports every prose `14` as a restatement of `Q` and cannot tell the two apart, and neither can a reader.

⚠️ **This table owns every quantity the plan ACTS ON.** Values measured at walk 0, 2026-08-23. **RE-DERIVE every one; if yours differ, yours supersede and you say so.**

| id | pin | value | probe |
|---|---|---|---|
| D0 | **`Q`** — bare headings, this plan's whole subject | **14** | ⚠️ **enumerate, do not merely count.** The probe is TWO stages and each needs a different flag: **(1)** `grep -n '^## ' <abs LESSONS.md>` — **anchored, NOT `-F`**, because `^` must stay a metacharacter; **(2)** filter that output to lines NOT containing the literal `[status:` with `grep -vF -- '[status:'` — **`-F` here, on the FILTER**, because `[status:` is the literal that breaks. ⚠️⚠️ **Both halves were wrong in earlier forms of this pin and each failed differently.** Without `-F` on the filter, ugrep reads `[status:` as a bracket expression, errors `mismatched [` with exit 2 — and piped to `wc -l` that becomes `0` with exit 0, a silent false absence of all 14. With `-F` moved onto stage 1, `^## ` stops anchoring and the probe returns **49**, measured. **Never pipe either stage to `wc -l` as the only reading; print the lines and count them separately.** The COUNT is `D1` − `D5`, and a probe returning only a count cannot supply the set |
| D1 | **`N`** — dated headings in the register | **331** | `grep -cE '^## 20[0-9][0-9]' <abs LESSONS.md>` — equals the all-H2 count, so every H2 is a dated entry |
| D2 | `learned` | **14** | `grep -cE '^## .*\[status: learned\]' <abs LESSONS.md>` |
| D3 | `codified` | **225** | same form, `codified` |
| D4 | `pending` | **78** | same form, `pending` |
| D5 | headings carrying ANY `[status:]` | **317** | `grep -cE '^## .*\[status: ' <abs LESSONS.md>` — and `D2`+`D3`+`D4` must equal it |
| D6 | headings carrying `[target:]` | **239** | `grep -cE '^## .*\[target: ' <abs LESSONS.md>` — equals `D2`+`D3`; ⚠️ **no bare entry carries a target**, so a check expecting a mix fails on a correct run |
| D7 | mapping rows with `proposed_status = unknown` | **14** | `awk -F'\t' 'NR>1 && $4=="unknown"' <mapping tsv> \| wc -l` — must equal `Q`, and the two sets must be EQUAL, not merely equinumerous — ⚠️ **compare HEADING sets: `LESSONS.md` carries no entry id on any heading (0 of 331), so the id is a corpus-side key only.** An earlier form of this cell said *the id sets*, which is unrunnable file-side |
| D8 | sub-class `no_target` / `conflicting` / `threshold` | **9 / 3 / 2** | ⚠️ the `basis` column is FREE TEXT and carries a varying ratio, so "partition on it" is not executable as written — the discriminator is: `basis` contains `no_target` → no_target; contains `conflicting` → conflicting; otherwise → threshold. State the three id lists, not just the counts |
| D9 | corpus-wide proposals with `target_layer = 'none'` | **5** | `SELECT COUNT(*) FROM lesson_proposals WHERE target_layer='none'` |
| D10 | corpus `lesson_entries` — negative pin | **370** | `sqlite3 "file:<abs>?immutable=1" "SELECT COUNT(*) FROM lesson_entries"` |
| D11 | `/Users/marklehn/Developer/GitHub/LESSONS.md` sha256 | `8ba177d0400c87da4673247fb4de9af116a140845b8e30868ef70f8e7e22d363` | `shasum -a 256` — 641,081 bytes |

⚠️ **`D1` is 331, not the 327 the mapping was generated against.** SESSION 59's wrap appended after `diagnostic-501` ran. The appends landed at the END of the file, so the mapping's `line_no` column still resolves today — **but that is luck, not a contract. Locate every heading by WHOLE-LINE equality against `"## " + original_heading` and assert the occurrence count is exactly 1; never by the mapping's line number.** ⚠️ **Disposition at the boundary, stated because the guard is silent about it: a count of 0 or >1 HALTS on that entry and is reported with the heading verbatim — never resolve it by taking the first match.** ⚠️⚠️ **THE LOCATOR IS SINGLE-USE AND THE COMPANION MUST KNOW IT: once the executable has written a `[status:]` marker, all 14 bare-form headings return count=0 — measured, 14 of 14.** So a post-application re-run of this locator reports "something moved" for every entry, and that reading would be wrong. **Before application, count=0 means drift and HALTS. After application, count=0 is the EXPECTED result and the post-check is the ANNOTATED form.** State which side of the write any locator check sits on. Measured at authoring: `LESSONS.md` contains ZERO duplicate `## ` headings file-wide, so the guard is earnable today and a fire means something moved.

## Drafting Cycle
**Tier:** T1 computed — triggers fired: **T-7** (a companion executable applies this plan's verdicts without re-deriving them) and **T-8** (novel: no prior plan grades the quarantine set, and none has ever applied the `history` STATUS VALUE to `LESSONS.md`). T-5/T-6 do NOT fire — read-only, edits no doctrine, gate, template or contract. ⚠️ **SELF-ESCALATED to a T2-shaped cold panel by CEO call after the walk-6 escalation; the computed tier is unchanged and the panel is an addition to it, not a re-tiering.**
**Walk register:** `governance/knowledge/research/walk-register-diagnostic-bare-entry-ruling.md` — carries the §2.8 Conflict Ledger (OPENED LATE at walk 1b and declared as backfilled; each constraint records its sweep-at-opening result, and the REGISTER owns the count) and the walk-0 clone-diff completions.
**Walks:** walk 0 pinned; walks 1–6 and 8–9 complete (five lenses each, sequential, one commit per lens); **walk 7 = the cold panel** (4 seats, sequential). Plus two declared NON-WALKS that discharge nothing: **walk 1b** (the §2.8/§2.6 corrective) and a **SWEEP pass** after walk 5. ⚠️ **A corrective and a sweep clear no part of §2's bar, and the walk after a sweep is a FIRST pass over the swept arrangement.**
**Walk 0 (context pin) — REAL, measured 2026-08-23:** see the walk register. **Newest same-class, MEASURED not asserted (§2.6): `lessons-forge/…/Done/diagnostic-504.md`, ship date 2026-08-23T18:14:25-05:00** — ahead of 503 (12:32:45 same day) and 501 (2026-08-22), sorted across all three repos' shipped diagnostics. It is also the clone origin. This plan replaces text in no existing file, so §2.0's anchor measurements are structurally empty rather than skipped.
**Direction verdict (after walk 1): PROCEED.** Tested against the three forcing findings rather than judged; none fired. ⚠️ **Re-tested after the cold panel and still PROCEED** — the DISCOVERY seat returned one partial (c) hit (the premise that the three sub-class causes are *measured facts* was false for the conflicting class, whose cause is a source-code literal), and the CAPSTONE returned none. The dissolutions' conclusions, the clone origin and the deposit mechanism all survived.
- Weak spots:          w1 4 folded; w2 5 folded; w3 4 folded; w4 2 folded; w5 2 folded; w6 2 folded.
- Destruction:         w1 4 folded; w2 3 folded; w3 2 folded; w4 1 folded; w5 dry; w6 dry.
- Vulnerabilities:     w1 6 folded; w2 2 folded; w3 2 folded; w4 1 folded; w5 dry; w6 dry.
- Integration-record:  w1 3 folded; w2 3 folded; w3 2 folded; w4 1 folded; w5 1 folded; w6 dry.
- ACID:                w1 3 folded; w2 2 folded; w3 2 folded; w4 1 folded; w5 dry; w6 dry.

⚠️ **Walk 7 — the cold panel — is given its OWN lens lines rather than appended to the series above, and the geometry is deliberate (§3).** Appending it put walk 7's folds on a line that also carried w5/w6 `dry` tokens, and `plan_lint`'s last-lens-line check then read the line as dry and FELL SILENT — a WARN vanishing on a record-only edit, which §3 names as the signature of a log satisfying a check on the step's behalf. Split out, the warning returns EARNED, which is the correct state for an open cycle whose last event was a fold.

- Weak spots (w7):         9 folded.  ·  w8: 1 folded.  ·  w9: 3 folded.
- Destruction (w7):        3 folded.  ·  w8: 1 folded.  ·  w9: 1 folded.
- Vulnerabilities (w7):    8 folded.  ·  w8: 1 folded.  ·  w9: 1 folded.
- Integration-record (w7): 14 folded.  ·  w8: 1 folded.  ·  w9: 1 folded.
- ACID (w7):               6 folded.  ·  w8: 1 folded.  ·  w9: 1 folded.
⚠️ **Walk 7's 44 total includes 4 findings attributed to the clone-diff BRIEF rather than to a lens** (§2.6's attribution convention); they are carried in the register and are not double-counted in the five lines above. **Instruction-class series: w1 20 · w2 14 · w3 10 · w4 6 · w5 1 · w6 2 · w7 40 · w8 4 · w9 6.** ⚠️ **NOT converging: w8 and w9 each found damage from the immediately preceding round's folds, and the incremental-write region took three folds across two consecutive walks — §2.8's oscillation and repeated-region signals both fire.** ⚠️ **Walk 8 is a FIRST pass over the corrected arrangement, not a confirming one, and 3 of its 5 findings are the corrective round's own fold damage.**
**Conflicts:** X1 — no-halt-on-a-wrap-moved-quantity (C5) vs the plan's own unenforced-append finding (Q6c); joint-resolved w1b by making the immunity conditional. X2 — bound-every-escape (C6) vs the-Planner-must-receive-the-work (C16); joint-resolved w1b as deposit-then-flag. X3 — C6 vs routing a missing guard to UNDECIDED; joint-resolved w2 lens 2 with a separate bound, surfaced BEFORE the fold was written. X4 — basis-reconstructs (C16) vs the one-line TSV cell; joint-resolved w2 lens 5 by relocating the reconstruction. ⚠️ **X1 and X2 surfaced a lens LATE because the §2.8 ledger was not yet open; the register records that as the measured cost.**
**Cold panel:** ⚠️ **RUN — Fork C FULL FORM, CEO-called after walk 6's `ESCALATE:yield-rising`.** Scout + DISCOVERY + EXECUTION + CAPSTONE, sequential. **Full and not small because the magnitude proxy's clause (ii) is UNMEASURABLE for a read-only diagnostic that mutates no target artifact**, and §1's if-unsure rule sends unmeasurable to full form. **44 findings / 658,662 tokens** (standing baseline 563k / 45 across five seats). **Yield did NOT decay: 8 / 15 / 9 / 12.** Two scout HIGHs and one EXECUTION HIGH falsified Planner measurements that had survived six warm walks; **9 of the CAPSTONE's 12 were the panel's own fold damage.** ⚠️ **CAPSTONE verdict: NOT-READY at its read** — corrected in the round recorded below.
**Conformance (§5):** first run at walk-1 close (shape-stable, draft path); re-run per walk and at the capstone. `plan_lint` 0 FAIL throughout. `propagation_check` **14 (w1) → 16 (w2) → 15 (w5) → 24 (capstone)**, all one class — the numeral `14` restated where `Q` is declared; the rises are the disambiguation and correction folds themselves, which name both sets and so contain the numeral. ⚠️ **The record's own rule applies: the count never trends to zero, and the pass condition is zero OPERATIVE hits established by classification, not a falling total.** ⚠️ **`walk_register_lint` reports on STDERR and ALWAYS EXITS 0** — the register read CONFORMANT only after the corrective round; every earlier `exit=0` reading in this cycle certified nothing.
**Corrective round (post-panel):** all 44 panel findings author-verified and folded — **44 is the whole panel INCLUDING the capstone's 12 (8 + 15 + 9 + 12), not 44 plus 12** — the capstone's set being `D0` (which the EXECUTION seat's own `-F` fold had broken to return 49 instead of 14), three fold-interactions that each changed an emitted `[status:]` value, and this block, which the panel found had never been folded at all.
**Close:** not reached — the cycle is open. ⚠️ The canonical `Closing:` field name is itself `cycle_check`'s closure token (`CLOSURE_RE`, `cycle_check.py:37`), so an open cycle that carries it as a placeholder escalates `claimed-close-unmet` on every intermediate run. Written in a form that cannot match until it is earned, per §3; restored to canonical form at the close.

## Cycle Manifest
tier: T1
target: knowledge/research/bare-entry-ruling-2026-08-23.md
class: read-only
reads: /Users/marklehn/Developer/GitHub/LESSONS.md, /Users/marklehn/Developer/GitHub/lessons-forge/lessons-forge.db, /Users/marklehn/Developer/GitHub/lessons-forge/knowledge/research/annotation-mapping-2026-08-22.tsv, /Users/marklehn/Developer/GitHub/lessons-forge/knowledge/research/promotion-corrected-2026-08-23.md, /Users/marklehn/Developer/GitHub/lessons-forge/knowledge/research/promotion-corrected-2026-08-23.tsv, /Users/marklehn/Developer/GitHub/lessons-forge/scripts/detect_learned.py, /Users/marklehn/Developer/GitHub/lessons-forge/src/lessons_forge.py, /Users/marklehn/Developer/GitHub/bellows/scripts/walk_register_lint.py, /Users/marklehn/Developer/GitHub/bellows/bellows_root.py, /Users/marklehn/Developer/GitHub/bellows/gates.py, /Users/marklehn/Developer/GitHub/READONLY_AUDIT_CONTRACT.md, /Users/marklehn/Developer/GitHub/PLANNER_TEMPLATE.md, /Users/marklehn/Developer/GitHub/DRAFTING_CYCLE.md
writes: knowledge/research/bare-entry-ruling-2026-08-23.md, knowledge/research/bare-entry-ruling-2026-08-23.tsv
open_forks: (1) the companion executable is BLOCKED on this plan's output; (2) detect_learned.py:245 emits `learned` unconditionally and cannot emit `history`; (3) no validator defines the legal `[status:]` value set; (4) nothing tells a wrap appender to add `[status: pending]`
walks: open
yields: open
validation: owed
coherence: owed — cycle open
N/A

## MUST-PRESERVE

- ⚠️⚠️ **EVERY DATE IN THIS PLAN IS A FIXED LITERAL — do NOT re-derive "today".** The plan was authored 2026-08-23 and WILL dispatch later; both deposit filenames carry `2026-08-23` and are to be written exactly as declared. An agent that stamps the dispatch date creates files the deposit gate cannot find, which reads as a MISSING DELIVERABLE rather than a renamed one — `executable-505` shipped this class.
- **READ-ONLY.** No edit to `LESSONS.md`, no write to any `.db`, no re-label, no new annotation.
- ⚠️ **Corpus reads via `file:/Users/marklehn/Developer/GitHub/lessons-forge/lessons-forge.db?immutable=1`** — `immutable=1`, NOT `mode=ro`: the corpus is WAL, and `mode=ro` fails against a copy with no `-shm` sidecar and touches the live `-shm` mtime. **THREE DECOY CLASSES SIT BESIDE IT AND THEY FAIL IN THREE DIFFERENT WAYS — the inherited form of this guard collapsed them into one and named the wrong mechanism for two.** Verified by execution at walk 6: (1) **21 stale `pre-*.db` snapshots** — count confirmed exactly — carry the schema and answer FLUENTLY AND WRONG, and this is the SILENT class and the real hazard; (2) the siblings `forge.db` and `lessons.db` are 0 bytes and **ERROR LOUDLY** (`no such table: lesson_entries`), not silently — an error you will recognise, against a positive control returning `D10`; (3) a MISSING path is CREATED by `sqlite3` and answers `0` with exit 0, which is the other silent one (see the UNTRACKED-corpus bullet below). Assert `lesson_entries` equals `D10` before trusting any read. ⚠️⚠️ **THIS DIFFERS FROM THE CONTRACT'S `C2` ROW, WHICH NAMES `?mode=ro`, AND THE DIFFERENCE IS DELIBERATE — the contract itself says a differing rule is stated inline in the plan rather than edited into the contract, so this is that statement.** Fill `C2` as *satisfied by the stricter form* and paste every invocation as the row requires; do NOT tick it by switching to `mode=ro`, and do not report it FAILED. ⚠️ **The plan, not the contract, governs here** — `mode=ro` on this WAL corpus fails against a sidecar-less copy and touches the live `-shm` mtime, which is a WRITE to the store the row exists to protect.
- ⚠️ **`LESSONS.md` is in the governance ROOT repo** — `/Users/marklehn/Developer/GitHub/LESSONS.md` — not in lessons-forge, and not in your worktree.
- ⚠️⚠️ **THE CORPUS IS UNTRACKED, SO YOUR WORKTREE CONTAINS NO COPY OF IT — and the failure mode is CREATION, not an error.** Measured: `git ls-files --error-unmatch lessons-forge.db` does not match, against a tracked positive control that does. A relative `sqlite3 lessons-forge.db` from your cwd therefore CREATES an empty database, answers `0` with exit 0, and looks exactly like a true count. **The `D10` identity assert is what catches this, and it only catches it if you run it against the SAME absolute path every later read uses.**
- ⚠️ **The mapping TSV is QUOTED CSV inside a TSV, not raw TSV.** Headings containing `"` are wrapped in quotes with the inner quotes doubled (measured at exec-502; a naive split-on-tab parse aborts the run). Three of the 14 bare rows are in that form. ⚠️⚠️ **AND THE OBVIOUS PROOF DOES NOT WORK — an earlier form of this bullet mandated it anyway.** *Prove your parse recovers all `D7` rows* is a COUNT, and a naive split-on-tab **also returns 14 rows**: it does not abort, it silently yields the still-quoted, doubled-quote heading for entries `59`, `82` and `104`, which then matches ZERO lines in `LESSONS.md`. **The proof must compare VALUES: for those three ids, show the parsed heading has no leading `"` and no doubled `""`, and that `"## " + heading` matches exactly one line.** A count cannot separate a correct parse from the broken one — that is the whole failure mode.
- ⚠️ **A bare heading has NO marker to strip** — that is what makes it bare. Do not reach for the `_key_heading` normalizer to locate one; whole-line equality against `"## " + original_heading` is the locator, with an asserted occurrence count of exactly 1.
- **`grep` here is ugrep: `-F` for every literal search.** ⚠️ A zero-match `grep -c` prints `0` and EXITS 1 — read the printed count, never the exit status. ⚠️ A `-E` pattern with a nested quantifier over this corpus can exceed the shim's complexity limit and ERROR rather than return empty; if that happens, narrow the pattern — do not read the error as an absence.
- ⚠️ **Every negative probe in this plan needs a positive control.** Absence looks identical whether the thing, the pattern, the path or the unit is wrong, and this plan asks repeatedly whether a mechanism exists. State the control alongside each absence claim.
- ⚠️⚠️ **REGISTER FREEZE — a stated consequence of a lesson written THIS session, about THIS quantity.** `LESSONS.md:4629` reads *A session wrap cannot append to an artifact a dispatched plan is mid-flight on — the append is lost, or it moves a count that plan's QA asserts* `[status: pending]`. **The count it names is the bare-heading count, which is this plan's entire subject.** So: no append to `LESSONS.md` while this plan or its companion executable is in flight, mirroring `executable-505`'s corpus-freeze guard. ⚠️ This diagnostic is single-step and read-only, so it cannot be damaged the way the DEV/QA case was — **the freeze is stated for the COMPANION, which can be, and because the entry is `pending`: nothing enforces it, so the plan must carry it.**
- ⚠️ **Invoke any checker by ABSOLUTE path** from `/Users/marklehn/Developer/GitHub/bellows/scripts/`; shell state does not persist between invocations, and a relative call after a `cd` reports `can't open file` and exits non-zero, which is easily misread as a check failing.

## STEP 1 — READ-ONLY DIAGNOSTIC

**Role:** DEV (read-only audit). Contract: `/Users/marklehn/Developer/GitHub/READONLY_AUDIT_CONTRACT.md`. ⚠️⚠️ **ONE CLAUSE OF THAT CONTRACT IS FALSE FOR THIS PLAN AND IS OVERRIDDEN HERE.** The contract scopes itself to a governance audit *deposited to `governance/knowledge/decisions/`* and states as an environment FACT that *this plan runs IN-PLACE … there is no worktree and no teardown*. **THIS plan is a lessons-forge plan and DOES run in a worktree** — see the Commit block, where writing to the main checkout instead loses your deposits silently past both gates. Read the contract for `C1`–`C8`, the forbidden list and the closing sequence; **do NOT import its no-worktree environment claim, and do not expect `git rev-parse --show-prefix` to print `governance/`.** Everything else in it binds.

**Contract parameters.** `cwd`: your bellows worktree under `lessons-forge/.bellows-worktrees/<id>/`. `deposit_paths`: the two files in Deposits. `extra_forbidden`: any write to `LESSONS.md`, to any `.db`, or to any real `knowledge/decisions/` directory. ⚠️ **`extra_permitted`, stated because Q4 MANDATES running two guards and a guard cannot be run without inputs:** you MAY create fixture files under `./scratch-506/` in your worktree — a violating input and a clean control per guard — and you MUST report them under **`C4`, WHICH THIS PLAN DEFINES HERE because the contract does not: `C4` = "wrote only the declared deposits, plus the fixtures named in `extra_permitted`", listed with every path.** ⚠️ The contract offers `C4` only as an example number a plan MAY add, with near-opposite wording; an undefined row invoked as though it existed is a row the agent fills from guesswork. ⚠️⚠️ **`C<n>` DENOTES TWO DIFFERENT REGISTERS IN THIS SHOP AND THEY ARE NOT INTERCHANGEABLE: the read-only contract's rows are `C1`–`C3` and `C5`–`C8` (plus this plan's `C4`), while `C15`/`C16`/`C17` and their neighbours are drafting-cycle LEDGER constraints living in the walk register. Any `C<n>` in a STEP prompt means a contract row.** **Nothing else is writable, `./scratch-506/` is never committed, and the fixtures are never placed in `governance/knowledge/research/` where a real register lives.** ⚠️⚠️ **`C7` IS PARTLY BLIND HERE AND YOU MUST SAY SO RATHER THAN LET A CLEAN PAIR READ AS COVERAGE.** Measured: `lessons-forge/.gitignore:9` ignores `.bellows-worktrees/`, so **nothing inside your worktree — fixtures OR your uncommitted deposits — appears in `git -C /Users/marklehn/Developer/GitHub/lessons-forge status --porcelain`.** The contract makes the before/after pair the evidence; for this dispatch that pair is evidence about the four repos' MAIN trees and about nothing you wrote. **So: run `C7` as specified AND additionally paste `git -C <your-worktree> status --porcelain` before and after, and state in the row that the main-tree pair cannot see your work.** Declare `?? scratch-506/` there; nothing else new may show in either snapshot. ⚠️ Without this clause the plan would mandate an action its own contract forbids, and the agent would be forced to choose between them silently. `extra_preflight`: assert the corpus identity (path, byte size, `lesson_entries` equal to `D10`). ⚠️ **`D11` is RECORDED AND COMPARED, never asserted as a halt condition** — `LESSONS.md` legitimately gains headings at every session wrap, and a plan that HALTS on its own register's sha is the false-HALT `executable-451` measured and `executable-502` warned about. Report the sha you observe beside `D11`; if it differs, re-derive `D1`–`D8` and proceed on YOUR figures. **What you DO assert is set-level and wrap-immune: that the `Q` bare headings are the same 14 the mapping quarantines** — an appended wrap entry moves `D1` and `D4` legitimately and moves the bare set not at all. ⚠️⚠️ **That immunity is CONDITIONAL and the condition is the very gap this plan reports at Q6(c): it holds only while an appender writes `[status:]` on the new heading, and nothing enforces that.** A BARE append grows `Q` to 15, and the 15th is in no mapping and cannot be graded from this plan's inputs. **Disposition: do not silently regrade and do not drop it — report the new heading verbatim under `### Flags for CEO` and grade the 14 you were given.** This is also why the register-freeze clause in MUST-PRESERVE exists. ⚠️ **`C7` REPO SET, pinned explicitly because the contract HALTs on an unnamed set:** `/Users/marklehn/Developer/GitHub` (root), `/Users/marklehn/Developer/GitHub/lessons-forge`, `/Users/marklehn/Developer/GitHub/bellows`, `/Users/marklehn/Developer/GitHub/forge`. ⚠️ **`governance` is deliberately NOT a fifth entry, and the reasoning is worth carrying because a cold seat proposed adding it and a later seat measured why not: `governance/` has no project-local `.git`, so `git -C …/governance rev-parse HEAD` resolves to the ROOT repo already named first.** Adding it would put a duplicate pair into the very paragraph that warns against reading a clean pair as coverage. The two `Done/executable-*.md` files this plan quotes ARE covered — by the root entry. ⚠️ **`forge` is carried from the clone and this plan reads nothing in it — RETAINED deliberately, not by inattention.** The contract's hazard is UNDER-naming (a repo silently dropped from the one guard that watches blast radius); an extra repo costs one `rev-parse` and one `status` and buys a witness if something unexpected touches it.

**Q1 — Reproduce the quarantine.** Re-derive `Q` and the three sub-classes from the mapping, and list the ids in each. ⚠️⚠️ **THEN RE-DERIVE THE THREE WALK-0 DISSOLUTIONS THEMSELVES — they are this plan's SHAPE, they were measured by the Planner, and a downstream executable inherits them without re-checking.** (i) `target_layer='none'` returns exactly the 5 ids stated, corpus-wide — ⚠️ **and re-derive the COUNTERWEIGHT in the same query, because an earlier form of this plan handed you only the RECORD-side signal: the other nine carry a real `target_layer`, and `route='codify'` is set on `134`, `328`, `333` and on `123`'s rejected proposal. That is corpus evidence pointing the OTHER way, and withholding it from a question whose stated failure mode is a collapsed discriminator is how you collapse it;** (ii) each of `93, 116, 123` carries exactly 2 proposals and **no member of any pair is live** — ⚠️ **"live" is DEFINED here, because the whole dissolution rests on it: a proposal is live unless its `status` is `rejected`, `stale` or `superseded`.** **Report each proposal's `status` LITERALLY** so that definition can be checked against the data rather than taken on trust; the corpus carries seven distinct statuses (`accepted`, `implemented`, `proposed`, `reference`, `rejected`, `stale`, `superseded`). ⚠️ **`reference` is deliberately NOT classified by that definition, and the omission is the record's:** SESSION 58b found the 20 `reference` entries *were not a third category but an unfinished label*, so calling them live would contradict that finding and calling them dead would over-read it. **None of `93/116/123` carries `reference`, so the question does not arise on this data — say so rather than resolving it in passing.** (iii) `330` and `331` are the only bare entries whose `target_artifact` is CODE (a `.py` file) — ⚠️ **FIVE of the `Q` entries carry a non-empty `target_artifact`, not two: `93`/`116`/`123` carry `PLANNER_TEMPLATE.md`. An earlier form of this clause said "the ONLY bare entries with a non-empty `target_artifact`", which pin 3 retracted and this sentence did not follow — a fold landing where the defect was noticed rather than everywhere it was described.** Re-derive both figures; the CODE/DOC split is the claim, the presence/absence one is not. ⚠️ **Any divergence is a REPORTED FINDING that changes the plan's shape, not a number to correct in passing** — say which pin, which direction, and what you measured. A plan whose premises are handed down unverified is the failure `diagnostic-503` shipped. ⚠️ **Prove the two 14s are the SAME 14** — the bare headings in the file and the `unknown` rows in the mapping — **by comparing HEADING sets, not counts.** ⚠️ `LESSONS.md` carries no entry id on any heading (measured: 0 of 331), so the id is a corpus-side key only and the heading string is the sole common term; two disjoint sets of 14 would satisfy every count check in this plan. Assert `D2`+`D3`+`D4` = `D5` and `D6` = `D2`+`D3`, and state that no bare entry carries a `[target:]` marker. ⚠️ **Disposition if the two sets are NOT equal: HALT and report both sets with their symmetric difference — do not proceed on the intersection and do not adjust `Q`.** A mismatch means `LESSONS.md` moved in a way the mapping cannot describe, which is a finding the companion executable must not be handed silently.

**Q2 — RULE THREE: is this entry a RECORD or a RULE?** The CEO's discriminator: a RECORD accounts for what happened; a RULE states something a future actor must do or not do, such that a violation is identifiable. ⚠️ **Grade all 14, not only the 5 the corpus flags** — the question is whether the corpus's `none` set is COMPLETE or merely the part someone got to, and that is answerable only by grading the other 9 against the same discriminator. ⚠️ **Read the BODY, not the heading** — a heading is a one-line summary and several of these bodies end in an explicit "**The discipline rule:**" paragraph that the heading does not carry. ⚠️ **A DEGENERATE OUTCOME IS A SIGNAL, NOT A RESULT: if you grade all 14 the same class — all RECORD or all RULE — stop and re-check your discriminator before writing it up, and say in the findings that you did.** The plan's own walk-0 evidence predicts a SPLIT, so a uniform outcome means either that evidence is wrong — which is a reportable finding under Q1 — or the discriminator collapsed. ⚠️ **The prediction is deliberately stated WITHOUT naming which entries fall where:** the same paragraph that warns against a collapsed discriminator must not hand you a pre-assigned one, and an earlier draft did exactly that by pairing the split with the two evidence groups. ⚠️ **Flag the near cases in both directions**: an entry whose body states a rule that has since become unbuildable because the workflow it governs no longer exists is a genuinely close call, and so is a record that happens to contain one imperative sentence. ⚠️⚠️ **THE MIXED ENTRY IS THE COMMON CASE HERE AND THE PLAN OWES YOU A RULE FOR IT: several of these bodies are an incident account that CLOSES with a discipline paragraph.** ⚠️ **The CEO's discriminator governs and this is a TIE-BREAK UNDER it, not a replacement for it** — the discriminator remains *does this state something a future actor must do or not do, such that a violation is identifiable*. When that discriminator is satisfied by only PART of the body, break the tie by what the entry would be RETAINED for: if a future actor would consult it to know what to DO, it is a RULE and the incident is its evidence; if the account is the point and the closing sentence merely generalizes it, it is a RECORD. ⚠️ **State which sentence decided it** — that one quotation is what makes the call auditable, and it is the difference between a graded set and an asserted one. State the reasoning per entry; do not emit a bare verdict. ⚠️ **UNDECIDED is a legal third answer and you are expected to use it rather than force a binary** — 504's own instruction on the equivalent question reads *mark it undecided rather than assuming*. An UNDECIDED entry takes `unknown` per the derivation table, which is a surfaced state, not a silent one. ⚠️ **UNDECIDED is CAPPED, because an unbounded escape satisfies this plan's form while dodging its purpose:** each one must name the specific evidence that would settle it AND why that evidence is not obtainable from here — a reason that dissolves into one command is not a reason (§2.7's INHERITED-marker cost discipline) — and ⚠️ **The number three is a JUDGEMENT, not a measurement — it sits in no pin row, and this plan elsewhere says THE MIXED ENTRY IS THE COMMON CASE, which argues the other way. It is a tripwire, not a truth.** ⚠️ **And the escapes COMPOSE: three judgement-UNDECIDEDs plus two guard-absence ones is 5 of 14 unresolved with no single count exceeding its cap, and Q3's "no artifact exists yet" exit is uncapped on top.** Report the UNRESOLVED TOTAL across every escape route as one number beside the per-class counts. ⚠️⚠️ **AND NOTE THE ARITHMETIC, WHICH TWO FOLDS LEFT CONTRADICTORY: three judgement-UNDECIDEDs plus two guard-absence ones is FIVE, which is within both per-class caps AND OVER the whole-set threshold below.** That is not an oversight to route around: **the whole-set threshold governs, the per-class caps are subordinate to it, and reaching five means you flag** — `_gate_ceo_flags` then blocks, which is the intended outcome. **More than three UNDECIDED across the whole set means the grading did not succeed, and the right instrument is the flag, not a halt: deposit the TSV and the findings anyway, then raise it under `### Flags for CEO`.** ⚠️ Stated as the flag rather than a halt deliberately — verified at `bellows/gates.py`, `_gate_ceo_flags` turns any non-null flag into a blocking gate failure, so the Planner gets the pause WITH the work in hand, whereas a halt skips the closing commit and destroys the partial grading the Planner most needs to see.

**Q3 — Assign a target for every RULE entry.** ⚠️ **Q3's population is whatever Q2 graded RULE — it is NOT a figure you can read off the mapping, and you do not know it until Q2 has run.** Nine of the `Q` entries have a NULL `target_artifact` in the corpus, which is why the detector never graded them, but that set and Q2's RULE set are different objects: five of those nine are the `target_layer='none'` candidates, and a RECORD entry gets no target at all. **Assigning a target to an entry Q2 graded RECORD is a category error, not a thoroughness.** For each entry graded RULE, name the artifact its rule belongs in, using the Fork-2 discriminator already decided: **DEFINITION → `glossary.md`, RUNBOOK → `CLAUDE.md`, TRAP → CODE.** ⚠️⚠️ **`glossary.md` EXISTS IN NO REPO — measured (`find` over the whole tree returns nothing, against a positive control that finds `LESSONS.md`), and `LESSONS.md:4582` says so explicitly while `:4578` carries `[status: pending]`.** So Q3's second half is UNRUNNABLE for a DEFINITION entry: there is no artifact to probe. **Such an entry takes `unknown` with the reason stated — NOT `pending` by default**, because `pending` would silently assert a named target exists and merely lacks the rule. Name the build item (`glossary.md` itself) and move on. ⚠️⚠️ **THIS GOVERNS THE WHOLE QUESTION AND SUPERSEDES THE OLDER SENTENCE BELOW: the trigger "no artifact exists" always yields `unknown`, never `pending`.** Two folds gave that one trigger two different statuses; this is the resolution and the other reading is struck. ⚠️ **Then answer the SECOND half, which is what actually separates `codified` from `pending`: does that artifact ALREADY CONTAIN the rule?** Naming a target does not settle the status — see the derivation table — and this half is an absence claim carrying the full §2.7 discipline (probe derived from the entry's own text, positive control beside every zero). ⚠️⚠️ **A `target_artifact` IN THE TSV IS NOT A `[target:]` MARKER IN THE REGISTER, and the companion must not copy one to the other.** Measured convention: `[target:]` appears if and only if the status is `learned` or `codified` — 0 of 78 `pending` and 0 of 14 bare carry one. So a `pending` row MAY name a build-item artifact in the TSV for the reader, and the executable must NOT write it as a marker; doing so breaks `D6`. **State this in the findings, with the marker's form and position for the statuses that DO take one.** ⚠️ **"No artifact exists yet" is a legal answer and you must be willing to give it** — ⚠️ **and per the clause above it yields `unknown` with the reason, NOT `pending`** (an earlier form of this sentence said `pending`; that reading is struck, because `pending` asserts a named target exists and merely lacks the rule). Name the build item when you give it.

**Q4 — Grade each RULE entry against 504's two rules.** Rule One: a mechanism that leaves a material part of the lesson's scope uncovered is `codified`, not completion. ⚠️ **Apply it by ARGUING FROM THE CASES, not as settled law — `504` established it on six entries, none of which is in this set, and it required exactly this: if any case here is partly-enforced only in a trivial or immaterial way, that is an argument for a NARROWER rule and you should say so.** The clause was dropped in cloning; it is restored because this population has never been tested against the rule. Rule Two: a mechanism cannot enforce a lesson about that mechanism's own insufficiency — ask whether the mechanism REJECTS A VIOLATION of the lesson or is merely the lesson's SUBJECT. ⚠️ **Read `promotion-corrected-2026-08-23.md` §Q2–Q3 for both rules as stated, and cite the discriminator you applied; do not reconstruct them from this plan's summary.** ⚠️ **Three entries need a mechanism DEMONSTRATED, not assumed, and the verdict turns on it:**
- **`330` and `331`** name guards in `walk_register_lint.py` (`duplicate_row`, `headerless_rows`). Confirm each exists, then RUN it — a violating input must be rejected AND a clean control must pass. A guard that fires on everything proves nothing. ⚠️⚠️ **THE EXIT CODE IS NOT A DISCRIMINATOR — a clean register, a `duplicate_row` violation and a `headerless_rows` violation ALL EXIT 0.** ⚠️⚠️ **AND THE DISCRIMINATOR IS ON `stderr`, NOT stdout. An earlier form of this bullet said "the second field of the first stdout line" and that was WRONG — measured with the streams MERGED (`2>&1`), which cannot tell them apart.** Separated: **`stderr`** carries `<file>\t{CON,UNCON}FORMANT\tshapes: …`; **`stdout`** carries the TSV table whose FIRST line is the header `file  line  table  row_status  file_status …`, so its second field is the literal word `line`. Read `UNCONFORMANT` from stderr, or `file_status` (field 5) from a stdout DATA row — never field 2 of the first stdout line. (The headerless case additionally reports `shapes: (none)`.) **An agent that verifies these runs by exit status will record every fixture as passing, including the violations, and will report a guard as working on evidence that shows nothing.** ⚠️ **Both guard names were verified present at authoring — `duplicate_row` and `headerless_rows`, `walk_register_lint.py` v0.3, against a positive control for a name that should not exist. If either is absent or renamed when you look, that is itself a REPORTED FINDING and the affected verdict becomes UNDECIDED — do not substitute a guard that looks similar.** ⚠️ **A guard-absence UNDECIDED is a SEPARATE CLASS from a judgement UNDECIDED and is bounded separately: at most 2 (there are two guards), reported under its own heading, and it does NOT consume the cap of three that bounds hard-judgement UNDECIDEDs.** Both classes stay bounded — a tooling accident must not spend a budget meant for entries that are genuinely hard to grade, and it must not go unbounded either. ⚠️ **Running it does NOT trip the contract's ban on importing anything under `bellows/`, and you are not to refuse on that ground:** measured, `walk_register_lint.py` imports `re`, `sys` and `pathlib` only — no bellows module, no DB connection. The ban targets `python3 -c`/`-m` imports that reach a read-write handle; a stdlib-only script run as a subprocess is not that. Then apply Rule One: each lesson's "How to apply" carries a second clause the guard does not cover. ⚠️⚠️ **BEFORE EITHER RULE, ASK THE QUESTION 504'S TWO RULES DO NOT ASK: IS THE MECHANISM INVOKED BY ANYTHING?** Measured by the cold panel and then traced end-to-end by the Planner, because a REFERENCE COUNT is not a reachability proof and this one looks reachable until you follow it. `fold_check.py:99` does invoke `walk_register_lint`, and `cycle_check.py` does invoke `fold_check`, and `depositor.py:440` does invoke `cycle_check` — **so the chain appears to close, and it does not. It breaks in TWO places:** **(1)** `fold_check.readers_for()` adds `walk_register_lint` **only when the artifact's filename starts with `walk-register-`**; handed a PLAN it selects `plan_lint` instead. **(2)** `cycle_check`'s `fold_check` call lives in `emit_manifest()`, not in `run_check()` — and `depositor.py` calls `run_check`. It is further gated on a `.foldcheck.json` baseline existing beside the plan. **Net: no automatic path reaches this guard. It runs when a human runs `fold_check` against a register, and at no other time**, while `plan_lint` and `cycle_check` ARE wired into `depositor.py`. ⚠️ **RE-DERIVE THIS TRACE — it is a Planner measurement of exactly the class Q1 makes you re-derive, and it decides two verdicts.** Both breaks are one `sed` each (`fold_check.readers_for`, and which `cycle_check` function holds the call); the appearance of a closed chain is the trap, so follow it rather than counting references. **A divergence here is a REPORTED FINDING that changes `330`/`331`'s verdict.** **A guard nothing calls cannot REJECT a violation; it can only report one to whoever chooses to run it.** Rule Two asks whether the mechanism rejects a violation, so the invocation question is upstream of it and currently unasked — **state the invocation status for every mechanism you grade, and treat an uninvoked mechanism as failing Rule Two's reject condition unless you can show what calls it.** ⚠️ **This is a GAP IN THE INHERITED RULE SET, not a defect in this plan's targets — report it under Q6 as a sixth item.** ⚠️ **AND APPLY RULE TWO AS WELL — the derivation table requires BOTH rules and this instruction named only one.** Ask it explicitly: is `walk_register_lint`'s guard the lesson's SUBJECT, or does it REJECT a violation of the lesson? Answer it rather than assuming; a guard built in response to a defect is usually an enforcer and not a subject, but that is the finding to make, not the premise to start from.
- **`122`** names a discipline (resolve roots by marker walk-up). `bellows/bellows_root.py` implements it. ⚠️⚠️ **AN EARLIER FORM OF THIS BULLET SAID NO `__file__`-RELATIVE ROOT CONSTANT SURVIVES IN `bellows/*.py`. THAT WAS FALSE AND THE WAY IT WAS FALSE IS THE POINT.** The Planner probed for `__file__).parent.parent` — a pattern COMPOSED FROM MEMORY — and got zero. The live construct is `Path(__file__).parent.resolve()`, ONE `.parent`, and it survives at **`bellows.py:26`, `planner.py:11`, `verdict.py:13`**, with `bellows_root.py`'s own docstring calling that exact form *legacy*. A probe built from the author's recollection returned a confident false absence on the one measurement the verdict turned on — the class this plan's own MUST-PRESERVE forbids, committed by its author. **Derive your probe from the target text and pair it with a positive control; the three sites are handed to you as the control.** ⚠️ **That is the FIX, and the fix is not the enforcement** — the discriminating question is whether anything REJECTS a newly-authored `__file__`-relative root. Look for it, state the control for your search, and if nothing rejects it, the verdict is `codified` with the guard as a named build item.

**Q5 — Emit the mapping.** Deposit `knowledge/research/bare-entry-ruling-2026-08-23.tsv` with columns `entry_id`, `entry_heading`, `class`, `target_artifact`, `mechanism`, `mechanism_file`, `rule1_partly`, `rule2_circular`, `verdict`, `basis`. ⚠️ **`heading_line_no` was in an earlier column list and is DELETED, deliberately: this plan forbids locating by line number, so a line-number column's only possible use is the one use the plan bans — and a reader with two locators in front of them will sometimes reach for the cheap one.** What is lost is a human reader's convenience in jumping to an entry; the verbatim heading recovers that with one search, and the companion executable needs the heading regardless. ⚠️⚠️ **`entry_heading` IS THE VERBATIM HEADING LINE MINUS THE LEADING `## ` — every tag marker, every space, the date prefix, byte for byte — ⚠️ **and do NOT normalize the spacing: an earlier form of this clause said "both spaces", but the house style is not uniform (measured across the 14: four use two spaces before the first `[tag:`, ten use one; file-wide 51 / 269 / 11 with no tag). Copy what is there**.** ⚠️ **The clone precedent points the WRONG WAY here and following it would break the executable:** `promotion-corrected-2026-08-23.tsv` carries a SHORTENED heading (date prefix and tags stripped), which is fine for a reading artifact and useless as a locator — and the companion executable's only sanctioned locator is whole-line equality against `"## " + entry_heading`. A shortened value silently matches nothing. ⚠️ **Quote by the same convention the 501 mapping uses** — a field containing `"` is wrapped in quotes with inner quotes doubled — **and state the convention in the findings document**, because three of the 14 carry that form. ⚠️⚠️ **A LITERAL TAB IS THE TRIGGER THE CONVENTION MISSES, and `basis` is where one will arrive**: Q4 has you read a TAB-separated stderr line as a deciding fact, and pasting it raw yields an 11-field row that truncates `basis` and destroys the `→ ### <id>` pointer (measured). **Quote on `"`, tab OR newline; commas and quotes round-trip clean, a raw tab does not.** ⚠️ **And if you write the TSV with `csv.writer`, pass `lineterminator='\n'` — the default is `\r\n`, which puts a stray carriage return at the end of every row (measured in the panel's own re-run).** ⚠️⚠️ **AND NOTE WHAT QUOTING DOES TO THE POINTER: a quoted `basis` cell ends with `"`, not with `→ ### <id>`.** So the pointer rule is on the FIELD VALUE after unquoting, never on the raw line — **strip the quoting first, then assert the value ends `→ ### <id>`.** Two folds left this contradictory; this is the resolution. One row per (entry, mechanism) pair; an entry with no mechanism gets one row with the mechanism columns empty. `class` is `RECORD`, `RULE` or `UNDECIDED`; `verdict` is `HISTORY`, `LEARNED`, `CODIFIED`, `PENDING` or `UNKNOWN`, and it is DERIVED from the table above rather than chosen — state which row of that table each entry took, **by its `R<n>` id**. ⚠️ **The reconstruction is REQUIRED and a TSV cell CANNOT HOLD IT — this is a stated split, not an oversight.** A3 asks `basis` for the literal probe, its result and a positive control; the hit-classification rule above adds an operative-vs-mentioned verdict per hit. That is multi-line evidence, and a TSV row is one line. **So: the full reconstruction lives in the findings document under a per-entry heading — probe, raw result, positive control, and each hit classified — and `basis` carries the one-line DECIDING fact plus the heading it points at.** Neither artifact is complete alone, and the findings document says so. A later author must be able to re-run your reasoning from the deposited artifacts alone — the corpus, `LESSONS.md` and the two deposits — without this plan's conversation. A `basis` reading *"not found in PLANNER_TEMPLATE.md"* is exactly the unreconstructable form this clause forbids. ⚠️ **Rule Two is a property of the PAIR**, and `504`'s recording discipline is restored here as one unit after this plan dropped all three parts of it: **(a) an entry is `learned` if AT LEAST ONE of its mechanisms passes BOTH rules, and `codified` only if NONE does** — not a vague "best-passing mechanism"; **(b) both rule columns are recorded INDEPENDENTLY, never short-circuited once the first fires**, because the two carry different lessons for Q6 and a reader must see which applied; **(c) give the per-verdict counts explicitly so none can be derived from another.** State the per-entry verdict and the distinct-entry count separately from the row count. ⚠️ **NAME THE AUTHORITATIVE DEPOSIT, which `504` required and this plan dropped: the TSV is the machine-applied set; the findings document is its reconstruction. Where the two disagree the TSV governs AND the disagreement is itself a defect to report** — two mutually-referencing deposits with neither named authoritative is how a companion picks the wrong one. ⚠️ **STATE THE SET'S AUTHORITY:** the executable may apply these verdicts mechanically and may apply NOTHING else — no inference from a neighbouring row, no reconciliation against the 501 mapping, no arithmetic that recovers a different set. ⚠️⚠️ **THE POST-APPLICATION SUM IS NOT EVIDENCE AND YOU MAY NOT CITE IT AS ANY — it balances for EVERY assignment of the 14, because 317 + 14 = `D1` however the 14 are labelled.** This is the exact trap `504`'s MUST-PRESERVE warns about (*it balances for ANY value of `G`, which is why nothing internal to 503 caught its error*) — and this plan DROPPED that warning while keeping the identity, until the cold panel restored it. **Report the sum as arithmetic hygiene and label it non-discriminating.** ⚠️ **The relation that CAN be violated is the marker cross-tab: every `[target:]`-bearing heading is `learned` or `codified`, and no `pending`, `history` or `unknown` heading carries one.** Measured pre-run: 239 = 14 + 225, with 0 of 78 `pending` and 0 of 14 bare. ⚠️⚠️ **Two cautions, both measured by the panel by simulating assignments on a scratch copy.** (1) **It is a MARKER-CONSISTENCY check, not an assignment check** — two wildly different assignments of the 14 both pass it; it catches a malformed write, never a wrong verdict, and must not be cited as evidence the grading is right. (2) **The numerals 239/14/225 are PRE-RUN. Do not hand them forward as a post-run expectation** — any `codified` verdict legitimately raises the `[target:]` count (a simulated 6-codified assignment measured 245), so asserting the old number would HALT a correct run. **Assert the RELATION, recompute the numerals.** **Assert the arithmetic from your own re-derived figures; do not transcribe a numeral from this plan.**

**Q6 — What must change so `bare` cannot recur?** **SIX gaps, (a) through (f) below.** ⚠️ An earlier form said *four*, listed five, and then asked for "one of the four" while excluding (e), the item this plan calls the most direct cause. Size each, say where it belongs (`detect_learned.py`, a checker, the diagnostic template, or doctrine), and state what it would cost:
(a) `scripts/detect_learned.py:245` emits `learned` unconditionally — wrong under the SESSION 59 ruling, and with no way to emit `history` at all. ⚠️ **The adjacent question is already ANSWERED and you should not re-litigate it: a new status VALUE round-trips through ingest safely.** `_STATUS_TARGET_MARKER_RE` (`src/lessons_forge.py:52`) keys on the marker NAME and consumes any value, so `_key_heading` maps `[status: history]`, `[status: unknown]` and `[status: pending]` to one identical key — verified by EXECUTING the real matcher over all four forms plus a marker-free positive control, not by reading it. The exec-499/500 re-insertion class does not fire on this change;
(b) nothing anywhere defines the legal `[status:]` value set, so neither a bare heading nor a misspelled value is catchable — ⚠️ **and the deeper half, measured: the values are not DEFINED anywhere durable either.** `LESSONS.md` carries 225 `codified` labels and zero statements of what `codified` MEANS; the phrase *written into its target, unenforced* appears in full in `shop_next_session.md` only — `503` and `505` carry the stem, not the phrase — and in **no** governing artifact — not `PLANNER_TEMPLATE.md`, not `DRAFTING_CYCLE.md`, not `COMPANY.md`, not `ARCHITECTURE.md`, not `lessons-forge/CLAUDE.md` (probed with a positive control that located the phrase where it does live). A register annotated with a vocabulary that lives only in a session baton decays the moment the baton rolls;
(c) nothing tells a wrap appender to add `[status: pending]`, so every append is invisible to the build queue — SESSION 59's three appends carried it by hand;
(d) the detector scores a prose-term ratio against a target that may be CODE, where the score means nothing — the mechanism that quarantined `330` and `331`;
(e) ⚠️ **the most direct cause, and the one none of (a)–(d) names: the pipeline ALREADY HAD a value for this case and the executable declined to write it.** The 501 mapping proposed `unknown` for all `Q` rows and `executable-502` quarantined them as BARE instead. ⚠️ **Whether Fork 3 already covered this is the QUESTION, not the premise — do not accept the framing an earlier draft of this plan used.** Fork 3 (SESSION 58b) decided *undecidable ⇒ `unknown`, surfaced, never auto-routed* **about the 20 `reference` entries**; whether that generalizes to the mapping's `unknown` rows is exactly what was never settled, and it is why `502` left them bare pending a CEO ruling rather than in defiance of one. **Read Fork 3's actual scope before answering.** Then say whether writing the proposed value — even provisionally — should be the default, and what would be lost. ⚠️ **Distinguish two things this plan deliberately keeps apart: it ADOPTS the convention for its own `Q` entries — the derivation table routes an UNDECIDED entry to `unknown` rather than leaving it bare — and it ASKS whether that should be the general default for the pipeline. The first is settled and is not evidence for the second; a single applied instance is a precedent, not a policy argument.**
(f) ⚠️ **the gap Q4 hands you: `504`'s two rules never ask whether a mechanism is INVOKED, so a warn-only guard nothing calls can read as enforcement.** Size it as doctrine, not as tooling.
⚠️ **Say which single one of the SIX would have prevented the largest share of this plan's work**, and argue it.

⚠️⚠️ **WRITE INCREMENTALLY — THE STEP IS SATURATED AND HAS NO RESUME.** This one step now carries five mandated re-derivations, 14 entries each needing a Q2 grade with its deciding sentence, a Q3 artifact probe with a positive control, Q4 mechanism work, and a findings section apiece — plus two cross-referencing deposits. **A single saturated step's measured risk is not grading quality, it is RESUME** (§2.7), and this plan has no resume story: if you run out of room, everything unwritten is lost.
**So: create the findings document EARLY and append each entry's section AS IT IS ESTABLISHED — rather than composing the whole at the end.** ⚠️ **Work the entries in ASCENDING `entry_id` and say so in the receipt: `Q` is a set and carries no order, so the order must be stated, not improvised.**
⚠️⚠️ **BE CLEAR ABOUT WHAT THIS DOES AND DOES NOT BUY, because an earlier form of this clause claimed something false — that a step killed part-way *leaves usable partials*. It does not.** `_teardown_worktree` merges **commits** and returns nothing when there are none, so an uncommitted file in your worktree dies with the worktree; and the contract permits exactly ONE commit, in the closing sequence. **Nothing you have not committed survives you.**
**What incremental writing actually buys is the ability to STOP EARLY ON PURPOSE:** if you are running short, stop grading, complete the closing sequence with the entries you have — a TSV covering `k` of the 14 and a findings document matching it — and raise the shortfall under `### Flags for CEO` naming exactly which `entry_id`s are ungraded. **That set is committed, and it reaches the Planner. A silent death loses everything, so choose the deliberate stop before you are forced into the other one.** Write the TSV last, from the sections. ⚠️ **And say in your Output Receipt how far you got**: a partial set the Planner can see beats a complete one the Planner never receives, and the `### Flags for CEO` route exists for exactly that.

**Findings document:** Q1–Q6, each answered with command output or `file:line`, **plus one `### <entry_id> — <heading>` section per `Q` entry carrying the reconstruction the TSV cannot hold.** ⚠️ **Sketching one real block showed the section list was short by four items the plan mandates elsewhere and homes nowhere. The section carries, in this order:** (1) the derivation-table row this entry took, **by its `R<n>` id** (walk 8 gave the rows stable ids precisely so this could not be written as an ordinal); (2) the SENTENCE that decided RECORD-vs-RULE, quoted; (3) the per-entry reasoning; (4) for every mechanism, its INVOCATION status; (5) the probe, its raw result and the positive control; (6) each hit classified operative-vs-mentioned. ⚠️ **For a RECORD entry, items (4)–(6) are legitimately `n/a` and roughly five of the 14 will look that way — write `n/a` explicitly rather than omitting the slot**, so a thin section is visibly a RECORD and not an unfinished one. ⚠️ **`basis` points at these headings, so they are load-bearing rather than commentary — a TSV row whose `basis` names a heading that does not exist is a broken deposit.** Close with `## What could not be measured`, `## Open forks`, and `## Recommended executables`.

**Scope:**
- `/Users/marklehn/Developer/GitHub/lessons-forge/knowledge/research/bare-entry-ruling-2026-08-23.md`
- `/Users/marklehn/Developer/GitHub/lessons-forge/knowledge/research/bare-entry-ruling-2026-08-23.tsv`

**Deposits:**
- `/Users/marklehn/Developer/GitHub/lessons-forge/knowledge/research/bare-entry-ruling-2026-08-23.md`
- `/Users/marklehn/Developer/GitHub/lessons-forge/knowledge/research/bare-entry-ruling-2026-08-23.tsv`

**Commit:** ⚠️ **WORKTREE DISCIPLINE.** You are dispatched into `lessons-forge/.bellows-worktrees/<id>/`. Write both deposits at the SAME relative paths under YOUR cwd and commit them there in ONE commit: `git -C <your-worktree> add knowledge/research/<both> && git -C <your-worktree> commit -m "..."`. ⚠️ Do NOT write to the main checkout — `gates._resolve_deposit_path` falls back to "path as-is" and `_check_deposit_uncommitted` swallows the out-of-worktree git error, so writing to the wrong checkout passes both gates SILENTLY and the teardown-merge never picks your files up. Absolute for everything you READ, relative-to-cwd for everything you WRITE.
