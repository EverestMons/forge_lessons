# lessons-forge — rule on the 14 bare `LESSONS.md` entries: grade them under the CEO's `history` ruling and emit the executable's input

**Date:** 2026-08-23 | **Project:** lessons-forge | **Tier:** Medium | **Dispatch Mode:** bellows | **cycle_tier:** T1 | **Test Scope:** none (read-only diagnostic) | **Execution:** Step 1 (READ-ONLY DIAGNOSTIC) | **Priority:** 1

**auto_close:** false
**pause_for_verdict:** after_step_1

**Depends on:** `Done/diagnostic-501.md` and its mapping `knowledge/research/annotation-mapping-2026-08-22.tsv` (the quarantine list); `Done/diagnostic-504.md` and `knowledge/research/promotion-corrected-2026-08-23.md` (the two grading rules); `/Users/marklehn/Developer/GitHub/governance/knowledge/decisions/Done/executable-502.md` (which quarantined these 14 by construction) and `/Users/marklehn/Developer/GitHub/governance/knowledge/decisions/Done/executable-505.md` (which set the current state) — ⚠️ **both are GOVERNANCE plans, not lessons-forge ones**, and a bare `Done/…` reference to either resolves to a file that does not exist. Newest same-class and clone origin: **`Done/diagnostic-504.md`**, shipped 2026-08-23.

## Why this exists

`executable-502.md` annotated 313 mapping rows and left **14 untouched by design** — its D2 row reads "rows QUARANTINED, untouched … `proposed_status` is `unknown`". Those 14 headings are the register's only bare ones. They were never a judgement the pipeline made and lost; they are a judgement it declined to make.

**CEO ruling, 2026-08-23:** `[status: history]` is a **fourth legal value** — for a register entry that RECORDS what happened rather than states an enforceable rule. It exists because the three-value taxonomy has no cell for such an entry: `pending` claims buildable work that does not exist, `codified` claims a target that does not exist, `learned` claims an enforcement that does not exist. Under the SESSION 59 ruling `learned` means COMPLETION, so all three would be false of the same entry.

This plan grades the 14 under that ruling and emits the mapping a companion executable applies.

## What walk 0 measured, and why it changes the plan's shape

The 14 quarantine under three stated causes. **All three dissolve on measurement, each for a different reason** — so this is not "grade 14 hard cases", it is "three quarantine classes, none of which is what its label says".

1. **`target_layer = 'none'` occurs EXACTLY 5 times in the whole 378-proposal corpus, and those 5 entries are `59, 82, 88, 104, 112`.** An earlier classification pass had already recorded the record-not-rule judgement as data; the annotation pipeline had no status value that could express it, so it surfaced as `no_target` and then as bare. ⚠️ **This is a corroboration, not an authority** — it was produced by the same class of pass this plan is auditing, and Q2 tests the 5 from their BODIES. It is stated here because a reader must be able to tell a converging second measurement from a circular one. ⚠️ **If your body-reading DIVERGES from the column on any of the 5 — or finds a sixth RECORD outside them — your reading wins and the divergence is a REPORTED FINDING, not a discrepancy to reconcile away.** It would mean the column is incomplete or wrong, which is itself an answer Q6 needs; say which entry, which direction, and what in the body decided it.
2. **`conflicting-proposals-quarantined` is a misnomer.** Entries `93, 116, 123` carry exactly 2 proposals each, and measured, **both members of every pair are non-live** — one `stale`, one `rejected`. There is no live proposal to choose between and nothing to reconcile. The detector quarantined a pair with no live member.
3. **The 2 `threshold` entries are the ONLY bare entries carrying a `target_artifact`, and it is CODE** — `walk_register_lint.py` for both `330` and `331`. The detector scores a prose-term ratio against the target's text; run against a Python file that ratio measures nothing. **The threshold class is a detector defect, not an undecidable entry.** ⚠️ **The EVIDENCE is that a guard shipped for both; the VERDICT is Q4's and this plan does not pre-judge it** — an earlier draft called them the set's strongest `learned` candidates, which primes the agent on the two entries whose grading is the most mechanically involved in the whole set. Grade them from the guard's behaviour and Rule One, not from this paragraph.

## What this plan does NOT do

- **It does NOT edit `LESSONS.md`, write to any `.db`, or re-label anything.** Read-only. Its output is the executable's input.
- **It does NOT re-open the 317 annotated entries.** They are not candidates. ⚠️ Say so explicitly in the findings rather than leaving it implied — a reader must be able to tell "not a candidate" from "considered and rejected".
- **It does NOT re-run the annotation detector over the corpus**, and it does not need to: the three sub-class causes above are measured facts about the mapping and the corpus, and Q2's question — is this entry a record or a rule — is answered by reading the entry, not by re-scoring it. ⚠️ **The two exceptions are `330`, `331` and `122`, where the verdict DOES depend on whether a mechanism exists and fires** (Q4). Run those; do not assume them.
- ⚠️ **It does NOT accept the corpus's `target_layer` as the answer to Q2.** See the warning in pin 1.

## The verdict derivation (the constraint Q2–Q5 share)

⚠️ **Sites: Q2, Q3, Q4, Q5.** Stated here rather than inside one question because it spans four, and a constraint carried by whichever question the author happened to be drafting is the half-carried-guard class.

Each entry's status is DERIVED, never chosen:

| class (Q2) | is the rule already written into a named artifact? (Q3) | does a mechanism REJECT a violation? (Q4) | status |
|---|---|---|---|
| RECORD | n/a | n/a | `history` |
| RULE | yes | yes, and the pair passes BOTH 504 rules | `learned` |
| RULE | yes | no mechanism, or one failing either 504 rule | `codified` |
| RULE | no — no artifact contains it yet | not reached | `pending` |
| UNDECIDED | — | — | `unknown` — surfaced, never auto-routed (Fork 3, SESSION 58b) |

⚠️⚠️ **THE TWO MIDDLE COLUMNS ARE INDEPENDENT FOR A DOC TARGET AND COUPLED FOR A CODE ONE — read the table with that in mind or it will mislead you.** When the target artifact is a document, "written into it" and "a mechanism rejects a violation" are separate facts. When the target is CODE — as it is for `330` and `331`, whose target is `walk_register_lint.py` — writing the rule in IS the mechanism, so both columns read `yes` mechanically and the row lands on `learned` without anything having been judged. **Rule One is the whole discriminator there**, which is why Q4 makes those two verdicts turn on RUNNING the guard and then asking what the lesson's remaining clause is not covered by it. ⚠️ **Rows three and four are the pair this plan exists to separate, and Q3 is where they separate.** `codified` and `pending` differ ONLY by whether the target artifact ALREADY CONTAINS the rule — so naming a target does not settle it, and Q3 must answer both halves. ⚠️ **The second half is an ABSENCE CLAIM against a 420KB file and it carries the §2.7 discipline in full:** derive the probe from the entry's OWN text rather than composing one from memory, and pair every zero with a positive control. A bare-word probe is worthless here and the Planner measured it — `canary` returns 17 hits in `PLANNER_TEMPLATE.md` and settles nothing about the live-canary entry.

⚠️⚠️ **TWO PROPERTIES MAKE THIS THE HARDEST QUESTION IN THE PLAN, AND BOTH ARE CODIFIED RULES THIS PLAN WOULD OTHERWISE RE-TRIP.**
1. **Decoy density is HIGHEST in exactly these files.** `PLANNER_TEMPLATE.md` and `DRAFTING_CYCLE.md` are documents ABOUT conventions, so they quote the conventions they govern. §2.7: anchor every structural search line-anchored, and **strip fenced blocks and blockquotes before matching any token that also appears in prose** — measured there at nine prose mentions causing four misfired measurements, one landing 251 lines early.
2. **A HIT IS NOT AN ANSWER — CLASSIFY IT.** A target artifact that QUOTES a lesson (in a History row, a changelog, a retraction, a worked example) is not an artifact that ENACTS it, and `codified` means enacted. **For every hit, say whether the surrounding text is the rule OPERATING on a reader or the rule being MENTIONED; zero OPERATIVE hits is the `pending` condition, and the total is meaningless.** This is the same discipline the record already carries for retraction text — a well-run artifact accumulates literal instances of the very thing being negated.

⚠️ **Every one of the `Q` entries receives a value.** `unknown` is the escape for a genuinely undecidable entry; **bare is not.** ⚠️ **Two post-conditions live here and only one is yours.** THIS plan's, checkable at Step 1: every one of the `Q` entries has at least one TSV row and a non-empty per-entry `verdict` — assert it against your own deposit before the closing commit. The COMPANION's, which you cannot check and must not claim: bare reaches 0 in `LESSONS.md`. State the first as measured and the second as the executable's.

## Numbers discipline

⚠️⚠️ **TWO DIFFERENT SETS IN THIS PLAN BOTH NUMBER 14 — `Q` (the bare headings, this plan's subject) and `D2` (the `learned`-labelled headings, which this plan does not touch). Every unqualified "the 14" in this document means `Q`.** Said once, here, rather than qualified at each site: `propagation_check` reports every prose `14` as a restatement of `Q` and cannot tell the two apart, and neither can a reader.

⚠️ **This table owns every quantity the plan ACTS ON.** Values measured at walk 0, 2026-08-23. **RE-DERIVE every one; if yours differ, yours supersede and you say so.**

| id | pin | value | probe |
|---|---|---|---|
| D0 | **`Q`** — bare headings, this plan's whole subject | **14** | ⚠️ **enumerate, do not merely count** — `grep -n '^## ' <abs LESSONS.md>` filtered to lines NOT containing `[status:`; the COUNT is `D1` − `D5`, and a probe returning only the count cannot supply the set every later question acts on |
| D1 | **`N`** — dated headings in the register | **331** | `grep -cE '^## 20[0-9][0-9]' <abs LESSONS.md>` — equals the all-H2 count, so every H2 is a dated entry |
| D2 | `learned` | **14** | `grep -cE '^## .*\[status: learned\]' <abs LESSONS.md>` |
| D3 | `codified` | **225** | same form, `codified` |
| D4 | `pending` | **78** | same form, `pending` |
| D5 | headings carrying ANY `[status:]` | **317** | `grep -cE '^## .*\[status: ' <abs LESSONS.md>` — and `D2`+`D3`+`D4` must equal it |
| D6 | headings carrying `[target:]` | **239** | `grep -cE '^## .*\[target: ' <abs LESSONS.md>` — equals `D2`+`D3`; ⚠️ **no bare entry carries a target**, so a check expecting a mix fails on a correct run |
| D7 | mapping rows with `proposed_status = unknown` | **14** | `awk -F'\t' 'NR>1 && $4=="unknown"' <mapping tsv> \| wc -l` — must equal `Q`, and the id sets must be EQUAL, not merely equinumerous |
| D8 | sub-class `no_target` / `conflicting` / `threshold` | **9 / 3 / 2** | ⚠️ the `basis` column is FREE TEXT and carries a varying ratio, so "partition on it" is not executable as written — the discriminator is: `basis` contains `no_target` → no_target; contains `conflicting` → conflicting; otherwise → threshold. State the three id lists, not just the counts |
| D9 | corpus-wide proposals with `target_layer = 'none'` | **5** | `SELECT COUNT(*) FROM lesson_proposals WHERE target_layer='none'` |
| D10 | corpus `lesson_entries` — negative pin | **370** | `sqlite3 "file:<abs>?immutable=1" "SELECT COUNT(*) FROM lesson_entries"` |
| D11 | `/Users/marklehn/Developer/GitHub/LESSONS.md` sha256 | `8ba177d0400c87da4673247fb4de9af116a140845b8e30868ef70f8e7e22d363` | `shasum -a 256` — 641,081 bytes |

⚠️ **`D1` is 331, not the 327 the mapping was generated against.** SESSION 59's wrap appended after `diagnostic-501` ran. The appends landed at the END of the file, so the mapping's `line_no` column still resolves today — **but that is luck, not a contract. Locate every heading by WHOLE-LINE equality against `"## " + original_heading` and assert the occurrence count is exactly 1; never by the mapping's line number.** ⚠️ **Disposition at the boundary, stated because the guard is silent about it: a count of 0 or >1 HALTS on that entry and is reported with the heading verbatim — never resolve it by taking the first match.** Measured at authoring: `LESSONS.md` contains ZERO duplicate `## ` headings file-wide, so the guard is earnable today and a fire means something moved.

## Drafting Cycle
**Tier:** T1 — triggers fired: **T-7** (a companion executable applies this plan's verdicts without re-deriving them) and **T-8** (novel: no prior plan grades the quarantine set, and none has ever applied the `history` value). T-5/T-6 do NOT fire — read-only, edits no doctrine, gate, template or contract.
**Walk register:** `governance/knowledge/research/walk-register-diagnostic-bare-entry-ruling.md` — carries the §2.8 Conflict Ledger (16 constraints, each with its sweep-at-opening result, OPENED LATE at walk 1b and declared as backfilled) and the walk-0 clone-diff completions.
**Walks:** walk 0 pinned; walk 1 complete; walk 2 complete (five lenses each, sequential, one commit per lens); walk 1b = a declared NON-WALK corrective (§2.7's targeted-pass rule) that opened the ledger and finished walk 0's clone-diff. ⚠️ **Walk 1b discharges NOTHING and is not a walk** — it clears no part of §2's bar.
**Walk 0 (context pin) — REAL, measured 2026-08-23:** see the walk register. **Newest same-class, MEASURED not asserted (§2.6): `lessons-forge/…/Done/diagnostic-504.md`, ship date 2026-08-23T18:14:25-05:00** — ahead of 503 (12:32:45 same day) and 501 (2026-08-22), sorted across all three repos' shipped diagnostics. It is also the clone origin. This plan replaces text in no existing file, so §2.0's anchor measurements are structurally empty rather than skipped. The three measurements that fixed its shape are the three numbered pins above.
**Direction verdict (after walk 1): PROCEED.** Tested against the three forcing findings rather than judged. **(a) clone origin —** I2 IS a clone-diff finding, but it invalidates one inherited COLUMN convention (504's shortened heading), not the origin: the two-rule frame, per-pair rows, numbers discipline and read-only contract all stand and were folded from it intact. **(b) the mechanism its edits act by —** V1 came closest, mandating a run the contract did not permit; that was a gap closed by declaring `extra_permitted`, not an invalid mechanism. **(c) a premise licensing its scope —** A1 showed the wrap-immunity premise is CONDITIONAL, and the condition is now stated; the scope premise (these 14, this mapping, `history` as a CEO-ruled legal value) is untouched. None of the three fires.
- Weak spots:          w1 4 folded — instruction 4 / record 0; w2 5 folded — instruction 5 / record 0 (2 fold-introduced; 1 found by the C17 sweep-at-opening, not by reading).
- Destruction:         w1 4 folded — instruction 4 / record 0 (+1 fold-introduced machine-contract break caught by `fold_check`, folded); w2 3 folded — instruction 3 / record 0 (1 fold-introduced; 1 conflict joint-resolved BEFORE folding).
- Vulnerabilities:     w1 6 folded — instruction 6 / record 0 (4 lens findings + 2 class-sweep sites).
  w2 2 folded — instruction 2 / record 0 (1 EXECUTED — the guards signal by stdout token, not exit code; 1 fold-introduced halt hazard from w1's V1).
- Integration-record:  w1 3 folded — instruction 3 / record 0 (1 is a clone-diff finding: the precedent's heading column would break the executable's locator); w2 3 folded — instruction 2 / record 1.
- ACID:                w1 3 folded — instruction 3 / record 0 (2 fold-introduced: this walk's own f5 and f6); w2 2 folded — instruction 2 / record 0 (1 fold-introduced; X4 joint-resolved).
**Conflicts:** X4 — basis-reconstructs (C16) vs the one-line TSV cell; joint-resolved w2 lens 5 by relocating the reconstruction to the findings document. X3 — bound-every-escape (C6) vs routing a missing guard to UNDECIDED; joint-resolved w2 lens 2 with a separate bound for the separate class, surfaced BEFORE the fold was written. X1 — no-halt-on-a-wrap-moved-quantity (C5) vs the plan's own unenforced-append finding (Q6c); joint-resolved w1b by making the immunity conditional. X2 — bound-every-escape (C6) vs the-Planner-must-receive-the-work (C16); joint-resolved w1b as deposit-then-flag. ⚠️ Both surfaced at lens 5, one lens late: the §2.8 ledger was not open, and the register records that as the measured cost.
**Cold panel:** computed tier does not require one. ⚠️ **Live question at the freeze, not a waiver:** the immediately preceding plan in this exact class — `diagnostic-503` — shipped a promotion set that was wrong in both directions and reached an executable, and its own identity check could not see it. Unlike `504`, this plan's premise came from the Planner's own reading rather than from a cold seat, so no cold reading has been spent on it. Decide at the freeze and record the reasoning either way.
**Conformance (§5):** first run at walk-1 close (shape-stable), at the DRAFT path — `plan_lint` exit **0**, 0 FAIL; `cycle_check` **CONTINUE**; `propagation_check` exit **1**, 14 divergences, ALL one class — the numeral `14` restated in prose where `Q` is the declared symbol. ⚠️ **Carried into walk 2 with its over-match band pre-classified, not swept blind:** at least two hits are the checker firing on a DIFFERENT quantity that shares the value — `D2` (`learned`) is also 14 — and one is a verbatim quotation of `executable-502`'s own row. ⚠️ **The collision is itself worth carrying: `learned` = 14 and bare = 14, so every unqualified "the 14" in this plan is ambiguous between two different sets.** Re-run at the deposit path before the copy-in.
**Close:** not reached — the cycle is open. ⚠️ The canonical `Closing:` field name is itself `cycle_check`'s closure token (`CLOSURE_RE`, `cycle_check.py:37`), so an open cycle that carries it as a placeholder escalates `claimed-close-unmet` on every intermediate run. Written in a form that cannot match until it is earned, per §3; restored to canonical form at the close.

## Cycle Manifest
tier: T1
target: knowledge/research/bare-entry-ruling-2026-08-23.md
class: read-only
reads: /Users/marklehn/Developer/GitHub/LESSONS.md, /Users/marklehn/Developer/GitHub/lessons-forge/lessons-forge.db, /Users/marklehn/Developer/GitHub/lessons-forge/knowledge/research/annotation-mapping-2026-08-22.tsv, /Users/marklehn/Developer/GitHub/lessons-forge/knowledge/research/promotion-corrected-2026-08-23.md, /Users/marklehn/Developer/GitHub/lessons-forge/knowledge/research/promotion-corrected-2026-08-23.tsv, /Users/marklehn/Developer/GitHub/lessons-forge/scripts/detect_learned.py, /Users/marklehn/Developer/GitHub/lessons-forge/src/lessons_forge.py, /Users/marklehn/Developer/GitHub/bellows/scripts/walk_register_lint.py, /Users/marklehn/Developer/GitHub/bellows/bellows_root.py, /Users/marklehn/Developer/GitHub/bellows/gates.py, /Users/marklehn/Developer/GitHub/READONLY_AUDIT_CONTRACT.md, /Users/marklehn/Developer/GitHub/PLANNER_TEMPLATE.md, /Users/marklehn/Developer/GitHub/DRAFTING_CYCLE.md
writes: knowledge/research/bare-entry-ruling-2026-08-23.md, knowledge/research/bare-entry-ruling-2026-08-23.tsv
open_forks: (1) the companion executable is BLOCKED on this plan's output; (2) detect_learned.py:245 emits `learned` unconditionally and cannot emit `history`; (3) no validator defines the legal `[status:]` value set; (4) nothing tells a wrap appender to add `[status: pending]`
walks: open
yields: open
validation: owed
coherence: owed — cycle open
N/A

## MUST-PRESERVE

- **READ-ONLY.** No edit to `LESSONS.md`, no write to any `.db`, no re-label, no new annotation.
- ⚠️ **Corpus reads via `file:/Users/marklehn/Developer/GitHub/lessons-forge/lessons-forge.db?immutable=1`** — `immutable=1`, NOT `mode=ro`: the corpus is WAL, and `mode=ro` fails against a copy with no `-shm` sidecar and touches the live `-shm` mtime. **21 stale `pre-*.db` snapshots sit beside it answering fluently and wrong, and the siblings `forge.db` / `lessons.db` are 0-byte decoys that return FALSE ABSENCES.** Assert `lesson_entries` equals `D10` before trusting any read. ⚠️⚠️ **THIS DIFFERS FROM THE CONTRACT'S `C2` ROW, WHICH NAMES `?mode=ro`, AND THE DIFFERENCE IS DELIBERATE — the contract itself says a differing rule is stated inline in the plan rather than edited into the contract, so this is that statement.** Fill `C2` as *satisfied by the stricter form* and paste every invocation as the row requires; do NOT tick it by switching to `mode=ro`, and do not report it FAILED. ⚠️ **The plan, not the contract, governs here** — `mode=ro` on this WAL corpus fails against a sidecar-less copy and touches the live `-shm` mtime, which is a WRITE to the store the row exists to protect.
- ⚠️ **`LESSONS.md` is in the governance ROOT repo** — `/Users/marklehn/Developer/GitHub/LESSONS.md` — not in lessons-forge, and not in your worktree.
- ⚠️⚠️ **THE CORPUS IS UNTRACKED, SO YOUR WORKTREE CONTAINS NO COPY OF IT — and the failure mode is CREATION, not an error.** Measured: `git ls-files --error-unmatch lessons-forge.db` does not match, against a tracked positive control that does. A relative `sqlite3 lessons-forge.db` from your cwd therefore CREATES an empty database, answers `0` with exit 0, and looks exactly like a true count. **The `D10` identity assert is what catches this, and it only catches it if you run it against the SAME absolute path every later read uses.**
- ⚠️ **The mapping TSV is QUOTED CSV inside a TSV, not raw TSV.** Headings containing `"` are wrapped in quotes with the inner quotes doubled (measured at exec-502; a naive split-on-tab parse aborts the run). Three of the 14 bare rows are in that form. Parse accordingly and prove your parse recovers all `D7` rows.
- ⚠️ **A bare heading has NO marker to strip** — that is what makes it bare. Do not reach for the `_key_heading` normalizer to locate one; whole-line equality against `"## " + original_heading` is the locator, with an asserted occurrence count of exactly 1.
- **`grep` here is ugrep: `-F` for every literal search.** ⚠️ A zero-match `grep -c` prints `0` and EXITS 1 — read the printed count, never the exit status. ⚠️ A `-E` pattern with a nested quantifier over this corpus can exceed the shim's complexity limit and ERROR rather than return empty; if that happens, narrow the pattern — do not read the error as an absence.
- ⚠️ **Every negative probe in this plan needs a positive control.** Absence looks identical whether the thing, the pattern, the path or the unit is wrong, and this plan asks repeatedly whether a mechanism exists. State the control alongside each absence claim.
- ⚠️⚠️ **REGISTER FREEZE — a stated consequence of a lesson written THIS session, about THIS quantity.** `LESSONS.md:4629` reads *A session wrap cannot append to an artifact a dispatched plan is mid-flight on — the append is lost, or it moves a count that plan's QA asserts* `[status: pending]`. **The count it names is the bare-heading count, which is this plan's entire subject.** So: no append to `LESSONS.md` while this plan or its companion executable is in flight, mirroring `executable-505`'s corpus-freeze guard. ⚠️ This diagnostic is single-step and read-only, so it cannot be damaged the way the DEV/QA case was — **the freeze is stated for the COMPANION, which can be, and because the entry is `pending`: nothing enforces it, so the plan must carry it.**
- ⚠️ **Invoke any checker by ABSOLUTE path** from `/Users/marklehn/Developer/GitHub/bellows/scripts/`; shell state does not persist between invocations, and a relative call after a `cd` reports `can't open file` and exits non-zero, which is easily misread as a check failing.

## STEP 1 — READ-ONLY DIAGNOSTIC

**Role:** DEV (read-only audit). Contract: `/Users/marklehn/Developer/GitHub/READONLY_AUDIT_CONTRACT.md`.

**Contract parameters.** `cwd`: your bellows worktree under `lessons-forge/.bellows-worktrees/<id>/`. `deposit_paths`: the two files in Deposits. `extra_forbidden`: any write to `LESSONS.md`, to any `.db`, or to any real `knowledge/decisions/` directory. ⚠️ **`extra_permitted`, stated because Q4 MANDATES running two guards and a guard cannot be run without inputs:** you MAY create fixture files under `./scratch-506/` in your worktree — a violating input and a clean control per guard — and you MUST report them under `C4` as writes that are NOT deposits. **Nothing else is writable, `./scratch-506/` is never committed, and the fixtures are never placed in `governance/knowledge/research/` where a real register lives.** ⚠️⚠️ **DECLARE `?? scratch-506/` AS EXPECTED IN `C7` — the contract's `C7` is HALT-on-FAILED and compares `git status --porcelain` before and after, so the untracked fixture directory this clause permits would otherwise read as the mutation `C7` exists to stop.** It is named here so it is pre-existing-dirt-the-plan-names rather than an undeclared appearance; nothing else new may show in the after-snapshot. ⚠️ Without this clause the plan would mandate an action its own contract forbids, and the agent would be forced to choose between them silently. `extra_preflight`: assert the corpus identity (path, byte size, `lesson_entries` equal to `D10`). ⚠️ **`D11` is RECORDED AND COMPARED, never asserted as a halt condition** — `LESSONS.md` legitimately gains headings at every session wrap, and a plan that HALTS on its own register's sha is the false-HALT `executable-451` measured and `executable-502` warned about. Report the sha you observe beside `D11`; if it differs, re-derive `D1`–`D8` and proceed on YOUR figures. **What you DO assert is set-level and wrap-immune: that the `Q` bare headings are the same 14 the mapping quarantines** — an appended wrap entry moves `D1` and `D4` legitimately and moves the bare set not at all. ⚠️⚠️ **That immunity is CONDITIONAL and the condition is the very gap this plan reports at Q6(c): it holds only while an appender writes `[status:]` on the new heading, and nothing enforces that.** A BARE append grows `Q` to 15, and the 15th is in no mapping and cannot be graded from this plan's inputs. **Disposition: do not silently regrade and do not drop it — report the new heading verbatim under `### Flags for CEO` and grade the 14 you were given.** This is also why the register-freeze clause in MUST-PRESERVE exists. ⚠️ **`C7` REPO SET, pinned explicitly because the contract HALTs on an unnamed set:** `/Users/marklehn/Developer/GitHub` (root), `/Users/marklehn/Developer/GitHub/lessons-forge`, `/Users/marklehn/Developer/GitHub/bellows`, `/Users/marklehn/Developer/GitHub/forge`.

**Q1 — Reproduce the quarantine.** Re-derive `Q` and the three sub-classes from the mapping, and list the ids in each. ⚠️⚠️ **THEN RE-DERIVE THE THREE WALK-0 DISSOLUTIONS THEMSELVES — they are this plan's SHAPE, they were measured by the Planner, and a downstream executable inherits them without re-checking.** (i) `target_layer='none'` returns exactly the 5 ids stated, corpus-wide; (ii) each of `93, 116, 123` carries exactly 2 proposals and **no member of any pair is live** — report each proposal's `status` literally; (iii) `330` and `331` are the ONLY bare entries with a non-empty `target_artifact`, and it is a `.py` file. ⚠️ **Any divergence is a REPORTED FINDING that changes the plan's shape, not a number to correct in passing** — say which pin, which direction, and what you measured. A plan whose premises are handed down unverified is the failure `diagnostic-503` shipped. ⚠️ **Prove the two 14s are the SAME 14** — the bare headings in the file and the `unknown` rows in the mapping — by comparing id/heading SETS, not counts; two disjoint sets of 14 would satisfy every count check in this plan. Assert `D2`+`D3`+`D4` = `D5` and `D6` = `D2`+`D3`, and state that no bare entry carries a `[target:]` marker. ⚠️ **Disposition if the two sets are NOT equal: HALT and report both sets with their symmetric difference — do not proceed on the intersection and do not adjust `Q`.** A mismatch means `LESSONS.md` moved in a way the mapping cannot describe, which is a finding the companion executable must not be handed silently.

**Q2 — RULE THREE: is this entry a RECORD or a RULE?** The CEO's discriminator: a RECORD accounts for what happened; a RULE states something a future actor must do or not do, such that a violation is identifiable. ⚠️ **Grade all 14, not only the 5 the corpus flags** — the question is whether the corpus's `none` set is COMPLETE or merely the part someone got to, and that is answerable only by grading the other 9 against the same discriminator. ⚠️ **Read the BODY, not the heading** — a heading is a one-line summary and several of these bodies end in an explicit "**The discipline rule:**" paragraph that the heading does not carry. ⚠️ **Flag the near cases in both directions**: an entry whose body states a rule that has since become unbuildable because the workflow it governs no longer exists is a genuinely close call, and so is a record that happens to contain one imperative sentence. State the reasoning per entry; do not emit a bare verdict. ⚠️ **UNDECIDED is a legal third answer and you are expected to use it rather than force a binary** — 504's own instruction on the equivalent question reads *mark it undecided rather than assuming*. An UNDECIDED entry takes `unknown` per the derivation table, which is a surfaced state, not a silent one. ⚠️ **UNDECIDED is CAPPED, because an unbounded escape satisfies this plan's form while dodging its purpose:** each one must name the specific evidence that would settle it AND why that evidence is not obtainable from here — a reason that dissolves into one command is not a reason (§2.7's INHERITED-marker cost discipline) — and **more than three UNDECIDED across the whole set means the grading did not succeed, and the right instrument is the flag, not a halt: deposit the TSV and the findings anyway, then raise it under `### Flags for CEO`.** ⚠️ Stated as the flag rather than a halt deliberately — verified at `bellows/gates.py`, `_gate_ceo_flags` turns any non-null flag into a blocking gate failure, so the Planner gets the pause WITH the work in hand, whereas a halt skips the closing commit and destroys the partial grading the Planner most needs to see.

**Q3 — Assign a target for every RULE entry.** Nine of the 14 have a NULL `target_artifact`, which is why the detector never graded them. For each entry graded RULE, name the artifact its rule belongs in, using the Fork-2 discriminator already decided: **DEFINITION → `glossary.md`, RUNBOOK → `CLAUDE.md`, TRAP → CODE.** ⚠️ **Then answer the SECOND half, which is what actually separates `codified` from `pending`: does that artifact ALREADY CONTAIN the rule?** Naming a target does not settle the status — see the derivation table — and this half is an absence claim carrying the full §2.7 discipline (probe derived from the entry's own text, positive control beside every zero). ⚠️ **"No artifact exists yet" is a legal answer and you must be willing to give it** — it means the entry is `pending` with a named build item, not that a target must be invented to fill the column. Name the build item when you give it.

**Q4 — Grade each RULE entry against 504's two rules.** Rule One: a mechanism that leaves a material part of the lesson's scope uncovered is `codified`, not completion. Rule Two: a mechanism cannot enforce a lesson about that mechanism's own insufficiency — ask whether the mechanism REJECTS A VIOLATION of the lesson or is merely the lesson's SUBJECT. ⚠️ **Read `promotion-corrected-2026-08-23.md` §Q2–Q3 for both rules as stated, and cite the discriminator you applied; do not reconstruct them from this plan's summary.** ⚠️ **Three entries need a mechanism DEMONSTRATED, not assumed, and the verdict turns on it:**
- **`330` and `331`** name guards in `walk_register_lint.py` (`duplicate_row`, `headerless_rows`). Confirm each exists, then RUN it — a violating input must be rejected AND a clean control must pass. A guard that fires on everything proves nothing. ⚠️⚠️ **READ THE STDOUT TOKEN, NOT THE EXIT CODE — measured at authoring by running all three cases: a clean register, a `duplicate_row` violation and a `headerless_rows` violation ALL EXIT 0.** The discriminator is the second field of the first stdout line: `CONFORMANT` versus `UNCONFORMANT` (the headerless case additionally reports `shapes: (none)`). **An agent that verifies these runs by exit status will record every fixture as passing, including the violations, and will report a guard as working on evidence that shows nothing.** ⚠️ **Both guard names were verified present at authoring — `duplicate_row` and `headerless_rows`, `walk_register_lint.py` v0.3, against a positive control for a name that should not exist. If either is absent or renamed when you look, that is itself a REPORTED FINDING and the affected verdict becomes UNDECIDED — do not substitute a guard that looks similar.** ⚠️ **A guard-absence UNDECIDED is a SEPARATE CLASS from a judgement UNDECIDED and is bounded separately: at most 2 (there are two guards), reported under its own heading, and it does NOT consume the cap of three that bounds hard-judgement UNDECIDEDs.** Both classes stay bounded — a tooling accident must not spend a budget meant for entries that are genuinely hard to grade, and it must not go unbounded either. ⚠️ **Running it does NOT trip the contract's ban on importing anything under `bellows/`, and you are not to refuse on that ground:** measured, `walk_register_lint.py` imports `re`, `sys` and `pathlib` only — no bellows module, no DB connection. The ban targets `python3 -c`/`-m` imports that reach a read-write handle; a stdlib-only script run as a subprocess is not that. Then apply Rule One: each lesson's "How to apply" carries a second clause the guard does not cover. ⚠️ **AND APPLY RULE TWO TO THEM AS WELL — the derivation table requires BOTH rules and this instruction named only one.** Ask it explicitly: is `walk_register_lint`'s guard the lesson's SUBJECT, or does it REJECT a violation of the lesson? Answer it rather than assuming; a guard built in response to a defect is usually an enforcer and not a subject, but that is the finding to make, not the premise to start from.
- **`122`** names a discipline (resolve roots by marker walk-up). `bellows/bellows_root.py` implements it and no `__file__`-relative root constant survives in `bellows/*.py`. ⚠️ **That is the FIX, and the fix is not the enforcement** — the discriminating question is whether anything REJECTS a newly-authored `__file__`-relative root. Look for it, state the control for your search, and if nothing rejects it, the verdict is `codified` with the guard as a named build item.

**Q5 — Emit the mapping.** Deposit `knowledge/research/bare-entry-ruling-2026-08-23.tsv` with columns `entry_id`, `heading_line_no`, `entry_heading`, `class`, `target_artifact`, `mechanism`, `mechanism_file`, `rule1_partly`, `rule2_circular`, `verdict`, `basis`. ⚠️⚠️ **`entry_heading` IS THE VERBATIM HEADING LINE MINUS THE LEADING `## ` — every tag marker, both spaces, the date prefix, byte for byte.** ⚠️ **The clone precedent points the WRONG WAY here and following it would break the executable:** `promotion-corrected-2026-08-23.tsv` carries a SHORTENED heading (date prefix and tags stripped), which is fine for a reading artifact and useless as a locator — and the companion executable's only sanctioned locator is whole-line equality against `"## " + entry_heading`. A shortened value silently matches nothing. ⚠️ **Quote by the same convention the 501 mapping uses** — a field containing `"` is wrapped in quotes with inner quotes doubled — **and state the convention in the findings document**, because three of the 14 carry that form and a companion parser built for raw TSV aborts on them. One row per (entry, mechanism) pair; an entry with no mechanism gets one row with the mechanism columns empty. `class` is `RECORD`, `RULE` or `UNDECIDED`; `verdict` is `HISTORY`, `LEARNED`, `CODIFIED`, `PENDING` or `UNKNOWN`, and it is DERIVED from the table above rather than chosen — state which row of that table each entry took. ⚠️ **The reconstruction is REQUIRED and a TSV cell CANNOT HOLD IT — this is a stated split, not an oversight.** A3 asks `basis` for the literal probe, its result and a positive control; the hit-classification rule above adds an operative-vs-mentioned verdict per hit. That is multi-line evidence, and a TSV row is one line. **So: the full reconstruction lives in the findings document under a per-entry heading — probe, raw result, positive control, and each hit classified — and `basis` carries the one-line DECIDING fact plus the heading it points at.** Neither artifact is complete alone, and the findings document says so. A later author must be able to re-run your reasoning from the deposited artifacts alone — the corpus, `LESSONS.md` and the two deposits — without this plan's conversation. A `basis` reading *"not found in PLANNER_TEMPLATE.md"* is exactly the unreconstructable form this clause forbids. ⚠️ **Rule Two is a property of the PAIR** — an entry is graded on its best-passing mechanism, so state the per-entry verdict and the distinct-entry count separately from the row count. ⚠️ **STATE THE SET'S AUTHORITY:** the executable may apply these verdicts mechanically and may apply NOTHING else — no inference from a neighbouring row, no reconciliation against the 501 mapping, no arithmetic that recovers a different set. ⚠️ **Then reconcile the post-application state**: `learned` + `codified` + `pending` + `history` + `unknown` must equal `D1`, and bare must be 0. **Assert the arithmetic from your own re-derived figures; do not transcribe a numeral from this plan.**

**Q6 — What must change so `bare` cannot recur?** Four known gaps. Size each, say where it belongs (`detect_learned.py`, a checker, the diagnostic template, or doctrine), and state what it would cost:
(a) `scripts/detect_learned.py:245` emits `learned` unconditionally — wrong under the SESSION 59 ruling, and with no way to emit `history` at all. ⚠️ **The adjacent question is already ANSWERED and you should not re-litigate it: a new status VALUE round-trips through ingest safely.** `_STATUS_TARGET_MARKER_RE` (`src/lessons_forge.py:52`) keys on the marker NAME and consumes any value, so `_key_heading` maps `[status: history]`, `[status: unknown]` and `[status: pending]` to one identical key — verified by EXECUTING the real matcher over all four forms plus a marker-free positive control, not by reading it. The exec-499/500 re-insertion class does not fire on this change;
(b) nothing anywhere defines the legal `[status:]` value set, so neither a bare heading nor a misspelled value is catchable — ⚠️ **and the deeper half, measured: the values are not DEFINED anywhere durable either.** `LESSONS.md` carries 225 `codified` labels and zero statements of what `codified` MEANS; the phrase *written into its target, unenforced* appears in `shop_next_session.md` and two closed plans and in **no** governing artifact — not `PLANNER_TEMPLATE.md`, not `DRAFTING_CYCLE.md`, not `COMPANY.md`, not `ARCHITECTURE.md`, not `lessons-forge/CLAUDE.md` (probed with a positive control that located the phrase where it does live). A register annotated with a vocabulary that lives only in a session baton decays the moment the baton rolls;
(c) nothing tells a wrap appender to add `[status: pending]`, so every append is invisible to the build queue — SESSION 59's three appends carried it by hand;
(d) the detector scores a prose-term ratio against a target that may be CODE, where the score means nothing — the mechanism that quarantined `330` and `331`.
⚠️ **Say which single one of the four would have prevented the largest share of this plan's work**, and argue it.

**Findings document:** Q1–Q6, each answered with command output or `file:line`. Close with `## What could not be measured`, `## Open forks`, and `## Recommended executables`.

**Scope:**
- `/Users/marklehn/Developer/GitHub/lessons-forge/knowledge/research/bare-entry-ruling-2026-08-23.md`
- `/Users/marklehn/Developer/GitHub/lessons-forge/knowledge/research/bare-entry-ruling-2026-08-23.tsv`

**Deposits:**
- `/Users/marklehn/Developer/GitHub/lessons-forge/knowledge/research/bare-entry-ruling-2026-08-23.md`
- `/Users/marklehn/Developer/GitHub/lessons-forge/knowledge/research/bare-entry-ruling-2026-08-23.tsv`

**Commit:** ⚠️ **WORKTREE DISCIPLINE.** You are dispatched into `lessons-forge/.bellows-worktrees/<id>/`. Write both deposits at the SAME relative paths under YOUR cwd and commit them there in ONE commit: `git -C <your-worktree> add knowledge/research/<both> && git -C <your-worktree> commit -m "..."`. ⚠️ Do NOT write to the main checkout — `gates._resolve_deposit_path` falls back to "path as-is" and `_check_deposit_uncommitted` swallows the out-of-worktree git error, so writing to the wrong checkout passes both gates SILENTLY and the teardown-merge never picks your files up. Absolute for everything you READ, relative-to-cwd for everything you WRITE.
