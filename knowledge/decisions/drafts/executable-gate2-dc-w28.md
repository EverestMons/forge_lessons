# forge_lessons — executable: Gate-2 codification — the DC tranche of forge cycle W=28 (proposals 426, 427, 428, 429, 432, 439, 440, 441) onto DRAFTING_CYCLE v2.23, then the eight flip `implemented`

**Date:** 2026-09-01 | **Project:** forge_lessons | **Tier:** Small | **Dispatch Mode:** bellows | **cycle_tier:** T2 | **Test Scope:** targeted (no code changes — doctrine prose by a committed builder + a scoped DB flip; the forge suite runs into a `full-suite-*.txt` because this is the DB's home project and the gate reads a real summary line, never a pre-declared override) | **Execution:** Step 1 (DEV) → Step 2 (QA) | **qa_steps:** 2 | **pause_for_verdict:** always | **known_failures:** 0 | **Priority:** 1

**auto_close:** false

**Slug:** `gate2-dc-w28-2026-09-01`

**Depends on:** the CEO's "Thread 75 gate 2 — run it" (2026-09-01, session `1663ee38` — **this sentence is the citable record of the Gate-2 authorization**); forge_lessons plan `100007` (Done 2026-09-01 — cycle W=28) and the two Gate-1 acts that routed this batch: the cold non-author packet `/Users/marklehn/Developer/eluvian-governance/gate1-packet-2026-09-01.md` (governance `10714c6`; applied on the mini as `knowledge/development/g1w28-nonauthor-*.sql`, forge `ee0c0c6`) and the CEO's four author-conflict rulings (`knowledge/development/g1w28-ceo-*.sql`, forge `3232724`) — PROVENANCE; `/Users/marklehn/Developer/eluvian-governance/DRAFTING_CYCLE.md` (target — A1-pinned below); `/Users/marklehn/Developer/eluvian-governance/governance/knowledge/decisions/drafts/build-gate2-dc-w28-2026-09-01.py` (the edit mechanism — eleven anchored edits, additive by construction, four hard-raise output guards, dry-run proven scratch→scratch and under `-O`; **freeze item 0 re-verifies the on-disk builder equals its committed blob at deposit** — a plan-text hash goes stale at every builder fold). **Clone origin AND newest same-class (measured, M1 of the register): `executable-538`** (gate2-dc-route57, Done 2026-08-25, `/Users/marklehn/Developer/eluvian-governance/governance/knowledge/decisions/Done/executable-538.md`), its full-form panel's every fold carried deliberately (the register's ARTEFACTS pass counts each). **Dispatch-topology precedent:** `bellows/knowledge/decisions/halted-executable-100006.md` — the mini's cross-repo shape (a worktree plan editing the governance root by absolute path, two repositories, two commits); its halt was a deposit-declaration defect, not the shape. Walk register: `/Users/marklehn/Developer/eluvian-governance/governance/knowledge/research/walk-register-gate2-dc-w28-2026-09-01.md`.

⚠️ **ID NOTE:** the deposit filename carries no id; watchers and verdicts key on the SLUG (`id_sequence` read **100008** at authoring — a prediction, never minted here).

---

## Why this exists

Eight DRAFTING_CYCLE-targeted proposals stand `accepted|codify` from **TWO Gate-1 acts of 2026-09-01** — never conflate them (538's SC-1): **426, 427, 428, 429, 432** routed by the cold non-author packet (the shop's session `352aa9ee`/`github-e0` per the packet's own header — NOT the Remote Control relay; scout SC-1 — `status_updated_by='planner'`, stamp `2026-09-01T22:03:28Z`), **439, 440, 441** RULED by the CEO on the mini Planner's coverage table (forge `3232724` at 16:58:43 local, BEFORE the packet's `10714c6` at 17:02:02 — the packet's table 5 independently reached the same dispositions; scout SC-2; `status_updated_by='ceo'`, stamp `2026-09-01T21:58:31Z`). The other twelve `accepted` rows all target `PLANNER_TEMPLATE.md` and are **thread 76** — named here and in E11 so the remainder is recorded, not silent. This is the first Gate-2 codification authored and dispatched on the mini, the DB's home since 2026-09-01. Eleven edits, one file, all by the committed builder:

| edit | what |
|---|---|
| E1 | **§2.0 gains the walk-0 CONSUMER DRY-RUN** (426, walk-0 half): run the class assigner and the deposit-reading gates' extractor against what the plan DECLARES — the ROUTED pair (local scout SC-4 trimmed a wider first draft that also mandated `plan_lint`/`cycle_check`/the manifest parser, which keep §5's timing); route a question by whether a COMMAND can answer it — a read of a consumer's source is a hypothesis; the entry's bounding bullet carried. |
| E2 | **§2.0 clone-diff gains TWO further diffs** (427): the clone against the project's STANDING RULES, and against the parent's WALK REGISTER — the two classes a parent's final text is silent about. |
| E3 | **§2.7 execute-against-real-data bullet gains the read/ran METHOD LABEL** (426, label half). |
| E4 | **§2.7 subtractive-trim bullet gains the general VIOLATE-THE-RULE form** (428): one violation test per ledger row; CONJUNCTION and PROXY shapes; the UNCARRIED label. |
| E5 | **§1 gains the split-on-TIER paragraph** after the tier lines (429): a Planner act on the plan set; explicitly NOT a change to any trigger or tier — nothing in §4 moves. |
| E6 | **§2.7 walk cadence gains the SAMPLING bullet** (432): one class on consecutive passes → enumerate against PRODUCERS; run each consumer's own parser; scoped against the SWEEP pass. |
| E7 | **§2.7 edit mechanics gains the derived-enumeration bullet** (439): derive a guard's constraint set from the artifact in the same run; aim the mutant at the blind spot. |
| E8 | **§2.7 edit mechanics gains the referential-DISTANCE bullet** (440): per-pair distance for any reorder — the positional class the 2.22 row invited back through the corpus, answering its coverage citation. |
| E9 | **§2.7 mechanical-enumeration bullet gains the pool-is-the-POPULATION clause** (441): measure and state the selector's coverage; re-run the pool after editing the records the filter hid. |
| E10/E11 | **Version 2.22 → 2.23** (line 5 — a single edit, the ONE intended replacement) and the newest-first History row carrying the DB-flip statement, the §6 no-gate-edit declaration, the thread-76 remainder, and the provenance as the record states it (scout SC-1/SC-2: the packet's own session id; the CEO's ruling on the mini Planner's table, before the packet). |

**Dedup posture (measured at authoring, M3 of the register — every headline token 0 in live v2.22; the coverage gaps are the Gate-1 packet's own rows, re-read against the live text):** 426's walk-0 half — §2.0's five measurements are READS and name no consumer; the label exists nowhere in PT or DC (`verified from source` 0 in both) — its panel-seat FORM does exist as PST v1.2's PROPOSED FIX field (local scout SC-9), which E3 now cites as the instance it generalises. 427 — §2.0's three passes diff the clone against the PARENT only; `CLAUDE.md` appears nowhere in DC. 428 — the count-is-not-a-value clause is the ONE instance; the general form, conjunction and proxy are absent. 429 — §1 permits only self-escalation and prices no split. 432 — the SWEEP pass drains restatement; nothing tracks a class ACROSS passes or enumerates pins against EXTERNAL producers. 439/440/441 — the CEO's table-5 rulings; 440 is the fresh proposal the 2.22 row required (it answers `fold_check`'s coverage citation in its own text). ⚠️ **§6 coordinate-doctrine-and-gate, stated not deferred:** E5 touches §1, so the pairing question is owed — the answer is **no gate edit**: no trigger is added, no tier's demand changes, the `cycle_tier` declaration and `plan_lint`'s `^T([012])\b` check are untouched; QA Item 4 sweeps every new token against `plan_lint.py` and `gates.py` with a positive control.

**⚠️ Sentinel exposure (re-measured per id at authoring):** stamps are **MIXED — 426/427/428/429/432 = `planner|2026-09-01T22:03:28Z`; 439/440/441 = `ceo|2026-09-01T21:58:31Z`** — the stamp exclusion is a **TWO-value guard** (`NOT IN ('2026-09-01T22:03:28Z','2026-09-01T21:58:31Z')`); never clone a one-value shape onto a mixed batch. Categories uniform `governance_rule` (8/8, measured). **This flip does NOT drain the queue: ACC 20 → ACC_POST 12 = ONE cohort, the PT twelve (thread 76). 12 is the PASS value; 0 is a WRONG-WRITE alarm.**

**Numbers discipline — ⚠️ this table is the ONLY declaration site; every value MEASURED 2026-09-01 on the mini; the agent re-measures each and HALTS on mismatch with both values quoted.**

| id | pin | value | probe |
|---|---|---|---|
| P1 | `/Users/marklehn/Developer/eluvian-governance/DRAFTING_CYCLE.md` sha, pre-edit | `920e5038a55f16e611988abb74f94184e1d78912446d362b03b9c1ac3927436a` | `shasum -a 256 /Users/marklehn/Developer/eluvian-governance/DRAFTING_CYCLE.md` |
| P2 | version line, pre-edit | `**Version:** 2.22 (2026-09-01). Amended only through the Iteration Protocol (§6).` at line 5 | `sed -n 5p` |
| P3 | **`EDITS`** — builder stdout, first token | begins `OK — **11** edits` | the dry-run |
| P4 | **`NUMSTAT_ADDED`** / `NUMSTAT_REMOVED` — numstat scratch-in vs scratch-out | **12** added / 6 removed | `git diff --no-index --numstat` (exit 1 EXPECTED — read the numbers) |
| P5 | **`BYTES_OUT`** — byte counts, pre → post | 148477 → **164175** | `wc -c` (⚠️ file BYTES — the builder prints bytes too, but `wc -c` is the instrument) |
| P6 | **`LINES_OUT`** — line counts pre → post; `BULLETS_OUT` bullet lines pre → post | 363 → **369** / 168 → 172 | `wc -l` / `awk '/^- /'` piped to `wc -l` (a `grep -c` on line heads is NOT the instrument for bullets — the builder asserts the count; QA re-derives with awk) |
| P7 | builder output sha | `38e8978e7965c62cba6ea932c2838c684449bc81a45c1487dbc23fea2323ad5f` | `shasum -a 256 <scratch>/DC-out.md` — the live file MUST equal it after the copy |
| P8 | **`IMPL_PRE`** — DB pre-state: `PRE_F` 8 · `ACC` 20 · IMPL_PRE · `MAXID` 441 · `ENTRIES` 433 (measured 2026-09-01) | IMPL_PRE = **314** | E-a2 |
| P9 | **`IMPL_POST`** — flip sentinels: `CHANGES_F` 8 · `EXCL_F` 8 · `ACC_POST` 12 · IMPL_POST · `CAPTURE_LINES` 433 | IMPL_POST = **322** | E-b / E-c |
| P10 | `BKTOT` — backup: `integrity_check` `ok` · BKTOT = `MAXID` (441 — contiguous ids, measured 2026-09-01) · `BK` 8 | BKTOT = MAXID | E-a |
| P11 | **`SUITE_PASSED`** — the forge suite at forge_lessons `ee0c0c6` under `/Users/marklehn/Developer/bellows/.venv/bin/python` | **80** passed | QA Item 8 |

**HALT ROUTING:** Step 1 reads this plan, the builder, `DRAFTING_CYCLE.md`, the canonical DB; Step 2 reads Step 1's commits (BOTH repositories), the live doc, the DB read-only, `/Users/marklehn/Developer/eluvian-governance/RULE_20_SELF_CHECK_BLOCK.md`. Missing input → HALT naming it.

**Environment facts (verbatim, load-bearing):** `grep` is a ugrep shim — `/usr/bin/grep -F` for every literal probe; a zero-match `grep -c` prints `0` and EXITS 1 (read the count, never the exit code; never `&&`-chain zero-count probes); `git diff --no-index` exits 1 on the EXPECTED differing state; `--` before dash-leading literals; shell state does not persist between invocations — cd-first in every compound; `python3` on this machine is 3.9 with NO pytest and forge_lessons has NO venv — the suite interpreter is pinned (P11).

⚠️⚠️ **TWO REPOSITORIES, TWO COMMITS, ONE STEP.** This plan dispatches in a **forge_lessons worktree**. Its target is a **different repository** (`eluvian-governance`) that has no worktree — the doctrine edit lands IN PLACE at the governance root by ABSOLUTE path and commits THERE (its own commit, one file); every other deposit is WORKTREE-RELATIVE and commits in forge_lessons (PT Dispatch Path Rules: reads of shared state absolute, writes of the step's own deposits relative — ⚠️ the doctrine write is a DECLARED EXCEPTION to the write half, not an instance of it: the target is a different repository with no worktree, so the rule's failure shape — a main-tree write the worktree's commit cannot see — does not arise, because the file commits in ITS OWN repo; the 100006 precedent, scout SC-10). ⛔ **Worktree isolation does not cover the doctrine edit** — a halt after Task B leaves the governance root DIRTY and nothing cleans it; the recovery command is named at Task C and runs ONLY on the FRESH arm.

---

## Ledger

- **C1 — the builder is the only editor; scratch→scratch first; FOUR hard-raise output guards** (realpath vs live, samefile vs live, casefolded governance-root prefix, out≠in — every one proven firing at walk 0, re-proven under `-O`; never a bare `assert`) **and ADDITIVE BY CONSTRUCTION** — every replacement carries its anchor intact and every anchor line survives verbatim or as the prefix of its rewrite, the version line the ONE identity-scoped exception; two deletion mutants and one version mutant refuse (register f3/f4). ⚠️ **Content ADDED inside a replacement passes every additivity guard** (measured by the local scout at the walk-0 builder and re-run as M7 of the captured battery `/Users/marklehn/Developer/eluvian-governance/governance/knowledge/research/mutants-gate2-dc-w28-2026-09-01.txt`: an extra sentence inside E3 → `OK — 11 edits` (P3, measured), exit 0, an output that differs from P7) — **P7 and freeze item 0 are that class's ONLY carriers**: the reviewed output sha and the reviewed builder blob, both re-verified before the copy lands. *(observer: QA Item 2 byte-identity; Task B's sha = P7 HALT)*
- **C2 — the A1 pin gates the FRESH run** (per-pin line form; ⚠️ scoped to FRESH by construction — on either RE-ENTRY arm the edit has landed and the pin MUST mismatch; reading that as a HALT would orphan both recovery arms):
  - A1, derived at authoring by running the command:
    `shasum -a 256 /Users/marklehn/Developer/eluvian-governance/DRAFTING_CYCLE.md` → P1
- **C3 — post-conditions proven from the LIVE file after apply, and the live file's sha equals P7.** *(observer: QA Item 1)*
- **C4 — commits pinned in BOTH repositories; CAPTURE_COMMIT (governance) and the worktree commits recorded with numstat.** *(observer: QA Item 3)*
- **C5 — one action per ops compound; no amend anywhere.** *(observer: QA Item 3's reflog read)*
- **C6 — doctrine before DB:** the flip runs only after the governance commit AND the worktree dev-note commit; half-states completed by A0's arms. *(observer: A0)*
- **C7 — the flip is scoped CAS:** `id IN (426,427,428,429,432,439,440,441) AND status='accepted' AND route='codify'` (the write's predicate EQUALS the pre-flight guard's); PRE_F is STRUCTURAL in-txn (`g_pre` TEMP-TABLE CHECK aborts under `-bail` BEFORE the UPDATE); ⚠️ EXCL_F is a STATE predicate, CHANGES_F the only this-run-wrote sentinel; sentinels BY NAME, each the P8–P10 value measured 2026-09-01 — **PRE_F=8** (E-a2 AND in-txn), **BK=8**, **BKTOT=441**, **CHANGES_F=8**, **EXCL_F=8** (two-value stamp exclusion), **ACC_POST=12** (⚠️ the queue does NOT drain — 12 is the pass value, 0 is a WRONG-WRITE alarm), **IMPL_POST=322**, E-a2's **ACC=20** and **MAXID=441** — NINE, counted (plus Step 2's informational **NEWROWS**); capture inside the txn `id <= 441 AND id NOT IN (the eight)`, SIX columns → **433 rows**. *(observer: QA Item 5)*
- **C8 — serialized dispatch:** verified at the freeze — no other governance-root-writing or forge-writing plan in flight at deposit (100007 closed; `halted-100006` is halted, not in flight, and the depositor's in-flight scan skips it — stated so nobody reads that as a collision hold).

---

## How to Run This Plan

**Bootstrap prompt:**
```
Read the plan at knowledge/decisions/in-progress-executable-<id>.md (the daemon renames the deposited placeholder on claim). Execute Step 1 ONLY. After completing Step 1, STOP and wait for my confirmation.
```

---

## Scope

- `/Users/marklehn/Developer/eluvian-governance/DRAFTING_CYCLE.md`
- `knowledge/development/dev-log-gate2-dc-w28-2026-09-01.md`
- `knowledge/development/g2dcw28-flip.sql`
- `knowledge/qa/evidence/gate2-dc-w28-2026-09-01/flip-capture.txt`
- `knowledge/qa/evidence/gate2-dc-w28-2026-09-01/qa-receipt.md`
- `knowledge/qa/evidence/gate2-dc-w28-2026-09-01/probes-raw.txt`
- `knowledge/qa/evidence/gate2-dc-w28-2026-09-01/full-suite-gate2-dc-w28.txt`

*(The `pre-g2dcw28-*.db` backup lands at the forge_lessons ROOT — `/Users/marklehn/Developer/forge_lessons/`, beside the live DB, gitignored — untracked, named in the dev note.)*

---

## STEP 1 — DEV (apply the eleven edits at the governance root, then the flip)

> **FIRST — post a short visible chat message (1–2 sentences).** Do NOT rename this file. You are the Forge Developer.
>
> ⚠️⚠️ **EXECUTE STEP 1 ONLY — Step 1 ends at Task E-c's STOP, not before.** ⚠️ **Resolve both roots FIRST, in one compound, and state both on their own lines in the dev log:** `cd "$(git rev-parse --show-toplevel)" && [ -d src ] && [ -d knowledge/decisions ] && echo TREE_OK && pwd` — HALT unless TREE_OK (your cwd IS the claimed forge_lessons worktree; never `cd` to the main checkout; ⚠️ the probe tests DIRECTORIES on purpose — `scope_check` authorises any changed file whose name appears anywhere in the step text, so naming a source file here would silently widen this step's scope to it, measured at walk 3); `[ -f /Users/marklehn/Developer/eluvian-governance/DRAFTING_CYCLE.md ] && echo GOV_OK` — HALT unless GOV_OK. ⚠️ **No shell variable survives to the next compound** (the environment fact above), so every command below spells its paths LITERALLY — never `$GOV`, never a scratch variable; the governance root is `/Users/marklehn/Developer/eluvian-governance` written out each time. Every governance operand below is ABSOLUTE; every forge deposit is RELATIVE to the worktree toplevel; commits use explicit pathspecs, ONE action per compound (C5), no `-A`, no amend, no push.
>
> **Task A0 — branches, catch-all LAST.**
> **(1) A1 PIN (C2):** `shasum -a 256 /Users/marklehn/Developer/eluvian-governance/DRAFTING_CYCLE.md` equals P1.
> **(2) CLEANLINESS:** `git -C /Users/marklehn/Developer/eluvian-governance status --porcelain -- DRAFTING_CYCLE.md` empty, AND the builder equals its committed blob: `git -C /Users/marklehn/Developer/eluvian-governance show HEAD:governance/knowledge/decisions/drafts/build-gate2-dc-w28-2026-09-01.py | shasum -a 256` equals `shasum -a 256 /Users/marklehn/Developer/eluvian-governance/governance/knowledge/decisions/drafts/build-gate2-dc-w28-2026-09-01.py` (freeze item 0, re-run by you: a builder that differs from its committed blob is NOT the reviewed tool → HALT).
> **(3) RE-ENTRY key:** `git -C /Users/marklehn/Developer/eluvian-governance log --oneline -1 -- DRAFTING_CYCLE.md` — subject carries this plan's slug?
> **(4) FLIP state:** `sqlite3 -readonly /Users/marklehn/Developer/forge_lessons/lessons-forge.db "SELECT id||'|'||status||'|'||COALESCE(route,'')||'|'||COALESCE(status_updated_by,'')||'|'||COALESCE(status_updated_at,'') FROM lesson_proposals WHERE id IN (426,427,428,429,432,439,440,441) ORDER BY id;"`
> - **FRESH** = (1) match AND (2) both empty/equal AND (3) no AND (4) per id: 426/427/428/429/432 `accepted|codify|planner|2026-09-01T22:03:28Z` and 439/440/441 `accepted|codify|ceo|2026-09-01T21:58:31Z` (⚠️ the stamps are load-bearing — EXCL_F's exclusion list and E11's provenance row are built from these two literals) → Task B.
> - **RE-ENTRY (doctrine landed, flip owed)** = (3) yes AND porcelain clean AND (4) all eight `accepted|codify` → verify Task C on the committed file; then, if `knowledge/development/dev-log-gate2-dc-w28-2026-09-01.md` is absent or uncommitted in the worktree (D-1 landed and D-2 did not), perform D-2 first; then Task E.
> - **RE-ENTRY (complete)** = (3) yes AND (4) all eight `implemented` → verify Task C + re-run E-c fresh. **Tail half-states (the 538 D-3 clause — CHANGES_F is NOT reconstructable):** if the dev note lacks the flip sentinels/read-back section, re-append it from E-c's fresh read plus the committed `flip-capture.txt` line count, marked `re-derived on re-entry` — **CHANGES_F is unreconstructable, say so rather than invent it**; if the capture or dev note sits uncommitted, complete Task E's closing commit for exactly those paths; if the capture file is ABSENT (the `.output` path resolved elsewhere, or the run died between COMMIT and the closing commit), re-derive it READ-ONLY from the worktree toplevel with `cd "$(git rev-parse --show-toplevel)" && sqlite3 -readonly /Users/marklehn/Developer/forge_lessons/lessons-forge.db ".output knowledge/qa/evidence/gate2-dc-w28-2026-09-01/flip-capture.txt" "<the E-b capture SELECT — the one fenced line beginning SELECT id||'|'|| and ending ORDER BY id; — copied verbatim>"` — it is EXACTLY reconstructable post-commit because it excludes the eight flipped rows and nothing else moved (433 lines, HALT otherwise) — mark it `re-derived on re-entry`, and commit it. Then report complete.
> - **RE-ENTRY (applied, uncommitted)** = (1) MISMATCH but `shasum -a 256 /Users/marklehn/Developer/eluvian-governance/DRAFTING_CYCLE.md` equals **P7** AND (2) porcelain shows `DRAFTING_CYCLE.md` modified AND (3) no AND (4) all eight `accepted|codify` → the live file is byte-for-byte THIS plan's builder output left uncommitted by a refused D-1 commit (scout SC-6; the 100006 recovery arm): run Task C on the live file, then Task D onward. ⚠️ P7 equality is the ONLY licence for this arm — any other sha with a dirty porcelain is NONE-MATCH.
> - **NONE-MATCH** = anything else (incl. a pin mismatch whose sha is not P7) → **HALT quoting every measurement.** ⛔ Never run any `checkout --` on an A0 halt: a pin mismatch means the file carries changes that are NOT this plan's.
>
> **Task B — dry-run then apply.** `mkdir -p /tmp/g2dcw28-scratch && rm -f /tmp/g2dcw28-scratch/DC-out.md && python3 /Users/marklehn/Developer/eluvian-governance/governance/knowledge/decisions/drafts/build-gate2-dc-w28-2026-09-01.py /Users/marklehn/Developer/eluvian-governance/DRAFTING_CYCLE.md /tmp/g2dcw28-scratch/DC-out.md; echo "builder_exit=$?"` (⚠️ the fixed scratch dir `/tmp/g2dcw28-scratch` lives OUTSIDE the governance root — the builder REFUSES any output under it by casefolded prefix, plus live-path realpath AND samefile, plus output==input; a refusal here is a HALT, not a retry). Expect stdout BEGINNING `OK — 11 edits` (P3, measured at walk 0), then SEPARATE compounds: `git diff --no-index --numstat /Users/marklehn/Developer/eluvian-governance/DRAFTING_CYCLE.md /tmp/g2dcw28-scratch/DC-out.md` reading **12 / 6** (P4; exit 1 EXPECTED — read the numbers), `wc -c /tmp/g2dcw28-scratch/DC-out.md` = **164175** (P5), `shasum -a 256 /tmp/g2dcw28-scratch/DC-out.md` = P7. Mismatch → HALT with both numbers. Then apply by copy (its own compound): `cp /tmp/g2dcw28-scratch/DC-out.md /Users/marklehn/Developer/eluvian-governance/DRAFTING_CYCLE.md`; close that compound separately with `cmp /tmp/g2dcw28-scratch/DC-out.md /Users/marklehn/Developer/eluvian-governance/DRAFTING_CYCLE.md; echo "cmp_exit=$?"` → expect 0, else restore (`git -C /Users/marklehn/Developer/eluvian-governance checkout -- DRAFTING_CYCLE.md`) and HALT.
>
> **Task C — post-conditions from the LIVE file, counts read never exit codes** (`/usr/bin/grep -cF -- "<token>" /Users/marklehn/Developer/eluvian-governance/DRAFTING_CYCLE.md` each — ⚠️ the token goes in DOUBLE quotes exactly as written below: four tokens carry an apostrophe (`PARENT'S`, `CONSUMER'S`, `enumeration's`) that breaks a single-quoted form silently into a different search; no token contains `$`, a backtick or a double quote):
> - `"dry-run the real CONSUMERS of the plan against what the plan DECLARES"` == 1 (E1) · `"Route every such question by whether a COMMAND can answer it"` == 1 (E1's clause)
> - `"clone against the PARENT'S WALK REGISTER"` == 1 AND `"clone against the PROJECT'S STANDING RULES"` == 1 (E2)
> - `"Label every verification with its METHOD"` == 1 (E3)
> - `"PROVING A GUARD COVERS A RULE REQUIRES VIOLATING THE RULE"` == 1 · `"**CONJUNCTION**"` == 1 · `"**PROXY**"` == 1 · `"label it UNCARRIED"` == 1 (E4)
> - `"**Split on TIER, never on size.**"` == 1 · `"nothing in §4 moves"` == 1 (E5)
> - `"the review has become SAMPLING"` == 1 · `"ENUMERATE THE CLASS AGAINST ITS PRODUCERS"` == 1 · `"run EACH CONSUMER'S OWN PARSER"` == 1 (E6)
> - `"DERIVE that enumeration mechanically from the artifact IN THE SAME RUN"` == 1 · `"aim the mutant at the enumeration's BLIND SPOT"` == 1 (E7)
> - `"measure REFERENTIAL DISTANCE PER PAIR"` == 1 (E8)
> - `"The pool is the POPULATION, never a curated attribute over it"` == 1 (E9)
> - the eight provenance tags each == 1: `"*(Proposal 427, entry 419, codified 2026-09-01.)*"`, `"*(Proposal 428, entry 420, codified 2026-09-01.)*"`, `"*(Proposal 429, entry 421, codified 2026-09-01.)*"`, `"*(Proposal 432, entry 424, codified 2026-09-01.)*"`, `"*(Proposal 439, entry 431, codified 2026-09-01.)*"`, `"*(Proposal 440, entry 432, codified 2026-09-01.)*"`, `"*(Proposal 441, entry 433, codified 2026-09-01.)*"`, and BOTH 426 halves — `"the walk-0 half; the read/ran label is §2.7's; codified 2026-09-01.)*"` == 1 and `"the label half; the walk-0 consumer dry-run is §2.0's; codified 2026-09-01.)*"` == 1
> - `"**Version:** 2.23 (2026-09-01)."` == 1 AND `"**Version:** 2.22 (2026-09-01)."` == 0 (E10; the zero prints with exit 1 by design)
> - `"- **2.23 (2026-09-01):** slug gate2-dc-w28-2026-09-01"` == 1 AND `"- **2.22 (2026-09-01):**"` == 1 (E11 — the old row SURVIVES below the new one)
> - `wc -l /Users/marklehn/Developer/eluvian-governance/DRAFTING_CYCLE.md` = **369** (P6, measured at walk 0) · `awk '/^- /' /Users/marklehn/Developer/eluvian-governance/DRAFTING_CYCLE.md | wc -l` = **172** (P6, measured) · `shasum -a 256 /Users/marklehn/Developer/eluvian-governance/DRAFTING_CYCLE.md` = P7 (C3)
> Any failing → FRESH: `git -C /Users/marklehn/Developer/eluvian-governance checkout -- DRAFTING_CYCLE.md` (⛔ this is the ONLY place that command is sanctioned, and only after Task B reported `cmp_exit=0` in THIS run) + HALT; RE-ENTRY: HALT, no restore.
>
> **Task D — the governance commit, then the dev note commit (two repositories, two compounds).** (D-1) `cd /Users/marklehn/Developer/eluvian-governance && git add DRAFTING_CYCLE.md && git commit -m "[<id from your plan filename>] gate2-dc(gate2-dc-w28-2026-09-01): 426+427+428+429+432+439+440+441 — consumer dry-run, standing-rules + register diffs, read/ran label, violate-the-rule, split-on-tier, class-sampling, derived enumeration, referential distance, pool-is-population — DC v2.23" -- DRAFTING_CYCLE.md && git rev-parse HEAD` — the hash is **CAPTURE_COMMIT**; SEPARATE compound: `git -C /Users/marklehn/Developer/eluvian-governance show <CAPTURE_COMMIT> --numstat --format=` shows exactly ONE row, `DRAFTING_CYCLE.md` **12/6** (P4, measured at walk 0; mismatch → report loudly). ⚠️ **If the governance commit is REFUSED** (a hook, an index lock, identity, pathspec — the mini's governance pre-commit hook refused a commit during this very cycle) **after Task C passed in THIS run:** do NOT restore and do NOT retry blindly — leave the applied file in place, quote the refusal verbatim, and HALT; the applied-uncommitted RE-ENTRY arm at A0 (keyed on P7) resumes from here on re-dispatch (scout SC-6). (D-2) Back in the worktree (`cd "$(git rev-parse --show-toplevel)"` — ⚠️ you are still in the governance root after D-1; this compound must start with the worktree cd), write `knowledge/development/dev-log-gate2-dc-w28-2026-09-01.md` (both resolved roots, A0's determination and raws, the builder stdout, P3–P7 measured, the raw Task-C outputs, CAPTURE_COMMIT and its numstat; Task E appends after), then `git add knowledge/development/dev-log-gate2-dc-w28-2026-09-01.md && git commit -m "[<id>] gate2-dc-w28: doctrine landed at governance <CAPTURE_COMMIT-short> — dev log" -- knowledge/development/dev-log-gate2-dc-w28-2026-09-01.md && git rev-parse HEAD`. **Next: Task E-a.**
>
> ⚠️⚠️ **TASK D IS NOT THE END OF STEP 1 (plan 420 halted EXACTLY here and cost a corrective plan).** Step 1 is INCOMPLETE until E-c's closing commit; the ONLY `STOP` token in Step 1 is at the end of E-c. Do not report, do not yield — proceed directly to Task E.
>
> **Task E — the corpus flip (C6/C7). All DB operands ABSOLUTE; all deposits worktree-RELATIVE from the worktree toplevel.**
> **E-a evidence dir + backup.** `cd "$(git rev-parse --show-toplevel)" && mkdir -p knowledge/qa/evidence/gate2-dc-w28-2026-09-01`. A backup already present → REUSE, located by `find /Users/marklehn/Developer/forge_lessons -maxdepth 1 -name 'pre-g2dcw28-*.db'` (⚠️ the `.db` suffix is LOAD-BEARING — a bare prefix matches `-shm`/`-wal` siblings); >1 `.db` match → HALT. Else create: `sqlite3 -bail /Users/marklehn/Developer/forge_lessons/lessons-forge.db ".timeout 5000" ".backup /Users/marklehn/Developer/forge_lessons/pre-g2dcw28-$(date -u +%Y%m%d_%H%M%S).db"`. **ON BOTH BRANCHES, in exactly this form:** `sqlite3 -readonly "file:<backup-abs>?immutable=1" "PRAGMA integrity_check; SELECT 'BKTOT='||COUNT(*) FROM lesson_proposals; SELECT 'BK='||COUNT(*) FROM lesson_proposals WHERE id IN (426,427,428,429,432,439,440,441) AND status='accepted';"` → **`ok` + BKTOT=441 + BK=8** (P10); any off → HALT (on the REUSE branch a BKTOT/BK mismatch means STALE — do NOT reuse, do NOT delete; name it). ⚠️ **The `file:` URI prefix and `?immutable=1` are BOTH load-bearing** — a plain `sqlite3 -readonly <backup>` fails `(14)`, and a `<path>?immutable=1` WITHOUT `file:` silently creates a decoy file. ⚠️ **No restore runs without the CEO:** every HALT arm names the backup and stops.
> **E-a2 pre-flight (rollback-guarded, BEFORE any write):** `sqlite3 -bail /Users/marklehn/Developer/forge_lessons/lessons-forge.db ".timeout 5000" "BEGIN IMMEDIATE; SELECT 'PRE_F='||COUNT(*) FROM lesson_proposals WHERE id IN (426,427,428,429,432,439,440,441) AND status='accepted' AND route='codify'; SELECT 'ACC='||COUNT(*) FROM lesson_proposals WHERE status='accepted'; SELECT 'IMPL='||COUNT(*) FROM lesson_proposals WHERE status='implemented'; SELECT 'MAXID='||MAX(id) FROM lesson_proposals; ROLLBACK;"` → **PRE_F=8, ACC=20, IMPL=314, MAXID=441** (P8); off → HALT, nothing written.
> **E-b the flip** (write the file `knowledge/development/g2dcw28-flip.sql` with EXACTLY this content; runner, from the worktree toplevel so the relative `.output` path resolves there: `cd "$(git rev-parse --show-toplevel)" && sqlite3 -bail /Users/marklehn/Developer/forge_lessons/lessons-forge.db ".timeout 5000" ".read knowledge/development/g2dcw28-flip.sql"`):
> ```
> BEGIN IMMEDIATE;
> CREATE TEMP TABLE g_pre(x INTEGER CHECK(x=8));
> INSERT INTO g_pre VALUES((SELECT COUNT(*) FROM lesson_proposals WHERE id IN (426,427,428,429,432,439,440,441) AND status='accepted' AND route='codify'));
> SELECT 'PRE_F='||x FROM g_pre;
> .output knowledge/qa/evidence/gate2-dc-w28-2026-09-01/flip-capture.txt
> SELECT id||'|'||status||'|'||COALESCE(route,'')||'|'||category||'|'||COALESCE(status_updated_by,'')||'|'||COALESCE(status_updated_at,'') FROM lesson_proposals WHERE id <= 441 AND id NOT IN (426,427,428,429,432,439,440,441) ORDER BY id;
> .output stdout
> UPDATE lesson_proposals SET status='implemented', status_updated_at=strftime('%Y-%m-%dT%H:%M:%SZ','now'), status_updated_by='ceo' WHERE id IN (426,427,428,429,432,439,440,441) AND status='accepted' AND route='codify';
> SELECT 'CHANGES_F='||changes();
> SELECT 'EXCL_F='||COUNT(*) FROM lesson_proposals WHERE id IN (426,427,428,429,432,439,440,441) AND status='implemented' AND status_updated_at GLOB '20[0-9][0-9]-[0-9][0-9]-[0-9][0-9]T[0-9][0-9]:[0-9][0-9]:[0-9][0-9]Z' AND status_updated_at NOT IN ('2026-09-01T22:03:28Z','2026-09-01T21:58:31Z');
> SELECT 'ACC_POST='||COUNT(*) FROM lesson_proposals WHERE status='accepted';
> SELECT 'IMPL_POST='||COUNT(*) FROM lesson_proposals WHERE status='implemented';
> COMMIT;
> ```
> **PRE_F=8, CHANGES_F=8, EXCL_F=8, ACC_POST=12, IMPL_POST=322; capture 433 lines** (P9, measured 2026-09-01). ⚠️ The `g_pre` TEMP-TABLE CHECK makes PRE_F a STRUCTURAL gate — an off value aborts under `-bail` BEFORE the UPDATE, where a printed sentinel gates nothing; the UPDATE's CAS carries `AND route='codify'` so the write's predicate equals the guard's. ⚠️ **EXCL_F is a STATE predicate, NOT a change detector — CHANGES_F is the ONLY sentinel that says THIS run wrote: an already-flipped re-run prints EXCL_F=8 with CHANGES_F=0.** The capture's `id <= 441` cap makes QA Item 5's re-run insert-invariant across the inter-gate window. Every sentinel prints BEFORE COMMIT — **E-c is durability's only proof.** Off → HALT with numbers, never re-run. ⚠️ **ACC_POST=12 is the PASS value — the PT twelve remain; reading 0 as "drained, good" is the wrong shape.**
> **E-c post-COMMIT read-back, FRESH invocation, ASSERTED:** `sqlite3 -bail -readonly /Users/marklehn/Developer/forge_lessons/lessons-forge.db ".timeout 5000" "SELECT id||'|'||status||'|'||COALESCE(route,'')||'|'||status_updated_by||'|'||status_updated_at FROM lesson_proposals WHERE id IN (426,427,428,429,432,439,440,441) ORDER BY id;"` → ALL EIGHT `implemented|codify|ceo|<Z-stamp not in the two prior values>`; any other read → HALT loudly (name the backup). Then `wc -l knowledge/qa/evidence/gate2-dc-w28-2026-09-01/flip-capture.txt` → **433** (HALT on mismatch). Then append RAW to the dev note (E-a's backup lines, E-a2, E-b sentinels, E-c read-back); **commit the dev-note update + `knowledge/development/g2dcw28-flip.sql` + the capture** (one compound from the worktree toplevel, `git add` by the three explicit pathspecs, commit `[<id>] gate2-dc-w28: the eight flip implemented — sentinels, capture, read-back` `-- <the three paths>`, no amend). STOP.
>
> **Deposits:**
> - `/Users/marklehn/Developer/eluvian-governance/DRAFTING_CYCLE.md`
> - `knowledge/development/dev-log-gate2-dc-w28-2026-09-01.md`
> - `knowledge/development/g2dcw28-flip.sql`
> - `knowledge/qa/evidence/gate2-dc-w28-2026-09-01/flip-capture.txt`
>
> **Scope:**
> - `/Users/marklehn/Developer/eluvian-governance/DRAFTING_CYCLE.md`
> - `knowledge/development/dev-log-gate2-dc-w28-2026-09-01.md`
> - `knowledge/development/g2dcw28-flip.sql`
> - `knowledge/qa/evidence/gate2-dc-w28-2026-09-01/flip-capture.txt`

---

## STEP 2 — QA

> **Step 1's Receipt status must be `Status: Complete`.** Anything else → HALT. **FIRST — post a short visible chat message (1–2 sentences).** You are the Forge QA agent. `cd "$(git rev-parse --show-toplevel)"` first; every governance path below is spelled out — no shell variable survives between compounds.
>
> ⚠️⚠️ **BIND FIRST:** `CAPTURE_COMMIT` = `git -C /Users/marklehn/Developer/eluvian-governance log -1 --format=%H -- DRAFTING_CYCLE.md` — bound BEFORE anything consumes it; its subject must carry this plan's slug, else HALT.
>
> ⚠️⚠️ **PRECONDITION:** that commit was made before this step and not by this context. **ISOLATION (the inter-gate window is arbitrary wall-clock time):** FIRST record `shasum -a 256 /Users/marklehn/Developer/eluvian-governance/DRAFTING_CYCLE.md` and `git -C /Users/marklehn/Developer/eluvian-governance status --porcelain -- DRAFTING_CYCLE.md`; the porcelain MUST be empty and the sha MUST equal the committed blob's (`git -C /Users/marklehn/Developer/eluvian-governance show <CAPTURE_COMMIT>:DRAFTING_CYCLE.md | shasum -a 256`) — otherwise run every Item-1 probe against the COMMITTED extraction only (`git -C /Users/marklehn/Developer/eluvian-governance show <CAPTURE_COMMIT>:DRAFTING_CYCLE.md > /tmp/g2dcw28-HEAD-DC.md`), never the live path. **No Monitor; every command foreground; evidence written AND committed before the Rule 20 block runs.**
>
> **(A) Rule 20 self-check** — **MANDATORY, gate-enforced** (`rule_20_self_check` greps the QA receipt for the byte-exact banner `Rule 20 — QA Self-Check Results` and the line `PASSED — SELF-CHECK PASSED`; the verification table does NOT satisfy it). ⚠️ The daemon's own prompt may name the SHOP's path for the canonical block; on this machine the file is `/Users/marklehn/Developer/eluvian-governance/RULE_20_SELF_CHECK_BLOCK.md` — read THAT file, copy its canonical Python block, fill the four values, run it, paste its stdout into the receipt under a heading containing the word `Verification`:
> - `plan_slug`: `gate2-dc-w28-2026-09-01`
> - `qa_report_path`: `knowledge/qa/evidence/gate2-dc-w28-2026-09-01/qa-receipt.md`
> - `evidence_dir`: `"$(pwd)/knowledge/qa/evidence/gate2-dc-w28-2026-09-01"` (the QA's OWN worktree — never a main-tree path)
> - `required_evidence_files`: `["probes-raw.txt", "full-suite-gate2-dc-w28.txt", "flip-capture.txt"]`
>
> **(B) Deliverable verification — FAIL reported, never repaired:**
> - **Item 1 — Task C's battery re-run against the COMMITTED extraction:** `git -C /Users/marklehn/Developer/eluvian-governance show <CAPTURE_COMMIT>:DRAFTING_CYCLE.md > /tmp/g2dcw28-HEAD-DC.md` and run EVERY Task-C probe against THAT path — substitute `/tmp/g2dcw28-HEAD-DC.md` for the live path `/Users/marklehn/Developer/eluvian-governance/DRAFTING_CYCLE.md` in each probe, nothing else changes (all token counts, the version pair, the History pair, `wc -l` 369, bullet lines 172, sha = P7 — the P6/P7 values measured at walk 0) — raw → `probes-raw.txt`.
> - **Item 2 — C1 byte-identity:** the builder AS COMMITTED (`git -C /Users/marklehn/Developer/eluvian-governance show <CAPTURE_COMMIT>:governance/knowledge/decisions/drafts/build-gate2-dc-w28-2026-09-01.py > /tmp/g2dcw28-builder.py` — the builder was committed BEFORE the doctrine commit, so it is in that tree; `HEAD` could be a later commit) run on `git -C /Users/marklehn/Developer/eluvian-governance show <CAPTURE_COMMIT>~1:DRAFTING_CYCLE.md > /tmp/g2dcw28-pre.md` into `/tmp/g2dcw28-rebuilt.md`; `cmp /tmp/g2dcw28-rebuilt.md /tmp/g2dcw28-HEAD-DC.md; echo "cmp_exit=$?"` → 0 — byte-identical (raw → `probes-raw.txt`). ⚠️ Scratch under `/tmp` only — the builder refuses any output under the governance root.
> - **Item 3 — C4/C5:** CAPTURE_COMMIT numstat pasted (ONE row, `DRAFTING_CYCLE.md` 12/6 — P4, measured at walk 0); the two worktree commits' numstats (dev log 1 file; closing commit 3 files); toplevel printed; subjects per the Task forms; no-amend proven by the reflog in BOTH repositories: `git -C /Users/marklehn/Developer/eluvian-governance reflog show --date=iso HEAD | /usr/bin/grep -cF -- "(amend)"` → 0 over the Step-1 window, and the same in the worktree. ⚠️ `--numstat` is the instrument — never hand-count the raw diff.
> - **Item 4 — gate-neutrality sweep (§6 coordinate-doctrine-and-gate, the §1 edit's owed check):** each of `"Split on TIER"`, `"dry-run the real CONSUMERS"`, `"PARENT'S WALK REGISTER"`, `"VIOLATING THE RULE"`, `"become SAMPLING"`, `"BLIND SPOT"`, `"REFERENTIAL DISTANCE"`, `"pool is the POPULATION"`, `"read/ran"` counts **0** in `/Users/marklehn/Developer/bellows/scripts/plan_lint.py` AND `/Users/marklehn/Developer/bellows/gates.py` (positive control: `"Rule 20"` in `plan_lint.py` > 0; `"cycle_tier"` in `plan_lint.py` > 0 — the tier check is present and untouched).
> - **Item 5 — C7 flip re-verify (read-only, FRESH connection):** per id 426/427/428/429/432/439/440/441 `implemented|codify|ceo` Z-stamped NOT IN the two prior values; categories all `governance_rule` (re-read); **`accepted` = 12 — the queue does NOT drain; name the cohort: the twelve `PLANNER_TEMPLATE.md` rows (thread 76)**; `implemented` = 322; `SELECT 'NEWROWS='||COUNT(*) FROM lesson_proposals WHERE id > 441;` — **INFORMATIONAL, report the value: NEWROWS>0 does NOT fail this item**; the capture SELECT re-run EXACT (copy from `g2dcw28-flip.sql`, SIX columns incl. the `id <= 441` cap), diff vs the deposited 433-line file → empty. **State explicitly in the row that this re-read is a POST-COMMIT fresh-connection read, not a citation of Step 1's sentinels.**
> - **Item 6 — raw output throughout; backtick any ❌ marker inside quoted literals.**
> - **Item 7 — corpus preservation (window-tolerant):** `lesson_proposals` = **441 + NEWROWS** (a flip creates no row) and `lesson_entries` = **433 + any window ingests** (report the deltas; a shortfall BELOW the baseline is the failure); the six-column capture is a STATUS-SURFACE proof — the other columns and the entries table are covered by THESE counts, not by the diff.
> - **Item 8 — the suite (P11).** From the worktree toplevel: `/Users/marklehn/Developer/bellows/.venv/bin/python -m pytest src scripts -q -p no:cacheprovider > knowledge/qa/evidence/gate2-dc-w28-2026-09-01/full-suite-gate2-dc-w28.txt 2>&1; echo "exit=$?" >> knowledge/qa/evidence/gate2-dc-w28-2026-09-01/full-suite-gate2-dc-w28.txt` — the file must carry the raw `80 passed` summary line and the exit code; a failure count > 0 is a Critical finding, not a note. ⚠️ `python3` has no pytest here; the interpreter is pinned.
> - **The receipt** `knowledge/qa/evidence/gate2-dc-w28-2026-09-01/qa-receipt.md`: the per-item table `| Deliverable | Expected | Status (✅/❌) | Evidence |` citing the evidence files by path under a `## Verification Table` heading — ⚠️ the `rule_22_verification` gate substring-matches hedging vocabulary (`pending`, `partial`, `tentative`, `mostly`, …) in positive-status rows: status cells carry the glyph ONLY, and the word `proposed`/`accepted` (real column values) are the terms for DB states, never `pending`; then the Rule 20 block's stdout verbatim.
>
> Commit the evidence dir by explicit pathspec (`git add knowledge/qa/evidence/gate2-dc-w28-2026-09-01/ && git commit -m "[<id>] gate2-dc-w28: QA evidence — probes, suite, receipt" -- knowledge/qa/evidence/gate2-dc-w28-2026-09-01/`; cd-first, no amend), then STOP.
>
> **Deposits:**
> - `knowledge/qa/evidence/gate2-dc-w28-2026-09-01/qa-receipt.md`
> - `knowledge/qa/evidence/gate2-dc-w28-2026-09-01/probes-raw.txt`
> - `knowledge/qa/evidence/gate2-dc-w28-2026-09-01/full-suite-gate2-dc-w28.txt`
>
> **Scope:**
> - `knowledge/qa/evidence/gate2-dc-w28-2026-09-01/qa-receipt.md`
> - `knowledge/qa/evidence/gate2-dc-w28-2026-09-01/probes-raw.txt`
> - `knowledge/qa/evidence/gate2-dc-w28-2026-09-01/full-suite-gate2-dc-w28.txt`

Rule 20 banner (byte-exact, produced by RUNNING the canonical block — never hand-authored):

```
============================================================
Rule 20 — QA Self-Check Results
============================================================
PASSED — SELF-CHECK PASSED — all evidence files present, no hedging keywords found.
```

---

## Drafting Cycle

**Tier:** T2 — T-6 fires (DRAFTING_CYCLE.md is governance surface); T-2 fires (eight canonical rows flip) and caps at T1; T-3 does not fire (authored and dispatched on the mini); T-8 does not fire (structure-for-structure clone of `executable-538`). **Panel composition: FULL form** — the magnitude proxy MEASURED at walk 0: (ii) mechanized edits hold (every DC mutation flows through the committed builder, anchors count-1-asserted by execution, `OK — 11 edits`); (iii) scripted probes hold (every post-condition proven earnable — every new-rule token 0 in live v2.22 — and every guard proven firing); (i) structure-clone HOLDS as §2.6 words it — a shipped same-class parent named with its Done date (538, 2026-08-25) and both walk-0 clone-diffs run. **All three conditions pass, so the small form is LICENSED; the full form is chosen as a SELF-ESCALATION over a passing proxy** (scout SC-12 corrected the first draft, which called (i) doubtful): the doubt is on the dispatch topology — a forge_lessons worktree editing another repository by absolute path, which no shipped DC Gate-2 plan has run — an axis the proxy does not measure; the §2.6 metering comparison row will say so. Composition: scout + DISCOVERY (weak-spots + destruction, evidence-attack + clone-diff briefs, both parents handed) + EXECUTION + CAPSTONE.

**Walk register:** /Users/marklehn/Developer/eluvian-governance/governance/knowledge/research/walk-register-gate2-dc-w28-2026-09-01.md
(Schema 0.3, eight-column rows for every phase including panel seats, committed per phase — the parenthetical sits on its OWN line because `cycle_check`'s register-line regex captures to end-of-line.)

**Walk 0 (context pin, measured — the register's M1–M5):** target sha P1 (v2.22; newest same-class `[538]` Done 2026-08-25; newest mini writer `halted-100006`, its pin already stale by its own success — no new blocker); all eleven anchors count-1 by execution with line/length/column recorded; every new-rule token 0 pre-edit; no target line last written by a halted or in-flight plan; flip pre-state measured PER ID (the MIXED two-stamp form above); builder dry-run scratch→scratch with the four guards, the seven-mutant battery (captured with raw output at `/Users/marklehn/Developer/eluvian-governance/governance/knowledge/research/mutants-gate2-dc-w28-2026-09-01.txt`, M7 the documented pass carried by P7) and the `-O` re-run recorded. **Clone-diff vs 538, three passes (FACTS re-measured for this batch; ARTEFACTS — every parent mechanism counted 1 in the clone, none dropped, five added; STRUCTURE — the two-repository commit split read both ways) PLUS the two further diffs this plan's own E2 mandates — the project's standing rules (forge_lessons/bellows `CLAUDE.md`, PT Dispatch Path Rules; governance has none) and the parent's walk register (X-1, SC-1..SC-9, D-4, W1-4, CP-1, CP-2 each checked in the clone).** **Consumer dry-run (this plan's own E1 rule, run on the plan that ships it, register f5–f7):** class assigner → `shop-infra` = declared; deposit extractor → step 1 four paths, step 2 receipt FIRST + a `full-suite` `.txt`; QA-step detection → step 2 only; header parser → T2 / qa_steps 2 / always / known_failures 0; cross-repo uncommitted check → rc 128, no failure; `plan_lint` → 0 FAIL after the P1 pin row went absolute (PIN-CHECK ok); `propagation_check` → exit 2 (unparseable) fixed to exit 1 with six classified residual hits; `cycle_check` → CONTINUE; `fold_check` baseline saved, re-saved once for the INTENDED P1 change. **Walk-0 yields: EIGHT findings (register f1–f8) — instruction 5 / record 3, 7 of 8 fold-introduced, every one caught by execution: four builder findings (the retention post-condition's predicted count, the derived in-place set, the deletion mutant that passed, the version-line exception), three consumer dry-run findings (the unverifiable pin row, the unparseable Numbers table, the dry-run record), and the register's own shape caught by the pre-commit hook.** **Scout: CONVENED TWICE — the Air's idle session (13 findings) and a fresh local session (12; six shared): 0 HIGH, 0 DIRECTION, every pin held on both machines; all 19 distinct findings author-verified and folded (register, the two per-seat rows), the builder re-proven and P5/P7 re-pinned after the fold.**

**Freeze checklist (the 538 form):** (0) builder-hash currency — on-disk builder == its committed blob; (1) faithful-mirror `plan_lint` at the deposit-path resolution expecting the pre-classified advisory set only ((o2) worktree-relative deposit paths, the 100007 form); (2) A0-fresh re-verify (pin + porcelain + flip-state) immediately before the deposit move; (3) `id_sequence` read AT deposit (read-only); (4) receipt BEFORE ready-staging (R-F3); (5) post-move `ls` of the deposited path; (6) C8 serialized-dispatch re-verified.

**Conformance (§5):** first run at walk 0 (shape-stability, before any adversarial pass) and re-run after every walk; LAST run (walk 2 close): `plan_lint` exit 0 / 0 FAIL at the faithful mirror — expected WARN set (o2)×6 worktree-relative deposits, (r)×10 Task-C probe constants, (q)×1 scratch sha, (f)×1 the `<declare>` validation placeholder, plus the two walk-in-progress structural WARNs (closing indicates fold; no Closing line yet — ⚠️ named by description, never by its literal token: `cycle_check`'s closure regex reads that token ANYWHERE in this block as a closure claim and escalates `claimed-close-unmet`, measured at walk 2) that the close clears; `propagation_check` exit 1 — six residual hits classified in register f6 (four value collisions, two mechanism-site expectations); `cycle_check` CONTINUE. The freeze re-runs all three and this line is corrected from the measured set before deposit.

**Direction verdict (after walk 1): PROCEED.** Tested, not judged — no finding from the author's walk 0, either scout, or walk 1 invalidates the clone origin (538), the mechanism (the builder, re-proven after every fold), or a scope-licensing premise (no gate edit owed; no target line owned by a halted or in-flight plan; C8).

**Walks:**
- Weak spots:          w1 3 folded — instruction 3 / record 0 (shell state does not persist between compounds, yet 31 `$GOV` and 6 `$S` references spanned compounds — every path now literal, the scratch dir fixed; QA Item 2 pinned the builder blob at `HEAD` — now at `<CAPTURE_COMMIT>`)
- Destruction:         w1 1 folded — instruction 1 / record 0 (a run dying between COMMIT and the closing commit left the capture unreconstructed — RE-ENTRY(complete) now re-derives it read-only, exactly, because it excludes the flipped rows)
- Vulnerabilities:     w1 1 folded — instruction 1 / record 0 (Task C's header showed a single-quoted probe form while four tokens carry an apostrophe — a silently different search; double-quoted form mandated)
- Integration-record:  w1 1 folded — instruction 0 / record 1 (the walk-0 paragraph still said "three mutants" and "scout: convened" after seven captured mutants and two scouts — brought to the record)
- ACID:                w1 1 folded — instruction 1 / record 0 (atomicity across two repositories: RE-ENTRY(doctrine landed) skipped D-2, so a death between the governance commit and the worktree commit would have left E-c appending to a dev log that never existed)
- **Walk 1 total: 6 findings, 6 folded — instruction 5 / record 1; 0 of 6 fold-introduced (all pre-existing-v0, invisible to two lens-4 scouts because none is an integration question).**
- Weak spots:          w2 2 folded — instruction 2 / record 0 (an ellipsis inside the RE-ENTRY(complete) re-derive command — an instruction no agent can execute as written, introduced by walk 1's lens-2 fold; QA Item 1 said "against THAT path" while every Task-C probe now spells the live path — the substitution named)
- Destruction:         w2 dry
- Vulnerabilities:     w2 dry (Task C's double-quoted tokens measured: none contains `$`, a backtick or a double quote — the header's claim is true)
- Integration-record:  w2 2 folded — instruction 1 / record 1 (the conformance line's rewrite quoted the closing token literally and `cycle_check` read it as a closure claim → ESCALATE:claimed-close-unmet, caught by the post-walk consumer run, token replaced by a description; and the line still described the walk-0 run as "first run" and deferred the last run to the freeze — now reads the last run)
- ACID:                w2 dry
- **Walk 2 total: 4 findings, 4 folded — instruction 3 / record 1; 2 of 4 fold-introduced. The bar is NOT met (instruction 3); walk 3 follows.** (The fourth, found by the post-walk consumer run: the conformance line quoted the closing token literally and `cycle_check` escalated on it — the E1 rule catching its own plan again.)

**Cold panel:** FULL form — scout at walk 0, then DISCOVERY, EXECUTION, CAPSTONE; seat findings and folds recorded on the walk register at their phase rows in the eight-column form; a further capstone pass fires if the capstone's own report produces folds.

## Cycle Manifest
tier: T2
target: DRAFTING_CYCLE.md
class: shop-infra
reads: /Users/marklehn/Developer/eluvian-governance/DRAFTING_CYCLE.md, /Users/marklehn/Developer/eluvian-governance/governance/knowledge/decisions/drafts/build-gate2-dc-w28-2026-09-01.py, /Users/marklehn/Developer/forge_lessons/lessons-forge.db, /Users/marklehn/Developer/eluvian-governance/gate1-packet-2026-09-01.md, /Users/marklehn/Developer/eluvian-governance/governance/knowledge/decisions/Done/executable-538.md, /Users/marklehn/Developer/forge_lessons/knowledge/decisions/Done/executable-100007.md, /Users/marklehn/Developer/bellows/knowledge/decisions/halted-executable-100006.md, /Users/marklehn/Developer/eluvian-governance/RULE_20_SELF_CHECK_BLOCK.md
writes: /Users/marklehn/Developer/eluvian-governance/DRAFTING_CYCLE.md, knowledge/development/dev-log-gate2-dc-w28-2026-09-01.md, knowledge/development/g2dcw28-flip.sql, knowledge/qa/evidence/gate2-dc-w28-2026-09-01/flip-capture.txt, knowledge/qa/evidence/gate2-dc-w28-2026-09-01/qa-receipt.md, knowledge/qa/evidence/gate2-dc-w28-2026-09-01/probes-raw.txt, knowledge/qa/evidence/gate2-dc-w28-2026-09-01/full-suite-gate2-dc-w28.txt, lessons-forge.db, pre-g2dcw28-HHMMSS.db
open_forks: none — the twelve remaining accepted rows are the PT tranche, thread 76
walks: 2
yields: 6, 4
validation: <declare>
coherence: N/A
