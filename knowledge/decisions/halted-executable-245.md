# Lessons Forge — Gate 2 Codification (cycle 2026-07-20): the walk-the-list stop condition + ACID lens + diagnostic triggers + 2 rules → PLANNER_TEMPLATE v4.76
**Date:** 2026-07-20 | **Tier:** Medium | **Dispatch Mode:** bellows | **Test Scope:** none | **Execution:** Step 1 (SA) → Step 2 (DEV) → Step 3 (QA) | **qa_steps:** 3 | **pause_for_verdict:** always

## CEO Context

Gate 2 of the 2026-07-20 cycle. Gate 1 closed 2026-07-20 (plan 244): five proposals (**155–159**) all routed `codify`. This plan codifies them as **FIVE edits** to governance-root `PLANNER_TEMPLATE.md` (live **v4.75**, the dedup baseline): three amendments to the `## The Drafting Cycle` section (a coherent cluster — they share the section and interact) plus one new Rule and one new Checklist item. **All five proposals → `implemented`.**

**⭐ CEO DECISION embedded — proposal 159 codifies in the WALK-THE-LIST form, NOT the entry's verbatim text.** LESSONS entry 94 (file ordinal; **DB `entry_id` 151** — the SA's join returns the DB id, and the Gate-1 record cites the same lesson as "entry 151"; both names resolve to the one lesson, cite the changelog row as "entry 94 / DB entry 151") states a *within-lens iterate-to-dry* model. The CEO's 2026-07-20 correction supersedes it, and LESSONS entry 99 (C1) is the deciding evidence: across the 2026-07-20 drafting-cycle experiment, three defects INTRODUCED by a fold were caught only by a *different* lens reading *different* evidence — never by re-running the owning lens. Iterate-to-dry keeps asking the same question; walk-the-list rotates the question. **The codified stop condition must contain ALL of these elements (each is a numbered requirement, per the very lesson this plan also codifies as Rule 54):**
- (a) one pass per lens; walk the whole lens list in order, folding accepted findings after each pass;
- (b) a lens is re-run only on a **subsequent walk** of the whole list — never immediately on its own fold;
- (c) the cycle is done when a **full walk returns zero or only-minor findings**;
- (d) the last event before deposit must be a **lens pass, never a fold** (a fold produces a draft no pass has examined) — **with the closing mechanism stated: if the walk's final lens pass folded anything, a brief confirming pass over the folded draft supplies the closing lens pass** (expected dry, since only minor findings were folded; any new MATERIAL finding re-opens the walk instead; **new MINOR findings at the closing check are RECORDED in the deposit, not folded — a minor-only closing pass still satisfies the deposit condition**, per entry 94's own deposit-condition sentence; this closing check is exempt from (b)'s walk-rotation rule — it is a deposit-condition check, not a lens re-run);
- (e) the honest-zero guard: a dry pass is a reportable success, not underperformance — manufacturing minor findings to appear diligent is the same Goodhart failure as forcing a predicted number;
- (f) diminishing-returns is RETAINED as the outer framing: a full walk that finds nothing new — **zero or only-minor, the same threshold as (c)** — IS the diminishing-returns signal;
- (g) cross-lens contradictions are the closing lens's job (ACID runs last — see edit 2).
The changelog row must name the supersession explicitly: entry 94's within-lens iteration is **replaced** by the walk-the-list form, evidence entry 99/C1.

**Other embedded decisions (all CEO, recorded at Gate 1 and after):**
- **156 codifies as the GENERAL observed-delta rule** — the live template's daemon-write-path live-canary paragraph (in the Bellows Execution Model section) is the narrower instance and gets cited as its worked example, not duplicated.
- **155 lands as Rule 54, an explicit SIBLING of Rule 51** (`:1040`, "corrections at verdict time go into plan text, not verdict disposition prose"). The relationship: Rule 51 says WHERE a verdict-time correction goes (plan text); Rule 54 says WHAT FORM any requirement must take (a structural home — numbered row, named test, or gate) and that location precedes numbering. SA must read Rule 51 and draft Rule 54 to reference it; a duplicate would be a dedup failure.
- **ADR-004 Decision 6 constraint (extraction-readiness):** the Drafting Cycle section will later be extracted as a standalone spec (`governance/adr/ADR-004-eluvian-practice-architecture.md`). Keep the three section edits **self-contained** — no new cross-references from inside the section into other template sections beyond what already exists; the Rule 54 / Checklist #32 additions live outside the section and may cite it, not vice versa.

**Statuses transition via DIRECT SQL — there is no helper.** Verified at authoring: `set_proposal_status` does **not** exist in `src/lessons_forge.py` (plan 228's "set_proposal_status semantics" was a description, not a function — do not hunt for it; the same phantom-identifier class as the plan-205 `_parse_session_limit_reset` citation, Rule 52). The DEV uses `UPDATE lesson_proposals SET status='implemented', status_updated_at=<UTC now>, status_updated_by='ceo' WHERE id IN (155,156,157,158,159)` on the canonical DB, read-write connection, `conn.commit()` after.

**Gate 2 is a governance edit with NO test suite — Planner verification is the safety net (Test Scope: none).** SA blueprints exact insertion/replacement text; DEV applies VERBATIM; QA verifies structure. PLANNER_TEMPLATE lives in the governance ROOT — the DEV edits it in place by absolute path; it stays **UNCOMMITTED** (the Planner commits cross-repo at wrap; plan-134/208/228 precedent). **⚠️ Wrap-commit protocol (binds the PLANNER, recorded here because the wrap is this schedule's final write):** between QA's certification and the wrap commit, the template sits modified-uncommitted in the main tree — an unguarded window. Before committing it, the Planner MUST re-run `shasum -a 256` on the template and match it against the hash in the Step-2 dev-log. Match → commit (the certified bytes are the committed bytes). Mismatch → a post-QA edit landed; investigate before committing — committing blind would attribute foreign edits to this Gate 2 under QA's certification. This closes the integrity chain end-to-end: SA read (commit pin) → DEV write (A0) → QA read (row 0 hash) → wrap commit (this re-match).

**⚠️ Forward note for the NEXT lessons-forge cycle (record, no action here):** LESSONS entry 99 (walk-the-list, C1) is un-ingested; when the next cycle classifies it, its substance will ALREADY be codified by this plan. Its future proposal should route **`reference`** at that cycle's Gate 1 — codified here, evidence role — not `codify`. Recording this now prevents a duplicate-codification confusion later; the QA milestone paragraph repeats it.

**Deposit-once discipline:** deposited exactly once.

## How to Run This Plan

**Bootstrap prompt:**
```
Read the plan at lessons-forge/knowledge/decisions/executable-gate-2-codification-2026-07-20.md. Execute Step 1 ONLY. After completing Step 1, STOP and wait for my confirmation. Do NOT proceed to Step 2.
```

---
---

## STEP 1 — SA (Solution Architect)

---

> **FIRST — post a short visible chat message (1-2 sentences) confirming you are starting this plan and your immediate next action.** Do NOT rename the plan file.
>
> You are the Solution Architect. Run commands from your own working tree; the target file is the governance-root `/Users/marklehn/Developer/GitHub/PLANNER_TEMPLATE.md` (**READ-ONLY this step** — you blueprint, DEV applies). Read the five proposals VERBATIM from canonical (`sqlite3 "file:/Users/marklehn/Developer/GitHub/lessons-forge/lessons-forge.db?mode=ro" "SELECT id, suggested_action, reasoning FROM lesson_proposals WHERE id BETWEEN 155 AND 159"`) and the SOURCE lesson bodies **via the DB join, not ordinal counting** (fragile): `SELECT p.id, p.entry_id, e.source_heading, e.raw_content FROM lesson_entries e JOIN lesson_proposals p ON p.entry_id = e.id WHERE p.id BETWEEN 155 AND 159` — `raw_content` IS the lesson body, and `p.entry_id` is what verifies the entry-94/DB-151 dual citation the changelog row carries. For the stop condition ALSO read the walk-the-list evidence entry from `/Users/marklehn/Developer/GitHub/LESSONS.md`, located by its heading (`## 2026-07-20: Walk the lens list, don't re-run one lens to dry`; grep the substring `Walk the lens list` to sidestep shell-quoting the apostrophe) — it is un-ingested, so it is not in the DB. Read the live `## The Drafting Cycle` section (template `:314–:341` at authoring — re-derive live line numbers, do not trust these), Rule 51 (`:1040`), Rule 53 (`:1056`, the current highest Rule), and Checklist #31 (`:1264`, the current highest item).
>
> **Scope:**
> - `knowledge/development/gate-2-blueprint-2026-07-20.md`
>
> **Blueprint the FIVE edits — exact text + exact anchor, each dedup-checked against the live template:**
> 1. **Amend `### Escalation Triggers`** (source: proposal 158): add the diagnostic-shaped triggers alongside the executable-shaped bullets — a diagnostic escalates to the full cycle when (i) its findings will be authored from without re-verification; (ii) its questions' answers depend on data whose availability is unconfirmed; (iii) it scopes a multi-plan arc; (iv) it touches a leak, money, or governance surface. Add the guidance line: for a diagnostic the highest-value lens is **weak spots aimed at the questions themselves** — is each answerable, answerable *from here*, and phrased so "unknown" is an acceptable answer. Evidence cite: diag-229 Q6/Q7 (struck for answering data questions from data that did not exist).
> 2. **Amend `### The Full Cycle` — add ACID as the FIFTH named lens, run last** (source: proposal 157): examines the plan's accepted requirements **as a system** — atomicity, consistency, isolation, durability — specifically hunting contradictions between requirements folded by earlier passes (the plan-239 class: neither requirement wrong, their interaction wrong) and converting accidental properties into stated ones. Extend the existing "passes 1–3 require adversarial imagination, pass 4 requires institutional memory" note with the pass-5 counterpart (systems reasoning over the plan's own requirements) — the note is the semicolon tail of lens 4's line; extend it in place, leaving the four lens DESCRIPTIONS byte-identical.
> **Edit 2 also owns the LENS-COUNT SWEEP — three live phrases become FALSE when the fifth lens lands; blueprint all three** (verified live at authoring; re-derive): `:324` "Escalate to the full four-pass cycle when:" → "Escalate to the **full cycle** when:" (count-free — durable if the lens list grows again); `:330` "under four **named lenses**" → "under five **named lenses**" (touching ONLY that phrase — the off-queue first sentence of the line is element (v), byte-untouched); `:341` "four heavy passes" → "five heavy passes" (this one site is physically in `### Why this process exists` — it belongs to THIS sweep, not to edit 3's blast radius). **EXEMPT: the historical sentence "Plan 224 was the first to run the full four-lens cycle…" keeps "four-lens"** — it records history, not doctrine.
> 3. **Rewrite the iteration/stop paragraph of `### The Full Cycle`** (source: proposal 159, in the CEO's walk-the-list form): replace the current "Repeat until a pass honestly reports diminishing returns" model with text containing ALL seven elements (a)–(g) from CEO Context. **⚠️ The paragraph being replaced carries doctrine BEYOND the stop condition — enumerated so none silently evaporates. Five live IN the replaced paragraph and must be PRESERVED in the rewrite; the sixth lives OUTSIDE it and is verified INTACT, not restated** (verified against the live file: off-queue is in the `### The Full Cycle` INTRO at ~:330, not in the ~:337 paragraph — restating it in the rewrite would duplicate doctrine and trip the dedup discipline): (i) severity-rank findings each pass; (ii) verify claims against code and data mid-analysis (grep, not assume); (iii) report what held up alongside what failed; (iv) fold **all** accepted findings; (v) **the off-queue/deposit-equals-dispatch intro sentence at the top of `### The Full Cycle` survives UNCHANGED** (outside the replacement — confirm presence, do NOT restate); (vi) fold-and-deposit **exactly once**. A rewrite that ships (a)–(g) but drops any of (i)–(iv)/(vi), or that restates or disturbs (v), is a FAIL at QA row 2. **Blast radius is EXACTLY ONE paragraph:** the replacement target is the single "Each pass: severity-rank findings … (deposit-once discipline)." paragraph — the off-queue/lens-list intro above it is edit 2's territory and the `### Why this process exists` subsection below it is NOT part of the replacement (its permitted touches are exactly TWO, each owned elsewhere: the one-phrase diminishing-returns reconciliation check — its "preserves the cycle's own diminishing-returns stop signal" clause must remain true under (f); blueprint the minimal phrase if needed, none if not — and edit 2's "four heavy passes" count fix, which is edit 2's sweep, not an edit-3 touch). A blueprint that replaces more than that one paragraph is over-scope — the Planner rejects it at the verdict gate.
> 4. **New Rule 54** (Orchestration Plan Rules, after Rule 53; source: proposal 155): every requirement needs a **structural home** — a numbered verification row, a named test, or a gate condition; prose paragraphs do not survive execution; when a verdict adds a requirement mid-plan, restate it as a numbered row in the plan file, not only in the verdict; **location (in the plan file the agent works from) precedes numbering**. Explicit sibling framing to Rule 51 (51 = where corrections go; 54 = what form requirements take). Evidence cite: the 2026-07-19 arc (three prose-only instructions dropped; every numbered/named/gated instruction honoured).
> 5. **New Checklist #32** (Plan Authoring Checklist, after #31; source: proposal 156): verification of any wired call must observe a **behaviour change through the real entry point** — a before-value, the action, an after-value. Grep proves wiring exists; a green suite proves it does not throw; only an observed delta proves it works; when designing such a check, verify the construction actually produces the expected delta. Cite the Bellows-model live-canary paragraph as the specific instance of this general rule (locate it live; do not duplicate its text). Evidence cite: plan 230 (stopped one row short of closing on a money path because every test called the engine directly).
>
> Also blueprint: version bump on BOTH header lines — `**Version:** 4.76` (line ~5) and `**Last Updated:** <EXECUTION-DATE> (v4.76)` (line ~6). **The literal token `<EXECUTION-DATE>` is the ONE sanctioned substitution in this otherwise-verbatim blueprint:** write it wherever the date appears (the Last Updated line and the changelog row's date cell); DEV resolves it to the actual apply date at application time (expected 2026-07-20; 228 precedent dates by execution). This keeps DEV a faithful applicator — a hardcoded date would force either a stale date or a silent verbatim breach if dispatch rolls past midnight; **ONE** new changelog row at the TOP of the changelog table — **which in the live template is the `| Date | Lesson |` table under `## Lessons Learned` (its current top data row is the v4.75 row); the word "changelog" appears nowhere in the template, and a SECOND Date-led table exists (Forge Observations) — do not confuse them** — naming all five edits, the five→`implemented` mapping, and the entry-94 supersession (replaced by the walk-the-list form; evidence entry 99/C1) — **the row is a markdown table cell: no unescaped `|` inside the cell text** (it would shear the table); confirm **NO renumbering** of existing rules/checklist items (new items are exactly 54 and 32).
>
> **Pin what DEV will apply to what you read (cross-step drift guard):** record in the blueprint `git -C /Users/marklehn/Developer/GitHub rev-parse HEAD` and the template's last-touching commit (`git -C /Users/marklehn/Developer/GitHub log -1 --format=%H -- PLANNER_TEMPLATE.md`). DEV re-checks the latter before applying — if the template changed between your read and DEV's write, the blueprint's anchors are stale even if the tree is clean.
>
> **Dedup discipline:** for each edit, grep the live template for existing coverage and record the greps + hit counts in the blueprint. Known-adjacent items that are NOT duplicates (relate, don't duplicate): Rule 51 (edit 4's sibling), the live-canary paragraph (edit 5's instance), Rule 20/QA-evidence norms. Any OTHER hit → flag it in the blueprint with a relate-or-halt recommendation rather than silently proceeding.
>
> **Deposit:** `knowledge/development/gate-2-blueprint-2026-07-20.md` — for each of the 5 edits: the exact insertion/replacement text (ready to paste), the exact anchor (quote the live line to insert after / replace), and the dedup note. Plus the version/changelog text, the status-transition list (all five → implemented, direct SQL per CEO Context), a per-element checklist showing **BOTH lists**: the seven added elements (a)–(g) AND the six preserved-doctrine items (i)–(vi) per their definitions in edit 3 ((v) = intro-intact, not restated) — a blueprint self-certifying only (a)–(g) is incomplete — **and an Output Receipt with status** (Step 2's entry gate halts without it). Canonical Python file-write pattern — no heredoc. Commit the deposit. In `### Ledger Updates` include `#### Prompt Feedback`.
>
> **Deposits:**
> - `lessons-forge/knowledge/development/gate-2-blueprint-2026-07-20.md`
>
> **STOP. Do NOT proceed to Step 2. Wait for CEO verdict.**

---
---

## STEP 2 — DEV

---

> **Before starting, read the Step 1 blueprint and confirm Output Receipt status Complete; otherwise halt and report.** Post a short visible chat message confirming you are starting Step 2.
>
> You are the Forge Developer. Apply the Step-1 blueprint to `/Users/marklehn/Developer/GitHub/PLANNER_TEMPLATE.md` (governance root, by ABSOLUTE path, in place) **VERBATIM** — you are a faithful applicator, not a re-author; if the blueprint and the live template disagree on an anchor, **halt and report** rather than improvise. **Recovery rule for a botched application — SCOPED to this plan's own work:** the pre-edit template is committed (Task A0 verifies clean), so the safety net is `git -C /Users/marklehn/Developer/GitHub restore PLANNER_TEMPLATE.md` → HALT and report. Never hand-patch over a bad apply — a restore is clean, an incremental fix-up over a mangled state is how a governance file rots. **`git restore` discards ALL uncommitted changes to the file — use it ONLY when the modifications are this plan's own (a botched or mangled apply, per A0's own-anchors branch). On A0's foreign-dirty branch, restore would DESTROY someone else's uncommitted work: that branch is HALT-only, never restore.** **The two restore paths differ deliberately in what follows:** A0's restore covers a PRIOR dispatch's leftovers → restore and PROCEED (this dispatch is fresh); a botch by THIS dispatch → restore and HALT — the retry belongs to a fresh dispatch with fresh context, not to the agent that just failed (self-retry over one's own botch is the hand-patch spiral by another name). **The two A0 protections — snapshot-aside and per-hunk attribution — apply before EVERY restore in this plan, INCLUDING this mid-step path** (a foreign edit can land during the step just as in the death→re-dispatch window; any unattributable hunk → HALT without restoring). Then transition the five proposals' status on the canonical lessons-forge DB.
>
> **Scope:**
> - `/Users/marklehn/Developer/GitHub/PLANNER_TEMPLATE.md` (edit in place; leave UNCOMMITTED — the Planner commits at wrap)
> - `knowledge/development/gate-2-codification-dev-2026-07-20.md`
>
> **Task A0 — pre-edit cleanliness gate, with a resume disambiguation.** Before touching the template: `git -C /Users/marklehn/Developer/GitHub status --short -- PLANNER_TEMPLATE.md` must be **empty** (clean — verified true at authoring), AND the template's last-touching commit (`git -C /Users/marklehn/Developer/GitHub log -1 --format=%H -- PLANNER_TEMPLATE.md`) must **match the one recorded in the Step-1 blueprint** — a clean tree with a different last commit means the template changed between SA's read and now, and the blueprint's anchors are stale **→ HALT and report; do not apply a blueprint against a template state the SA never read.** **If the tree is DIRTY, disambiguate before halting:** grep the modified template for this plan's own anchors (`4.76` — note line 5 carries no `v` prefix, so grep the bare number; the walk-the-list stop text; `### 54.` in the Rules section). **ANY one anchor present → treat as this plan's own work** (the version bump is applied FIRST as one atomic edit covering both header lines — Task A — so even the earliest death leaves `4.76` present). **Present → a prior dispatch touched the file — but anchors prove the plan's work is IN the dirt, not that the dirt is EXCLUSIVELY the plan's. Before ANY restore, two protections:** (1) **snapshot the dirty file aside — to a DURABLE, gitignored, MAIN-TREE path that survives worktree teardown and is never committed** (it may contain foreign content — committing it would publish someone else's uncommitted work): `cp /Users/marklehn/Developer/GitHub/PLANNER_TEMPLATE.md /Users/marklehn/Developer/GitHub/lessons-forge/.bellows-cache/template-dirty-snapshot-$(date -u +%Y%m%dT%H%M%SZ)-$$.md` (the `-$$` PID suffix makes the name collision-safe — two dirty-recovery dispatches in the same second would otherwise clobber one snapshot, silently breaking the "never unrecoverable" guarantee) (the `$(date …)` resolves inline — never paste a literal `<stamp>` token, which bash parses as a redirection; `.bellows-cache/` is gitignored; create it if absent), absolute path recorded in your dev log (no restore is ever unrecoverable — and the snapshot must outlive your worktree to make that true); (2) **attribute the dirt** — `git -C /Users/marklehn/Developer/GitHub diff -- PLANNER_TEMPLATE.md`: every hunk must match blueprint-recipe content; **ANY unattributable hunk = MIXED dirt (a foreign edit landed in the death→re-dispatch window) → treat as foreign → HALT, never restore.** All hunks attributable → `git -C /Users/marklehn/Developer/GitHub restore PLANNER_TEMPLATE.md`, then **verify the restore landed**: the status check reads clean AND the last-touching commit still matches the blueprint's recorded hash (a foreign COMMIT in the window means the restored baseline is one the SA never read → HALT, stale anchors). Only then proceed with a fresh FULL application from the clean baseline — the blueprint is the complete recipe; restore-and-reapply is deterministic. **Absent → foreign modifications → HALT and report — do NOT restore** (restoring would destroy someone else's uncommitted work). (Use `git -C <root>` — the template is tracked by the ROOT repo; a bare `git status` from your worktree passes vacuously, LESSONS entry 100.)
>
> **Task A — apply the 5 edits** from the blueprint. **Apply the VERSION BUMP FIRST, as ONE edit covering BOTH header lines — it is the resume marker:** if this dispatch dies mid-apply, `4.76` in the header is what lets a re-dispatch's A0 recognize the dirt as this plan's own work (a death after only a later-ordered edit would otherwise read as foreign and force an unnecessary investigation; a death BETWEEN the two header lines would leave a marker only line 5's bare `4.76` matches — which is why A0 greps the bare number and the bump is atomic). Then, in blueprint order: the two `### Escalation Triggers` / `### The Full Cycle` amendments and the stop-condition rewrite; Rule 54; Checklist #32; *(version bump — already applied first, do not re-apply)*; one new changelog row at the top. **The ONE sanctioned deviation from verbatim: resolve every literal `<EXECUTION-DATE>` token in the blueprint to today's actual date** (the blueprint marks the date this way deliberately — see the SA step). NO renumbering of existing items. NO other edits beyond the blueprint.
>
> **Task B — status transitions on canonical** (`/Users/marklehn/Developer/GitHub/lessons-forge/lessons-forge.db`, absolute path, read-WRITE connection — do not reuse a `?mode=ro` handle): direct SQL, **no helper exists** (per CEO Context).
>
> **FIRST — isolation pre-flight, against the MAIN tree by ABSOLUTE path (a relative `ls` from your worktree is VACUOUS — lifecycle renames are uncommitted main-tree filesystem operations, invisible to any worktree checkout; plan 244's own pre-flight was retroactively vacuous for exactly this reason, reporting "no in-progress plans" while its own in-progress file sat in the main tree).** Run `ls /Users/marklehn/Developer/GitHub/lessons-forge/knowledge/decisions/` and assert BOTH: (a) **the listing SHOWS this plan under its live name** — `in-progress-executable-*.md` under Bellows dispatch (the daemon claimed it), OR `executable-*gate-2*.md` under manual bootstrap (no rename occurs) — the positive signal proving you are reading the live tree (neither present means your view is stale → halt, do not proceed on a vacuous read); and (b) no OTHER lessons-forge `in-progress-*`/`verdict-pending-*` plan appears (a concurrent corpus writer would false-fail QA row 7's delta). Then: (c) the five targets must read `status='proposed' AND route='codify'` in the pre-write capture below — with the diagnosis split: **all five reading `implemented` with routes intact = a completed prior Task B** (the benign resume shape) — **skip the UPDATE *and* the delta assertion (no write happened this run, so the −5/+5 delta cannot and need not hold); verify only the read-back FACTS (all five `implemented`, routes still `codify`), report completed-state, and proceed to the deposit**; **mixed or unexpected values = an intervening change** between Gate 1 and now — **HALT**, do not clobber an intervening decision.
>
> **⚠️ Raw SQL has no structural bound — unlike a helper that touches only its named id, a typo'd WHERE clause can hit other rows. Two guards, in order:**
> 1. **BEFORE the write**, capture (a) the full status distribution, (b) the FULL restoration map — every row, all three columns the UPDATE mutates: `SELECT id, status, status_updated_at, status_updated_by FROM lesson_proposals ORDER BY id` (~160 rows, bounded; a typo'd WHERE that hits 5 already-`implemented` rows passes rowcount==5 and clobbers their update-metadata invisibly — only a map covering ALL rows and ALL mutated columns can restore that, and the DB is untracked so there is no git recovery), and **(c-evidence) the per-id read of the five targets INCLUDING route: `SELECT id, status, route FROM lesson_proposals WHERE id IN (155,156,157,158,159)`** — this is the query pre-flight check (c) and its completed-state diagnosis evaluate against (neither (a) nor (b) carries a `route` column), and it is a REQUIRED deposit item (QA row 7's branch decision reads it).
> 2. Execute the UPDATE with a REAL timestamp — `datetime.now(timezone.utc).isoformat()`, the module's own convention; never a literal placeholder string: `UPDATE lesson_proposals SET status='implemented', status_updated_at=?, status_updated_by='ceo' WHERE id IN (155,156,157,158,159)` — and **check `cur.rowcount == 5` BEFORE committing. On any other rowcount: `conn.rollback()`, HALT, report** — the transaction is still open, so a bad WHERE clause dies un-committed. Only on rowcount 5 → `conn.commit()`. (The completed-prior-Task-B case is intercepted upstream by pre-flight check (c)'s diagnosis split — by the time the UPDATE runs here, the targets were verified `proposed`, so the delta is `−5/+5` or something is genuinely wrong.)
>
> Verify by read-back: all five `implemented`, routes still `codify`, and the status distribution moved by exactly `proposed −5, implemented +5` relative to the pre-write capture (report both actual distributions; on any other delta, halt).
>
> **Deposit:** `knowledge/development/gate-2-codification-dev-2026-07-20.md` — the applied-edit confirmation (grep each new anchor: the diagnostic-trigger text, the ACID lens line, the walk-the-list stop text, `### 54.` within the Rules section, `### 32.` within the Checklist section — **section-scoped: `### N.` numbering repeats across sections, an unscoped grep hits the pre-existing Rule 32** — and `v4.76`), the before/after proposal-status distributions (RAW), **the raw pre-write per-id read of 155–159 with route (capture c-evidence — QA row 7's branch decision reads this, not your narrative)**, **the post-Task-B per-id read of the five as RAW `sqlite3` CLI output** — run `sqlite3 "file:/Users/marklehn/Developer/GitHub/lessons-forge/lessons-forge.db?mode=ro" "SELECT id, status, status_updated_at, status_updated_by FROM lesson_proposals WHERE id BETWEEN 155 AND 159"` AFTER Task B and paste its output verbatim (on the normal path these are the values you just committed; on the completed-state path they are the prior run's committed values — either way it is the current post-Task-B state). **Deposit the CLI output, NOT Python-side values — QA re-runs this identical query and byte-compares raw-to-raw, so both sides must be the same tool** (this pins the DB's Step-2→Step-3 window: the DB analog of the template shasum), **the post-edit template hash (`shasum -a 256 /Users/marklehn/Developer/GitHub/PLANNER_TEMPLATE.md`) — QA verifies the file is byte-identical at its read**, and an Output Receipt. Canonical Python file-write pattern — no heredoc. Commit the deposit (the dev-log only — NOT the template). In `### Ledger Updates` include `#### Prompt Feedback`.
>
> **Deposits:**
> - `/Users/marklehn/Developer/GitHub/PLANNER_TEMPLATE.md`
> - `lessons-forge/knowledge/development/gate-2-codification-dev-2026-07-20.md`
>
> **STOP. Do NOT proceed to Step 3. Wait for CEO verdict.**

---
---

## STEP 3 — QA

---

> **Before starting, read the Step 2 deposit and confirm Output Receipt status Complete; otherwise halt and report.** Post a short visible chat message confirming you are starting Step 3 (QA).
>
> You are Lessons Forge QA. **Verification + reporting only — no edits.** Do NOT use the Monitor tool. If a check fails, report it — do NOT fix it. Do NOT edit PROJECT_STATUS directly — route the milestone via the receipt's `### Ledger Updates` only (the third of the three standing QA bans; the trio has produced clean gates since plans 222/223).
>
> **MANDATORY — Rule 20 self-check banner.** Your deposit MUST contain, verbatim, a section headed exactly `## Rule 20 — QA Self-Check Results` followed (anywhere below it) by a line reading exactly `**PASSED — SELF-CHECK PASSED**`. End with a self-grep confirming the banner is present in your deposited report.
>
> **Evidence-source rule:** every DB row states its DB; canonical reads via `sqlite3 "file:/Users/marklehn/Developer/GitHub/lessons-forge/lessons-forge.db?mode=ro" "<query>"`. Deposit RAW output, never a summary.
>
> **Scope:**
> - `knowledge/qa/gate-2-codification-qa-2026-07-20.md`
>
> Verification table, one row per claim:
> 0. **The template you are reading is the template DEV wrote** — recompute `shasum -a 256 /Users/marklehn/Developer/GitHub/PLANNER_TEMPLATE.md` and match it against the hash in the Step-2 deposit. **If the deposit contains NO hash, this row is unverifiable — HALT** (do not proceed to content rows on an unpinnable artifact). **Mismatch → HALT before any content row** (rows 1–6 would certify a state DEV did not produce, and the wrap commit would then attribute foreign edits to this Gate 2). Disambiguate via row 8's git status: dirty-with-different-hash → a foreign edit landed after DEV; report what `git -C /Users/marklehn/Developer/GitHub diff -- PLANNER_TEMPLATE.md` shows beyond the blueprint.
> 1. **Version** is `4.76` on BOTH header lines of `/Users/marklehn/Developer/GitHub/PLANNER_TEMPLATE.md` (quote both lines), **and the substitution token was fully resolved: `grep -c '<EXECUTION-DATE>'` on the template outputs `0` — note grep EXITS 1 on zero matches, which is the EXPECTED/PASSING state here, not a command failure** (a surviving literal token outputs ≥1 and is the placeholder-literalization class shipping into standing governance).
> 2. **The stop condition is the walk-the-list form** — quote the rewritten paragraph(s) in full and verify, as a per-element table row each: ALL seven elements (a)–(g) from CEO Context present, AND all six preserved-doctrine items (i)–(vi) verified — (i)–(iv)/(vi) present IN the rewritten paragraph (severity-ranking, verify-mid-analysis, report-what-held, fold-all, deposit-once); **(v) checked differently: the off-queue intro sentence ABOVE the paragraph is intact and NOT duplicated inside the rewrite**. Then confirm the quoted text contains **no instruction to re-run the SAME lens immediately on its own fold before advancing** — that is the superseded within-lens model; its absence is checked in the quoted paragraph, not by fuzzy grep over the whole section.
> 3. **ACID is the fifth named lens, listed last**, with the systems-reasoning note extended; the four existing lens descriptions unchanged **apart from the mandated note extension (the note is the tail of lens 4's line — the lens DESCRIPTIONS themselves must be byte-identical)**. **And the lens-count sweep landed:** the extracted `## The Drafting Cycle` section contains ZERO occurrences of `four-pass cycle`, `four **named lenses**`, and `four heavy passes`, while the historical `four-lens cycle` (the Plan-224 sentence) REMAINS — **grep all four phrases FIXED-STRING (`grep -cF`), never as regex: the bold markers are `*` quantifiers to regex grep, which fails to match the literal phrase (a vacuous pass on the exact miss this check exists to catch) while matching the asterisk-free "four named lenses" in the v4.75 history row (a false FAIL). Scope to the extracted section; show all four counts.**
> 4. **The diagnostic-shaped escalation triggers** are present (all four: authored-from, unconfirmed-data, multi-plan arc, leak/money/governance) plus the weak-spots-at-the-questions guidance; the executable-shaped triggers unchanged.
> 5. **Rule 54** present after Rule 53, is the structural-home rule, and references Rule 51 as sibling; **Checklist #32** present after #31, is the observed-delta rule, and cites the live-canary paragraph as its instance. **NO renumbering — and the count checks MUST be SECTION-SCOPED: `### N.` numbering repeats across sections (at AUTHORING time Rule 32 sat at `:871` and Rule 33 at `:875` — these line numbers SHIFT after the section edits upstream; never cite raw line numbers as evidence, use the section extraction only). An unscoped grep for `### 32.`/`### 33.` false-fails a correct apply.** Extract each section's text (from `## Orchestration Plan Rules` to the next `## ` heading; from `## Plan Authoring Checklist` to the next `## ` heading) and check WITHIN each: Rules section has exactly one `### 54.` and no `### 55.`; Checklist section has exactly one `### 32.` and no `### 33.`. Confirm Rule 53 and Checklist #31 heading lines unchanged (quote each).
> 6. **Exactly ONE new changelog row**, at the top, naming the five edits, five→implemented, and the entry-94 supersession; the v4.75 row intact beneath.
> 7. **Canonical statuses:** proposals 155–159 all `implemented` with route still `codify`. Delta check by path — **decided by the deposit's RAW per-id capture of 155–159 (the c-evidence query, a mandated deposit item), not the DEV's narrative** (the capture showing the five as `proposed` = normal-write path; already `implemented` = completed-state path; a narrative claiming one while the raw capture shows the other is itself a FAIL — qa-evidence-raw-output discipline): normal write → the full status distribution shifted by exactly `proposed −5, implemented +5` vs the capture; **completed-state → the distribution is instead UNCHANGED vs the capture — verify the facts and the no-change, do not demand a −5/+5 that no write produced**. **Two hardening clauses:** (α) **re-run the EXACT query from Step 2's deposit** — `sqlite3 "file:…?mode=ro" "SELECT id, status, status_updated_at, status_updated_by FROM lesson_proposals WHERE id BETWEEN 155 AND 159"` — and **byte-compare its raw output to Step 2's deposited raw CLI output** (same tool both sides, so the comparison is well-defined). Any difference means a concurrent write touched one of 155–159 in the Step-2→Step-3 verdict window (even a valid-shape ISO timestamp) → HALT, this Gate 2 must not certify a foreign write — the DB analog of the template shasum. (This also catches the placeholder-literalization class: a stored literal `<UTC now>` is preserved identically on both sides, so instead assert the `status_updated_at` cells are real ISO-8601 UTC timestamps, not placeholder/empty, as a second clause.) If Step 2's deposit lacks the raw post-Task-B per-id output, this check is unperformable → HALT; (β) if the distribution delta differs from the expected shape ONLY in rows outside ids 155–159, that is a corpus write landing in the Step-2→Step-3 verdict window (paused plans do not block dispatch) — reconcile via the per-id facts, report the extraneous delta and its likely source, and do NOT fail the row on it; the five targets' facts are the gate (quote both raw distributions either way; do NOT hardcode absolute totals).
> 8. **The template is MODIFIED but UNCOMMITTED — use `git -C`, the file is tracked by the ROOT repo, not this one:** `git -C /Users/marklehn/Developer/GitHub status --short -- PLANNER_TEMPLATE.md` must show ` M` (that is the positive signal — LESSONS entry 100: a bare `git status` from the lessons-forge worktree passes VACUOUSLY because the file is absent there). A committed template is a FAIL — **but diagnose it before reporting: if the tree is CLEAN and the committed content already contains v4.76 with a matching row-0 hash, the likely cause is a premature wrap-sweep commit (the wrap actor ran before plan completion — a sequencing violation outside this plan), not a DEV failure. Halt and report WHICH.** (Cross-repo commit is the Planner's at wrap, sequenced AFTER plan completion.)
>
> If any row fails, report and halt — do not pass a broken deliverable.
>
> **Deposit:** `knowledge/qa/gate-2-codification-qa-2026-07-20.md` — the table with raw evidence, the per-element rows for BOTH lists ((a)–(g) added and (i)–(vi) preserved), Rule 20 banner + PASSED line, Output Receipt. Canonical Python file-write pattern — no heredoc. Commit it. In `### Ledger Updates` include: `#### Project Status` — one milestone paragraph (Gate 2 2026-07-20 complete: PLANNER_TEMPLATE v4.76 — walk-the-list stop condition (entry-94 model superseded, C1/99 evidence), ACID fifth lens, diagnostic escalation triggers, Rule 54 structural-home, Checklist #32 observed-delta; proposals 155–159 implemented; `proposed` at its actual post-state count per row 7's quoted distribution — expected 0; if nonzero, state the count and which ids remain (never force the number); **⚠️ WRAP REMINDER: before committing the template cross-repo, the Planner re-matches its shasum against the Step-2 dev-log hash — see the plan's wrap-commit protocol**; entry 99's future classification routes `reference` — its substance shipped here); `#### Prompt Feedback` — standard.
>
> **Deposits:**
> - `lessons-forge/knowledge/qa/gate-2-codification-qa-2026-07-20.md`
>
> **On full completion: the deposit commit is your ABSOLUTE LAST operation. Do NOT move this plan file to `Done/` — under Bellows dispatch that move is the daemon's, performed on continue-verdict consumption; an agent-side move is the Mode-A violation ("moved to Done/ without authorization") and flips this step's gates to FAILED on an otherwise clean run.** (This replaces the boilerplate closing line ten prior plans carried from the manual-execution era — every prior agent ignored it; the Failure-3 invariant says the agent never performs this move.)
