# Executable: Gate 2 batch 1 — §5 conformance-scheduling pair (proposals 232 + 245): DRAFTING_CYCLE v1.7 → v1.8, then flip both to `implemented`

**Type:** Executable
**Project:** lessons-forge
**Depends on:** **executable-326** (lessons-forge, Done — the Gate-1 routing plan that WROTE `status='accepted', route='codify'` on these two rows; its transaction stamped `status_updated_at='2026-08-09T01:20:01Z'`, the exact value this plan's G2 sentinel and QA row 5 pin as the prior-value exclusion, and the state A0 branch 5 checks), executable-311 (lessons-forge, Done — the classification cycle that created the proposals), the Gate-1 decision record (`/Users/marklehn/Developer/GitHub/gate1-packet-2026-08-08.md`, Group 3, CEO decision: codify — provenance only, no step reads it), DRAFTING_CYCLE.md at v1.7 (precondition, checked at A0)
**Created:** 2026-08-09
**Author:** Planner
**Slug:** `gate2-s5-conformance-2026-08-09` (authoring-time; stable across any crash-redo re-deposit — the A0 re-entry key and the Rule 20 `plan_slug`)
**dispatch_mode:** bellows
**pause_for_verdict:** always
**Priority:** 5
**cycle_tier:** T2
**qa_steps:** 2
**Test Scope:** targeted (Rule 21 — see the justification below)

⚠️ **ID NOTE — the deposit filename is decided AT DEPOSIT.** Slug+date name form; id read from `id_sequence` at deposit, never at authoring (the thrice-proven consumed-in-window class).

---

## Why this exists — the conformance pass keeps running too late, and its result is unrecorded

Proposals **232** (entry 224) and **245** (entry 237) are one scheduling edit, routed `codify` at Gate 1 (Group 3, the §5 pair — the first of the three queued Gate-2 batches). The evidence, from the DB rows: a plan went through five lenses, a separate ACID pass, and a five-reader cold panel **while the one-command conformance check never ran** — and nearly every cold-panel finding was detectable by the linter (entry 224); six adversarial phases and roughly a hundred findings provided **zero coverage of mechanical conformance**, because "does this conform to a convention the tooling requires" is structurally not a correctness, safety, or consistency question (entry 237). Re-proved live at diag-322's c2 (lint had never run pre-close; self-corrected ad-hoc). Already adopted as Tier-1 practice by the 2026-08-08 drafting-cycle review (the residue battery); doctrine catches up here.

**Routing:** the corpus path proper (§6 amend-only-through-the-corpus) — LESSONS.md entries → forge ingest → cycle 311 classification → Gate 1 route `codify` → this Gate-2 plan. No deviation to declare. The two remaining Gate-2 batches (the DC-surgical batch; the template batch) inherit v1.8 and run as separate sequential cycles. ⚠️ Item counts are deliberately NOT carved into E3 — the packet's group sizes use a fractional half-item convention and Group 6's residues fold into Group 4, so a bare integer in an immutable changelog row would be unreadable and would silently omit 261 (the v1.7 row's precedent: name the batches, not their counts).

## Scope — one doctrine file, three edits; one scoped 2-row flip; nothing else

- **Edits exactly ONE existing file:** `/Users/marklehn/Developer/GitHub/DRAFTING_CYCLE.md` (root repo), via edits E1/E2/E3 below. The AFTER text is GIVEN — the agent PLACES it, never composes it.
- **One DB write:** a scoped `UPDATE` flipping proposals 232 and 245 `accepted → implemented` at the canonical absolute path `/Users/marklehn/Developer/GitHub/lessons-forge/lessons-forge.db`.
- **No code edit.** No `.py`, no gate, no template, no other doc. Needing one means the premise failed → HALT. (The two declared `.sql` deposit files are DATA for sqlite `.read` — writing them is in scope, not a code edit.) §6's coordinate-doctrine-and-gate: **no gate edit is owed** — the amended §5 carries no structure `plan_lint` parses (§4 reads the header tier line and the `## Drafting Cycle` block, not §5 prose); §4 is unchanged and remains in lockstep — stated, not assumed.
- **No LESSONS.md touch.** The register stands at **227** (re-counted 2026-08-09 after this session's append); no step reads or writes it.
- ⚠️ **DOWNSTREAM EFFECT OF THE FLIP, NAMED — it is not status-cosmetic.** `accepted` is NOT in `_TERMINAL_STATUSES`; `implemented` IS (`lessons-forge/src/lessons_forge.py:31`, read 2026-08-09). The ingest update path treats the two differently: when a LESSONS.md entry is later EDITED, non-terminal proposals are **staled** (re-queueing the entry for classification) while terminal ones are only **flagged** (`:170–194`). So after this flip, a future edit to entries 224/237 will no longer re-queue them for re-proposal. **That is the intended meaning of "codified" and the reason the flip exists** — but it is a live behavioural change to a consumer, so it is stated here and VERIFIED at QA row 9 rather than assumed (the 326 consumer-check form).
- ⚠️ **The doctrine edit lands in the REAL governance root, outside any bellows worktree** — `_gate_scope_check` is cwd-scoped and cannot see it; the QA doctrine-integrity rows are the only guard and they fail closed. Every command touching the doctrine file uses the ABSOLUTE operand. **PLACEMENT vs VERIFICATION tooling (explicit license):** file tools (Read + Edit) MAY be used to PLACE the edits — the ~1,600-char single-line AFTER texts are quoting-hostile to sed/perl; VERIFICATION is Bash-only (`grep -F` / `shasum`), never a file-tool read — the stale-view risk under a live daemon applies to reads-as-evidence, not to the edit mechanism, whose result Bash then proves.
- ⚠️ **Verdict-window posture:** from the Step-1 doctrine commit until close, v1.8 GOVERNS the shop. A HALT holds it live for the CEO — never `git restore`/`revert` anything on a HALT; the root repo carries unrelated working state and a parallel terminal may be live. Rollback is a CEO decision, not an agent action.
- **Deposit basenames are DECLARED — do not re-date any at run time** (the 320/329 clause): the slug date stays even if the run date differs; the only place a live date appears is G2's in-statement `strftime`.
- **Provenance note:** inherited-convention cites in this plan (`291:428`, "the 309/320 convention", "the 298 model") are Done-plan-readable conventions, not re-executed measurements; every MEASURED value carries its own `measured 2026-08-09` or dry-run mark.
- ⚠️ **Expected `plan_lint` state at deposit (recorded per the very rule this plan codifies):** all checks PASS, with exactly ONE known-benign WARN — (o1) missing-path on the conditional `resume-sweep.txt` (produced only on A0 state 3; the held bellows batch's expected-lint declaration (m) is that WARN's designed home). Any OTHER WARN or FAIL at deposit is unexplained → do not deposit.

### Rule 21 — justification for `Test Scope: targeted`

This plan changes **no source code**: its deliverables are prose edits to one governance markdown file plus one scoped two-row `UPDATE`. No module, schema, or route is touched, so a full-suite run would exercise nothing this plan can break. **Targeted scope = the lessons-forge suite** (`lessons-forge/src/test_lessons_forge.py`), which covers the corpus helpers that read `lesson_proposals` — and, this repo having a single test module, the targeted run IS the full run (the 311/326 single-module precedent, re-verified not inherited: **55 collected, 55 passed, 0 skipped**, measured 2026-08-09). ⚠️ "Targeted = full" is a PREMISE about the repo's shape, not a fact about this plan — QA row 7 re-checks it at run time rather than inheriting it, because a second test module added before dispatch would silently make the targeted run a partial one. ⚠️ Per the DEV-step lesson the suite is deliberately NOT authored into Step 1; it lives in QA.

### ⚠️ Environment facts — observed, not predicted
1. `grep` is a ugrep shim: **`-F` for every literal**; a non-`-F` search can exit 1 SILENTLY on a present line. A zero-match `grep -c` prints `0` and exits 1 — the printed count is the assertion; do not `&&`-chain zero-count probes.
2. Shell state does NOT persist between commands — assign and use in the same invocation. (The flip's timestamp is computed in-statement, so NO shell variable carries it; the one shell variable the plan does use — Task G's scratch dir — is created and consumed in the same invocation, which is what this fact requires.)
3. zsh aborts on an unmatched glob and both remedies fail — use `find`, never a glob.
4. The DB is **gitignored and absent from any worktree**: a bare relative `sqlite3 lessons-forge.db` silently CREATES an empty file. Canonical absolute path only; every read-only query uses the single `-readonly` form fixed in Step 2's evidence rule.

---

## The three edits — AFTER text GIVEN

**E1 — §5 paragraph REPLACEMENT (the codification).** Anchor (unique, count=1 measured 2026-08-09): the paragraph beginning `Once the plan's shape is stable and before the closing walk,` — replace that ENTIRE single-line paragraph (the only paragraph of §5, ending `no lens is looking for.`) with:

> Run the non-adversarial conformance pass at **shape-stability** — as soon as the plan's structure stops moving, and in every case **before the adversarial passes close** (before the cold panel on T2; before the closing walk otherwise), **never for the first time at deposit**: execute `plan_lint`, then walk the plan against the Orchestration Plan Rules and the Plan Authoring Checklist **by scope** (the two sections number independently — never grep `### N.` unscoped). This checks the plan against the codified authoring rules; the lenses check it against reality. Most items are N/A; the value is the few that are not and that no lens is looking for. **The scheduling is load-bearing, not stylistic:** adversarial review asks whether the plan is RIGHT; conformance asks whether it is ADMISSIBLE — disjoint classes, one of which costs a command — so a late first run spends expensive reader attention on defects the command would have named (measured: a plan passed five lenses, a separate ACID pass, and a five-reader cold panel while the one-command check never ran, and nearly every panel finding was lint-detectable). **Record the linter's exit code in the plan, with the phase at which it last ran** — an unrecorded pass is indistinguishable from one that never ran. Re-runs stay cheap and normal (practice re-runs it after every culmination; a final run at deposit is expected); this rule fixes the FIRST run's timing, and the recorded exit code is the LAST run's. (Proposals 232 + 245, codified 2026-08-09.)

⚠️ E1 preserves the middle sentences byte-identical (`execute \`plan_lint\`, then walk … no lens is looking for.`) — the diff is the opening clause and the two appended obligations. C11 post-condition: old opening clause ABSENT **and** new text PRESENT.

⚠️ **Bound reconciliation — a recorded DECISION:** the proposals read "before the adversarial passes"; their own parenthetical "(when shape stabilises)" concedes shape stabilizes only mid-walks. E1's primary clause ("at shape-stability — as soon as the plan's structure stops moving") carries the proposals' actual schedule; the enforceable FLOOR is deliberately the panel/close boundary, because shape-stability is a judgment event, not a checkable one. A deliberate narrowing ratified at the panel — not an equivalence claim against the entries' text.

⚠️ **Delta by population, stated so the row is not mis-scoped later:** on **T2** the floor moves EARLIER (pre-panel, where the measured waste was). On **T1** the floor clause is the same "before the closing walk" the live text already carries, so the enforceable delta there is the explicit **never-first-at-deposit prohibition** plus the recording obligation and the in-section rationale. The edit is a scheduling change on T2 and a deposit prohibition on T1 — both intended.

**E2 — version line.** LENGTHENED anchor (count=1 measured): `**Version:** 1.7 (2026-08-08). Amended only through the Iteration Protocol` — within that line only, swap the version token to `1.8 (2026-08-09)`. ⚠️ The bare string `1.7 (2026-08-08)` counts **2** in the file (version line + History row); a replace-all destroys the changelog. The date `2026-08-09` is DECLARED (the Gate-1 routing decision date) — do not recompute at run time.

**E3 — History row, PREPEND as the FIRST bullet under `## History`** (ordering confirmed newest-first at authoring):

> - **1.8 (2026-08-09):** slug gate2-s5-conformance-2026-08-09; Gate-2 codification of proposals 232 + 245 (the §5 scheduling pair; Gate 1 Group 3, routed codify — the first of the three queued batches; corpus path, no §6 deviation). §5 rescheduled: the conformance pass runs at shape-stability — before the adversarial passes close, never for the first time at deposit — with the rationale carried in-section (RIGHT vs ADMISSIBLE are disjoint classes; the measured cycle whose five-lens/ACID/five-seat review never ran the one-command check), and the linter's exit code is recorded in the plan with the phase it last ran. Codifies practice already adopted at Tier 1 of the 2026-08-08 review (the residue battery re-runs it per culmination; this row fixes the floor, not that cadence). §6 coordinate-doctrine-and-gate: no gate edit — §5 carries no structure `plan_lint` parses; §4 unchanged and in lockstep. DB: proposals 232, 245 → implemented (scoped flip, lessons-forge). Inheritors: the two remaining Gate-2 batches (the DC-surgical batch, whose items 227/254 re-base onto §2.6's registry; the template batch) and the held bellows batch (rows 25/27/28, whose metered-A/B panel runs under the live doctrine).

⚠️ **E3 must NOT contain the string `1.7 (2026-08-08)`** (refer to predecessors by number alone) — QA's count row depends on it going 2 → 1.

**Measured line deltas [EXECUTED HERE — 2026-08-09, dry-run `git diff --no-index --numstat` on a scratch copy]: `3 added / 2 deleted`** (E1 1↔1, E2 1↔1, E3 +1). QA row 3 compares the real commit's numstat against THIS pinned pair, not against the dev-log.

---

## Conflict Ledger — run-time constraints

- **C1** — every edit anchored on a QUOTED UNIQUE string, never a line number. `grep -cF '<anchor>'` must return exactly 1. `>1` → lengthen; `0` → HALT (absent means the file changed).
- **C2** — the version edit is a surgical swap against the lengthened anchor; never replace-all.
- **C3** — doctrine committed BEFORE the DB flip (the 291:428 convention) — a die-between is detectable from the doctrine pins alone.
- **C4** — the backup is ADJACENT to the flip: created immediately before Task G and after the commit, so it inverts exactly one write. ⚠️ **Scoped, not absolutist** — A0 state 2 legitimately REUSES a backup taken in a crashed prior dispatch, which is not freshly adjacent. The `BK=2` restorability assert is what restores this row's guarantee there: it proves the reused file still holds the pre-flip state, so it is still a true inverse of the one pending write; if anything moved those rows since, `BK` ≠ 2 and the run HALTs. Verification, not recency, is what makes the backup an inverse.
- **C5** — the flip is scoped `WHERE id IN (232, 245) AND status='accepted'`; no whole-corpus predicate. `status_updated_by='ceo'` (the Gate-1 decision's actor; schema CHECK allows it).
- **C6** — `changes()` must equal exactly **2** AND `status_updated_at` must GLOB-match a timestamp on both rows — both printed as in-transaction sentinels the agent READS before trusting the run. ⚠️ The protection is STRUCTURAL, not conditional (sqlite scripts cannot conditionally ROLLBACK — G2 prices this): G1's rehearsal proves the predicate matches exactly 2, the scoped `AND status='accepted'` bounds the write, and a wrong sentinel → HALT with the adjacent Task-B backup as the CEO's recovery instrument.
- **C7** — the timestamp is computed IN-STATEMENT via `strftime('%Y-%m-%dT%H:%M:%SZ','now')` — a DELIBERATE deviation from the 298 shell-`$TS` mechanism: **no shell variable carries the timestamp**, so the empty-`$TS` failure class cannot arise for it. ⚠️ Scoped claim, not a global one: Task G's stderr capture does use a shell variable, and it is governed instead by same-invocation creation (Environment fact 2). The value-guarded GLOB assert stays as defense in depth.
- **C8** — the outside-range capture is taken inside **G2's flip transaction, before the UPDATE** — same-instant set identity with the flip (the 298 form). ⚠️ **QA's comparison BASELINE is always the DEPOSITED capture — never a QA-time reconstruction of it.** QA does re-run the projection to obtain CURRENT state (row 6 mandates that, and the re-run is bounded: fixed predicate, fixed id bound, partitioned result); what is forbidden is treating a fresh read as the baseline or widening the predicate. The deposited file is the left side of the diff, always.
- **C9** — the commit is path-scoped to exactly `DRAFTING_CYCLE.md` at the root repo; never `git add -A`. `git -C` absolute; post-commit name-only assertion.
- **C10** — SQLite access sets `busy_timeout`; `database is locked` is a HALT, not a retry loop.
- **C11** — every verification asserts the POST-condition per edit kind: E1 (replacement) old-absent AND new-present; E2 old-token-absent-at-line AND new-present; E3 (pure addition) new-present plus surrounding region unchanged.
- **C12** — **SCHEDULE ORDER IS LOAD-BEARING. Do not reorder:** A0 → A1 → E1/E2/E3 → E0(denylist) → DOC_SHA pin → F(commit) → F2(post-commit verify) → B(backup) → G1(rehearsal) → G2(flip txn: capture → UPDATE → sentinels → COMMIT) → G3(readback) → deposits.
- **C13** — E3's History row asserts the flip in past tense from Task F onward, EARNED only at G2 minutes later in the same step; a stop inside the F→G2 window leaves it unearned, and **A0 state 2 is that half-state's designated completion path** (the 328-class, handled by design rather than by luck). Check: state 2's condition is exactly this window's signature.
- **C14** — **SERIALIZED BELLOWS DISPATCH IS AN ASSUMPTION DOING LOAD-BEARING WORK, stated rather than hidden** (the 298 precondition-4 form): two agents must not write root doctrine or the corpus concurrently. The guards that do NOT depend on it: A1's pin, E0's porcelain read, F2's content verify, G1's PRE count, and QA rows 1/6 — each detects, after the fact, what serialization should have prevented.

---

## How to Run This Plan

**Bootstrap prompt:**
```
Read the plan at knowledge/decisions/in-progress-executable-<id>.md (the daemon renames the deposited placeholder on claim). Execute Step 1 ONLY. After completing Step 1, STOP and wait for my confirmation.
```

Step 1 (DEV) → verdict gate → Step 2 (QA). `pause_for_verdict: always`. No step renames this file.

⚠️ **HALT ROUTING — the inputs each step reads; if any is missing or unreadable, HALT the step that needs it and NAME it, never improvise** (the 306/309 list form — dropped by three prior clones and restored by their panels; carried here up front): **Step 1 reads** this plan file, the live `/Users/marklehn/Developer/GitHub/DRAFTING_CYCLE.md`, and the canonical DB. **Step 2 reads** this plan file, the Step-1 dev-log, the live doctrine file, the canonical DB (read-only form), the merged Step-1 evidence captures, and `/Users/marklehn/Developer/GitHub/RULE_20_SELF_CHECK_BLOCK.md`. ⚠️ An unreadable deposited capture is NOT license to re-derive it — that re-opens the open-ended comparison row 6 exists to close; it is a HALT (distinct from the crash-recovery DECLARED FALLBACK, which fires only when the Step-1 receipt itself reports the capture lost). Re-derive this list from the steps as written before running.

---
---

## STEP 1 — DEV (place the three edits, commit, then flip)

---

> **FIRST — post a short visible chat message (1–2 sentences) confirming you are starting this plan.** Do NOT rename this plan file. You are the Developer. ⚠️ **The doctrine edit lands in the REAL governance root at the ABSOLUTE path `/Users/marklehn/Developer/GitHub/DRAFTING_CYCLE.md` — not in your worktree; no teardown cleans it up.** If you HALT after editing has begun, SAY SO LOUDLY in the same breath: leave the tree exactly as it is (no restore — CEO inspects), report which of E1/E2/E3 landed and `git -C /Users/marklehn/Developer/GitHub status --porcelain -- DRAFTING_CYCLE.md`.
>
> **⚠️ TASK A0 — PRE-EDIT STATE CLASSIFICATION. Evaluate IN THIS ORDER; FIRST match wins (most-advanced-first):**
> 1. **Flip already done** — both rows read `implemented` (query at the canonical absolute DB path, `-readonly`) → verify the doctrine commit exists (`git -C /Users/marklehn/Developer/GitHub log --oneline -5 -- DRAFTING_CYCLE.md`, newest message must name the slug `gate2-s5-conformance-2026-08-09`; C3 says docs precede the flip, but a crash may have violated it — a missing commit here is a REPORTABLE anomaly, not a license to re-edit). ⚠️ **Then check whether the crashed run's deposits SURVIVED:** the capture and dev-log die with an unmerged worktree. If `outside-range-ids.txt` is not in the merged tree, you CANNOT reconstruct it (same-instant is unrecoverable by definition) — deposit a RECOVERY dev-log naming exactly which artifacts are missing, produce the G3 read-back fresh (that one IS reproducible), and state in the receipt that QA row 6 must take its DECLARED FALLBACK. Never fabricate a capture after the fact. Then report complete. ⚠️ On this path the daemon's deposit-existence gate will flag the missing capture — **that gate failure is the CORRECT signal for the human read of a crash recovery, not a defect to engineer around.**
> 2. **Docs committed, flip not done** — newest doctrine commit names the slug AND both rows still read `accepted` → **skip to TASK B (backup), then TASK G (the flip)** — NOT to Task F (the commit already happened). ⚠️ **DOC_SHA on this path is taken FROM THE COMMIT, never from the live file** — `git -C /Users/marklehn/Developer/GitHub show <that-commit>:DRAFTING_CYCLE.md | shasum -a 256` — and your dev-log says so explicitly. A DOC_SHA hashed from the live tree here would make QA row 1's three-way agreement compare the live file against itself, silently voiding the independence that row exists for; recording the commit-sourced value keeps row 1 a real check, and any live-vs-commit divergence then surfaces there as the foreign edit it is. ⚠️ A `pre-gate2-s5-` backup MAY already exist on this path (a crash between Task B and G2's COMMIT lands here): rediscover it with the PREFIX-ONLY form — `find /Users/marklehn/Developer/GitHub/lessons-forge -maxdepth 1 -name 'pre-gate2-s5-*.db'` with NO `-newer` clause (Task B's `-newer` form is a creation check; a foreign post-crash touch of the doctrine file would hide an existing backup from it and mint a duplicate) — REUSE what it finds; if none, run Task B normally. ⚠️ **Either way, run Task B's `BK=2` restorability assert against the backup you end up holding** — creation is skippable on this path, verification is not. ALSO re-run the doctrine porcelain check here: if `DRAFTING_CYCLE.md` is dirty ON TOP of the commit, a foreign edit landed post-commit — report it LOUDLY and proceed to the flip (the flip never touches doctrine; QA row 1 fails closed on the drift), never sweep it into any commit.
> 3. **Docs modified-uncommitted** — `git -C /Users/marklehn/Developer/GitHub status --porcelain -- DRAFTING_CYCLE.md` non-empty → **HALT.** Recovery route: run a per-edit `grep -F` sweep of the three AFTER texts against the live file, DEPOSIT the landed/not-landed table at `knowledge/qa/evidence/gate2-s5-conformance-2026-08-09/resume-sweep.txt` (chat output is not durable), and report. The CEO directs restore-and-redo or complete-forward; do not restore on your own initiative.
> 4. **Fresh-with-unexplained-backup** — a backup matching prefix `pre-gate2-s5-` exists (find via `find`, never a glob) with no doctrine commit and both rows `accepted` → **HALT.** With the backup adjacent to the flip, an unexplained one is evidence of an ATTEMPTED corpus mutation, not harmless setup residue.
> 5. **Fresh** — porcelain clean for the doctrine path; live version line reads `1.7` (anchor `**Version:**`, absolute operand); no `pre-gate2-s5-` backup; both rows `accepted` with `route='codify'` → proceed to A1.
>
> ⚠️ **Version-line cross-check on every path:** if the version reads neither `1.7` nor a `1.8` whose FIRST History bullet names this plan's slug — match by SLUG, not id — an in-window bump by another actor landed → **HALT; the edits and History row need re-basing.**
>
> ⚠️ **If the observed state matches NONE of the five branches** (e.g. ONE row `implemented` and one `accepted` — a state the atomic flip cannot produce, so it means manual interference) → **HALT and report the full observed triple: the porcelain output, the version line + first History bullet, and the per-id status/route read-back.** Never improvise a route.
>
> **⚠️ TASK A1 — RE-VERIFY THE AUTHORING PIN BEFORE EDITING.** `shasum -a 256 /Users/marklehn/Developer/GitHub/DRAFTING_CYCLE.md` must equal
> `1558110cde566b936dc96ab3b0af578eafcdcf92105492cae0202d63120fb45f`
> **Any mismatch → HALT** (every anchor below was proven against these bytes). Then re-prove each edit's anchor count: `grep -cF` the E1 opening-clause anchor → 1 (⚠️ it contains an APOSTROPHE — wrap the pattern in DOUBLE quotes; a single-quoted `'…plan''s…'` form concatenates WITHOUT the apostrophe and returns 0 on the present line, a measured authoring near-miss); the E2 lengthened anchor → 1; `grep -cF '1.7 (2026-08-08)'` → 2; `## History` heading present. Any mismatch → HALT with the matches quoted.
>
> **TASKS E1/E2/E3 — APPLY THE EDITS** exactly as given above (absolute operand; before applying each, confirm this plan's AFTER text is what you are placing — you compose nothing). ⚠️ **ONE-PHYSICAL-LINE MANDATE (producing site):** E1's replacement paragraph and E3's History row each land as exactly ONE physical line — no hard wraps, no reflow; the numstat pin (`3 2`) and QA row 4's awk bullet count are computed on that assumption, and a wrapped-but-correct edit false-HALTs at Task F. Verify each landed via `grep -F`/`shasum`, never a file-tool read. Post-conditions per C11: E1 old opening clause `Once the plan's shape is stable and before the closing walk,` ABSENT and new text PRESENT; E2 version line reads `1.8 (2026-08-09)` and `grep -cF '1.7 (2026-08-08)'` now returns **1**; E3 the new row is the FIRST bullet under `## History` and the prior first row is INTACT immediately below it.
>
> **⚠️ TASK E0 — PRE-COMMIT DENYLIST, scoped to the governance doctrine set.** `git -C /Users/marklehn/Developer/GitHub status --porcelain` → expect `DRAFTING_CYCLE.md` modified. **HALT iff any of the OTHER governance doctrine files is dirty: `PLANNER_TEMPLATE.md`, `RULE_20_SELF_CHECK_BLOCK.md`, `READONLY_AUDIT_CONTRACT.md`, `SPECIALIST_TEMPLATE.md`, `INTERMEDIATE_DECISION_PHRASES.md`** — foreign governance activity in-window is a re-base risk. Any OTHER dirty root file (baton/session files, gitlinks — a live parallel terminal legitimately dirties these) is REPORTED, never a HALT: the Task-F commit is path-scoped and F2's name-only assertion PROVES nothing else entered it, so a dirty non-doctrine file cannot contaminate this plan's commit. (This deliberately narrows the 298 all-root-`*.md` denylist — that form false-HALTs a correct run whenever a parallel terminal is mid-session, which is now normal shop topology.)
>
> **⚠️ TASK DOC_SHA — PIN BEFORE THE COMMIT.** `shasum -a 256` the edited file; record as **DOC_SHA** in your dev-log. A pin taken after the commit certifies whatever the commit contains.
>
> **TASK F — COMMIT, path-scoped, BEFORE touching the DB (C3):** `git -C /Users/marklehn/Developer/GitHub add DRAFTING_CYCLE.md && git -C /Users/marklehn/Developer/GitHub commit -m "[<id>] gate2(gate2-s5-conformance-2026-08-09): §5 conformance scheduling (232+245) — doctrine 1.7 -> 1.8" -- DRAFTING_CYCLE.md` (add-before-pathspec-commit, the 309/320 convention; `<id>` = this plan's deposited id, read from your own plan filename). Record `git -C /Users/marklehn/Developer/GitHub diff HEAD^ HEAD --numstat -- DRAFTING_CYCLE.md` — ⚠️ the `-C` is load-bearing: your cwd is a lessons-forge worktree, and the bare form diffs the WRONG repo (measured live — it returns that repo's own HEAD pair) → guaranteed false HALT. Expect `3	2	DRAFTING_CYCLE.md` per the authoring dry-run; a different pair → HALT and report, do not rationalize (a wrapped-but-correct edit is the likely cause — see the one-physical-line mandate).
>
> **TASK F2 — POST-COMMIT VERIFY:** `git -C /Users/marklehn/Developer/GitHub show HEAD:DRAFTING_CYCLE.md | shasum -a 256` must equal DOC_SHA; `git -C /Users/marklehn/Developer/GitHub show HEAD --name-only --format=` must list exactly `DRAFTING_CYCLE.md`. Mismatch → HALT (a write landed in the E0→F window and is inside this plan's commit).
>
> **TASK B — BACKUP, HERE and not earlier (C4):** `sqlite3 -bail /Users/marklehn/Developer/GitHub/lessons-forge/lessons-forge.db ".timeout 5000" ".backup /Users/marklehn/Developer/GitHub/lessons-forge/pre-gate2-s5-$(date -u +%Y%m%d_%H%M%S).db"` — assert exit 0 and empty stderr, as in Task G. Then `find /Users/marklehn/Developer/GitHub/lessons-forge -maxdepth 1 -name 'pre-gate2-s5-*.db' -newer /Users/marklehn/Developer/GitHub/DRAFTING_CYCLE.md | head -1` and `ls -la` the found path. ⚠️ The backup filename's date token is incidental naming, not a resume key — locate it by `find` on the prefix, never reconstruct.
>
> ⚠️⚠️ **ASSERT THE BACKUP IS RESTORABLE, NOT MERELY PRESENT — AND RUN THIS ON EVERY PATH THAT REACHES THE FLIP, INCLUDING A0 STATE 2's REUSE** (where Task B's creation step is skipped but this assert is NOT: a reused backup from a crashed prior dispatch is the LEAST verified instrument in the plan and the one most likely to be needed). This file is the CEO's ONLY recovery instrument for the flip, and "exists and is non-empty" proves neither that it opens as a database nor that it holds the pre-flip state — a check that cannot fail is not a guard. Run against the FOUND path: `sqlite3 -bail -readonly "<found-backup>" ".timeout 5000" "SELECT 'BK='||COUNT(*) FROM lesson_proposals WHERE id IN (232,245) AND status='accepted';"` → must print **`BK=2`**, exit 0, empty stderr. Anything else (unopenable file, missing table, wrong count) → **HALT before the flip** — proceeding would leave a mutation with no working inverse.
>
> **TASK G — THE FLIP: rehearsal → flip → read-back, three invocations.** ⚠️ **NO heredocs (the #1 forbidden op). Author each SQL file via your file-WRITE tool** in your worktree, then run `sqlite3 -bail /Users/marklehn/Developer/GitHub/lessons-forge/lessons-forge.db ".timeout 5000" ".read <absolute-path-to-file>"`. ⚠️⚠️ **`-bail` IS MANDATORY on every sqlite3 invocation in this task, and each invocation is asserted `exit 0` with EMPTY stderr (capture stderr to a fresh scratch dir OUTSIDE every git tree, then assert the file is empty — ⚠️ **shell state does NOT persist between commands, so create and use the scratch dir IN THE SAME invocation**, e.g. `S=$(mktemp -d) && sqlite3 -bail … 2>"$S/g-stderr.txt"; echo "exit=$?"; cat "$S/g-stderr.txt"` — a bare `$SCRATCH` from an earlier command expands EMPTY and redirects to `/g-stderr.txt`, which fails read-only before sqlite3 ever runs; repo-tree scratch would dirty the porcelain pins and the deposit gates, and bare `/tmp` is the 324 shadow-import lesson) — measured on a scratch WAL fixture: WITHOUT `-bail`, a daemon-held lock makes `.read` SKIP the failed `BEGIN IMMEDIATE`, run the UPDATE in autocommit once the lock clears, print BOTH sentinels green (`CHANGES=2`/`GLOBOK=2`), and confine every error to stderr — a committed un-bracketed flip on a green-looking run.** Non-zero exit or non-empty stderr → HALT (C10), report the stderr content and the per-id row states. ⚠️ **The DB is at the canonical absolute path; the CAPTURES land in YOUR OWN TREE** (the 298 model — commit with your deposits; the teardown merge carries them to the QA step's tree). `mkdir -p` your tree's evidence dir first and derive its ABSOLUTE path from `pwd` when you author the `.sql` files — sqlite `.output` must receive that derived absolute path, never a guessed one.
>
> **G1 — REHEARSAL (read-only by construction; file `knowledge/development/gate2-s5-flip-rehearsal.sql`), content exactly:**
> ```
> BEGIN IMMEDIATE;
> SELECT 'PRE='||COUNT(*) FROM lesson_proposals WHERE id IN (232,245) AND status='accepted' AND route='codify';
> ROLLBACK;
> ```
> Assert: stdout shows **`PRE=2`**, exit 0, empty stderr. This is both the predicate proof C6/C14 rest on and a live `BEGIN IMMEDIATE` lock probe. ⚠️ **This rehearsal is load-bearing and must not be skipped as redundant: it HALTS BEFORE any write.** The `CHANGES` sentinel can only REPORT a bad predicate after the UPDATE has committed — verified by construction 2026-08-09 (one row pre-flipped → the flip committed the other and printed `CHANGES=1` at exit 0). Prevention and reporting are not the same guard. `PRE` ≠ 2 → **HALT and name which id is off and its current status** (a row no longer `accepted` may be a legitimate in-window stale — the 298 forensic-signature discipline: report WHICH, never just "the flip failed").
>
> **G2 — THE FLIP, with the capture INSIDE the same transaction (file `knowledge/development/gate2-s5-flip.sql`), content exactly** (the `.output` path is your tree's evidence dir, derived from `pwd` when you author the file):
> ```
> BEGIN IMMEDIATE;
> .output <your-tree-abs>/knowledge/qa/evidence/gate2-s5-conformance-2026-08-09/outside-range-ids.txt
> SELECT id||'|'||category||'|'||status||'|'||COALESCE(route,'')||'|'||COALESCE(status_updated_at,'')||'|'||COALESCE(status_updated_by,'') FROM lesson_proposals WHERE id <= 273 AND id NOT IN (232,245) ORDER BY id;
> .output stdout
> UPDATE lesson_proposals SET status='implemented', status_updated_at=strftime('%Y-%m-%dT%H:%M:%SZ','now'), status_updated_by='ceo' WHERE id IN (232,245) AND status='accepted';
> SELECT 'CHANGES='||changes();
> SELECT 'GLOBOK='||COUNT(*) FROM lesson_proposals WHERE id IN (232,245) AND status_updated_at GLOB '20[0-9][0-9]-[0-9][0-9]-[0-9][0-9]T[0-9][0-9]:[0-9][0-9]:[0-9][0-9]Z' AND status_updated_at <> '2026-08-09T01:20:01Z';
> COMMIT;
> ```
> ⚠️ **The `<> '2026-08-09T01:20:01Z'` clause is load-bearing, not decoration:** both rows ALREADY carry a GLOB-matching timestamp from the Gate-1 routing write (measured 2026-08-09 — a bare GLOB count returns 2 BEFORE this flip runs, making it vacuous). Excluding the pinned prior value is what turns the sentinel from a shape check into a value guard that can only be satisfied by this plan's own write.
> Assert the capture file exists with **271 lines** (corpus measured 273 rows, MAX(id)=273, 2026-08-09 — an in-window forge cycle inserts at 274+, which the `id <= 273` bound deliberately excludes). ⚠️ **This assert is read AFTER the transaction has committed, so a mismatch does NOT mean "abort the flip" — the flip has already landed.** A count below 271 means a corpus row was DELETED between authoring and this run; above 271 means the id bound's premise (MAX(id)=273) no longer holds. Either way: **do not re-run G2, do not edit the capture** — record the observed count, name which ids differ from the expectation, and HALT for a CEO read with the flip reported as landed.
> ⚠️ **Known mechanism limit, priced:** sqlite scripts cannot conditionally ROLLBACK, so the sentinels print before a COMMIT that will happen regardless. The protection is STRUCTURAL, not conditional: G1 just proved exactly 2 rows match, and the `AND status='accepted'` predicate makes the UPDATE unable to touch anything else — the only way `CHANGES` ≠ 2 is a concurrent actor flipping the SAME two rows in the G1→G2 window, which the sentinel makes visible, and the Task-B backup (taken adjacent, minutes before) makes recoverable. **Read both sentinels: `CHANGES=2` and `GLOBOK=2`. Either off → HALT, report both values and the per-id current status; the backup is the CEO's restore instrument, not yours.** ⚠️⚠️ **THE ID PAIR IS AN IMMUTABLE INPUT: a sentinel mismatch is NEVER resolved by editing the id list — not by dropping an id to make the count match, not by adding one, not by widening the predicate. The only response is HALT with the numbers** (the 326 immutable-lists rule).
>
> **G3 — READ-BACK.** Same invocation discipline as G1/G2 — `-bail`, `.timeout 5000`, exit-0 and empty-stderr asserted (this is a read, so add `-readonly`): `sqlite3 -bail -readonly /Users/marklehn/Developer/GitHub/lessons-forge/lessons-forge.db ".timeout 5000" "SELECT id||'|'||status||'|'||status_updated_by||'|'||status_updated_at FROM lesson_proposals WHERE id IN (232,245) ORDER BY id;"` → deposit the RAW output in your tree's evidence dir as `flip-readback.txt` (the declared deposit) — both rows `implemented|ceo|<timestamp>`, and the timestamp must differ from `2026-08-09T01:20:01Z` for the same reason G2's sentinel excludes it.
>
> **Output Receipt required** — DOC_SHA, the commit hash, the numstat pair, PRE/CHANGES/GLOBOK sentinel values, and every file deposited. **End the dev-log with `### Ledger Updates` and `#### Prompt Feedback` sections** (the both-steps form the 329 panel restored — the daemon parses these per step; a Step-1 observation with no section never reaches the ledger).
>
> **⚠️ FINAL ACTION OF THIS STEP — COMMIT YOUR DEPOSITS IN THE WORKTREE** (separate from Task F, which commits the doctrine file in the ROOT repo): stage exactly the files your Scope block below lists **for the A0 path you took** and commit with **the pathspec on the COMMIT naming exactly those paths**, then assert `git show --name-only --format= HEAD` prints exactly them (the 326/265 form). ⚠️ **Committing is what carries the captures through the teardown merge into the QA step's tree — the `deposit_uncommitted` gate FAILS an uncommitted deposit, and an unmerged capture strands QA row 6 on its crash-recovery fallback.** ⚠️ **`lessons-forge.db` and the `pre-gate2-s5-*.db` backup are deliberately ABSENT from Scope and from the commit — the DB is UNTRACKED by shop policy (plan 30); `git add`ing either would re-track it AGAINST that policy.** The DB mutation's evidence is the capture + read-back, never the DB itself.
>
> **Scope:**
> - `knowledge/development/dev-log-gate2-s5-conformance-step-1-2026-08-09.md`
> - `knowledge/development/gate2-s5-flip-rehearsal.sql`
> - `knowledge/development/gate2-s5-flip.sql`
> - `knowledge/qa/evidence/gate2-s5-conformance-2026-08-09/outside-range-ids.txt`
> - `knowledge/qa/evidence/gate2-s5-conformance-2026-08-09/flip-readback.txt`
>
> ⚠️ On the A0 state-3 recovery path ONLY, `knowledge/qa/evidence/gate2-s5-conformance-2026-08-09/resume-sweep.txt` joins this Scope list (and the commit assertion then names it too).
>
> **Deposits:**
> - `lessons-forge/knowledge/development/dev-log-gate2-s5-conformance-step-1-2026-08-09.md`
> - `lessons-forge/knowledge/development/gate2-s5-flip-rehearsal.sql`
> - `lessons-forge/knowledge/development/gate2-s5-flip.sql`
> - `lessons-forge/knowledge/qa/evidence/gate2-s5-conformance-2026-08-09/outside-range-ids.txt`
> - `lessons-forge/knowledge/qa/evidence/gate2-s5-conformance-2026-08-09/flip-readback.txt`
>
> ⚠️ `resume-sweep.txt` is produced ONLY on A0 state 3 and is named here so that path's deposit is in scope rather than a surprise. The doctrine file is modified in the ROOT repo and is NOT a deposit of this step — it is committed at Task F and verified at F2.

## STEP 2 — QA

---

> **FIRST — Deliverable Verification (Rule 8 / Rule 17).** Open the Step-1 dev-log, confirm its Output Receipt is Complete, then verify every file it claims exists and carries the described change. Table: `| Deliverable | Expected | Status (✅/❌) | Evidence |`. Any ❌ → report and HALT; make no edits yourself.
>
> **MANDATORY — Rule 20 self-check (canonical block, Checklist #4 — the exact template, NOT a paraphrase).** Run the canonical block from `/Users/marklehn/Developer/GitHub/RULE_20_SELF_CHECK_BLOCK.md` (ABSOLUTE path). Fill: `plan_slug`: `gate2-s5-conformance-2026-08-09`; `qa_report_path`: `<your-own-tree-abs>/knowledge/qa/gate2-s5-conformance-qa-2026-08-09.md`; `evidence_dir`: `<your-own-tree-abs>/knowledge/qa/evidence/gate2-s5-conformance-2026-08-09/` (derive from `pwd`, NOT hardcoded); `required_evidence_files`: `[doc-integrity.txt, db-invariants.txt, gate-neutrality.txt, pytest_targeted.txt]`. Deposit **all four** BEFORE running the block — the block `sys.exit(1)`s on any missing name, so a count that disagrees with the list false-HALTs a correct run after both writes have landed. **Include the block's literal stdout: the banner `Rule 20 — QA Self-Check Results` and the `PASSED — SELF-CHECK PASSED` line must both appear byte-exact (em-dash U+2014).** ⚠️ Those two strings are quoted deliberately — `plan_lint` check (c) does a whole-plan substring search and hard-fails if either is absent; the QA step is where they must live (the §3 prohibition is scoped to the `## Drafting Cycle` block, the RECORD).
>
> ⚠️ **REPORT STRUCTURE — the verification section never closes on its own: immediately after the verification table, write exactly `## Evidence and Narrative`**, and keep the Rule 20 stdout, the Output Receipt and `### Ledger Updates` under `##`-level headings.
>
> **Evidence rule:** RAW command output, never a summary. The doctrine file is read at its absolute root path.
>
> ⚠️ **ONE read-only DB form, used for EVERY query in this step — do not mix mechanisms:** `sqlite3 -bail -readonly "/Users/marklehn/Developer/GitHub/lessons-forge/lessons-forge.db" ".timeout 5000" "<SQL>"` — canonical absolute path (the DB does not exist in your worktree; a relative name silently creates an empty one), `-readonly` as the read-only mechanism, `.timeout 5000` per C10 (a `database is locked` in the verdict window is a HALT with the error verbatim, never an unclassified error and never a retry loop), `-bail` and an exit-0/empty-stderr assert as in Step 1.
>
> **Verification table, one row per claim (HALT on any FAIL).** ⚠️ **FAIL means an assertion this table makes is FALSE.** Two rows carry explicit branches that are NOT failures — row 6's outside-range concurrent activity and row 7's scope widening — where the assertion still holds and the observation is REPORTED with its detail. Naming such an observation is the requirement; converting it into a HALT punishes legitimate parallel work, and suppressing it is worse. Everything else that does not assert true is a HALT.
>
> **1. DOC INTEGRITY — GIT IS THE PRIMARY REFERENT, the dev-log only drift-detection.** Discover the doctrine commit INDEPENDENTLY: `git -C /Users/marklehn/Developer/GitHub log --format='%H %s' -20 -- DRAFTING_CYCLE.md`, take the newest commit whose message names the slug `gate2-s5-conformance-2026-08-09`. Then assert the three-way agreement: `git -C /Users/marklehn/Developer/GitHub show <that-commit>:DRAFTING_CYCLE.md | shasum -a 256` == `shasum -a 256` of the LIVE file == the dev-log's DOC_SHA (the dev-log is now drift-DETECTION, not the referent). ⚠️ The `-C` is load-bearing on the `show` too: the bare form fatals from your worktree cwd and `shasum` then hashes EMPTY input at pipeline exit 0 (measured) — the empty-input signature is `e3b0c442…`; treat that sha as "the show failed", never as a real value. Also: `git -C /Users/marklehn/Developer/GitHub status --porcelain -- DRAFTING_CYCLE.md` EMPTY; `git -C /Users/marklehn/Developer/GitHub show <that-commit> --name-only --format=` lists exactly `DRAFTING_CYCLE.md`. → `doc-integrity.txt`
> **2. E1 POST-CONDITION (C11).** Old opening clause (`grep -cF "Once the plan's shape is stable and before the closing walk,"` → 0 — ⚠️ the anchor contains an APOSTROPHE: quote every probe with DOUBLE quotes; single-quote concatenation silently drops it, a measured authoring near-miss) AND new text present via E1-UNIQUE probes (`grep -cF "(Proposals 232 + 245, codified 2026-08-09.)"` → 1; `grep -cF "Record the linter's exit code in the plan, with the phase at which it last ran"` → 1) AND **the SCHEDULING clause itself — the codification's load-bearing content, probed directly:** `grep -cF "before the cold panel on T2"` → **1** (E1-unique) and `grep -cF "never for the first time at deposit"` → **2** (E1 + E3, the exact both-sites count — ⚠️ NOT 1; the string lands in both edits, and expecting 1 fails a correct run) AND the preserved middle byte-intact (`grep -cF "the value is the few that are not and that no lens is looking for."` → 1). → `doc-integrity.txt`
> **3. NUMSTAT vs THE PLAN'S PIN.** Using `<that-commit>` as discovered in row 1 (one token, one referent, across rows 1/3/4): `git -C /Users/marklehn/Developer/GitHub diff <that-commit>^ <that-commit> --numstat -- DRAFTING_CYCLE.md` → exactly `3	2` (the authoring dry-run pin; the dev-log is not the referent). → `doc-integrity.txt`
> **4. VERSION + CHANGELOG INTEGRITY.** Version line reads `1.8 (2026-08-09)`; `grep -cF '1.7 (2026-08-08)'` → **1** (down from 2 — the History row, intact); the version-line assertion is probed, not eyeballed — `grep -cF '**Version:** 1.8 (2026-08-09). Amended only through the Iteration Protocol'` → 1; the new 1.8 row is the FIRST History bullet, names the slug, and does NOT contain `1.7 (2026-08-08)`; ⚠️ **and E3's SUBSTANTIVE TAIL is probed, not just its head** — `grep -cF 'runs under the live doctrine'` → **1** (E3's final clause; measured 0 in the doctrine pre-edit, so it is EARNABLE and satisfied only by this edit landing whole — without it, a History row truncated after its opening passes every other check here). ⚠️ **Do NOT substitute `the template batch) and the held bellows batch`: that phrase already occurs in the v1.7 row** (measured 1 pre-edit), so it is pre-satisfied and blind to the truncation it appears to guard; row-count via THIS exact command (method is part of the pin): `awk '/^## History/{f=1;next} f&&/^## /{f=0} f&&/^- /{n++} END{print n+0}' /Users/marklehn/Developer/GitHub/DRAFTING_CYCLE.md` → **9** (baseline 8, measured 2026-08-09). → `doc-integrity.txt`
> **5. FLIP READ-BACK.** Per-id: both 232 and 245 `status='implemented'`, `status_updated_by='ceo'`, `status_updated_at` GLOB-matching the SAME full pattern G2 asserts (`20[0-9][0-9]-[0-9][0-9]-[0-9][0-9]T[0-9][0-9]:[0-9][0-9]:[0-9][0-9]Z` — one pattern, two sites, kept identical) **AND differing from the pinned Gate-1 value `2026-08-09T01:20:01Z`** — both rows carried a GLOB-matching timestamp before this plan ran, so the shape check alone certifies nothing about this write (a status write that landed while the timestamp write did not would pass a bare GLOB unremarked). Exactly 2 rows. `category` preserved `governance_rule` on both. → `db-invariants.txt`
> **6. BLAST RADIUS AT VALUE LEVEL, INDEPENDENTLY RE-DERIVED, WITH THE VERDICT-WINDOW PARTITION (the 326 form — the DEV→QA gate is an arbitrary wall-clock window over a LIVE shared DB and a parallel terminal is demonstrably active).** Re-run the EXACT capture projection yourself (`WHERE id <= 273 AND id NOT IN (232,245) ORDER BY id`) — do NOT reuse Step 1's output as your own — and `diff` it against the Step-1 deposited `outside-range-ids.txt` (271 lines). **PARTITION every differing line: (i) any line whose id is 232 or 245 is impossible by the projection's own predicate — its appearance means the capture or the comparison is malformed → HALT; (ii) any id PRESENT in the capture but ABSENT from your re-derivation is a DELETED corpus row → HALT — a row vanishing is corpus damage, not concurrent work, and the benign-concurrency reading below does not cover it; (iii) every other differing line is CONCURRENT ACTIVITY — NAME each one in the report with its id and its before/after status, confirm none touches 232/245, and do NOT halt on it.** Naming the delta is the requirement; forcing reconciliation or punishing legitimate parallel work is not (the bookend doctrine) — but a disappearance is not parallel work. Do NOT widen the predicate to id > 273 — in-window inserts at 274+ are out of scope by construction. → `db-invariants.txt` ⚠️ **DECLARED FALLBACK (fires ONLY when the Step-1 receipt reports the capture lost to a crashed worktree — the A0 state-1 recovery):** the same-instant comparison is then unverifiable and is REPORTED as such, never silently passed; run the degraded sweep instead and label the row `DEGRADED`: (i) `SELECT COUNT(*) FROM lesson_proposals WHERE id <= 273` → 273 (no deletions); (ii) `SELECT id, status FROM lesson_proposals WHERE status_updated_at = (SELECT status_updated_at FROM lesson_proposals WHERE id = 232) ORDER BY id` → must INCLUDE `232` and `245`, both `implemented` (sqlite's `'now'` is statement-stable — verified live: a two-row UPDATE produced `COUNT(DISTINCT ts)=1` — so G2 stamps both rows identically). ⚠️ **A third id sharing that one-second instant is REPORTED, not automatically a HALT:** bulk writes at second granularity are normal in this corpus (the Gate-1 routing stamped 51 rows on one value, measured 2026-08-09), so a co-timestamped row is evidence of concurrent bulk work, not of forgery — HALT only if such a row is one this plan should never have touched *and* its status moved to `implemented`; (iii) the accepted|codify count reads 42 (Gate-1's 44 minus these two — report, with drift explained by named in-window activity or HALT).
> **7. TARGETED TESTS — AND THE PREMISE THAT MAKES THEM SUFFICIENT.** First re-check the Rule 21 premise instead of inheriting it: `find /Users/marklehn/Developer/GitHub/lessons-forge/src -name 'test_*.py'` → must list **exactly** `test_lessons_forge.py`. A second module means "targeted = full" is FALSE for this run — report it and run the whole `src/` suite instead of the single file (a scope widening is reportable, never a HALT). Then `python3 -m pytest src/test_lessons_forge.py -q` from the lessons-forge tree → **zero regressions against the recorded baseline (55 passed / 0 skipped, measured 2026-08-09 — verify the live counts; if the baseline itself moved, report the delta rather than asserting the number)**. → `pytest_targeted.txt`
> **8. GATE-NEUTRALITY, FULL-SURFACE WITH POSITIVE CONTROL (the 329 item-2 form, machinery and not just citation).** (a) `grep -rn -F 'DRAFTING_CYCLE' /Users/marklehn/Developer/GitHub/bellows --include='*.py'` → **classify EVERY hit**; expected classes (measured 2026-08-09): WARN-message citation strings + comments in `bellows/scripts/plan_lint.py`, fixture text in `bellows/tests/test_plan_lint.py`, and **zero hits in `gates.py`** — any hit outside those classes means a live coupling landed and the E3 no-gate-edit claim is FALSE → HALT. (b) POSITIVE CONTROL proving the instrument speaks (the (D) standard — a bare negative supports nothing): `grep -c -F 'plan_lint' /Users/marklehn/Developer/GitHub/bellows/scripts/plan_lint.py` → nonzero, same instrument, known-present token. (c) A zero-match `grep -c` prints `0` and exits 1 — the printed count is the assertion. ⚠️ The shim injects `--ignore-files`, so (a)'s recursive sweep skips gitignored paths — immaterial today (only `__pycache__` is ignored under bellows, measured), stated so the coverage bound is known. → `gate-neutrality.txt`
>
> **9. CONSUMER SEMANTICS — the flip's EFFECT, not just its row values (the 326 item-1(e) form, restored here at the confirming walk).** The flip moves both proposals across the terminal/non-terminal boundary, which changes what the ingest update path does to them. Verify the effect, from source and from the live corpus: (a) read `/Users/marklehn/Developer/GitHub/lessons-forge/src/lessons_forge.py` and confirm `implemented` IS in `_TERMINAL_STATUSES` and `accepted` is NOT — quote the line; (b) run `get_unclassified_entries` against the canonical DB (read-only) and confirm entries **224** and **237** are ABSENT from the work list — they were dispositioned before the flip and must stay dispositioned after it (baseline measured 2026-08-09: work list empty, both absent). **A flip that re-queued its own entries for re-proposal would be a silent regression no row-value check can see.** Paste both the source quote and the raw helper output. → `db-invariants.txt`
>
> **Then `## Evidence and Narrative`, then the Output Receipt.**
>
> ⚠️ **`### Ledger Updates` — the channel has SIX documented failure modes; all guarded here:** author via `Write`/`Edit` (the daemon parses assistant text + Write/Edit content, NOT Bash), EXACTLY ONCE, complete, never re-edit; `##`-level scope after `## Evidence and Narrative`; substance INSIDE the section; end with a blank line after the last subsection; one row per bullet with no second physical line (dup-append and null-parse are both known modes — the Planner lands rows manually at wrap if the parse fails, dup-guarded).
>
> **`#### Forward Register`: the word `NONE`** (the 326 form — "empty" is ambiguous between omitting the heading and emitting a heading with no rows, and the second IS the null-parse mode named above). No Forward rows are owed at authoring; a genuinely new item discovered at run time replaces `NONE` and follows the one-row-per-bullet contiguous form.
>
> **⚠️ FINAL ACTION — COMMIT YOUR DEPOSITS**, pathspec on the COMMIT naming exactly the Scope files, then assert `git show --name-only --format= HEAD` prints exactly them. **`lessons-forge.db` is never staged** (untracked by shop policy — plan 30).
>
> **Scope:**
> - `knowledge/qa/gate2-s5-conformance-qa-2026-08-09.md`
> - `knowledge/qa/evidence/gate2-s5-conformance-2026-08-09/doc-integrity.txt`
> - `knowledge/qa/evidence/gate2-s5-conformance-2026-08-09/db-invariants.txt`
> - `knowledge/qa/evidence/gate2-s5-conformance-2026-08-09/gate-neutrality.txt`
> - `knowledge/qa/evidence/gate2-s5-conformance-2026-08-09/pytest_targeted.txt`
>
> **Deposits:**
> - `lessons-forge/knowledge/qa/gate2-s5-conformance-qa-2026-08-09.md`
> - `lessons-forge/knowledge/qa/evidence/gate2-s5-conformance-2026-08-09/doc-integrity.txt`
> - `lessons-forge/knowledge/qa/evidence/gate2-s5-conformance-2026-08-09/db-invariants.txt`
> - `lessons-forge/knowledge/qa/evidence/gate2-s5-conformance-2026-08-09/gate-neutrality.txt`
> - `lessons-forge/knowledge/qa/evidence/gate2-s5-conformance-2026-08-09/pytest_targeted.txt`

---

## Drafting Cycle

**Tier:** T2 — trigger fired: T-6 (governance surface: one doctrine file + the proposal corpus). Clone lineage: machinery cloned from executable-298 (the v1.4 corpus-path batch, 22 edits → scaled to 3), diffed against BOTH newest same-class siblings — executable-329 for the doctrine half (slug re-entry, absolute operands, verdict-window posture, gate-neutrality QA) and executable-326 for the flip half (Scope blocks, deposit-commit-as-final-action, Test Scope header, verdict-window partition, immutable-lists rule).

**Walks:** 6 (lenses 1–4 per walk; ACID runs apart, its own phase). Walks 3–6 are confirming passes — 8, 3, 3, 5 folded; none dry. Each rotated to a region no prior confirming pass had read: Step-1 tasks (w3), the record and G-tasks (w4), the framing prose and AFTER texts (w5), the QA verification table AS A SYSTEM (w6).
- Weak spots:          w1 5 folded (HIGH: QA row-2 probe string also lands in E3 — expected count 1 fails a correct run; probe swapped to an E1-unique literal. Plus: state-2 backup-may-exist, apostrophe-quoting trap ×2 sites, id substitution, post-commit-dirty reporting); w2 dry (rotated to the E1/E3 doctrine text itself: T2 bound confirmed stricter-not-relaxed, recording-placement freedom confirmed deliberate with this plan as the reference implementation, entry quotes verified against the DB rows); **w3 (confirming) 2 folded — NOT dry**: the Task-B backup post-condition was file-presence only and could not fail, leaving the flip's ONLY recovery instrument unproven (now a restorability assert against the found path); QA row 3 had no evidence destination, a half-state from the seat-4 fold that assigned destinations to its siblings. **w4 (confirming 2) 2 folded — NOT dry**: G3 carried no `sqlite3` invocation form at all, so Task G's `-bail`/exit-0/stderr mandate had no landing site on the read-back (a half-state — the seat-5 finding that named it was folded on its `$SCRATCH` half only); and the capture's post-commit line-count assert had no meaning-on-mismatch clause despite w3 recording one as folded (see the retraction under Vulnerabilities). **w6 3 folded**: the QA table read AS A SET exposed asymmetric verification depth — E1 carried four content probes while E3, which becomes permanent changelog, was checked only for placement/slug/absence, so a truncated History row passed every row (tail probe added, and the FIRST tail probe chosen was itself pre-satisfied by the v1.7 row — caught by the close-discipline grep and replaced with an earnable one); row 4 asserted the version line with no probe command among three that had one; and rows 1/3 named the same commit by two tokens. w5 dry (E1/E3 AFTER text re-read as doctrine prose against the live §5 and the live §2 precedent for uncited in-doctrine measurements — no weak spot; every Why-paragraph claim re-traced to its DB row or the packet).
- Destruction:         w1 1 folded (E0 denylist re-scoped to the enumerated governance doctrine set — the all-root-md form false-HALTs under normal parallel-terminal topology; commit path-scoping + F2 name-only carry the guard); w2 dry (erased-phrase sweep re-confirmed §5-only; no obligation relaxed; unmechanized exit-code duty consistent with warn-first, the row-40 lineage); **w3 1 folded**: row 6's partition read EVERY outside-range difference as benign concurrent work — a row DISAPPEARING is corpus damage, not parallel work, and had no HALT route (deletion branch added); **w4 dry**; **w5 dry** (the rotated prose region grants no authority and relaxes nothing; E1 re-confirmed strictly additive plus the T2 tightening — a fourth independent reading); **w6 1 folded**: the flip is NOT status-cosmetic — `accepted` is non-terminal and `implemented` IS terminal, so after this flip a future edit to entries 224/237 will be FLAGGED rather than staled and will no longer re-queue for re-proposal. Intended (that is what codified means) but a live behavioural change to a real consumer, previously neither stated nor verified; now named in Scope and checked at QA row 9 (re-examined: the G3 and capture-assert folds add obligations only; the capture-vs-re-derivation comparison re-verified sound — outside-range rows are untouched by the flip, so pre-flip capture and post-flip re-read agree by construction).
- Vulnerabilities:     w1 2 folded (A0 none-state else-HALT; timestamp pattern unified across the flip and its QA row) + 3 premises EXECUTED live: the suite passes in a DB-less worktree (55 passed), the in-statement timestamp emits the exact matched form, the gate module has zero doctrine-file references; w2 dry (one anomaly INVESTIGATED to ground: the absent fold-as-last-event warning was the battery running before the ACID record line was written — sequencing, not a checker defect; it now fires earned. Close-state block sketched against the self-check's four checks; probe uniqueness re-verified across edits); **w3 2 folded**: the read-only mechanism was named two incompatible ways after the seat-5 fold (one form now fixed for the whole QA step); ~~the capture's line-count assert is POST-commit, so its meaning on mismatch is now stated~~ — ⚠️ **RETRACTED: this second attestation was FALSE when written.** The fold was recorded in the Cycle Log and the walk register but never landed in the artifact; w4's audit of the record against the artifact caught it and landed it there. Struck rather than silently corrected, per §2.7 — a later reader must see that the original claim was not honest, not a tidied version of it. **w4 (confirming 2) 0 folded — dry** (the G3 and capture-meaning gaps below are w4's lens-1 findings, not this lens's). **w5 1 folded**: Rule 21's "targeted = full" was an unverified PREMISE about the repo's shape — a second test module added before dispatch would silently make the targeted run a partial one, and nothing checked it. QA row 7 now re-derives the premise (module enumeration) before running, widening scope by report rather than by HALT. **w6 dry** (every row's command form re-checked as a set: `-C` coverage, double-quoted apostrophe probes, the pinned awk method, the single read-only DB form, `find` over globs — uniform).
- Integration-record:  w1 2 folded (E3 inheritor list completed to the v1.7 precedent form — held bellows batch added; the two SQL-script deposits declared data-not-code to close a false-HALT reading); w2 dry (DC/register fold counts reconciled 10+3; naming, priority, qa_steps, residue-draft conventions all matched to 298/329 precedent); **w3 3 folded**: the Conformance paragraph reported three standing warnings after one had cleared (its SECOND decay), and two absolutist claims — environment fact 2 and C7's shell-variable boast — were falsified by the plan's own later folds and are now scoped. **w5 2 folded**: `Depends on:` omitted **executable-326** — the plan that actually WROTE `accepted|codify` on these rows and whose transaction produced `2026-08-09T01:20:01Z`, the exact value G2's sentinel and QA row 5 pin as the prior-value exclusion; a reader tracing that constant's provenance had no pointer, and the packet (a decision record no step reads) was standing in for the executing plan. Also the same measurement was named two ways across sites ("55 collected" in Rule 21, "55 passed" in QA row 7) — now stated once as collected/passed/skipped. **w6 1 folded**: the newest same-class sibling (326) carried a CONSUMER check — does the corpus's own work-list helper still behave correctly after the status change — and this clone dropped it. Restored as QA row 9, with the live baseline measured at authoring (work list empty; entries 224/237 absent both before and required absent after). The table's blanket HALT-on-any-FAIL header was also reconciled with the two rows that legitimately report-without-halting. **w4 1 folded**: the w3 Vulnerabilities attestation was FALSE — a fold recorded in both the Cycle Log and the walk register that never landed in the artifact. Retracted in place with a strike per §2.7 rather than quietly corrected, and the missing obligation landed. ⚠️ Mechanized forward: every future walk closes by grepping each claimed fold's own text against the artifact before the record line is written — the record decays while the artifact converges, and only the artifact was being checked.
- ACID:                a1 3 folded (5.1 the crashed-worktree capture loss made state-1 recovery + QA row 6 jointly unsatisfiable → declared DEGRADED fallback with the statement-stable-timestamp sweep, verified live; 5.2 the F→G2 unearned-History window recorded as C13 with state 2 its designated completion; 5.3 serialized-dispatch assumption surfaced as C14 with its non-dependent guards named); **a2 2 folded** — ACID had not run since a1, which was BEFORE the panel, so it had never read the current schedule (the G-task restructure, the backup assert, the consumer row all postdate it): 5.1/5.4 the `BK=2` restorability assert lived inside Task B, and A0 state 2 REUSES a backup instead of running Task B — so the recovery instrument went unverified on precisely the recovery path, the one most likely to need it (assert now mandated on every path reaching the flip, both enumerated); and its §2.7 sweep exposed C4's absolutist "never earlier" wording, which the legitimate reuse path falsifies (rewritten: verification, not recency, is what makes the backup an inverse). **a3 DRY** — the closing ACID confirmation re-derived every path that reaches the flip (two do; three cannot) and confirmed each carries the guard, then swept the ledger for further absolutist wording: C1's "never a line number" and C2's "never replace-all" are true on every path and stand. No finding.

**Conformance (§5, run at shape-stability — the practice this plan codifies, applied to itself):** first run at draft v1 → **exit 1**: one FAIL (the QA self-check banner pair paraphrased where the whole-plan check requires the literals) + deposit-path-form warnings ×9 + input-path warnings ×2 — all folded. Re-runs after every culmination since; latest → **exit 0**, TWO warnings standing: (1) the fold-as-last-event warning — clears only when a confirming pass runs dry, and the confirming pass run so far folded eight, so it is still owed; (2) the ONE declared known-benign input-path warning on the conditional recovery-sweep artifact, expected to persist at deposit. The missing-cold-panel-line warning CLEARED EARNED when the panel record below was written. ⚠️ This paragraph has now decayed against its own artifact twice — first mis-labelling the panel warning as a walk-line warning, then reporting three standing after one had cleared. Both were caught by a lens reading the record against the tool output, not by the tool; the record-without-prevent asymmetry, observed on the very paragraph that reports mechanical conformance.

**Cut pass (post-walk-6, CEO-directed):** the six-walk yield was re-classified by ORIGIN and the result redirected the work: **14 of the 19 confirming-pass findings were defects this cycle's own folds created; only 5 pre-existed** (walk 4 was 0-for-3 pre-existing). That is §2.8's oscillation signal, not thoroughness, and it retired the "unexamined region" framing that had been justifying each pass (LESSONS 227). Four deletion candidates were then tested against live data per §2.7 rather than argued — **three FAILED their subsumption test and were KEPT**: G1's rehearsal is not subsumed by the `CHANGES` sentinel (constructed: one row pre-flipped → the flip still committed the other and reported `CHANGES=1` at exit 0 — the rehearsal PREVENTS, the sentinel only REPORTS); A0's unexplained-backup state is not subsumed by the none-match HALT (without it that state matches `fresh` and proceeds); the backup restorability assert guards the only inverse the flip has. ⚠️ **The size ratio that motivated the cut was itself a bad measurement and is retracted:** per-edit line count ignores that fixed machinery — the A0 state machine, the ledger, the QA framing — does not scale down with edit count; a two-row flip needs the same transaction discipline as a sixteen-row one. What was genuinely cuttable was **record living in the step bodies** (panel-seat attributions, provenance narration, deleted-machinery archaeology), which §3 assigns to this log and the walk register, not to instructions an agent executes. That trim landed with every guard asserted present afterward: 14 ledger rows, 9 QA rows, 14 named guards, all retained.

**Cold panel (T2):** five seats, run sequentially under v1.7's §2.6 registry, author-verified and folded between seats; CEO autonomous-grant waiver recorded (the session-29/320 precedent — one directive, all five seats). Per-seat yield and brief attribution: seat 1 weak-spots + evidence-attack brief, 5 findings/1 HIGH (a QA probe string shared with another edit would have failed a correct run); seat 2 destruction + clone-diff brief, 9 findings/1 HIGH (a cited sibling's gate-neutrality machinery carried as citation without its substance); seat 3 vulnerabilities, register-plain, 7 findings/3 HIGH, all three MEASURED by running the plan's own commands against scratch fixtures (an un-bailed script committing a flip outside its transaction with both sentinels green; two wrong-repo command forms); seat 4 integration + clone-diff-record brief, 13 findings/3 HIGH (three mandated authoring conventions absent — including the one this plan's own source lesson names — plus a newer same-class sibling never diffed); seat 5 ACID + system brief, 10 findings/2 HIGH, aimed at fold interactions (an undefined shell variable introduced by one fold and forbidden by the plan's own environment facts; a recovery branch that voided a QA row's independence). **Register-plain seat 3 delivered the highest HIGH density — the adds-not-monopolizes parity holds for a third consecutive panel.**

**Closing:** ACID a3 ran DRY and is the last event — the closing lens pass, earned rather than authored. ⚠️ The cut pass exposed that ACID had not run since a1, which predated the panel: it had never read the current schedule at all. a2 then found the recovery instrument unverified on the recovery path, and a3 confirmed the fix by enumerating every path that reaches the flip. Before that, a closing walk over the whole artifact returned **one non-material fold** — a single dangling provenance annotation the cut had missed at one site — and **no material finding**, so the walk is not re-opened (§2). Last event = lens pass; fold-and-deposit exactly once. ⚠️ **The confirming-pass history is recorded honestly rather than smoothed:** walks 3–6 folded 8 / 3 / 3 / 5 and none was dry, and re-classifying those 19 findings by ORIGIN is what ended the loop — 14 were this cycle's own fold damage, 5 pre-existed (LESSONS 227). The passes were not converging on the artifact; they were converging on their own repairs. ⚠️ **Freeze-time re-verification, all re-derived against the live file rather than inherited:** authoring SHA unchanged (`1558110c…`), dry-run numstat `3 2`, `1.7 (2026-08-08)` 2 → 1, old §5 clause absent post-edit, History bullets 8 → 9, E3 tail probe measured 0 pre-edit (earnable). Deposit-state lint: **exit 0 with exactly the ONE declared known-benign warning** — the closing warning cleared EARNED when a3 came back dry, never by rewording. Fold-and-deposit exactly once; this draft is READY.
