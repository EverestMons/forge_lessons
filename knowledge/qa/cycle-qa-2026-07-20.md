# Cycle QA — 2026-07-20

## Verification Table

| # | Claim | Verdict | DB Source |
|---|---|---|---|
| 1 | Full test suite passes (baseline 55) | **PASS** — 55 collected, 55 passed in 0.13s. Prior QA (2026-07-17) baseline: 55. 0 regressions. | N/A (pytest) |
| 2 | `get_unclassified_entries(conn)` returns `[]` | **PASS** — empty result. No `### Deferred entries (CEO-approved)` heading in Step 1 deposit. | canonical (read-only URI) |
| 3a | Dangling proposals = 0 | **PASS** — `0` | canonical |
| 3b | Invalid category = 0 | **PASS** — `0` | canonical |
| 3c | Invalid confidence = 0 | **PASS** — `0` | canonical |
| 3d | Cycle proposals 155–159 all have `route IS NULL` | **PASS** — all 5 rows show NULL route | canonical |
| 3e | Non-ambiguous cycle proposals have `target_layer IS NOT NULL` | **PASS** — 0 rows with NULL target_layer | canonical |
| 3f | Non-ambiguous cycle proposals have `target_artifact IS NOT NULL` | **PASS** — 0 rows with NULL target_artifact | canonical |
| 3g | governance_rule proposals carry `target_layer='governance'` + `target_artifact='PLANNER_TEMPLATE.md'` | **PASS** — all 5 confirmed | canonical |
| 4 | Plan-204 hash-trap held | **PASS** — see detail below | canonical |
| 5a | Report exists, proposal count matches DB | **PASS** — 50 lines, 5 proposals in report, 5 in DB | worktree file + canonical |
| 5b | Zero `- **Route:**` lines in report | **PASS** — `grep` count = 0 | worktree file |
| 5c | Zero `Recently-implemented overlap:` lines in report | **PASS** — `grep` count = 0 | worktree file |
| 5d | `detect_recently_implemented_overlaps` absent from `src/` | **PASS** — `grep -r` exit 1 (no matches) | worktree src/ |
| 6 | No schema drift | **PASS** — `.schema` output matches `src/db.py` DDL for both tables | canonical |
| 7 | PLANNER_TEMPLATE.md unchanged | **PASS** — `git -C /Users/marklehn/Developer/GitHub diff --exit-code -- PLANNER_TEMPLATE.md` returned exit 0 | root repo |
| 8 | Post-cycle DB counts | **PASS** — 151 entries (146 + 5, expected). See detail below. | canonical |

## Row 4 Detail — Plan-204 Hash-Trap Watch

**Pre-cycle baseline (from Step 1 dev log):**

| Metric | Pre-Cycle |
|---|---|
| Proposals by status | implemented=105, reference=3, rejected=15, stale=3, superseded=28 (total 154) |
| Proposals by category | duplicate=19, governance_rule=112, instrumentation=8, narrative=5, structural=10 (total 154) |
| Total entries | 146 |
| Entry 146 content_hash | `964c278b05146acacf084c625eb9250bc6db4b86ecdef76b5d0634553b070076` |

**Post-cycle (canonical DB, read now):**

| Metric | Post-Cycle |
|---|---|
| Proposals by status | implemented=105, proposed=5, reference=3, rejected=15, stale=3, superseded=28 (total 159) |
| Proposals by category | duplicate=19, governance_rule=117, instrumentation=8, narrative=5, structural=10 (total 159) |
| Total entries | 151 |
| Entry 146 content_hash | `964c278b05146acacf084c625eb9250bc6db4b86ecdef76b5d0634553b070076` |

**Diff:**
- `stale` count unchanged (3 → 3): no proposal demoted. PASS.
- No terminal-status proposal moved off its status. The only deltas are +5 `proposed` (new cycle) and +5 `governance_rule` (new cycle). PASS.
- Entry 146 `content_hash` unchanged despite trailing `---` separator added above the 07-20 entry. The plan-204 normalization fix held. PASS.
- Step 1 recorded: `updated_count` = 0, `terminal_proposals_flagged` = []. PASS.

## Row 8 Detail — Post-Cycle DB Counts

| Metric | Value |
|---|---|
| Total entries | 151 (146 pre + 5 ingested; includes ~57 orphan rows from historically-reworded headings) |
| Proposals by status | implemented: 105, proposed: 5, reference: 3, rejected: 15, stale: 3, superseded: 28 |
| Proposals by category | duplicate: 19, governance_rule: 117, instrumentation: 8, narrative: 5, structural: 10 |
| Total proposals | 159 |

## Raw Full-Suite Tail

```
src/test_lessons_forge.py::test_terminal_status_guard[implemented] PASSED [ 90%]
src/test_lessons_forge.py::test_terminal_status_guard[reference] PASSED  [ 92%]
src/test_lessons_forge.py::test_terminal_status_guard[rejected] PASSED   [ 94%]
src/test_lessons_forge.py::test_terminal_status_guard[superseded] PASSED [ 96%]
src/test_lessons_forge.py::test_nonterminal_still_stales PASSED          [ 98%]
src/test_lessons_forge.py::test_trailing_separator_only_delta_zero_stales PASSED [100%]

============================== 55 passed in 0.13s ==============================
```

## Raw Invariant Queries

```
sqlite3 "file:/Users/marklehn/Developer/GitHub/lessons-forge/lessons-forge.db?mode=ro"

-- Dangling proposals
SELECT 'dangling_proposals', COUNT(*) FROM lesson_proposals WHERE entry_id NOT IN (SELECT id FROM lesson_entries);
dangling_proposals|0

-- Invalid category
SELECT 'invalid_category', COUNT(*) FROM lesson_proposals WHERE category NOT IN ('structural','instrumentation','governance_rule','language','narrative','duplicate');
invalid_category|0

-- Invalid confidence
SELECT 'invalid_confidence', COUNT(*) FROM lesson_proposals WHERE confidence NOT IN ('low','medium','high');
invalid_confidence|0

-- Cycle proposals route
SELECT 'cycle_proposals_with_route', COUNT(*) FROM lesson_proposals WHERE id IN (155,156,157,158,159) AND route IS NOT NULL;
cycle_proposals_with_route|0

-- Null target fields (non-ambiguous)
SELECT 'null_target_layer', COUNT(*) FROM lesson_proposals WHERE id IN (155,156,157,158,159) AND status != 'ambiguous' AND target_layer IS NULL;
null_target_layer|0
SELECT 'null_target_artifact', COUNT(*) FROM lesson_proposals WHERE id IN (155,156,157,158,159) AND status != 'ambiguous' AND target_artifact IS NULL;
null_target_artifact|0

-- governance_rule target fields
SELECT 'gov_rule_target_layer', target_layer FROM lesson_proposals WHERE id IN (155,156,157,158,159) AND category='governance_rule' GROUP BY target_layer;
gov_rule_target_layer|governance
SELECT 'gov_rule_target_artifact', target_artifact FROM lesson_proposals WHERE id IN (155,156,157,158,159) AND category='governance_rule' GROUP BY target_artifact;
gov_rule_target_artifact|PLANNER_TEMPLATE.md
```

## Raw Unclassified-Entries Query

```
sqlite3 "file:/Users/marklehn/Developer/GitHub/lessons-forge/lessons-forge.db?mode=ro" "SELECT id, source_heading FROM lesson_entries WHERE id NOT IN (SELECT DISTINCT entry_id FROM lesson_proposals WHERE status != 'stale');"
(empty — no rows returned)
```

## Raw Schema Comparison

**lesson_entries — canonical DB:**
```sql
CREATE TABLE lesson_entries (
            id              INTEGER PRIMARY KEY AUTOINCREMENT,
            source_file     TEXT    NOT NULL,
            source_heading  TEXT    NOT NULL,
            entry_date      TEXT,
            raw_content     TEXT    NOT NULL,
            content_hash    TEXT    NOT NULL,
            tags            TEXT,
            ingested_at     TEXT    NOT NULL,
            UNIQUE(source_file, source_heading)
        );
CREATE INDEX idx_lesson_entries_source ON lesson_entries(source_file);
CREATE INDEX idx_lesson_entries_date ON lesson_entries(entry_date);
CREATE INDEX idx_lesson_entries_hash ON lesson_entries(content_hash);
```

**lesson_proposals — canonical DB:**
```sql
CREATE TABLE IF NOT EXISTS "lesson_proposals" (
                id                  INTEGER PRIMARY KEY AUTOINCREMENT,
                entry_id            INTEGER NOT NULL REFERENCES lesson_entries(id) ON DELETE CASCADE,
                category            TEXT    NOT NULL CHECK(category IN ('structural', 'instrumentation', 'governance_rule', 'language', 'narrative', 'duplicate')),
                subcategory         TEXT,
                suggested_action    TEXT    NOT NULL,
                reasoning           TEXT    NOT NULL,
                confidence          TEXT    NOT NULL CHECK(confidence IN ('low', 'medium', 'high')),
                status              TEXT    NOT NULL DEFAULT 'proposed' CHECK(status IN ('proposed', 'accepted', 'rejected', 'ambiguous', 'stale', 'superseded', 'implemented', 'reference')),
                target_layer        TEXT    CHECK(target_layer IS NULL OR target_layer IN ('structure', 'governance', 'language', 'none')),
                target_artifact     TEXT,
                duplicate_of        INTEGER,
                route               TEXT    CHECK(route IS NULL OR route IN ('codify', 'backlog', 'reference')),
                proposed_at         TEXT    NOT NULL,
                status_updated_at   TEXT,
                status_updated_by   TEXT    CHECK(status_updated_by IS NULL OR status_updated_by IN ('planner', 'ceo', 'auto'))
            );
CREATE INDEX idx_lesson_proposals_entry ON lesson_proposals(entry_id);
CREATE INDEX idx_lesson_proposals_status ON lesson_proposals(status);
CREATE INDEX idx_lesson_proposals_category ON lesson_proposals(category);
```

Both match `src/db.py` DDL — columns, constraints, indexes all identical. No schema drift.

## Rule 20 — QA Self-Check Results

| # | Rule 20 Requirement | Status |
|---|---|---|
| 1 | Every verification row has DB-source column | All 18 rows specify source |
| 2 | SQL/PRAGMA rows state which DB they ran against | All queries use canonical read-only URI |
| 3 | RAW output deposited, not summaries | Full suite tail, raw counts, raw query results, raw schema |
| 4 | Verification table present with pass/fail per claim | 18 rows, all PASS |

**PASSED — SELF-CHECK PASSED**

---

### Output Receipt

| Field | Value |
|---|---|
| Step | 3 — QA |
| Plan | Lessons Forge Cycle 2026-07-20 |
| Status | **Complete** |
| Verification rows | 18 (all PASS) |
| Test baseline | 55 (matches prior QA 2026-07-17) |
| Test result | 55 passed, 0 failed, 0 regressions |
| Hash-trap | Held — entry 146 hash unchanged, updated_count=0, terminal_proposals_flagged=[] |
| Schema drift | None |
| PLANNER_TEMPLATE.md | Unchanged (exit 0) |
| Post-cycle entries | 151 |
| Post-cycle proposals | 159 |

### Ledger Updates

#### Project Status

Cycle 2026-07-20 complete: the 07-19/07-20 batch (5 entries) ingested and classified as governance_rule/high (proposals 155–159), lessons report deposited, corpus integrity held (plan-204 hash-trap passed, plan-207 retirement intact, no schema drift, PLANNER_TEMPLATE.md unchanged). Gate 1 route disposition pending, including the Drafting-Cycle amendment cluster (proposals 157–159 targeting `## The Drafting Cycle` in PLANNER_TEMPLATE.md) and the three-way diminishing-returns/iterate-to-dry/one-pass-per-lens tension surfaced in the classifications summary.

#### Prompt Feedback

**2026-07-20 — Lessons Forge Cycle 2026-07-20 (QA Step 3)**

1. The verification table format with DB-source column per row makes evidence provenance unambiguous — every assertion traces to either the canonical DB (read-only URI), pytest, or the root repo git state.
2. The hash-trap watch (row 4) is straightforward when the Step 1 dev log records a proper pre-cycle baseline — the diff is mechanical. The plan's instruction to halt if that baseline is missing from Step 1's deposit is a real guard, not ceremony.
3. Schema comparison between `.schema` output and `src/db.py` DDL is an effective no-drift check — both tables matched column-for-column with identical constraints and indexes.
4. The PLANNER_TEMPLATE.md check requires explicit `git -C <root>` targeting because the file lives in the root repo, not the submodule — a bare `git diff` from the worktree would pass vacuously.
5. Test baseline reconciliation via `--collect-only` against the prior QA report's count (both 55) confirms no tests were added or removed this cycle, consistent with a verification-only QA step.
