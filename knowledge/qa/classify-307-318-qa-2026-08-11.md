# QA Report — classify-307-318-2026-08-11

## Deliverable Verification (Rule 8/17)

| # | Deliverable | Criterion | Status |
|---|---|---|---|
| 1 | Per-entry coverage | Every id 307–318 has ≥1 proposal | ✅ |
| 2 | Unclassified = 0 + positive control | `get_unclassified_entries` empty on live; returns the twelve on pre-classify backup | ✅ |
| 3 | INSERT-only | `COUNT(*) WHERE id<=314 AND status<>'proposed'` unchanged (314→314); total 326 = 314+12; all new rows status=proposed, route=NULL | ✅ |
| 4 | Report | Exists, surfaces 12 proposals, spot-check 307/317/318 match DB | ✅ |
| 5 | Flag coverage | All 12 carry Flag (G) with named observable; Flag (D) on {310,312,313,315} cite codifying location | ✅ |
| 6 | Pytest vs baseline | 55/0 → 55/0, delta 0/0 | ✅ |

## Rule 20 Self-Check

```
============================================================
Rule 20 — QA Self-Check Results
============================================================
PASSED — SELF-CHECK PASSED — all evidence files present, no hedging keywords found.
Evidence folder: /Users/marklehn/Developer/GitHub/lessons-forge/.bellows-worktrees/349/knowledge/qa/evidence/classify-307-318-2026-08-11/
Files verified: 2
```

## Evidence and Narrative

### QA check 1 — Per-entry coverage re-derived

Every entry id 307–318 has exactly one proposal (ids 315–326). Full listing in `qa-db-checks.txt`.

### QA check 2 — Unclassified entries + positive control

`get_unclassified_entries` against the live DB returns an empty list. The same helper run against the pre-classify backup (`pre-classify-s36-20260811_235358.db` via `?immutable=1`) returns exactly `[307..318]` — the instrument is proven to detect unclassified entries against a known-positive state. The pre-INGEST backup is NOT a valid control source (it returns 0, not 12, because those entries did not exist yet); only the pre-CLASSIFY backup is valid.

### QA check 3 — INSERT-only proven

`COUNT(*) FROM lesson_proposals WHERE id<=314 AND status<>'proposed'` = 314, unchanged from Step 1's pre-capture value. No existing row was touched. Total proposals = 326 = 314 + 12. All 12 new rows (315–326) have `status='proposed'` and `route=NULL`.

### QA check 4 — Report verification

`reports/lessons-report-2026-08-11.md` exists and surfaces 12 proposals. Spot-checked entries 307 (instrumentation/DRAFTING_CYCLE.md), 317 (governance_rule/PLANNER_TEMPLATE.md — edit-anchor uniqueness), and 318 (governance_rule/PLANNER_TEMPLATE.md — four doctrine defects): all three match the DB `suggested_action` content.

### QA check 5 — Flag coverage

All 12 proposals carry a Flag (G) reading with a named observable. Flag (D) entries: {310, 312, 313, 315}. The required minimum {312, 313, 315} cite explicit v2.x versions (v2.4, v2.3, v2.4 respectively). Entry 310 cites DRAFTING_CYCLE.md §2.6 as its codifying location — a section reference rather than a version number, but the classification is honest and the citation is specific.

### QA check 6 — Pytest (Rule 21)

Rule 21 premise re-derived: single test module (`src/test_lessons_forge.py`). Pytest: 55 passed, 0 failed in 0.17s. Baseline: 55/0. Delta: 0/0.

## Receipt

- All 6 QA checks passed
- DB state verified read-only (all queries via `?immutable=1`)
- Evidence deposited before Rule 20 self-check

### Ledger Updates

#### Forward Register

NONE.
