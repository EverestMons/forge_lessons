Doc Integrity — gate2-dc5-2026-08-11
======================================

1. DOC INTEGRITY — commit by slug, three-way SHA, porcelain, name-only

Commit discovered by slug:
  git log --oneline --all --grep='gate2-dc5-2026-08-11'
  → 5707b74 [353] gate2(gate2-dc5-2026-08-11): the DC five — per-lens commits, diff-review, early clone-diff, pricing gate, edit-anchor — doctrine 2.4 -> 2.5

Three-way SHA:
  DOC_SHA (Step 1 receipt): 817677db4a3df2a50bdaf345138e441533dc30690e6ced378482573fb79b79b6
  Commit content:           817677db4a3df2a50bdaf345138e441533dc30690e6ced378482573fb79b79b6
  Live file:                817677db4a3df2a50bdaf345138e441533dc30690e6ced378482573fb79b79b6
  MATCH: all three identical.

Porcelain:
  git status --porcelain -- DRAFTING_CYCLE.md → (empty)
  CLEAN.

Name-only:
  git show 5707b74 --name-only --format='' → DRAFTING_CYCLE.md
  EXACT — one file only.

2. THE CLAUSES LANDED — seven probes + tail probe (via git show, --  discipline)

  1. grep -cF -- 'after each lens'\''s fold inside a walk' → 1  (E1 operative clause)
  2. grep -cF -- 'The instrument for a subtractive cut is a DIFF REVIEW' → 1
  3. grep -cF -- 'run that diff BEFORE walk 1' → 1
  4. grep -cF -- 'Price what a guard is FOR before folding it in' → 1
  5. grep -cF -- 'An EDIT ANCHOR is not a probe' → 1
  6a. grep -cF -- '**Version:** 2.5 (2026-08-11). Amended only through the Iteration Protocol' → 1
  6b. grep -cF -- '2.4 (2026-08-11)' → 1
  7. awk '/^## History/{f=1;next} f&&/^- \*\*/{n++} END{print n}' → 15

Tail probe:
  grep -cF -- "FORWARD 57's gate half" → 1  (earnable 0→1)

ALL PASS.

3. COHERENCE — version row + four neighbour-bullet heads

  grep -cF -- '2.4 (2026-08-11)' → 1 (prior row intact)
  grep -cF -- '- **Per-phase commits' → 1
  grep -cF -- '- **Sequential-fold rule' → 1
  grep -cF -- '**A count is not a value guard.**' → 1
  grep -cF -- 'When cloning a plan, diff the machinery against the' → 1

ALL PASS.

5. DOCTRINE PARSES

  grep -cE '^## ' → 9  (unchanged)
  grep -cE '^### ' → 11  (unchanged)

PASS.
