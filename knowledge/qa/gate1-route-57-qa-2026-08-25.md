# QA Report — gate1-route-57 (Plan 536, Step 2)

**Date:** 2026-08-25
**Plan slug:** gate1-route-57
**ROOT:** `/Users/marklehn/Developer/GitHub/lessons-forge/.bellows-worktrees/536`

## Q0 — Re-pin

**Q0.1:** `git log -1 --oneline` over the three evidence files returns `b2fccd2 [536] Step 1 — gate1 route assignment 354-410 (23 codify, 23 reject, 6 ref, 3 backlog; 378/389 held)` — Step 1's commit is the newest.

**Q0.2:** `SELECT COUNT(*) FROM lesson_proposals WHERE status='proposed' AND id BETWEEN 354 AND 410;` returns **2**. Confirmed.

## Verification Table

| Row | Claim | Status | Evidence |
|-----|-------|--------|----------|
| 1(a) | `accepted\|codify` id SET matches CODIFY-23 | ✅ | Symmetric difference: `set()` (empty). DB: `354,355,361,364,365,366,368,370,372,373,379,383,384,385,386,387,390,393,395,401,406,407,409`. → `routing-verification.txt` |
| 1(b) | `rejected` (route IS NULL) id SET matches REJECT-23 | ✅ | Symmetric difference: `set()` (empty). DB: `356,357,358,359,362,363,367,374,375,376,377,380,381,382,392,394,396,397,398,399,400,404,410`. → `routing-verification.txt` |
| 1(c) | `reference\|reference` SET matches REFREF-6; `reference\|backlog` SET matches REFBACK-3 | ✅ | Symmetric differences: both `set()` (empty). REFREF DB: `388,391,402,403,405,408`. REFBACK DB: `360,369,371`. → `routing-verification.txt` |
| 1(d) | All 55 carry `status_updated_by='planner'` and `status_updated_at='2026-08-25T22:58:03Z'` | ✅ | 55 matching rows, 0 mismatches. → `routing-verification.txt` |
| 2 | Held rows 378/389 did not move | ✅ | `378\|proposed\|-\|-\|-\|PLANNER_TEMPLATE.md` and `389\|proposed\|-\|-\|-\|-` — all six columns byte-identical between pre-dump and live DB. → `routing-verification.txt` |
| 3 | Untouched population — no foreign ids, no target_artifact changes | ✅ | 55 changed rows, 112 changed lines. Row 378 in diff block but content unchanged. Foreign ids: `set()` (none). Target artifact changes: `set()` (none). → `diff-audit.txt` |
| 4 | Dumps are the committed ones | ✅ | `git show HEAD:<path>` matches working-tree copy byte-for-byte for both pre-dump and post-dump. → `diff-audit.txt` |
| 5 | Full suite passes | ✅ | `63 passed in 0.15s`. Baseline: 63. Delta: 0. → `pytest_full.txt` |
| 6 | DB was not committed | ✅ | No `lessons-forge.db` in Step 1 commit. `git ls-files --error-unmatch lessons-forge.db` errors — DB remains untracked. → `routing-verification.txt` |
| 7 | Nothing else moved | ✅ | `git status --porcelain` empty before QA deposits. `DRAFTING_CYCLE.md`, `PLANNER_TEMPLATE.md`, `PANEL_SEAT_TEMPLATE.md`, `LESSONS.md`, `knowledge/FORWARD.md` absent from Step 1 commit (`b2fccd2`). |

## Evidence and Narrative

All seven verification rows pass. The routing transaction landed exactly as specified: 23 codify, 23 reject, 6 reference/reference, 3 reference/backlog, with held rows 378 and 389 untouched. Symmetric differences confirm the exact id sets, not just counts. The single transaction timestamp `2026-08-25T22:58:03Z` is consistent across all 55 rows with zero mismatches, confirming no second writer touched the batch during the verdict window.

The pre/post dump diff shows exactly 55 content-changed rows (378 appears in the diff block but its content is byte-identical). Zero foreign ids, zero target_artifact changes. Both committed dumps match working-tree copies byte-for-byte.

The full test suite (63 tests) passes with zero delta from the authoring-time baseline. The DB remains untracked per shop policy.

```
============================================================
Rule 20 — QA Self-Check Results
============================================================
PASSED — SELF-CHECK PASSED — all evidence files present, no hedging keywords found.
Evidence folder: /Users/marklehn/Developer/GitHub/lessons-forge/.bellows-worktrees/536/knowledge/qa/evidence/gate1-route-57/
Files verified: 3
```

## Output Receipt

| Field | Value |
|-------|-------|
| Plan | 536 |
| Slug | gate1-route-57 |
| Step | 2 (QA) |
| Evidence files | `pytest_full.txt`, `routing-verification.txt`, `diff-audit.txt` |
| Test summary | 63 passed in 0.15s |
| Verification rows | 7/7 pass |
| Rule 20 | PASSED |

## Ledger Updates

#### Prompt Feedback

None.
