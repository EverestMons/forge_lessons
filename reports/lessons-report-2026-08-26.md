# Lessons Report — 2026-08-26


## Summary


| Category | Count |
|---|---|
| governance_rule | 3 |

**Total proposals:** 3


## Governance Rule


### 2026-08-26: A shipped mechanism does not update the doctrine that described its predecessor — sweep for descriptions, not just contradictions [tag: doctrine-drift]


- **Suggested action:** Add rule: when a mechanism ships, sweep doctrine for sections describing the predecessor — superseding a proposal never updates the doctrine it targeted.
- **Reasoning:** [AUTHOR-CONFLICT] Entry describes: the R-F3 receipt-clearance-claim admission flow shipped and the old proposal was correctly superseded, but the doctrine text was never updated — supersession acts on the proposal record, not on the doctrine. Proposes a governance rule: when a mechanism ships, sweep doctrine for sections DESCRIBING the predecessor. Note: the fix-INSTANCE (PT deposit conventions to R-F3 flow) is already discharged by proposal 401's codification; the class half (the sweep rule itself) is what remains.
- **Confidence:** high

### 2026-08-26: Record errors observed mid-batch are ROUTED, never patched unrouted — six PT sites carried through the route-57 arc [tag: record-integrity]


- **Suggested action:** Add rule: record errors observed mid-batch are ROUTED via declaration and corpus entry, never patched unrouted — a plan patches only what its routed proposals authorize.
- **Reasoning:** [AUTHOR-CONFLICT] Entry describes: across the route-57 Gate-2 batches, six PT record errors were observed and carried by declaration instead of being silently fixed — the routing law. Proposes a governance rule: observed record errors in a doctrine batch get a DECLARATION and a corpus entry, never an unrouted in-passing patch. Note: the six-site work order is already DISCHARGED by plan 552; the class half (the routing law as a rule) is what remains.
- **Confidence:** high

### 2026-08-25: An instruction-source migration needs a SUPERSET PROOF — clauses drop silently when a ritual moves homes, and an equivalence directive has two directions  [tag: process-integrity]


- **Suggested action:** Add rule: instruction-source migrations require a clause-level SUPERSET proof — diff old vs new vs enforcement, and verify both directions of any equivalence directive.
- **Reasoning:** Entry describes: an instruction-source migration that silently dropped two clauses when a ritual moved homes, found only when a three-source diff was requested explicitly. Proposes a governance rule: migrations require a clause-level SUPERSET diff of old-vs-new, and where a checker enforces the instructions, diff all THREE sources (old instructions, new instructions, enforced check set).
- **Confidence:** high
